---
phase: 01-cleaning-workspace
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - panelcleaner/inpainting.py
  - panelcleaner/image_ops.py
  - panelcleaner/data/__init__.py
  - manga_ai_studio/adapters/torch_impl.py
  - manga_ai_studio/adapters/factory.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - tests/test_inpainting/__init__.py
  - tests/test_inpainting/test_lama_adapter.py
  - tests/test_inpainting/test_inpaint_gui.py
autonomous: true
requirements:
  - CLEAN-06
user_setup:
  - service: lama_inpainting_model
    why: "The LaMa inpainting model weights (big-lama.pt ~200MB) are required to run inpainting. The vendored model_downloader.fetch fetches them from HuggingFace/GitHub on first use."
    env_vars: []
    dashboard_config:
      - task: "On first run of Tools -> Inpaint (C), allow the LaMa model download (~200MB) to complete. If behind a proxy, set HF endpoint env vars per model_downloader.py."
        location: "Triggered automatically by panelcleaner.model_downloader on first inpaint run"

must_haves:
  truths:
  - "Tools -> Inpaint (C) runs LaMa inpainting asynchronously via the vendored Worker(QRunnable) and does NOT freeze the UI (RESEARCH Pitfall 3); the Inpaint action is disabled when no mask is present, no page is open, or another async op is running (UI-SPEC surface 7)"
  - "The inpaint task converts the canvas mask to a binary numpy (H,W) uint8 array via core/mask_editor.mask_to_numpy_binary (the .copy()-detached conversion from plan 04) and the canvas image to an RGB numpy array; it NEVER passes QImage/QPixmap into the worker thread (RESEARCH Pitfall 3)"
  - "TorchLamaModel.inpaint(image_rgb: np.ndarray, mask_binary: np.ndarray) -> np.ndarray wraps panelcleaner.inpainting.InpaintingModel: PIL.Image.fromarray -> SimpleLama(image, mask) -> np.asarray (PanelCleaner inpainting.py:30-37 pattern); output is cropped to the input size if SimpleLama resized it"
  - "TorchLamaModel.load validates model_path.is_file() and raises FileNotFoundError before constructing SimpleLama (inpainting.py:21-24 pattern; T-01-04 model-path validation); it sets os.environ['LAMA_MODEL'] = str(model_path) before SimpleLama() (inpainting.py:23)"
  - "The inpainted result REPLACES the image layer (UI-SPEC surface 7); the mask overlay is intentionally left visible so the user sees what was processed"
  - "Every QImage created from the inpainted numpy result is .copy()-detached before storage in mask_item/image_item or before crossing a signal/slot boundary (RESEARCH Pitfall 2, PATTERNS.md §Shared Pattern 5; MangaCleaner_GPU main_window.py:265-267 on_task_finished is the BUGGY reference — do NOT copy verbatim)"
  - "Progress feedback is non-modal: status-bar left reads 'Inpainting… {percent}%', a thin 3px determinate QProgressBar shows under the toolbar, the Inpaint button enters spinner/disabled state (UI-SPEC surfaces 7 + 9)"
  - "Model-load failure shows a QMessageBox::Critical 'Couldn't load the inpainting model.' and the persistent #7a1f1f error chip in the status bar (UI-SPEC §Copywriting, surface 9)"
  - "Preview toggle: a hold-to-preview toolbar button 'Preview (hold)' shows the original (pre-inpaint) image while held; View -> Show Original (P) is a sticky toggle; both restore the inpainted result on release/second-press (UI-SPEC surface 7)"
  - "The inpaint op pushes the inpainted image patch onto the IMAGE undo stack via history.push_image_action (consumed by plan 06); for Phase 1 the 'patch' is the full inpainted region bounding the mask (RESEARCH Open Question 3 — full snapshot for simplicity)"
  - "Inpaint is NOT destructive — no confirmation dialog (UI-SPEC §Copywriting: 'Inpaint is NOT destructive ... fully reversible via image undo Ctrl+Z')"
  - "backend selection: config key inpainting_backend resolves 'torch' -> TorchLamaModel via backend_factory (D-02); 'onnx' -> OnnxInpaintModel (raises NotImplementedError from plan 01)"
artifacts:
  - "panelcleaner/inpainting.py vendored MINIMALLY — the InpaintingModel class only (load + __call__ from PanelCleaner inpainting.py:16-40), with pcleaner. -> panelcleaner. import rewrites; the batch inpaint_page driver and its MaskData/PageData deps (masker.py, structures.py, output_structures.py) are DEFERRED to Phase 2"
  - "panelcleaner/image_ops.py vendored (GPL, transitive dep for mask<->RGBA conversions; PanelCleaner image_ops.py — note scipy + cv2 deps)"
  - "manga_ai_studio/adapters/torch_impl.py extended with class TorchLamaModel(InpaintModel) implementing load/inpaint/preprocess/postprocess/configure/get_info"
  - "manga_ai_studio/adapters/factory.py extended: backend_factory('inpainting', 'torch') -> TorchLamaModel (was NotImplementedError in plan 03)"
  - "manga_ai_studio/gui/canvas.py extended: set_image_from_numpy(rgb_array), get_image_numpy() -> np.ndarray, show_original(bool) for the preview toggle, store original-vs-inpainted image pair"
  - "manga_ai_studio/gui/main_window.py extended: Tools -> Inpaint (C) enabled + wired, _run_inpaint_task, _on_inpaint_finished (with .copy() discipline), _on_inpaint_error, _on_inpaint_progress, preview hold button + sticky P shortcut, image-patch push to history (plan 06 consumes)"
  - "tests/test_inpainting/test_lama_adapter.py asserting the adapter contract with a mock SimpleLama (no torch/model weights in CI)"
  - "tests/test_inpainting/test_inpaint_gui.py asserting worker wiring, .copy() discipline, preview toggle, disabled-when-no-mask"
key_links:
  - "MainWindow.inpaint -> Worker(_run_inpaint_task) -> canvas.get_image_numpy() + core.mask_editor.mask_to_numpy_binary(canvas.get_mask()) -> TorchLamaModel.inpaint(rgb, mask_binary) -> Worker.signals.result -> MainWindow._on_inpaint_finished -> canvas.set_image_from_numpy (with .copy())"
  - "adapters/factory.backend_factory('inpainting', config.inpainting_backend) -> TorchLamaModel (D-02)"
  - "TorchLamaModel.load -> model_path.is_file() check + os.environ['LAMA_MODEL'] + SimpleLama() (inpainting.py:21-24)"
  - "View -> Show Original (P) / Preview(hold) -> canvas.show_original(bool) -> swap image_item pixmap between original and inpainted"
prohibitions:
  - "Must NOT copy MangaCleaner_GPU main_window.py:on_task_finished verbatim (lines 234-258) — it has the latent QImage buffer bug (no .copy() on the numpy->QImage path at line 245). Reimplement the result-display path with MANDATORY .copy() (RESEARCH Pitfall 2, PATTERNS.md §Shared Pattern 5)."
  - "Must NOT import torch or SimpleLama directly in gui/main_window.py — all model access goes through the TorchLamaModel adapter (RESEARCH §Don't Hand-Roll; the plan-03 prohibition applies identically here)."
  - "Must NOT run SimpleLama inference on the GUI thread (RESEARCH Pitfall 3); the worker touches only numpy/PIL and emits signals."
  - "Must NOT vendor the full PanelCleaner batch pipeline (masker.py, structures.py, output_structures.py, inpaint_page) — that is Phase 2 batch scope (FLOW-03), not Phase 1 interactive inpainting (CLEAN-06). Vendor inpainting.py MINIMALLY (InpaintingModel class only)."
  - "Must NOT show a confirmation dialog before inpaint (UI-SPEC §Copywriting: inpaint is non-destructive, reversible via Ctrl+Z)."
---

<objective>
Deliver the LaMa inpainting vertical slice (CLEAN-06): a user can run LaMa inpainting on the
mask to remove text and restore the underlying artwork, with async progress feedback, result
display, and a before/after preview toggle.

Per D-01/D-02: implement the concrete `TorchLamaModel(InpaintModel)` adapter in
`adapters/torch_impl.py` wrapping the vendored `panelcleaner.inpainting.InpaintingModel`
(SimpleLama). Vendor PanelCleaner's `inpainting.py` MINIMALLY — only the `InpaintingModel`
class (load + `__call__` from inpainting.py:16-40) needed for interactive inpainting; the batch
`inpaint_page` driver and its MaskData/PageData dependencies belong to Phase 2 (FLOW-03), not
here. Also vendor `image_ops.py` (GPL, mask<->RGBA conversions; Pitfall 6 centralization).

The CRITICAL quality gate for this plan is QImage `.copy()` discipline at every numpy->Qt bridge
in the result-display path. MangaCleaner_GPU `main_window.py:on_task_finished` (lines 234-258) is
the BUGGY reference — it builds `QImage(rgba.data, ...)` without `.copy()` (line 245), leaving
the QImage pointing at a numpy buffer that GCs and causes intermittent segfaults (RESEARCH
Pitfall 2). Plan 05 MUST reimplement that path with `.copy()` before storage/signal passage.

Per UI-SPEC surface 7: inpaint is non-blocking (Worker), the result replaces the image layer
(mask left visible), and a hold-to-preview button + sticky `P` shortcut toggle original-vs-result.

Purpose: completes the cleaning loop (detect -> edit mask -> inpaint), the core cleaning parity.
Output: TorchLamaModel adapter, async inpaint worker wiring, result display with .copy()
discipline, preview toggle.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-cleaning-workspace/01-CONTEXT.md
@.planning/phases/01-cleaning-workspace/01-RESEARCH.md
@.planning/phases/01-cleaning-workspace/01-PATTERNS.md
@.planning/phases/01-cleaning-workspace/01-UI-SPEC.md
@.planning/phases/01-cleaning-workspace/01-VALIDATION.md
@.planning/phases/01-cleaning-workspace/01-SKELETON.md
@.planning/phases/01-cleaning-workspace/01-03-PLAN.md
@.planning/phases/01-cleaning-workspace/01-04-PLAN.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Vendor minimal inpainting.py + image_ops.py + implement TorchLamaModel(InpaintModel) + wire factory</name>
  <files>
    panelcleaner/inpainting.py,
    panelcleaner/image_ops.py,
    panelcleaner/data/__init__.py,
    manga_ai_studio/adapters/torch_impl.py,
    manga_ai_studio/adapters/factory.py,
    tests/test_inpainting/__init__.py,
    tests/test_inpainting/test_lama_adapter.py
  </files>
  <read_first>
    C:\Src\PanelCleaner\pcleaner\inpainting.py (FULL FILE — lines 1-13 imports, lines 16-40 InpaintingModel class: model_path validation at 21, os.environ['LAMA_MODEL'] at 23, SimpleLama() at 24, __call__ with size-reclamp at 30-37; lines 43+ inpaint_page is the BATCH driver — DO NOT vendor it, it is Phase 2 scope),
    C:\Src\PanelCleaner\pcleaner\image_ops.py (lines 1-12 imports — note scipy + cv2 + PIL deps; locate convert_mask_to_rgba and the RGBA<->mask helpers around lines 50-70 for the centralized mask format per Pitfall 6),
    C:\Src\PanelCleaner\pcleaner\model_downloader.py (already vendored in plan 01 — confirm get_inpainting_model_path(config) is available at line 143/149; INPAINTING_URL at line 21; the model file is big-lama.pt / anime-manga-big-lama.pt),
    .planning\phases\01-cleaning-workspace\01-PATTERNS.md (§adapters/base.py lines 66-101 InpaintingModel ABC; §adapters/torch_impl.py lines 105-156 — note TorchLamaModel is NOT yet implemented per plan 03's comment; §panelcleaner/inpainting.py lines 226-240 vendoring guidance),
    .planning\phases\01-cleaning-workspace\01-RESEARCH.md (§Pattern 1 lines 230-358 InpaintModel.inpaint(image, mask) contract; §Code Examples LaMa Inpainting lines 734-751; §Common Pitfalls P2 QImage lifetime 659-664, P5 model distribution 680-685, P7 LaMa tile boundary artifacts 694-699),
    manga_ai_studio/adapters/base.py (the plan-01 InpaintModel ABC to subclass),
    manga_ai_studio/adapters/torch_impl.py (the plan-03 file — add TorchLamaModel next to TorchCTDModel),
    manga_ai_studio/adapters/factory.py (the plan-03 file — change inpainting+torch from NotImplementedError to TorchLamaModel)
  </read_first>
  <behavior>
    - Test 1 (test_lama_load_missing_model_raises): TorchLamaModel().load(Path("nonexistent.pt")) raises FileNotFoundError (the path check runs BEFORE any torch/SimpleLama import — T-01-04)
    - Test 2 (test_lama_inpaint_wraps_simple_lama): with a fake SimpleLama injected (monkeypatch panelcleaner.inpainting.SimpleLama), TorchLamaModel.inpaint(rgb_4x4, mask_4x4_binary) calls the fake's __call__(PIL_image, PIL_mask) exactly once and returns a numpy array of the same HxWx3 shape as the input
    - Test 3 (test_lama_inpaint_crops_to_input_size): when the fake SimpleLama returns a PIL image larger than the input, TorchLamaModel.inpaint crops the result to the input's (height, width) (inpainting.py:34-37 pattern)
    - Test 4 (test_lama_inpaint_passes_binary_mask): the PIL mask handed to SimpleLama is a 1-channel image where painted regions are 1/255 and unpainted are 0 (the convert-to-binary contract from inpainting.py docstring line 27-28)
    - Test 5 (test_lama_load_sets_env_var): after a successful load (with the fake), os.environ['LAMA_MODEL'] equals str(model_path) (inpainting.py:23)
    - Test 6 (test_factory_inpainting_torch): backend_factory('inpainting', 'torch') returns a TorchLamaModel instance (was NotImplementedError in plan 03)
    - Test 7 (test_factory_inpainting_onnx_raises_on_load): backend_factory('inpainting', 'onnx') returns OnnxInpaintModel; .load raises NotImplementedError
  </behavior>
  <action>
    Vendor `panelcleaner/inpainting.py` MINIMALLY (D-12 GPL-compat, near-verbatim — but ONLY the interactive class). Create `panelcleaner/inpainting.py` containing:
    - The stdlib + third-party imports from PanelCleaner inpainting.py:1-6 (`import os`, `from pathlib import Path`, `from PIL import Image`, `from loguru import logger`, `from simple_lama_inpainting import SimpleLama`). Add `from typing import Tuple` if used.
    - `import panelcleaner.model_downloader as md` (rewrite of `import pcleaner.model_downloader as md` — model_downloader was vendored in plan 01). Do NOT import `panelcleaner.config`, `panelcleaner.image_ops`, `panelcleaner.structures`, or `panelcleaner.output_structures` — those are needed only by the batch `inpaint_page` driver which we are NOT vendoring.
    - The `class InpaintingModel` verbatim from PanelCleaner inpainting.py:16-40: `__init__(self, config)` resolving `self.model_path = md.get_inpainting_model_path(config)`, the `if not self.model_path.is_file(): raise FileNotFoundError(f"Model not found: {self.model_path}")` guard (line 21-22 — T-01-04), `os.environ["LAMA_MODEL"] = str(self.model_path)` (line 23), `self.simple_lama = SimpleLama()` (line 24). The `__call__(self, image: Image, mask: Image) -> Image` method verbatim (lines 30-37) including the size-reclamp `if inpainted_image.size != image.size: ... inpainted_image.crop(...)`.
    - Add a module-level comment: `# The batch inpaint_page driver and its MaskData/PageData dependencies (masker.py, structures.py, output_structures.py) are deferred to Phase 2 (FLOW-03). Phase 1 interactive inpainting uses only InpaintingModel above.` Do NOT copy the `inpaint_page` function.
    - Preserve the upstream docstrings and GPL header notice.

    Vendor `panelcleaner/image_ops.py` (D-12 GPL-compat, near-verbatim). Copy the full file from `C:\Src\PanelCleaner\pcleaner\image_ops.py` and rewrite `pcleaner.` -> `panelcleaner.` / `from pcleaner` -> `from panelcleaner` everywhere imports appear. This pulls `cv2`, `numpy`, `scipy`, and PIL submodules — `scipy` is already declared in `pyproject.toml [project.dependencies]` by plan 01-01 (single source of truth); do NOT re-declare it and do NOT touch pyproject.toml in this plan. Do NOT refactor the mask<->RGBA conversion helpers (Pitfall 6 — they are the canonical mask format path). image_ops.py:15 imports `panelcleaner.data` (the `import pcleaner.data` line, rewritten) — create `panelcleaner/data/__init__.py` as an empty placeholder to satisfy that import (minimal, deterministic choice over stripping the data import which would diverge from the vendored source). The placeholder exists solely so the import resolves; image_ops.py does not access any resource attributes at module-load time that would break with an empty package.

    Extend `manga_ai_studio/adapters/torch_impl.py` (the plan-03 file). Add imports: `from PIL import Image` and `from panelcleaner.inpainting import InpaintingModel` and `from manga_ai_studio.adapters.base import InpaintModel` (InpaintModel ABC from plan 01; TorchCTDModel already imports DetectionModel). Define `class TorchLamaModel(InpaintModel)` (D-01/D-02; the plan-03 module comment `# TorchLamaModel(InpaintModel) is implemented in plan 05` is replaced by this class):
    - `__init__(self, config=None)`: store config; `self.model = None`; `self.model_path = None`.
    - `load(self, model_path: Path, device: str = "cpu") -> None`: validate `model_path.is_file()` and raise `FileNotFoundError(f"Model not found: {model_path}")` BEFORE any torch/SimpleLama import (T-01-04; the test test_lama_load_missing_model_raises guards this). Set `os.environ["LAMA_MODEL"] = str(model_path)` (inpainting.py:23). Import SimpleLama lazily inside load: `from simple_lama_inpainting import SimpleLama`; construct `self.model = SimpleLama()` (LaMa is CPU/GPU auto-selected by simple_lama_inpainting; the `device` arg is accepted for ABC compatibility but simple_lama_inpainting manages its own device). Store `self.model_path = model_path`. NOTE: the load takes a `model_path` directly (not a full `config`) so the adapter stays testable with a fake path; the GUI layer resolves the path via `md.get_inpainting_model_path(config)` before calling load (mirroring InpaintingModel.__init__ but split for testability).
    - `inpaint(self, image: np.ndarray, mask: np.ndarray) -> np.ndarray`: assert `self.model is not None` (raise RuntimeError("Model not loaded") if None). Validate shapes: `image` is `(H,W,3)` uint8 RGB; `mask` is `(H,W)` uint8 binary (255=paint, 0=keep) — convert via `mask_binary = (mask > 0).astype(np.uint8) * 255` defensively. Convert: `pil_image = Image.fromarray(image, mode="RGB")`; `pil_mask = Image.fromarray(mask_binary, mode="L")`. Call `result_pil = self.model(pil_image, pil_mask)` (InpaintingModel.__call__ — inpainting.py:30). Apply the size reclamp (InpaintingModel.__call__ already does this, but the adapter asserts it too): `if result_pil.size != pil_image.size: result_pil = result_pil.crop((0, 0, pil_image.size[0], pil_image.size[1]))`. Return `np.asarray(result_pil)` as `(H,W,3)` uint8 RGB.
    - `preprocess(self, image) -> np.ndarray`: return `image` (SimpleLama handles preprocessing; RESEARCH Pattern 1).
    - `postprocess(self, model_output) -> np.ndarray`: return `model_output` (no postprocessing — the PIL->numpy conversion in inpaint() is the postprocess).
    - `configure(self, **kwargs) -> None`: store kwargs (tile_size, etc. — RESEARCH Pitfall 7 LaMa tile boundary; Phase 1 uses SimpleLama's default tiling).
    - `get_info(self) -> dict`: return `{"backend": "torch", "model": "big-lama", "model_path": str(self.model_path)}`.

    Extend `manga_ai_studio/adapters/factory.py` (the plan-03 file). Change the `inpainting+torch` dispatch from `raise NotImplementedError("Inpainting torch adapter lands in plan 05")` to `return TorchLamaModel()` (import lazily inside the function body to avoid importing SimpleLama when the factory is imported). Keep `inpainting+onnx -> OnnxInpaintModel()` (the plan-01 stub).

    Create `tests/test_inpainting/__init__.py` (empty) and `tests/test_inpainting/test_lama_adapter.py`. Use pytest + unittest.mock (NO torch, NO SimpleLama, NO model weights). Tests:
    - `test_lama_load_missing_model_raises`: `TorchLamaModel().load(Path("nonexistent.pt"))` raises FileNotFoundError; assert it raises BEFORE any `import simple_lama_inpainting` (use `monkeypatch.setitem(sys.modules, "simple_lama_inpainting", ...)` that raises if imported, to prove the path check short-circuits).
    - `test_lama_inpaint_wraps_simple_lama`: build a `FakeSimpleLama` whose `__call__(image, mask)` records args and returns a PIL image of the same size; `monkeypatch.setattr("panelcleaner.inpainting.SimpleLama", FakeSimpleLama)` OR inject `instance.model = FakeSimpleLama()` directly after constructing TorchLamaModel (the latter is cleaner — skip load). Build a 4x4x3 uint8 RGB array and a 4x4 binary mask; call `instance.inpaint(rgb, mask)`; assert the returned array is `(4,4,3)` uint8 and the FakeSimpleLama was called once with a PIL image and a PIL mask.
    - `test_lama_inpaint_crops_to_input_size`: FakeSimpleLama returns a 8x8 PIL image; inpaint on a 4x4 input; assert the returned array is `(4,4,3)`.
    - `test_lama_inpaint_passes_binary_mask`: FakeSimpleLama records the mask's mode and extrema; assert the mask handed in is mode "L" with extrema (0, 255).
    - `test_lama_load_sets_env_var`: monkeypatch SimpleLama; call load on a real temp file path (create an empty temp file); assert `os.environ["LAMA_MODEL"] == str(path)`; clean up the env var after.
    - `test_factory_inpainting_torch`: `backend_factory("inpainting", "torch")` returns a `TorchLamaModel`.
    - `test_factory_inpainting_onnx_raises_on_load`: `backend_factory("inpainting", "onnx")` returns `OnnxInpaintModel`; `.load(Path("x"))` raises NotImplementedError.
    Mark all tests `@pytest.mark.unit`.
  </action>
  <verify>
    <automated>python -c "from panelcleaner.inpainting import InpaintingModel; from manga_ai_studio.adapters.torch_impl import TorchLamaModel; from manga_ai_studio.adapters.factory import backend_factory; print('imports ok')" && pytest tests/test_inpainting/test_lama_adapter.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `panelcleaner/inpainting.py` contains `class InpaintingModel`, `if not self.model_path.is_file(): raise FileNotFoundError`, `os.environ["LAMA_MODEL"]`, `SimpleLama()`, and the `__call__` with the size-reclamp `crop`. It does NOT contain `def inpaint_page` (batch deferred to Phase 2). It contains zero `from pcleaner` / `import pcleaner` (all rewritten to panelcleaner).
    - Source: `panelcleaner/image_ops.py` exists with `pcleaner.` -> `panelcleaner.` rewrites applied, and `panelcleaner/data/__init__.py` exists as an empty placeholder satisfying the `import panelcleaner.data` at image_ops.py:15
    - Source: `manga_ai_studio/adapters/torch_impl.py` contains `class TorchLamaModel(InpaintModel)`, `from panelcleaner.inpainting import InpaintingModel`, `from PIL import Image`, `Image.fromarray(image, mode="RGB")`, `Image.fromarray(mask_binary, mode="L")`, `self.model(pil_image, pil_mask)`, `np.asarray(result_pil)`, and `os.environ["LAMA_MODEL"]`
    - Source: `manga_ai_studio/adapters/factory.py` contains `TorchLamaModel()` in the inpainting+torch branch (no longer `raise NotImplementedError` for that branch); grep for `NotImplementedError("Inpainting torch` returns nothing
    - Behavior: test_lama_load_missing_model_raises passes (FileNotFoundError before torch import — T-01-04)
    - Behavior: test_lama_inpaint_wraps_simple_lama passes
    - Behavior: test_lama_inpaint_crops_to_input_size passes (size reclamp)
    - Behavior: test_lama_inpaint_passes_binary_mask passes (mode "L", extrema 0/255)
    - Behavior: test_lama_load_sets_env_var passes
    - Behavior: test_factory_inpainting_torch passes
    - Behavior: test_factory_inpainting_onnx_raises_on_load passes
    - CLI: `pytest tests/test_inpainting/test_lama_adapter.py -x` exits 0
    - D-12 compliance: panelcleaner/inpainting.py and panelcleaner/image_ops.py are vendored (GPL v3 derivative — license-compatible); TorchLamaModel in adapters/ is our own code wrapping the vendored class
  </acceptance_criteria>
  <done>
    panelcleaner/inpainting.py vendored minimally (InpaintingModel class only; batch deferred); image_ops.py vendored; TorchLamaModel(InpaintModel) wraps SimpleLama with the size-reclamp and binary-mask contract; factory resolves inpainting/torch and inpainting/onnx; all lama adapter tests green with mock SimpleLama (no torch in CI).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Async inpaint worker wiring + result display with .copy() discipline + preview toggle (hold + sticky P)</name>
  <files>
    manga_ai_studio/gui/main_window.py,
    manga_ai_studio/gui/canvas.py,
    tests/test_inpainting/test_inpaint_gui.py
  </files>
  <read_first>
    C:\Users\Stella\Downloads\MangaCleaner_GPU\_internal\src\frontend\main_window.py (lines 234-258 on_task_finished — the BUGGY reference: line 245 builds QImage(rgba.data, ...) WITHOUT .copy(); lines 263-283 on_lama_clean + run_thread mask extraction at 265-267 — REFERENCE ONLY per D-12, reimplement with .copy() discipline; do NOT copy verbatim),
    C:\Users\Stella\Downloads\MangaCleaner_GPU\_internal\src\frontend\main_window.py (lines 204-228 on_undo_image/on_redo_image/on_undo_mask/on_redo_mask — the history push at on_task_finished line 236; REFERENCE ONLY, plan 06 implements history),
    C:\Src\PanelCleaner\pcleaner\gui\worker_thread.py (already vendored into manga_ai_studio/gui/worker_thread.py by plan 03 — Worker(QRunnable), WorkerSignals, progress_callback/abort_flag auto-injection),
    .planning\phases\01-cleaning-workspace\01-PATTERNS.md (§gui/main_window.py worker wiring lines 334-359, §Shared Pattern 2 worker dispatch 636-648, §Shared Pattern 5 buffer lifetime 673-685 — the MANDATORY .copy() rule),
    .planning\phases\01-cleaning-workspace\01-UI-SPEC.md (§Surface 7 LaMa inpainting lines 241-249, §Surface 9 Feedback states lines 268-275, §Copywriting lines 122-138 — status 'Inpainting… {percent}%', model-load error, inpaint is non-destructive; §Keyboard P sticky preview line 296),
    .planning\phases\01-cleaning-workspace\01-RESEARCH.md (§Common Pitfalls P2 QImage lifetime 659-664, P3 touching Qt from worker 666-671, P7 LaMa tile artifacts 694-699),
    manga_ai_studio/gui/worker_thread.py (the plan-03 vendored Worker — reuse, do not modify),
    manga_ai_studio/gui/canvas.py (the plan-04 file — add set_image_from_numpy, get_image_numpy, show_original; get_mask and mask_to_numpy_binary already available via core/mask_editor),
    manga_ai_studio/gui/main_window.py (the plan-04 file — enable Inpaint C, add worker + handlers + preview toggle),
    manga_ai_studio/core/mask_editor.py (the plan-04 module — mask_to_numpy_binary is the .copy()-detached mask extraction)
  </read_first>
  <behavior>
    - Test 1 (test_inpaint_action_disabled_without_mask): when a page is open but no mask exists, the Inpaint action (Tools menu, shortcut C) is disabled; when a mask exists it is enabled; disabled while another async op is running
    - Test 2 (test_inpaint_worker_dispatches_adapter): MainWindow.inpaint builds a Worker wrapping _run_inpaint_task; the task calls TorchLamaModel.inpaint(rgb, mask_binary) and returns a dict {"image": rgb_result, "bbox": (x,y,w,h)}; no Qt object is touched inside the task (assert via monkeypatching the adapter)
    - Test 3 (test_inpaint_result_display_uses_copy): the inpainted numpy result is converted to a QImage via QImage(arr.data, ...).copy() BEFORE being stored in the canvas image_item; a regression test asserts the stored QImage is detached from the numpy buffer (mutating the numpy array after storage does NOT change the displayed image)
    - Test 4 (test_inpaint_replaces_image_layer): after _on_inpaint_finished, the canvas image_item pixmap is the inpainted result (different from the original); the mask overlay remains visible
    - Test 5 (test_inpaint_progress_status_bar): during the worker run, status_bar_left reads "Inpainting… {percent}%" and the 3px progress bar is visible; after finish it reads "Inpainting complete" (UI-SPEC §Copywriting)
    - Test 6 (test_inpaint_error_chip): on a worker error (simulated), MainWindow shows QMessageBox::Critical with "Couldn't load the inpainting model." and the #7a1f1f error chip in the status bar
    - Test 7 (test_preview_hold_button): MainWindow has a "Preview (hold)" tool button; pressing it calls canvas.show_original(True) (image swaps to original); releasing calls canvas.show_original(False) (swaps back to inpainted)
    - Test 8 (test_preview_sticky_p): the View -> Show Original (P) action toggles a persistent preview; first press -> show_original(True); second press -> show_original(False)
    - Test 9 (test_inpaint_no_confirmation_dialog): triggering Inpaint does NOT show a confirmation dialog (inpaint is non-destructive per UI-SPEC §Copywriting) — assert QMessageBox.question is not invoked
    - Test 10 (test_inpaint_pushes_image_history): after _on_inpaint_finished, the image undo stack receives an entry (history.push_image_action called with the inpainted bbox + patch) — uses a mock/stub history since plan 06 implements the real one
  </behavior>
  <action>
    Extend `manga_ai_studio/gui/canvas.py` (the plan-04 file). Add numpy bridge methods (these are the .copy()-discipline-critical paths):
    - `def get_image_numpy(self) -> np.ndarray | None`: if no image is loaded, return None. Read the current displayed image: grab `qpix = self.image_item.pixmap()`; if null return None; convert to QImage via `qpix.toImage().convertToFormat(QImage.Format_RGB888)`; extract `ptr = qimg.bits(); ptr.setsize(qimg.sizeInBytes()); arr = np.frombuffer(ptr, np.uint8).reshape(qimg.height(), qimg.width(), 3)`; return `arr.copy()` — the `.copy()` is MANDATORY (the array detaches from the QImage buffer before the local qimg GCs; RESEARCH Pitfall 2, PATTERNS.md §Shared Pattern 5). This is the image the worker hands to TorchLamaModel.inpaint.
    - `def set_image_from_numpy(self, rgb: np.ndarray, bbox: tuple[int,int,int,int] | None = None) -> QImage`: validate `rgb.ndim == 3 and rgb.shape[2] == 3 and rgb.dtype == np.uint8` (T-01-13 input validation — defensive shape check on worker-produced arrays). Store `self._original_image_numpy = self.get_image_numpy()` BEFORE overwriting (for the preview toggle) if not already stored. If bbox is provided (the inpainted region), composite only the bbox region into the existing image numpy (so non-masked pixels are untouched — avoids full-image replacement artifacts); else replace the whole image. Convert to QImage: `h, w = rgb.shape[:2]; qimg = QImage(rgb.data, w, h, w*3, QImage.Format_RGB888)`; **`qimg = qimg.copy()`** — the `.copy()` is the CRITICAL security/quality gate (MangaCleaner_GPU main_window.py:245 omits this and causes intermittent segfaults; RESEARCH Pitfall 2). Set `self.image_item.setPixmap(QPixmap.fromImage(qimg))`; update sceneRect if size changed. Store `self._inpainted_qimage = qimg` for the preview toggle. Return qimg.
    - `def show_original(self, show: bool) -> None`: if `show` and `self._original_image_numpy is not None`: convert the original numpy to a QPixmap (with the same .copy() discipline) and swap `self.image_item.setPixmap(...)`. Else if not show and `self._inpainted_qimage is not None`: restore `self.image_item.setPixmap(QPixmap.fromImage(self._inpainted_qimage))`. Track `self._showing_original = show`.
    - Initialize `self._original_image_numpy = None`, `self._inpainted_qimage = None`, `self._showing_original = False` in `__init__`. Reset all three in `set_image`/`clear` (a new page clears the inpaint history).
    - Add `def has_inpaint_result(self) -> bool`: return `self._inpainted_qimage is not None` (drives whether the preview toggle is enabled).

    Extend `manga_ai_studio/gui/main_window.py` (the plan-04 file). Enable the plan-02 Tools -> Inpaint `C` action (`action_inpaint`): set enabled iff a page is open AND `self.canvas.has_mask()` AND not `self._op_running`. Re-evaluate this in `on_page_selected`, `canvas.mask_modified`, and the op-start/finish handlers. Connect `action_inpaint.triggered` to `def inpaint(self) -> None`. In `inpaint`:
    - Do NOT show a confirmation dialog (UI-SPEC §Copywriting: inpaint is non-destructive). (test_inpaint_no_confirmation_dialog.)
    - Build `model = backend_factory("inpainting", self.profile.config... inpainting_backend or "torch")`; resolve the model path via `md.get_inpainting_model_path(config)` (the vendored model_downloader from plan 01); load the model on the worker thread (not the GUI thread — model load can take seconds).
    - Build `worker = Worker(self._run_inpaint_task, image_rgb, mask_binary, model_path)` where `_run_inpaint_task(image_rgb, mask_binary, model_path, progress_callback=None, abort_flag=None)` (the plan-03 Worker auto-injection contract): `model.load(model_path)`; `result_rgb = model.inpaint(image_rgb, mask_binary)`; compute the mask bbox `bbox = compute_mask_bbox(mask_binary)` (the (x,y,w,h) of the painted region — `np.where(mask_binary>0)` then min/max); return `{"image": result_rgb, "bbox": bbox}`. The task touches ONLY numpy/PIL and the model — NO Qt objects (RESEARCH Pitfall 3; test_inpaint_worker_dispatches_adapter).
    - Extract the mask BEFORE starting the worker, on the GUI thread: `mask_binary = mask_to_numpy_binary(self.canvas.get_mask())` (the plan-04 .copy()-detached conversion). Extract the image: `image_rgb = self.canvas.get_image_numpy()`. Both are plain numpy arrays safe to pass to the worker.
    - Connect `worker.signals.progress -> self._on_inpaint_progress(percent, message)`: update status_bar_left to `f"Inpainting… {percent}%"` (UI-SPEC §Copywriting), show + update the 3px progress bar (`self.progress_bar.setValue(percent)`), keep the Inpaint button in spinner/disabled state.
    - Connect `worker.signals.result -> self._on_inpaint_finished(result)`.
    - Connect `worker.signals.error -> self._on_inpaint_error(worker_error)`.
    - Connect `worker.signals.finished -> self._on_inpaint_cleanup`.
    - Set `self._op_running = True`; disable Detect + Inpaint actions; `QThreadPool.globalInstance().start(worker)`. Phase 1 uses the D-09b in-process fallback (QThreadPool); the D-07/D-08 subprocess split is deferred until a dependency conflict forces it (same rationale as plan 03's in-process detection worker).

    Implement `def _on_inpaint_finished(self, result)`: take `result["image"]` (numpy HxWx3 RGB) and `result["bbox"]`. Call `self.canvas.set_image_from_numpy(result["image"], bbox=result["bbox"])` — this method enforces the .copy() discipline internally. Push the inpainted patch onto the image undo stack: `if self.history is not None: self.history.push_image_action(bbox[0], bbox[1], original_patch_numpy)` where `original_patch_numpy` is the pre-inpaint region captured before set_image_from_numpy overwrote it (grab it from `self.canvas._original_image_numpy` sliced to the bbox, or capture it in _run_inpaint_task and return it in the result dict). Plan 06 defines the real HistoryManager; for now call the method if `self.history` exists (graceful no-op if plan 06 hasn't run yet — but plan 06 runs after this, so wire the call site and let plan 06 instantiate `self.history`). Update status_bar_left to "Inpainting complete"; reset `_op_running`; re-enable Detect/Inpaint (Inpaint enabled iff has_mask); hide the progress bar; enable the preview toggle (since `canvas.has_inpaint_result()` is now True).

    Implement `def _on_inpaint_error(self, worker_error)`: show `QMessageBox.critical(self, "Couldn't load the inpainting model.", "Check that the model files exist in the `models/` folder and see the log for details.")` (UI-SPEC §Copywriting — note the `{detection | inpainting}` variant); show the persistent #7a1f1f error chip in the status bar (the same chip pattern from plan 03's _on_detection_error — reuse or factor a shared `_show_error_chip(message)` helper); log via loguru; reset `_op_running`; re-enable actions.

    Add the preview toggle UI (UI-SPEC surface 7):
    - A `QToolButton` `self.btn_preview_hold` with text "Preview (hold)" in the toolbar (icon-only or text — UI-SPEC doesn't mandate; use text for discoverability in Phase 1). Connect `btn_preview_hold.pressed -> lambda: self.canvas.show_original(True)` and `btn_preview_hold.released -> lambda: self.canvas.show_original(False)`. Disabled until `canvas.has_inpaint_result()`.
    - The View -> Show Original `P` action (`action_show_original`, created as a stub in plan 02): make it checkable; connect `action_show_original.toggled -> self.canvas.show_original`. (Sticky toggle — UI-SPEC surface 7.)
    - Keep the hold-button and sticky-P in sync: if sticky-P is on, the hold button is a no-op (or vice versa). For Phase 1 simplicity, the hold button wins while pressed regardless of sticky state, and on release the state returns to whatever sticky-P dictates.

    Extend `tests/test_inpainting/test_inpaint_gui.py`. Use pytest-qt + unittest.mock. Build a small EditorCanvas, set an image, set a mask (a small red disc via core/mask_editor.paint_mask_stroke), then drive MainWindow.inpaint with a mocked TorchLamaModel (monkeypatch backend_factory to return a fake whose inpaint returns a known RGB array). Tests:
    - test_inpaint_action_disabled_without_mask: no mask -> action_inpaint.isEnabled() is False; paint a mask -> True.
    - test_inpaint_worker_dispatches_adapter: monkeypatch the adapter; trigger inpaint; assert the fake inpaint was called with numpy arrays (not QImage) and the worker emitted result.
    - test_inpaint_result_display_uses_copy: after _on_inpaint_finished, grab `canvas.image_item.pixmap().toImage()`; mutate the numpy array that was passed in (set all to 0); assert the displayed image is UNCHANGED (the .copy() detached it). This is the Pitfall-2 regression guard.
    - test_inpaint_replaces_image_layer: assert the displayed image differs from the original and `canvas.has_inpaint_result()` is True; assert `canvas.mask_item.isVisible()` is still True (mask left visible per UI-SPEC surface 7).
    - test_inpaint_progress_status_bar: simulate worker progress signal; assert status_bar_left contains "Inpainting…" and the progress bar is visible.
    - test_inpaint_error_chip: build a Worker that raises; connect error; assert the status-bar chip stylesheet contains "#7a1f1f" and the critical dialog copy contains "Couldn't load the inpainting model.".
    - test_preview_hold_button: assert btn_preview_hold exists; call canvas.show_original(True); assert `canvas._showing_original is True` and the image_item pixmap matches the original; call show_original(False); assert it matches the inpainted.
    - test_preview_sticky_p: assert action_show_original is checkable and toggling it calls show_original with the matching bool.
    - test_inpaint_no_confirmation_dialog: monkeypatch QMessageBox.question to record calls; trigger inpaint; assert it was not called.
    - test_inpaint_pushes_image_history: attach a stub history object with a recording push_image_action; trigger inpaint; assert push_image_action was called with (x, y, patch) where the patch is numpy.
    Mark GUI tests `@pytest.mark.usefixtures("qtbot")`.
  </action>
  <verify>
    <automated>pytest tests/test_inpainting/ -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `manga_ai_studio/gui/canvas.py` contains `def get_image_numpy`, `def set_image_from_numpy`, `def show_original`, `def has_inpaint_result`, `_original_image_numpy`, `_inpainted_qimage`, `_showing_original`, AND both `get_image_numpy` and `set_image_from_numpy` contain a `.copy()` on the numpy/QImage bridge (the Pitfall-2 gate — grep the file for `.copy()` returns at least 2 hits in these methods)
    - Source: `manga_ai_studio/gui/main_window.py` contains `def inpaint`, `def _run_inpaint_task`, `def _on_inpaint_finished`, `def _on_inpaint_error`, `def _on_inpaint_progress`, `Worker(self._run_inpaint_task`, `QThreadPool.globalInstance().start(worker)`, `mask_to_numpy_binary(self.canvas.get_mask())`, `set_image_from_numpy`, the literal "Inpainting… {percent}%" and "Couldn't load the inpainting model." copy, `btn_preview_hold`, `action_show_original`, and `show_original`
    - Source: main_window.py does NOT import `torch` or `SimpleLama` directly (grep returns nothing) — all model access via the adapter
    - Source: main_window.py inpaint() does NOT call QMessageBox.question (inpaint is non-destructive — grep for `QMessageBox.question` in the inpaint method returns nothing)
    - Behavior: test_inpaint_action_disabled_without_mask passes
    - Behavior: test_inpaint_worker_dispatches_adapter passes (no Qt in worker)
    - Behavior: test_inpaint_result_display_uses_copy passes (Pitfall-2 regression guard — mutating the source numpy does not change the displayed image)
    - Behavior: test_inpaint_replaces_image_layer passes (mask left visible)
    - Behavior: test_inpaint_progress_status_bar passes
    - Behavior: test_inpaint_error_chip passes (#7a1f1f + critical dialog)
    - Behavior: test_preview_hold_button passes
    - Behavior: test_preview_sticky_p passes
    - Behavior: test_inpaint_no_confirmation_dialog passes
    - Behavior: test_inpaint_pushes_image_history passes (history hook for plan 06)
    - CLI: `pytest tests/test_inpainting/ -x` exits 0
    - D-12 compliance: the result-display path is reimplemented with .copy() discipline; main_window.py docstring/action notes "reimplemented patterned after MangaCleaner_GPU main_window.py:on_task_finished (with the QImage buffer-lifetime fix)" and does NOT copy the buggy reference verbatim
  </acceptance_criteria>
  <done>
    Tools -> Inpaint (C) runs TorchLamaModel.inpaint via Worker without freezing UI; the result replaces the image layer with MANDATORY .copy() at the numpy->QImage bridge (Pitfall-2 regression guard green); the mask stays visible; progress feedback (status bar + 3px bar + button spinner) and the #7a1f1f error chip work; the hold-to-preview button + sticky P toggle original-vs-result; inpaint pushes the image patch onto the history stack for plan 06; no confirmation dialog (non-destructive).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models/ dir -> adapter (LaMa model load) | LaMa model weight file loaded by SimpleLama; tampering/corruption risk |
| backend worker thread -> GUI thread | numpy image/mask IN, numpy inpainted result OUT via Qt signal (must not touch Qt in worker) |
| worker-produced numpy -> QImage (display) | numpy buffer crosses into QImage; lifetime hazard if not .copy()-detached |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04b | Tampering (model weight tampering) | TorchLamaModel.load / SimpleLama | high | mitigate | TorchLamaModel.load validates `model_path.is_file()` and raises FileNotFoundError BEFORE importing simple_lama_inpainting (test_lama_load_missing_model_raises guards the short-circuit). The model path is resolved via the vendored `md.get_inpainting_model_path(config)` (known-good cache dir), not a user-typed string. model_downloader.fetch applies sha256 verification on download. (RESEARCH §Security Domain T-2; same disposition as plan 03's T-01-04.) |
| T-01-07b | Denial of Service (thread-safety crash) | Worker -> Qt mutation | high | mitigate | The inpaint worker touches ONLY numpy/PIL and the adapter; ALL Qt mutation happens in main-thread signal handlers (_on_inpaint_finished/_on_inpaint_error). The worker receives plain numpy arrays (extracted via canvas.get_image_numpy and mask_to_numpy_binary on the GUI thread before dispatch) and returns a plain numpy dict — never QImage/QPixmap. (RESEARCH Pitfall 3; same disposition as plan 03's T-01-07.) |
| T-01-11b | Tampering (QImage buffer lifetime — use-after-free) | canvas.set_image_from_numpy / get_image_numpy | high | mitigate | EVERY numpy<->QImage bridge in this plan enforces `.copy()`: get_image_numpy returns `arr.copy()` (detaches from QImage bits before the local qimg GCs); set_image_from_numpy calls `QImage(...).copy()` before storing in image_item (test_inpaint_result_display_uses_copy is the regression guard — mutating the source numpy after storage must NOT change the displayed image). This EXPLICITLY fixes the MangaCleaner_GPU main_window.py:245 bug (RESEARCH Pitfall 2, PATTERNS.md §Shared Pattern 5). |
| T-01-13 | Tampering (input validation — worker output shape) | canvas.set_image_from_numpy | medium | mitigate | set_image_from_numpy validates `rgb.ndim == 3 and rgb.shape[2] == 3 and rgb.dtype == np.uint8` before the QImage bridge; a malformed worker result (e.g., a 2-D array or wrong channel count) raises ValueError instead of constructing a corrupt QImage. (RESEARCH §Security Domain V5 input validation.) |
| T-01-14 | Denial of Service (large inpaint region) | TorchLamaModel.inpaint / SimpleLama | medium | mitigate | The mask is already bounded by the page size (<=10000x10000px, enforced in plan 02). SimpleLama tiles internally (RESEARCH Pitfall 7); Phase 1 uses the default tile size. A pathological full-page mask would be slow but not crash; the worker is abortable via the abort_flag (plan-03 Worker contract). No separate region-size cap in Phase 1; revisit if DoS is observed. |
</threat_model>

<verification>
- `pytest tests/test_inpainting/ -x` green
- The Pitfall-2 regression guard (test_inpaint_result_display_uses_copy) passes — the QImage .copy() discipline holds
- Running Inpaint (C) on a page with a detected/edited mask removes the text and restores artwork without freezing the UI (manual check per VALIDATION.md §Manual-Only — CLEAN-06)
- The mask overlay remains visible after inpaint; Edit -> Clear Mask or View -> Toggle Mask Overlay (M) hides it
- The Preview (hold) button and View -> Show Original (P) swap original vs result correctly (manual)
- Model-load failure path shows the #7a1f1f error chip + critical dialog (manual or via test)
</verification>

<success_criteria>
- panelcleaner/inpainting.py vendored minimally (InpaintingModel class only; batch deferred to Phase 2); image_ops.py vendored
- TorchLamaModel(InpaintModel) wraps SimpleLama with the size-reclamp + binary-mask contract; factory resolves inpainting/torch + inpainting/onnx
- Async inpaint via the plan-03 Worker; UI stays responsive; progress feedback + error chip
- Result replaces image layer; mask left visible; MANDATORY .copy() at every numpy->QImage bridge (Pitfall-2 regression guard green)
- Preview toggle (hold button + sticky P) swaps original vs inpainted
- Inpaint pushes the image patch onto the history stack (plan 06 hook); no confirmation dialog (non-destructive)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05 — LaMa Inpainting)

**Modules / classes:**
- `panelcleaner.inpainting.InpaintingModel` (vendored MINIMALLY, GPL v3): `__init__(config)` resolving model_path via `md.get_inpainting_model_path(config)`, FileNotFoundError guard, `os.environ["LAMA_MODEL"]`, `SimpleLama()`; `__call__(image: PIL.Image, mask: PIL.Image) -> PIL.Image` with size-reclamp crop. (Batch `inpaint_page` DEFERRED to Phase 2.)
- `panelcleaner.image_ops` (vendored, GPL v3): mask<->RGBA conversions, image utilities (scipy + cv2 + PIL deps)
- `manga_ai_studio.adapters.torch_impl.TorchLamaModel(InpaintModel)`: `load(model_path, device="cpu")` (FileNotFoundError before SimpleLama import, sets LAMA_MODEL env), `inpaint(image_rgb, mask_binary) -> np.ndarray` (PIL round-trip via SimpleLama, size reclamp), `preprocess`, `postprocess`, `configure`, `get_info`
- `manga_ai_studio.adapters.factory.backend_factory` (extended): `inpainting+torch -> TorchLamaModel`, `inpainting+onnx -> OnnxInpaintModel`
- `manga_ai_studio.gui.canvas.EditorCanvas` (extended): `get_image_numpy() -> np.ndarray` (with `.copy()`), `set_image_from_numpy(rgb, bbox=None) -> QImage` (with `.copy()`), `show_original(bool)`, `has_inpaint_result() -> bool`; state `_original_image_numpy`, `_inpainted_qimage`, `_showing_original`
- `manga_ai_studio.gui.main_window.MainWindow` (extended): `inpaint()`, `_run_inpaint_task(image_rgb, mask_binary, model_path, progress_callback, abort_flag)`, `_on_inpaint_finished(result)` (calls canvas.set_image_from_numpy + history.push_image_action), `_on_inpaint_error(worker_error)` (#7a1f1f chip + critical dialog), `_on_inpaint_progress(percent, message)`, `_on_inpaint_cleanup()`; Tools -> Inpaint (C) enabled iff page+mask+not-running; `btn_preview_hold` (Preview hold), `action_show_original` (sticky P)

**Config keys consumed:**
- `inpainting_backend` ("torch" default; "onnx" -> OnnxInpaintModel stub) — read via Profile/Config in MainWindow.inpaint
- LaMa model path via `md.get_inpainting_model_path(config)` (model cache dir / big-lama.pt)

**Test files:**
- `tests/test_inpainting/test_lama_adapter.py`: test_lama_load_missing_model_raises, test_lama_inpaint_wraps_simple_lama, test_lama_inpaint_crops_to_input_size, test_lama_inpaint_passes_binary_mask, test_lama_load_sets_env_var, test_factory_inpainting_torch, test_factory_inpainting_onnx_raises_on_load
- `tests/test_inpainting/test_inpaint_gui.py`: test_inpaint_action_disabled_without_mask, test_inpaint_worker_dispatches_adapter, test_inpaint_result_display_uses_copy (Pitfall-2 guard), test_inpaint_replaces_image_layer, test_inpaint_progress_status_bar, test_inpaint_error_chip, test_preview_hold_button, test_preview_sticky_p, test_inpaint_no_confirmation_dialog, test_inpaint_pushes_image_history
</artifacts_produced>

<output>
Create `.planning/phases/01-cleaning-workspace/01-05-SUMMARY.md` when done
</output>
