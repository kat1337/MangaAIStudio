---
phase: 01-cleaning-workspace
plan: 07
type: execute
wave: 7
depends_on:
  - 01-06
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_detection/test_ctd_adapter.py
  - tests/test_inpainting/test_lama_adapter.py
  - tests/test_inpainting/test_inpaint_gui.py
autonomous: true
gap_closure: true
requirements:
  - CLEAN-02
  - CLEAN-06
  - FLOW-02
user_setup: []

must_haves:
  truths:
  - "User can run heatmap text detection on a first-run machine with no cached CTD model — _resolve_detection_model_path (main_window.py) calls download_torch_model(cache_dir) with the real cache_dir from self.profile_manager.config.get_model_cache_dir(); no TypeError is swallowed; the returned path is under the cache dir and matches the vendored default filename comictextdetector.pt (CR-01 closed, re-verify truth #2)"
  - "User can run LaMa inpainting on a first-run machine with no cached LaMa model — _resolve_inpainting_model_path (main_window.py) passes self.profile_manager.config (the Config instance, which has get_model_cache_dir) to get_inpainting_model_path; no AttributeError is swallowed; the returned path is under the cache dir and matches the vendored default filename anime-manga-big-lama.pt (CR-02 closed, re-verify truth #4)"
  - "User can Ctrl+Z an inpaint and only the inpainted bbox region reverts — _on_inpaint_finished (main_window.py) slices the captured pre-inpaint numpy to the bbox region BEFORE pushing to history; the pushed patch shape equals (bbox_h, bbox_w, _); HistoryManager.pop_image_undo + canvas.apply_undo_image then write a correctly-shaped region (CR-03 closed, re-verify truth #6)"
  - "No bare 'except Exception:' remains in _resolve_detection_model_path, _resolve_inpainting_model_path, or the _on_inpaint_finished history-push site — except clauses are narrowed to the expected failure modes (FileNotFoundError, OSError) so programming errors (TypeError, AttributeError) propagate to dev/test instead of silently degrading in production"
  - "The widened regression tests exercise the REAL code paths (no stub of download_torch_model, get_inpainting_model_path, or HistoryManager on the buggy sites) so this class of arity/wrong-arg/shape bug cannot recur silently"
artifacts:
  - "manga_ai_studio/gui/main_window.py (3 sites fixed): _resolve_detection_model_path (line 1049) passes cache_dir to download_torch_model and narrows the except; _resolve_inpainting_model_path (line 1158) passes Config (not Profile) to get_inpainting_model_path and uses the correct default filename; _on_inpaint_finished (lines 1268-1282) slices the bbox region from the pre-inpaint image before pushing to history and narrows the except on the push"
  - "tests/test_detection/test_ctd_adapter.py extended with a regression test that calls the detection model resolver with a real Config instance and asserts (a) the call signature matches download_torch_model(cache_dir), (b) the returned path is under config.get_model_cache_dir(), (c) the filename matches the vendored default — WITHOUT stubbing download_torch_model"
  - "tests/test_inpainting/test_lama_adapter.py extended with a regression test that calls the inpainting model resolver with a real Config instance and asserts (a) no AttributeError is raised, (b) the returned path is under config.get_model_cache_dir(), (c) the filename is anime-manga-big-lama.pt — WITHOUT stubbing get_inpainting_model_path"
  - "tests/test_inpainting/test_inpaint_gui.py widened: test_inpaint_pushes_image_history now uses a REAL HistoryManager (not a _StubHistory) and asserts patch.shape[:2] == (bbox_h, bbox_w); a new test_inpaint_undo_patch_matches_bbox asserts that a 4x4 bbox on a 16x16 page yields a pushed patch of shape (4, 4, _)"
key_links:
  - "main_window._resolve_detection_model_path -> panelcleaner.model_downloader.download_torch_model(config.get_model_cache_dir()) -> Path under cache_dir / comictextdetector.pt"
  - "main_window._resolve_inpainting_model_path -> panelcleaner.model_downloader.get_inpainting_model_path(self.profile_manager.config) -> Path under cache_dir / anime-manga-big-lama.pt"
  - "main_window._on_inpaint_finished -> canvas.get_image_numpy() -> slice [y:y+h, x:x+w] -> history.push_image_action(x, y, bbox_patch) -> pop_image_undo -> apply_undo_image writes the correctly-shaped region"
  - "regression tests in tests/test_detection + tests/test_inpainting exercise the REAL vendored call signatures (no stubs) so arity/arg/shape bugs surface in CI"
prohibitions:
  - "Must NOT widen the except clauses back to 'except Exception:' — the whole point of closing these gaps is that programming errors (TypeError, AttributeError) must propagate. Narrow to (FileNotFoundError, OSError) for the resolver paths; for the history-push site, either let exceptions propagate entirely OR narrow to a specific set that does NOT include TypeError/AttributeError/ValueError."
  - "Must NOT stub download_torch_model or get_inpainting_model_path in the regression tests — the tests exist precisely to catch arity/arg bugs in those calls. A stubbed function cannot catch a wrong-signature call site."
  - "Must NOT stub HistoryManager in test_inpaint_pushes_image_history — the widening requirement is explicit: use a real HistoryManager instance. The previous _StubHistory only recorded (x, y, isinstance(patch, np.ndarray)) and could not catch the shape bug."
  - "Must NOT include CR-04 (eager torch import in torch_impl.py:41) in this plan — it is a WARNING-tier contract violation, explicitly DEFERRED to a future polish phase (see deferred section). The factory correctly lazy-imports torch_impl so the GUI runtime path is protected."
  - "Must NOT touch pyproject.toml, vendored panelcleaner/ files, or any file outside files_modified — these are surgical fixes to call sites in main_window.py plus test widenings."
---

<objective>
Close the three BLOCKER gaps from 01-VERIFICATION.md that block the Phase 1 cleaning-loop goal. All three were independently reproduced at runtime by the verifier (not just static analysis); all three pass the current 110/110 test suite only because every model-touching test stubs the affected code paths.

The three gaps share one root cause: **bare `except Exception:` swallowing real errors and returning plausible-looking defaults** (CR-01, CR-02) or pushing the wrong data shape (CR-03). Each fix is surgical (3-10 lines in main_window.py) and MUST ship with a widened regression test that exercises the REAL code path (no stub of the buggy site) so this class of bug cannot recur silently.

Purpose: deliver the documented Phase 1 goal — "User can clean manga pages interactively ... detect text masks ... run LaMa inpainting ... and undo/redo." Today, three of those four capabilities are broken at the runtime level on the documented first-run path. This plan unblocks them.

Output: 3 fixed call sites in main_window.py + 3 widened regression tests (one per gap). No new modules, no vendored-file changes, no pyproject.toml edits. CR-04 explicitly deferred (see deferred section).

This plan is `gap_closure: true` so `/gsd-execute-phase 1 --gaps-only` picks it up. It depends on 01-06 (the last original plan) being complete — which it is.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cleaning-workspace/01-CONTEXT.md
@.planning/phases/01-cleaning-workspace/01-RESEARCH.md
@.planning/phases/01-cleaning-workspace/01-PATTERNS.md
@.planning/phases/01-cleaning-workspace/01-VERIFICATION.md
@.planning/phases/01-cleaning-workspace/01-REVIEW.md
@.planning/phases/01-cleaning-workspace/01-05-PLAN.md
@.planning/phases/01-cleaning-workspace/01-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Gap 1 / CR-01 / CLEAN-02): Fix _resolve_detection_model_path to call download_torch_model(cache_dir) and narrow the bare except</name>
  <files>
    manga_ai_studio/gui/main_window.py,
    tests/test_detection/test_ctd_adapter.py
  </files>
  <read_first>
    .planning\phases\01-cleaning-workspace\01-VERIFICATION.md (gaps[0] — the full reproduction: TypeError: download_torch_model() missing 1 required positional argument: 'cache_dir'; swallowed by bare except at main_window.py:1050; returns fake Path("comictextdetector.pt") which does not exist),
    .planning\phases\01-cleaning-workspace\01-03-PLAN.md (the original detection plan — match its conventions and threat-model T-01-04 model-path validation),
    .planning\phases\01-cleaning-workspace\01-03-SUMMARY.md (what was actually built for detection — confirm _resolve_detection_model_path is the only call site for download_torch_model in the GUI),
    manga_ai_studio/gui/main_window.py (lines 1038-1053 — the buggy _resolve_detection_model_path method; line 1044 already reads self.profile_manager.config.current_profile for the configured path, so self.profile_manager.config is the Config instance — confirm this),
    panelcleaner/model_downloader.py (lines 16 TORCH_MODEL_NAME = "comictextdetector.pt"; lines 112-120 def download_torch_model(cache_dir: Path) -> Path | None — the REAL signature to call),
    panelcleaner/config.py (line 1174 def get_model_cache_dir(self) -> Path — lives on Config, NOT on Profile; line 1067 class Config; line 936 class Profile — confirm get_model_cache_dir is on Config only),
    manga_ai_studio/config/profile_manager.py (line 29 self.config = Config() — ProfileManager.config IS the Config; line 67 config.current_profile = profile — current_profile is the Profile),
    tests/test_detection/test_ctd_adapter.py (FULL FILE — match import style and @pytest.mark.unit conventions; the existing tests stub TextDetector but the new regression test MUST NOT stub download_torch_model),
    tests/conftest.py (the profile_manager fixture returns a real ProfileManager rooted in tmp_path — its .config is a real Config instance usable for the regression test)
  </read_first>
  <behavior>
    - Test 1 (test_resolve_detection_model_path_calls_download_with_cache_dir): construct a real ProfileManager (or use the conftest fixture), construct a MainWindow wired to it, call _resolve_detection_model_path(); assert the returned Path's PARENT equals config.get_model_cache_dir() (the resolver delegated to download_torch_model(cache_dir), which writes into cache_dir). Do NOT stub download_torch_model — the test exists to catch arity bugs. If the network is unavailable, download_torch_model returns None; in that case the resolver's narrowed except (FileNotFoundError/OSError) catches it and returns cache_dir / TORCH_MODEL_NAME — assert the returned path's parent is STILL config.get_model_cache_dir() and the filename is comictextdetector.pt (matching panelcleaner/model_downloader.py:16 TORCH_MODEL_NAME). Either branch proves the call signature is correct.
    - Test 2 (test_resolve_detection_model_path_filename_matches_vendored_default): assert the returned Path's .name == "comictextdetector.pt" (panelcleaner/model_downloader.py:16). This catches the original bug where a wrong default was hardcoded in a different filename.
    - Test 3 (test_resolve_detection_model_path_no_bare_except): grep the source of _resolve_detection_model_path and assert there is no 'except Exception' (the except must be narrowed). This is a source-level guard against regression of the anti-pattern.
    - Test 4 (test_resolve_detection_model_path_programming_errors_propagate): monkeypatch panelcleaner.model_downloader.download_torch_model to raise TypeError (simulating a future signature drift); assert _resolve_detection_model_path PROPAGATES the TypeError rather than swallowing it. This is the core anti-pattern guard — the narrowed except must NOT catch TypeError.
  </behavior>
  <action>
    Fix `manga_ai_studio/gui/main_window.py` in `_resolve_detection_model_path` (lines 1038-1053). The current body calls `download_torch_model()` with NO args at line 1049 and wraps the whole body in a bare `except Exception:` at line 1050 that returns `Path("comictextdetector.pt")`. Rewrite the method body to:
    - Resolve the Config instance: `config = self.profile_manager.config` (this is the Config — ProfileManager.config is set to `Config()` at profile_manager.py:29; `config.current_profile` is the Profile, which LACKS get_model_cache_dir — do NOT pass the Profile).
    - Keep the configured-path short-circuit: `profile = config.current_profile; configured = profile.text_detector.model_path; if configured: return Path(configured)` (unchanged).
    - Replace line 1049's `return Path(download_torch_model())` with the real cache_dir call: `cache_dir = config.get_model_cache_dir(); resolved = download_torch_model(cache_dir)`. The vendored function (model_downloader.py:112) returns `Path | None` — it writes the file into `cache_dir` and returns the path, or returns None if the download failed (network offline). Handle both: `if resolved is not None: return Path(resolved)`.
    - Replace the fallback `return Path("comictextdetector.pt")` (line 1053) with a fallback that still points into the cache dir with the vendored default filename: `return cache_dir / "comictextdetector.pt"` (matches panelcleaner/model_downloader.py:16 TORCH_MODEL_NAME). This way, even if the download fails, TorchCTDModel.load's FileNotFoundError surfaces a path the user can actually find, and a manual install to that path will work.
    - Narrow the bare `except Exception:` (line 1050) to `except (FileNotFoundError, OSError):` — these are the legitimate filesystem/network failure modes during a model download. Do NOT catch TypeError, AttributeError, ValueError, or KeyboardInterrupt — let those propagate so signature drift and programming errors surface in dev/test (test_resolve_detection_model_path_programming_errors_propagate guards this). Log the actual exception via `logger.warning(f"Detection model resolution failed: {exc}")` before the fallback so the failure is observable in logs (T-01-08) rather than silent.
    - Update the docstring to reflect the new behavior: "Delegates to download_torch_model(cache_dir) where cache_dir = config.get_model_cache_dir(). On filesystem/network failure (FileNotFoundError, OSError), returns cache_dir / comictextdetector.pt so TorchCTDModel.load surfaces a meaningful FileNotFoundError (T-01-04). Programming errors (TypeError, AttributeError) propagate."

    Extend `tests/test_detection/test_ctd_adapter.py`. Add a new test class or section "Gap-closure regression tests (CR-01)". Add the four behaviors above. Use the conftest `profile_manager` fixture (returns a real ProfileManager rooted in tmp_path; its `.config` is a real Config) and the existing MainWindow construction helper if one exists in test_ctd_adapter.py (look for `_make_window` or similar; if absent, factor one or construct a MainWindow directly with the profile_manager fixture). For test_resolve_detection_model_path_programming_errors_propagate: `monkeypatch.setattr("panelcleaner.model_downloader.download_torch_model", lambda cache_dir: (_ for _ in ()).throw(TypeError("simulated signature drift")))` — or define a small function that raises TypeError — then `with pytest.raises(TypeError): window._resolve_detection_model_path()`. Mark all four tests `@pytest.mark.unit`.
  </action>
  <verify>
    <automated>python -c "import inspect, panelcleaner.model_downloader as md; sig = inspect.signature(md.download_torch_model); print('sig:', sig)" && pytest tests/test_detection/test_ctd_adapter.py -x -k "resolve_detection or detection_model_path"</automated>
  </verify>
  <acceptance_criteria>
    - Source: main_window.py `_resolve_detection_model_path` contains `download_torch_model(cache_dir)` where `cache_dir` is derived from `config.get_model_cache_dir()` (grep `download_torch_model(` in the method returns the cache_dir-arg form; grep `get_model_cache_dir` in the method returns >= 1 hit)
    - Source: main_window.py `_resolve_detection_model_path` does NOT contain `download_torch_model()` followed immediately by a newline or `)` without an arg (grep `download_torch_model\(\)` returns 0 hits in the file)
    - Source: the except clause in `_resolve_detection_model_path` is narrowed — grep `except Exception` scoped to the method returns 0 hits; grep `except (FileNotFoundError, OSError)` returns >= 1 hit
    - Source: the fallback return is `cache_dir / "comictextdetector.pt"` (under the cache dir), NOT a bare `Path("comictextdetector.pt")`
    - Behavior: test_resolve_detection_model_path_calls_download_with_cache_dir passes — the returned path's parent is config.get_model_cache_dir() (either branch: download success or download-returns-None fallback)
    - Behavior: test_resolve_detection_model_path_filename_matches_vendored_default passes — returned .name == "comictextdetector.pt"
    - Behavior: test_resolve_detection_model_path_programming_errors_propagate passes — TypeError raised by a patched download_torch_model PROPAGATES out of the resolver (not swallowed)
    - CLI: `pytest tests/test_detection/test_ctd_adapter.py -x -k "resolve_detection or detection_model_path"` exits 0
    - CLI: `pytest tests/test_detection/test_ctd_adapter.py -x` exits 0 (no regression in existing detection tests)
    - No-stub guard: test_resolve_detection_model_path_calls_download_with_cache_dir does NOT call monkeypatch.setattr on `download_torch_model` (the REAL vendored function is exercised — only test_resolve_detection_model_path_programming_errors_propagate patches it, and only to assert propagation)
  </acceptance_criteria>
  <done>
    _resolve_detection_model_path calls download_torch_model(cache_dir) with the real cache_dir from Config.get_model_cache_dir(); the bare except is narrowed to (FileNotFoundError, OSError); the fallback returns a real path under the cache dir; programming errors propagate; four regression tests (including the propagation guard and the no-stub signature guard) green; existing detection tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Gap 2 / CR-02 / CLEAN-06): Fix _resolve_inpainting_model_path to pass Config (not Profile) and use the vendored default filename anime-manga-big-lama.pt</name>
  <files>
    manga_ai_studio/gui/main_window.py,
    tests/test_inpainting/test_lama_adapter.py
  </files>
  <read_first>
    .planning\phases\01-cleaning-workspace\01-VERIFICATION.md (gaps[1] — the full reproduction: AttributeError: 'Profile' object has no attribute 'get_model_cache_dir'; swallowed by bare except at main_window.py:1159; returns fake Path("big-lama.pt") which is ALSO the wrong filename — vendored default is anime-manga-big-lama.pt),
    .planning\phases\01-cleaning-workspace\01-05-PLAN.md (the original inpaint plan — match its conventions and threat-model T-01-04b),
    .planning\phases\01-cleaning-workspace\01-05-SUMMARY.md (what was actually built for inpaint — confirm _resolve_inpainting_model_path is the only call site for get_inpainting_model_path in the GUI),
    manga_ai_studio/gui/main_window.py (lines 1145-1160 — the buggy _resolve_inpainting_model_path method; line 1157 reads self.profile_manager.config.current_profile into a variable named `profile` and PASSES that Profile to get_inpainting_model_path — this is the bug; the Config is self.profile_manager.config),
    panelcleaner/model_downloader.py (lines 143-149 def get_inpainting_model_path(config) -> Path: return config.get_model_cache_dir() / "anime-manga-big-lama.pt" — confirms (a) it expects a Config, (b) the vendored default filename is anime-manga-big-lama.pt, NOT big-lama.pt; line 134-140 get_old_inpainting_model_path is the DEPRECATED big-lama.pt resolver — do NOT use it),
    panelcleaner/config.py (line 1174 def get_model_cache_dir — on Config only; line 936 class Profile — Profile does NOT have it; this is exactly what raises AttributeError),
    manga_ai_studio/config/profile_manager.py (line 29 self.config = Config() — the Config; line 67 config.current_profile = profile — the Profile),
    tests/test_inpainting/test_lama_adapter.py (FULL FILE — match import style and @pytest.mark.unit conventions; the existing tests stub SimpleLama but the new regression test MUST NOT stub get_inpainting_model_path),
    tests/conftest.py (the profile_manager fixture returns a real ProfileManager — its .config is a real Config usable for the regression test)
  </read_first>
  <behavior>
    - Test 1 (test_resolve_inpainting_model_path_returns_cache_dir_path): construct a real ProfileManager + MainWindow; call _resolve_inpainting_model_path(); assert the returned Path's parent equals config.get_model_cache_dir() and the filename is "anime-manga-big-lama.pt" (matching panelcleaner/model_downloader.py:149). No AttributeError raised — proving the Config (not Profile) is passed. Do NOT stub get_inpainting_model_path.
    - Test 2 (test_resolve_inpainting_model_path_filename_matches_vendored_default): assert returned .name == "anime-manga-big-lama.pt". This catches BOTH the original wrong-default bug (big-lama.pt) AND confirms the resolver delegates to the current vendored function (not the deprecated get_old_inpainting_model_path).
    - Test 3 (test_resolve_inpainting_model_path_no_bare_except): grep the source of _resolve_inpainting_model_path and assert there is no 'except Exception'.
    - Test 4 (test_resolve_inpainting_model_path_programming_errors_propagate): monkeypatch panelcleaner.model_downloader.get_inpainting_model_path to raise AttributeError (simulating a future signature change); assert _resolve_inpainting_model_path PROPAGATES the AttributeError. The narrowed except must NOT catch AttributeError.
  </behavior>
  <action>
    Fix `manga_ai_studio/gui/main_window.py` in `_resolve_inpainting_model_path` (lines 1145-1160). The current body at line 1157-1158 reads `profile = self.profile_manager.config.current_profile; return Path(get_inpainting_model_path(profile))` — passing a Profile where Config is required. Rewrite to:
    - Resolve the Config: `config = self.profile_manager.config` (the Config instance — ProfileManager.config at profile_manager.py:29; NOT current_profile, which is a Profile and lacks get_model_cache_dir).
    - Call the vendored resolver with the Config: `return Path(get_inpainting_model_path(config))`. The vendored function (model_downloader.py:143-149) returns `config.get_model_cache_dir() / "anime-manga-big-lama.pt"` — correct Config, correct default filename, under the cache dir.
    - Narrow the bare `except Exception:` (line 1159) to `except (FileNotFoundError, OSError):` — filesystem failure modes only. Do NOT catch AttributeError, TypeError, ValueError, or KeyboardInterrupt (test_resolve_inpainting_model_path_programming_errors_propagate guards this). Log via `logger.warning(f"Inpainting model resolution failed: {exc}")` before the fallback (T-01-08).
    - Replace the wrong fallback `return Path("big-lama.pt")` (line 1160) with the correct vendored default under the cache dir: `return config.get_model_cache_dir() / "anime-manga-big-lama.pt"` (matches panelcleaner/model_downloader.py:149). This fixes TWO bugs at once: the wrong filename AND the bare relative path (now under the cache dir so a manual install actually works).
    - Update the docstring to reflect the new behavior: "Delegates to get_inpainting_model_path(config) where config is the Config instance (Config has get_model_cache_dir; Profile does not — CR-02). Returns config.get_model_cache_dir() / anime-manga-big-lama.pt on filesystem failure. Programming errors propagate."

    Extend `tests/test_inpainting/test_lama_adapter.py`. Add a new test section "Gap-closure regression tests (CR-02)". Add the four behaviors above. Use the conftest `profile_manager` fixture and an existing MainWindow construction helper if present in test_inpaint_gui.py (look for `_make_window`; if it lives in test_inpaint_gui.py, either import it or replicate a minimal MainWindow construction here — the resolver only needs self.profile_manager wired, so a full canvas/page is not required). For test_resolve_inpainting_model_path_programming_errors_propagate: monkeypatch `panelcleaner.model_downloader.get_inpainting_model_path` to raise AttributeError; `with pytest.raises(AttributeError): window._resolve_inpainting_model_path()`. Mark all four tests `@pytest.mark.unit`.
  </action>
  <verify>
    <automated>python -c "import inspect, panelcleaner.model_downloader as md; print('sig:', inspect.signature(md.get_inpainting_model_path)); print('default:', md.get_inpainting_model_path.__doc__)" && pytest tests/test_inpainting/test_lama_adapter.py -x -k "resolve_inpainting or inpainting_model_path"</automated>
  </verify>
  <acceptance_criteria>
    - Source: main_window.py `_resolve_inpainting_model_path` calls `get_inpainting_model_path(config)` where `config = self.profile_manager.config` (grep `get_inpainting_model_path(` returns the config-arg form; grep `self.profile_manager.config.current_profile` scoped to this method returns 0 hits — the Profile is no longer passed)
    - Source: the except clause is narrowed — grep `except Exception` scoped to `_resolve_inpainting_model_path` returns 0 hits; grep `except (FileNotFoundError, OSError)` returns >= 1 hit
    - Source: the fallback return is `config.get_model_cache_dir() / "anime-manga-big-lama.pt"` (grep `"anime-manga-big-lama.pt"` returns >= 1 hit in the method; grep `"big-lama.pt"` returns 0 hits in the method — the wrong default is gone)
    - Behavior: test_resolve_inpainting_model_path_returns_cache_dir_path passes — returned path's parent == config.get_model_cache_dir(); filename == "anime-manga-big-lama.pt"; no AttributeError
    - Behavior: test_resolve_inpainting_model_path_filename_matches_vendored_default passes — .name == "anime-manga-big-lama.pt"
    - Behavior: test_resolve_inpainting_model_path_programming_errors_propagate passes — AttributeError PROPAGATES
    - CLI: `pytest tests/test_inpainting/test_lama_adapter.py -x -k "resolve_inpainting or inpainting_model_path"` exits 0
    - CLI: `pytest tests/test_inpainting/test_lama_adapter.py -x` exits 0 (no regression in existing lama tests)
    - No-stub guard: test_resolve_inpainting_model_path_returns_cache_dir_path does NOT monkeypatch get_inpainting_model_path (the REAL vendored function is exercised — only the propagation-guard test patches it)
  </acceptance_criteria>
  <done>
    _resolve_inpainting_model_path passes the Config (not Profile) to get_inpainting_model_path; the bare except is narrowed; the fallback uses the correct vendored default (anime-manga-big-lama.pt) under the cache dir; four regression tests green (including the no-stub signature guard and the propagation guard); existing lama tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Gap 3 / CR-03 / FLOW-02): Fix _on_inpaint_finished to push the bbox patch (not the full image) and widen test_inpaint_pushes_image_history to assert patch shape with a REAL HistoryManager</name>
  <files>
    manga_ai_studio/gui/main_window.py,
    tests/test_inpainting/test_inpaint_gui.py
  </files>
  <read_first>
    .planning\phases\01-cleaning-workspace\01-VERIFICATION.md (gaps[2] — the full reproduction: _on_inpaint_finished captures the FULL canvas image via self.canvas.get_image_numpy() and pushes (x1, y1, full_image); pop_image_undo reads patch.shape[:2] as the FULL image's (h, w) and slices current_img[y:y+h, x:x+w]; apply_undo_image then writes a mis-shaped region. Reproduced with 16x16 page + 4x4 bbox -> undo writes 12x12 instead of 4x4),
    .planning\phases\01-cleaning-workspace\01-06-PLAN.md (the original undo/redo plan — Task 1 defines the HistoryManager contract: push_image_action(x, y, patch) with patch.copy(); pop_image_undo reads patch.shape[:2] as the patch dims. The plan-06 contract REQUIRES the patch to be the bbox region, not the full image — this gap is a plan-05 implementation bug that plan-06's tests did not catch because test_inpaint_pushes_image_history stubbed history.),
    .planning\phases\01-cleaning-workspace\01-06-SUMMARY.md (confirm HistoryManager.push_image_action + pop_image_undo + canvas.apply_undo_image are the consumer chain; the patch shape is load-bearing),
    manga_ai_studio/gui/main_window.py (lines 1262-1285 — the buggy _on_inpaint_finished; line 1270 x1, y1 = int(bbox[0]), int(bbox[1]); line 1272 original_patch_numpy = self.canvas.get_image_numpy() — THIS captures the FULL image; bbox is (x, y, w, h) per compute_mask_bbox at line 1326-1347; the fix must slice [y:y+h, x:x+w] before pushing; line 1280 self.history.push_image_action(x1, y1, original_patch_numpy); line 1281 bare except Exception: pass — this swallows any history bug),
    manga_ai_studio/core/history_manager.py (lines 129-160 — pop_image_undo / pop_image_redo read `h, w = patch.shape[:2]` and slice current_img[y:y+h, x:x+w]; this is the consumer contract — the pushed patch MUST be exactly (bbox_h, bbox_w, _)),
    manga_ai_studio/gui/canvas.py (lines 422-463 — apply_undo_image reads `ph, pw = patch_np.shape[:2]` and writes current[y0:y1, x0:x1] = sub; with a full-image patch on a 16x16 page and bbox (4,4,4,4), it clips to a 12x12 region sourced from the WRONG part of the full image — silent corruption),
    tests/test_inpainting/test_inpaint_gui.py (lines 446-476 — the buggy test_inpaint_pushes_image_history; uses a _StubHistory class at lines 460-462 that only records (x, y, patch) and only asserts x == 4, y == 4, isinstance(patch, np.ndarray); this is WHY the bug was missed — the stub cannot assert patch shape; the widening MUST replace _StubHistory with a real HistoryManager from manga_ai_studio.core.history_manager),
    manga_ai_studio/core/history_manager.py (the real HistoryManager — HistoryManager(limit=20) with push_image_action(x, y, patch) that .copy()s the patch; usable directly in the test with NO stub)
  </read_first>
  <behavior>
    - Test 1 (test_inpaint_pushes_bbox_patch_not_full_image): WIDEN the existing test_inpaint_pushes_image_history. Replace the _StubHistory with a REAL HistoryManager instance (from manga_ai_studio.core.history_manager). After _on_inpaint_finished with a 16x16 page and bbox (4, 4, 4, 4), pop the undo entry via history.pop_image_undo(current_img); assert the popped patch.shape[:2] == (4, 4) — exactly the bbox height and width. This is the assertion that was MISSING and let the bug ship.
    - Test 2 (test_inpaint_undo_roundtrip_preserves_surrounding_region): end-to-end shape guard. Build a 16x16 page with distinct pixel values per region. Run _on_inpaint_finished with a result + bbox (4, 4, 4, 4). Then simulate Ctrl+Z: current = canvas.get_image_numpy(); x, y, patch = history.pop_image_undo(current); canvas.apply_undo_image(x, y, patch). Assert the region OUTSIDE the bbox (e.g., pixel (0,0) and pixel (15,15)) is UNCHANGED — proving the undo wrote ONLY the 4x4 bbox region, not a 12x12 or 16x16 region.
    - Test 3 (test_inpaint_finished_no_bare_except_on_history_push): grep the source of _on_inpaint_finished and assert there is no 'except Exception' wrapping the history.push_image_action call. If an except remains, it must be narrowed to a set that does NOT include TypeError/AttributeError/ValueError (so a future history API change surfaces).
    - Test 4 (test_inpaint_finished_push_uses_bbox_slice): assert (via a real HistoryManager recording the patch) that the pushed patch is a VIEW/COPY of the pre-inpaint image sliced to [bbox_y : bbox_y + bbox_h, bbox_x : bbox_x + bbox_w] — i.e., patch.shape == (bbox_h, bbox_w, 3) for an RGB image. Use a 16x16x3 page and bbox (4, 4, 4, 4) -> patch.shape == (4, 4, 3).
  </behavior>
  <action>
    Fix `manga_ai_studio/gui/main_window.py` in `_on_inpaint_finished` (lines 1262-1285). The current body at lines 1268-1274 captures the FULL image:
    ```
    original_patch_numpy = None
    if self.history is not None and bbox is not None:
        x1, y1 = int(bbox[0]), int(bbox[1])
        try:
            original_patch_numpy = self.canvas.get_image_numpy()
        except Exception:
            original_patch_numpy = None
    ```
    and at line 1280 pushes `original_patch_numpy` (the full image) with `self.history.push_image_action(x1, y1, original_patch_numpy)`. Rewrite the capture block to slice the bbox region from the pre-inpaint image BEFORE set_image_from_numpy overwrites it:
    - Unpack the bbox fully: `x1, y1, bw, bh = int(bbox[0]), int(bbox[1]), int(bbox[2]), int(bbox[3])` (compute_mask_bbox returns (x, y, w, h) per main_window.py:1347 — the current code only reads x1, y1 and ignores w, h; that is the root of the shape bug).
    - Capture the pre-inpaint image: `pre_inpaint = self.canvas.get_image_numpy()` (called BEFORE set_image_from_numpy at line 1276 — keep this ordering; after set_image_from_numpy the displayed image is the inpainted result and the pre-inpaint data is gone). Wrap ONLY the get_image_numpy call in a narrow `except (AttributeError, RuntimeError):` (canvas-not-ready failure modes); do NOT use bare except Exception.
    - Slice the bbox region: `original_patch_numpy = pre_inpaint[y1 : y1 + bh, x1 : x1 + bw].copy()` if pre_inpaint is not None else None. The `.copy()` is MANDATORY (pre_inpaint is a view into the canvas buffer; the slice must be detached before crossing into the HistoryManager — RESEARCH Pitfall 2, same discipline as every other numpy<->history bridge in plan 06). The slice shape is exactly (bh, bw, 3) for an RGB page — matching what pop_image_undo and apply_undo_image expect.
    - At line 1280, the push call `self.history.push_image_action(x1, y1, original_patch_numpy)` is now correct (the patch is bbox-shaped, not full-image-shaped).
    - Narrow the bare `except Exception: pass` at lines 1281-1282 wrapping the push. Remove it entirely if possible (history.push_image_action only fails on programming errors, which should propagate). If a defensive catch is desired for the history-full edge case, narrow to `except IndexError:` (which push_image_action does not raise — its overflow handling is `.pop(0)`, not an exception) — effectively, just remove the try/except. The VERIFICATION report flags this exact bare except as WR-05 ("Masks real history bugs") — removing it is the fix.
    - Add a one-line comment above the slice: `# CR-03 fix: push the bbox region (not the full image) so pop_image_undo + apply_undo_image write a correctly-shaped region on Ctrl+Z.` for traceability to the gap.
    - Update the docstring's "History hook" paragraph to reflect the slice: "pushes the bbox-shaped pre-inpaint patch (sliced from the pre-inpaint image before set_image_from_numpy overwrites it) so image-undo (Ctrl+Z) reverts just the inpainted region (CR-03)."

    Widen `tests/test_inpainting/test_inpaint_gui.py::test_inpaint_pushes_image_history` (lines 446-476). Replace the `_StubHistory` class with a REAL `HistoryManager` instance:
    - Import at top of file (if not already): `from manga_ai_studio.core.history_manager import HistoryManager`.
    - In the test body, replace `window.history = _StubHistory()` with `window.history = HistoryManager(limit=20)`.
    - After `window._on_inpaint_finished({"image": result_rgb, "bbox": bbox})`, pop the undo entry: `current = window.canvas.get_image_numpy(); popped = window.history.pop_image_undo(current); assert popped is not None; x, y, patch = popped`.
    - Replace the existing weak assertions (`assert x == 4 and y == 4; assert isinstance(patch, np.ndarray)`) with the SHAPE assertion that catches CR-03: `assert (x, y) == (4, 4); assert patch.shape[:2] == (4, 4), f"patch must be bbox-shaped (4,4,_), got {patch.shape}"`. The full-image bug would produce patch.shape[:2] == (16, 16); this assertion catches it.
    - Delete the `_StubHistory` class definition (it is no longer used) OR leave it if other tests reference it (grep first).

    Add `test_inpaint_undo_roundtrip_preserves_surrounding_region` to the same file. Build a 16x16 RGB page where each pixel is set to a distinct value (e.g., `page = np.arange(16*16*3, dtype=np.uint8).reshape(16, 16, 3)`). Set it on the canvas. Wire a real HistoryManager. Build a result_rgb of a different known pattern and bbox (4, 4, 4, 4). Call `_on_inpaint_finished`. Then simulate Ctrl+Z: `current = window.canvas.get_image_numpy(); result = window.history.pop_image_undo(current); x, y, patch = result; window.canvas.apply_undo_image(x, y, patch)`. Assert `np.array_equal(window.canvas.get_image_numpy()[0, 0], page[0, 0])` (top-left pixel outside the bbox is unchanged) and `np.array_equal(window.canvas.get_image_numpy()[15, 15], page[15, 15])` (bottom-right outside the bbox is unchanged). With the CR-03 bug, apply_undo_image would have overwritten up to a 12x12 region sourced from the wrong slice, corrupting these pixels.

    Add `test_inpaint_finished_push_uses_bbox_slice` — the explicit shape guard (Test 4 above). Same setup; assert the pushed patch (via a real HistoryManager pop) has shape exactly (bbox_h, bbox_w, 3) for the RGB page.

    Mark all three new/widened tests `@pytest.mark.gui` (they use qtbot) or `@pytest.mark.unit` where no Qt widget interaction is needed.
  </action>
  <verify>
    <automated>pytest tests/test_inpainting/test_inpaint_gui.py -x -k "inpaint_pushes_image_history or inpaint_undo_roundtrip or inpaint_finished_push"</automated>
  </verify>
  <acceptance_criteria>
    - Source: main_window.py `_on_inpaint_finished` unpacks bbox into (x1, y1, bw, bh) (grep `bw, bh` or `bbox[2], bbox[3]` returns >= 1 hit in the method); the capture block slices `pre_inpaint[y1 : y1 + bh, x1 : x1 + bw]` before pushing (grep `y1 + bh` and `x1 + bw` return >= 1 hit each)
    - Source: the slice is `.copy()`-detached (grep `.copy()` in the capture block returns >= 1 hit — Pitfall-2 discipline)
    - Source: the bare `except Exception: pass` wrapping push_image_action is GONE (grep `except Exception` scoped to _on_inpaint_finished returns 0 hits — the WR-05 anti-pattern is closed at this site)
    - Behavior: test_inpaint_pushes_image_history passes with the REAL HistoryManager and the new `patch.shape[:2] == (4, 4)` assertion (the full-image bug would fail this — patch.shape would be (16, 16))
    - Behavior: test_inpaint_undo_roundtrip_preserves_surrounding_region passes — pixels outside the bbox are unchanged after a simulated Ctrl+Z (the CR-03 bug would corrupt them)
    - Behavior: test_inpaint_finished_push_uses_bbox_slice passes — pushed patch shape == (bbox_h, bbox_w, 3)
    - CLI: `pytest tests/test_inpainting/test_inpaint_gui.py -x` exits 0 (all inpaint GUI tests green, including the existing 9 that must not regress)
    - No-stub guard: test_inpaint_pushes_image_history does NOT instantiate `_StubHistory` — it uses the real `HistoryManager` (grep `_StubHistory` in the test returns 0 hits OR the class is deleted)
    - Cross-check: `pytest tests/test_history.py -x` exits 0 (the plan-06 history tests still pass — the consumer contract is unchanged; only the producer call site is fixed)
  </acceptance_criteria>
  <done>
    _on_inpaint_finished unpacks bbox into (x, y, w, h), slices the pre-inpaint image to the bbox region, .copy()-detaches it, and pushes the bbox-shaped patch (not the full image) to history.push_image_action; the bare except on the push is removed (WR-05 closed at this site); test_inpaint_pushes_image_history uses a REAL HistoryManager and asserts patch.shape[:2] == (bbox_h, bbox_w); the round-trip test proves Ctrl+Z writes only the bbox region; all inpaint GUI tests + all history tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config -> model_downloader (resolver paths) | the resolver reads config.get_model_cache_dir() and passes it to the vendored download/path functions; a wrong arg type (Profile vs Config) or arity is a programming error that must NOT be swallowed |
| canvas pre-inpaint numpy -> HistoryManager (patch push) | the bbox-shaped patch crosses into the history stack; if the patch shape does not match the bbox, the undo consumer writes a mis-shaped region (silent data corruption) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Tampering (silent feature failure via swallowed exceptions) | main_window._resolve_detection_model_path, _resolve_inpainting_model_path, _on_inpaint_finished | high | mitigate | Narrow every bare `except Exception:` touched by this plan to the specific expected failure modes (FileNotFoundError, OSError for filesystem/network during model resolution; remove the bare except on the history push entirely). Programming errors (TypeError, AttributeError, ValueError) PROPAGATE so signature drift and wrong-arg bugs surface in dev/test. The propagation-guard regression tests (test_resolve_detection_model_path_programming_errors_propagate, test_resolve_inpainting_model_path_programming_errors_propagate) lock this. This is the shared root cause from 01-VERIFICATION.md §Gaps Summary. |
| T-01-18 | Tampering (image data corruption via mis-shaped undo patch) | main_window._on_inpaint_finished -> HistoryManager.push_image_action -> pop_image_undo -> apply_undo_image | high | mitigate | The pushed patch is sliced to exactly (bbox_h, bbox_w, _) from the pre-inpaint image, .copy()-detached (Pitfall-2). The consumer contract (pop_image_undo reads patch.shape[:2] as the dims; apply_undo_image writes current[y:y+h, x:x+w]) now receives correctly-shaped data. The widened test_inpaint_pushes_image_history (real HistoryManager, asserts patch.shape[:2] == (bbox_h, bbox_w)) and the new round-trip test (asserts surrounding region unchanged) lock this. |
| T-01-19 | Information Disclosure (model resolution failures hidden from logs) | main_window._resolve_detection_model_path, _resolve_inpainting_model_path | medium | mitigate | The narrowed except blocks log via logger.warning(f"... failed: {exc}") before the fallback (T-01-08), so a filesystem/network failure during model resolution is observable in logs rather than silent. The user-visible error path (TorchCTDModel.load / TorchLamaModel.load FileNotFoundError -> _on_detection_error / _on_inpaint_error -> QMessageBox + #7a1f1f chip) is unchanged and still fires. |

Note: these three gap-closure fixes do not introduce new trust boundaries — they close existing ones. The resolver paths already cross the Config -> vendored-model_downloader boundary; the patch push already crosses the canvas -> HistoryManager boundary. The fixes make the existing boundaries fail loudly on programming errors instead of silently degrading.
</threat_model>

<verification>
- `pytest tests/test_detection/test_ctd_adapter.py tests/test_inpainting/test_lama_adapter.py tests/test_inpainting/test_inpaint_gui.py -x` green (all gap-closure regression tests pass)
- `pytest -q` green with an INCREASED test count (was 110; +9 to +12 new regression tests across the three widened test files — exact count depends on the executor's factoring, but every widened test from the `<behavior>` blocks MUST exist and pass)
- `pytest tests/test_history.py -x` still green (the plan-06 HistoryManager consumer contract is unchanged; only the producer call site in main_window.py is fixed — no history regression)
- Source-level anti-pattern sweep: `grep -n "except Exception" manga_ai_studio/gui/main_window.py | grep -E "_resolve_detection_model_path|_resolve_inpainting_model_path|_on_inpaint_finished"` returns EMPTY (all three touched sites have narrowed-or-removed except clauses)
- Source-level arity sweep: `grep -n "download_torch_model()" manga_ai_studio/gui/main_window.py` returns EMPTY (no more zero-arg call); `grep -n "get_inpainting_model_path(profile)" manga_ai_studio/gui/main_window.py` returns EMPTY (no more Profile-arg call)
- Source-level default-filename sweep: `grep -n '"big-lama.pt"' manga_ai_studio/gui/main_window.py` returns EMPTY (wrong default gone); `grep -n '"anime-manga-big-lama.pt"' manga_ai_studio/gui/main_window.py` returns >= 1 hit (correct default present)
- Re-verification targets: the three FAILED truths in 01-VERIFICATION.md (truths #2, #4, #6) now have passing regression tests that exercise the REAL code paths; re-running gsd-verifier on this phase should flip those truths from FAILED to VERIFIED
</verification>

<success_criteria>
- CR-01 closed: _resolve_detection_model_path calls download_torch_model(cache_dir) with the real cache_dir; except narrowed; programming errors propagate; 4 regression tests green
- CR-02 closed: _resolve_inpainting_model_path passes Config (not Profile) to get_inpainting_model_path; correct vendored default filename (anime-manga-big-lama.pt); except narrowed; programming errors propagate; 4 regression tests green
- CR-03 closed: _on_inpaint_finished pushes the bbox-shaped patch (not the full image); bare except on the push removed (WR-05 closed at this site); test_inpaint_pushes_image_history uses a REAL HistoryManager and asserts patch.shape[:2] == (bbox_h, bbox_w); round-trip test proves Ctrl+Z writes only the bbox region
- Shared root cause addressed: all three touched sites have narrowed-or-removed except clauses; no bare `except Exception:` remains at any of the three gap sites
- Full test suite still green; existing 110 tests do not regress
- CR-04 explicitly DEFERRED (see deferred section) — not silently dropped, not in scope
</success_criteria>

<deferred>
## Deferred items (explicitly OUT OF SCOPE for this gap-closure plan)

- **CR-04 (WARNING tier, not a BLOCKER):** The eager `from panelcleaner.comic_text_detector.inference import TextDetector` at `manga_ai_studio/adapters/torch_impl.py:41` imports torch at module top-level, violating the module docstring's "torch is imported lazily inside load()" claim. Two test files (`tests/test_detection/test_ctd_adapter.py:22`, `tests/test_inpainting/test_lama_adapter.py:26`) import `torch_impl` at top-level, which forces torch into the test environment. This is a contract/docstring violation + test-isolation concern, NOT a goal-level gap: `adapters/factory.py` correctly lazy-imports `torch_impl` (factory.py:41, 58), so the GUI launch path does NOT pull torch until a model is actually constructed. The verifier downgraded CR-04 to WARNING for this reason. It is deferred to a future polish phase (candidate: a Phase 1.5 cleanup, or rolled into Phase 2 when the ONNX backend lands and the lazy-import contract becomes load-bearing for the second backend). Tracked here so it is not lost.

- **WR-01 (Warning):** `main_window.inpaint()` at line 1180 checks `self.canvas.has_mask()` instead of `has_mask_content()`. The action-state gate already uses `has_mask_content`, so the GUI path is safe; only a programmatic call could waste a model load on an empty mask. Not a goal-level gap; deferred.

- **WR-03 (Warning):** `abort_flag=None` accepted by both task functions (main_window.py:998, 1217) but never polled — worker abort is documented but non-functional. Acceptable for MVP per the original plan; deferred.

- **WR-04 (Info):** `history_manager.py:84, 127` use `list.pop(0)` (O(n)) for overflow trim. Negligible at limit=20; one-line `deque(maxlen=limit)` fix if the limit grows. Deferred.

- **WR-07 (Info):** `canvas.py:235, 313` `Format_ARGB32` byte-order assumption depends on host endianness. Works on all little-endian hosts (Windows/x86/Apple Silicon); would break on big-endian Qt builds. Deferred.

- **IN-03 (Info):** `tools_panel.py:211` `_on_tool_triggered` defined but never connected (dead code). Deferred.

- **IN-08 (Info):** `panelcleaner/inpainting.py:16` unused `from loguru import logger` import. Deferred.

The human-verification items in 01-VERIFICATION.md frontmatter (real CTD/Lama model downloads on a clean machine, real Ctrl+Z visual regression on a real manga page) remain BLOCKED until this gap-closure plan ships — items 1-3 are gated on CR-01/02/03 being closed. Once this plan executes, those manual checks become meaningful and should be re-run before marking Phase 1 `passed`.
</deferred>

<artifacts_produced>
## Artifacts this phase produces (Plan 07 — Gap Closure: CR-01, CR-02, CR-03)

**Modules / classes modified (no new modules):**
- `manga_ai_studio.gui.main_window.MainWindow._resolve_detection_model_path` (FIXED, CR-01): now calls `download_torch_model(config.get_model_cache_dir())`; except narrowed to `(FileNotFoundError, OSError)`; fallback returns `cache_dir / "comictextdetector.pt"`; programming errors propagate; logs via `logger.warning` before fallback
- `manga_ai_studio.gui.main_window.MainWindow._resolve_inpainting_model_path` (FIXED, CR-02): now calls `get_inpainting_model_path(self.profile_manager.config)` (Config, not Profile); except narrowed; fallback returns `config.get_model_cache_dir() / "anime-manga-big-lama.pt"` (correct vendored default); programming errors propagate
- `manga_ai_studio.gui.main_window.MainWindow._on_inpaint_finished` (FIXED, CR-03): unpacks bbox into (x, y, w, h); slices `pre_inpaint[y:y+h, x:x+w].copy()` before pushing; `history.push_image_action(x, y, bbox_patch)` now receives a bbox-shaped patch; the bare `except Exception: pass` on the push is REMOVED (WR-05 closed at this site)

**Test files modified (widened, no new test files):**
- `tests/test_detection/test_ctd_adapter.py` (+4 tests): test_resolve_detection_model_path_calls_download_with_cache_dir, test_resolve_detection_model_path_filename_matches_vendored_default, test_resolve_detection_model_path_no_bare_except, test_resolve_detection_model_path_programming_errors_propagate
- `tests/test_inpainting/test_lama_adapter.py` (+4 tests): test_resolve_inpainting_model_path_returns_cache_dir_path, test_resolve_inpainting_model_path_filename_matches_vendored_default, test_resolve_inpainting_model_path_no_bare_except, test_resolve_inpainting_model_path_programming_errors_propagate
- `tests/test_inpainting/test_inpaint_gui.py` (+2 tests, 1 widened): test_inpaint_pushes_image_history (WIDENED — real HistoryManager, asserts patch.shape[:2] == (bbox_h, bbox_w); _StubHistory removed), test_inpaint_undo_roundtrip_preserves_surrounding_region (NEW — proves Ctrl+Z writes only the bbox region), test_inpaint_finished_push_uses_bbox_slice (NEW — explicit shape guard)

**Symbols / call sites modified:**
- `download_torch_model` call site in `_resolve_detection_model_path` — arity fixed (0 args -> 1 arg, the cache_dir)
- `get_inpainting_model_path` call site in `_resolve_inpainting_model_path` — arg type fixed (Profile -> Config)
- `history.push_image_action` call site in `_on_inpaint_finished` — patch shape fixed (full image -> bbox slice)
- 3 bare `except Exception:` clauses narrowed or removed (shared root cause)

**Config keys consumed (unchanged):**
- `text_detector.model_path` (Profile) — still the short-circuit for a user-configured detection model path
- `inpainting_backend` (Profile, default "torch") — unchanged
- `get_model_cache_dir()` (Config) — now correctly reached by both resolvers (was unreachable for inpaint due to CR-02)

**Threat IDs mitigated:**
- T-01-17 (silent feature failure via swallowed exceptions) — mitigated at all three touched sites
- T-01-18 (image data corruption via mis-shaped undo patch) — mitigated; round-trip regression test locks it
- T-01-19 (model resolution failures hidden from logs) — mitigated; logger.warning before fallback

**Re-verification targets (the 3 FAILED truths in 01-VERIFICATION.md that this plan flips to VERIFIED):**
- Truth #2 (CLEAN-02): "User can run heatmap text detection and see auto-generated mask" — _resolve_detection_model_path now works on first run
- Truth #4 (CLEAN-06): "User can run LaMa inpainting on the mask" — _resolve_inpainting_model_path now works on first run
- Truth #6 (FLOW-02 image half): "User can undo/redo image (inpaint) operations via Ctrl+Z/Ctrl+Shift+Z" — _on_inpaint_finished now pushes the correct patch shape
</artifacts_produced>

<output>
Create `.planning/phases/01-cleaning-workspace/01-07-SUMMARY.md` when done
</output>
