---
phase: 08-masker-selective-inpaint
plan: 10
type: execute
wave: 1
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06, 08-07, 08-08, 08-09]
gap_closure: true
files_modified:
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/core/detection_boxes.py
  - manga_ai_studio/core/batch_runner.py
  - tests/test_gui_gap_closure.py
  - tests/test_core/test_detection_boxes.py
  - tests/test_core/test_batch_runner.py
autonomous: true
requirements: [MASK-01, MASK-02, MASK-03, MASK-05]

estimate:
  tokens: 39000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After Detect + a box move, the next std-dev/radius change or Inpaint override commit recomposes the auto mask at the box's CURRENT (moved) position — the composited content bbox equals the moved rect, never the birth origin (CR-01)."
    - "A std-dev threshold tweak after a geometry op (which invalidated per-box masks per the 08-01 policy) leaves the existing auto plane untouched — no silent whole-plane wipe (CR-03)."
    - "Hand-painted strokes and the erase ledger on the current page survive a detect-mode batch restore — only the AUTO plane is replaced (CR-02)."
    - "Inpaint/batch-clean consumption clears manual, erase, AND auto planes signal-silently — no resurrect on the next stroke/undo/page-switch recompose, and a re-run of Inpaint does not re-process the cleaned region (CR-04)."
    - "A batch detect keeps the page's persisted USER-origin boxes (D-03 merge, same semantics as the interactive path) and refreshes every box's mask/std_dev over the merged set (WR-01)."
    - "A detect-mode batch marks every touched page dirty — Close prompts save instead of silently losing the detected boxes (WR-02)."
  artifacts:
    - manga_ai_studio/gui/canvas.py defines consume_mask_display()
    - manga_ai_studio/core/detection_boxes.py defines merge_page_boxes_for_detect(existing, fresh_detected)
    - tests/test_gui_gap_closure.py exists with the five GUI probes (moved-box recompose, no-fit guard, batch-stroke preservation, consumption plane-clear/no-resurrect, detect-batch dirty)
    - tests/test_core/test_detection_boxes.py extends with test_merge_page_boxes_keeps_user_replaces_detected
    - tests/test_core/test_batch_runner.py extends with test_batch_detect_preserves_user_boxes
    - manga_ai_studio/gui/main_window.py: _on_std_dev_threshold_changed, _rederive_auto_layer, _recompose_boxes_auto_plane read canvas.boxes_snapshot(); _on_batch_finished marks dirty in detect modes; both consumption sites call consume_mask_display; the detect-batch refresh calls set_auto_binary
  key_links:
    - "boxes_snapshot() materializes current geometry at call-time -> every recompose consumer composes from it (the fix for CR-01) while _refit_changed_boxes keeps its live mask/std_dev write-back"
    - "set_auto_binary replaces only the auto plane -> detect-batch refresh cannot touch the live manual/erase planes (the fix for CR-02)"
    - "consume_mask_display -> recompose_mask over emptied planes -> the displayed composite is cleared AND the planes cannot resurrect it (the fix for CR-04)"
    - "merge_page_boxes_for_detect -> derive_page_mask_state(merged) -> page.boxes = merged -> user boxes survive + fits refresh (the fix for WR-01)"
    - "_on_batch_finished dirty marking in detect modes -> the lossless-close path (the fix for WR-02)"
---

<objective>
Close the four Critical (CR-01..CR-04) and two Warning (WR-01, WR-02) gap-closure defects
recorded in `08-VERIFICATION.md` that caused failed Truths 2, 3 (SC-2/SC-3 post-move
incorrectness) and the D-01 hand-strokes-survive contract failure. The phase machinery
exists and is test-green; these fixes make the composite mask LaMa consumes correct in
the reachable flows the verification probed. WR-03's zero-boxes guard for the
`_rederive_auto_layer` mode-ON branch (the Anti-Patterns row + gap-1 missing remedy) is
closed as a bonus inside Task 1's rewrite of that exact branch — two lines, not a separate
scope item.

Purpose: GR-01/GR-03 wrong-region-or-wiped auto composition breaks the raison d'etre of
MASK-02/MASK-03; CR-02/CR-04/WR-01/WR-02 destroy or silently lose user data (hand strokes,
consumed overlays, persisted user boxes, and detected boxes on close). All six headline
defects plus the WR-03 guard are localized fixes that do not change the phase architecture
(WR-03's guard is source-asserted, not probe-covered).

Output: corrected `main_window.py` recompose/refresh/consume paths, a new
`canvas.consume_mask_display()`, a new headless `merge_page_boxes_for_detect()`, the
batch worker D-03 merge + finished-handler dirty marking, and a new GUI regression suite
(`tests/test_gui_gap_closure.py`) whose probes FAIL on the pre-fix code and PASS after —
plus extended headless suites for the batch merge.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-VERIFICATION.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-VALIDATION.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/core/batch_runner.py
@manga_ai_studio/core/detection_boxes.py

# This plan is a gap-closure companion to the already-executed 08-01..08-09 phase
# plans. It does NOT re-plan the phase; it fixes the six verified defects. No prior
# phase SUMMARY is load-bearing beyond what the verification block itself documents —
# the four probe repairs each reference the exact `file:line` evidence from
# `08-VERIFICATION.md`.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: CR-01 + CR-03 + WR-03 — recompose consumers read live geometry; threshold slot gains the no-fit guard; mode-ON re-derive gains the zero-boxes guard</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_gap_closure.py</files>
  <read_first>manga_ai_studio/gui/main_window.py — `_on_std_dev_threshold_changed` (~:3047-3073), `_rederive_auto_layer` (~:3075-3119), `_refit_changed_boxes` (~:3355-3422), `_recompose_boxes_auto_plane` (~:3712-3745), `_on_ocr_finished` (~:5336-5369); tests/test_gui_detection_boxes.py — the `_window_with_page` / `_blk` / `_isolate_settings` harness the probes mirror</read_first>
  <action>
    Root cause CR-01: the canvas never re-materializes a moved/resized box's `pagebox.box`
    (only `setRect`; geometry materialization lives solely in `BoxItem.current_box` via
    `canvas.boxes_snapshot()`). Three recompose consumers read the LIVE pageboxes whose
    `.box` is birth geometry, so `_on_std_dev_threshold_changed` (:3064),
    `_rederive_auto_layer` mode-ON (:3112) and `_recompose_boxes_auto_plane` (:3730) compose
    the auto mask at the pre-move origin. The consistent fix (matching the existing 08-07
    architecture where `_refit_changed_boxes` already treats `boxes_snapshot()` as the
    geometry source): make EVERY recompose consumer read `canvas.boxes_snapshot()` — a call
    that materializes current int geometry from the live rects AND carries each box's
    `mask`/`std_dev`/`inpaint_override` (the field-carry 08-07 added at canvas.py:2153-2164).
    Do NOT write `.box` back onto the live pageboxes anywhere — that would replace the Box
    object identity that `_on_ocr_finished` (:5352) routes on via `id(it.pagebox.box)`.

    Changes to `manga_ai_studio/gui/main_window.py` (per D-02/D-12/D-14 locked decisions):

     1. `_on_std_dev_threshold_changed` (:3064-3071): replace the live-read with
        `current_boxes = self.canvas.boxes_snapshot()`. Keep the existing zero-boxes return
        (:3065-3066). THEN insert the CR-03 no-fit guard immediately after it and BEFORE the
        `image_np is None` return (:3067-3069): `if not any(pb.mask is not None for
        pb in current_boxes): return` — byte-mirroring `_recompose_boxes_auto_plane` :3733
        (comment: "no fit data to derive from (post-geometry invalidation)") and in the SAME
        relative position (zero-boxes → no-fit → image → compose) so the two sibling slots
        are structurally identical; the placement relative to the image check is deliberate,
        behaviourally identical either way, and must NOT be "fixed" by a future reader.
        Rationale: after a geometry op invalidates per-box masks (08-01 policy) while the
        auto plane keeps the geometry-transformed content, a threshold tweak must NOT compose
        an all-mask-None empty binary and silently wipe the plane.
        `compose_auto_binary(current_boxes, float(value), page_size)` and
        `refresh_box_inpaint_states()` stay unchanged.
     2. `_rederive_auto_layer` mode-ON branch (:3111-3117): replace `boxes = [it.pagebox for
        it in self.canvas._box_items]` with `boxes = self.canvas.boxes_snapshot()`, and
        immediately after that snapshot read add the WR-03 zero-boxes guard
        `if not boxes: return` (mirroring the threshold slot's zero-boxes return at :3065-3066)
        BEFORE `derive_page_mask_state(...)` — WITHOUT it, a dilation/radius nudge on a page
        with zero boxes would derive over an empty list and wipe a previously mode-OFF
        full-heatmap auto layer after a mode switch (WR-03 — the `08-VERIFICATION.md`
        Anti-Patterns row and gap-1 missing remedy; this guard closes it). Because
        `derive_page_mask_state` MUTATES the list it is handed (writes mask/std_dev — the
        interactive :4515-4522 pattern mutates the live pageboxes), a snapshot input would
        strand the fresh fits on detached copies and `refresh_box_inpaint_states` (reading
        live pageboxes) would show stale borders. Therefore, AFTER the derive call, add the
        live write-back loop that mirrors `_refit_changed_boxes` (:3418-3420) exactly: for
        `zip(self.canvas._box_items, boxes)`, assign `live_item.pagebox.mask = fitted.mask`
        and `live_item.pagebox.std_dev = fitted.std_dev` (no `.box` write — identity safety).
        Keep `set_auto_binary(derivation.auto_binary)` + `refresh_box_inpaint_states()`.
    3. `_recompose_boxes_auto_plane` (:3730-3743): replace the live-read with
       `current_boxes = self.canvas.boxes_snapshot()`. Keep the zero-boxes return (:3731-3732)
       and the existing `any(pb.mask is not None ...)` no-fit guard (:3733-3734) unchanged —
       the snapshot carries the same stored fields. `compose_auto_binary` +
       `refresh_box_inpaint_states` unchanged.
    4. `_refit_changed_boxes` (:3355-3422): NO change — it already reads
       `boxes_snapshot()` for both the geometry diff and the derive input, and its live
       mask/std_dev write-back is the mechanism that keeps live pageboxes fresh after a
       move/resize/create commit. Its existing comment (:3360-3362) documents the
       snapshot-as-geometry contract this plan completes across the other consumers.

    Regression probes — create `tests/test_gui_gap_closure.py` (module docstring cites this
    plan and `08-VERIFICATION.md`; `importorskip("PySide6")`, `@pytest.mark.gui`, reusing
    the `_window_with_page` data-model registration pattern). Write the two probes FIRST and
    commit them RED against the pre-fix code (they fail on the stale-read code — this is the
    TDD contract), then apply the fixes and make them GREEN:

    - `test_recompose_after_move_uses_live_geometry` — window (mode ON) → drive
      `_on_detection_finished({"mask": full-page-content binary, "blocks": [_blk(5,5,29,34)]})`
      → capture `before = window.canvas.boxes_snapshot()` → move the sole box item via
      `item.setRect(QRectF(25,15,24,29))` (birth (5,5,29,34) → moved (25,15,49,44)) → emit
      `window.canvas.boxes_modified.emit(before)` (runs `_refit_changed_boxes`, refreshing
      live masks) → call `window._on_std_dev_threshold_changed(50.0)`. Assert the recomposed
      auto content (np.argwhere over `window.canvas._auto_bin`) has its min-corner at the
      MOVED origin ((25,15)) and its max-corner at (49,44) — i.e. the composite did NOT jump
      back to the birth origin (5,5,29,34). The VERIFICATION probe recorded the exact pre-fix
      failure (content jumped back to (5,5,29,34)).
    - `test_threshold_tweak_after_invalidation_does_not_wipe_auto_plane` — window (mode ON)
      → detect with content → assert `canvas._auto_bin` has content → invalidate every live
      box fit by setting `item.pagebox.mask = None` on each `_box_items` entry (simulating
      the 08-01 geometry-op invalidation) → assert the auto plane still holds content →
      call `window._on_std_dev_threshold_changed(15.5)` → assert `window.canvas._auto_bin`
      is UNCHANGED (same reference-or-equivalent content, not None, not all-zero). The
      VERIFICATION probe recorded the pre-fix wipe (content went None after one tweak).
  </action>
  <behavior>
    - Test 1: detect → move box (5,5,29,34)→(25,15,49,44) → boxes_modified.emit → threshold
      change → auto content bbox min-corner == (25,15) and max-corner == (49,44) (moved rect),
      not (5,5)/(29,34) (birth rect). Pre-fix: fails (bbox at birth origin). Post-fix: passes.
    - Test 2: detect-with-content → pb.mask=None on all boxes (geometry-op invalidation) →
      threshold change → auto plane retains its pre-tweak content (not wiped). Pre-fix: fails
      (plane emptied). Post-fix: passes (no-fit guard returns early).
  </behavior>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_gap_closure.py -x -q</automated>
  </verify>
  <done>Both probes pass on the fixed code and failed on the pre-fix code with the 08-VERIFICATION.md signatures; the six suite files listed in acceptance criteria stay green. All three recompose consumers compose from boxes_snapshot(); the threshold slot carries the no-fit guard; the mode-ON re-derive carries the WR-03 zero-boxes guard.</done>
  <acceptance_criteria>
    - Both probes in tests/test_gui_gap_closure.py exist and FAILED with the same pre-fix
      signature recorded in 08-VERIFICATION.md (content bbox back at birth origin / auto
      plane wiped), then PASS after the fixes.
    - Source assertion (grep, filtered): `_on_std_dev_threshold_changed` contains
      `self.canvas.boxes_snapshot()` and the exact guard `if not any(pb.mask is not None for
      pb in current_boxes): return`; `_rederive_auto_layer` contains
      `self.canvas.boxes_snapshot()`, the WR-03 zero-boxes guard `if not boxes: return`
      immediately after it (before `derive_page_mask_state(`), and the
      `zip(self.canvas._box_items, boxes)` write-back; `_recompose_boxes_auto_plane` contains
      `self.canvas.boxes_snapshot()`.
    - Suites stay green: tests/test_gui_detection_boxes.py, tests/test_gui_inspector_override.py,
      tests/test_gui_mask_planes.py, tests/test_gui_border_states.py, tests/test_gui_boxes.py.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-04 + CR-02 — consume_mask_display clears all planes; detect-batch refresh replaces only the auto plane</name>
  <files>manga_ai_studio/gui/canvas.py, manga_ai_studio/gui/main_window.py, tests/test_gui_gap_closure.py</files>
  <read_first>manga_ai_studio/gui/canvas.py — `recompose_mask` (:582-612), `set_auto_binary` (:551-580), `set_planes` (:633-684); manga_ai_studio/gui/main_window.py — inpaint consumption (:4975-4993), `_refresh_current_page_after_batch` detect branch (:6385-6404) and clean branch (:6407-6423)</read_first>
  <action>
    CR-04 (inpaint/batch-clean consumption clears only the display composite, not the three
    planes → resurrect on next stroke/undo/page-switch; re-Inpaint re-processes the cleaned
    region): add a canvas method that clears ALL THREE planes signal-silently. CR-02
    (detect-mode batch restore passes EXPLICITLY-empty manual/erase to `set_planes` while the
    dispatch-time flush persisted only the flat composite → hand strokes and the erase ledger
    are wiped with no undo): replace only the AUTO plane so the current page's live
    manual/erase planes survive. Both fixes preserve the 2-stack undo contract (no
    `mask_modified` emission for these non-paint actions).

    Changes to `manga_ai_studio/gui/canvas.py`:

    1. Add `consume_mask_display(self) -> None` next to `recompose_mask`/`set_planes`. If
       `_mask_manual` is not None and not null: fill both `_mask_manual` and `_mask_erase`
       fully transparent via `Qt.GlobalColor.transparent` fill, set `_auto_bin = None`, and
       call `recompose_mask()` (which rebuilds `_mask` from the emptied planes → a fully
       transparent composite, refreshed through `update_mask_display`). If `_mask_manual` is
       None/null (no page), fall back to filling `_mask` transparent + `update_mask_display()`
       (the pre-existing display-only clear). The method MUST NOT emit `mask_modified` —
       document that consumption is not a paint action and must never push a mask-undo entry
       (the CR-16 2-stack contract the current comments at main_window.py:4981-4989 and
       :6414-6419 invoke).
    2. No other canvas change.

    Changes to `manga_ai_studio/gui/main_window.py`:

    3. Inpaint consumption (:4990-4993): replace the four-line
       `canvas_mask = self.canvas.get_mask(); canvas_mask.fill(...); update_mask_display()`
       block with `self.canvas.consume_mask_display()`. Keep the `_set_session_dirty()` call
       above (:4979) and the history push below (:4995-5001) untouched.
    4. Batch-clean consumption (:6420-6423): replace the same fill-on-composite block with
       `self.canvas.consume_mask_display()`. Keep the reload (set_image_from_path) and the
       D-07/dirty behavior around it untouched. The `ImageFile` mask/plane slots are NOT
       touched (the documented :6323-6327 invariant — persisted state stays for consistent
       re-navigation; the planes are the resurrection vector, and they are cleared here).
    5. CR-02 detect-branch restore (:6392-6401): delete the `empty_bin` + the two
       `numpy_binary_to_mask_qimage(empty_bin)` constructions and replace the
       `self.canvas.set_planes(manual_empty, erase_empty, auto_bin)` call with
       `self.canvas.set_auto_binary(auto_bin)`. `set_auto_binary` stores the (H,W) uint8
       binary into the auto plane only and recomposes — the live manual/erase planes (the
       current page's strokes, which `_flush_current_canvas_mask_to_data_model` :6133-6164
       persists only as the flat composite) stay untouched. `auto_bin` is already unpacked at
       the canvas mask dims (:6393-6396) so the strict dims guard in `set_auto_binary` holds.
       This honors the D-01 "hand strokes always survive re-detection" contract that the
       interactive `_on_detection_finished` path already honors via `set_auto_binary`.

    Regression probes — append to `tests/test_gui_gap_closure.py` (RED first, then GREEN):

    - `test_consume_mask_display_clears_planes_and_no_resurrection` — seed non-empty manual,
      erase and auto planes via `canvas.set_planes(nonempty_manual, nonempty_erase,
      content_bin)`; connect a stub recorder to `canvas.mask_modified`; call
      `canvas.consume_mask_display()`; assert `mask_modified` never fired (signal-silent),
      the manual/erase planes are content-free and `_auto_bin` is None, and the composite
      (`mask_to_numpy_binary(canvas.get_mask())`) is content-free; then call
      `canvas.recompose_mask()` again (the stroke-commit stand-in) and assert the composite
      stays content-free — the consumed overlay cannot resurrect.
    - `test_detect_batch_refresh_preserves_manual_and_erase` — window; seed content into the
      live manual and erase planes on the current page (e.g. via
      `canvas.set_planes(nonempty_manual, nonempty_erase, None)`); set `imf.auto_mask` to a
      packed distinct binary and `imf.mask` to a non-null content QImage (the flat-restore
      path :6353 prerequisites); call `window._refresh_current_page_after_batch("detect")`;
      assert the manual and erase planes still carry the seeded content AND
      `canvas._auto_bin` equals the unpacked `imf.auto_mask` (the batch result replaced only
      the auto plane). Pre-fix: manual/erase are wiped → fails.
    - `test_batch_clean_refresh_clears_planes` — seed non-empty planes; call
      `window._refresh_current_page_after_batch("clean")` (no cleaned file present so reload
      is skipped); assert the manual/erase planes are content-free and `_auto_bin` is None —
      the clean-path consume clears planes so a re-clean cannot re-process the region.
  </action>
  <behavior>
    - Test 1: seeded planes → consume_mask_display → no mask_modified emission, all three
      planes content-free, composite content-free, and a subsequent recompose_mask stays
      content-free. Pre-fix: no such method exists (RED on import/attr) and the display-only
      clear would resurrect.
    - Test 2: seeded manual/erase + packed imf.auto_mask → detect refresh → manual/erase
      planes unchanged AND auto plane equals the batch binary. Pre-fix: manual/erase wiped.
    - Test 3: clean refresh → planes content-free after. Pre-fix: planes keep content.
  </behavior>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_gap_closure.py tests/test_gui_mask_planes.py tests/test_gui_batch.py -x -q</automated>
  </verify>
  <done>consume_mask_display() exists on EditorCanvas and is called at both consumption sites; the detect-batch refresh replaces only the auto plane; the three probes pass post-fix (all three planes clear with no resurrection on recompose, manual/erase survive a detect refresh, clean refresh clears planes).</done>
  <acceptance_criteria>
    - The three probes exist; they failed pre-fix (test 1 on AttributeError/plane content
      remaining, tests 2-3 on wiped/preserved planes) and pass post-fix.
    - Source assertion (grep, filtered, on manga_ai_studio/gui/main_window.py):
      `consume_mask_display` is called at TWO distinct sites (the inpaint finish handler and
      the batch-clean branch of `_refresh_current_page_after_batch`) and `set_auto_binary` is
      called in the detect branch of `_refresh_current_page_after_batch`.
    - Suites stay green: tests/test_gui_mask_planes.py, tests/test_gui_detection_boxes.py,
      tests/test_gui_batch.py.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-01 + WR-02 — batch D-03 merge preserves user boxes; detect-mode batch marks pages dirty</name>
  <files>manga_ai_studio/core/detection_boxes.py, manga_ai_studio/core/batch_runner.py, manga_ai_studio/gui/main_window.py, tests/test_core/test_detection_boxes.py, tests/test_core/test_batch_runner.py</files>
  <read_first>manga_ai_studio/core/detection_boxes.py (module — build_detected_pageboxes / derive_page_mask_state / compose_auto_binary); manga_ai_studio/core/batch_runner.py detect branch (:176-208); manga_ai_studio/gui/main_window.py `_on_batch_finished` (:6197-6214) and the batch dispatch (:6078-6100); tests/test_core/test_batch_runner.py harness (FakeDetectionModel, make_pages)</read_first>
  <action>
    WR-01 (batch_runner.py:202 `page.boxes = boxes` overwrites persisted USER boxes with no
    D-03 merge — non-current pages lose user-drawn box state incl. overrides): apply the
    interactive D-03 replace-detected-keep-user semantics headlessly. WR-02 (detect-mode
    batch never marks pages dirty → Close silently loses detected boxes): mark the batch
    pages dirty when a detect-mode batch completes.

    Changes:

    1. `manga_ai_studio/core/detection_boxes.py` — add the headless, Qt-free helper
       `merge_page_boxes_for_detect(existing: Sequence[PageBox], fresh_detected: Sequence[PageBox]) -> list[PageBox]`.
       Semantics mirror the interactive `_build_detected_boxes` D-03 rule (:4594-4613): keep
       every existing box whose `origin == USER` (preserving its payload/style/
       inpaint_override/geometry/the empty D-15 seam fields the derive will refill), replace
       every DETECTED-origin box with the fresh `fresh_detected` list; return
       `[kept user boxes] + list(fresh_detected)`. Do NOT copy-detach here (the caller's
       `derive_page_mask_state` mutates the returned list in place, which is the interactive
       contract); write a module docstring citing this plan and the 08-07/08-09 consumers.
    2. `manga_ai_studio/core/batch_runner.py` detect branch (:187-208): change to
       `boxes = build_detected_pageboxes(blk_list, img_w, img_h)`, then
       `merged = merge_page_boxes_for_detect(page.boxes, boxes)`, then
       `derivation = derive_page_mask_state(image_rgb, mask_refined, merged, masker_conf, int(masker_conf.mask_dilation_radius))`
       (the merged list — user + fresh detected — certifies, matching the interactive
       all-boxes-derive at main_window.py:4515-4522), then `page.boxes = merged` in place of
       the current `page.boxes = boxes`. Keep the raw/auto/mask persistence
       (:203-208) exactly as-is. The worker stays PIL/numpy-only (detection_boxes.py is
       headless by construction — the merge helper must import no Qt).
    3. `manga_ai_studio/gui/main_window.py` `_on_batch_finished` (:6197-6214): after the
       summary is rendered and BEFORE `_refresh_action_states()`, add: when
       `self._batch_mode in ("detect", "detect_and_clean")` and the summary `ok` count is
       greater than zero, set `imf.dirty = True` for every entry in `self.image_files` (the
       exact pages the worker mutated — batch dispatch passes `list(self.image_files)` at
       :6081/:6090, so the worker's `page.boxes`/`auto_mask`/`mask` writes land on those same
       objects) and call `self._update_title()` so the title `*` suffix reflects the state.
       This makes Close prompt/allow save of the freshly detected boxes instead of silently
       dropping them. Do NOT dirty in clean-only mode (behavior unchanged). Note
       `_batch_mode` is still set when `result` fires (cleanup resets it later via
       `finished`), so the read is safe here.

    Regression probes (RED first, then GREEN):

    - `tests/test_core/test_detection_boxes.py` —
      `test_merge_page_boxes_keeps_user_replaces_detected`: headless unit — existing =
      [USER-origin PageBox with inpaint_override "never" and a payload, DETECTED-origin
      PageBox], fresh_detected = one new DETECTED PageBox; assert the merged list contains
      the USER box (override "never" + payload intact), does NOT contain the old DETECTED
      box, and ends with the fresh detected box.
    - `tests/test_core/test_batch_runner.py` —
      `test_batch_detect_preserves_user_boxes`: seed an ImageFile whose `boxes` list contains
      a USER-origin box (inpaint_override "always") + a stale DETECTED-origin box; run
      `batch_detect` over it with FakeDetectionModel + masker_conf; assert
      `page.boxes` still contains the USER box with inpaint_override "always" AND that both
      the kept user box and the fresh detected boxes carry a non-None `mask`/`std_dev`
      (the derivation ran over the merged set). Pre-fix: the USER box is absent from
      `page.boxes` after the run → fails.
    - `tests/test_gui_gap_closure.py` — `test_detect_batch_marks_pages_dirty`: window with
      two image_files; set `window._batch_mode = "detect"`; call
      `window._on_batch_finished({"ok": 2, "failed": [], "total": 2})`; assert every
      `imf.dirty is True` and the title reflects the dirty state; then a fresh window with
      `window._batch_mode = "clean"` and the same summary asserts `imf.dirty` stays False
      (no spurious dirty marking in clean mode).
  </action>
  <behavior>
    - Test 1: merge keeps USER-origin boxes with state intact and replaces DETECTED-origin
      boxes with fresh detected ones (pure function).
    - Test 2: batch_detect over a page with a persisted user box → the user box survives in
      page.boxes with its override AND all boxes get refreshed mask/std_dev. Pre-fix: the
      user box is overwritten away (page.boxes = boxes).
    - Test 3: detect-mode batch summary → every image_files entry dirty; clean-mode summary →
      no dirty. Pre-fix: dirty stays False in detect mode.
  </behavior>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_detection_boxes.py tests/test_core/test_batch_runner.py tests/test_gui_gap_closure.py -x -q</automated>
  </verify>
  <done>merge_page_boxes_for_detect() exists in detection_boxes.py; the batch worker merges before deriving and persists page.boxes = merged; detect/detect_and_clean batch completion dirties every touched image; the three probes pass post-fix and failed pre-fix.</done>
  <acceptance_criteria>
    - The three probes exist; tests 1-2 failed pre-fix (merge absent / user box overwritten)
      and pass post-fix; test 3 failed pre-fix (dirty False after detect batch) and passes.
    - Source assertion (grep, filtered): detection_boxes.py defines
      `def merge_page_boxes_for_detect`; batch_runner.py calls `merge_page_boxes_for_detect`
      and no longer assigns `page.boxes = boxes` in the detect branch (it assigns
      `page.boxes = merged`); main_window.py `_on_batch_finished` contains the
      `"detect"`/`"detect_and_clean"` dirty loop.
    - Suites stay green: tests/test_core/test_detection_boxes.py,
      tests/test_core/test_masker_machinery.py, tests/test_core/test_masker_config_roundtrip.py,
      tests/test_core/test_batch_runner.py, tests/test_gui_batch.py.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user strokes/masks planes → composite | Canvas three-plane state (manual/erase/auto) crossing into the LaMa-consumed composite — corruption here means wrong-region or lost inpainting |
| persisted boxes (User origin) → batch worker | `ImageFile.boxes` crossing the worker boundary on batch detect — a merge miss destroys user box state |
| in-memory data model → close/save | The dirty flag decides whether detected/edited state survives a Close — a false-clean loses data silently |

## STRIDE Threat Register
ASVS Level 1 — block on high; every row is a data-integrity concern (user strokes / masks /
user boxes loss or misplacement), not a network/external-input class threat. Severity =
impact × likelihood over reachable flows; all four Criticals were probe-confirmed except
CR-04 (code-confirmed) — the two Warnings are worker/finished-handler code-confirmed.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10-01 | Integrity (optimistic) | `_on_std_dev_threshold_changed`, `_rederive_auto_layer`, `_recompose_boxes_auto_plane` | critical | mitigate | Re-compose only from `canvas.boxes_snapshot()` (Task 1) — current geometry at call-time, never stale birth `pagebox.box`; last-resort no-fit guard (CR-03) prevents an empty-binary wipe of the geometry-transformed plane |
| T-08-10-02 | Integrity (distortion) | batch_runner detect branch | high | mitigate | `merge_page_boxes_for_detect` applies the interactive D-03 replace-detected-keep-user rule before `derive_page_mask_state`; `page.boxes = merged` (WR-01, Task 3) — user box state cannot be overwritten by the worker |
| T-08-10-03 | Denial of service (user-data) | detect-mode batch refresh `_refresh_current_page_after_batch("detect")` | critical | mitigate | `set_auto_binary` in place of `set_planes(empty, empty, ...)` (CR-02, Task 2) — only the auto plane is replaced; live manual/erase planes and the erase ledger survive the restore |
| T-08-10-04 | Tampering (display/state) | inpaint + batch-clean consumption sites | critical | mitigate | `canvas.consume_mask_display()` clears all three planes signal-silently (CR-04, Task 2) — consumed overlay cannot resurrect on stroke/undo/page-switch recompose, so re-Inpaint cannot re-process the cleaned region; no mask-undo corruption (no `mask_modified` emission) |
| T-08-10-05 | Repudiation / integrity | detect-mode batch close path | high | mitigate | `_on_batch_finished` marks every touched `ImageFile.dirty` in detect/detect_and_clean modes with ok > 0 (WR-02, Task 3) — Close prompts save instead of silently dropping detected boxes/masks |
| T-08-10-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages — every fix is in-repo Python; no supply-chain surface introduced by this plan |
</threat_model>

<verification>
1. Probes must FAIL on the pre-fix code (each task commits its probes RED first, recording
   the failing assertion), then PASS after the fix — the RED evidence for CR-01/CR-03 must
   reproduce the exact outputs logged in `08-VERIFICATION.md` (content bbox back at
   (5,5,29,34); auto plane wiped to None).
2. The new GUI regression file:
   `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_gap_closure.py -x -q`
3. The headless probes:
   `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_detection_boxes.py tests/test_core/test_batch_runner.py -x -q`
4. Suites that must stay green (the 08-01..08-09 regression surface):
   `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_detection_boxes.py tests/test_core/test_masker_machinery.py tests/test_core/test_masker_config_roundtrip.py tests/test_gui_mask_planes.py tests/test_gui_inspector_override.py tests/test_gui_detection_boxes.py -q`
5. Full-suite regression (baseline 869 passed):
   `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
6. Post-fix human UAT (from `08-VERIFICATION.md` "Post-fix UAT items"): move a box after
   detection, then commit `Never` via the Inspector override and confirm the content is
   removed at the box's CURRENT position (not the pre-move origin) — the moved-box +
   override flow that FAILED and is fixed by Task 1. Also spot-check that running Batch →
   Detect from a page with hand-painted strokes keeps the strokes on the current page
   (Task 2) and that the session closes with a save prompt after a detect batch (Task 3).
</verification>

<success_criteria>
- All six headline defects (CR-01, CR-02, CR-03, CR-04, WR-01, WR-02) are fixed in source
  with a failing-then-passing probe as evidence per defect; the WR-03 zero-boxes guard is
  additionally closed in `_rederive_auto_layer` (verified by the Task 1 source assertion,
  not a probe).
- Truths T2 (SC-2 selective per-box inpaint composite correct), T3 (SC-3 override mask
  effect correct post-move) and the D-01 hand-strokes-survive contract are re-verified by
  the probes; WR-01/WR-02 (user box preservation + lossless close) are covered by their
  probes.
- The full 869-test suite stays green; no 08-01..08-09 behavior regressed.
- The moved-box + override flow from the verification report passes the human UAT check.
</success_criteria>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-10-SUMMARY.md` when done, recording
per-defect probe RED→GREEN evidence and the final suite counts.
</output>
