---
phase: 08-masker-selective-inpaint
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - manga_ai_studio/core/detection_boxes.py
  - tests/test_core/test_masker_machinery.py
  - tests/test_core/test_detection_boxes.py
autonomous: true
requirements: [MASK-01, MASK-02, MASK-05]
must_haves:
  truths:
    - "A pure, Qt-free, torch-free function turns (page RGB, heatmap, boxes, MaskerConfig, radius) into per-box (mask, std_dev) fits plus the constrained auto-plane binary — consumed by BOTH the GUI seam (08-07) and batch (08-09)"
    - "Detected heatmap content outside the union of all boxes NEVER enters the auto plane (D-02/MASK-05) — dilate first, then intersect, so auto content cannot exit a box either"
    - "Dilation applies to detected content only (D-07) and the pre-dilation binary is returned for retention (D-08)"
    - "The std-dev gate is decoupled from fitting: fits always store best_mask + std_dev (gate-lifted), and which boxes CONTRIBUTE is a pure function of threshold + override — threshold changes never re-fit"
    - "All boxes certify — user-origin boxes are fitted identically to detected ones"
  artifacts:
    - manga_ai_studio/core/detection_boxes.py (new)
    - tests/test_core/test_masker_machinery.py (new — first real callers of the vendored machinery)
    - tests/test_core/test_detection_boxes.py (new)
  key_links:
    - "build_detected_pageboxes is the extraction of the V5 loop from main_window._build_detected_boxes (:3944-3970) — 08-07 rewires the GUI to consume it"
    - "derive_page_mask_state / compose_auto_binary / dilate_auto_mask are the seam primitives 08-07 (interactive) and 08-09 (batch) call"
---

<objective>
Build the detection→mask seam core as pure functions (PIL/numpy/scipy only), and write the first real call-site tests for the vendored masker machinery (today only import-tested).

Purpose: D-02's discard, MASK-01's dilation, and MASK-02's std-dev-gated per-box fitting must live in ONE headless implementation shared by the GUI handler and the batch loop (RESEARCH §5.1 pure-function extraction precedent). The machinery battery doubles as living documentation of the vendored contracts (RESEARCH §1).

Output: core/detection_boxes.py with build_detected_pageboxes, derive_page_mask_state, compose_auto_binary, dilate_auto_mask; two new unit test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-RESEARCH.md (§1 — exact contracts, §1.4 — the minimal sequence, §2 — the model)
@.planning/phases/08-masker-selective-inpaint/08-PATTERNS.md (file 4 + Shared Pattern 1 — the vendored call sequence)
panelcleaner/image_ops.py
panelcleaner/masker.py
panelcleaner/structures.py
manga_ai_studio/gui/main_window.py (:3944-3970 — the V5 loop being extracted)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Masker machinery battery — first real call-site tests</name>
  <files>tests/test_core/test_masker_machinery.py</files>
  <read_first>
    - panelcleaner/image_ops.py :116-127 (mask_intersection, compose_masks), :350-360 (make_mask_steps_convolution), :483-540 (border_std_deviation — BlankMaskError at :513-515), :541-583 (cut_out_box/cut_out_mask), :584-730 (pick_best_mask — None vs failed semantics, offset math :624-641), :811-830 (grow_mask — size 0 passthrough)
    - panelcleaner/structures.py :39-160 (Box: as_tuple, pad(amount, canvas_size)), :579-616 (MaskFittingResults)
    - .planning/phases/08-masker-selective-inpaint/08-RESEARCH.md §1.1-§1.2 (the contract table)
    - tests/test_core/test_masker_vendor.py (existing import-tests — extend the file's spirit in a NEW file, do not edit)
  </read_first>
  <behavior>
    - Test: grow_mask grows a single centered square by exactly N pixels on each side (N in {0, 2, 5}); size 0 returns an equal mask (PIL "1").
    - Test: mask_intersection discards mask pixels outside a box mask and keeps inside pixels exactly.
    - Test: border_std_deviation on a uniform-color page with a centered mask returns std == 0 (within 1e-9); on a page with a high-contrast stripe crossing the mask edge returns std > 10; an empty (all-white "1") mask raises BlankMaskError.
    - Test: pick_best_mask on a synthetic page (uniform box region + text-like black strokes inside a box) returns a MaskFittingResults with a non-None best_mask and a finite analytics_std_deviation; with a blank precise mask in the box returns None; with a deliberately noisy border and a low mask_max_standard_deviation returns failed=True.
    - Test: compose_masks(base_size, [(mask, (x, y)), ...]) pastes each mask at its origin and unions overlaps.
  </behavior>
  <action>
    Create tests/test_core/test_masker_machinery.py (unit marker — pure PIL/numpy/scipy, NO torch, NO Qt, NO model weights). Build synthetic pages with PIL: `Image.new("RGB", (w, h), (240, 240, 240))` with `ImageDraw` rectangles/lines for text-like strokes and contrast stripes; masks as `Image.new("1", size)` with filled regions. Drive the vendored functions directly, one test group per behavior above. These are the first tests that exercise the machinery for real — treat contract surprises (arity, coordinate order, return shapes) as findings to encode as assertions, per the 01-07 gap-closure discipline (exercise the REAL vendored function, no monkeypatching).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_masker_machinery.py -q</automated>
  </verify>
  <done>
    - Test command: pinned-interpreter pytest on tests/test_core/test_masker_machinery.py passes with at least the five behavior groups above (grow, intersection, std-dev uniform/textured/blank, pick_best_mask fit/None/failed, compose).
    - The test file imports nothing from torch, manga_ai_studio.gui, or PySide6 (source assertion by import list).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_detected_pageboxes — headless extraction of the V5 loop</name>
  <files>manga_ai_studio/core/detection_boxes.py, tests/test_core/test_detection_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py :3944-3970 (the V5 loop to extract — int coercion, per-edge clamp to img_w/img_h, zero-area drop, DETECTED origin, default style)
    - manga_ai_studio/core/box_model.py (textblock_to_box, DETECTED constant, PageBox fields incl. inpaint_override from 08-01)
    - manga_ai_studio/core/text_style.py (TextStyle / default_style shape used by the loop)
    - .planning/phases/08-masker-selective-inpaint/08-PATTERNS.md file 4 (the extraction sketch)
  </read_first>
  <behavior>
    - Test: blk_list with in-bounds, out-of-bounds (negative x1, x2 beyond img_w), and zero-area (post-clamp x2<=x1) entries yields exactly the valid clamped PageBoxes with origin DETECTED and clamped integer coords.
    - Test: default_family=None yields style None; default_family="Arial" yields a default TextStyle for every box.
    - Test: the function does not import or touch Qt/canvas (it takes img_w/img_h ints).
  </behavior>
  <action>
    Create manga_ai_studio/core/detection_boxes.py (new module; numpy/PIL only — no Qt imports anywhere in the file). Implement `build_detected_pageboxes(blk_list, img_w: int, img_h: int, default_family: str | None = None) -> list[PageBox]` as the exact extraction of the V5 loop from main_window._build_detected_boxes (:3944-3970): per blk, `textblock_to_box(blk)` int coercion, per-edge clamp `min(max(coord, 0), img_w/img_h)` into a fresh vendored `Box`, drop boxes with `clamped.x2 <= clamped.x1 or clamped.y2 <= clamped.y1`, and construct `PageBox(box=clamped, origin=DETECTED, payload=blk, style=default_style(default_family) if default_family else None)`. Keep the module docstring citing the extraction source (GUI handler keeps its D-03/D-04 gate wrapper — 08-07 rewires it to call this).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_detection_boxes.py -q</automated>
  </verify>
  <done>
    - Source assertion: core/detection_boxes.py defines `def build_detected_pageboxes(blk_list, img_w: int, img_h: int, default_family=None)`.
    - Test command: pinned-interpreter pytest on tests/test_core/test_detection_boxes.py passes (clamp/drop/style behaviors).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: derive_page_mask_state + compose_auto_binary + dilate_auto_mask — the seam primitives</name>
  <files>manga_ai_studio/core/detection_boxes.py, tests/test_core/test_detection_boxes.py</files>
  <read_first>
    - panelcleaner/masker.py :63-104 (the canonical call sequence — binarize dither=NONE, mask_intersection, per-box pick_best_mask with masking/reference boxes, None/failed filtering)
    - panelcleaner/image_ops.py functions from Task 1 PLUS `Box.pad` in panelcleaner/structures.py (clamped growth)
    - .planning/phases/08-masker-selective-inpaint/08-RESEARCH.md §1.4 (minimal sequence), §2.4 (interaction corners), Pitfalls 13-7/13-8/13-9
    - panelcleaner/config.py MaskerConfig (all fields incl. mask_dilation_radius from 08-01)
  </read_first>
  <behavior>
    - Test: heatmap with content inside one box and outside all boxes -> auto binary contains ONLY the in-box content (D-02/MASK-05 discard); the out-of-box pixels appear in raw_binary but never in auto_binary.
    - Test: dilation radius 3 grows the auto content by ~3 px but CANNOT push it past the box border (dilate-then-intersect ordering); radius 0 leaves it unchanged; hand-set manual content is irrelevant to this function (D-07 is structural — the function never sees strokes).
    - Test: per-box results — a uniform-region box gets a non-None mask and a finite std_dev; a box with no heatmap content gets mask None and std_dev None (noise outcome); a textured-region box STILL gets a stored mask + std_dev above the threshold (gate decoupled — the fit ran gate-lifted).
    - Test: compose_auto_binary with threshold T — boxes with override None + std_dev <= T contribute their stored mask; override "always" contributes regardless of std_dev; override "never" contributes nothing; boxes with mask None contribute nothing.
    - Test: a page in boxes-mode with zero boxes yields an EMPTY auto binary; dilate_auto_mask(raw, r) on a raw binary returns grow-by-r (mask-only mode's full-heatmap path).
    - Test: user-origin boxes are fitted identically (a USER PageBox over heatmap content receives a fit).
  </behavior>
  <action>
    In core/detection_boxes.py add:

    `@dataclass BoxMaskFit: mask: PIL.Image.Image | None; std_dev: float | None` and `@dataclass PageMaskDerivation: raw_binary: np.ndarray; auto_binary: np.ndarray; fits: dict[int, BoxMaskFit]` (keyed by `id(pagebox)` — callers apply within the same object lifetime).

    `derive_page_mask_state(image_rgb: np.ndarray, heatmap: np.ndarray, boxes: list[PageBox], masker_conf, dilation_radius: int) -> PageMaskDerivation`. Sequence (the vendored masker.py:63-104 call adapted per RESEARCH §1.4): (1) binarize the heatmap — `np.where(heatmap > 0, 255, 0).astype(np.uint8)` -> PIL -> `.convert("1", dither=Image.NONE)` (Pitfall 13-7: an explicit threshold, NEVER a dithered convert); this pre-dilation binary is `raw_binary` (D-08 retention data). (2) `dilated = ops.grow_mask(detected, dilation_radius)` when radius > 0 else `detected`. (3) Build `box_union` = union of ALL boxes (user + detected — all boxes certify) as a mode-"1" PIL image via ImageDraw rectangles over `pb.box.as_tuple`. (4) `cut = ops.mask_intersection(dilated, box_union)` — dilate-then-intersect so auto content can never exit a box (MASK-05 literal). (5) Per box: `reference_box = pb.box.pad(masker_conf.mask_growth_step_pixels * masker_conf.mask_growth_steps, image_size)`; call `ops.pick_best_mask(base=page_pil, precise_mask=cut, box_mask=box_union, masking_box=pb.box, reference_box=reference_box, masker_conf=fitting_conf, analytics_page_path=None-safe-path)` where `fitting_conf = attrs.evolve(masker_conf, mask_max_standard_deviation=1e9)` — the GATE-LIFTED fit (P-5): best_mask and the honest measured std_dev are ALWAYS stored; the gate is applied downstream, so threshold changes never re-fit. A None result (blank precise mask in the box — noise, Pitfall 13-9) becomes BoxMaskFit(None, None). Crop `fit.best_mask` (sized to reference_box, origin `fit.mask_coords`) back to the masking box: translate `pb.box` into reference coordinates by subtracting `(reference_box.x1, reference_box.y1)` and use `ops.cut_out_mask` — store the box-cropped mode-"1" image plus `fit.analytics_std_deviation`. (6) `auto_binary` = compose of CONTRIBUTING boxes per the same gate as compose_auto_binary (call it — do not duplicate).

    `compose_auto_binary(boxes: list[PageBox], threshold: float, page_size: (w, h)) -> np.ndarray` — pure recomposition from STORED per-box masks: contributing = override == "always" OR (override is None AND std_dev is not None AND std_dev <= threshold AND pb.mask is not None with content); union via `ops.compose_masks(page_size, [(pb.mask, (pb.box.x1, pb.box.y1)) for contributing])` -> numpy 0/255. This is what threshold changes and override flips call (no fitting).

    `dilate_auto_mask(raw_binary: np.ndarray, radius: int) -> np.ndarray` — the mask-only-mode path: `ops.grow_mask` on the raw binary (D-03 keeps the full heatmap; MASK-01 still applies).

    Guard every per-box call against `BlankMaskError` (image_ops.py:513-515) by pre-checking `cut.getbbox()` overlap with the box — a box with no detected content maps to BoxMaskFit(None, None), never an exception (Pitfall 13-8).

    Extend tests/test_core/test_detection_boxes.py with the behavior block above (synthetic pages: uniform fill + black text-like strokes in one box region; a high-contrast stripe region in another).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_detection_boxes.py tests/test_core/test_masker_machinery.py -q</automated>
  </verify>
  <done>
    - Source assertion: core/detection_boxes.py defines `derive_page_mask_state`, `compose_auto_binary`, `dilate_auto_mask`, `BoxMaskFit`, `PageMaskDerivation`; the module has no Qt/torch imports.
    - Source assertion: the binarize step contains `dither=Image.NONE`; the discard step calls `mask_intersection`; the fit step evolves `mask_max_standard_deviation`.
    - Test command: pinned-interpreter pytest on the two new test files passes, including: out-of-box discard, dilation-clamped-at-box-border, gate-decoupled fit storage, compose gate matrix (always/never/auto), zero-boxes empty auto.
    - Full-suite run green (pinned interpreter).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CTD model output (heatmap, blk_list) → seam core | Untrusted numeric model output crossing into geometry/allocation (the V5 discipline from Phase 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Tampering | heatmap/blk coords | medium | mitigate | Heatmap is explicitly thresholded (>0) before any PIL conversion; blk coords pass through the V5 int-coercion + per-edge clamp + zero-area drop (build_detected_pageboxes); reference boxes clamp via Box.pad(canvas_size). No allocation is sized from unvalidated dims. |
| T-08-05 | DoS | per-box fitting cost | low | mitigate | Fits run on box-sized cutouts (bounded by box area); mask_selection_fast short-circuits when enabled; a page with pathological box counts is bounded by the page itself. |
</threat_model>

<verification>
- Pinned-interpreter full suite green.
- The D-02 discard and the gate-decoupling behaviors are locked by tests that 08-07/08-09 will rely on.
</verification>

<success_criteria>
- All acceptance criteria hold; the module is importable headless (no Qt/torch in the import graph).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 08-03)

New symbols (all in the new manga_ai_studio/core/detection_boxes.py):
- `build_detected_pageboxes(blk_list, img_w, img_h, default_family=None) -> list[PageBox]`
- `derive_page_mask_state(image_rgb, heatmap, boxes, masker_conf, dilation_radius) -> PageMaskDerivation`
- `compose_auto_binary(boxes, threshold, page_size) -> np.ndarray`
- `dilate_auto_mask(raw_binary, radius) -> np.ndarray`
- `BoxMaskFit` dataclass (`mask: PIL Image | None`, `std_dev: float | None`)
- `PageMaskDerivation` dataclass (`raw_binary`, `auto_binary`, `fits: dict[int, BoxMaskFit]`)
- tests/test_core/test_masker_machinery.py, tests/test_core/test_detection_boxes.py (new test files)
</artifacts>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-03-SUMMARY.md` when done
</output>
