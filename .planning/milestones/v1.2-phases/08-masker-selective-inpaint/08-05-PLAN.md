---
phase: 08-masker-selective-inpaint
plan: 05
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - manga_ai_studio/gui/tools_panel.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_detection_settings.py
autonomous: true
requirements: [MASK-01, MASK-02]
must_haves:
  truths:
    - "The right-side Tools dock shows a 'Detection settings' section holding the relocated Detect Boxes toggle and all nine masker parameters (D-05/D-06), each with its PanelCleaner INI-comment tooltip"
    - "Control values always equal the live profile object's values (populated at startup, updated on change, persisted to the profile INI on change — D-10)"
    - "The Detect Boxes toggle is gone from the Tools menu and persists as QSettings view-state (A7/A8)"
    - "Every new control is keyboard-reachable via the dock Tab chain (the 05-UI-SPEC contract)"
    - "The dock never clips at 1024x720 — the panel body wraps in a vertical-only QScrollArea (A11)"
  artifacts:
    - ToolsPanel "Detection settings" section (widgets + signals)
    - MainWindow commit handlers + _save_masker_profile helper
  key_links:
    - "dilation_changed / std_dev_threshold_changed / masker_params_changed / detect_boxes_changed signals -> MainWindow commit handlers (this plan persists; plan 08-07 adds the LIVE current-page application)"
    - "control <-> profile.masker binding (MaskerConfig incl. mask_dilation_radius from 08-01)"
---

<objective>
Build the detection-settings UI over the vendored MaskerConfig (D-05/D-06), persist through the profile INI (D-10), relocate the Detect Boxes toggle out of the Tools menu (A8), and fix the startup persistence chain.

Purpose: MASK-01's radius and MASK-02's gate threshold need user-facing controls modeled on how PanelCleaner presents the same parameters, before the seam plan (08-07) can wire live re-dilate/gate behavior to them.

Output: ToolsPanel section + signals, MainWindow wiring + save-through, QSettings toggle state, startup load already delivered by 08-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-RESEARCH.md (§4 — config surface + UI shape)
@.planning/phases/08-masker-selective-inpaint/08-PATTERNS.md (file 5 + file 13)
@.planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §36 + §Copywriting rows (layout, ranges, tooltips — executor-authoritative, copy verbatim)
panelcleaner/config.py (:553-673 — MaskerConfig + the INI comment strings that are the tooltip source)
manga_ai_studio/gui/tools_panel.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToolsPanel detection-settings section</name>
  <files>manga_ai_studio/gui/tools_panel.py, tests/test_gui_detection_settings.py</files>
  <read_first>
    - manga_ai_studio/gui/tools_panel.py (whole file — tool row + brush row layout :99-202, class-scope Signals :89-93, slider/spin sync :275-288, programmatic set :290-298)
    - .planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §36 (the exact section layout diagram + ranges/defaults) and the Copywriting table rows for every control (tooltips verbatim)
    - panelcleaner/config.py :576-640 (MaskerConfig.export_to_conf comments — the canonical parameter descriptions)
  </read_first>
  <action>
    Wrap the panel body in a vertical-only QScrollArea (A11): the tool row and brush row stay visually at top, `root.addStretch(1)` (:202) is kept inside the scroll content. Below the brush row add the section per UI-SPEC §36: a 1px `#3a3a42` divider (QFrame.HLine), the "Detection settings" header (12px Semibold, muted `#9a9aa2`), then QFormLayout rows (row spacing 6px):

    1. "Detect Boxes" QCheckBox (default checked) — the relocated toggle (D-05).
    2. "Dilation radius" — QSlider(0..10) + QSpinBox(0..10, suffix " px"), mirror-synced via the brush-row blockSignals pattern (:275-288); default 2 (D-09).
    3. "Std-dev threshold" QDoubleSpinBox(0..100, step 0.5, 1 decimal, default 15).
    4. "Growth step" QSpinBox(0..20, suffix " px", default 2).
    5. "Growth steps" QSpinBox(0..50, default 11).
    6. "Min thickness" QSpinBox(0..50, suffix " px", default 4).
    7. "Off-white threshold" QSpinBox(0..255, default 240).
    8. "Improvement threshold" QDoubleSpinBox(0..1, step 0.01, 2 decimals, default 0.10).
    9. "Allow colored masks" QCheckBox (default checked).
    10. "Fast mask selection" QCheckBox (default unchecked).

    Set every tooltip verbatim from the 08-UI-SPEC Copywriting table (which adapts the INI comments and appends the live/next-detect clauses: radius + std-dev threshold are LIVE, the seven fit params say "Applies on the next Detect Text").

    Extend the four paint-tool button tooltips — UI-SPEC §Copywriting row "Tool tooltips — paint tools (D-15, EXTENDED)"; THIS task owns that row (tools_panel.py owns the tool buttons; no other Phase 8 plan touches them). The `_make_tool_action` texts at :113-123 ("Brush tool (B)" / "Rectangle tool (R)" / "Lasso tool (L)" / "Eraser tool (E)" — the text doubles as the tooltip) gain the D-15 clauses. Brush verbatim from the spec: "Brush tool (B) — paints under text boxes; hold Alt to select or move a box."; the other three follow the same suffix pattern, giving "Rectangle tool (R) — paints under text boxes; hold Alt to select or move a box.", "Lasso tool (L) — paints under text boxes; hold Alt to select or move a box.", and "Eraser tool (E) — paints under text boxes; hold Alt to select or move a box.". Crop's tooltip is unchanged (D-17 — crop keeps today's box behavior: presses on boxes still select).

    Add class-scope Signals (the :89-93 convention): `detect_boxes_changed = Signal(bool)`, `dilation_changed = Signal(int)`, `std_dev_threshold_changed = Signal(float)`, `masker_params_changed = Signal()`. The checkbox emits detect_boxes_changed on toggled; the radius pair emits dilation_changed (single emission per change — the mirror writes with blockSignals); the threshold spin emits std_dev_threshold_changed; the seven fit controls each emit masker_params_changed (they share one persist + apply-next-detect fate).

    Add `set_masker_values(masker_conf, detect_boxes: bool)` — programmatic population with blockSignals (the set_brush_size :290-298 precedent) so MainWindow can load profile values at startup without re-emission.

    Create tests/test_gui_detection_settings.py (gui marker, pytest-qt) IN THIS TASK with the widget-level tests (no MainWindow needed — construct ToolsPanel directly): set_masker_values population without emission, the slider/spin mirror's single emission, and the Tab-chain focus traversal. Task 2 extends this file with the MainWindow wiring tests.
  </action>
  <behavior>
    - Test: constructing ToolsPanel and calling set_masker_values with a MaskerConfig(mask_dilation_radius=5, mask_max_standard_deviation=20.0) sets the radius spin to 5 and threshold spin to 20.0 WITHOUT emitting dilation_changed/std_dev_threshold_changed (spy count 0).
    - Test: moving the radius slider to 7 emits dilation_changed(7) exactly once and the spinbox shows 7 (and vice versa from the spinbox).
    - Test: all ten controls are focusable and in the Tab chain (QWidget.focusWidget traversal from the panel finds each).
    - Test: the four paint-tool buttons' tooltips read exactly "Brush tool (B) — paints under text boxes; hold Alt to select or move a box." / "Rectangle tool (R) — paints under text boxes; hold Alt to select or move a box." / "Lasso tool (L) — paints under text boxes; hold Alt to select or move a box." / "Eraser tool (E) — paints under text boxes; hold Alt to select or move a box."; Crop's tooltip is byte-identical to its pre-Phase-8 text.
  </behavior>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui_detection_settings.py -q</automated>
  </verify>
  <done>
    - Source assertion: tools_panel.py declares the four new Signals and `def set_masker_values(self, masker_conf, detect_boxes)`.
    - Source assertion: the four paint-tool `_make_tool_action` texts each carry the clause "— paints under text boxes; hold Alt to select or move a box." (UI-SPEC Copywriting row "Tool tooltips — paint tools (D-15, EXTENDED)"); the Crop action text is unchanged.
    - GUI test: constructing ToolsPanel and calling set_masker_values with a MaskerConfig(mask_dilation_radius=5, mask_max_standard_deviation=20.0) sets the radius spin to 5 and threshold spin to 20.0 WITHOUT emitting dilation_changed/std_dev_threshold_changed (spy count 0).
    - GUI test: moving the radius slider to 7 emits dilation_changed(7) exactly once and the spinbox shows 7 (and vice versa from the spinbox).
    - GUI test: all ten controls are focusable and in the Tab chain (QWidget.focusWidget traversal from the panel finds each).
  </done>
</task>

<task type="auto">
  <name>Task 2: MainWindow wiring — toggle relocation, persistence, save-through</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_detection_settings.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py :788-794 (action_detect_boxes_mode declaration), :923 (tools_menu.addAction — the line to remove), :1883-1897 (the QSettings reader pattern for view-state), :3694/3820/4120/4146 (profile access pattern `self.profile_manager.config.current_profile`)
    - manga_ai_studio/config/profile_manager.py (save_profile signature)
    - manga_ai_studio/gui/tools_panel.py (as modified by Task 1)
    - .planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §36 persistence bullet + A7/A8
  </read_first>
  <action>
    Remove the `tools_menu.addAction(self.action_detect_boxes_mode)` line (:923) — the dock checkbox is the single user-facing control (A8). KEEP `action_detect_boxes_mode` alive as the state holder (its isChecked() is still read by the detection paths): sync bidirectionally — tools_panel.detect_boxes_changed -> a `_on_detect_boxes_changed(checked)` handler that sets `self.action_detect_boxes_mode.setChecked(checked)` and writes QSettings key `"detectBoxesMode"`; an action.toggled connection back that re-populates the checkbox with blockSignals (no feedback loop). At startup, read `"detectBoxesMode"` (default True) from `self._settings` (the :1883-1897 pattern) and apply to both the action and the checkbox via set_masker_values.

    Add a `_save_masker_profile()` helper: `self.profile_manager.save_profile(self.profile_manager.config.current_profile, "default")` wrapped in try/except OSError with a logger.warning (a read-only config dir must not crash the app). Wire the four signals:
    - `_on_dilation_changed(value: int)`: set `profile.masker.mask_dilation_radius = value`, call `_save_masker_profile()`. (The LIVE current-page re-dilate is added by plan 08-07 — this plan persists only; note the extension point with a comment.)
    - `_on_std_dev_threshold_changed(value: float)`: set `profile.masker.mask_max_standard_deviation = value`, `_save_masker_profile()`. (Same 08-07 extension note.)
    - `_on_masker_params_changed()`: copy the seven fit controls' values into the profile fields (mask_growth_step_pixels, mask_growth_steps, min_mask_thickness, off_white_max_threshold, mask_improvement_threshold, allow_colored_masks, mask_selection_fast) via one `masker_values()` reader on ToolsPanel, then `_save_masker_profile()` once.
    - `_on_detect_boxes_changed(checked: bool)`: as above.

    At startup, after building the tools panel, call `tools_panel.set_masker_values(self.profile_manager.config.current_profile.masker, <QSettings detectBoxesMode>)` so controls render the persisted profile (08-01's load_profile made it non-default).

    Extend tests/test_gui_detection_settings.py (created by Task 1) with the MainWindow wiring tests: MainWindow construction with a tmp-path ProfileManager; simulate a dilation change and assert the profile object updated AND the INI on disk round-trips (re-load via a second ProfileManager on the same dir); assert the Tools menu no longer contains a "Detect Boxes" action while `action_detect_boxes_mode` still toggles; assert the checkbox toggling flips the action and persists to QSettings (isolate settings via the _isolate_settings pattern, tests/test_gui_detection_boxes.py:75-88).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui_detection_settings.py -q</automated>
  </verify>
  <done>
    - Source assertion: the string `tools_menu.addAction(self.action_detect_boxes_mode)` no longer appears in main_window.py; a `_save_masker_profile` method exists.
    - GUI test: dilation change updates profile + INI round-trip; Detect Boxes absent from the Tools menu; checkbox <-> action sync both directions.
    - Full suite green (pinned interpreter).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile INI ↔ controls | User-editable config round-trip (existing boundary; new keys join via 08-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Tampering | spinbox/slider ranges | low | mitigate | Every control is range-clamped at the widget level (declared ranges in §36) — invalid values are unreachable; profile loads fall back to defaults on coercion failure (vendored behavior). |
| T-08-10 | DoS | save-on-change frequency | low | accept | Saves are user-paced preference commits of a small INI; atomic via Profile.safe_write. |
</threat_model>

<verification>
- Pinned-interpreter full suite green.
- Controls render persisted values at startup (populated truth from UI-SPEC §UI Considerations).
</verification>

<success_criteria>
- All acceptance criteria hold; no detection-behavior change yet (that is 08-07 — the signals persist only).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 08-05)

New symbols:
- ToolsPanel signals: `detect_boxes_changed = Signal(bool)`, `dilation_changed = Signal(int)`, `std_dev_threshold_changed = Signal(float)`, `masker_params_changed = Signal()`
- ToolsPanel widgets: detect-section checkbox/slider/spins (`dilation_slider`, `dilation_spinbox`, `std_dev_threshold_spin`, growth/thickness/off-white/improvement spins, `allow_colored_check`, `fast_selection_check`), the QScrollArea wrap
- `ToolsPanel.set_masker_values(masker_conf, detect_boxes)` + `ToolsPanel.masker_values() -> dict`
- MainWindow: `_save_masker_profile()`, `_on_detect_boxes_changed`, `_on_dilation_changed`, `_on_std_dev_threshold_changed`, `_on_masker_params_changed`
- QSettings key `"detectBoxesMode"`
- tests/test_gui_detection_settings.py (new test file)
</artifacts>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-05-SUMMARY.md` when done
</output>
