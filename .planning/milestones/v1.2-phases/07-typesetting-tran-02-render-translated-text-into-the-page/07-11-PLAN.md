---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 11
type: execute
wave: 4
depends_on: [07-09]
files_modified:
  - manga_ai_studio/core/text_style.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/inspector_panel.py
  - tests/test_core/test_text_style.py
  - tests/test_gui_inspector_styling.py
  - tests/test_gui_detection_boxes.py
autonomous: true
gap_closure: true
gap_ids: [G-07-3]
requirements:
  - TRAN-02
assumption_delta:
  decision: no-change
  rationale: "G-07-3 adds an app-level default font per the user report ('it should let me select a default font'). The prescribed pieces: QSettings key 'defaultFontFamily' via the existing _settings() helper (main_window.py:1873), a default_style() factory in core/text_style.py (Qt-free — the family is passed in, QSettings stays GUI-side), and application at the two new-box creation sites (canvas._commit_create :2104, main_window._build_detected_boxes :3903). A minimal 'Set as Default Font' affordance in the Inspector font row is the writer for the key (without it the feature is dead — the user could never select anything). text_style.py stays Qt-free (core/ discipline — the factory takes the family as a parameter)."
estimate:
  tokens: 19500
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A user-selected default font family persists across sessions (QSettings 'defaultFontFamily', the same _settings() store as the recents) and is selectable from the Inspector font row"
    - "New user-drawn boxes (canvas._commit_create) and newly detected boxes (main_window._build_detected_boxes) are born with the saved family; with no saved family they keep style None and the renderer's TextStyle() defaults (Liberation Sans) apply"
    - "core/text_style.py stays Qt-free: default_style(font_family) is a pure factory; the QSettings read lives in MainWindow"
  artifacts:
    - manga_ai_studio/core/text_style.py :: default_style(font_family: str | None = None) -> TextStyle (None -> DEFAULT_FONT_FAMILY; every other field == the defaults)
    - manga_ai_studio/gui/main_window.py :: _default_font_family() reader (self._settings().value('defaultFontFamily', '')), _on_inspector_default_font_requested(family) writer + transient status, _build_detected_boxes (:3903-3905) passes style=default_style(fam) if fam else None (fam = self._default_font_family(); empty -> None -> renderer defaults); canvas.new_box_style_provider wiring
    - manga_ai_studio/gui/canvas.py :: new_box_style_provider attribute + _commit_create (:2104-2108) applies it (fallback TextStyle())
    - manga_ai_studio/gui/inspector_panel.py :: a 'Set as Default Font' affordance on the Font row + the default_font_requested = Signal(str)
    - tests/test_core/test_text_style.py :: default_style factory tests
    - tests/test_gui_inspector_styling.py :: the Set-as-default button writes the key (isolated QSettings) + the user-box application test
    - tests/test_gui_detection_boxes.py :: the detected-box application test (mocked-detection seam)
  key_links:
    - "_settings() 'defaultFontFamily' <-> _default_font_family() <-> default_style(family): the persistence, the read, and the factory are one chain — the family string never diverges"
    - "canvas.new_box_style_provider <-> _commit_create: the canvas is event-driven (no args on the create path), so the provider callable is the seam (the is-primary-owner precedent)"
  prohibitions:
    - "text_style.py must not import Qt (QSettings stays in MainWindow; the factory takes the family as a parameter)"
    - "Existing boxes are never re-styled by the default change — the default applies to NEW boxes only (per-box flat styles, D-06)"
    - "The recents QSettings convention is reused, not duplicated: _settings() is the only QSettings accessor"
---

<objective>
Close **G-07-3 (major)** from 07-UAT.md: "it should let me select a default font". Root cause (already diagnosed): no app-level default font exists — `TextStyle()` hardcodes 'Liberation Sans' (text_style.py:32,131); new user boxes (canvas.py:2104-2108) and detected boxes (main_window.py:3903-3905) fall back to defaults. QSettings (`_settings()`, main_window.py:1873) is used only for recents. Fix: QSettings key 'defaultFontFamily' + a `default_style()` factory in text_style.py + application at the two new-box creation sites, with a minimal Inspector affordance to set the key (the user's 'select' action).

Purpose: new boxes inherit the user's chosen default font — the same choice every session, applied to user-drawn and detected boxes alike.

Output: the factory, the QSettings persistence chain, the Set-as-Default affordance, and application at both creation sites.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UAT.md
@manga_ai_studio/core/text_style.py
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/inspector_panel.py
@tests/test_core/test_text_style.py
@tests/test_gui_inspector_styling.py
@tests/test_gui_detection_boxes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN — default_style() factory + the QSettings chain + the Set-as-Default affordance</name>
  <files>manga_ai_studio/core/text_style.py, manga_ai_studio/gui/inspector_panel.py, manga_ai_studio/gui/main_window.py, tests/test_core/test_text_style.py, tests/test_gui_inspector_styling.py</files>
  <read_first>
    - manga_ai_studio/core/text_style.py — the module header (Qt-free discipline: stdlib only), DEFAULT_FONT_FAMILY (:32), the TextStyle dataclass (:107-142)
    - manga_ai_studio/gui/inspector_panel.py — the Font row (:367-371: `self.font_combo = QFontComboBox()`, `form.addRow("Font", self.font_combo)`), the class-scope Signal declarations (:282 area), the QSS block for the styling widgets (:125-133), the commit-handler wiring pattern
    - manga_ai_studio/gui/main_window.py — _settings() (:1873-1876), the recents write pattern (:1890-1895: `self._settings().setValue(...)`), _show_transient_status (:3484+), the inspector commit wiring (where the panel's signals connect)
    - tests/test_core/test_text_style.py — the existing factory/round-trip test conventions
    - tests/test_gui_inspector_styling.py — the panel fixture (_make_inspector) + the _isolate-settings convention from tests/test_gui_project.py (_isolate_settings: monkeypatch window._settings to a throwaway INI, test_gui_project.py:61-67)
  </read_first>
  <behavior>
    - Test 1 (RED gate, unit): `default_style(None)` returns TextStyle() with font_family == DEFAULT_FONT_FAMILY; `default_style("Yu Gothic UI")` returns the default style with font_family == 'Yu Gothic UI' and every other field equal to the TextStyle() defaults. Fails pre-fix (no factory exists).
    - Test 2 (RED gate, GUI): clicking the Set-as-Default affordance in the Font row emits `default_font_requested` with the current font combo family; the MainWindow handler writes QSettings 'defaultFontFamily' (isolated INI) and shows the transient status.
  </behavior>
  <action>RED: add the factory tests to tests/test_core/test_text_style.py and the affordance test to tests/test_gui_inspector_styling.py (drive the button click, capture the signal via the connect driver, then the window-level handler with an isolated QSettings via the test_gui_project._isolate_settings pattern). Run — must fail (no factory, no signal). GREEN, three production edits: (1) core/text_style.py — add `def default_style(font_family: str | None = None) -> TextStyle: return TextStyle(font_family=font_family or DEFAULT_FONT_FAMILY)` (pure, Qt-free, near DEFAULT_FONT_FAMILY). (2) inspector_panel.py — add `default_font_requested = Signal(str)` to the class-scope signals and a compact 'Set as Default Font' affordance on the Font row (a small QToolButton with a tooltip 'Use this font for new boxes' — QSS-consistent, reuse the existing styling-widget tokens; emitting the CURRENT font combo family on click; disabled in the empty state like the other styling controls). (3) main_window.py — `_default_font_family()` reading `str(self._settings().value("defaultFontFamily", "") or "")` (empty -> None -> factory default), `_on_inspector_default_font_requested(family)` writing `self._settings().setValue("defaultFontFamily", family)` + `_show_transient_status(f"Default font: {family}")` + wiring the panel signal. Run — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_text_style.py tests/test_gui_inspector_styling.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_default_style factory tests FAIL pre-fix (no factory) and PASS post-fix
    - The Set-as-Default click emits the family; the MainWindow handler writes the isolated QSettings key + flashes the status
    - text_style.py imports stay stdlib-only (no Qt import added — the core/ discipline)
    - Grep gate: `grep -c 'def default_style' manga_ai_studio/core/text_style.py` == 1
    - Grep gate: `grep -c '"defaultFontFamily"' manga_ai_studio/gui/main_window.py` >= 2 (reader + writer)
  </acceptance_criteria>
  <done>The factory exists (Qt-free), the affordance emits the family, and the MainWindow persists it under 'defaultFontFamily' in the shared _settings() store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-GREEN — apply the default at both new-box creation sites</name>
  <files>manga_ai_studio/gui/canvas.py, manga_ai_studio/gui/main_window.py, tests/test_gui_inspector_styling.py, tests/test_gui_detection_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/canvas.py — _commit_create (:2089-2132: the PageBox construction at :2104-2108), __init__ (where the provider attribute initializes near _box_items), the is-primary-owner provider precedent (set_primary_owner / _weakref at :1660-1662)
    - manga_ai_studio/gui/main_window.py — _build_detected_boxes (:3845+, the PageBox construction at :3903-3905), the canvas construction/wiring site (where set_primary_owner is called — the new_box_style_provider is set beside it)
    - tests/test_gui_inspector_styling.py — the window-level box tests (_window_with_box helpers, the create-flow driving convention)
    - tests/test_gui_detection_boxes.py — the mocked-detection seam (result = {"mask": ..., "blocks": [_blk(...)]} driving _on_detection_finished)
  </read_first>
  <behavior>
    - Test 1 (RED gate): with the isolated QSettings key set to 'Yu Gothic UI', creating a user box (drive the existing create flow) yields `item.pagebox.style.font_family == 'Yu Gothic UI'`; with the key absent, a new box has `style is None` (the renderer defaults apply). Fails pre-fix (the created box always has style None).
    - Test 2 (RED gate): running the mocked detection with the key set yields detected PageBoxes whose style.font_family == 'Yu Gothic UI'.
  </behavior>
  <action>RED: add the two application tests per the behavior block (isolated QSettings: set the key via `window._settings().setValue("defaultFontFamily", "Yu Gothic UI")` after _isolate_settings; drive the create flow with the existing helper and the detection seam with the mocked result dict). Run — must fail (styles absent). GREEN, two production edits: (1) canvas.py — add `self.new_box_style_provider = None` in __init__ (a `Callable[[], TextStyle] | None`); in `_commit_create`, build the user pagebox with `style=self.new_box_style_provider() if self.new_box_style_provider is not None else None` (None keeps the today behavior — the renderer falls back to TextStyle()). (2) main_window.py — where the canvas is constructed/wired, set `canvas.new_box_style_provider = lambda: default_style(self._default_font_family()) if self._default_font_family() else None` (import default_style from core.text_style; the provider returns None when no family is saved — a no-key box keeps style None and the renderer's TextStyle() defaults apply, matching today's behavior so the existing box tests pass UNCHANGED); in `_build_detected_boxes` (:3903-3905), construct detected pageboxes with `fam = self._default_font_family()` and `style = default_style(fam) if fam else None` (the same empty-family contract). Do NOT touch existing boxes (D-06 flat per-box styles — the default applies to new boxes only). Run — must pass, then the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py tests/test_gui_detection_boxes.py tests/test_gui_boxes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Both application tests FAIL pre-fix (no style applied) and PASS post-fix
    - The no-key case is unchanged: user boxes keep style None, detected boxes keep style None (renderer defaults) — the existing box tests pass UNCHANGED
    - Existing boxes are never re-styled (no code path touches a live box's style in this plan)
    - Grep gate: `grep -c 'new_box_style_provider' manga_ai_studio/gui/canvas.py` >= 2 (init + _commit_create)
    - Grep gate: `grep -c 'default_style(' manga_ai_studio/gui/main_window.py` >= 2 (provider wiring + _build_detected_boxes)
    - Full suite green at the re-baselined count (687 baseline from 07-VERIFICATION.md + ~4 new tests = 698 passed, 0 failed)
  </acceptance_criteria>
  <done>New user-drawn and detected boxes are born with the saved default family; with no saved family they keep style None and the renderer's TextStyle() defaults (Liberation Sans) apply; existing boxes untouched; full suite green.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/core/text_style.py`:
  - `default_style(font_family: str | None = None) -> TextStyle` — the Qt-free factory (None -> DEFAULT_FONT_FAMILY)
- `manga_ai_studio/gui/main_window.py`:
  - `_default_font_family()` — the QSettings 'defaultFontFamily' reader
  - `_on_inspector_default_font_requested(family)` — the writer + transient status
  - `_build_detected_boxes` — detected pageboxes carry `style=default_style(fam) if fam else None` (fam = `self._default_font_family()`; empty -> None -> renderer defaults)
  - canvas wiring — `canvas.new_box_style_provider = lambda: default_style(self._default_font_family()) if self._default_font_family() else None` (empty family -> None)
- `manga_ai_studio/gui/canvas.py`:
  - `new_box_style_provider` attribute (None default) + `_commit_create` applies it
- `manga_ai_studio/gui/inspector_panel.py`:
  - `default_font_requested = Signal(str)` + the 'Set as Default Font' affordance on the Font row
- Tests: `default_style` factory tests (test_text_style.py), the affordance/persistence test + user-box application test (test_gui_inspector_styling.py), the detected-box application test (test_gui_detection_boxes.py)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QSettings store → new-box style | The persisted family string crosses into TextStyle construction at two creation sites; the family is bounded at the source (the QFontComboBox's real family list) and coerced to a string on read |
| Inspector affordance → QSettings | The Set-as-Default click writes the registry/INI store; the value is a font family string, not a path or executable input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-18 | Tampering | QSettings 'defaultFontFamily' read | low | mitigate | `str(... or "")` coercion on read; an empty/absent value -> None -> the factory default (Liberation Sans); the family only ever feeds QFont rendering (V5-bounded: the value originates from the QFontComboBox family list, never free text) |
| T-07-19 | Spoofing | new-box style application | medium | mitigate | `default_style` is the single factory both creation sites use — the same spelling the Inspector shows; the provider seam keeps the canvas Qt-free of QSettings (no second QSettings accessor) |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_text_style.py tests/test_gui_inspector_styling.py tests/test_gui_detection_boxes.py -x -q` — affected modules green (RED gates proven RED→GREEN)
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 698 passed, 0 failed (687 baseline from 07-VERIFICATION.md + ~4 new)
- Grep gates: `def default_style` x1 in text_style.py; `"defaultFontFamily"` x2+ in main_window.py; `new_box_style_provider` x2+ in canvas.py; `default_style(` x2+ in main_window.py
- Qt-free gate: text_style.py imports unchanged (stdlib/dataclasses only)
</verification>

<success_criteria>
- The user can select a default font (Inspector affordance → persistent QSettings key), and every new user-drawn or detected box is born with it (no saved family → style stays None, the renderer's TextStyle() defaults apply)
- Existing boxes are untouched (D-06 flat styles); full suite green at 698
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-11-SUMMARY.md` when done
</output>
