---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - manga_ai_studio/gui/text_renderer.py
  - tests/test_core/test_typeset_layout.py
  - tests/test_core/test_typeset_effects.py
autonomous: true
requirements:
  - TRAN-02
estimate:
  tokens: 45000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "layout_vertical produces per-char placements per the D-11 contract: Han/Kana and vertical-form punctuation (。．，、：；！？・) stay UPRIGHT; halfwidth ASCII (0x21..0x7E) plus the bracket/dash set (「」『』（）《》〈〉【】—…～-( )) rotate 90°; columns flow top-to-bottom and right-to-left (later characters at smaller x), wrap at inner height, column width 1 em, gap 0, per-char centering within the column (W3C + BallonsTranslator conventions)"
    - "Vertical paint rotates ONLY the classified runs 90° clockwise (painter.translate + rotate(90) + drawText per run — RESEARCH Common Operation 3) — never the whole block (D-11 rejects QPainter.rotate on the block); upright runs draw unrotated; Auto-fit in vertical mode fits via column count (floor(inner_w / 1em)) and the run's vertical extent with the same bounded loop (A8)"
    - "Outer glow renders as a soft halo BEHIND the glyphs (silhouette alpha → numpy stack blur → colorize → CompositionMode_DestinationOver at zero offset); drop shadow is the same silhouette pass translated by the style's dx/dy; both skip when disabled (D-14)"
    - "Effect geometry expands the item's bounding rect (effect padding = outline half-width + blur radius + |offset|); effect allocation is BOUNDED (max surface dimension + pixel budget) — an oversized request degrades to no-glow with a loguru warning, never OOM (T-07-07)"
    - "Effects compose with BOTH horizontal and vertical layouts through one shared pass code path (D-01 — canvas ≡ bake)"
  artifacts:
    - manga_ai_studio/gui/text_renderer.py :: layout_vertical + char_rotates + vertical classification constants (_ASCII_ROTATE/_ROTATE_EXTRA/_ALIGN_CENTER) + rotated-run paint + glow/shadow effect passes + effect padding + bounded allocation
    - tests/test_core/test_typeset_layout.py :: test_vertical_* geometry tests
    - tests/test_core/test_typeset_effects.py :: outline/glow/shadow pixel tests + padding + allocation-bound degrade
  key_links:
    - "layout_vertical ← the overlay/bake vertical flag (style.vertical or payload.vertical) — wired together in plan 07-05 so canvas and bake flip atomically (no divergence window)"
    - "effect passes ← paint() — the single code path both the overlay and bake_typeset_page call"
  flagged_assumptions:
    - "Text length/width math operates on Unicode code points (Python str semantics), never bytes — no multi-byte truncation in the tategaki layout or fit-in-box loops (probe edge: encoding)"
  backstops:
    - statement: "vertical boxes render upright CJK glyphs in RTL columns with rotated Latin/numbers"
      verification: backstop
    - statement: "effects look clean and the canvas stays fluid while dragging a selected box"
      verification: backstop
  prohibitions:
    - "OCR/translation text is never rendered via setHtml or rich-text — plain-text rendering only (ASVS V5; QTextLayout plain runs / setPlainText)"
    - "TextStyle is never mutated in place — every style change assigns a fresh instance via dataclasses.replace; snapshots detach style (Pitfall 8/1); the Mixed sentinel never persists into a TextStyle"
    - "Vertical text is never whole-block rotated — tategaki is per-run layout; the bake composites text only (no chrome) onto a detached page copy (Pitfall 2)"
    - "Group/style ops push exactly ONE BOXES snapshot; overlay reposition never re-layouts during mousemove (RC-1 — setPos-only)"
---

<objective>
Build the renderer depth the tracer plan deliberately left flat: the **true tategaki vertical path** (D-11 — the phase's highest-risk decision) and the **glow + drop-shadow effect passes** (D-14) in `gui/text_renderer.py`, both headless-testable. Vertical boxes become geometrically correct (upright CJK, RTL columns, rotated Latin/numbers) and the renderer gains the effect machinery the Inspector controls expose in plan 07-05. No GUI wiring here — the vertical flag flips canvas AND bake together in 07-05 (no divergence window); effects stay OFF by default so nothing user-visible changes until the Inspector lands.

Purpose: tategaki and effects are pure geometry + pixel passes — the ideal parallel Wave-2 work. Correctness is pinned here by unit tests (classification membership, RTL column order, rotated advance, silhouette pixels) so 07-05 only wires flags and controls.

Output: `layout_vertical` + classification constants + rotated-run paint + glow/shadow silhouette passes + effect padding + bounded allocation in `gui/text_renderer.py`, with the geometry tests in `test_typeset_layout.py` and the pixel tests in the new `test_typeset_effects.py`.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-CONTEXT.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-RESEARCH.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-PATTERNS.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UI-SPEC.md
@manga_ai_studio/gui/text_renderer.py
@manga_ai_studio/gui/box_item.py
</context>
<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vertical layout geometry — classification, RTL columns, wrap, centering (D-11)</name>
  <files>manga_ai_studio/gui/text_renderer.py, tests/test_core/test_typeset_layout.py</files>
  <read_first>
    - manga_ai_studio/gui/text_renderer.py — the Task 1 layout()/LayoutResult from plan 07-01 (the vertical form extends it; keep the pure-geometry structure so a future document-layout wrapper can reuse it — RESEARCH A1 mitigation)
    - 07-RESEARCH.md — Pattern 2 (the full vertical contract: classification sets, column rules, W3C conventions), Common Operation 2 (the verbatim `_ASCII_ROTATE`/`_ROTATE_EXTRA`/`_ALIGN_CENTER` sets + `char_rotates` + `layout_vertical` signature), Pitfall 3 (the vertical-not-tategaki regression and its warning signs)
    - 07-PATTERNS.md — §`gui/text_renderer.py` (module shape: Qt-only imports, pure functions, the vertical classification constants block)
    - 07-UI-SPEC.md — surface 34 tategaki row (columns from the box's top-right inner edge, 1 em columns, gap 0, alignment semantics A3/A8) and the tategaki backstop row
  </read_first>
  <behavior>
    - Test 1 (classification): char_rotates("A") and char_rotates("1") and char_rotates("!") are True (halfwidth ASCII); char_rotates("「") and char_rotates("—") and char_rotates("…") are True (bracket/dash set); char_rotates("あ") and char_rotates("漢") and char_rotates("。") and char_rotates("：") are False (upright CJK/vertical-form)
    - Test 2 (RTL flow): for a CJK string long enough to wrap, later characters sit at smaller x than earlier characters of the wrapped line (columns advance right-to-left); the first column starts at the box's right inner edge
    - Test 3 (wrap at height): characters beyond inner_h start a new column; no placement exceeds inner_w on the left
    - Test 4 (rotated advance): a Latin run's per-char advance is measured along its HEIGHT (rotated), not its width; an upright CJK char's advance is its width
    - Test 5 (centering + alignment): each char is centered within its 1 em column; align_h right/center/left shifts the column BLOCK's x; align_v top/middle/bottom shifts the run along the column axis (A3)
    - Test 6 (vertical auto-fit): the vertical fit loop operates on column count (floor(inner_w / 1em)) and the run's vertical extent; a long text floors at the 5 px floor like the horizontal loop (A8)
    - Test 7 (encoding probe edge): a string with multi-byte CJK + ASCII indexes by str (code points) — len(text) placements, no byte-level truncation
  </behavior>
  <action>RED: write the seven test groups in tests/test_core/test_typeset_layout.py (pure geometry against `layout(..., vertical=True)` or a dedicated `layout_vertical` — assert positions as floats, never pixel rendering). GREEN: implement in gui/text_renderer.py per RESEARCH Pattern 2 + Common Operation 2: module constants `_ASCII_ROTATE = set(chr(i) for i in range(0x21, 0x7F))`, `_ROTATE_EXTRA = {"「","」","『","』","（","）","《","》","〈","〉","【","】","—","…","～","-","(",")"}`, `_ALIGN_CENTER = {"。","．","，","、","·","：","；","！","？"}`, the public `char_rotates(ch: str) -> bool`, and `layout_vertical(text, style, inner_w, inner_h)` returning per-char placements `[{char, x, y, rotate, w, h}, ...]`: columns top-to-bottom, flow right-to-left (first column per align_h: right/center/left), wrap when the column exceeds inner_h, column width = max char advance in the column (1 em basis), per-char centering, align_v offsets the run along the column axis; the vertical Auto-fit variant reuses the bounded loop semantics (12 iters × 0.9, 5 px floor) applied to column count + vertical extent. `layout()` with vertical=True returns the vertical placement form in LayoutResult. Docstring: name kumimoji compression, tate-chū-yoko combining, and RTL-script bottom-to-top flow as future polish (CONTEXT Deferred — render-only scope). Keep the functions pure (no QPainter calls inside layout) per RESEARCH A1 (a future document-layout wrapper can consume them).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_layout.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 asserts the exact membership of the three sets (all characters listed above)
    - Test 2 asserts x strictly decreases across the wrap boundary of a wrapped line (e.g. char[i+n].x < char[i].x for a column-wrapped pair) — the RTL direction pinned by test, not prose
    - Test 4 asserts a Latin char's placed height exceeds its width (rotated) and a CJK char's width exceeds or equals its height (upright)
    - Test 6 asserts the vertical fit loop terminates and respects the floor
    - The existing horizontal layout tests stay green (layout() vertical=False unchanged)
  </acceptance_criteria>
  <done>The vertical layout geometry — upright CJK, rotated Latin, RTL columns, wrap, centering, vertical Auto-fit — is locked by pure-geometry tests; committed as test-then-implement commits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Effects passes — outline stroke, glow + shadow silhouettes, padding, bounded allocation (D-14)</name>
  <files>manga_ai_studio/gui/text_renderer.py, tests/test_core/test_typeset_effects.py</files>
  <read_first>
    - manga_ai_studio/gui/text_renderer.py — the Task 1 paint() (outline via QPainterPath.addText + strokePath) and the vertical placements from Task 1 (effects must compose with both)
    - 07-RESEARCH.md — Pattern 3 (outline stroke; glow/shadow silhouette+blur passes with the BallonsTranslator composition order), Common Operation 5 (the numpy-blur + DestinationOver recipe with the mandatory `.copy()` detach), Pitfall 5 (effect OOM/jank — the bounded-allocation policy), Common Operation 4 (the stroke recipe)
    - 07-UI-SPEC.md — §Spacing exceptions (outline 0..10/2, glow 0..20/4, shadow offset 0..10/2), §Color effect defaults, the effects backstop row
    - manga_ai_studio/gui/box_item.py — `_OVERLAY_FIT_*` constants (unchanged reference) if the effect surface needs the same discipline
  </read_first>
  <behavior>
    - Test 1 (outline ring, horizontal): render a glyph with outline enabled at width 2 — pixels inside the glyph carry the fill color, pixels at the glyph edge carry the outline color, and the ring is absent with width 0
    - Test 2 (glow halo): with glow on (radius 6), alpha > 0 exists OUTSIDE the glyph's ink bbox within the radius band; the glyph fill pixels keep the fill color; with glow off, no pixels outside the ink bbox
    - Test 3 (shadow offset): with shadow on (dx 2, dy 2), silhouette pixels appear at the glyph position + (2,2); with an offset larger than the radius, no silhouette pixels at the un-offset position
    - Test 4 (padding): the LayoutResult/effect-surface rect expands by the padding formula (outline half-width + blur radius + |offset|) and the paint output extends beyond the glyph bbox by the expected margin
    - Test 5 (allocation bound): a pathological radius (e.g. 500 px on a full-page rect) degrades to NO glow — the paint succeeds, a loguru warning is emitted, and no exception/OOM occurs
    - Test 6 (vertical composition): a vertical layout with a rotated Latin run renders the outline around the rotated glyphs and the shadow at +dx/+dy — the same passes serve both orientations
  </behavior>
  <action>RED: write the six test groups in the new tests/test_core/test_typeset_effects.py (render via renderer.paint into a QImage under pytest-qt's qapp; assert pixels with tolerance for antialiasing — e.g. color-distance checks rather than exact equality at edges). GREEN: implement in gui/text_renderer.py per RESEARCH Pattern 3 + Common Operation 5: (1) outline — the existing glyph-path stroke with RoundCap/RoundJoin at style.outline width/color, skipped when disabled or width 0, applied per run for BOTH orientations (vertical rotated runs stroke along the rotated path); (2) glow — render the glyph silhouette (fill alpha) into an offscreen QImage, extract the alpha via numpy, stack-blur O(1)/px at style.glow.radius_px, colorize with style.glow.color × opacity, composite with CompositionMode_DestinationOver at zero offset; (3) shadow — the same silhouette-blur pass translated by style.shadow.dx/dy and composited DestinationOver with the shadow color/opacity; (4) padding — compute `effect_padding(style) = outline half-width + max(glow radius, shadow radius) + |max offset|` and expand the returned surface rect / bounding rect so neither canvas nor bake clips the halo (Pitfall 2 guard: the bake's page painter must draw the passes without clipping); (5) bounded allocation — constants for the max effect surface dimension and pixel budget (e.g. EFFECT_MAX_DIMENSION 4096 and EFFECT_MAX_PIXELS 64 MP — planner pins), oversize → skip glow/shadow + `loguru.logger.warning` (the BallonsTranslator EffectRasterAllocationError policy, T-07-07); every numpy↔QImage bridge detaches with .copy() (Pitfall 2). Run the new test file, then the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_effects.py tests/test_core/test_typeset_layout.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 asserts outline-colored pixels at the glyph edge and fill-colored pixels inside (width 2); width 0 renders no outline-colored pixels
    - Test 2 asserts glow pixels (glow color alpha > 0) within the radius band outside the ink bbox and none when disabled
    - Test 3 asserts shadow pixels at +(dx,dy) and none at the un-offset position when offset > radius
    - Test 5 asserts the paint call succeeds with the pathological radius and the warning is emitted (caplog) — no exception
    - Test 6 asserts outline + shadow pixels present on a vertical rotated-run render
    - The plan-07-01 bake equivalence test still passes (effects OFF → byte-identical output as before)
  </acceptance_criteria>
  <done>The three effects (outline configurable, glow, shadow) render via one shared pass path for both orientations, padding prevents clipping, and allocation is bounded with a degrade path — all locked by pixel tests; committed.</done>
</task>

</tasks>
## Artifacts this phase produces

- `manga_ai_studio/gui/text_renderer.py` (MOD):
  - `char_rotates(ch: str) -> bool` + classification constants `_ASCII_ROTATE` (0x21..0x7E), `_ROTATE_EXTRA` (brackets/dashes/ellipsis), `_ALIGN_CENTER` (vertical-form punctuation)
  - `layout_vertical(text, style, inner_w, inner_h) -> list[dict]` — per-char placements [{char, x, y, rotate, w, h}]; RTL columns, wrap at inner_h, 1 em columns, per-char centering, vertical Auto-fit variant
  - `layout()` vertical mode — LayoutResult carries the vertical placement form
  - Vertical paint: per-run `painter.translate + rotate(90) + drawText` for rotated runs, plain drawText for upright runs
  - Effect passes: `effect_padding(style) -> float` (outline half-width + blur radius + |offset|); glow + shadow silhouette passes (offscreen QImage → numpy alpha → stack blur → colorize × opacity → CompositionMode_DestinationOver at offset); bounded allocation constants (EFFECT_MAX_DIMENSION / EFFECT_MAX_PIXELS) with degrade-to-no-glow + loguru warning
- `tests/test_core/test_typeset_layout.py` (extended): test_vertical_classification, test_vertical_rtl_column_flow, test_vertical_wrap_at_height, test_vertical_rotated_advance, test_vertical_centering_and_alignment, test_vertical_auto_fit, test_vertical_code_point_indexing
- `tests/test_core/test_typeset_effects.py` (NEW): test_effects_outline_ring_horizontal, test_effects_glow_halo, test_effects_shadow_offset, test_effects_padding, test_effects_allocation_bounded_degrade, test_effects_vertical_composition

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Style effect values → effect surfaces | Crafted radii/widths (from `.mas`/`_ocr.json` or the future Inspector) cross into pixmap allocation; must never OOM the app |
| OCR/translation text → vertical layout | Untrusted text crosses into the per-char classifier/painter; must never become rich text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | DoS | effect pixmap allocation | medium | mitigate | Bounded effect surface (max dimension + pixel budget); oversize degrades to no-glow with a loguru warning (the BallonsTranslator EffectRasterAllocationError policy) — test_effects_allocation_bounded_degrade locks it |
| T-07-08 | Tampering | vertical classification/paint | low | mitigate | Classification sets + RTL order + rotated-advance all under test; the paint path rotates ONLY classified runs (never whole-block — D-11); no setHtml anywhere (ASVS V5, plan-07-01 gate) |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_layout.py tests/test_core/test_typeset_effects.py -q`
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (600-test baseline green)
- Backstop gates (end-of-phase human check — the UI-SPEC backstop rows): (1) tategaki on a real manga page with a CJK font — upright glyphs, RTL columns, rotated Latin/numbers; (2) effects look clean at 100%/150%/200% and the canvas stays fluid while dragging a selected box
</verification>

<success_criteria>
- Vertical layout geometry (classification, RTL, wrap, centering, vertical Auto-fit) locked by pure tests
- Glow + shadow render via silhouette-blur passes identical for canvas and bake; padding prevents clipping
- Effect allocation bounded with degrade-to-no-glow; no OOM path
- All plan-07-01 tests stay green (effects OFF → byte-identical rendering)
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-03-SUMMARY.md` when done
</output>


