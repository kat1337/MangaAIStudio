---
phase: 03-text-box-detection-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - panelcleaner/structures.py
  - panelcleaner/masker.py
  - manga_ai_studio/core/box_model.py
  - manga_ai_studio/core/image_file.py
  - tests/test_core/test_structures.py
  - tests/test_core/test_masker_vendor.py
  - tests/test_core/test_box_model.py
autonomous: true
requirements: [TEXT-01]
tags: [vendoring, data-model, d-14, d-15, headless]

must_haves:
  truths:
    - "panelcleaner.structures imports cleanly and exposes Box, BoxType, MaskFittingResults, MaskData, MaskerData, PageData (the full upstream surface, not the 18-line stub)"
    - "panelcleaner.masker imports cleanly with the output_structures import guarded (Pitfall 1); mask_page is dead code in our context but present near-verbatim for upstream diffability"
    - "Box.as_tuple_xywh returns (x1, y1, x2-x1, y2-y1) ints — maps directly to QRectF(x, y, w, h); (px, py) in Box when x1<=px<=x2 and y1<=py<=y2"
    - "PageBox carries (box, origin, payload, mask=None, std_dev=None) — the D-15 seam defaults leave mask+std_dev None so a later inpaint phase fills them without rework"
    - "ImageFile has a boxes slot defaulting to None, mirroring the Phase 2 mask slot; has_boxes() returns bool(self.boxes)"
    - "textblock_to_box coerces TextBlock.xyxy to ints and is the single V5 validation boundary (clamp/drop happens at the caller in plan 03-04)"
  artifacts:
    - "panelcleaner/structures.py (full vendored, ~749 lines, GPL v3 header, pcleaner.* imports rewritten to panelcleaner.*)"
    - "panelcleaner/masker.py (new vendored, ~151 lines, ost import guarded try/except)"
    - "manga_ai_studio/core/box_model.py (PageBox dataclass + textblock_to_box + DETECTED/USER origin constants)"
    - "manga_ai_studio/core/image_file.py (boxes slot + has_boxes method)"
    - "tests/test_core/test_structures.py (Box geometry, QRectF mapping, BoxType enum, full-structures import smoke)"
    - "tests/test_core/test_masker_vendor.py (masker import smoke with ost stubbed; std-dev machinery re-export check)"
    - "tests/test_core/test_box_model.py (PageBox defaults, origin constants, textblock_to_box int coercion, D-15 seam defaults-None lock)"
  key_links:
    - "masker.py ost-import guard (RESEARCH Pitfall 1) — the load-bearing line that keeps masker importable without vendoring output_structures.py"
    - "structures.py full replacement (RESEARCH Pitfall 2) — image_ops.pick_best_mask calls st.MaskFittingResults; the stub defines nothing"
    - "PageBox.mask and PageBox.std_dev default None (D-15) — the seam a later inpaint phase fills via image_ops.pick_best_mask/border_std_deviation"
    - "textblock_to_box int coercion (ASVS V5) — model-produced xyxy treated as untrusted; coerced at the boundary"
---

<objective>
Lay the headless data-model foundation for Phase 3: vendor PanelCleaner's full `structures.py` + `masker.py` (D-14), create the `PageBox` origin-tagged wrapper with the D-15 selective-inpaint seam open (mask/std_dev default None), and extend `ImageFile` with a `boxes` slot mirroring Phase 2's `mask` slot. This plan creates NO Qt code and NO detection wiring — it is the pure-Python, headless-testable base that plans 03-03 (BoxItem) and 03-04 (detection seam) build on.

Purpose: D-14 mandates vendoring these two files near-verbatim so the D-15 selective-per-box-inpaint machinery exists for a later phase; the PageBox data model is the single shape consumed by the GUI layer (BoxItem), the persistence layer (ImageFile.boxes), and the undo layer (BOXES snapshots). Getting this right headlessly first means plans 03-03..05 wire against a stable, tested model.

Output: Two vendored files, one new model module, one modified dataclass, three Wave 0 test files (per VALIDATION.md). No behavior change to the running app — purely additive library code.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-text-box-detection-interaction/03-CONTEXT.md
@.planning/phases/03-text-box-detection-interaction/03-RESEARCH.md
@.planning/phases/03-text-box-detection-interaction/03-PATTERNS.md
@.planning/phases/01-cleaning-workspace/01-CONTEXT.md
# Vendoring source (GPL v3, the near-verbatim reference per Phase 1 D-12):
@../PanelCleaner/pcleaner/structures.py
@../PanelCleaner/pcleaner/masker.py
# The existing stub to replace + the D-12 vendoring header pattern to mirror:
@panelcleaner/structures.py
@panelcleaner/image_ops.py
# The Phase 2 mask-slot pattern this plan's boxes slot mirrors:
@manga_ai_studio/core/image_file.py
# The Phase 1 vendoring discipline (STATE.md accumulated decisions):
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor structures.py (full) + masker.py (ost-guarded) per D-14</name>
  <files>panelcleaner/structures.py, panelcleaner/masker.py, tests/test_core/test_structures.py, tests/test_core/test_masker_vendor.py</files>
  <read_first>
    - `C:\Src\PanelCleaner\pcleaner\structures.py` (the 749-line upstream source — copy near-verbatim)
    - `C:\Src\PanelCleaner\pcleaner\masker.py` (the 151-line upstream source — copy near-verbatim, guard the ost import)
    - `panelcleaner/structures.py` (the 18-line stub being replaced — its comment says "deferred to Phase 2"; Phase 3 owns the replacement)
    - `panelcleaner/image_ops.py` lines 1-13 (the D-12 vendoring header to mirror: SPDX-License-Identifier + "Vendored near-verbatim from PanelCleaner..." + "imports rewritten pcleaner. -> panelcleaner.")
    - `.planning/phases/03-text-box-detection-interaction/03-PATTERNS.md` Group A (the vendoring analogs + the ost-stub pattern at lines 149-155)
    - `.planning/phases/03-text-box-detection-interaction/03-RESEARCH.md` §Common Pitfalls Pitfall 1 (ost-not-vendored) + Pitfall 2 (stub-structures) + §Vendoring verification
    - `tests/test_core/test_image_io.py` lines 1-45 (the pure-numpy headless test header to mirror: `from __future__ import annotations`, `@pytest.mark.unit`, no qtbot)
  </read_first>
  <behavior>
    - `from panelcleaner.structures import Box, BoxType, MaskFittingResults, MaskData, MaskerData, PageData` succeeds (Pitfall 2 — full surface, not stub)
    - `Box(10, 20, 110, 220).as_tuple_xywh == (10, 20, 100, 200)` (the QRectF mapping)
    - `(50, 50) in Box(10, 20, 110, 220)` is True; `(5, 5) in Box(...)` is False (the __contains__ hit-test)
    - `BoxType.BOX.value == 0` and the enum has BOX/EXTENDED_BOX/MERGED_EXT_BOX/REFERENCE_BOX
    - `import panelcleaner.masker` succeeds with output_structures NOT vendored (Pitfall 1 — the ost guard makes mask_page dead-but-importable)
    - `from panelcleaner.image_ops import border_std_deviation, color_std, pick_best_mask` succeeds (the D-15 machinery already vendored in Phase 1; this test confirms the full structures.py unblocks it — Pitfall 2)
    - `panelcleaner.masker.mask_page` is callable (present near-verbatim; not called by Phase 3)
  </behavior>
  <action>
    Vendor the two files per D-14 (near-verbatim, GPL v3 -> GPL v3, Phase 1 D-12 discipline). For `panelcleaner/structures.py`: replace the 18-line stub with the full upstream `C:\Src\PanelCleaner\pcleaner\structures.py` (749 lines). Prepend the D-12 vendoring header mirroring `panelcleaner/image_ops.py:1-7` (SPDX-License-Identifier: GPL-3.0-or-later + a one-line note "Vendored near-verbatim from PanelCleaner pcleaner/structures.py (GPL v3) per CONTEXT.md D-12/D-14. Imports rewritten pcleaner. -> panelcleaner."). Rewrite the four upstream imports: `pcleaner.config as cfg` -> `panelcleaner.config as cfg`, `pcleaner.data` -> `panelcleaner.data`, `pcleaner.ocr.supported_languages as osl` -> `panelcleaner.ocr.supported_languages as osl`, `pcleaner.helpers as hp` -> `panelcleaner.helpers as hp`. All four targets resolve in the vendored tree (RESEARCH §Standard Stack verified config.py 1746 lines, data/, ocr/supported_languages.py, helpers.py 335 lines). Keep the rest of the file byte-identical to upstream — do NOT mutate the `Box` class, the `BoxType` enum, or any batch struct (PageData, MaskData, MaskFittingResults, MaskerData, etc.). Per RESEARCH Pitfall 2, the full `MaskFittingResults` definition (upstream line ~564) is load-bearing: `image_ops.pick_best_mask` calls `st.MaskFittingResults(...)` at image_ops.py:706,717 and the stub defines nothing.

    For `panelcleaner/masker.py`: create a new file vendoring `C:\Src\PanelCleaner\pcleaner\masker.py` (151 lines). Prepend the same D-12 vendoring header (substituting masker.py for structures.py). Rewrite the three upstream imports: `pcleaner.image_ops as ops` -> `panelcleaner.image_ops as ops`, `pcleaner.structures as st` -> `panelcleaner.structures as st`. Guard the third import per RESEARCH Pitfall 1: replace the bare `import pcleaner.output_structures as ost` with a try/except ImportError that sets `ost = None` on failure, with a comment noting output_structures.py is the batch GUI analytics pipeline (not vendored) and mask_page is dead code in our context. Keep `mask_page` and `save_denoising_data` verbatim otherwise (RESEARCH Open Q 3 — near-verbatim is easier to diff upstream than a carved file; ost=None is harmless since Phase 3 never calls mask_page). Do NOT vendor output_structures.py or analytics.py (out of scope; would drag in the batch GUI analytics pipeline).

    Create `tests/test_core/test_structures.py` mirroring the `tests/test_core/test_image_io.py:1-45` header (`from __future__ import annotations`, `import pytest`, `@pytest.mark.unit`, no qtbot/PySide6 import at module top — structures.py imports PIL/attrs/loguru but the Box tests need none of them at import time; if a test does need PIL it imports locally). Cover: the full-surface import smoke (Box, BoxType, MaskFittingResults, MaskData, MaskerData, PageData all importable — Pitfall 2 regression), `Box.as_tuple_xywh` QRectF mapping (assert `(10,20,100,200)` for `Box(10,20,110,220)`), `Box.__contains__` hit-test (point inside True, point outside False), BoxType enum values, and a regression that `from panelcleaner.image_ops import pick_best_mask` no longer hits AttributeError on `st.MaskFittingResults` (the stub-replacement proof).

    Create `tests/test_core/test_masker_vendor.py` (same headless header). Cover: `import panelcleaner.masker` succeeds with ost NOT vendored (the Pitfall 1 guard proof — this test FAILS if the guard is missing and ost is imported unconditionally), `panelcleaner.masker.mask_page` is a callable (present near-verbatim), and `from panelcleaner.image_ops import border_std_deviation, color_std, pick_best_mask` succeeds (the D-15 machinery availability check — these are the functions a later inpaint phase calls; Phase 3 does not call them but the seam must be open).

    Run the RESEARCH §Vendoring verification smoke commands after writing: `python -c "from panelcleaner.structures import Box, BoxType, MaskFittingResults, MaskData, PageData; print('structures ok')"`, `python -c "import panelcleaner.masker; print('masker ok')"`, `python -c "from panelcleaner.image_ops import border_std_deviation, color_std, pick_best_mask; print('std-dev machinery ok')"`. Note in the task commit message that the stale `panelcleaner/__pycache__/structures.cpython-*.pyc` (the old stub's bytecode) is auto-invalidated by Python's mtime check; no manual cleanup needed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_core/test_structures.py tests/test_core/test_masker_vendor.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `panelcleaner/structures.py` line count is ~749 (replaces the 18-line stub); `grep -c "^class Box" panelcleaner/structures.py` returns >= 1
    - `panelcleaner/structures.py` contains `as_tuple_xywh` and `class MaskFittingResults` and `class BoxType` (grep each)
    - `panelcleaner/masker.py` exists and `grep -c "try:" panelcleaner/masker.py` returns >= 1 (the ost guard) and `grep -c "output_structures" panelcleaner/masker.py` returns >= 1
    - `panelcleaner/masker.py` does NOT contain a bare unconditional `import panelcleaner.output_structures` (the guard wraps it) — `grep -c "import panelcleaner.output_structures as ost" panelcleaner/masker.py` is 0 (it lives inside the try)
    - `python -c "from panelcleaner.structures import Box, BoxType, MaskFittingResults, MaskData, MaskerData, PageData; print('ok')"` prints `ok`
    - `python -c "import panelcleaner.masker; print('ok')"` prints `ok` (ost guard works)
    - `python -c "from panelcleaner.image_ops import border_std_deviation, color_std, pick_best_mask; print('ok')"` prints `ok` (Pitfall 2 fixed — pick_best_mask can now see MaskFittingResults)
    - `python -c "from panelcleaner.structures import Box; b=Box(10,20,110,220); assert b.as_tuple_xywh==(10,20,100,200); assert (50,50) in b and (5,5) not in b; print('Box ok')"` prints `Box ok`
    - `python -m pytest tests/test_core/test_structures.py tests/test_core/test_masker_vendor.py -q` exits 0
    - Both vendored files begin with the SPDX-License-Identifier GPL-3.0-or-later header (grep)
    - Both vendored files contain the "imports rewritten pcleaner. -> panelcleaner" note (grep)
  </acceptance_criteria>
  <done>Vendored structures.py (full) and masker.py (ost-guarded) import cleanly; the three Wave 0 test files exist and pass headlessly; the D-12 GPL v3 headers are present; pick_best_mask no longer hits AttributeError on MaskFittingResults (Pitfall 2 closed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PageBox model (D-15 seam) + textblock_to_box validator + ImageFile.boxes slot</name>
  <files>manga_ai_studio/core/box_model.py, manga_ai_studio/core/image_file.py, tests/test_core/test_box_model.py</files>
  <read_first>
    - `manga_ai_studio/core/image_file.py` (the existing ImageFile dataclass + the mask slot at line 69 + has_mask_content at lines 93-110 — the exact pattern the boxes slot + has_boxes mirror)
    - `manga_ai_studio/core/mask_editor.py` lines 56-69 (the ToolMode enum + module-level constants pattern — the analog for DETECTED/USER origin constants)
    - `.planning/phases/03-text-box-detection-interaction/03-RESEARCH.md` §Code Examples "D-15 seam shape" (the PageBox dataclass shape) + §Security Domain V5 (textblock_to_box int coercion)
    - `.planning/phases/03-text-box-detection-interaction/03-PATTERNS.md` Group B `core/box_model.py` (the analogs + the anti-pattern: do NOT subclass Box)
    - `.planning/phases/03-text-box-detection-interaction/03-CONTEXT.md` D-14 + D-15 (the locked decisions this task honors) + Claude's Discretion "origin flag + undo identity layer onto vendored Box"
    - `panelcleaner/comic_text_detector/utils/textblock.py` line 50 (TextBlock.xyxy is `[int, int, int, int]` — the input textblock_to_box validates)
  </read_first>
  <behavior>
    - `PageBox(box=Box(1,2,3,4), origin="detected")` has `.mask is None` and `.std_dev is None` by default (D-15 seam open, not closed)
    - `PageBox(box=Box(1,2,3,4), origin="user")` has `.payload is None` (user boxes carry no TextBlock until Phase 4)
    - `DETECTED` and `USER` are module-level string constants ("detected" / "user") — the D-03 origin discriminator
    - `textblock_to_box` accepts a TextBlock-like object (duck-typed `.xyxy` attribute) and returns a `Box` with int coordinates (V5 coercion)
    - `ImageFile(path=...)` has `.boxes is None` by default (mirrors `.mask is None`)
    - `ImageFile(path=..., boxes=[...]).has_boxes()` returns True; `ImageFile(path=...).has_boxes()` returns False (mirrors has_mask_content)
  </behavior>
  <action>
    Create `manga_ai_studio/core/box_model.py`. Define two module-level origin constants `DETECTED = "detected"` and `USER = "user"` (D-03 discriminator — single source of truth so the GUI, persistence, and undo layers all agree on the spelling). Define a `@dataclass class PageBox` that COMPOSES (not subclasses — CONTEXT D-14 / Claude's Discretion anti-pattern: do not mutate the vendored Box) a vendored `Box`. Fields: `box: Box` (the vendored frozen bbox — immutable, safe to share), `origin: str` (DETECTED or USER), `payload: Optional[object] = None` (the TextBlock for Phase 4/5 OCR/export; None for user boxes), `mask: Optional[object] = None` (D-15 seam — per-box mask, populated by a LATER inpaint phase via image_ops.pick_best_mask; None in Phase 3), `std_dev: Optional[float] = None` (D-15 seam — per-box border std-deviation, populated by a later phase via image_ops.border_std_deviation; None in Phase 3). The docstring MUST state explicitly that mask+std_dev are None in Phase 3 and are the D-15 plan-for-it seam — so a later phase fills them without rework (per CONTEXT D-15). Do NOT add a mutable box_id field; stable identity for undo comes from Python object identity of the PageBox instance (D-10) — if plan 03-02 needs a stable hashable key it can use id(pagebox).

    Define `def textblock_to_box(blk) -> Box:` — the single V5 input-validation boundary that coerces a model-produced TextBlock's xyxy into a vendored Box. Read `x1, y1, x2, y2 = blk.xyxy` (TextBlock.xyxy is `[int,...]` per textblock.py:50 but treat as untrusted per ASVS V5), and return `Box(int(x1), int(y1), int(x2), int(y2))`. The int() coercion is the V5 control; bounds-clamping against the image rect happens at the CALLER in plan 03-04 (this function does not know the image dimensions — it only guarantees int coordinates). Keep this function pure (no Qt, no image access) so it is headless-testable with a duck-typed fake TextBlock.

    Extend `manga_ai_studio/core/image_file.py`: add a `boxes` field to the `ImageFile` dataclass after the `mask` field, typed `list["PageBox"] | None = None` (use a forward-reference string or `from typing import Optional` + `Optional[list]` to avoid importing PageBox at module top if it would create a cycle — PageBox imports only from panelcleaner.structures, no cycle risk, so a direct `from manga_ai_studio.core.box_model import PageBox` is fine). Mirror the Phase 2 mask-slot pattern exactly (image_file.py:69). Add `def has_boxes(self) -> bool: return bool(self.boxes)` mirroring the trivial shape noted in PATTERNS.md (do NOT reimplement has_mask_content's numpy scan — boxes have no pixel content to scan). Update the docstring to document the boxes slot as per-page, in-memory only (no disk serialization in Phase 3 — PROJ-01 .mas is Phase 5).

    Create `tests/test_core/test_box_model.py` (headless header mirroring test_image_io.py). Cover: `PageBox(box=Box(1,2,3,4), origin=DETECTED).mask is None` and `.std_dev is None` (the D-15 seam-defaults-None lock — this is the regression a later phase relies on), `PageBox(box=Box(1,2,3,4), origin=USER).payload is None`, the DETECTED/USER constants equal "detected"/"user", `textblock_to_box` with a duck-typed fake TextBlock (a small `types.SimpleNamespace(xyxy=[10,20,110,220])` or a namedtuple) returns `Box(10,20,110,220)` with int coords (V5 coercion — feed it floats like `[10.0, 20.0, 110.0, 220.0]` and assert the result equals the int Box), `ImageFile(path=Path('.'))` has `.boxes is None` and `.has_boxes()` is False, `ImageFile(path=Path('.'), boxes=[PageBox(Box(1,2,3,4), DETECTED)]).has_boxes()` is True. Mark `@pytest.mark.unit`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_core/test_box_model.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `manga_ai_studio/core/box_model.py` exists and contains `class PageBox` and `def textblock_to_box` and `DETECTED =` and `USER =` (grep each)
    - `grep -c "mask: Optional" manga_ai_studio/core/box_model.py` >= 1 and `grep -c "std_dev: Optional" manga_ai_studio/core/box_model.py` >= 1 (D-15 seam fields present)
    - `grep -c "std_dev: Optional\[float\] = None" manga_ai_studio/core/box_model.py` == 1 (the D-15 default-None lock)
    - PageBox does NOT subclass Box — `grep -c "class PageBox(Box)" manga_ai_studio/core/box_model.py` == 0 (the D-14 anti-pattern)
    - `manga_ai_studio/core/image_file.py` contains `boxes:` (grep) and `def has_boxes` (grep)
    - `python -c "from manga_ai_studio.core.box_model import PageBox, textblock_to_box, DETECTED, USER; from panelcleaner.structures import Box; p=PageBox(Box(1,2,3,4), DETECTED); assert p.mask is None and p.std_dev is None and p.origin=='detected'; print('PageBox ok')"` prints `PageBox ok`
    - `python -c "from manga_ai_studio.core.image_file import ImageFile; from pathlib import Path; f=ImageFile(path=Path('.')); assert f.boxes is None and not f.has_boxes(); print('ImageFile ok')"` prints `ImageFile ok`
    - `python -m pytest tests/test_core/test_box_model.py -q` exits 0
    - D-15 is surfaced in the plan (this must_haves.truths + the PageBox docstring) — not silently dropped
  </acceptance_criteria>
  <done>PageBox composes a vendored Box with origin/payload/mask=None/std_dev=None defaults (D-15 seam open); textblock_to_box is the V5 int-coercion boundary; ImageFile.boxes slot + has_boxes mirror Phase 2's mask slot; test_box_model.py passes headlessly and locks the seam defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output -> app data model | `TextBlock.xyxy` (blk_list) crosses here in plan 03-04; this plan creates the `textblock_to_box` validator that is the single coercion point |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `textblock_to_box` (box_model.py) | medium | mitigate | Coerce xyxy to int at the boundary (V5 input validation); bounds-clamping against the image rect happens at the caller in plan 03-04 (textblock_to_box does not know image dims). Box is `@frozen` (immutable) — no aliasing mutation after construction. |
| T-03-SC | Tampering | Vendored `structures.py` + `masker.py` (D-14) | low | accept | No package-manager install (Phase 3 installs nothing — RESEARCH §Package Legitimacy Audit). Vendored source is GPL v3 -> GPL v3 per Phase 1 D-12 (license-compatible, already cleared). Transitive deps (attrs/PIL/loguru/numpy/scipy) verified present in pyproject. output_structures.py + analytics.py deliberately NOT vendored (out of scope). |

No network, no auth, no on-disk serialization in this plan (single-user offline desktop app; boxes are in-memory only per CONTEXT — PROJ-01 .mas save is Phase 5). Threat surface is proportionate: one V5 input-validation control + one supply-chain acceptance.
</threat_model>

<verification>
- `python -m pytest tests/test_core/test_structures.py tests/test_core/test_masker_vendor.py tests/test_core/test_box_model.py -q` exits 0 (all Wave 0 headless tests green)
- `python -c "from panelcleaner.structures import Box; from panelcleaner.masker import mask_page; from manga_ai_studio.core.box_model import PageBox, textblock_to_box; from manga_ai_studio.core.image_file import ImageFile; print('foundation imports ok')"` prints `foundation imports ok`
- The RESEARCH §Vendoring verification smoke block (4 python -c commands) all print ok
- `python -m pytest tests/ -m unit -q` still green (no regression to Phase 1/2 headless tests — the HistoryManager entry-shape widen is plan 03-02, not this plan)
</verification>

<success_criteria>
- D-14 honored: both files vendored near-verbatim with GPL v3 headers and rewritten imports; structures.py is the FULL upstream (not the stub); masker.py ost-import is guarded (Pitfall 1).
- D-15 honored and SURFACED: PageBox carries mask=None + std_dev=None defaults (the seam a later inpaint phase fills); the must_haves + docstring state this explicitly — the seam is not closed.
- The Box data model is headless-testable and imported by nothing yet (no Qt coupling) — plans 03-03 (BoxItem) and 03-04 (detection build) wire against a stable, tested model.
- ImageFile.boxes slot mirrors Phase 2's mask slot (D-11 of Phase 2 pattern) — ready for plan 03-05's per-page persistence seam.
</success_criteria>

<output>
Create `.planning/phases/03-text-box-detection-interaction/03-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

**Vendored (D-14):**
- `panelcleaner.structures.Box` (frozen dataclass x1/y1/x2/y2 + as_tuple/as_tuple_xywh/__contains__/area/merge/overlaps/pad/scale/translate) — REPLACES the 18-line stub
- `panelcleaner.structures.BoxType` (enum BOX/EXTENDED_BOX/MERGED_EXT_BOX/REFERENCE_BOX)
- `panelcleaner.structures.MaskFittingResults` / `MaskData` / `MaskerData` / `PageData` (batch structs — D-15 seam types; carries analytics_std_deviation + mask_box)
- `panelcleaner.masker.mask_page` + `panelcleaner.masker.save_denoising_data` (dead code in Phase 3; vendored for the std-dev seam + upstream diffability)

**New (our own, core/):**
- `manga_ai_studio.core.box_model.PageBox` (dataclass: box/origin/payload/mask/std_dev — the D-15 seam shape)
- `manga_ai_studio.core.box_model.textblock_to_box` (V5 int-coercion validator)
- `manga_ai_studio.core.box_model.DETECTED` / `USER` (origin string constants — D-03 discriminator)
- `manga_ai_studio.core.image_file.ImageFile.boxes` (slot, list[PageBox] | None) + `ImageFile.has_boxes()` (mirrors Phase 2 mask slot)

**Wave 0 test files (per VALIDATION.md):**
- `tests/test_core/test_structures.py`, `tests/test_core/test_masker_vendor.py`, `tests/test_core/test_box_model.py`
