---
phase: 04-ocr-recognition-text-editing
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/adapters/torch_impl.py
  - manga_ai_studio/adapters/factory.py
  - panelcleaner/ocr/ocr_mangaocr.py
  - tests/test_core/test_torch_ocr_model.py
  - tests/test_core/test_adapters.py
autonomous: true
requirements: [TEXT-02]
user_setup: []

must_haves:
  truths:
    - "backend_factory('ocr', 'torch') returns a TorchOCRModel instance (no longer raises NotImplementedError)"
    - "TorchOCRModel.recognize(numpy.ndarray) -> str does the numpy→PIL.Image conversion internally (manga-ocr accepts PIL, NOT numpy — Pitfall 2)"
    - "The vendored MangaOcr is a singleton (load-once per session); initialize_model() triggers the first-run HF download"
    - "TorchOCRModel.load() lazy-imports manga_ocr (module importable without the heavy dep — D-07)"
  artifacts:
    - "manga_ai_studio/adapters/torch_impl.py with TorchOCRModel (mirrors TorchLamaModel shape)"
    - "manga_ai_studio/adapters/factory.py with the 'ocr' branch resolved (NotImplementedError replaced)"
    - "panelcleaner/ocr/ocr_mangaocr.py vendored near-verbatim (GPL v3 → GPL v3 per D-12)"
    - "tests/test_core/test_torch_ocr_model.py + extended tests/test_core/test_adapters.py"
  key_links:
    - "TorchOCRModel.recognize calls self.model(pil_image) where self.model is the vendored MangaOcr singleton"
    - "backend_factory('ocr', 'torch') lazy-imports TorchOCRModel (D-07)"
---

<objective>
Build the OCR adapter (D-14) HEADLESSLY and in parallel with Plans 01/02: vendor PanelCleaner's MangaOcr singleton wrapper near-verbatim into panelcleaner/ocr/ocr_mangaocr.py (re-pathing the pcleaner→panelcleaner import), implement TorchOCRModel in adapters/torch_impl.py mirroring TorchLamaModel's exact shape (lazy-import in load(), numpy→PIL round-trip in recognize(), plain str return — Pitfall 2), and replace the backend_factory("ocr") NotImplementedError stub (factory.py:50-51) with the TorchOCRModel resolution. All manga-ocr-dependent tests skip-gate on model availability so CI does not force a ~450MB download.

Purpose: TEXT-02's "run manga-ocr on just that region" requires the OCR adapter to exist and backend_factory to resolve it. Per RESEARCH Pattern 1 + the vendored MangaOcr reference (43 lines, proven singleton), TorchOCRModel mirrors TorchLamaModel exactly — the integration is de-risked. Per RESEARCH Pitfall 2, the load-bearing detail is the numpy→PIL conversion (manga-ocr's __call__ accepts PIL.Image, NOT numpy). The adapter is the seam Plan 06 (OCR dispatcher) consumes; building it first as a tested module means Plan 06 is pure threading glue.

Output: TorchOCRModel + vendored MangaOcr + factory fix + adapter unit tests (skip-gated on model availability) all green.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ocr-recognition-text-editing/04-CONTEXT.md
@.planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md
@.planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md
@.planning/phases/04-ocr-recognition-text-editing/04-VALIDATION.md

@manga_ai_studio/adapters/torch_impl.py
@manga_ai_studio/adapters/factory.py
@manga_ai_studio/adapters/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor MangaOcr singleton wrapper near-verbatim (D-14, GPL v3 → GPL v3)</name>
  <files>panelcleaner/ocr/ocr_mangaocr.py</files>
  <read_first>
    - ../PanelCleaner/pcleaner/ocr/ocr_mangaocr.py (THE vendor reference — 43 lines, GPL v3; copy near-verbatim per Phase 1 D-12)
    - panelcleaner/ocr/supported_languages.py (the already-vendored sibling — confirms the panelcleaner.ocr.* module path; do NOT re-vendor)
    - panelcleaner/ocr/__init__.py (the existing ocr package init — confirm what's already exported)
    - .planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md (Code Examples lines 493-525 — the verbatim MangaOcr source; Pattern 1 lines 257-284; Pitfall 2 lines 442-447 — manga-ocr accepts PIL.Image)
    - .planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md (ocr_mangaocr.py assignment lines 68-111 — vendor re-path note: pcleaner.ocr.supported_languages → panelcleaner.ocr.supported_languages; placement panelcleaner/ocr/ next to supported_languages.py)
    - ../PanelCleaner/LICENSE (GPL v3 header text to preserve at the top of the vendored file)
  </read_first>
  <action>
    Create panelcleaner/ocr/ocr_mangaocr.py by copying ../PanelCleaner/pcleaner/ocr/ocr_mangaocr.py near-verbatim. Preserve the GPL v3 license header at the top of the file (Phase 1 D-12 — GPL v3 → GPL v3 license-compatible). Required transformations:

    1. Re-path the supported_languages import: `import pcleaner.ocr.supported_languages as osl` → `import panelcleaner.ocr.supported_languages as osl` (the vendored dir is panelcleaner/, confirmed by the existing panelcleaner/ocr/supported_languages.py).
    2. Keep the MangaOcr singleton class with: `_instance`, `_model`, `_init_args` class attrs; `__new__` creating the singleton; `__init__` deferred (pass); `initialize_model(*args, **kwargs)` lazy-constructing `MangaOcrModel(*args, **kwargs)` from the `manga_ocr` package on first call; `__call__(self, img_or_path)` accepting `Image.Image | Path | str` and returning `model(img_or_path) -> str`.
    3. Keep the `from manga_ocr import MangaOcr as MangaOcrModel` import at module top (it is the runtime dep — NOT lazy here because the wrapper itself is lazy-imported by TorchOCRModel.load; the wrapper's own import is only paid when OCR is actually used).
    4. Keep `from PIL import Image` and `from loguru import logger` (already project deps).
    5. If the upstream has a `langs()` staticmethod that calls into supported_languages, keep it (D-14 leaves the language hook designed-in) — but do NOT add new surface; the v1 UI does not consume it (D-14: no config UI).

    Do NOT vendor ocr.py, ocr_tesseract.py, or parsers.py (D-14: tesseract OUT for v1; parsers.py is a different format per RESEARCH Don't Hand-Roll). Only ocr_mangaocr.py is newly vendored in Phase 4.

    Confirm the vendored module imports cleanly: `python -c "from panelcleaner.ocr.ocr_mangaocr import MangaOcr; print(MangaOcr)"` should print the class without raising (the `from manga_ocr import MangaOcr as MangaOcrModel` line executes at module import — manga_ocr is installed per RESEARCH Environment Availability).
  </action>
  <verify>
    <automated>python -c "from panelcleaner.ocr.ocr_mangaocr import MangaOcr; assert MangaOcr._instance is None; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f panelcleaner/ocr/ocr_mangaocr.py` exits 0
    - `grep -c "class MangaOcr:" panelcleaner/ocr/ocr_mangaocr.py` is 1
    - `grep -c "import panelcleaner.ocr.supported_languages" panelcleaner/ocr/ocr_mangaocr.py` is 1 (re-pathed; no `pcleaner.` references remain)
    - `grep -c "pcleaner\." panelcleaner/ocr/ocr_mangaocr.py` is 0 (full re-path per Phase 3 D-12 discipline)
    - `grep -i -c "GPL" panelcleaner/ocr/ocr_mangaocr.py` is >= 1 (license header preserved)
    - `python -c "from panelcleaner.ocr.ocr_mangaocr import MangaOcr; print('ok')"` exits 0
    - `python -c "from panelcleaner.ocr import ocr_mangaocr, supported_languages; print('both import')"` exits 0 (sibling co-existence)
  </acceptance_criteria>
  <done>MangaOcr singleton wrapper vendored near-verbatim into panelcleaner/ocr/ocr_mangaocr.py, pcleaner→panelcleaner re-path applied, GPL v3 header preserved, module imports cleanly alongside supported_languages.py.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TorchOCRModel adapter mirroring TorchLamaModel + factory fix (TDD)</name>
  <files>manga_ai_studio/adapters/torch_impl.py, manga_ai_studio/adapters/factory.py, tests/test_core/test_torch_ocr_model.py, tests/test_core/test_adapters.py</files>
  <read_first>
    - manga_ai_studio/adapters/torch_impl.py (TorchLamaModel lines ~166-281 — THE pattern to mirror exactly: __init__ with self.config/self.model=None/self.model_path=None, load() lazy-import + path validation, numpy→PIL round-trip in the call, "Model not loaded" guard, preprocess/postprocess/get_info/configure stubs)
    - manga_ai_studio/adapters/factory.py (the existing `if kind == "ocr": raise NotImplementedError(...)` stub at lines 50-51 to replace; the detection block at 37-43 is the mirror template; line 19 imports OCRModel already)
    - manga_ai_studio/adapters/base.py (OCRModel ABC lines 71-96 — confirm recognize(self, image: np.ndarray) -> str signature; the ABC takes numpy, the adapter does numpy→PIL internally)
    - .planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md (Pattern 1 lines 257-284 — exact TorchOCRModel body; Pitfall 2 lines 442-447 — numpy→PIL; Pitfall 6 lines 472-476 — first-run download)
    - .planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md (torch_impl.py assignment lines 29-65 + factory.py assignment lines 267-289 — exact class skeleton + factory replacement block)
    - tests/test_core/test_adapters.py (existing test shape — extend with test_ocr_factory; mirror the existing detection-factory test)
    - ../PanelCleaner/pcleaner/model_downloader.py (get_ocr_model_directory / is_ocr_downloaded / OCR_DIR_NAME — the cache-check the worker uses; not called by the adapter itself, just confirm the seam exists)
  </read_first>
  <behavior>
    - backend_factory("ocr", "torch") returns a TorchOCRModel instance (no longer raises NotImplementedError)
    - backend_factory("ocr") with no backend arg defaults to torch and returns TorchOCRModel (mirror how detection/inpainting default)
    - backend_factory("ocr", "onnx") raises NotImplementedError mentioning the ONNX OCR backend (D-14 hook designed-in, not built-out)
    - TorchOCRModel() constructs with model=None, model_path=None (mirrors TorchLamaModel.__init__)
    - TorchOCRModel.load(model_path, device="cpu") lazy-imports MangaOcr from panelcleaner.ocr.ocr_mangaocr, constructs MangaOcr(), and calls .initialize_model() (triggers the first-run HF download inside the worker, not at module import)
    - TorchOCRModel.recognize(np.ndarray) does PIL.Image.fromarray(image, mode="RGB") then returns self.model(pil_image) — the str output (Pitfall 2: manga-ocr accepts PIL, NOT numpy)
    - TorchOCRModel.recognize raises RuntimeError("Model not loaded") when called before load() (mirror TorchLamaModel guard)
    - The module manga_ai_studio.adapters.torch_impl imports WITHOUT manga_ocr installed (the heavy import is lazy inside load()) — D-07
  </behavior>
  <action>
    RED first: create tests/test_core/test_torch_ocr_model.py. Use a FakeMangaOcr (`types.SimpleNamespace(return_value="こんにちは")` with a __call__ returning the canned str) injected via monkeypatch so the test does NOT require the real manga-ocr model (skip-gate the real-model test with `@pytest.mark.skipif(not _model_available(), reason="manga-ocr model not cached")`). Tests: test_load_lazy_imports_model, test_recognize_converts_numpy_to_pil (assert Image.fromarray was called with mode="RGB" — monkeypatch Image.fromarray to capture), test_recognize_returns_str, test_recognize_raises_when_not_loaded, test_module_imports_without_manga_ocr (import torch_impl with manga_ocr monkeypatched to None — module still imports because the import is inside load()). Extend tests/test_core/test_adapters.py with test_ocr_factory_returns_torch_ocr_model, test_ocr_factory_onnx_not_implemented. Mark @pytest.mark.unit; the real-model test gets a separate marker (integration). Run pytest → RED.

    GREEN: add to manga_ai_studio/adapters/torch_impl.py (after TorchLamaModel) a `class TorchOCRModel(OCRModel):` mirroring TorchLamaModel's shape:

    - `def __init__(self, config=None) -> None:` self.config = config; self.model = None; self.model_path = None
    - `def load(self, model_path: Path, device: str = "cpu") -> None:` — validate model_path is not None (informational for manga-ocr since it resolves from HF cache, but keep the parameter for ABC symmetry); lazy `from panelcleaner.ocr.ocr_mangaocr import MangaOcr`; `self.model = MangaOcr()`; `self.model.initialize_model()` (triggers the first-run download — but only inside the worker thread, T-01-07); self.model_path = model_path
    - `def recognize(self, image: np.ndarray) -> str:` — guard `if self.model is None: raise RuntimeError("Model not loaded — call load() before recognize().")`; `from PIL import Image` (module-top is fine, PIL is a core dep); `pil_image = Image.fromarray(image, mode="RGB")`; `return self.model(pil_image)` (str)
    - preprocess / postprocess / configure / get_info — copy the TorchLamaModel stub shapes verbatim (return-input-unchanged / return-metadata-dict). get_info returns {"backend": "torch", "model": "manga-ocr/kha-white/manga-ocr-base", "device": ...}.

    Update manga_ai_studio/adapters/factory.py: replace the two-line `if kind == "ocr": raise NotImplementedError("OCR adapter lands in Phase 4")` (lines 50-51) with the mirror of the detection block:

    `if kind == "ocr":` / `    if backend == "torch":` / `        from manga_ai_studio.adapters.torch_impl import TorchOCRModel` / `        return TorchOCRModel()` / `    if backend == "onnx":` / `        raise NotImplementedError("ONNX OCR backend lands in a future phase")` / `    raise ValueError(f"Unknown backend: {kind}/{backend}")`

    The lazy import keeps the factory importable without manga_ocr installed (D-07). Confirm the factory's existing default-backend handling (how detection defaults to "torch" when backend is None) applies identically to ocr — re-read factory.py to confirm whether there's a `backend = backend or "torch"` line or per-kind defaulting; mirror whatever detection uses.
  </action>
  <verify>
    <automated>python -m pytest tests/test_core/test_torch_ocr_model.py tests/test_core/test_adapters.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TorchOCRModel" manga_ai_studio/adapters/torch_impl.py` is 1
    - `grep -c "def recognize" manga_ai_studio/adapters/torch_impl.py` is >= 1 (TorchOCRModel.recognize present)
    - `grep -c "Image.fromarray" manga_ai_studio/adapters/torch_impl.py` is >= 2 (TorchLamaModel + TorchOCRModel both do numpy→PIL)
    - `grep -c 'raise NotImplementedError("OCR adapter lands in Phase 4")' manga_ai_studio/adapters/factory.py` is 0 (stub removed)
    - `grep -c "TorchOCRModel()" manga_ai_studio/adapters/factory.py` is 1 (factory now resolves it)
    - `python -c "from manga_ai_studio.adapters.factory import backend_factory; m = backend_factory('ocr', 'torch'); print(type(m).__name__)"` prints TorchOCRModel
    - `python -c "import manga_ai_studio.adapters.torch_impl"` exits 0 even when manga_ocr is not loaded at import time (lazy import inside load)
    - `python -m pytest tests/test_core/test_torch_ocr_model.py tests/test_core/test_adapters.py -x -q` exits 0
  </acceptance_criteria>
  <done>TorchOCRModel mirrors TorchLamaModel (lazy load, numpy→PIL recognize, str return, model-not-loaded guard); factory resolves backend_factory('ocr','torch')→TorchOCRModel and raises NotImplementedError for onnx (D-14 hook); all adapter tests green with the real-model test skip-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| numpy image region → manga-ocr model | the page-region crop crosses into the heavy model call; must run off the GUI thread (Plan 06 enforces) |
| HF cache → model load | first-run ~450MB download from HuggingFace; cache-check before download (CR-11 pattern, enforced in Plan 06's _resolve_ocr_model_path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-05 | Tampering / Elevation | swapped kha-white/manga-ocr-base in HF cache | low | accept | HF uses content-addressed SHA256 blobs; Phase 1 T-01-04 model-weight-tampering mitigation applies (validate path exists before load). Local single-user tool — low risk. |
| T-4-06 | Denial of Service | repeated ~450MB model downloads | medium | mitigate | Vendored MangaOcr is a singleton (load-once); Plan 06 _resolve_ocr_model_path cache-checks via is_ocr_downloaded() before triggering initialize_model() (CR-11 pattern). |
| T-4-SC | Tampering | pip installs (manga-ocr, transformers) | high | mitigate | Both packages confirmed false-positive SUS in RESEARCH Package Legitimacy Audit (long-established, canonical GitHub repos, already in pyproject.toml [torch] extra). No new install in Phase 4 — `[ASSUMED]`/`[SUS]` checkpoint NOT warranted per RESEARCH §Package Legitimacy Audit. |
</threat_model>

<verification>
- `python -m pytest tests/test_core/test_torch_ocr_model.py tests/test_core/test_adapters.py -x -q` green (real-model test skip-gated)
- `python -m pytest tests/ -q` full suite green
- `python -c "from manga_ai_studio.adapters.factory import backend_factory; print(type(backend_factory('ocr','torch')).__name__)"` prints TorchOCRModel
</verification>

<success_criteria>
- panelcleaner/ocr/ocr_mangaocr.py vendored near-verbatim (GPL v3 preserved, pcleaner→panelcleaner re-path)
- TorchOCRModel in torch_impl.py mirrors TorchLamaModel (lazy load, numpy→PIL recognize, str return)
- factory.py resolves backend_factory('ocr','torch'); NotImplementedError for onnx (D-14 hook)
- TEXT-02 adapter substrate complete and decoupled from GUI (Plan 06 is pure threading glue)
</success_criteria>

<output>
Create `.planning/phases/04-ocr-recognition-text-editing/04-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan creates (downstream plan-review-convergence: exclude these from drift verification):
- `panelcleaner.ocr.ocr_mangaocr.MangaOcr` (singleton wrapper, vendored near-verbatim)
- `manga_ai_studio.adapters.torch_impl.TorchOCRModel` (OCRModel subclass; load/recognize/preprocess/postprocess/configure/get_info)
- `backend_factory("ocr", "torch")` returns TorchOCRModel (factory.py branch)
- `backend_factory("ocr", "onnx")` raises NotImplementedError (D-14 designed-in hook)
</content>
</invoke>
