---
phase: 06-refinement-polish-deferred-fixes-full-curve-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/image_ops.py
  - tests/test_core/test_image_ops.py
autonomous: true
requirements:
  - PROJ-04
estimate:
  tokens: 25000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "curve_lut(points) returns a 256-entry uint8 LUT; the Linear curve (no interior points) is byte-identical to np.arange(256)"
    - "A degenerate point set (duplicate x, out-of-range y) never produces NaN, out-of-range indices, or an inverted map — the T-05-07 backstop (sort, dedupe, clip, np.interp, round, astype)"
    - "curve_lut is a pure function: identical input points yield byte-identical LUTs; applying the LUT twice is a deterministic curve-of-curve composition (idempotency probe)"
    - "curves_page validates (H,W,3) uint8, applies the master LUT to all channels then per-channel LUTs after the master (A1 order), and returns a detached .copy()"
  artifacts:
    - manga_ai_studio/core/image_ops.py :: curve_lut + curves_page
    - tests/test_core/test_image_ops.py :: curve unit tests (identity, monotone, backstop, composition, validation, detach)
  key_links:
    - "curves_page -> curve_lut composition order (A1): out_c = channel_lut_c[master_lut[v]]"
    - "fancy-index LUT apply lut[image] on (H,W,3) uint8 — the levels_page pattern"
  prohibitions:
    - "levels_lut/levels_page and their tests must stay untouched (kept beside the new LUTs)"
    - "No new dependencies — numpy only"
    - "No inverted map can ever render (T-05-07 discipline)"
---

<objective>
Land the curve-editor's headless math: `curve_lut` (points → 256-entry uint8 LUT) and `curves_page` (master + per-channel LUT composition over a page image) in `core/image_ops.py`, beside the kept `levels_lut`/`levels_page`. This is PROJ-04's "curves" half — the foundation plan 06-04's CurvesDialog and 06-05's `_on_curves` build on.

Purpose: The curve editor's pixel math is pure numpy — headless, unit-testable first (the established image_ops discipline: no Qt, no display, safe from any thread). Locking the composition order (A1) and the degenerate-input backstop here means the GUI plans never debug pixel math.

Output: `curve_lut` + `curves_page` in `core/image_ops.py` with a headless RED-GREEN unit suite in `tests/test_core/test_image_ops.py` (identity, monotone S-curve, backstop, A1 composition probe, validation errors, .copy() detach, idempotency).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-CONTEXT.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-RESEARCH.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-PATTERNS.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-UI-SPEC.md
@manga_ai_studio/core/image_ops.py
@tests/test_core/test_image_ops.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: curve_lut + curves_page — the points→LUT→page path (RED-GREEN)</name>
  <files>manga_ai_studio/core/image_ops.py, tests/test_core/test_image_ops.py</files>
  <read_first>
    - manga_ai_studio/core/image_ops.py — `levels_lut`/`levels_page` at :431-470 (the LUT pattern to mirror: docstring discipline, ValueError on bad input, round().astype(uint8) output, fancy-index apply, trailing .copy())
    - tests/test_core/test_image_ops.py — `test_resize_and_levels` at :352-410 (the byte-exact LUT test shape: np.array_equal, flags["OWNDATA"]/np.shares_memory detach assertions, ValueError assertions)
  </read_first>
  <behavior>
    - Test 1 (identity): curve_lut([]) and curve_lut([(0,0),(255,255)]) are byte-identical to np.arange(256, dtype=np.uint8)
    - Test 2 (monotone): curve_lut([(0,0),(64,40),(192,215),(255,255)]) is monotone non-decreasing, lut[0]==0, lut[255]==255, and lut[64] >= 40 within interpolation tolerance (rounding)
    - Test 3 (backstop): curve_lut with duplicate-x points and out-of-range coords (e.g. [(255, 300), (255, -5), (0,0)]) returns uint8 with no NaN (np.isfinite(lut.astype(float)).all()), all values within [0,255], dtype np.uint8, len 256
    - Test 4 (page apply): curves_page(img, [(0,0),(255,255)], {}) on a (10,10,3) uint8 image equals img exactly; curves_page with a master darkening curve maps pixels per lut[img] byte-exact
    - Test 5 (validation + detach): curves_page raises ValueError for (H,W) 2-D input and for float dtype; result does not share memory with the input (flags["OWNDATA"], not np.shares_memory)
  </behavior>
  <action>RED: write the five behavior tests in tests/test_core/test_image_ops.py (new `test_curve_lut_*` / `test_curves_page_*` functions following the test_resize_and_levels shape) and run them — they must fail (ImportError/AttributeError on the missing functions). GREEN: add `curve_lut(points: list[tuple[int,int]]) -> np.ndarray` and `curves_page(image_rgb, master_points, channel_points: dict[str, list[tuple[int,int]]]) -> np.ndarray` to core/image_ops.py, per RESEARCH Common Operation 1/2: curve_lut sorts by x, dedupes duplicate x last-wins, clips x and y to [0,255], inserts (0,0)/(255,255) endpoints when absent, samples with np.interp(np.arange(256), xs, ys), returns np.clip(np.round(lut), 0, 255).astype(np.uint8); curves_page validates (H,W,3) uint8 (ValueError otherwise, mirroring levels_page's error string "expected (H,W,3) uint8 RGB"), applies master via lut[image], then per-channel lut on each channel plane in place, returns .copy() (Pitfall 2). Both get docstrings naming D-04/D-06/PROJ-04 and the backstop guarantee.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_image_ops.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All five RED-GREEN tests pass; the curve tests appear as test_curve_lut_linear_is_identity, test_curve_lut_s_curve_monotone, test_curve_lut_degenerate_backstop, test_curves_page_master_apply_byte_exact, test_curves_page_validation_and_detach
    - curve_lut([]) == np.arange(256, dtype=np.uint8) byte-identical (np.array_equal)
    - The degenerate-input test asserts np.isfinite(lut.astype(float)).all() and (lut >= 0).all() and (lut <= 255).all() and lut.dtype == np.uint8
    - curves_page validation asserts ValueError for both the 2-D and float-dtype inputs with the exact message "expected (H,W,3) uint8 RGB"
    - The detach test asserts not np.shares_memory(result, img) and result.flags["OWNDATA"]
  </acceptance_criteria>
  <done>curve_lut + curves_page exist in core/image_ops.py and the five headless tests are green; committed as test-then-implement commits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composition-order probe + idempotency + per-channel independence</name>
  <files>manga_ai_studio/core/image_ops.py, tests/test_core/test_image_ops.py</files>
  <read_first>
    - manga_ai_studio/core/image_ops.py — the Task 1 curve_lut/curves_page implementations (the file is already read this plan; re-read only the added sections)
    - 06-RESEARCH.md — Common Operation 2 (composition shape), Assumption A1 (master∘channel order), Assumption A6 (last-wins duplicate-x backstop)
  </read_first>
  <behavior>
    - Test 1 (A1 order pin): with master_lut darkening (e.g. points [(0,0),(128,100),(255,255)]) and a channel R brightening curve, curves_page output for the R plane equals ch_lut[master_lut[img[...,0]]] exactly — per-channel applied AFTER master, not before and not multiplicative
    - Test 2 (independence): editing only channel G's points leaves the R and B planes byte-identical to the master-only output
    - Test 3 (idempotency probe): calling curve_lut twice with the same points yields byte-identical arrays; applying curves_page twice with the same points yields byte-identical results (deterministic curve-of-curve composition)
    - Test 4 (duplicate-x last-wins): curve_lut([(64,40),(64,200),(0,0),(255,255)]) uses y=200 at x=64 (last-wins), not 40
  </behavior>
  <action>RED: write the four tests (test_curves_page_channel_after_master_order, test_curves_page_channel_independence, test_curve_lut_and_page_idempotent, test_curve_lut_duplicate_x_last_wins) — they fail on the Task 1 implementation only if the composition order is wrong (the probe's job); the idempotency and last-wins tests must pass immediately (they lock Task 1's guarantees). GREEN: if the A1 probe fails, fix curves_page to apply per-channel AFTER the master (out_c = ch_lut[master_lut[v]]); no other production change expected. Run the full test file — the whole suite must be green, including the pre-existing levels/rotate/crop/resize tests (levels_lut/levels_page untouched).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_image_ops.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_curves_page_channel_after_master_order asserts np.array_equal(r_plane_out, ch_lut[master_lut[img[..., 0]]]) — the A1 order is pinned by test, not by prose
    - test_curves_page_channel_independence asserts the unedited R/B planes equal the master-only curves_page output byte-exactly
    - test_curve_lut_and_page_idempotent asserts byte-identical outputs for identical inputs (the probe edge — same input, same output)
    - test_curve_lut_duplicate_x_last_wins asserts lut[64] == 200
    - The full test_core/test_image_ops.py suite passes with no changes to levels_lut/levels_page or their tests (grep the diff: only additions)
  </acceptance_criteria>
  <done>The A1 composition order is pinned by a passing probe test; idempotency and last-wins locks are in; the whole image_ops test module is green.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/core/image_ops.py`:
  - `curve_lut(points: list[tuple[int, int]]) -> np.ndarray` — 256-entry uint8 piecewise-linear LUT with the T-05-07 backstop (sort by x, duplicate-x last-wins, clip to [0,255], default (0,0)/(255,255) endpoints, np.interp, round, astype)
  - `curves_page(image_rgb: np.ndarray, master_points: list[tuple[int, int]], channel_points: dict[str, list[tuple[int, int]]]) -> np.ndarray` — master-then-per-channel LUT composition, (H,W,3) uint8 validation, detached .copy() result
- `tests/test_core/test_image_ops.py` (extended): test_curve_lut_linear_is_identity, test_curve_lut_s_curve_monotone, test_curve_lut_degenerate_backstop, test_curves_page_master_apply_byte_exact, test_curves_page_validation_and_detach, test_curves_page_channel_after_master_order, test_curves_page_channel_independence, test_curve_lut_and_page_idempotent, test_curve_lut_duplicate_x_last_wins

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curve point input → LUT | Untrusted point coordinates (from the future GUI, tests, or callers) cross into numpy math; must never corrupt output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | curve_lut fancy-index/arange math | medium | mitigate | Backstop by construction: sort/dedupe/clip to [0,255] + np.interp + round().astype(np.uint8) — uint8 indices can never be out of range; test_curve_lut_degenerate_backstop locks it (T-05-07 discipline) |
| T-06-02 | DoS | curves_page preview recompute | low | accept | Preview cost equals the existing levels_page cost (accepted since Phase 5); page size bounded by MAX_IMAGE_DIMENSION at load (canvas.py:79); the dialog computes its histogram ONCE at open (D-08) |
| T-06-SC | Tampering | pip installs | high | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_image_ops.py -x -q` — the whole module (existing levels/rotate/crop/resize + new curve tests) green
- Grep gate: `git diff --stat` shows only additions to core/image_ops.py (levels_lut/levels_page untouched)
</verification>

<success_criteria>
- curve_lut identity / monotone / degenerate-input / last-wins behaviors locked by tests
- curves_page master-then-channel composition (A1) pinned by the probe test
- Idempotency probe (same input → byte-identical output, both single and double application) green
- No production code outside core/image_ops.py touched; no new dependencies
</success_criteria>

<output>
Create `.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-01-SUMMARY.md` when done
</output>
