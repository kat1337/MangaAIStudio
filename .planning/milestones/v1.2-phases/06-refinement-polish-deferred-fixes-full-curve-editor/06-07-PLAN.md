---
phase: 06-refinement-polish-deferred-fixes-full-curve-editor
plan: 07
type: execute
wave: 2
depends_on:
  - 06-06
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_curves_dialog.py
autonomous: true
gap_closure: true
requirements:
  - PROJ-04
estimate:
  tokens: 15000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Ctrl+Z after a curves apply flashes 'Undo: curves'; Ctrl+Shift+Z flashes 'Redo: curves' — the UI-SPEC surface 28 copy contract is honored at runtime (VERIFICATION truth 26 / WR-01 restored)"
    - "A non-geometry single-image pop (a bbox-shaped inpaint patch at a non-origin (x, y)) is still labeled 'inpaint' even while a geometry op name is recorded — the (0,0) full-frame gate scopes the op-name override"
    - "The undo/redo op-name set stays ('rotate', 'crop', 'curves', 'resize') — 'levels' replaced, not appended (the tuple literal is unchanged by this fix)"
    - "The curves lifecycle assertions (byte-exact restore, ONE image-only entry, empty stack after undo) are unchanged and green"
  artifacts:
    - manga_ai_studio/gui/main_window.py :: _undo_op_label_for_result (:2927-2941) — single-entry image branch preferring _last_geometry_op_name for full-frame (0,0) entries
    - tests/test_gui_curves_dialog.py :: flash-text assertions in test_curves_apply_pushes_one_entry ('Undo: curves' / 'Redo: curves') + test_single_image_pop_keeps_inpaint_label (scoping guard)
  key_links:
    - "_undo_op_label_for_result single-entry branch -> _last_geometry_op_name (recorded by _apply_geometry_op at :1192) — a curves pop is an image-only (0,0) entry, so the op name now wins over the kind-based 'inpaint' fallback"
    - "history.push_geometry_state stores the image store as (0, 0, patch) (history_manager.py:435) — the (x, y) == (0, 0) check identifies geometry-record shape"
  prohibitions:
    - "The undo flash for a curves pop must be 'curves', never 'inpaint' (UI-SPEC surface 28)"
    - "The op-name set must not re-add 'levels' — supersession stays ('rotate', 'crop', 'curves', 'resize')"
    - "Non-geometry single-image pops (bbox inpaint patches) must keep the 'inpaint' label — the override is scoped to full-frame (0,0) entries"
---

<objective>
Close gap **WR-01 (warning)** from 06-VERIFICATION.md (truth 26 failed at runtime): Ctrl+Z after a curves apply flashes "Undo: inpaint" instead of "Undo: curves". `_undo_op_label_for_result` (main_window.py:2927-2941) consults the recorded op name (`_last_geometry_op_name`) only when the pop result has >1 element; a curves apply pushes an image-ONLY geometry record (mask/boxes None — D-15 geometry-free), so the pop is a single-element list and the single-entry branch resolves via `_undo_op_label("image")` → "inpaint" (main_window.py:2909-2910). The tuple literal at :2905 is correct; the runtime resolution is not. The lifecycle tests assert the restored image but never the flash text.

Purpose: honor the UI-SPEC surface 28 copy contract this phase extended with `curves` — the flash must name the op the user actually undid.

Output: the single-entry (0,0)-gated op-name override in `_undo_op_label_for_result`, flash-text assertions in the curves lifecycle test, and a scoping guard proving bbox inpaint pops keep the "inpaint" label.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-VERIFICATION.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-REVIEW.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-06-PLAN.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/core/history_manager.py
@tests/test_gui_curves_dialog.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-GREEN — curves undo/redo flash labels 'curves' (WR-01 fix + flash assertions)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_curves_dialog.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py — _undo_op_label (:2897-2913, the set at :2905 and the kind fallbacks), _record_geometry_op_name (:2915-2925), _undo_op_label_for_result (:2927-2941, the WR-01 site), on_undo/on_redo flash emission (:2982, :2995), _show_transient_status (:3065-3073 — writes status_bar_left.text())
    - manga_ai_studio/core/history_manager.py — push_geometry_state image-store shape (:435: (0, 0, image_patch)), pop_image_undo return shape (:246: (x, y, patch))
    - tests/test_gui_curves_dialog.py — test_curves_apply_pushes_one_entry (:654-713; on_undo at :709, stack-empty assert at :713 — the flash assertions append AFTER :713)
  </read_first>
  <behavior>
    - Test 1 (RED gate): in test_curves_apply_pushes_one_entry, after the existing on_undo() + processEvents + image/stack asserts (:709-713), assert `"Undo: curves" in window.status_bar_left.text()`; then `window.on_redo()` + processEvents; assert the canvas is byte-exact the post-op image again (np.array_equal vs the `expected` curves_page output) and `"Redo: curves" in window.status_bar_left.text()`. MUST FAIL on current code: the single-entry pop resolves via _undo_op_label("image") → the flash reads "Undo: inpaint".
  </behavior>
  <action>RED: extend test_curves_apply_pushes_one_entry in tests/test_gui_curves_dialog.py with the undo-flash and redo-flash assertions per the behavior above (keep every existing assertion untouched). Run — must fail with the mislabel. GREEN: fix `_undo_op_label_for_result` (main_window.py:2927-2941) — keep the len(result) > 1 branch verbatim, and add a single-entry image branch before the kind fallthrough: when `kind == "image"` and `self._last_geometry_op_name` is set, destructure `x, y, _patch = value` and return `self._undo_op_label(self._last_geometry_op_name)` when `(x, y) == (0, 0)` (the geometry-record shape — history_manager.py:435 stores full-frame patches at (0,0)); otherwise fall through to the kind-based label. Update the docstring to state the single-entry geometry-record case (curves pushes image-only). Document the accepted limitation in the docstring: after two consecutive image-only geometry ops, the second undo labels by the LAST recorded op name — the stamp-based robust resolution is out of gap scope (the review's minimal fix is the prescribed shape). Run — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The RED run failed with the 'Undo: inpaint' mislabel (proven RED on the exact probe symptom); the GREEN run passes with 'Undo: curves' / 'Redo: curves'
    - on_redo restores the post-op image byte-exact (np.array_equal vs the curves_page `expected`)
    - Every pre-existing assertion in test_curves_apply_pushes_one_entry is unchanged and green (byte-exact apply, ONE image-only entry, mask/boxes untouched, geometry_altered False, 'Curves applied.' flash, D-14 post-op baseline, restore-semantics Ctrl+Z → byte-identical pre-dialog image + empty stack)
    - Grep gate: `grep -n '_last_geometry_op_name' manga_ai_studio/gui/main_window.py | Select-String '29[2-4][0-9]'` shows the op-name consult inside the single-entry branch (the fix site)
  </acceptance_criteria>
  <done>Ctrl+Z flashes 'Undo: curves' and Ctrl+Shift+Z flashes 'Redo: curves' after a curves apply; the lifecycle test locks the flash text.</done>
</task>

<task type="auto">
  <name>Task 2: Label-scoping guard + supersession sweep (bbox inpaint pops keep 'inpaint')</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_curves_dialog.py</files>
  <read_first>
    - manga_ai_studio/core/history_manager.py — push_image_action (:204-219, the real public push for bbox image entries)
    - manga_ai_studio/gui/main_window.py — on_undo (:2963-2983) and _current_undo_state (:2943-2961) — the pop path the guard test drives through the REAL window
    - tests/test_gui_curves_dialog.py — the _fake_exec Accepted driver from test_curves_apply_pushes_one_entry (:672-682) to reuse for the guard test's apply
  </read_first>
  <action>Add `test_single_image_pop_keeps_inpaint_label` to tests/test_gui_curves_dialog.py: drive a real curves Apply (the _fake_exec Accepted shape with BRIGHTEN points), then on_undo() — flash 'Undo: curves', `_last_geometry_op_name` now stale == 'curves' — then push a NON-origin bbox image entry through the real HistoryManager: `window.history.push_image_action(5, 5, np.zeros((10, 10, 3), dtype=np.uint8))`; then window.on_undo() + processEvents; assert `"Undo: inpaint" in window.status_bar_left.text()` (the (0,0) gate keeps bbox-shaped inpaint undos mislabeled-free even with a stale geometry op name) and `not window.history.can_undo()`. This is the scoping guard: it proves the override is restricted to full-frame geometry entries and locks the fallback for ordinary inpaint undos. Then the supersession sweep: run the full curves module and the full suite (the count must be the 06-06 baseline + this plan's additions — no test lost, no regressions).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_single_image_pop_keeps_inpaint_label passes: 'Undo: inpaint' flash for a (5,5) bbox entry while _last_geometry_op_name == 'curves'; stack empty after the pop
    - Grep gate (supersession): `grep -c '("rotate", "crop", "curves", "resize")' manga_ai_studio/gui/main_window.py` == 1 — the op-name set literal at :2905 is exactly the four names, 'levels' superseded not appended. (The only 'levels' occurrence in the file is the historical note inside an INDENTED comment at :2895 — a column-0 `grep -v '^#'` cannot filter it, so a plain `'levels'` == 0 gate is un-runnable; the set literal is the reliable gate.)
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — the 06-06 baseline count (598) plus this plan's additions, 0 failed
  </acceptance_criteria>
  <done>The op-name override is scoped to full-frame geometry pops; bbox inpaint undos keep 'inpaint'; the superseded set is verified by grep and the suite.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/main_window.py`:
  - `_undo_op_label_for_result` — single-entry branch: `kind == "image"` + recorded op name + `(x, y) == (0, 0)` → the recorded geometry-op name; otherwise the kind-based label (bbox inpaint pops keep 'inpaint'); docstring documents the accepted two-consecutive-ops limitation
- `tests/test_gui_curves_dialog.py` (extended):
  - test_curves_apply_pushes_one_entry — undo-flash ('Undo: curves') and redo-flash ('Redo: curves') assertions + byte-exact redo restore
  - test_single_image_pop_keeps_inpaint_label — scoping guard (real push_image_action bbox entry → 'Undo: inpaint')

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history pop result → status flash | The popped record's shape (multi-kind vs single image entry) crosses into label resolution; a mislabel lies to the user about which op Ctrl+Z reversed (undo-feedback integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Tampering | _undo_op_label_for_result single-entry resolution | medium | mitigate | Prefer _last_geometry_op_name only for full-frame (0,0) image entries (the geometry-record shape, history_manager.py:435); bbox inpaint pops keep the kind-based 'inpaint' label; flash assertions ('Undo: curves'/'Redo: curves') + scoping guard test lock both directions (ASVS V7 feedback integrity) |
| T-06-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py -x -q` — the extended lifecycle test + scoping guard green (RED→GREEN proven on the mislabel)
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 598 + this plan's additions passed, 0 failed
- Grep gates: op-name set literal is exactly ('rotate', 'crop', 'curves', 'resize') — 'levels' exists only in the historical indented comment at :2895
</verification>

<success_criteria>
- Truth 26 ("The undo/redo flash op-name set is ('rotate', 'crop', 'curves', 'resize') — 'levels' replaced, not appended") is PASS-able at runtime: Ctrl+Z flashes 'Undo: curves', Ctrl+Shift+Z flashes 'Redo: curves'
- Ordinary single-kind pops (mask edit / box edit / bbox inpaint) keep their Phase-3 labels — the override is scoped by the (0,0) full-frame gate
- Full suite green; no new dependencies
</success_criteria>

<output>
Create `.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-07-SUMMARY.md` when done
</output>

<!-- planner-discipline-allow: 'curves' -->
<!-- planner-discipline-allow: 'levels' -->
<!-- planner-discipline-allow: 'inpaint' -->

