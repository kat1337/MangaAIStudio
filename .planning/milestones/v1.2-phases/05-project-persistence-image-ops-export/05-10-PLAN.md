---
phase: 05-project-persistence-image-ops-export
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_project.py
autonomous: true
requirements: [PROJ-01]
gap_closure: true
gap_ids: [G-05-1, G-05-2]

estimate:
  tokens: 20000
  raw_tokens: 10000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Open Project… (Ctrl+O) opens the file dialog and loads the selected manifest or page .mas without crashing (G-05-1)"
    - "Save Project As… opens the folder dialog at an EXISTING default <chapter>.mas-project folder, and saves land inside it (G-05-2, D-02)"
    - "A cancelled or redirected Save As leaves no stray empty .mas-project folder behind"
    - "The D-02 non-destructive overwrite contract of project_io.save_project is unchanged"
  artifacts:
    - "manga_ai_studio/gui/main_window.py — zero-arg triggered wiring for action_open_project/action_save_project + pre-created default dir in _choose_project_dir"
    - "tests/test_gui_project.py — 5 regression tests (open-trigger loads session, save-trigger saves in place, default dir exists pre-dialog, cancel cleanup, different-dir cleanup)"
  key_links:
    - "action_open_project.triggered -> zero-arg lambda -> _open_project() dialog path"
    - "_choose_project_dir -> QFileDialog.getExistingDirectory(dir=existing folder) -> project_io.save_project (mkdir exist_ok, D-02 untouched)"
---

<objective>
Close the two UAT gaps from 05-UAT.md test 1 (CR-01 end-to-end repro) — both are defects in `manga_ai_studio/gui/main_window.py`:

- **G-05-1 (blocker):** `action_open_project.triggered.connect(self._open_project)` — `QAction.triggered` emits the checked `bool` as its first argument, which lands in the `manifest_path` slot, skips the `is None` dialog branch, and crashes at `selected.name` with `AttributeError: 'bool' object has no attribute 'name'` (line 2065). Recent Projects works only because `_make_recent_project_opener` calls `_open_project(project_dir / "manifest.json")` with an explicit Path.
- **G-05-2 (major):** `_choose_project_dir` passes a NON-EXISTENT default folder (e.g. `<parent>/<chapter>.mas-project`, created only later by `project_io.save_project`'s `mkdir`) to `QFileDialog.getExistingDirectory`. The native dialog refuses the non-existent default, falls back to the source parent, and the save writes `manifest.json` + per-page `.mas` files directly into the album root — a D-02 violation.

Purpose: restore the PROJ-01 save/resume contract — Open Project… must work from the menu/shortcut, and Save Project As… must land inside the `<chapter>.mas-project/` sibling folder.
Output: production fix in `main_window.py`, 5 regression tests in `tests/test_gui_project.py`, `05-10-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-project-persistence-image-ops-export/05-UAT.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/core/project_io.py
@tests/test_gui_project.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Open Project… triggered-bool crash + harden the same wiring class (G-05-1)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_project.py</files>
  <read_first>
    - main_window.py:315 — the bug site: `self.action_open_project.triggered.connect(self._open_project)`
    - main_window.py:339 — the same latent class: `self.action_save_project.triggered.connect(self._save_project)` where `_save_project(force_as: bool = False)` would receive the injected bool
    - main_window.py:2035-2093 — `_open_project` routing; the else-branch `selected = manifest_path` (2062-2063) and `selected.name` (2065) are where the bool crashed
    - main_window.py:1763, 2414-2420 — `_make_recent_opener` / `_make_recent_project_opener` closures (already absorb `_checked` — leave unchanged)
    - tests/test_gui_project.py:46-95 — `_make_window`, `_stub_dir_dialog`, `_stub_open_dialog` helpers; 308-374 — the open-side test patterns
  </read_first>
  <behavior>
    - Test 1 `test_open_project_trigger_loads_session` (RED — fails on the current code): save a 2-page session, open a FRESH window, stub QFileDialog.getOpenFileName to return the manifest path, then call `window.action_open_project.trigger()` — the action must load the session (len(image_files) == 2, _project_dir set) WITHOUT raising AttributeError. Current code raises `AttributeError: 'bool' object has no attribute 'name'` because the triggered bool lands in the manifest_path slot.
    - Test 2 `test_save_project_trigger_saves_in_place` (wiring-contract guard — passes on current code, pins the hardened wiring): after a first Save As (project dir recorded), dirty the session again, stub the folder dialog, call `window.action_save_project.trigger()` — the folder dialog must NOT be shown (captured calls == []) and the save writes to the existing project dir. Guards against a future checkable action or changed default silently flipping the save into Save-As mode.
  </behavior>
  <action>
    Fix the QAction triggered-bool wiring bug class in manga_ai_studio/gui/main_window.py (per UAT G-05-1 diagnosis):

    - Line 315: replace `self.action_open_project.triggered.connect(self._open_project)` with the zero-arg wrapper `self.action_open_project.triggered.connect(lambda: self._open_project())`. QAction.triggered emits the checked bool as its first argument; PySide6 drops it for a zero-arg Python callable — the established in-file pattern (tool actions at 696-735 and rotate actions at 758-774 already connect via zero-arg lambdas). Do NOT add a `_checked` parameter to `_open_project` (the D-09 recent-opener closures already absorb `_checked`; keep the public dialog-driven path), and do NOT use functools.partial (the argument is still injected).
    - Line 339: same latent class — `self.action_save_project.triggered.connect(self._save_project)` where `_save_project(force_as: bool = False)` would receive the emitted bool: wrap as `lambda: self._save_project()`. Benign today (the emitted False equals the default) but identical to the crash class — close it.
    - Audit EVERY remaining `triggered.connect` target in main_window.py (lines 301-815, 1758, 2409, 2495, 2547-2548): all remaining targets are zero-arg methods, zero-arg lambdas, or signal-to-signal (`toggleViewAction().trigger` at 543/546). `canvas.zoom_in`/`zoom_out` (481/485) accept `wheel: bool = False` and are benign because triggered emits exactly False == the default — leave them unchanged. Record the audit conclusion in a one-line comment at line 315.
    - Add the two regression tests to tests/test_gui_project.py, reusing the module helpers (`_make_window`, `_stub_open_dialog`, `_stub_dir_dialog`, `_dirty`, `_isolate_settings`). Name them EXACTLY `test_open_project_trigger_loads_session` and `test_save_project_trigger_saves_in_place` — the Task 1 verify runs `python -m pytest tests/test_gui_project.py -q -k "trigger"`, which selects tests by name substring, so BOTH names MUST contain the substring "trigger" (natural naming like `test_open_project_action_*` would collect 0 tests and fail the verify with pytest exit 5). Test 1 (`test_open_project_trigger_loads_session`) goes through the REAL signal path — `window.action_open_project.trigger()` (pytest-qt emits synchronously, so the pre-fix AttributeError propagates out of trigger() and fails the test). Test 2 (`test_save_project_trigger_saves_in_place`): first `_save_project(force_as=True)` with the folder dialog stubbed, then `_dirty(window)`, clear the captured dialog calls, `window.action_save_project.trigger()`, and assert the dialog was never shown.
  </action>
  <acceptance_criteria>
    - The two tests named `test_open_project_trigger_loads_session` and `test_save_project_trigger_saves_in_place` exist in tests/test_gui_project.py, and `python -m pytest tests/test_gui_project.py -q -k "trigger"` collects exactly those 2 tests (no pre-existing test in this file matches "trigger") and both pass — never the exit-5 no-tests-collected failure.
    - `action_open_project.trigger()` loads a stubbed-manifest session with no exception; session state (image_files order, _project_dir) matches the dialog-path open.
    - `action_save_project.trigger()` with an existing project dir saves in place without showing the folder dialog.
    - No other QAction triggered connect in main_window.py passes a parameterized slot that could misinterpret the checked bool (audit documented in the code comment).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_gui_project.py -q -k "trigger"</automated>
  </verify>
  <done>`python -m pytest tests/test_gui_project.py -q -k "trigger"` collects and passes both pinned tests — `test_open_project_trigger_loads_session` and `test_save_project_trigger_saves_in_place` (exit 0; a non-pinned natural naming would collect 0 tests and exit 5); Open Project… via the action loads the dialog path with no AttributeError; the wiring audit is documented at line 315.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save Project As… — pre-create the default .mas-project folder before the dialog (G-05-2)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_project.py</files>
  <read_first>
    - main_window.py:1849-1866 — `_choose_project_dir`, the bug site: passes a NON-EXISTENT default to getExistingDirectory; the native dialog falls back to the source parent, so saves land in the album root (D-02 violation)
    - main_window.py:1892-1927 — `_save_project` routing through `_choose_project_dir` for first save / force_as
    - manga_ai_studio/core/project_io.py:328-360 — `save_project` (mkdir(parents=True, exist_ok=True) at :346 tolerates the pre-created folder; D-02 non-destructive overwrite unchanged)
    - tests/test_gui_project.py:46-83 — `_make_window` / `_stub_dir_dialog` (the stub appends `(a, k)` per call — the dir argument is positional index 2)
  </read_first>
  <behavior>
    - Test 1 `test_save_as_default_dir_precreated` (RED — fails on the current code): with a 2-page session open, stub QFileDialog.getExistingDirectory to CAPTURE its call and return the default path `tmp_path/chapter.mas-project`; call `window._choose_project_dir()` — the captured dir argument must be an EXISTING directory (`Path(calls[0][0][2]).is_dir()` is True) and equal the default. Current code passes a non-existent default, so the assertion fails.
    - Test 2 `test_save_as_cancel_cleanup` (RED): same session, stub the dialog to return "" (cancel) — `_choose_project_dir()` returns None AND the pre-created default folder `tmp_path/chapter.mas-project` was removed (no stray empty folder).
    - Test 3 `test_save_as_different_pick_cleanup` (RED): same session, create `tmp_path/elsewhere`, stub the dialog to return it — the method returns `tmp_path/elsewhere` AND the stray pre-created default folder was removed.
  </behavior>
  <action>
    Fix `_choose_project_dir` in manga_ai_studio/gui/main_window.py (per UAT G-05-2 diagnosis):

    - After computing `default` (logic unchanged: `<source-parent>/<chapter-name>.mas-project`, or `<image-parent>/<image-stem>.mas-project` for a single-page session), record `created = not default.exists()`.
    - If `created`, call `default.mkdir(parents=True, exist_ok=True)` inside try/except OSError: on failure log a warning, set `created = False`, and fall back to `first.path.parent` as the dialog default (the pre-fix behavior — Save As stays functional on read-only parents). The pre-creation is REQUIRED: the native folder dialog refuses a non-existent default and silently falls back to the source parent — which is exactly how saves leaked into the album root.
    - Pass `str(default)` to QFileDialog.getExistingDirectory (call shape unchanged).
    - After the dialog: on cancel (`not directory`) — if `created` and `default.is_dir()`, best-effort `default.rmdir()` inside try/except OSError (rmdir removes EMPTY directories only — the "only if created by us and left empty" guard; swallow + debug-log) — then return None. If the user picked a different folder (`Path(directory) != default`) — apply the same best-effort rmdir to the stray self-created default — then return `Path(directory)`. If the pick equals the default, leave it (the save populates it).
    - Do NOT touch project_io.save_project: its `mkdir(parents=True, exist_ok=True)` (project_io.py:346) tolerates the pre-created folder and its D-02 non-destructive overwrite semantics (only owned manifest.json + *.mas written/replaced; foreign folder content untouched) are unchanged by this fix.
    - Add the three regression tests to tests/test_gui_project.py using the module's `_stub_dir_dialog` helper. Name them EXACTLY `test_save_as_default_dir_precreated`, `test_save_as_cancel_cleanup`, `test_save_as_different_pick_cleanup` — the Task 2 verify runs `python -m pytest tests/test_gui_project.py -q -k "save_as"`, which selects tests by name substring, so ALL THREE names MUST contain the substring "save_as" (natural naming like `test_choose_project_dir_*` would collect 0 tests and fail the verify with pytest exit 5). Assert `Path(calls[0][0][2]).is_dir()` for the pre-dialog existence check (`test_save_as_default_dir_precreated`), `not (tmp_path / "chapter.mas-project").exists()` for the cleanup checks (`test_save_as_cancel_cleanup` and `test_save_as_different_pick_cleanup`; create `tmp_path/elsewhere` first in the latter).
  </action>
  <acceptance_criteria>
    - The three tests named `test_save_as_default_dir_precreated`, `test_save_as_cancel_cleanup`, `test_save_as_different_pick_cleanup` exist in tests/test_gui_project.py, and `python -m pytest tests/test_gui_project.py -q -k "save_as"` collects exactly those 3 tests (no pre-existing test in this file matches "save_as") and all pass — never the exit-5 no-tests-collected failure.
    - The folder dialog's dir argument points at an EXISTING directory for both single-page and multi-page sessions.
    - Cancel leaves no self-created empty default folder; a different pick removes the stray self-created default; the default pick keeps it for the save.
    - A save through the fixed dialog lands inside the .mas-project folder (manifest.json + per-page .mas), not the album root (D-02).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_gui_project.py -q -k "save_as"</automated>
  </verify>
  <done>`python -m pytest tests/test_gui_project.py -q -k "save_as"` collects and passes all three pinned tests — `test_save_as_default_dir_precreated`, `test_save_as_cancel_cleanup`, `test_save_as_different_pick_cleanup` (exit 0; a non-pinned natural naming would collect 0 tests and exit 5); the folder dialog opens at an existing default folder and cleanup leaves no stray directories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qt action system -> slot wiring | The checked-bool argument of `QAction.triggered` crosses into parameterized slots; a misread argument previously crashed the session open path (G-05-1) |
| Native folder dialog -> filesystem | `QFileDialog.getExistingDirectory` receives a default path; a non-existent default silently redirects the save target (G-05-2), and our fix creates/removes directories on the user's filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10-01 | DoS | `_open_project` slot wiring (main_window.py:315) | medium | mitigate | Zero-arg lambda keeps the injected checked-bool out of the manifest_path slot; regression test `action_open_project.trigger()` asserts the session loads with no AttributeError |
| T-05-10-02 | Tampering | `_choose_project_dir` dir creation/cleanup (main_window.py:1849-1866) | medium | mitigate | Only a directory the method ITSELF created (`created` flag) and that is EMPTY (`rmdir()` removes only empty dirs, OSError swallowed+logged) is ever removed; pre-existing folders and user picks are never touched; mkdir failure falls back to the parent default with Save As still functional |
| T-05-10-03 | Tampering | `_save_project` latent arg injection (main_window.py:339) | low | mitigate | Zero-arg lambda wrap; regression test pins that trigger() saves in place without forcing the folder dialog |
</threat_model>

<verification>
- `python -m pytest tests/test_gui_project.py -q` — the full GUI project-persistence file passes: the 16 existing tests + 5 new regression tests.
- `python -m pytest -q` — the full suite passes: 498 collected (the 493-test baseline measured at 05-09 + 5 new), 0 failed.
- Manual re-check (human gate — `human_verify_mode: end-of-phase`): repeat UAT test 1's CR-01 repro — press Ctrl+O → the Open Project file dialog appears and the session loads; choose Save Project As… → the folder dialog opens AT an existing `<chapter>.mas-project` folder and the save writes inside it, never into the album root.
</verification>

<success_criteria>
- G-05-1 closed: `action_open_project.trigger()` loads a stubbed-manifest session without AttributeError (regression test green); the triggered-connect audit found no other slot that misreads the checked bool.
- G-05-2 closed: `_choose_project_dir` hands the dialog an existing default folder; cancel and different-pick cleanup verified by tests; saves land inside the `.mas-project` folder per D-02.
- Full suite green at ≥ 498 passed, 0 failed; UAT test 1 re-passes on manual re-run.
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-10-SUMMARY.md` when done
</output>
