---
phase: 05-project-persistence-image-ops-export
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_gui_boxes.py
autonomous: true
gap_closure: true
requirements: [PROJ-04]
user_setup: []

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "test_moved_box_via_real_events_persists_round_trip passes deterministically in every environment: the real-event drag moves the box via rect() by ~50px with the 60x60 shape intact, and the round-trip restores EXACTLY the moved position"
    - "The regression value is preserved: a pos()/rect() divergence defect (box left at (20,20,80,80)) still fails the precondition — the assertions are truncation-tolerant, not weaker"
    - "No production code changes: canvas.py is untouched (drag math verified exact — the delta delivered is applied verbatim)"
    - "Full suite after the fix: 462 collected / 462 passed (baseline at plan time was 462 collected / 461 passed / 1 failed)"
  artifacts:
    - "tests/test_gui_boxes.py -- fixed assertions in test_moved_box_via_real_events_persists_round_trip + root-cause docstring"
  key_links:
    - "precondition assertion and round-trip assertion both anchor on the DELIVERED move position (moved_now), not a hardcoded literal — environment-independent by construction"
---

<objective>
Gap closure (BLOCKER remediation): the pre-existing deterministic failure `tests/test_gui_boxes.py::test_moved_box_via_real_events_persists_round_trip` (Phase 4 UAT re-test 4 regression) breaks every plan's "full suite green" acceptance criterion and 05-04 Task 2's targeted `tests/test_gui_boxes.py` verify. Measured at plan time: **462 collected / 461 passed / 1 failed**.

**Root cause (verified live, not assumed):** the canvas move math is EXACT — `mouseMoveEvent` computes `new_rect = move_anchor_box_pos + (curr - drag_anchor)` and applies the delivered scene delta verbatim (probe: a delivered 49.09px delta moved the box exactly 49.09px). The 1px shortfall comes from the TEST's delivery chain: PySide6's `mapFromScene`/`mapToScene` expose only int-truncating overloads and `QTest.mousePress/Move/Release` accepts only int `QPoint` (QPointF raises TypeError — verified), while the MainWindow canvas's fit-to-window transform is FRACTIONAL in the pytest environment (measured scale 0.18333, viewport 1188x1037; standalone runs at scale 0.2 with integer offsets land exactly at (70,70,130,130)). The int truncation of fractional viewport coords loses ~0.91 scene px → the delivered delta is 49.09 → box lands at (69,69,129,129). The defect is in the TEST's over-precise assertions against its own int-truncating delivery mechanism — NOT in canvas.py (Phase 4 code is correct). Per the checker's verified-fix instruction: fix the test, do not touch production code.

**Fix shape:** (1) precondition asserts the box MOVED via rect() by ~50px (tolerance for sub-pixel truncation) with the 60x60 shape intact — a pos()-divergence defect leaves rect() at the original and fails this check; (2) the round-trip assertion compares against the ACTUAL moved position (`moved_now`) — the real regression (persisted == moved) checked exactly, environment-independent; (3) the docstring documents the artifact.

Purpose: unblock the phase's green baseline so every plan's full-suite criterion and 05-04 Task 2's `tests/test_gui_boxes.py` verify (Wave 2) run against a green suite. Runs in **Wave 1** (parallel-safe: no file overlap with 05-01/02/03) and MUST land before Wave 2 starts.

Output: fixed `tests/test_gui_boxes.py` in a SEPARATE commit; full suite green (462/462).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/test_gui_boxes.py
@manga_ai_studio/gui/canvas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the move-round-trip regression test — truncation-tolerant assertions (test-only, separate commit)</name>
  <files>tests/test_gui_boxes.py</files>
  <read_first>
    - tests/test_gui_boxes.py (the failing function test_moved_box_via_real_events_persists_round_trip at ~line 915 and its driver _drive_real_body_move at ~line 747; the sibling canvas-only test test_boxitem_pos_and_rect_do_not_diverge_after_move at ~line 772 — UNCHANGED, it uses an integer-exact canvas-only transform and must keep its exact (70,70) assertions; the test-gap note at lines 700-722 explaining why real QTest delivery is the contract)
    - manga_ai_studio/gui/canvas.py (mouseMoveEvent box-move branch at 1013-1025 — the EXACT math to reference in the docstring; do NOT modify this file)
  </read_first>
  <action>
    Modify ONLY `tests/test_gui_boxes.py::test_moved_box_via_real_events_persists_round_trip` (no production code, no other test functions, no changes to `_drive_real_body_move` — QTest int delivery is the documented strongest approximation and stays):

    - Update the test docstring: document the measured root cause — PySide6 QTest + mapFromScene deliver INT viewport coords while the MainWindow canvas fit-to-window transform is fractional in the pytest environment (measured scale 0.18333, viewport 1188x1037), so a 50px scene drag is delivered ~0.9px short (delta 49.09 → box at (69,69,129,129)); the canvas applies the delivered delta exactly (canvas.py:1013-1025) — this is a TEST-side sub-pixel truncation artifact, not a canvas defect. State that the assertions below are therefore truncation-tolerant while still failing on the real UAT re-test 4 defect (a pos()/rect() divergence leaves rect() at (20,20,80,80) → delta 0).

    - Precondition (replaces `assert moved_now == (70, 70, 130, 130)`; `moved_now = item.current_box().as_tuple` is a (x1,y1,x2,y2) TUPLE — assert on indices):
      - `moved_now[0] != 20 or moved_now[1] != 20` — the box moved at all (message: a pos()-routed move leaves rect() at the ORIGINAL position — UAT re-test 4 defect).
      - `moved_now[0] - 20 == moved_now[1] - 20` — the diagonal drag moves both axes equally (message: drag was (50,50)→(100,100), the delivered scene delta is axis-symmetric).
      - `45 <= moved_now[0] - 20 <= 50` — moved ~50px with sub-pixel truncation tolerance (message: the ~0.9px measured truncation at the fit scale; a move through pos() yields 0 and fails).
      - `moved_now[2] - moved_now[0] == 60 and moved_now[3] - moved_now[1] == 60` — the 60x60 shape is preserved (a move never changes width/height; a resize bug would fail here).

    - Round-trip (replaces `assert restored[0].box.as_tuple == (70, 70, 130, 130)`): `assert restored[0].box.as_tuple == moved_now` — the persisted position must EQUAL the actually-moved position (the UAT re-test 4 persistence regression, checked exactly and environment-independent; message: the moved position must persist across the A→B→A round-trip; if it is the ORIGINAL (20,20,80,80) the move was visible but the snapshot materialized the stale rect()).

    - Do NOT weaken the surrounding flow (folder setup, real-event move, A→B→A navigation, boxes_snapshot checks) — only the two assertion blocks and the docstring change.

    Commit SEPARATELY (per the checker's separate-commit requirement): `fix(test): make move-round-trip regression truncation-tolerant (QTest int delivery at fractional fit scale)`.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py::test_moved_box_via_real_events_persists_round_trip tests/test_gui_boxes.py::test_boxitem_pos_and_rect_do_not_diverge_after_move tests/test_gui_boxes.py::test_corner_resize_via_real_qtest_events_changes_rect -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_moved_box_via_real_events_persists_round_trip passes (precondition + round-trip both green in the pytest environment where it previously failed with (69,69,129,129))
    - test_boxitem_pos_and_rect_do_not_diverge_after_move still passes UNCHANGED with its exact (70,70) assertions (the integer-exact canvas-only path is unaffected)
    - test_corner_resize_via_real_qtest_events_changes_rect still passes
    - `git show --stat HEAD` shows ONLY tests/test_gui_boxes.py in the fix commit (separate-commit requirement; no manga_ai_studio/ changes)
    - `grep -c "moved_now" tests/test_gui_boxes.py` is >= 2 (both assertions anchor on the delivered move)
  </acceptance_criteria>
  <done>The regression test is fixed and green with its regression value intact; committed separately with no production changes.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite re-measure — 462 collected / 462 passed baseline restored</name>
  <files>tests/test_gui_boxes.py (no changes expected — re-measurement only; Task 1 made this file's only modifications)</files>
  <action>
    Re-measure the full suite after Task 1's fix (the checker requires re-measurement at execution):

    - Run the full suite; assert the result is 462 collected / 462 passed, 0 failed (the measured pre-fix state was 462 collected / 461 passed / 1 failed).
    - If any OTHER test fails, STOP and report — the suite was green except the one pre-existing failure before this plan.
    - Record the new measured baseline (collected / passed / failed) in the SUMMARY file's baseline note so later plans reference the post-fix state.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full suite: 462 collected / 462 passed, 0 failed
    - The post-fix baseline (462/462) is recorded in 05-09-SUMMARY.md
  </acceptance_criteria>
  <done>Full suite green again; new measured baseline recorded for later plans.</done>
</task>

</tasks>

## Artifacts this plan produces (plan 05-09)

- `tests/test_gui_boxes.py` (MODIFY) — `test_moved_box_via_real_events_persists_round_trip`: truncation-tolerant precondition + moved-position-anchored round-trip assertion + root-cause docstring

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21 | Tampering | test assertions weakened by tolerance | low | mitigate | tolerance is bounded (45..50 of a 50px drag) and the pos()-divergence failure mode (delta 0) still trips it; shape check (60x60) catches move-vs-resize confusion; the exact persistence check compares against moved_now |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this plan (RESEARCH Package Legitimacy Audit); no checkpoints needed |

<verification>
- Task 1: targeted real-event tests (move round-trip + the two sibling real-event regressions) via pytest
- Task 2: full suite `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` → 462 collected / 462 passed
- Wave note: this plan lands in Wave 1 alongside 05-01/02/03 and must complete before Wave 2 (05-04) — 05-04 Task 2's verify targets tests/test_gui_boxes.py
</verification>

<success_criteria>
- The pre-existing 1px-shortfall failure is remediated (test-only fix, separate commit)
- Full suite measured green: 462 collected / 462 passed
- No production code changed; the UAT re-test 4 regression value (persisted == moved) is intact
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-09-SUMMARY.md` when done.
</output>
