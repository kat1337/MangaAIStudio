---
phase: 08-masker-selective-inpaint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/box_model.py
  - panelcleaner/config.py
  - manga_ai_studio/core/image_ops.py
  - manga_ai_studio/__main__.py
  - tests/test_core/test_box_model.py
  - tests/test_core/test_masker_config_roundtrip.py
  - tests/test_core/test_image_ops.py
autonomous: true
requirements: [MASK-01, MASK-02, MASK-03]
must_haves:
  truths:
    - "PageBox carries a tri-state inpaint override field named inpaint_override (None=Auto, 'always', 'never') — NOT manual_override, which stays the Phase 4 reading-order pin"
    - "PageBox.inpaint_state(threshold) is a pure function returning one of 'will_inpaint' / 'gate_skipped' / 'forced' / 'never' from override + std_dev + mask content"
    - "MaskerConfig has a persisted mask_dilation_radius field defaulting to 2 that round-trips through the profile INI"
    - "Rotate/crop/resize carry inpaint_override and style onto the rebuilt PageBox and reset mask/std_dev to None"
  artifacts:
    - manga_ai_studio/core/box_model.py (extended)
    - panelcleaner/config.py (one new field + export/import lines)
    - tests/test_core/test_masker_config_roundtrip.py (new)
  key_links:
    - "inpaint_state(threshold) is the single derivation consumed by BoxItem.set_inpaint_state (plan 08-06) and refresh_box_inpaint_states (plan 08-07)"
    - "mask_dilation_radius is the single knob consumed by derive_page_mask_state (plan 08-03), the ToolsPanel slider (plan 08-05), and batch (plan 08-09)"
---

<objective>
Foundations for Phase 8: the per-box override + state-derivation model, the MASK-01 dilation radius as a persisted MaskerConfig field (D-09/D-10), and the geometry-op field-carry policy that keeps the new fields from being silently dropped (RESEARCH Pitfall 13-6).

Purpose: Every later Phase 8 plan consumes these symbols. Delivering them first, headless and Qt-free where possible, lets the seam core (08-03), serialization (08-04), and UI plans build in parallel.

Output: Extended PageBox (`inpaint_override`, `inpaint_state`, detached `copy()`), extended MaskerConfig (`mask_dilation_radius`) with INI round-trip, startup profile load, geometry-op field policy, and regression tests for all of it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-RESEARCH.md
@.planning/phases/08-masker-selective-inpaint/08-PATTERNS.md
manga_ai_studio/core/box_model.py
panelcleaner/config.py
manga_ai_studio/core/image_ops.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PageBox inpaint_override field + inpaint_state() + copy() detachment</name>
  <files>manga_ai_studio/core/box_model.py, tests/test_core/test_box_model.py</files>
  <read_first>
    - manga_ai_studio/core/box_model.py (the whole file — field block :92-105, copy() :177-193, has_recognized_text pure-method shape :162-174; NOTE :101 `manual_override` is TAKEN by Phase 4)
    - .planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §Color "The border-state contract" table + §37 state derivation wording (the exact state semantics this function must implement)
    - .planning/phases/08-masker-selective-inpaint/08-PATTERNS.md file 7 (field + copy() analogs)
  </read_first>
  <behavior>
    - Test: a fresh PageBox has inpaint_override None; assigning "always"/"never" round-trips.
    - Test: inpaint_state matrix — override "always" -> "forced" (regardless of std_dev); override "never" -> "never"; override None + std_dev=8.0 + mask with content + threshold 15 -> "will_inpaint"; override None + std_dev=20.0 + threshold 15 -> "gate_skipped"; override None + std_dev None -> "gate_skipped"; override None + std_dev=8.0 + mask None -> "gate_skipped"; override None + std_dev=8.0 + empty mask (all-white PIL "1", getbbox() None) -> "gate_skipped".
    - Test: copy() detaches the mask — pb.copy().mask is not pb.mask when pb.mask is a PIL Image (mutating/replacing the original leaves the copy unaffected); copy() preserves std_dev and inpaint_override.
  </behavior>
  <action>
    Add field `inpaint_override: Optional[str] = None` to PageBox after `style` (box_model.py:105), with a docstring line documenting D-14's tri-state encoding: None = Auto (std-dev gate decides), "always" = force inpaint, "never" = skip. The field name is `inpaint_override` per the RESEARCH Q3 recommendation — `manual_override` is already the Phase 4 reading-order pin (box_model.py:101) and MUST NOT be reused or renamed. Extend the class docstring's D-15 seam note: mask/std_dev are now POPULATED by Phase 8 (drop the "DEFERRED" wording from the field comments at :95-96, keep the field declarations).

    Add method `inpaint_state(self, threshold: float) -> str` mirroring the pure-method shape of `has_recognized_text` (:162-174). Returns exactly one of "forced", "never", "will_inpaint", "gate_skipped": "forced" when self.inpaint_override == "always"; "never" when self.inpaint_override == "never"; "will_inpaint" when override is None AND self.std_dev is not None AND self.std_dev <= threshold AND the box has auto mask content; else "gate_skipped". Auto mask content check goes in a small private helper `_has_auto_mask_content()` returning True iff self.mask is not None and self.mask.getbbox() is not None (PIL mode-"1" semantics; mask is stored box-cropped per plan 08-03). This is the single derivation site — BoxItem (08-06) and MainWindow refresh (08-07) consume it; do not duplicate the logic.

    Extend `copy()` (:191-193) to also pass `mask=_copy.copy(self.mask) if self.mask is not None else None` into the `replace(...)` call — Pitfall 8: PIL Image is mutable; without detachment a BOXES undo would restore post-edit masks (the 04-05 lesson applied to the new field).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_box_model.py -q</automated>
  </verify>
  <done>
    - Source assertion: box_model.py contains the literal field declaration `inpaint_override: Optional[str] = None` and a method `def inpaint_state(self, threshold: float) -> str:`.
    - Source assertion: copy() body contains `mask=_copy.copy(self.mask)` (or an equivalent explicit None-guarded mask detachment).
    - Test command: pinned-interpreter pytest on tests/test_core/test_box_model.py passes, including the new state-matrix and copy-detachment tests listed in behavior.
    - The strings "forced", "never", "will_inpaint", "gate_skipped" are the only return values of inpaint_state (asserted by the matrix test).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MaskerConfig.mask_dilation_radius + INI round-trip + startup profile load</name>
  <files>panelcleaner/config.py, manga_ai_studio/__main__.py, tests/test_core/test_masker_config_roundtrip.py</files>
  <read_first>
    - panelcleaner/config.py :553-673 (MaskerConfig @define block, export_to_conf INI string block, import_from_conf try_to_load lines — verified anchors)
    - manga_ai_studio/__main__.py (whole file, 45 lines — ProfileManager construction at :23-25)
    - manga_ai_studio/config/profile_manager.py (save_profile / load_profile signatures)
    - .planning/phases/08-masker-selective-inpaint/08-RESEARCH.md §4.1-§4.2 (startup gap + option-(a) recommendation)
  </read_first>
  <behavior>
    - Test: default MaskerConfig().mask_dilation_radius == 2 (D-09).
    - Test: a Profile with masker.mask_dilation_radius = 5 saved via ProfileManager into a tmp dir and reloaded yields mask_dilation_radius == 5 (full INI round-trip through safe_write/load).
    - Test: a profile INI written WITHOUT the mask_dilation_radius key (simulate a PanelCleaner-authored INI) loads with the default 2 (unknown/missing options fall back gracefully).
    - Test: importing a ProfileManager whose config dir holds a "default" profile INI with radius 7 makes config.current_profile.masker.mask_dilation_radius == 7 after load_profile("default").
  </behavior>
  <action>
    Add field `mask_dilation_radius: Pixels = 2` to the vendored MaskerConfig (panelcleaner/config.py, after `mask_selection_fast`, before `debug_mask_color`). This is a conscious deviation from near-verbatim vendoring per RESEARCH §4.2 option (a) — add a one-line comment above the field marking it as a Manga AI Studio addition (Phase 3 D-14 discipline, like the 03-01 qualified-name rewrites).

    In `export_to_conf` (:576-640), add an INI block for the new key inside the f-string, with a comment that doubles as the canonical tooltip text (D-06/D-12): state that it grows auto-detected masks by N pixels so letter edges get covered, that hand-painted strokes are never dilated, and that changing it re-dilates the current page immediately. Emit the value as `mask_dilation_radius = {self.mask_dilation_radius}`.

    In `import_from_conf` (:642-673), add `try_to_load(self, config_updater, section, Pixels, "mask_dilation_radius")` beside the existing lines.

    In manga_ai_studio/__main__.py, after the ProfileManager construction (:23-25), load the persisted profile: call `profile_manager.load_profile("default")` guarded by a try/except OSError that logs and continues (Profile.load itself falls back to defaults on malformed INI, config.py:1031-1034 — the guard only covers filesystem errors). This closes the RESEARCH §4.1 startup gap so D-10 persistence actually loads at launch.

    Create tests/test_core/test_masker_config_roundtrip.py with the four behavior tests above, using the tmp_path ProfileManager pattern from the existing profile tests.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_masker_config_roundtrip.py -q</automated>
  </verify>
  <done>
    - Source assertion: panelcleaner/config.py MaskerConfig block contains `mask_dilation_radius: Pixels = 2`; export_to_conf contains the literal `mask_dilation_radius = {self.mask_dilation_radius}`; import_from_conf contains a try_to_load call naming "mask_dilation_radius".
    - Source assertion: __main__.py calls load_profile with the name "default" before MainWindow construction.
    - Test command: pinned-interpreter pytest on tests/test_core/test_masker_config_roundtrip.py passes (default 2, round-trip 5, missing-key fallback 2, startup-load 7).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Geometry-op field policy — carry inpaint_override + style, invalidate mask/std_dev</name>
  <files>manga_ai_studio/core/image_ops.py, tests/test_core/test_image_ops.py</files>
  <read_first>
    - manga_ai_studio/core/image_ops.py :127-163 (transform_box_payload — the rotate rebuild), :283-290 (crop _clip_box rebuild), :370-422 (resize_boxes rebuild) — all three construct PageBox with an explicit field list that already drops `style`
    - .planning/phases/08-masker-selective-inpaint/08-RESEARCH.md §6.5 (Pitfall 13, the field-drop trap) and Q7
    - tests/test_core/test_image_ops.py (existing geometry-transform tests — extend, do not rewrite)
  </read_first>
  <behavior>
    - Test (rotate, crop, resize — one per transform): a PageBox with inpaint_override="never" and style=TextStyle(font_family="X") survives the transform with both values intact.
    - Test: a PageBox with mask set (a small PIL "1" image) and std_dev=9.5 comes out of every transform with mask None and std_dev None (deliberate invalidation — a rotated std-dev/border relation is stale; refit happens on the next fit trigger).
  </behavior>
  <action>
    In all three PageBox rebuild sites — transform_box_payload (:156-163), the crop path (:283-290), and resize_boxes (:413-422) — add `inpaint_override=pagebox.inpaint_override` and `style=pagebox.style` to the explicit constructor field lists, and set `mask=None, std_dev=None` on the rebuilt PageBox. Update the adjacent NOTE comment at :158-162: style is now carried (closing the live Phase 7 latent drop in the same constructors we are already editing), and mask/std_dev are deliberately invalidated per the Phase 8 policy (RESEARCH §6.5 option b; override is user intent and cheap to carry).

    Do not change any geometry math — only the PageBox field lists and their comments.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_image_ops.py -q</automated>
  </verify>
  <done>
    - Source assertion: each of the three rebuild sites passes inpaint_override and style through and sets mask=None and std_dev=None on the new PageBox.
    - Test command: pinned-interpreter pytest on tests/test_core/test_image_ops.py passes including the new carry/invalidate tests.
    - Full-suite spot check: pinned-interpreter pytest on tests/test_core/test_box_model.py tests/test_core/test_masker_config_roundtrip.py tests/test_core/test_image_ops.py is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile INI on disk → MaskerConfig | User-editable config file crossing into app state (existing vendored boundary; one new key joins it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | MaskerConfig INI import | low | mitigate | The new key loads only through the vendored `try_to_load(..., Pixels, ...)` typed coercion — a garbage value raises inside the existing guarded path and falls back to defaults (config.py:1031-1034), same as every other Masker key. |
| T-08-01b | DoS | inpaint_state / config values | low | accept | Values are range-bounded floats/ints used only in comparisons; no allocation or parse surface. |
</threat_model>

<verification>
- Pinned-interpreter full test suite green after this plan (baseline 717 collected + new tests).
- `inpaint_state` semantics match the 08-UI-SPEC §Color border-state table exactly (matrix test is the contract).
</verification>

<success_criteria>
- All three tasks' acceptance criteria hold; no GUI files touched (plan is headless).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 08-01)

New symbols:
- `PageBox.inpaint_override: Optional[str] = None` (field; None/"always"/"never")
- `PageBox.inpaint_state(threshold: float) -> str` (returns "will_inpaint" | "gate_skipped" | "forced" | "never")
- `PageBox._has_auto_mask_content() -> bool` (private helper)
- `MaskerConfig.mask_dilation_radius: Pixels = 2` (vendored config field + INI export/import lines)
- `manga_ai_studio/__main__.py` startup `load_profile("default")` call
- tests/test_core/test_masker_config_roundtrip.py (new test file)

Changed contracts:
- `PageBox.copy()` now detaches `mask`
- core/image_ops.py rotate/crop/resize rebuilds now carry `inpaint_override` + `style` and invalidate `mask`/`std_dev`
</artifacts>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-01-SUMMARY.md` when done
</output>
