---
phase: 08-masker-selective-inpaint
plan: 06
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - manga_ai_studio/gui/box_item.py
  - tests/test_gui_border_states.py
autonomous: true
requirements: [MASK-03]
must_haves:
  truths:
    - "A box's border encodes the inpaint outcome at a glance: solid = C will inpaint its detected text, dashed = it won't; origin hue = the auto gate decided, grey = the user's override decided (D-11/D-12, UI-SPEC §Color A1)"
    - "The four states render exactly: will_inpaint = origin hue 2px SOLID; gate_skipped = origin hue 2px DASHED (6/4 scene px); forced = #e8e8ea 2px SOLID; never = #9a9aa2 2px DASHED"
    - "Selection stays width-encoded (3px + tint + handles) with the state's hue/style at selected width — the Phase 3/7 affordance is untouched"
    - "No visual change to handles, bubble badge, text overlay, or typeset text — only the box rect's QPen participates (D-18 discipline)"
  artifacts:
    - `BoxItem.set_inpaint_state(state)` + the state pen derivation
  key_links:
    - "state derivation input is PageBox.inpaint_state(threshold) (08-01) — BoxItem never computes the gate itself"
    - "MainWindow.refresh_box_inpaint_states() (08-07) is the sole caller driving set_inpaint_state"
---

<objective>
Add the inpaint-state dimension to the box border rendering (D-11's token mapping, locked by UI-SPEC A1) as a self-contained BoxItem extension.

Purpose: MASK-03's visibility half — the predictive indicator ("what would C do right now?"). The rendering must land before the seam plan wires the refresh triggers so 08-07 only has to call it.

Output: `BoxItem.set_inpaint_state(state)` extending `_apply_origin_pen`/`_apply_look_for`, with pen/dash/tint behavior locked by tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-RESEARCH.md (§8 — border-state evidence)
@.planning/phases/08-masker-selective-inpaint/08-PATTERNS.md (file 6)
@.planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §Color border-state contract table + §37 (executor-authoritative colors/dash/selection interplay)
manga_ai_studio/gui/box_item.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BoxItem.set_inpaint_state — the state pen dimension</name>
  <files>manga_ai_studio/gui/box_item.py, tests/test_gui_border_states.py</files>
  <read_first>
    - manga_ai_studio/gui/box_item.py :540-640 (_apply_origin_pen :549-566, _apply_look_for :626-639, itemChange :606-624 — the pen derivation + re-apply hooks), plus the origin_hue helper and the _SELECTED_PEN_WIDTH/_UNSELECTED_PEN_WIDTH/_TINT_ALPHA constants
    - .planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §Color (the border-state contract table + reading rules 1-3) and §Accessibility (pattern-over-hue rationale)
    - manga_ai_studio/core/box_model.py (inpaint_state return values from 08-01)
  </read_first>
  <behavior>
    - Test: for state "will_inpaint" unselected — pen color equals the origin hue (green #5fd068 detected / amber #f5a623 user), style SolidLine, width 2; dash pattern empty.
    - Test: "gate_skipped" unselected — origin hue, style CustomLine with dash pattern [6, 4], width 2.
    - Test: "forced" unselected — color #e8e8ea, SolidLine, width 2.
    - Test: "never" unselected — color #9a9aa2, CustomLine [6, 4], width 2.
    - Test: selected variants — width 3 and brush tint alpha per state (origin hue tint for will_inpaint/gate_skipped; rgba(232,232,234) tint for forced; rgba(154,154,162) tint for never); handles still visible.
    - Test: set_inpaint_state(None) renders the Phase 3 look (origin hue, solid, no dash) — backward-compat default before any refresh call.
    - Test: selection change (setSelected) re-derives the pen with the CURRENT state (the itemChange hook).
  </behavior>
  <action>
    Add `self._inpaint_state: str | None = None` to BoxItem.__init__ and a public `set_inpaint_state(self, state: str | None) -> None` that stores it and re-derives the pen by calling the same path `_apply_origin_pen` uses (or `_apply_look_for` when selected), then `update()` — one item repaint, negligible cost (PATTERNS file 6 shape).

    Extend the pen derivation: introduce a helper `_inpaint_pen_color(base_hue) -> QColor` returning QColor("#e8e8ea") when state == "forced", QColor("#9a9aa2") when state == "never", else the origin hue; and apply the dash: states "gate_skipped" and "never" set `pen.setStyle(Qt.PenStyle.CustomLineLine)`-equivalent with `setDashPattern([6, 4])` (scene units — scales with zoom like the border itself), all other states SolidLine. Selection semantics stay width-encoded: selected = current state color at width 3 + tint (tint from the same state color at _TINT_ALPHA — giving rgba(232,232,234,~0.12) for forced and rgba(154,154,162,~0.12) for never per the UI-SPEC table); unselected = width 2, NoBrush. Wire the derivation through `_apply_origin_pen` (:549-566) and `_apply_look_for` (:626-639) so itemChange's ItemSelectedChange re-apply (:606-624) keeps working unchanged. Do NOT touch CornerHandle, the text overlay, the badge, or hit-testing.

    Create tests/test_gui_border_states.py (gui marker): construct BoxItems (small page scene, DETECTED and USER origins), call set_inpaint_state for each of the four states + None, and assert pen color/style/dashPattern/width and brush tint per the behavior block (read back via `item.pen()`).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui_border_states.py -q</automated>
  </verify>
  <done>
    - Source assertion: box_item.py defines `def set_inpaint_state(self, state)` and applies a [6, 4] dash pattern for the two skipped states; `_apply_origin_pen`/`_apply_look_for` consume the state.
    - Test command: pinned-interpreter pytest on tests/test_gui_border_states.py passes with the full state x selection matrix (4 states + None, selected + unselected, both origins).
    - Full suite green (pinned interpreter) — existing box rendering tests unaffected (set_inpaint_state(None) default preserves the Phase 3 look).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure rendering extension; state strings come from PageBox.inpaint_state (08-01), not from file input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-11 | Repudiation | border indicator vs mask truth | low | mitigate | Solid/dashed is derived from the SAME pure function (inpaint_state) that gates mask contribution (08-03 compose_auto_binary uses identical inputs) — the border cannot disagree with the mask by construction; UI-SPEC reading rule 4 states the equivalence. |
</threat_model>

<verification>
- Pinned-interpreter full suite green.
- Colors/dash match the UI-SPEC §Color table exactly (no new hex values; #e8e8ea/#9a9aa2 are reused palette members).
</verification>

<success_criteria>
- All acceptance criteria hold; zero changes outside box_item.py + the new test file.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 08-06)

New symbols:
- `BoxItem._inpaint_state` attribute (str | None, default None)
- `BoxItem.set_inpaint_state(state: str | None)` (public API consumed by 08-07's refresh_box_inpaint_states)
- `BoxItem._inpaint_pen_color(base_hue)` helper
- Border-state constants: forced hex #e8e8ea, never hex #9a9aa2, dash pattern [6, 4] scene px
- tests/test_gui_border_states.py (new test file)
</artifacts>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-06-SUMMARY.md` when done
</output>
