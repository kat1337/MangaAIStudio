---
phase: 08-masker-selective-inpaint
plan: 07
type: execute
wave: 3
depends_on: [08-02, 08-03, 08-04, 08-05, 08-06]
files_modified:
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - tests/test_gui_detection_boxes.py
autonomous: true
requirements: [MASK-01, MASK-02, MASK-05]
must_haves:
  truths:
    - "Detect Boxes ON: detection builds boxes FIRST, derives the box-constrained dilated auto plane, populates per-box mask/std_dev, and refreshes border states — heatmap content outside boxes never enters the mask (D-02/MASK-05)"
    - "Detect Boxes OFF: the full dilated heatmap lands in the auto plane exactly as Phase 1 did, no boxes, no gate (D-03); the pre-dilation binary is retained in BOTH modes (D-08)"
    - "A D-04 Cancel aborts the whole finish handler BEFORE any mask or box mutation (the reorder trap fixed)"
    - "Changing the dilation radius re-derives the current page from the retained raw mask without re-running the model (D-08 live); changing the std-dev threshold re-gates + recomposes without re-fitting (D-12 predictive)"
    - "Detection remains a non-undoable baseline (no MASK/BOXES pushes); it marks the session dirty explicitly"
    - "The save loop persists the four plane binaries (raw/auto/manual/erase unpacked from the ImageFile packed slots into the build_page_entries dict) so a .mas project restores border states and the auto plane without a re-detect (criterion 5); box move/resize release and user-box create re-fit the changed boxes and refresh states"
  artifacts:
    - reworked `_on_detection_finished` / `_build_detected_boxes`
    - `MainWindow.refresh_box_inpaint_states()`, `_refit_changed_boxes()`, live-control slots, restore-path wiring
    - `canvas.boxes_snapshot()` extended to carry mask/std_dev/inpaint_override
  key_links:
    - "_on_detection_finished consumes build_detected_pageboxes + derive_page_mask_state (08-03) and canvas.set_auto_binary (08-02)"
    - "tools_panel.dilation_changed/std_dev_threshold_changed (08-05) -> live slots here"
    - "refresh_box_inpaint_states -> PageBox.inpaint_state (08-01) -> BoxItem.set_inpaint_state (08-06)"
    - "save-loop page_state dict at the build_page_entries call site (main_window.py:2182) carries raw_binary/auto_binary/manual_binary/erase_binary unpacked from the ImageFile packed slots (08-02's _snapshot_current_page fills them) -> 08-04 build_page_entries writes the four container entries -> parse keys raw_packed/auto_packed/manual_packed/erase_packed -> the load-side restore wiring in Task 3"
---

<objective>
Rework the detection→mask seam in MainWindow: the reorder (boxes before mask, gate before mutation), the constrained derivation, the predictive border-state refresh, the live radius/threshold slots, the refit triggers, and the .mas-load restore path.

Purpose: This is where D-01/D-02/D-03/D-04/D-07/D-08/D-12 become user-visible behavior. Everything before this plan built the pieces; this plan connects them on the interactive path (batch is 08-09).

Output: New `_on_detection_finished` sequence + helpers, extended `_build_detected_boxes` consuming the extracted core, extended `boxes_snapshot`, live-control wiring, gate-copy rewording, inpaint completion copy, and the extended detection-seam test battery.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-RESEARCH.md (§3 — the reorder, §2.3-§2.4, §8, §9)
@.planning/phases/08-masker-selective-inpaint/08-PATTERNS.md (file 1 — the seam today, with the exact current code)
@.planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §37 refresh triggers + §Copywriting (Replace Mask reword A10, Inpaint tooltip/completion, surface 7)
@.planning/phases/08-masker-selective-inpaint/08-01-SUMMARY.md
@.planning/phases/08-masker-selective-inpaint/08-02-SUMMARY.md
@.planning/phases/08-masker-selective-inpaint/08-03-SUMMARY.md
manga_ai_studio/gui/main_window.py
manga_ai_studio/gui/canvas.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _on_detection_finished reorder + constrained derivation</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_detection_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py :3701-3742 (detect_text + pre-worker _confirm_replace_mask gate), :3863-3899 (_on_detection_finished — the seam to rework), :3901-4015 (_build_detected_boxes incl. the D-04 gate at :3929-3932 and _suppress_boxes_push at :3985), :4088-4110 (_confirm_replace_mask copy to reword)
    - manga_ai_studio/core/detection_boxes.py (build_detected_pageboxes / derive_page_mask_state / dilate_auto_mask from 08-03)
    - manga_ai_studio/gui/canvas.py set_auto_binary / recompose semantics (from 08-02)
    - tests/test_gui_detection_boxes.py :47-88 (the direct-drive harness + _isolate_settings)
    - .planning/phases/08-masker-selective-inpaint/08-PATTERNS.md file 1 (the current seam code verbatim + the gate-ordering pitfall)
  </read_first>
  <behavior>
    - Test (mode ON, boxes built): drive `window._on_detection_finished({"mask": heatmap_with_in_box_and_out_of_box_content, "blocks": [blk]})` with `_confirm_replace_boxes` patched True — the canvas composite mask contains ONLY the in-box content; the PageBox has non-None mask and float std_dev; the box's inpaint state renders (border pen dashed/solid per threshold).
    - Test (D-04 Cancel): patch `_confirm_replace_boxes` False with an existing detected box — the handler returns with the canvas mask UNCHANGED (byte-equal binary), boxes unchanged, no status "Detection complete".
    - Test (mode OFF): uncheck detect boxes — the FULL heatmap (dilated by the profile radius) lands in the composite; no boxes are built; raw retained.
    - Test: both modes store the pre-dilation binary on the current ImageFile.raw_detected_mask (packed).
    - Test: detection pushes NO history entries (undo stacks empty after a detect) and marks the current page dirty.
    - Test: re-detect replaces detected boxes (their mask/std_dev/inpaint_override reset to None on the NEW boxes) and keeps user boxes WITH their state (D-03; overrides on replaced boxes vanish with them).
  </behavior>
  <action>
    Rework `_on_detection_finished` (:3863-3899) into the new sequence:

    Mode ON (`action_detect_boxes_mode.isChecked()`):
    1. D-04 GATE FIRST — run the existing replace-detected pre-check (`box_origin_counts()[0] >= 1` -> `_confirm_replace_boxes()`); on Cancel, set status "Detection cancelled" and RETURN before ANY mask or box mutation (the reorder trap: today the full mask is composited before the gate runs).
    2. Build detected boxes via `_build_detected_boxes(result.get("blocks") or [])` (refactored in Task 2 to consume build_detected_pageboxes; D-03 replace-detected-keep-user preserved; _suppress_boxes_push discipline unchanged).
    3. Read the current page image (`self.canvas.get_image_numpy()`), the profile `masker` conf + radius, and ALL canvas boxes (user + detected — all certify).
    4. `derivation = derive_page_mask_state(image_rgb, mask, all_pageboxes, conf, radius)`; apply `derivation.fits` onto the PageBox objects (`pb.mask`, `pb.std_dev`).
    5. `self.canvas.set_auto_binary(derivation.auto_binary)` and pack `derivation.raw_binary` into the current ImageFile's `raw_detected_mask` (D-08 retention; the auto/manual/erase planes flush via the existing seams).
    6. `self.refresh_box_inpaint_states()` (Task 3) + status "Detection complete" + explicit `self._set_session_dirty()` (recompose emits nothing, so the mask_modified-driven dirty hook does not fire — set it directly).

    Mode OFF (D-03): retain the raw binary the same way, then `auto = dilate_auto_mask(raw_binary, radius)` (MASK-01 applies in mask-only mode — the 01-UAT origin story) and `canvas.set_auto_binary(auto)`; NO boxes, no fits, no state refresh; status + dirty as today's path.

    Keep the `mask is None` early-return (:3870-3872) first. Reword `_confirm_replace_mask` (:4100-4104) to the A10 copy: body "Replace the auto-detected mask with a new detection? Your hand-painted strokes are kept.", buttons [Cancel] [Replace Mask] — drop the stale undo sentence (false under the layered model: re-detect replaces only the auto plane and detection is a non-undoable baseline).

    Extend tests/test_gui_detection_boxes.py with the behavior block using the existing `_window_with_page`/`_blk`/`_isolate_settings` harness (:47-88), adding a nonzero heatmap region inside AND outside the box.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui_detection_boxes.py -q</automated>
  </verify>
  <done>
    - Test command: pinned-interpreter pytest on tests/test_gui_detection_boxes.py passes including the six behavior cases (constrained content, Cancel-aborts-before-mutation, mode-off full heatmap, raw retention, no-undo-entries + dirty, replace-keeps-user-state).
    - Source assertion: the gate check runs before any `set_auto_binary`/`set_boxes` call in the handler body; `derive_page_mask_state` is called with the profile masker conf and radius.
    - Source assertion: the Replace Mask dialog body string matches the A10 copy ("Your hand-painted strokes are kept.").
  </done>
</task>

<task type="auto">
  <name>Task 2: _build_detected_boxes consumes the core; boxes_snapshot carries per-box state; refit triggers</name>
  <files>manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/canvas.py, tests/test_gui_detection_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py :3901-4015 (_build_detected_boxes — keep the gate/replace/set_boxes/overlay wrapper, replace ONLY the V5 loop body), :2879-2930 (_on_boxes_modified — the refit hook site), :3027-3052 (_inspector_style_commit grouped-commit shape, for reference)
    - manga_ai_studio/gui/canvas.py :1821-1863 (boxes_snapshot — the explicit PageBox field list to extend)
    - manga_ai_studio/core/detection_boxes.py (build_detected_pageboxes signature from 08-03)
  </read_first>
  <action>
    Refactor `_build_detected_boxes`: replace the inline V5 loop (:3944-3970 — pixmap dims read, per-blk clamp/drop/construct) with a call to `build_detected_pageboxes(blk_list, img_w, img_h, self._default_font_family())` where img_w/img_h come from the same `canvas.image_item.pixmap()` source; everything else in the method (the D-04 gate is now hoisted to the handler by Task 1 — remove the duplicated in-method gate return, the D-03 user-box merge, `_suppress_boxes_push` around set_boxes, overlay refresh) keeps its current shape.

    Extend `canvas.boxes_snapshot()` (:1849-1862): add `mask=item.pagebox.mask, std_dev=item.pagebox.std_dev, inpaint_override=item.pagebox.inpaint_override` to the explicit PageBox constructor, with the mask detached (`item.pagebox.mask.copy() if item.pagebox.mask is not None else None` — PIL is mutable; Pitfall 8/13-10). Update the docstring's "D-15 seam stays None" note. This makes BOXES undo/page-switch/save preserve the per-box state (a fourth field-drop site alongside the three geometry ones 08-01 fixed).

    Refit triggers: in `_on_boxes_modified` (:2879-2930), after the existing push, call the new `self._refit_changed_boxes(before)` — compare each before-snapshot PageBox's `box.as_tuple` to the CURRENT canvas pagebox tuples; for boxes whose geometry changed (moved/resized) AND for newly created boxes (present in current, absent before), re-run the per-box fit against the retained raw binary: reuse `derive_page_mask_state` restricted to the changed boxes (passing the full box list for the union but applying fits only to changed boxes is acceptable — the function is idempotent per page), then `set_auto_binary(compose_auto_binary(all_boxes, threshold, size))` + `refresh_box_inpaint_states()`. Guard: skip entirely when `self._suppress_boxes_push` (restores never refit) and when there is no retained raw mask (never detected — refit is a no-op). This lands recompute-on-release (D-12's debounce: the hook fires on commit, never per mousemove) and covers the user-box create-commit path (boxes_modified fires on create).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui_detection_boxes.py tests/test_gui_boxes.py -q</automated>
  </verify>
  <done>
    - Source assertion: `_build_detected_boxes` calls `build_detected_pageboxes` and no longer contains the inline per-blk clamp loop; boxes_snapshot's constructor call carries mask/std_dev/inpaint_override.
    - GUI test: after a detect, moving a box and releasing triggers a refit — the box's std_dev changes when moved onto a different-contrast region, and the border state re-derives (drive via the direct harness + canvas move-commit).
    - GUI test: a BOXES undo after an override-free detect+move round-trip restores per-box mask/std_dev (snapshot carry).
    - Full suite green (pinned interpreter).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: refresh_box_inpaint_states + live radius/threshold slots + .mas save/restore wiring + inpaint copy</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_detection_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py :2697-2698 (the brush_size_changed wiring shape to mirror), :1984-2000 (_set_session_dirty), :2161-2193 (the save loop — the `project_io.build_page_entries({...})` dict at :2182-2190 currently carries only boxes/image_rgb/mask_bin/geometry_altered/original_path/original_sha256; this task extends it with the four plane keys), :4265-4340 (_on_inpaint_finished — the completion copy site), :4179-4264 (inpaint action + tooltip), plus the .mas session-rebuild path (`_load_project_session`/`_load_single_page_mas` — locate via grep "current_image" restore block; the 05-05 plan summaries in STATE.md describe it)
    - manga_ai_studio/core/detection_boxes.py (compose_auto_binary signature)
    - manga_ai_studio/gui/tools_panel.py (the four signals from 08-05)
    - .planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §37 (refresh trigger list) + Copywriting (Inpaint tooltip + completion flash rows)
  </read_first>
  <behavior>
    - Test: `refresh_box_inpaint_states()` iterates every canvas box item and calls set_inpaint_state with `pb.inpaint_state(profile.masker.mask_max_standard_deviation)`.
    - Test (D-08 live re-dilate): after a detect, emitting `tools_panel.dilation_changed(5)` re-derives the auto plane from the retained raw mask WITHOUT a model call (no worker, status stays) — the composite mask grows; a second emit with 0 shrinks it back; hand strokes painted between the two emits are unchanged and still present.
    - Test (D-12 threshold): emitting `std_dev_threshold_changed(0.0)` flips a std_dev>0 box's border to dashed and REMOVES its content from the composite (pure recompose — no refit; std_dev unchanged); emitting 100.0 flips it back.
    - Test (no raw): dilation change on a never-detected page is a silent no-op (mask unchanged, no exception).
    - Test (.mas restore): save + reload a session (tmp project dir) driven through the REAL Save Project path (not a hand-built container — the save-loop dict extension below is the only writer of the plane entries, so a fallback-path restore cannot mask a missing save side) — border states render from the round-tripped per-box fields, the auto plane is restored from the persisted automask entry, and the raw plane is retained (a post-load radius change re-dilates from it) without any detect call.
    - Test (completion copy): after `Inpaint` completes with boxes on the page, the status shows "Inpainting complete · {n} box(es) inpainted" (+" · {m} skipped" when m>0); with no boxes, exactly "Inpainting complete".
  </behavior>
  <action>
    Add `refresh_box_inpaint_states(self) -> None`: read the threshold from `self.profile_manager.config.current_profile.masker.mask_max_standard_deviation`; for every box item in `self.canvas._box_items`, call `item.set_inpaint_state(item.pagebox.inpaint_state(threshold))`. Wire it on the §37 trigger set: detection finish (Task 1), threshold change, radius change, override commit (08-08 adds its call), box move/resize/create commit (Task 2's refit), page load/restore.

    Extend the 08-05 slots: `_on_dilation_changed(value)` (after persisting) calls a new `_rederive_auto_layer()`: if the current ImageFile has `raw_detected_mask` — mode ON with boxes: re-run `derive_page_mask_state` against the current boxes/params (fits re-applied; D-08 says "instantly re-dilates the current detected mask without re-running the model" — the fit re-runs, the CTD model does not) then `set_auto_binary` + `refresh_box_inpaint_states()`; mode OFF (no boxes): `set_auto_binary(dilate_auto_mask(raw, value))`; no raw: silent no-op. `_on_std_dev_threshold_changed(value)` (after persisting) recomposes ONLY: `set_auto_binary(compose_auto_binary(current_boxes, value, page_size))` + `refresh_box_inpaint_states()` — no refit (the gate-decoupled fits make this pure). Neither slot pushes history or dirties (settings are non-undoable preferences; RESEARCH §9).

    .mas SAVE side (this task owns the wiring — nothing else writes the plane entries): `_snapshot_current_page` (08-02) flushes the live planes only into the in-memory ImageFile packed slots; the container write happens in the save loop at main_window.py:2161-2193, whose `project_io.build_page_entries({...})` dict (:2182-2190) currently carries only boxes/image_rgb/mask_bin/geometry_altered/original_path/original_sha256. Extend that dict with the four plane keys 08-04's build_page_entries accepts — `raw_binary` / `auto_binary` / `manual_binary` / `erase_binary` — each sourced from the loop ImageFile's packed slot (`imf.raw_detected_mask` / `imf.auto_mask` / `imf.mask_manual` / `imf.mask_erase`) and unpacked via `mask_planes.unpack_binary(slot, h, w)` with h/w from that page's `image_rgb.shape[:2]`; a None slot omits the key (08-04 writes the entry only when the key is present and not None). The legacy `mask_bin` composite key stays unchanged. Naming alignment across the three layers — ImageFile slots `raw_detected_mask`/`auto_mask`/`mask_manual`/`mask_erase` -> save-loop keys `raw_binary`/`auto_binary`/`manual_binary`/`erase_binary` -> 08-04 parse keys `raw_packed`/`auto_packed`/`manual_packed`/`erase_packed` (unpacked again via mask_planes.unpack_binary against the meta dims).

    .mas LOAD side: in the project-open path (the 05-05 session rebuild that populates ImageFiles with restored mask/boxes/current_image), after the current page's canvas restore, rebuild the auto plane: prefer the page's persisted `auto_packed` entry (unpack -> set via `canvas.set_planes(manual, erase, auto)` when plane entries exist), else compose from the loaded per-box masks via `compose_auto_binary`, else the legacy fallback already handled by 08-02's set_mask path; then `refresh_box_inpaint_states()`. Pass the 08-04 parse outputs (packed planes) through the session-rebuild data so this wiring has the data (save side: 08-02's `_snapshot_current_page` + Task 2's boxes_snapshot land the state in the ImageFile slots, and the save-loop dict extension above is what actually carries it into the .mas container).

    Inpaint feedback (surface 7): extend the `action_inpaint` tooltip to the UI-SPEC copy ("Hand-painted mask is always inpainted; detected text is inpainted only inside text boxes."); in `_on_inpaint_finished`, when the page has boxes, set the status to "Inpainting complete · {n} box(es) inpainted" + " · {m} skipped" when m > 0, where n = boxes whose state is will_inpaint or forced AND which contributed mask content, m = all other boxes (gate-skipped + never + no-content); with no boxes keep exactly "Inpainting complete".
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui_detection_boxes.py -q</automated>
  </verify>
  <done>
    - Source assertion: main_window.py defines `refresh_box_inpaint_states`, `_rederive_auto_layer`; `_on_dilation_changed`/`_on_std_dev_threshold_changed` call them after persisting.
    - Source assertion: the save-loop `build_page_entries({...})` dict at main_window.py:2182-2190 includes the `raw_binary` / `auto_binary` / `manual_binary` / `erase_binary` keys sourced (unpacked) from the ImageFile packed slots.
    - Test command: pinned-interpreter pytest on tests/test_gui_detection_boxes.py passes including live re-dilate (grow + shrink + strokes preserved), threshold re-gate (border + composite flip, std_dev unchanged), no-raw no-op, .mas restore via the real save path without detect, and the completion-copy cases.
    - Full suite green (pinned interpreter).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker result dict (model output) → seam | Untrusted model output consumed by the new derivation (V5 discipline continues) |
| .mas load → canvas planes/boxes | Untrusted file state restored onto the canvas (decoded/validated by 08-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Tampering | _on_detection_finished inputs | medium | mitigate | The result dict flows only through the thresholded binarize + V5-clamped boxes (08-03); no geometry from the result is used for allocation beyond the already-validated page dims. |
| T-08-13 | DoS | refit on every boxes_modified | low | mitigate | Refit is restricted to geometry-CHANGED/new boxes (tuple diff vs the before-snapshot), fires on commit only, and no-ops without a retained raw mask. |
| T-08-14 | Tampering | restored .mas planes | medium | mitigate | Restore consumes only 08-04-validated packed blobs (length cross-checked) and 08-03 compose over loaded per-box masks — no new untrusted parse site here. |
</threat_model>

<verification>
- Pinned-interpreter FULL suite green (this plan changes the phase's central behavior; the whole suite is the regression net).
- The D-04 Cancel test proves no mask mutation precedes the gate (the reorder trap's regression lock).
</verification>

<success_criteria>
- All acceptance criteria hold; the LaMa call path in `inpaint` (:4179+) is untouched except for status copy (D-02 — the constraint lives at the seam, not the inpainter).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 08-07)

New symbols:
- `MainWindow.refresh_box_inpaint_states()`
- `MainWindow._refit_changed_boxes(before)`
- `MainWindow._rederive_auto_layer()`
- Reworked `_on_detection_finished` sequence + `_build_detected_boxes` consuming `build_detected_pageboxes`
- Extended `canvas.boxes_snapshot()` field carry (mask/std_dev/inpaint_override)
- Extended save-loop `page_state` dict at the `build_page_entries` call site (main_window.py:2182 — the four plane keys raw_binary/auto_binary/manual_binary/erase_binary; the sole writer of the 08-04 plane container entries)
- Reworded `_confirm_replace_mask` copy (A10)
- Inpaint action tooltip + completion copy extension
- New tests in tests/test_gui_detection_boxes.py (constrained mask, Cancel ordering, mode-off, live controls, restore)
</artifacts>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-07-SUMMARY.md` when done
</output>
