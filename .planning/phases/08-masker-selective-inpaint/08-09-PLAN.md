---
phase: 08-masker-selective-inpaint
plan: 09
type: execute
wave: 5
depends_on: [08-03, 08-07]
files_modified:
  - manga_ai_studio/core/batch_runner.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_core/test_batch_runner.py
autonomous: true
requirements: [MASK-01, MASK-02, MASK-05]
must_haves:
  truths:
    - "Batch Detect / Batch Detect+Clean build per-page boxes from the same detect pass, constrain the mask to box interiors (dilation applied), and persist boxes + per-box mask/std_dev onto the ImageFiles (D-04)"
    - "Batch clean output inpaints only the constrained mask — a page with zero gate-passing boxes passes through untouched (the D-03 empty-mask passthrough extends naturally)"
    - "The batch worker stays Qt-free in its new computation (PIL/numpy only; the one off-thread QImage construction remains the existing precedented site)"
    - "After a detect-mode batch, the current page's canvas restores boxes + the auto plane + border states without desync (the 02-04 Bug-D family lesson)"
  artifacts:
    - batch_runner constrained-detect path + `masker_conf` parameter threading
    - MainWindow `_dispatch_batch` profile supply + mode-aware refresh extension
  key_links:
    - "batch loop -> build_detected_pageboxes + derive_page_mask_state (08-03) — the SAME core as the interactive seam"
    - "_refresh_current_page_after_batch -> set_boxes (suppressed) + set_auto_binary + refresh_box_inpaint_states (08-07 helpers)"
---

<objective>
Adopt the D-02 rule in the batch pipeline (D-04): box building + constrained-mask derivation inside the worker loop, config threading from the profile, and the mode-aware post-batch refresh extended to boxes/planes/states.

Purpose: Batch output content changes per D-04 — detected text outside boxes no longer reaches the saved mask, and batch-detect runs now persist reviewable per-page boxes with per-box state, making the two-stage batch workflow (detect → review in editor → clean) selective-aware.

Output: batch_runner changes, _dispatch_batch arg threading, refresh extension, fake-model tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-RESEARCH.md (§5 — batch integration)
@.planning/phases/08-masker-selective-inpaint/08-PATTERNS.md (file 9)
@.planning/phases/08-masker-selective-inpaint/08-03-SUMMARY.md
@.planning/phases/08-masker-selective-inpaint/08-07-SUMMARY.md
manga_ai_studio/core/batch_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: batch_runner — constrained detect path + masker_conf threading</name>
  <files>manga_ai_studio/core/batch_runner.py, tests/test_core/test_batch_runner.py</files>
  <read_first>
    - manga_ai_studio/core/batch_runner.py :45-49 (thread-safety contract docstring), :118-185 (_run_batch_task detect/clean branches — the detect branch currently DISCARDS _blk_list at :149-156; the D-03 passthrough gate at :165-167), :191-260 (batch_detect/batch_clean/batch_detect_and_clean signatures — the det_model_path threading to mirror)
    - manga_ai_studio/core/detection_boxes.py (build_detected_pageboxes + derive_page_mask_state from 08-03)
    - manga_ai_studio/core/image_file.py (slots incl. packed plane slots from 08-02)
    - tests/test_core/test_batch_runner.py (existing fake det_model pattern)
  </read_first>
  <behavior>
    - Test (detect mode): a fake det_model returning (heatmap with in-box + out-of-box content, [blk]) + a masker_conf -> page.boxes is populated (one DETECTED PageBox with non-None mask and float std_dev), page.mask composite contains ONLY in-box content, page.raw_detected_mask holds the packed pre-dilation binary, page.auto_mask holds the packed auto binary.
    - Test (detect_and_clean): the inpaint fake receives the CONSTRAINED mask binary (out-of-box pixels zero); a page whose only box fails the gate (std above threshold, override None) produces an empty composite -> passthrough_original copies the source untouched and the inpaint fake is NOT called for that page.
    - Test: the derivation runs without Qt in the worker (the test asserts on numpy/PIL outputs before any QImage step; the single numpy_binary_to_mask_qimage call is the precedented off-thread QImage site).
    - Test: abort_flag still checked at loop top only; a conf with mask_dilation_radius=4 produces a larger mask than radius 0 (MASK-01 in batch).
  </behavior>
  <action>
    Thread the config: add a `masker_conf` keyword parameter to `batch_detect` and `batch_detect_and_clean` (mirroring how det_model_path/det_backend flow at :191-222) and pass it into `_run_batch_task`.

    Rework the detect branch (:149-156): after `mask_refined, blk_list = det_model.detect(image)`, convert `image_bgr` to RGB once, build boxes via `build_detected_pageboxes(blk_list, img_w, img_h)` (dims from `image.shape[1]`, `image.shape[0]` — no canvas exists), then `derivation = derive_page_mask_state(image_rgb, mask_refined, boxes, masker_conf, masker_conf.mask_dilation_radius)`; apply fits onto the PageBoxes; persist onto the ImageFile: `page.boxes = boxes`, `page.mask = numpy_binary_to_mask_qimage(derivation.auto_binary).copy()` (the existing precedented off-thread construction), `page.raw_detected_mask = pack_binary(derivation.raw_binary)`, `page.auto_mask = pack_binary(derivation.auto_binary)` (manual/erase stay None — batch pages have no hand strokes). Keep per-page failure non-fatal (the D-04 try/except already wraps the body). When `blk_list` is empty, boxes is an empty list and the derivation's auto binary is empty — same persistence shape (an empty mask triggers the clean-stage passthrough).

    The clean stage (:159-184) is UNCHANGED — it already reads `mask_to_numpy_binary(page.mask)`, which is now the constrained composite (D-02: the LaMa call is untouched). Extend the module docstring's thread-safety note: the new derivation is PIL/numpy only.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_batch_runner.py -q</automated>
  </verify>
  <done>
    - Source assertion: `batch_detect` and `batch_detect_and_clean` declare a `masker_conf` parameter; the detect branch calls `build_detected_pageboxes` and `derive_page_mask_state` and writes page.boxes/raw_detected_mask/auto_mask.
    - Test command: pinned-interpreter pytest on tests/test_core/test_batch_runner.py passes including constrained-mask, gate-fail-passthrough, radius effect, and abort-position cases.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MainWindow batch dispatch + mode-aware post-batch refresh</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_core/test_batch_runner.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py :5335-5420 (_dispatch_batch — the args-tuple site supplying det_model_path/det_backend from the profile), :5601+ (_refresh_current_page_after_batch — the mode-aware refresh; detect mode currently restores masks via the D-11 pattern), :2772-2786-area patterns for suppressed set_boxes
    - .planning/phases/08-masker-selective-inpaint/08-RESEARCH.md §5 item 5 (the 02-04 Bug-D family desync risk)
    - .planning/phases/08-masker-selective-inpaint/08-07-SUMMARY.md (refresh_box_inpaint_states / set_auto_binary helpers)
  </read_first>
  <behavior>
    - Test: `_dispatch_batch("detect")` passes `profile.masker` (with the current radius) into the worker args (assert via a captured fake Worker or by inspecting the args tuple).
    - Test: after a detect-mode batch on a multi-page folder where the CURRENT page received boxes + auto mask, `_refresh_current_page_after_batch("detect")` restores: canvas boxes equal the persisted list (set_boxes called under _suppress_boxes_push), the composite mask equals the persisted auto plane, and border states render from the per-box fields.
    - Test: clean-mode refresh behavior is unchanged (reload + clear — the existing mode branch).
  </behavior>
  <action>
    In `_dispatch_batch`, supply `masker_conf=self.profile_manager.config.current_profile.masker` to the batch_detect / batch_detect_and_clean argument tuples (the profile access pattern at :3694/:3820).

    Extend `_refresh_current_page_after_batch` detect-mode branch: after the existing mask restore, ALSO restore boxes — split the persisted `imf.boxes` by origin and call `canvas.set_boxes(user, detected)` wrapped in the `_suppress_boxes_push` guard (the on_page_selected Step 4b discipline — batch is headless, no D-04 GUI gate applies per RESEARCH §5 item 5); restore the auto plane from the packed `imf.auto_mask` via `canvas.set_planes`/`set_auto_binary` (manual/erase empty for batch pages — pass empty planes rather than None where the API distinguishes); call `self.refresh_box_inpaint_states()`. Clean-mode branch unchanged.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_batch_runner.py -q</automated>
  </verify>
  <done>
    - Source assertion: _dispatch_batch passes the profile masker conf into both detect-mode batch calls; the detect-mode refresh branch calls set_boxes under the suppression guard and refresh_box_inpaint_states.
    - Test command: pinned-interpreter pytest on tests/test_core/test_batch_runner.py passes including the dispatch-args and current-page-restore cases.
    - FULL SUITE green (pinned interpreter) — the phase-closing run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source image files → worker | Untrusted image files read in the worker (existing boundary; the derivation consumes their pixels) |
| worker → ImageFile state | Worker-produced arrays/QImages crossing to the GUI thread via the existing Worker signal discipline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-16 | DoS | derivation in worker loop | low | mitigate | Same box-bounded fitting cost as the interactive path; per-page failure stays non-fatal (D-04 try/except) and abort is checked at loop top. |
| T-08-17 | Tampering | source image decode | medium | mitigate | Reads go through the existing `_read_image_bgr` guard chain; derivation allocates only against the decoded image's own shape (no file-supplied dims reach allocation). |
</threat_model>

<verification>
- Pinned-interpreter FULL suite green — this plan closes the phase's automated surface.
- Manual UAT items that remain for the phase gate: batch output quality on a real chapter, re-dilate slider latency, border legibility on real artwork (08-VALIDATION.md Layer 3).
</verification>

<success_criteria>
- All acceptance criteria hold; batch_detect-only runs persist per-page boxes + constrained masks (D-04 verbatim).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 08-09)

New symbols:
- `batch_detect(..., masker_conf=...)` / `batch_detect_and_clean(..., masker_conf=...)` parameter
- `_run_batch_task` constrained detect branch (build_detected_pageboxes + derive_page_mask_state + per-page persistence)
- `_dispatch_batch` profile-masker supply
- `_refresh_current_page_after_batch` detect-mode boxes/planes/states restore
</artifacts>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-09-SUMMARY.md` when done
</output>
