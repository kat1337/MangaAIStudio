---
phase: 08-masker-selective-inpaint
plan: 08
type: execute
wave: 4
depends_on: [08-07]
files_modified:
  - manga_ai_studio/gui/inspector_panel.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_inspector_override.py
autonomous: true
requirements: [MASK-03]
must_haves:
  truths:
    - "The Inspector shows an 'Inpaint' combo (Auto/Always/Never) per selection and a read-only 'Std dev' value — the override is D-13/D-14's per-box control"
    - "Committing an override applies to EVERY selected box, pushes exactly ONE BOXES undo entry named 'inpaint override', recomposes the auto plane (Never boxes' content leaves the mask; Always boxes' content joins it), refreshes border states, and flashes the transient status"
    - "Multi-select with differing values shows the 'Mixed' sentinel entry; the row stays ENABLED in multi-select (edit-all — the Phase 7 vertical-checkbox precedent, A5)"
    - "One Ctrl+Z restores the previous overrides AND the recomposed mask"
  artifacts:
    - InspectorPanel "Inpaint" QComboBox + "Std dev" row + `inpaint_override_changed(str)` signal
    - MainWindow `_on_inspector_inpaint_committed` grouped-commit handler
  key_links:
    - "commit -> set field on PageBox.inpaint_override (08-01) -> compose_auto_binary (08-03) -> canvas.set_auto_binary (08-02) -> BoxItem.set_inpaint_state (08-06)"
---

<objective>
Deliver MASK-03's override half: the Inspector Auto/Always/Never field with the Phase 7 grouped-commit discipline, live border/mask recomposition, and the transient status flash.

Purpose: D-13 places the override in the Inspector; D-14 locks the tri-state. The commit path must be one undoable group op whose effect is immediately visible in both the border states and the mask layer (D-12's predictive model).

Output: Two new Inspector rows, the commit signal + handler, tests for single/multi/Mixed/undo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-RESEARCH.md (§9 — undo integration)
@.planning/phases/08-masker-selective-inpaint/08-PATTERNS.md (file 8 + Shared Pattern 6/8)
@.planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §38 (the row contract — executor-authoritative) + Copywriting rows (Inpaint override tooltip, Std dev row, override status flash)
@.planning/phases/08-masker-selective-inpaint/08-07-SUMMARY.md
manga_ai_studio/gui/inspector_panel.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspector rows — Inpaint combo + Std dev label</name>
  <files>manga_ai_studio/gui/inspector_panel.py, tests/test_gui_inspector_override.py</files>
  <read_first>
    - manga_ai_studio/gui/inspector_panel.py :285-299 (class-scope Signals), :442-465 (style-section row pattern), :509-540 (loaded-memory guard), :546-630 (load_box blocked-signals population), :666-674 (Mixed sentinel pattern)
    - .planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §38 (placement after "Origin", widget choice A4, multi-select behavior A5, tooltips)
  </read_first>
  <action>
    Add two rows to the Inspector QFormLayout directly after "Origin": (1) "Inpaint" — a QComboBox with entries in order "Auto", "Always", "Never" plus a non-editable "Mixed" sentinel entry added/removed dynamically (the Phase 7 font-combo pattern); tooltip verbatim from the UI-SPEC Copywriting row ("Whether Inpaint (C) cleans this box's detected text: Auto uses the std-dev gate (solid border = will clean, dashed = skipped), Always forces it, Never skips it."). (2) "Std dev" — a read-only muted QLabel showing f"{value:.1f}" or the em-dash "—" when not computed; tooltip from the UI-SPEC row.

    Add class-scope Signal `inpaint_override_changed = Signal(str)`. Population in `load_box`: single selection — combo shows the box's mapping (None -> "Auto", "always" -> "Always", "never" -> "Never") with blockSignals, then record the loaded-memory guard (`self._loaded_inpaint = <current text>`, the WR-01 pattern); Std dev shows the box's std_dev or "—". Multi-select: differing values -> show the "Mixed" sentinel (never committed — the sentinel stays in the widget layer); equal values -> that value; Std dev shows "—" (per-box data, not editable-all). The Inpaint row stays ENABLED in multi-select (A5 — unlike per-box content fields); Std dev is read-only always.

    Commit: on currentIndexChanged (or activated — follow the existing combo commit convention in the file), skip when the new text equals the loaded-memory guard or is "Mixed", else emit `inpaint_override_changed(text)`.

    Create tests/test_gui_inspector_override.py (gui marker) covering: single-box population from all three field values + None; Mixed display on differing multi-select; the sentinel never emits; the guard suppresses spurious commits on load.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui_inspector_override.py -q</automated>
  </verify>
  <done>
    - Source assertion: inspector_panel.py declares `inpaint_override_changed = Signal(str)`; the combo's entries are exactly Auto/Always/Never (+ dynamic Mixed).
    - Test command: pinned-interpreter pytest on tests/test_gui_inspector_override.py passes (population, Mixed, no-spurious-commit, multi-select row enabled).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MainWindow commit handler — one snapshot, live recompose, status flash</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_inspector_override.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py :2772-2786 (connect_commit_handlers wiring block — add the new kwarg), :3027-3052 (_inspector_style_commit — the ONE-snapshot grouped commit shape to mirror), :2904-2918 area (set_pending_boxes_op_name flash path)
    - manga_ai_studio/core/detection_boxes.py (compose_auto_binary from 08-03)
    - manga_ai_studio/gui/canvas.py set_pending_boxes_op_name + boxes_modified (from 08-02/08-07)
    - .planning/phases/08-masker-selective-inpaint/08-UI-SPEC.md §38 commit-semantics bullet + Copywriting "Status — override flip" row
  </read_first>
  <behavior>
    - Test: single box, commit "Never" — the PageBox.inpaint_override becomes "never", the box's border renders the never state (grey dashed), its auto content is REMOVED from the composite mask (mask_to_numpy_binary of canvas.get_mask() loses the box's pixels), and ONE BOXES undo entry was pushed (undo stack length 1, op flash "inpaint override").
    - Test: commit "Always" on a gate-skipped box (std_dev above threshold, stored mask present) — its content JOINS the composite (the gate-lifted fit from 08-03 guarantees a stored mask) and the border renders forced.
    - Test: multi-select of 3 boxes, one commit — all three boxes updated, still exactly ONE undo entry; one Ctrl+Z restores all three overrides AND the composite (recompose ran on apply_undo too — wire the boxes-apply path to recompose).
    - Test: transient status shows "Inpaint: Never — 1 box(es)." shape after a commit.
  </behavior>
  <action>
    Extend `connect_commit_handlers` with an optional `on_inpaint_override=None` kwarg (the :1073-1090 backward-compatible pattern) and wire it in the :2772-2786 block to a new `_on_inspector_inpaint_committed(value: str)`.

    The handler mirrors `_inspector_style_commit`: capture `before = self.canvas.boxes_snapshot()` (per-box mask/std_dev/override now carried per 08-07); map the combo text (None for "Auto", "always", "never"); apply `pb.inpaint_override = mapped` to EVERY selected box; `self.canvas.set_pending_boxes_op_name("inpaint override")`; emit `boxes_modified` ONCE with the before-snapshot. Then the live effects: `self.canvas.set_auto_binary(compose_auto_binary(current_boxes, threshold, page_size))`, `self.refresh_box_inpaint_states()`, and the transient status "Inpaint: {Auto|Always|Never} — {n} box(es)." (the _show_transient_status helper). The dirty marking rides the existing boxes_modified -> _set_session_dirty connection.

    Undo integration: the BOXES apply path (`apply_undo_boxes`) restores PageBox objects including overrides — add a recompose + refresh call after the set_boxes restore in `apply_undo_boxes` so the mask layer follows the restored overrides (the 08-07 seam discipline: every path that changes overrides recomposes).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui_inspector_override.py -q</automated>
  </verify>
  <done>
    - Source assertion: `_on_inspector_inpaint_committed` exists, pushes ONE boxes_modified with a before-snapshot, sets the pending op name "inpaint override", and calls compose_auto_binary + set_auto_binary + refresh_box_inpaint_states.
    - Test command: pinned-interpreter pytest on tests/test_gui_inspector_override.py passes including the four behavior cases (never-removes-content, always-joins-content, one-entry multi-box + undo, status flash).
    - Full suite green (pinned interpreter).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The override is user-authored in-session state; the .mas round-trip of the field is validated by 08-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-15 | Repudiation | override commit granularity | low | mitigate | One snapshot per commit (incl. multi-box) + the "inpaint override" op-name flash makes every override change a single, named, reversible Ctrl+Z unit. |
</threat_model>

<verification>
- Pinned-interpreter full suite green.
- Border state and mask content agree after every commit and after undo (the UI-SPEC reading-rule-4 equivalence).
</verification>

<success_criteria>
- All acceptance criteria hold; the Inspector remains a pure follower (no direct PageBox mutation outside the commit handler).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 08-08)

New symbols:
- `InspectorPanel.inpaint_override_changed = Signal(str)`
- InspectorPanel widgets: `inpaint_combo` (Auto/Always/Never + Mixed), `std_dev_label` (read-only), `_loaded_inpaint` guard
- `InspectorPanel.connect_commit_handlers(..., on_inpaint_override=None)` kwarg
- `MainWindow._on_inspector_inpaint_committed(value)`
- Undo op name string "inpaint override"
- Status flash format "Inpaint: {Auto|Always|Never} — {n} box(es)."
- tests/test_gui_inspector_override.py (new test file)
</artifacts>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-08-SUMMARY.md` when done
</output>
