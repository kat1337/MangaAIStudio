---
phase: 08-masker-selective-inpaint
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - manga_ai_studio/core/project_io.py
  - tests/test_core/test_project_io.py
autonomous: true
requirements: [MASK-02, MASK-03]
must_haves:
  truths:
    - "A .mas project round-trips per-box std_dev, inpaint_override, and the box-cropped per-box mask (success criterion 5 — the D-15 seam closes through persistence)"
    - "The per-page plane binaries (auto/manual/erase) and the retained raw detection binary persist as optional container entries — a project reloaded mid-tuning can re-dilate and never loses hand strokes to a recomposition"
    - "Legacy .mas files written by Phase 5/7 load clean with defaults for every new field (the Phase 7 optional-key pattern — no version bump)"
  artifacts:
    - core/project_io.py extended pagebox JSON keys ("std_dev", "inpaint_override", "mask") + optional page entries ("rawmask.bin", "automask.bin", "manualmask.bin", "erasemask.bin")
  key_links:
    - "json_to_pagebox output feeds the 08-07 load path (border states restore without re-detect)"
    - "parse_page_entries plane outputs feed canvas.set_planes (08-02) on project open"
---

<objective>
Extend the .mas format with the Phase 8 per-box fields and per-page plane binaries, all as optional keys/entries (no `_FORMAT_VERSION` bump), with validated, bounded decoding of the new untrusted payloads.

Purpose: ROADMAP success criterion 5 requires the D-15 seam (`PageBox.mask`/`std_dev` + the override) to round-trip through project save/load, and the layered mask model requires plane provenance to survive a save/load cycle (otherwise a post-load radius change would erase hand strokes — see plan 08-02's ledger invariant).

Output: project_io read/write extensions + round-trip and legacy-compat tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-masker-selective-inpaint/08-CONTEXT.md
@.planning/phases/08-masker-selective-inpaint/08-RESEARCH.md (§6 — serialization impact; §13 Pitfalls 4/7)
@.planning/phases/08-masker-selective-inpaint/08-PATTERNS.md (file 10 + Shared Pattern 7)
manga_ai_studio/core/project_io.py
manga_ai_studio/core/mask_planes.py (from 08-02)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pagebox_to_json / json_to_pagebox — std_dev, inpaint_override, per-box mask</name>
  <files>manga_ai_studio/core/project_io.py, tests/test_core/test_project_io.py</files>
  <read_first>
    - manga_ai_studio/core/project_io.py :182-280 (pagebox_to_json / json_to_pagebox — the hand-picked field dict and the optional-key style load at :236-240), :545-588 (validate_meta — keep structural)
    - manga_ai_studio/core/box_model.py (inpaint_override field from 08-01; mask is a box-cropped PIL "1" image from 08-03)
    - manga_ai_studio/core/image_io.py (save_image_bytes — the single PNG encode source)
    - tests/test_core/test_project_io.py :175 (test_legacy_mas_without_style_loads_with_defaults — the template to clone)
  </read_first>
  <behavior>
    - Test: full round-trip — a PageBox with std_dev=12.3, inpaint_override="never", and a box-cropped PIL "1" mask (a filled region) serializes to JSON and rebuilds with equal std_dev, equal override, and an equal-size mask whose binary content matches (np.array equality).
    - Test: legacy dict WITHOUT the new keys (only box/origin/edited/bubble_no/manual_override/style/payload) loads clean with std_dev None, inpaint_override None, mask None.
    - Test: inpaint_override value "sometimes" (not in {"always","never"}) raises ProjectFormatError on load; std_dev "abc" raises ProjectFormatError via float coercion.
    - Test: per-box mask PNG whose decoded size does not match the box's (w, h) raises ProjectFormatError.
  </behavior>
  <action>
    pagebox_to_json (:182-207): add three keys — `"std_dev": pb.std_dev`, `"inpaint_override": pb.inpaint_override`, and `"mask": None-or-base64-PNG-bytes` encoding `pb.mask` when it is not None: encode the box-cropped mode-"1" image to PNG bytes in-memory via PIL (`Image.save` to a BytesIO with format="PNG" — save_image_bytes expects RGB pages, so use PIL directly here for mode "1") and base64 it into an ASCII str. Update the docstring: the D-15 seam contract ("NEVER writes mask/std_dev") is superseded by Phase 8.

    json_to_pagebox (:210-280): optional-key reads after the style block — `std_dev = float(d["std_dev"]) if d.get("std_dev") is not None else None` wrapped so TypeError/ValueError raise `ProjectFormatError("std_dev must be a number")`; `override_raw = d.get("inpaint_override")` validated against `{"always", "never"}` (None passes; any other value raises ProjectFormatError — structural, matching the format-version rejection stance); mask decode — base64 -> PNG bytes -> `Image.open` on a BytesIO with `.copy()` after open (buffer lifetime), verify `decoded.size == (box.x2-box.x1, box.y2-box.y1)` else ProjectFormatError, convert to mode "1" (`dither=Image.NONE`), and pass `mask=decoded, std_dev=std_dev, inpaint_override=override_raw` into the PageBox constructor alongside the existing fields.

    Decode hardening (ASVS V5): wrap the b64 decode + Image.open in try/except (binascii.Error, ValueError, OSError, PIL UnidentifiedImageError) re-raising ProjectFormatError — no raw library exception escapes the loader (the T-05-01 pattern). PIL's default `Image.MAX_IMAGE_PIXELS` decompression-bomb guard stays active (do not raise it); the explicit size cross-check above additionally bounds the decoded image to the declared box dims, which are themselves clamped by MAX_IMAGE_DIMENSION-style validation on the box coords.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_project_io.py -q</automated>
  </verify>
  <done>
    - Source assertion: pagebox_to_json emits the keys "std_dev", "inpaint_override", "mask"; json_to_pagebox reads all three as optional with the validation described.
    - Test command: pinned-interpreter pytest on tests/test_core/test_project_io.py passes including round-trip, legacy-without-new-keys, invalid-override-raises, and mask-size-mismatch-raises.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Optional page entries — rawmask.bin / automask.bin / manualmask.bin / erasemask.bin</name>
  <files>manga_ai_studio/core/project_io.py, tests/test_core/test_project_io.py</files>
  <read_first>
    - manga_ai_studio/core/project_io.py :285-334 (build_page_entries — the entries dict + meta dims pattern), :423-483 (parse_page_entries — required meta.json/image.png, optional mask.bin with the meta-dims cross-check at :451-471), :95-180 (container read/write helpers)
    - manga_ai_studio/core/mask_planes.py (pack_binary / unpack_binary from 08-02)
    - .planning/phases/08-masker-selective-inpaint/08-RESEARCH.md §6.3-§6.4 (format options; raw persistence recommendation)
  </read_first>
  <behavior>
    - Test: build_page_entries with page_state carrying raw/auto/manual/erase binaries writes the four extra entries; without them (legacy-shaped state) writes none of the four.
    - Test: parse_page_entries on a container with the entries returns the four unpacked arrays with content matching what was packed; on a container WITHOUT them returns None for all four (legacy project).
    - Test: a crafted rawmask.bin blob whose length does not equal ceil(h*w/8) for the declared mask dims raises ProjectFormatError.
    - Test: an end-to-end save→load round-trip of a page with boxes (with masks/override/std_dev) + planes rebuilds everything equal.
  </behavior>
  <action>
    build_page_entries (:285-334): accept optional page_state keys `raw_binary`, `auto_binary`, `manual_binary`, `erase_binary` ((H,W) uint8 0/255 numpy or None). When not None, write entries `rawmask.bin` / `automask.bin` / `manualmask.bin` / `erasemask.bin` containing `mask_planes.pack_binary(...).tobytes()`. The entries ride the existing per-entry LZMA2 container path unchanged (entry names are naturally optional on parse — RESEARCH §6.3c).

    parse_page_entries (:423-483): after the existing mask.bin handling, read each of the four entries when present: `np.frombuffer(blob, dtype=np.uint8)` then validate `len == (h*w + 7) // 8` against the meta-declared mask dims (raise ProjectFormatError on mismatch — same discipline as the :467-471 cross-check) and return them as PACKED arrays keyed `raw_packed` / `auto_packed` / `manual_packed` / `erase_packed` (None when absent). Callers unpack via mask_planes.unpack_binary with the page dims.

    Keep `validate_meta` (:545-588) structural — the new data is entry-level, not manifest-level. Do NOT bump `_FORMAT_VERSION`.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_core/test_project_io.py -q</automated>
  </verify>
  <done>
    - Source assertion: build_page_entries writes the four optional entry names; parse_page_entries returns the four packed keys with the length cross-check.
    - Test command: pinned-interpreter pytest on tests/test_core/test_project_io.py passes including the crafted-blob-length rejection and the end-to-end round-trip.
    - Full suite green (pinned interpreter).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mas file on disk → PageBox/page state | Untrusted compressed container crossing into app state (the phase's primary untrusted-input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Tampering | per-box mask PNG decode | high | mitigate | b64+PNG decode fully wrapped (no raw exceptions escape); decoded size cross-checked against the box dims derived from clamped coords; PIL's default MAX_IMAGE_PIXELS bomb guard left enabled — a crafted mask cannot exceed the declared box bounds. |
| T-08-07 | Tampering | packed plane blobs | medium | mitigate | Blob length validated as exactly ceil(h*w/8) against meta-declared dims before unpack; unpack slices to h*w — a short/long blob is a ProjectFormatError, never a mis-shaped array. |
| T-08-08 | Tampering | override/std_dev values | low | mitigate | Enum validation against {"always","never"} and float() coercion both raise ProjectFormatError on garbage; NaN std_dev fails every gate comparison downstream (fail-safe gate-skip). |
</threat_model>

<verification>
- Pinned-interpreter full suite green.
- Legacy Phase 5/7 project files (fixture or synthesized dicts) load with defaults — the no-version-bump contract.
</verification>

<success_criteria>
- All acceptance criteria hold; main_window is NOT touched by this plan (the load-path consumption is 08-07).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 08-04)

New serialization keys/entries (format surface, no version bump):
- pagebox JSON keys: `"std_dev"`, `"inpaint_override"`, `"mask"` (base64 PNG, box-cropped mode "1")
- page container entries: `"rawmask.bin"`, `"automask.bin"`, `"manualmask.bin"`, `"erasemask.bin"` (np.packbits blobs, optional)
- parse_page_entries return keys: `raw_packed`, `auto_packed`, `manual_packed`, `erase_packed`
- build_page_entries page_state input keys: `raw_binary`, `auto_binary`, `manual_binary`, `erase_binary`
</artifacts>

<output>
Create `.planning/phases/08-masker-selective-inpaint/08-04-SUMMARY.md` when done
</output>
