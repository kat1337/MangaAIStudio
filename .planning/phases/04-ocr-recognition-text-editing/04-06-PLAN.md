---
phase: 04-ocr-recognition-text-editing
plan: 06
type: execute
wave: 4
depends_on: [03, 05]
files_modified:
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - tests/test_gui_boxes.py
autonomous: false
requirements: [TEXT-02]
user_setup:
  - service: huggingface
    why: "First-run manga-ocr model download (~450MB) from kha-white/manga-ocr-base"
    env_vars:
      - name: HF_HUB_CACHE
        source: "HuggingFace default cache dir (default ~/.cache/huggingface/hub); only override if a custom location is desired"
    dashboard_config: []

must_haves:
  truths:
    - "Alt+drag to draw a user box triggers manga-ocr automatically on draw-release (D-01) — the box arrives with recognized text"
    - "Text menu 'Run OCR' runs manga-ocr on the single selected box, off the GUI thread via Worker(QRunnable) + _op_running gate (D-01)"
    - "Text menu 'OCR All Boxes' (Ctrl+R) fills every text-empty box on the page, one model load, sequential per-box, status-bar progress (D-03)"
    - "Re-OCR on a box with edited=True shows the D-04 confirm dialog; re-OCR on edited=False overwrites silently (D-04)"
    - "First-run shows 'Loading OCR model…' indeterminate progress before the worker starts the ~450MB download (Pitfall 6)"
    - "OCR result writes pagebox.set_recognized_text(str) (Plan 01 setter, edited=False), refreshes the BoxItem overlay, pushes a BOXES snapshot"
  artifacts:
    - "manga_ai_studio/gui/main_window.py with _run_ocr_task / _run_ocr_all_task / run_ocr_selected / run_ocr_all / _resolve_ocr_model_path / _on_ocr_finished / _on_ocr_error / _on_ocr_cleanup / _confirm_reocr / _confirm_reocr_all + Text menu (Run OCR / OCR All Ctrl+R) + _commit_create OCR hook (canvas-side signal wired in MainWindow)"
    - "tests/test_gui_boxes.py OCR dispatcher test cases (mocked model — real model skip-gated)"
  key_links:
    - "run_ocr_selected / run_ocr_all call backend_factory('ocr','torch') (Plan 03) and dispatch via Worker"
    - "_run_ocr_task crops the region by box xyxy, calls model.recognize(numpy), returns {text: str} (RESEARCH Pattern 2)"
    - "_on_ocr_finished calls pagebox.set_recognized_text + BoxItem.refresh_text_overlay + boxes_modified emit"
    - "canvas._commit_create emits an ocr_requested signal (or calls a MainWindow-injected callback) that triggers run_ocr on the newly-created box (D-01)"
---

<objective>
Wire the OCR dispatcher into MainWindow (TEXT-02, D-01/D-02/D-03/D-04): the Worker(QRunnable)+_op_running+status-bar-progress pattern (mirroring detect_text / _run_detection_task / _resolve_detection_model_path), the two OCR entry points (auto-OCR on Alt+drag draw-release D-01; "Run OCR" action on the selected box; "OCR All Boxes on Page" Ctrl+R D-03), the D-04 re-OCR confirm gate (silent overwrite when edited=False, confirm dialog when edited=True), the first-run "Loading OCR model…" UX (Pitfall 6), and the new Text menu. The OCR work runs OFF the GUI thread (T-01-07) via the existing Worker pattern; the recognized-text str is immutable + signal-safe.

Purpose: TEXT-02's "draw a rectangle and run manga-ocr on just that region to create a box with recognized text" requires (a) the auto-OCR hook on the Phase 3 _commit_create seam (D-01), (b) the "Run OCR" action for re-OCR / detected-box fill (D-01/D-02), and (c) the "OCR All Boxes on Page" batch (D-03, aligns with Phase 5's _ocr.json export which needs text on every box). Per RESEARCH Pattern 2, the dispatch mirrors the detection worker EXACTLY; per RESEARCH Pitfall 4, the D-04 edited-flag gate MUST precede the overwrite; per RESEARCH Pitfall 6, the first-run download shows indeterminate progress before the worker starts.

Output: OCR dispatcher + Text menu + auto-OCR hook + D-04 gates, all green (real-model tests skip-gated; dispatcher tests use a mocked model).
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ocr-recognition-text-editing/04-CONTEXT.md
@.planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md
@.planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md
@.planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md
@.planning/phases/04-ocr-recognition-text-editing/04-03-SUMMARY.md
@.planning/phases/04-ocr-recognition-text-editing/04-05-SUMMARY.md

@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/worker_thread.py
@manga_ai_studio/adapters/factory.py
@manga_ai_studio/adapters/torch_impl.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OCR dispatcher (Worker + _op_running + status-bar progress) + _resolve_ocr_model_path + D-04 confirm gates</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (detect_text ~1404-1445 — THE dispatcher template; _run_detection_task ~1447-1499 — the Worker task template; _resolve_detection_model_path ~1501-1555 — the CR-11 cache-check pattern; _on_detection_finished ~1566-1592 — the result handler; _on_detection_error ~1749-1764 — the error UX; _confirm_replace_boxes ~1711-1737 — the confirm-gate template; _confirm_replace_mask ~1772-1792; _refresh_action_states ~610; _build_status_bar ~535; progress_bar + status_bar_left fields)
    - manga_ai_studio/gui/worker_thread.py (Worker(QRunnable) + WorkerSignals + SharableFlag — reused directly; the progress_callback + abort_flag auto-injection as last kwargs)
    - manga_ai_studio/adapters/factory.py (Plan 03 — backend_factory('ocr','torch') returns TorchOCRModel)
    - manga_ai_studio/adapters/torch_impl.py (Plan 03 — TorchOCRModel.load / recognize(numpy) -> str)
    - panelcleaner/model_downloader.py (get_ocr_model_directory / is_ocr_downloaded — the cache-check _resolve_ocr_model_path delegates to; OCR_DIR_NAME)
    - .planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md (Pattern 2 lines 286-321 — the exact dispatcher + worker-task body; Pitfall 4 lines 459-464 — D-04 edited-flag gate; Pitfall 6 lines 472-476 — first-run download UX; Code Examples lines 618-634 — _resolve_ocr_model_path body)
    - .planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md (main_window.py assignment lines 418-482 — dispatcher/worker/menu/dock patterns)
    - .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md (§14 OCR surface — two entry points + Ctrl+R + D-04 gate + first-run UX; §Copywriting — "Run OCR" / "OCR All Boxes" / D-04 confirm dialog copy / status messages)
    - manga_ai_studio/core/box_model.py (Plan 01 — set_recognized_text sets edited=False; the D-04 gate reads pagebox.edited)
  </read_first>
  <behavior>
    - run_ocr_selected() returns early if self._op_running; requires exactly one box selected; checks the D-04 gate (if box.edited and not self._confirm_reocr(): return); dispatches a Worker(_run_ocr_task, image_path, box_xyxy, model); sets _op_running=True; status_bar_left="Recognizing text… box N"; progress_bar indeterminate
    - _run_ocr_task(image_path, box_xyxy, model, progress_callback=None, abort_flag=None) reads the page via cv2.imdecode(np.fromfile(...)), crops region = image[y1:y2, x1:x2].copy() (Pitfall 2 clean copy), resolves the model path via _resolve_ocr_model_path, model.load(model_path, device="auto") (singleton — load-once per session), returns {"text": model.recognize(region)}
    - _on_ocr_finished(result_dict) writes box.set_recognized_text(result["text"]) (Plan 01 setter, edited=False), refreshes the BoxItem (refresh_text_overlay + refresh_badge), captures pre-state + emits boxes_modified (BOXES snapshot push), clears _op_running, status_bar_left="OCR complete · 1 box recognized" (transient)
    - _on_ocr_error(worker_error) logs full traceback to loguru, shows the #7a1f1f error chip + QMessageBox.critical "Couldn't load the OCR model." (Phase 1 T-01-08 pattern), clears _op_running
    - run_ocr_all() returns early if _op_running; counts edited boxes on the page; if any edited and not self._confirm_reocr_all(count): return (D-04 batch gate); dispatches a Worker(_run_ocr_all_task, image_path, list_of_text_empty_box_xyxys, model); determinate progress_bar 0..100; status_bar_left="OCR All… {done}/{total} · box N"
    - _run_ocr_all_task loops over the boxes, one model load, abort-checked between boxes (Phase 2 D-09 SharableFlag pattern), progress_callback(done/total) each box; returns a list of {box_index, text} results
    - _resolve_ocr_model_path() delegates to panelcleaner.model_downloader.get_ocr_model_directory() / is_ocr_downloaded() — CR-11 cache-check short-circuits when is_ocr_downloaded() is True (do NOT re-download ~450MB on every session)
    - _confirm_reocr() shows a QMessageBox with the D-04 single-box copy; returns True iff the user clicks "Re-run OCR"
    - _confirm_reocr_all(count) shows the D-04 batch copy with the count; returns True iff "Re-run OCR on All"
    - First run: before dispatching the worker, if not is_ocr_downloaded(): status_bar_left="Loading OCR model…" + progress_bar indeterminate (Pitfall 6)
  </behavior>
  <action>
    In manga_ai_studio/gui/main_window.py MainWindow, add the OCR dispatcher cluster mirroring detect_text. Use a mocked TorchOCRModel in tests (monkeypatch backend_factory) so the dispatcher tests do NOT require the real model — skip-gate the real-model test.

    Methods to add (mirror the detect_text / _run_detection_task / _on_detection_finished / _on_detection_error / _resolve_detection_model_path / _confirm_replace_boxes shapes verbatim, swapping detection→ocr):

    - `def run_ocr_selected(self) -> None:` — gate on self._op_running; get the single selected box via self.canvas (find the selected-box accessor — canvas._selected_box() or iterate _box_items for isSelected); if None return; D-04 gate: `if box.pagebox.has_recognized_text() and box.pagebox.edited and not self._confirm_reocr(): return`; `model = backend_factory("ocr", self._ocr_backend())` (mirror self._detection_backend() — likely returns "torch" default; add self._ocr_backend() if not present); construct Worker(self._run_ocr_task, self.current_image_path(), box.pagebox.box, model); wire signals result→_on_ocr_finished, error→_on_ocr_error, finished→_on_ocr_cleanup; self._op_running=True; self._refresh_action_states(); status_bar_left + progress_bar (indeterminate range 0,0); if not is_ocr_downloaded(): status_bar_left="Loading OCR model…"; QThreadPool.globalInstance().start(worker)

    - `def _run_ocr_task(self, image_path, box_xyxy, model, progress_callback=None, abort_flag=None) -> dict:` — lazy `import cv2, numpy as np`; `image = cv2.imdecode(np.fromfile(str(image_path), dtype=np.uint8), cv2.IMREAD_COLOR)`; read x1,y1,x2,y2 from box_xyxy (the vendored Box accessor — confirm via panelcleaner/structures.py; coerce via int()); `region = image[y1:y2, x1:x2].copy()` (Pitfall 2); `model_path = self._resolve_ocr_model_path()`; `model.load(model_path, device="auto")`; `return {"text": model.recognize(region), "box_id": id(box_xyxy)}` (the box_id lets _on_ocr_finished find the right BoxItem — OR pass the box index and let the handler look it up; pick one and be consistent)

    - `def run_ocr_all(self) -> None:` — gate on self._op_running; collect text-empty boxes: `empty_boxes = [it for it in self.canvas._box_items if not it.pagebox.has_recognized_text()]`; if none, return; count edited boxes on the page (for the D-04 batch gate); `edited_count = sum(1 for it in self.canvas._box_items if it.pagebox.has_recognized_text() and it.pagebox.edited)`; `if edited_count and not self._confirm_reocr_all(edited_count): return`; dispatch Worker(self._run_ocr_all_task, self.current_image_path(), [it.pagebox.box for it in empty_boxes], [id(it.pagebox) for it in empty_boxes], model); determinate progress

    - `def _run_ocr_all_task(self, image_path, box_xyxys, box_ids, model, progress_callback=None, abort_flag=None) -> list:` — read image once; model.load once; loop: for i,(xyxy,bid) in enumerate(zip(box_xyxys, box_ids)): if abort_flag and abort_flag.is_set(): break; region = image[...].copy(); text = model.recognize(region); results.append({"box_id": bid, "text": text}); if progress_callback: progress_callback((i+1)*100 // len(box_xyxys), f"box {i+1}"); return results

    - `def _on_ocr_finished(self, result):` (single-box) — find the BoxItem by box_id; `box.pagebox.set_recognized_text(result["text"])` (Plan 01 setter, edited=False); `box.refresh_text_overlay(); box.refresh_badge()`; capture pre-state via canvas.boxes_snapshot() (CR-01 pre-state) and emit boxes_modified; clear _op_running; status_bar_left transient "OCR complete · 1 box recognized"; _refresh_action_states

    - `def _on_ocr_all_finished(self, results):` — for each result: find box by box_id; set_recognized_text; refresh. ONE boxes_modified emit for the batch (or one per box — executor picks; one batch emit is simpler per UI-SPEC §20 batch-undo pattern). Status: "OCR complete · N boxes recognized".

    - `def _on_ocr_error(self, worker_error):` — `logger.error(f"OCR failed: {worker_error}")`; self.error_chip.setText("OCR model error"); self.error_chip.show(); QMessageBox.critical(self, "Couldn't load the OCR model.", "The manga-ocr model files couldn't be loaded or downloaded. Check your network connection (first run downloads ~450 MB) and see the log for details."); clear _op_running; _refresh_action_states

    - `def _on_ocr_cleanup(self):` — hide progress_bar; restore status_bar_left to the box-count line; (the _op_running flag is cleared in finished/error handlers — confirm against how detection does it)

    - `def _resolve_ocr_model_path(self) -> Path:` — lazy `from panelcleaner.model_downloader import get_ocr_model_directory, is_ocr_downloaded`; `if is_ocr_downloaded(): return get_ocr_model_directory()` (CR-11 short-circuit); `return get_ocr_model_directory()` (the MangaOcr singleton's initialize_model() does the actual fetch inside the worker, off the GUI thread — Pitfall 6)

    - `def _confirm_reocr(self) -> bool:` — mirror _confirm_replace_boxes (QMessageBox, title "Run OCR", body the D-04 single-box copy from UI-SPEC §Copywriting, buttons [Cancel] [Re-run OCR]); return clickedButton is the Re-run button

    - `def _confirm_reocr_all(self, count: int) -> bool:` — same shape, body the D-04 batch copy with {count}, buttons [Cancel] [Re-run OCR on All]

    - `def _ocr_backend(self) -> str:` — return "torch" (D-14 default; the ONNX hook raises NotImplementedError in the factory — wired for the future)
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_boxes.py -x -q -k "ocr or reocr"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def run_ocr_selected\|def run_ocr_all\|def _run_ocr_task\|def _run_ocr_all_task" manga_ai_studio/gui/main_window.py` is 4
    - `grep -c "def _on_ocr_finished\|def _on_ocr_error\|def _on_ocr_cleanup" manga_ai_studio/gui/main_window.py` is >= 2
    - `grep -c "def _resolve_ocr_model_path" manga_ai_studio/gui/main_window.py` is 1
    - `grep -c "def _confirm_reocr" manga_ai_studio/gui/main_window.py` is >= 1
    - `grep -c "is_ocr_downloaded" manga_ai_studio/gui/main_window.py` is >= 1 (CR-11 cache-check)
    - `grep -c "backend_factory(\"ocr\"" manga_ai_studio/gui/main_window.py` is >= 1
    - `grep -c "Loading OCR model" manga_ai_studio/gui/main_window.py` is >= 1 (Pitfall 6 first-run UX)
    - `python -m pytest tests/test_gui_boxes.py -x -q` exits 0 (dispatcher tests with mocked model pass; real-model test skip-gated)
    - A test asserting run_ocr_selected with a mocked model writes set_recognized_text + emits boxes_modified passes
    - A test asserting the D-04 gate: edited=True + user clicks Cancel → no OCR runs passes
    - A test asserting the D-04 gate: edited=False → silent overwrite (no dialog) passes
  </acceptance_criteria>
  <done>OCR dispatcher mirrors detect_text (Worker+_op_running+progress); _run_ocr_task crops + recognizes + returns str; _on_ocr_finished writes set_recognized_text + refreshes overlay + emits boxes_modified; _resolve_ocr_model_path cache-checks via is_ocr_downloaded; D-04 single + batch confirm gates; first-run "Loading OCR model…" UX; all GUI box tests green with mocked model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Text menu (Run OCR / OCR All Ctrl+R) + auto-OCR hook on _commit_create (D-01)</name>
  <files>manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/canvas.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_build_menus ~224; the existing _build_tools_menu / _build_view_menu shapes; action_detect_text ~392 — the QAction template; toolbar section ~500; _refresh_action_states ~610)
    - manga_ai_studio/gui/canvas.py (_commit_create ~1487-1517 — the seam D-01 appends the OCR call to; boxes_modified signal ~154; the existing signal emission patterns)
    - manga_ai_studio/gui/box_item.py (Plan 04/05 — refresh_text_overlay the auto-OCR result triggers)
    - .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md (§Surface 1 — Text menu between View and Tools; contents Run OCR / OCR All Boxes (Ctrl+R) / separator / Auto-Number submenu / separator / Load Translations… — note: Auto-Number + Load Translations land in Plan 07, this plan adds Run OCR + OCR All only; §14 — D-01 auto-OCR on draw-release; §Shortcuts — Ctrl+R free)
    - .planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md (canvas.py Hook 1 lines 390 — _commit_create OCR hook via a new signal ocr_requested or MainWindow-injected callback; main_window.py menu pattern lines 479)
    - .planning/phases/04-ocr-recognition-text-editing/04-CONTEXT.md (D-01 — both OCR paths; the draw gesture preserved, Phase 4 appends OCR after the empty-box commit)
  </read_first>
  <behavior>
    - A new Text menu exists between View and Tools with "Run OCR" and "OCR All Boxes" (Ctrl+R) entries (Plan 07 adds Auto-Number + Load Translations to this menu)
    - Run OCR is enabled iff exactly one box is selected AND no async op is running
    - OCR All Boxes is enabled iff >= 1 box exists on the page AND no async op is running
    - Ctrl+R triggers run_ocr_all
    - After canvas._commit_create creates a user box (D-01), an OCR is automatically triggered on that box (off the GUI thread); the box arrives with recognized text once the worker finishes
    - A box < 8x8 scene px (the Phase 3 no-op threshold) does NOT trigger auto-OCR (UI-SPEC §14)
  </behavior>
  <action>
    In manga_ai_studio/gui/canvas.py EditorCanvas: add a new class-scope Signal `ocr_requested = Signal(object)` (carries the newly-created BoxItem) alongside boxes_modified. In _commit_create (after `item.setSelected(True)` ~line 1513 and BEFORE the boxes_modified emit at 1517), emit `self.ocr_requested.emit(item)` ONLY if the box is >= MIN_BOX_SIZE (already guaranteed by the early-return at line ~1488 — confirm; the 8x8 no-op already skips). Do NOT run manga-ocr on the GUI thread — the signal is the seam; MainWindow subscribes and dispatches the Worker. Keep the existing boxes_modified emit (the pre-create snapshot for undo) UNCHANGED — the OCR is a SEPARATE op that pushes its own BOXES entry on finish.

    In manga_ai_studio/gui/main_window.py: in __init__ after the canvas is constructed, connect `self.canvas.ocr_requested.connect(self._on_canvas_ocr_requested)`. Define `def _on_canvas_ocr_requested(self, box_item) -> None:` — select the box (it's already selected per _commit_create), then call the single-box OCR path: gate on self._op_running (if an op is running — e.g. a detection — skip the auto-OCR silently or queue it; executor picks — silently skipping is safer to avoid worker pileup); D-04 gate does NOT apply here (the box is freshly created with no text, so edited=False — silent overwrite is correct); dispatch run_ocr_selected's worker path. Refactor run_ocr_selected so the dispatch logic is reusable (extract a `_dispatch_ocr_for_box(box_item)` helper that both run_ocr_selected and _on_canvas_ocr_requested call).

    In _build_menus: add `def _build_text_menu(self) -> None:` creating a `text_menu = self.menuBar().addMenu("&Text")` placed between the View menu and the Tools menu (UI-SPEC §Surface 1 — recommended order File/Edit/View/Text/Tools/Help). Add:
    - `self.action_run_ocr = QAction("Run OCR", self); action_run_ocr.triggered.connect(self.run_ocr_selected); action_run_ocr.setEnabled(False); tooltip "Recognize text in the selected box with manga-ocr."` — text_menu.addAction(self.action_run_ocr)
    - `self.action_ocr_all = QAction("OCR All Boxes", self); action_ocr_all.setShortcut(QKeySequence("Ctrl+R")); action_ocr_all.triggered.connect(self.run_ocr_all); action_ocr_all.setEnabled(False); tooltip the UI-SPEC §Copywriting OCR All copy` — text_menu.addAction(self.action_ocr_all)
    - Call self._build_text_menu() in _build_menus at the right position.

    In _refresh_action_states: enable action_run_ocr iff exactly one box is selected AND not self._op_running (mirror action_detect_text's `page_open and not self._op_running` plus the one-box-selected check); enable action_ocr_all iff `page_open and self.canvas.box_count() > 0 and not self._op_running`.

    Verify Ctrl+R is free (UI-SPEC §Shortcuts conflict audit confirms — no existing Ctrl+R in main_window.py / canvas.py; grep to confirm).
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_boxes.py -x -q -k "text_menu or auto_ocr or ctrl_r"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _build_text_menu" manga_ai_studio/gui/main_window.py` is 1
    - `grep -c "action_run_ocr\|action_ocr_all" manga_ai_studio/gui/main_window.py` is >= 4 (construction + menu + refresh refs)
    - `grep -c 'QKeySequence("Ctrl+R")' manga_ai_studio/gui/main_window.py` is 1
    - `grep -c "ocr_requested" manga_ai_studio/gui/canvas.py` is >= 2 (Signal declaration + emit in _commit_create)
    - `grep -c "_on_canvas_ocr_requested" manga_ai_studio/gui/main_window.py` is >= 2 (connect + handler)
    - `python -m pytest tests/test_gui_boxes.py -x -q` exits 0
    - A test asserting Alt+drag draw-release emits ocr_requested (auto-OCR hook, D-01) passes
    - A test asserting the Text menu has Run OCR + OCR All Boxes entries passes
    - A test asserting Run OCR is disabled when no box selected passes
  </acceptance_criteria>
  <done>Text menu (Run OCR / OCR All Boxes Ctrl+R) wired between View and Tools; auto-OCR hook on canvas._commit_create via ocr_requested signal (D-01); action enable-states refresh correctly; all GUI box tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify OCR dispatcher on real manga (D-01/D-03/D-04, real model)</name>
  <files>.planning/phases/04-ocr-recognition-text-editing/04-06-SUMMARY.md</files>
  <action>
    Plan 04-06 wires the OCR dispatcher: Alt+drag draw-release auto-runs manga-ocr (D-01); Text menu Run OCR (selected box) + OCR All Boxes (Ctrl+R) (D-03); the D-04 re-OCR confirm gate on user-edited text; the first-run "Loading OCR model…" UX (Pitfall 6); status-bar progress. The ~450MB first-run download happens on the FIRST OCR call of the session (cached afterward via the CR-11 cache-check). The automated pytest-qt suite uses a MOCKED manga-ocr model for the dispatcher logic (the real model is not loaded in CI). This checkpoint runs the REAL manga-ocr model on a real Japanese manga page — the only place the real model is exercised end-to-end (RESEARCH Environment Availability + VALIDATION.md manual-only verifications).
  </action>
  <verify>
    <human-check>
      Launch the app (`python -m manga_ai_studio`) and perform these checks on a real Japanese manga page:
      1. Open a manga page with Japanese text.
      2. FIRST RUN: Alt+drag to draw a box over a Japanese text region. On release, confirm "Loading OCR model…" appears in the status bar (first-run ~450MB download — wait for it). Once loaded, confirm the box fills with recognized Japanese text and the canvas overlay shows it.
      3. SUBSEQUENT RUNS: draw another box — OCR should be fast (model cached, no re-download).
      4. Run text-box detection (D) to get detected boxes. Select one detected box (empty text). Text → Run OCR. Confirm it fills with recognized text (D-02 — OCR on detected boxes).
      5. Page with several boxes: Text → OCR All Boxes (or Ctrl+R). Confirm the status bar shows "OCR All… N/M · box X" progress and each text-empty box fills (D-03).
      6. Manually edit a box's recognized text (inline editor or Inspector — sets edited=True). Select it; Text → Run OCR. Confirm the D-04 confirm dialog appears ("This box has text you edited. Running OCR will overwrite your edit..."). Click Cancel — confirm no overwrite. Repeat, click Re-run OCR — confirm overwrite.
      7. OCR All on a page with edited boxes: confirm the batch D-04 gate appears with the count.
      8. Trigger an OCR error (e.g. temporarily rename the HF model cache dir) — confirm the #7a1f1f error chip + QMessageBox appear with friendly copy (then restore the cache).
    </human-check>
    <automated>python -m pytest tests/test_gui_boxes.py -x -q</automated>
  </verify>
  <done>All eight manual checks pass (auto-OCR on draw, Run OCR on selected, OCR All Ctrl+R progress, D-04 single + batch confirm gates, first-run download UX, error UX) and the test_gui_boxes.py suite is green (with the real-model test skip-gated unless the model is cached).</done>
  <resume-signal>Type "approved" or describe issues (e.g. OCR hangs, status bar not updating, D-04 gate not firing, first-run download freezing the UI)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page numpy → manga-ocr model (Worker thread) | the cropped region crosses into the heavy model call; runs off the GUI thread (T-01-07) |
| OCR result str → PageBox | model-generated untrusted text written via set_recognized_text; rendered as plain text by the overlay (Plan 04, ASVS V5) |
| HF cache → model load | first-run ~450MB download; cache-check before download |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-11 | Denial of Service | OCR on the GUI thread freezes the UI (esp. first-run download) | high | mitigate | All OCR runs via Worker(QRunnable) + QThreadPool (T-01-07); _resolve_ocr_model_path + model.load happen inside the worker task. Test asserts the worker is dispatched (not called inline). |
| T-4-12 | Tampering | D-04 gate bypassed — re-OCR silently overwrites a manual correction | high | mitigate | run_ocr_selected + run_ocr_all check pagebox.edited before dispatch; _confirm_reocr / _confirm_reocr_all dialogs fire when edited=True. Tests test_reocr_confirms_when_edited + test_reocr_silent_when_raw. |
| T-4-13 | Denial of Service | ~450MB model re-downloaded every session | medium | mitigate | _resolve_ocr_model_path short-circuits via is_ocr_downloaded() (CR-11 pattern, RESEARCH Pitfall 6). Test asserts the cache-check is called. |
| T-4-14 | Denial of Service | "OCR All" worker pileup if multiple dispatches overlap | low | mitigate | _op_running gate at the top of run_ocr_selected / run_ocr_all / _on_canvas_ocr_requested (mirrors detect_text). Auto-OCR on rapid draw is gated. |
</threat_model>

<verification>
- `python -m pytest tests/test_gui_boxes.py -x -q` green (mocked-model dispatcher tests)
- `python -m pytest tests/ -q` full suite green
- Human-verify checkpoint: auto-OCR on draw, Run OCR on selected, OCR All (Ctrl+R), D-04 confirm gates, first-run UX, error UX
</verification>

<success_criteria>
- OCR dispatcher (Worker + _op_running + status-bar progress) mirrors detect_text exactly
- Auto-OCR on Alt+drag draw-release via canvas.ocr_requested signal (D-01)
- Text menu (Run OCR / OCR All Ctrl+R) between View and Tools
- D-04 single + batch confirm gates (silent on edited=False, dialog on edited=True)
- _resolve_ocr_model_path CR-11 cache-check (no re-download per session)
- First-run "Loading OCR model…" UX (Pitfall 6)
- Human checkpoint approved (real OCR on a manga page, D-04 gate, first-run UX)
</success_criteria>

<output>
Create `.planning/phases/04-ocr-recognition-text-editing/04-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan creates (downstream plan-review-convergence: exclude these from drift verification):
- `manga_ai_studio.gui.main_window.MainWindow.run_ocr_selected()`
- `manga_ai_studio.gui.main_window.MainWindow.run_ocr_all()`
- `manga_ai_studio.gui.main_window.MainWindow._run_ocr_task(image_path, box_xyxy, model, ...)`
- `manga_ai_studio.gui.main_window.MainWindow._run_ocr_all_task(image_path, box_xyxys, box_ids, model, ...)`
- `manga_ai_studio.gui.main_window.MainWindow._on_ocr_finished(result)` / `_on_ocr_all_finished(results)` / `_on_ocr_error(err)` / `_on_ocr_cleanup()`
- `manga_ai_studio.gui.main_window.MainWindow._resolve_ocr_model_path()`
- `manga_ai_studio.gui.main_window.MainWindow._confirm_reocr() -> bool` / `_confirm_reocr_all(count) -> bool`
- `manga_ai_studio.gui.main_window.MainWindow._ocr_backend() -> str`
- `manga_ai_studio.gui.main_window.MainWindow._dispatch_ocr_for_box(box_item)` (shared by run_ocr_selected + auto-OCR)
- `manga_ai_studio.gui.main_window.MainWindow._on_canvas_ocr_requested(box_item)` (D-01 auto-OCR hook)
- `manga_ai_studio.gui.main_window.MainWindow._build_text_menu()` + `action_run_ocr` + `action_ocr_all` (Ctrl+R)
- `manga_ai_studio.gui.canvas.EditorCanvas.ocr_requested = Signal(object)` + emit in `_commit_create`
</content>
</invoke>
