---
phase: 04-ocr-recognition-text-editing
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - manga_ai_studio/gui/box_item.py
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_boxes.py
  - tests/test_gui_batch.py
  - .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md
autonomous: true
requirements: [TEXT-04]
tags: [gui, box-item, text-overlay, fit-in-box, menu-structure, toolbar, gap-closure, uat-test-1, tdd, pytest-qt]
must_haves:
  truths:
    - "A long recognized/translation text renders WRAPPED inside the box rect: the overlay document is laid out at the box inner width (rect width minus 2x inset), so no horizontal overshoot — the overlay's scene rect stays within the box rect (width AND height)"
    - "The overlay font ADAPTS to the box size: base viewport px = 14 x min(box_w, box_h)/100 (14 for a 100 scene-px box — the §16 reference), clamped to [10, 28] viewport px at every zoom; a bounded shrink-to-fit loop reduces the font (max 12 steps of 0.9, floor 5 viewport px) when the wrapped text still exceeds the inner height"
    - "A resized box re-wraps/re-fits its overlay text at resize COMMIT (one refresh per drag, never per-mousemove — the RC-1 setPos-only drag path is preserved)"
    - "The menu bar top-level shows exactly File, Edit, View, Text, Tools, Help — Recent Files and Batch appear ONLY as File submenus (their menuActions are in the File menu's action list, NOT the menubar's)"
    - "The toolbar's first action is Open Folder (Ctrl+Shift+O); Open Image stays in the File menu (Ctrl+O) and is no longer on the toolbar"
    - "The existing GUI suite stays green (no regressions beyond the pre-existing deselected flake)"
  artifacts:
    - "box_item.py — _overlay_inset() helper (pen_w/2 + _OVERLAY_INSET); _OVERLAY_BOX_REF_DIM/_OVERLAY_FIT_MAX_ITERS/_OVERLAY_FIT_STEP/_OVERLAY_FIT_FLOOR_VP constants; refresh_text_overlay builds the wrapped (setTextWidth) box-adaptive document with the bounded shrink-to-fit loop"
    - "canvas.py — _commit_resize calls item.refresh_text_overlay() after _sync_handles (resize-commit re-wrap)"
    - "main_window.py — recent_menu/batch_menu constructed as standalone QMenu('Recent Files'/'Batch', self) children (never via the menubar factory); toolbar first action is action_open_folder"
    - "tests/test_gui_boxes.py — 04-09 section: wrap test, box-adaptive font test (parametrized), no-overshoot+shrink test, resize-commit re-wrap test"
    - "tests/test_gui_batch.py — 04-09 section: menubar top-level order, File-submenu membership, File menu internal order, toolbar first-action + Open Image retention tests"
    - "04-UI-SPEC.md — two §16 amendment notes (fit-in-box geometry + box-adaptive base)"
  key_links:
    - "refresh_text_overlay (box_item.py:461-507) -> setTextWidth(inner_w) + box-adaptive font + shrink-to-fit loop -> _reposition_text_overlay (overlay stays inside the box)"
    - "canvas resize-commit (canvas.py:1609-1631 _commit_resize) -> refresh_text_overlay (re-wrap/re-fit at release; _advance_resize per-mousemove path stays setPos-only)"
    - "apply_overlay_zoom (stored _overlay_zoom) -> refresh_text_overlay (the fit loop re-runs at every zoom because the scene font changes the wrapped height)"
    - "_build_file_menu (main_window.py:257-315) -> standalone QMenu children -> file_menu.addMenu (menuAction lands ONLY in the File menu, not on the menubar action list)"
---

<objective>
Close the three NEW UAT test-1 gaps recorded in 04-UAT.md (the RC-1/2/3 gap is
marked resolved by plan 04-08 and is NOT re-opened here):

- **Gap 1 (MAJOR, user):** "it's still a bit small and it overshoots the box,
  renders horizontally — it should try to fit in the box and kind of adapt the
  text to the size of the text box". Root cause (verified in source):
  refresh_text_overlay (box_item.py:461-507) calls setPlainText with NO
  setTextWidth, so QTextDocument lays the text out on ONE horizontal line —
  long recognized/translation text extends beyond the box rect (the §16
  "Width-clamped to the box's inner width" contract, UI-SPEC line 255, is
  unimplemented) — and the font is a fixed 14 viewport-px base regardless of
  box size (the user's "bit small" for large boxes). Fix: wrap the document at
  the box inner width + make the font box-adaptive (base = 14 x min(box_w,
  box_h)/100 viewport px, clamped [10,28]) + a bounded shrink-to-fit loop so
  the text "tries to fit" the box height. Also re-wrap at resize COMMIT so the
  user sees the adaptation after a resize.
- **Gap 2 (MINOR, user):** "recent files and batch are ahead of File and Edit,
  it should be Recent files, File, and then batch". Root cause (verified):
  main_window.py:269/292 construct recent_menu/batch_menu via
  `self.menuBar().addMenu(...)`, which appends their menuActions to the
  MENUBAR action list; the later file_menu.addMenu (309/312) re-parents the
  QMenu into File but Qt does NOT remove the action from the menubar — Recent
  Files and Batch appear BOTH top-level (ahead of File/Edit) AND as File
  submenus. Fix: construct the two submenus as standalone QMenu children of
  the window; the top-level bar then holds exactly File, Edit, View, Text,
  Tools, Help. (Probe-verified: standalone QMenu construction + file_menu
  addMenu leaves the menuAction in the File menu only.)
- **Gap 3 (MINOR, user):** "on the small bar under there with tools, it should
  have open folder instead of open image". Root cause (verified):
  main_window.py:617 `self.toolbar.addAction(self.action_open_image)`. Fix:
  the toolbar's open action becomes action_open_folder (Ctrl+Shift+O, already
  connected to open_folder at line 266); Open Image stays in the File menu
  (Ctrl+O). Verified: _refresh_action_states (715-790) does NOT gate either
  open action (both stay enabled), so no state-refresh change is needed; no
  icons are set on either action today, so the toolbar simply shows the
  action's text label — no icon work (out of scope, pure swap).

Purpose: make the UAT test-1 truth "text overlay adapts to box size: overlay
text wraps/fits INSIDE the box rect (no horizontal overshoot), sized legibly
relative to the box" observable, and land the two requested app-surface UX
fixes, all as pure-fix TDD tasks with headless pytest-qt regression tests.
Output: 3 fix tasks + ~10 regression test functions (~12 cases), two §16 spec
amendment notes, full suite (439 baseline) green.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ocr-recognition-text-editing/04-UAT.md
@.planning/phases/04-ocr-recognition-text-editing/04-08-SUMMARY.md
@.planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md

@manga_ai_studio/gui/box_item.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/main_window.py
@tests/test_gui_boxes.py
@tests/test_gui_batch.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap 1 — overlay fit-in-box: wrap at box inner width + box-adaptive font + bounded shrink-to-fit + resize-commit re-wrap</name>
  <files>manga_ai_studio/gui/box_item.py, manga_ai_studio/gui/canvas.py, tests/test_gui_boxes.py, .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md</files>
  <behavior>
    - Test A (test_text_overlay_wraps_long_text_to_box_width): a BoxItem with recognized text "word " x 60 (the _pagebox_with_text reference box (20,20,220,120) — rect w 200, unselected pen 2 -> inset 3 -> inner width 194) after refresh_text_overlay() has item._text_overlay.textWidth() == approx(194.0) AND document().size().width() == approx(194.0) AND sceneBoundingRect().right() <= item.sceneBoundingRect().right() + 1.5 AND toPlainText() still equals the full text. FAILS pre-fix: no setTextWidth, the doc lays out on one line (~2500 px wide), right edge ~2523 >> box right 220.
    - Test B (test_text_overlay_font_adapts_to_box_size): parametrized (box, expected font at zoom 1): Box(20,20,220,120) -> 14.0 (min dim 100, the §16 reference — this case keeps the 04-08 contract), Box(20,20,200,150) -> 21.0 (min 150), Box(20,20,320,320) -> 28.0 (min 300 -> base 42, clamped to the §16 ceiling). After refresh_text_overlay() the document charFormat (the textCursor/select-Document access pattern) has font().pointSizeF() == approx(expected, abs=0.1) and font().pointSizeF() * 1.0 stays within [10, 28]. Text "hello" (fits at every size — no shrink interference). FAILS pre-fix: font is 14.0 for every box.
    - Test C (test_text_overlay_shrinks_to_fit_box_height): the reference box with "word " x 40 (wrapped at 194 the base 14 vp font needs ~8 lines — exceeds inner height 94): after refresh_text_overlay() the overlay sceneBoundingRect is CONTAINED in the box sceneBoundingRect (right() <= box.right() + 1.5 AND bottom() <= box.bottom() + 1.5 — the fit loop shrank the font) AND the document charFormat font().pointSizeF() < 14.0 (proves the shrink loop engaged) AND document().size().width() still == approx(194.0) (wrap preserved through the loop). FAILS pre-fix: single unwrapped line overflows width.
    - Test D (test_resize_commit_rewraps_overlay_text_canvas): a canvas with one DETECTED box carrying "word " x 40 (canvas.set_boxes pattern from 04-08's test_zoom_changed_reapplies_overlay_style_canvas); set canvas._resizing_box = item, canvas._resize_start_rect = QRectF(item.rect()), canvas._boxes_interaction_start_snapshot = []; item.setRect(QRectF(20, 20, 320, 200)); canvas._commit_resize() -> item._text_overlay.textWidth() == approx(314.0) (320 - 2x3) AND the charFormat font == approx(28.0) (min dim 200 -> base 28) — the resize COMMIT re-wrapped and re-fit to the new rect. FAILS pre-fix: _commit_resize never refreshes the overlay (stale 194-width doc, font 14).
  </behavior>
  <action>
    In manga_ai_studio/gui/box_item.py:

    1. Add four module constants next to _OVERLAY_FONT_BASE (line 108):
       - _OVERLAY_BOX_REF_DIM = 100.0 — the box min dimension (scene px) that maps to the §16 14-viewport-px base (the 04-08 reference box is exactly 100 min, so the 04-08 zoom-clamp tests keep passing UNCHANGED — verify this claim in the RED run).
       - _OVERLAY_FIT_MAX_ITERS = 12 — bounded shrink-to-fit iterations.
       - _OVERLAY_FIT_STEP = 0.9 — per-iteration viewport-px reduction factor.
       - _OVERLAY_FIT_FLOOR_VP = 5.0 — hard floor: below this viewport-px target the loop stops (a box that cannot hold the text even at 5 viewport px keeps the §16 clipped-overflow fallback; 12 steps of 0.9 reach ~0.28x, above the floor for the reference base).
    2. Add a private helper `_overlay_inset(self) -> float` returning
       `self.pen().widthF() / 2.0 + _OVERLAY_INSET`, docstring: the overlay
       inset (border half-width + 2px margin, UI-SPEC §16). Refactor
       _reposition_text_overlay (lines 509-522) to use it (replacing its
       inline pen_w/inset lines) so the position and the wrap width share ONE
       formula — the existing 04-08 position tests (topLeft 23,23 / 63,43)
       must stay green (pen 2 -> inset 3.0).
    3. Rework refresh_text_overlay (lines 461-507). Keep the empty-text early
       return (475-477) and the final _reposition_text_overlay() + setVisible
       lines (506-507). Replace the format build (483-502) with this contract,
       in order:
       - After setPlainText(text) (line 483), read doc = document(); zoom =
         self._overlay_zoom; inset = self._overlay_inset();
         inner_w = max(1.0, self.rect().width() - 2.0 * inset);
         inner_h = max(1.0, self.rect().height() - 2.0 * inset);
         self._text_overlay.setTextWidth(inner_w) — THE wrap fix (the
         document now lays out at the box inner width; the max() floor keeps a
         degenerate/negative-width rect from producing an invalid text width).
       - Box-adaptive base: base_vp = _OVERLAY_FONT_BASE * min(self.rect()
         .width(), self.rect().height()) / _OVERLAY_BOX_REF_DIM;
         target_vp = min(28.0, max(10.0, base_vp * zoom)) — the §16 clamp on
         the box-adapted base; for the 04-08 reference box this reduces
         EXACTLY to the 04-08 formula clamp(14*zoom, 10, 28).
       - Shrink-to-fit loop, bounded: for _ in range(_OVERLAY_FIT_MAX_ITERS):
         build fmt (QTextCharFormat with font pointSizeF = target_vp / zoom,
         textOutline QPen(_OVERLAY_OUTLINE.color(), 2.0 / zoom), foreground
         _OVERLAY_FILL); select Document; mergeCharFormat; then
         if doc.size().height() <= inner_h or target_vp <=
         _OVERLAY_FIT_FLOOR_VP: break; else target_vp *= _OVERLAY_FIT_STEP.
         The size read is live (probe-verified: QTextDocument.size() reflects
         mergeCharFormat font changes immediately, even on a QGraphicsTextItem
         child of a scene). Keep the §16 comment (the clamp keeps the RENDERED
         viewport-px size within [10,28]) and add one line noting the wrap +
         box-adaptive base + bounded fit loop (plan 04-09 Gap 1) — the loop
         never runs on the 04-08 short-text tests.
       - PLAIN-text discipline unchanged (setPlainText only, T-4-07): the loop
         re-merges QTextCharFormat over the same plain document, never
         setHtml.

    In manga_ai_studio/gui/canvas.py:
    - In _commit_resize (lines 1609-1631), after `item._sync_handles()`
      (line 1628) add `item.refresh_text_overlay()` — the resize COMMIT
      re-wraps/re-fits the overlay to the final rect ONCE per drag. The
      per-mousemove _advance_resize path (1607) stays setPos-only (RC-1
      discipline: refresh_text_overlay rebuilds the document, which is why it
      must NOT run per-mousemove). The WR-04 delta-check emit (1630-1631) is
      untouched. Move/zoom/content paths already refresh (apply_overlay_zoom,
      content setters) — no other canvas change.

    In tests/test_gui_boxes.py, add a new section header AFTER
    test_zoom_changed_reapplies_overlay_style_canvas (line ~1297):
    "-- UAT test 1 gap closure round 2 (plan 04-09): overlay fit-in-box --"
    with the four tests from <behavior>. Reuse _scene_with_box /
    _pagebox_with_text / _canvas_with_image_and_boxes; for Test B's custom
    boxes construct PageBox(box=box, origin=DETECTED) then
    pb.set_recognized_text("hello") directly (the _pagebox_with_text helper
    hard-codes the reference box). pytest.approx for all geometry asserts;
    the charFormat access pattern mirrors the 04-08 tests (textCursor,
    select Document). For Test D copy the canvas wiring pattern of
    test_zoom_changed_reapplies_overlay_style_canvas and drive
    canvas._commit_resize() directly with the three state fields set above
    (do not fire synthetic mouse events — the commit method is the unit).

    In .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md, TWO
    scoped edits (Edit tool, never Write):
    a) §16 Geometry line (~255, "Width-clamped to the box's inner width…"):
       append — "Fit-in-box amendment (04-09 gap closure): the width clamp is
       implemented via QGraphicsTextItem.setTextWidth (inner width = box width
       - 2x inset, inset = border half-width + 2px); the base font is
       BOX-ADAPTIVE (14 x min(box_w, box_h)/100 viewport px, i.e. 14 for a 100
       scene-px box) and a bounded shrink-to-fit loop (max 12 steps of 0.9,
       floor 5 viewport px) reduces the font when the wrapped text still
       exceeds the inner height, so the text 'tries to fit'; clipping remains
       only the pathological fallback."
    b) §16 Font+scaling base line (~260, "Base size 14 scene px at 100%…"):
       append — "04-09 amendment: the 14px base is scaled by the box's smaller
       dimension (min(box_w, box_h)/100), so larger boxes render larger text
       up to the [10,28] viewport-px clamp — the UAT test-1 fit-in-box truth
       (a 100 scene-px box keeps the original 14px base)."
    Do not touch any other spec row (the 04-08 deviation notes stay).
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_boxes.py::test_text_overlay_wraps_long_text_to_box_width tests/test_gui_boxes.py::test_text_overlay_font_adapts_to_box_size tests/test_gui_boxes.py::test_text_overlay_shrinks_to_fit_box_height tests/test_gui_boxes.py::test_resize_commit_rewraps_overlay_text_canvas -q</automated>
  </verify>
  <done>
    RED commit: the four tests fail on current source (Test A: single unwrapped
    line, doc width ~2500; Test B: font 14.0 for every box; Test C: width
    overflow; Test D: stale 194-width doc, font 14) — confirmed before
    implementation. GREEN commit: all pass (Test B 3 parametrized cases);
    `grep -v '^#' manga_ai_studio/gui/box_item.py | grep -c 'setTextWidth'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/box_item.py | grep -c '_OVERLAY_BOX_REF_DIM\|_OVERLAY_FIT_'`
    >= 6 (4 definitions + their uses in refresh_text_overlay),
    `grep -v '^#' manga_ai_studio/gui/box_item.py | grep -c 'def _overlay_inset'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/canvas.py | grep -c 'refresh_text_overlay'`
    >= 2 (existing docstring line ~1350 + the new _commit_resize call), and
    both UI-SPEC §16 amendment notes are present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gap 2 — menu bar: Recent Files + Batch are File submenus only (standalone QMenu construction)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_batch.py</files>
  <behavior>
    - Test A (test_menubar_top_level_has_only_the_six_core_menus): a fresh MainWindow (the _make_window helper) has menuBar().actions() texts EXACTLY ["&File", "&Edit", "&View", "&Text", "&Tools", "&Help"]. FAILS pre-fix: the list is ["Recent Files", "Batch", "&File", "&Edit", "&View", "&Text", "&Tools", "&Help"] (the user's exact report — Recent Files and Batch ahead of File and Edit).
    - Test B (test_recent_and_batch_menus_are_file_submenus_not_top_level): window.recent_menu.menuAction() and window.batch_menu.menuAction() ARE in the File menu's action list (file_menu = next(a for a in menuBar().actions() if a.text() == "&File").menu()) AND are NOT in menuBar().actions(). FAILS pre-fix on the second assertion (the actions are in the menubar action list — probe-verified; Qt does not remove the menuAction when addMenu re-parents the QMenu).
    - Test C (test_file_menu_internal_order): the File menu's non-separator action texts in order: ["Open Image\u2026", "Open Folder\u2026", "Recent Files", "Export Page\u2026", "Batch", "Quit"], with separators (isSeparator()) at indices 3 and 6 (after Recent Files and after Batch). Passes pre-fix (the File menu order is already correct — locks it so the parent fix cannot reorder).
  </behavior>
  <action>
    In manga_ai_studio/gui/main_window.py — the QMenu parent fix, nothing else:

    1. Add QMenu to the PySide6.QtWidgets import block (lines 39-49),
       alphabetically between QMainWindow (line 44) and QMessageBox (line 45).
    2. Line 269 currently constructs recent_menu via the MENU BAR's addMenu
       factory titled "Recent Files" — replace with a standalone QMenu child of
       the window: `self.recent_menu = QMenu("Recent Files", self)`. Update the
       existing nearby comment ("Recent Files submenu (max 8 via QSettings).")
       to add one sentence: the menu is created as a standalone child (never
       through the menu bar factory) so its menuAction never lands in the
       menubar's top-level action list — the File-menu addMenu below is the
       only home.
    3. Line 292 (titled "Batch") — same replacement:
       `self.batch_menu = QMenu("Batch", self)`, with a matching one-sentence
       comment (the Batch submenu is a File submenu only; the three batch
       actions + enablement are unchanged).
    4. Lines 306-314 (file_menu construction + addAction/addMenu order) are
       UNCHANGED: Open Image, Open Folder, Recent Files submenu, separator,
       Export, Batch submenu, separator, Quit — that order already matches the
       user's request ("Recent files, File, and then batch").
    5. _refresh_recent_menu (1071-1084) and _clear_recent_files (1067-1069)
       operate on self.recent_menu by reference — no change needed for the new
       construction. No other menuBar() references change; Edit/View/Text/
       Tools/Help keep their genuine top-level construction.

    Probe-verified contract (do NOT re-verify at runtime): QMenu.addMenu
    appends the submenu's menuAction to the receiving QMenu's action list but
    never removes it from a QMenuBar's list and does NOT change the QMenu's
    parent — which is exactly why the standalone construction is the fix, and
    why the tests assert action-list membership (NOT parent()).

    In tests/test_gui_batch.py, add a new section header:
    "-- UAT test 1 gap closure round 2 (plan 04-09): menu bar structure --"
    after the existing tests, with the three tests from <behavior> using the
    existing `_make_window(qtbot, tmp_path)` helper (no page load needed: the
    menubar exists because MainWindow.__init__ calls _build_menus at
    main_window.py:177; QAction wrappers from actions() are temporary — keep
    strong local references per the test_gui_boxes.py:2694 precedent). Guard
    the text comparisons with the exact menu titles including the "&"
    prefixes as written in the source.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_batch.py::test_menubar_top_level_has_only_the_six_core_menus tests/test_gui_batch.py::test_recent_and_batch_menus_are_file_submenus_not_top_level tests/test_gui_batch.py::test_file_menu_internal_order -q</automated>
  </verify>
  <done>
    RED commit: Test A and the not-in-menubar assertion of Test B fail on
    current source (top-level shows Recent Files and Batch ahead of File) —
    confirmed before implementation; Test C passes pre-fix by design (order
    lock). GREEN commit: all pass;
    `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'QMenu("Recent Files"'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'QMenu("Batch"'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'QMenu'`
    >= 3 (import + the two constructions), and the menubar-factory addMenu
    calls with the Recent Files / Batch titles no longer exist (count of the
    full `menuBar().addMenu(` + title sequences == 0).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gap 3 — toolbar open action is Open Folder (manga default), Open Image stays in the File menu</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_batch.py</files>
  <behavior>
    - Test A (test_toolbar_first_action_is_open_folder): window.toolbar.actions()[0] IS window.action_open_folder; its text() == "Open Folder\u2026"; its shortcut() == QKeySequence("Ctrl+Shift+O"); and window.action_open_folder is NOT the same object as window.action_open_image. FAILS pre-fix: the first toolbar action is window.action_open_image (main_window.py:617).
    - Test B (test_open_image_remains_in_file_menu_not_toolbar): window.action_open_image IS in the File menu's action list (file_menu = next(a for a in menuBar().actions() if a.text() == "&File").menu()) AND window.action_open_image is NOT in window.toolbar.actions(). FAILS pre-fix on the second assertion; the first passes pre-fix (locks retention).
    - Test C (test_toolbar_open_folder_triggered_opens_folder): connect a spy to window.action_open_folder.triggered, call window.action_open_folder.trigger(), assert the spy fired — the toolbar's action IS window.action_open_folder (Test A identity), whose triggered is connected to open_folder at main_window.py:266, so the wiring is covered by identity + the existing connection (no dialog stubbing needed).
  </behavior>
  <action>
    In manga_ai_studio/gui/main_window.py:

    1. Line 617 (`self.toolbar.addAction(self.action_open_image)`) becomes
       `self.toolbar.addAction(self.action_open_folder)`. This is the ONLY
       toolbar change. The action already exists (line 264), carries
       Ctrl+Shift+O (line 265), and is connected to open_folder (line 266) —
       the manga-workflow default the user asked for (open a folder of pages
       by default).
    2. Verified, NO change needed: _refresh_action_states (715-790) does not
       gate either open action (both stay enabled regardless of page/folder/
       async state), so the toolbar swap needs no state-refresh wiring; no
       icons are set on either action today (grep confirms no setIcon calls),
       so the toolbar renders the action's text label "Open Folder…" exactly
       as it rendered "Open Image…" — do NOT add icons (out of scope, pure
       swap). Open Image keeps Ctrl+O in the File menu (line 307) — both
       shortcuts continue to work.
    3. Update the _build_toolbar docstring's first bullet (lines 606-610,
       "Phase 1 ships: Open | …") minimally: "Open Folder" (the phase-2
       action, Ctrl+Shift+O — the manga default; Open Image lives in the File
       menu). Do not reword anything else.

    In tests/test_gui_batch.py, add a section header right after the Task 2
    section:
    "-- UAT test 1 gap closure round 2 (plan 04-09): toolbar open action --"
    with the three tests from <behavior> using `_make_window(qtbot, tmp_path)`.
    Import QKeySequence from PySide6.QtGui at the section top if not already
    imported. For Test C use a plain list as the spy (append on triggered —
    the pattern in test_gui_batch.py's batch tests).
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_batch.py::test_toolbar_first_action_is_open_folder tests/test_gui_batch.py::test_open_image_remains_in_file_menu_not_toolbar tests/test_gui_batch.py::test_toolbar_open_folder_triggered_opens_folder -q</automated>
  </verify>
  <done>
    RED commit: Tests A and the toolbar half of Test B fail on current source
    (first toolbar action is Open Image) — confirmed before implementation;
    the File-menu retention half and Test C's spy pass pre-fix (locks). GREEN
    commit: all pass;
    `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'self.toolbar.addAction(self.action_open_folder)'`
    == 1 and
    `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'self.toolbar.addAction(self.action_open_image)'`
    == 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCR/pasted text -> overlay document | Untrusted model/output text crosses into a QTextDocument rendered on the canvas. UNCHANGED by this plan — the fit loop re-merges QTextCharFormat over a PLAIN document (setPlainText only, no HTML), so the ASVS V5 mitigation holds verbatim (T-4-07). |
| Box rect (canvas geometry) -> wrap/fit arithmetic | Box coordinates are user-dragged (untrusted within scene bounds) and feed setTextWidth + the fit loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-07 | Spoofing | refresh_text_overlay document build | high | mitigate | Existing: PLAIN document, setPlainText + mergeCharFormat (never setHtml). The 04-09 fit loop re-merges the same QTextCharFormat per iteration over the same plain doc — no new input surface; regression guard test_text_overlay_document_is_plain_not_rich stays green. |
| T-4-12g | Tampering | shrink-to-fit arithmetic (target_vp / zoom) | low | mitigate | Bounded loop (_OVERLAY_FIT_MAX_ITERS = 12, _OVERLAY_FIT_FLOOR_VP = 5.0) so the loop always terminates; division by zoom is safe because apply_overlay_zoom guards zoom <= 0 -> 1.0 (T-4-08g) before refresh runs. |
| T-4-13g | Tampering | box-rect-derived wrap dimensions (setTextWidth) | low | mitigate | inner_w/inner_h floored with max(1.0, ...) so a degenerate or negative-size rect can never produce a zero/negative text width (QTextDocument would clamp anyway; the guard keeps the fit loop's size reads sane). |
| T-4-14g | Spoofing | menu-bar / toolbar action re-parenting | low | accept | No new input surface: the same actions re-parented (recent/batch submenus now File-only; toolbar shows the existing open_folder action whose handler + OS-validated path logic (Phase 2) are unchanged). The Ctrl+O / Ctrl+Shift+O shortcuts stay bound to the same actions. |
</threat_model>

<verification>
Per-task gates: Task 1 — the four fit-in-box tests pass (RED state confirmed
against current source first: single-line layout, flat 14 font, no resize
refresh); Task 2 — the top-level-order test and the not-in-menubar assertion
fail RED, order-lock test passes by design; Task 3 — the first-action and
not-in-toolbar assertions fail RED, retention/spy tests pass by design. Grep
gates per each task's <done>.

Suite regression (after all three tasks, from the repo root):

    python -m pytest tests/test_gui_boxes.py tests/test_gui_batch.py tests/test_gui_canvas.py -q \
      --deselect tests/test_gui_boxes.py::test_moved_box_via_real_events_persists_round_trip

must stay fully green (the pre-existing flake stays deselected — out of scope,
do NOT fix it). Final full-suite run:

    python -m pytest tests/ -q --deselect tests/test_gui_boxes.py::test_moved_box_via_real_events_persists_round_trip

must show 439 passed + the ~12 new cases, 0 failed (fixes must not regress the
existing suite — in particular the 04-08 overlay tests: geometry tracking,
zoom clamp math, viewport-px outline, stored-zoom refresh).

<human-check>
The on-artwork visual judgment (wrapped text fits the box and reads sized
relative to it over real manga artwork) is a UAT re-test call, not an offscreen
assert — deferred to the UAT test-1 re-verification (end-of-phase human gate,
per the project cadence recorded in STATE.md). The mechanical contract is fully
covered by the automated suite above.
</human-check>
</verification>

<success_criteria>
- The UAT test-1 fit-in-box truth is observable: long overlay text wraps inside
  the box rect (no horizontal overshoot, Test A), the font grows with the box
  up to the [10,28] viewport-px clamp (Test B) and shrinks (bounded) when the
  wrapped text would exceed the box height (Test C), and a resize commit
  re-wraps/re-fits (Test D).
- The menu bar shows exactly File, Edit, View, Text, Tools, Help top-level;
  Recent Files and Batch appear only inside File, in the order the user
  requested (Open Image, Open Folder, Recent Files, sep, Export, Batch, sep,
  Quit).
- The toolbar's first action is Open Folder (Ctrl+Shift+O); Open Image remains
  in the File menu (Ctrl+O).
- Every code change has a RED-then-GREEN TDD commit pair (3 tasks -> 3 RED +
  3 GREEN commits, i.e. `test(04-09): ...` then `fix(04-09): ...` per task).
- No new features, no new dependencies, no model downloads; only box_item.py /
  canvas.py / main_window.py / test_gui_boxes.py / test_gui_batch.py /
  04-UI-SPEC.md touched; the two §16 amendment notes are in the spec; the full
  suite (439 baseline + ~12 new cases) stays green.
</success_criteria>

<output>
Create `.planning/phases/04-ocr-recognition-text-editing/04-09-SUMMARY.md` when done
</output>
