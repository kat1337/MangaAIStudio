---
phase: 04-ocr-recognition-text-editing
plan: 07
type: execute
wave: 5
depends_on: [02, 06]
files_modified:
  - manga_ai_studio/gui/load_translations_dialog.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/box_item.py
  - tests/test_gui_boxes.py
autonomous: false
requirements: [TEXT-05]
user_setup: []

must_haves:
  truths:
    - "Text → Load Translations… opens a QDialog with a QPlainTextEdit paste area + page picker + Apply; Apply runs translation_parser.parse_translations + apply_translations and fills set_translation on matched boxes (D-17)"
    - "A 'Load from File…' button inside the dialog imports a .txt file via QFileDialog and runs the same parser (D-17 file front-end)"
    - "After Apply, a parser-result report shows 'Applied N translation(s)... M line(s) skipped' (D-15/D-17)"
    - "Text → Auto-Number ▸ RTL (Manga) / LTR (Manhwa) assigns bubble numbers 1..N via reading_order.assign_bubble_numbers with preserve-manual conflict policy (D-15/D-16)"
    - "Per-box manual override (Inspector QSpinBox, Plan 04) sets manual_override=True and sticks across page-level re-auto (D-16)"
    - "Load Translations + Auto-Number both push ONE BOXES snapshot (batch undo entry) and refresh the BoxItem badges/overlays"
  artifacts:
    - "manga_ai_studio/gui/load_translations_dialog.py (NEW) LoadTranslationsDialog(QDialog) with paste + file-import front-ends"
    - "manga_ai_studio/gui/main_window.py with _open_load_translations / _apply_translations / _auto_number_rtl / _auto_number_ltr + Text menu additions (Auto-Number submenu + Load Translations entries)"
    - "tests/test_gui_boxes.py Load Translations + Auto-Number GUI test cases"
  key_links:
    - "_apply_translations calls translation_parser.parse_translations + apply_translations (Plan 02) and reports (applied, unmatched, skipped)"
    - "_auto_number_rtl / _auto_number_ltr call reading_order.assign_bubble_numbers(boxes, rtl=True/False) (Plan 02)"
    - "Both call canvas.boxes_snapshot() (CR-01 pre-state) + emit boxes_modified + refresh BoxItem badges/overlays"
---

<objective>
Complete the TEXT-05 subsystem (D-15/D-16/D-17/D-18): the Load Translations dialog (two front-ends — paste + file-import — sharing the Plan 02 parser core), the parser-result report, and the Auto-Number menu actions (RTL manga / LTR manhwa via the Plan 02 reading-order algorithm with preserve-manual conflict policy). Both Load Translations and Auto-Number push ONE BOXES snapshot (batch undo entry, UI-SPEC §20) and refresh the BoxItem badges/overlays. The parser + reading-order algorithm are pure-Python Plan 02 modules; this plan is pure GUI glue + the menu wiring — no algorithm logic in the GUI.

Purpose: TEXT-05's "add a manual translation as a second text field per box" is delivered three ways: (a) manual typing in the Inspector (Plan 04), (b) paste/import via the Load Translations parser (D-15/D-17), (c) the reading-order auto-number + manual-override subsystem that the parser keys off (D-15/D-16/D-18). The parser reflects the user's actual scanlation workflow (an external translation model outputs the typesetting-tool format; Phase 4 ingests it). Per RESEARCH Don't Hand-Roll, the parser regex is `^\[(\d+)\]:\s*(.*)$`; per RESEARCH Pattern 4, the reading order is XY-Cut column-bucketing. Per UI-SPEC §20, unmatched lines are skipped + reported (ASVS V5 — no per-line modal error).

Output: LoadTranslationsDialog + Auto-Number menu actions + parser-result report + GUI tests, all green. This is the final plan of the phase.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ocr-recognition-text-editing/04-CONTEXT.md
@.planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md
@.planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md
@.planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md
@.planning/phases/04-ocr-recognition-text-editing/04-02-SUMMARY.md
@.planning/phases/04-ocr-recognition-text-editing/04-06-SUMMARY.md

@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/box_item.py
@manga_ai_studio/core/translation_parser.py
@manga_ai_studio/core/reading_order.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LoadTranslationsDialog (paste + file-import front-ends, shared parser core)</name>
  <files>manga_ai_studio/gui/load_translations_dialog.py, manga_ai_studio/gui/main_window.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_confirm_replace_boxes ~1711-1737 — THE QDialog template; export_page ~258 — the QFileDialog.getSaveFileName pattern; the existing _on_detection_error ~1749-1764 — the error UX pattern for file-read errors)
    - manga_ai_studio/core/translation_parser.py (Plan 02 — parse_translations(text) -> (matches, skipped); apply_translations(matches, boxes, page_no) -> (applied, unmatched))
    - manga_ai_studio/core/box_model.py (Plan 01 — PageBox.bubble_no, set_translation, manual_override)
    - .planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md (Don't Hand-Roll line 404 — parser regex; Code Examples lines 570-614 — exact parser signatures; Security V12 lines 774 — file-read open(...encoding="utf-8") + OSError/UnicodeDecodeError handling; Open Question 4 — one batch BOXES entry)
    - .planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md (load_translations_dialog.py assignment lines 185-207 — QDialog + QFileDialog + file-read + apply seam patterns)
    - .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md (§20 — paste dialog: QComboBox page selector + QPlainTextEdit paste area + [Cancel] [Apply]; "Load from File…" button inside the dialog; placeholder copy; parser-result report copy; §Copywriting — error file-import copy)
    - .planning/phases/04-ocr-recognition-text-editing/04-CONTEXT.md (D-17 two front-ends; D-15 parser format; D-18 page-global numbers)
  </read_first>
  <behavior>
    - LoadTranslationsDialog is a QDialog with: a QComboBox page selector (default "Current page"; lists all open pages by name), a QPlainTextEdit paste area (mono font; placeholder the UI-SPEC copy), a "Load from File…" button, [Cancel] [Apply] buttons
    - Apply: reads the paste text, runs translation_parser.parse_translations → (matches, skipped); the MainWindow applies matches to the selected page's boxes via apply_translations → (applied, unmatched); shows the parser-result report ("Applied N translation(s) to page X. M line(s) did not match a bubble number and were skipped."); closes the dialog
    - "Load from File…": opens QFileDialog.getOpenFileName(filter "*.txt"); reads the file via open(path, "r", encoding="utf-8"); on OSError/UnicodeDecodeError shows "Couldn't read '{filename}'." (Phase 1 file-unreadable copy pattern); on success loads the file content into the paste area (or applies directly — executor picks; loading into the paste area lets the user review before Apply)
    - On no matches: shows "No lines matched any bubble number on page X..." (UI-SPEC §Copywriting)
    - The dialog itself does NOT mutate boxes — it returns the parsed (matches, page_no) and the MainWindow does the apply + boxes_modified emit (RESEARCH §Pitfall 3 separation; UI-SPEC §20 — the MainWindow applies through apply_translations + pushes ONE batch BOXES snapshot)
    - Multi-page files (with "Page N:" markers per D-17) — the parser merges across markers; the file front-end applies to ALL open pages (executor confirms against the parser's PAGE_MARKER_RE handling from Plan 02)
  </behavior>
  <action>
    Create manga_ai_studio/gui/load_translations_dialog.py. Define `class LoadTranslationsDialog(QDialog):` with:

    - `def __init__(self, parent=None, page_names: list[str] = None, current_page_index: int = 0):` — build the UI: a QVBoxLayout; a QFormLayout / QHBoxLayout with a QLabel "Page:" + `self.page_combo = QComboBox()` populated with page_names (default the current page selected); `self.paste_edit = QPlainTextEdit()` with mono font (Consolas/Cascadia Mono per UI-SPEC) + the placeholder text from UI-SPEC §20 ("Paste your translation list here, one line per box:\n[1]: first translated line\n[2]: second translated line\n[SFX -3]: *sound effect*"); a QHBoxLayout with a `self.load_file_btn = QPushButton("Load from File…")` (connect clicked → self._on_load_file) and a stretch; a QHBoxLayout with `self.cancel_btn = QPushButton("Cancel")` (connect → self.reject) and `self.apply_btn = QPushButton("Apply")` (connect → self._on_apply; setDefault(True)); setWindowTitle("Load Translations"); apply the dark QSS (mirror tools_panel/InspectorPanel tokens). Set objectName("load_translations_dialog").

    - `def _on_load_file(self) -> None:` — `path, _ = QFileDialog.getOpenFileName(self, "Load Translations", "", "Text files (*.txt)")`; if not path: return; try: `with open(path, "r", encoding="utf-8") as f: content = f.read()`; self.paste_edit.setPlainText(content); except (OSError, UnicodeDecodeError) as e: `logger.error(f"Couldn't read translation file: {e}")`; QMessageBox.critical(self, "Couldn't read '{filename}'.", "The file may be corrupt or in an unsupported format.") (UI-SPEC §Copywriting file-import error copy).

    - `def _on_apply(self) -> None:` — read text = self.paste_edit.toPlainText(); page_index = self.page_combo.currentIndex(); store them as self.result_text / self.result_page_index and call self.accept() (the dialog returns Accepted; MainWindow reads result_text + result_page_index and runs the parser + apply). Do NOT parse or mutate boxes inside the dialog — keep the dialog pure (RESEARCH §Pitfall 3 separation). The dialog's job is to collect (text, page_index); the MainWindow does the parse + apply + report.

    - Accessors: `def get_text(self) -> str: return self.result_text`; `def get_page_index(self) -> int: return self.result_page_index`.

    In manga_ai_studio/gui/main_window.py MainWindow, add:

    - `def _open_load_translations(self) -> None:` — gather page_names from self.file_table (the sidebar model); current_index = self.canvas current page index (mirror how _last_page_index is read — Phase 2 seam); construct `dlg = LoadTranslationsDialog(self, page_names, current_index)`; `if dlg.exec() == QDialog.DialogCode.Accepted:` text = dlg.get_text(); page_index = dlg.get_page_index(); self._apply_translations(text, page_index)

    - `def _apply_translations(self, text: str, page_index: int) -> None:` — `from manga_ai_studio.core.translation_parser import parse_translations, apply_translations`; `matches, skipped = parse_translations(text)`; if page_index is the current page: boxes = [it.pagebox for it in self.canvas._box_items]; capture pre-state via canvas.boxes_snapshot() (CR-01); `applied, unmatched = apply_translations(matches, boxes, page_no=page_index)`; emit boxes_modified (ONE batch entry per UI-SPEC §20 / RESEARCH Open Q 4); for each matched box: refresh its BoxItem (refresh_text_overlay + refresh_badge via the corresponding BoxItem — find by identity); show the parser-result report via QMessageBox.information: title "Load Translations", body the success or no-matches copy from UI-SPEC §Copywriting ("Applied {applied} translation(s) to page {n}. {unmatched + skipped} line(s) did not match..." — combine unmatched + skipped into one "skipped" count for the user-facing copy, or report them separately — executor picks; the UI-SPEC copy uses one "skipped" total). For non-current pages (multi-page file): apply to the ImageFile.boxes at that index without refreshing the canvas (the canvas refresh happens when the user navigates there). Wrap the parse in a try/except (ASVS V7 — parser should never raise per Plan 02, but belt-and-suspenders): on unexpected exception, logger.error + QMessageBox.critical "Couldn't apply translations — see the log."

    Wire the menu entry in _build_text_menu (Plan 06 created this): add a separator + `self.action_load_translations = QAction("Load Translations…", self); triggered.connect(self._open_load_translations); tooltip the UI-SPEC copy` — text_menu.addAction(self.action_load_translations). Enable iff a page is open and not self._op_running (refresh in _refresh_action_states).
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_boxes.py -x -q -k "load_translations or parser_apply"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f manga_ai_studio/gui/load_translations_dialog.py` exits 0
    - `grep -c "class LoadTranslationsDialog" manga_ai_studio/gui/load_translations_dialog.py` is 1
    - `grep -c "QPlainTextEdit\|QFileDialog\|QComboBox" manga_ai_studio/gui/load_translations_dialog.py` is >= 3 (the three contracted widgets)
    - `grep -c "def _open_load_translations\|def _apply_translations" manga_ai_studio/gui/main_window.py` is 2
    - `grep -c "from manga_ai_studio.core.translation_parser import" manga_ai_studio/gui/main_window.py` is >= 1
    - `grep -c "action_load_translations" manga_ai_studio/gui/main_window.py` is >= 2
    - `python -m pytest tests/test_gui_boxes.py -x -q` exits 0
    - A test asserting paste "[1]: hello\n[2]: world" + Apply fills set_translation on bubble 1 and 2 boxes passes
    - A test asserting the parser-result report shows on Apply passes
    - A test asserting unmatched numbers are reported (no crash) passes
    - A test asserting ONE boxes_modified emit for the batch apply passes (UI-SPEC §20)
  </acceptance_criteria>
  <done>LoadTranslationsDialog (paste + file-import) returns (text, page_index); MainWindow applies via parse_translations + apply_translations, pushes ONE batch BOXES snapshot, refreshes overlays/badges, shows the parser-result report; file-read errors handled; all GUI box tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-Number menu (RTL Manga / LTR Manhwa) with preserve-manual conflict policy</name>
  <files>manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/canvas.py, manga_ai_studio/gui/box_item.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_build_text_menu from Plan 06; _refresh_action_states; the status-bar transient-message pattern — "Undo: {op}" / "Numbered N boxes..." from UI-SPEC §Copywriting)
    - manga_ai_studio/gui/canvas.py (boxes_snapshot ~1276; _box_items; the boxes_modified signal)
    - manga_ai_studio/gui/box_item.py (refresh_badge from Plan 04 — the auto-number result triggers a badge refresh on every box)
    - manga_ai_studio/core/reading_order.py (Plan 02 — assign_bubble_numbers(boxes, rtl) returns the count auto-numbered; preserves manual_override boxes)
    - manga_ai_studio/core/box_model.py (Plan 01 — PageBox.bubble_no, manual_override)
    - .planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md (Open Question 3 — preserve-manual conflict policy; Pattern 4 — XY-Cut)
    - .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md (§17 reading order RTL manga / LTR manhwa; manual override amber border; §Surface 1 Auto-Number submenu RTL/LTR; §Copywriting "Numbered N boxes (RTL/TB)." transient)
    - .planning/phases/04-ocr-recognition-text-editing/04-CONTEXT.md (D-15/D-16 auto-number + manual override; D-18 page-global)
  </read_first>
  <behavior>
    - Text → Auto-Number ▸ RTL (Manga) calls reading_order.assign_bubble_numbers([it.pagebox for it in canvas._box_items], rtl=True); LTR (Manhwa) calls with rtl=False
    - After assign_bubble_numbers, every BoxItem's badge refreshes (refresh_badge) to show the new number
    - Manual-override boxes (manual_override=True, set via the Inspector QSpinBox in Plan 04) KEEP their bubble_no across re-auto (preserve-manual conflict policy); gaps are left in the auto sequence
    - The action pushes ONE BOXES snapshot (batch entry) via canvas.boxes_snapshot() + boxes_modified emit
    - Status-bar transient "Numbered N boxes (RTL/TB)." for ~3s (UI-SPEC §Copywriting)
    - Empty page (no boxes) is a no-op
  </behavior>
  <action>
    In manga_ai_studio/gui/main_window.py MainWindow, add:

    - `def _auto_number(self, rtl: bool) -> None:` — `from manga_ai_studio.core.reading_order import assign_bubble_numbers`; boxes = [it.pagebox for it in self.canvas._box_items]; if not boxes: return; before = self.canvas.boxes_snapshot() (CR-01 pre-state); count = assign_bubble_numbers(boxes, rtl=rtl); for it in self.canvas._box_items: it.refresh_badge() (refresh every badge to show new numbers + manual-override amber borders); self.canvas.boxes_modified.emit(before) (ONE batch BOXES entry — the auto-number is one logical op); status-bar transient: self.status_bar_left.setText(f"Numbered {count} boxes ({'RTL/TB' if rtl else 'LTR/TB'}).") — schedule a revert to the box-count line after ~3s (mirror how other transients revert — a QTimer.singleShot(3000, self._refresh_status_bar) or the existing transient mechanism).

    - `def _auto_number_rtl(self) -> None: self._auto_number(rtl=True)`
    - `def _auto_number_ltr(self) -> None: self._auto_number(rtl=False)`

    Wire the submenu in _build_text_menu (Plan 06 created this): add a separator + `auto_number_menu = text_menu.addMenu("Auto-Number")`; `self.action_auto_number_rtl = QAction("RTL (Manga)", self); triggered.connect(self._auto_number_rtl); tooltip the UI-SPEC RTL copy`; `self.action_auto_number_ltr = QAction("LTR (Manhwa)", self); triggered.connect(self._auto_number_ltr); tooltip the UI-SPEC LTR copy`; auto_number_menu.addAction(self.action_auto_number_rtl); auto_number_menu.addAction(self.action_auto_number_ltr). Enable both iff a page is open with >= 1 box and not self._op_running (refresh in _refresh_action_states — mirror action_ocr_all gating from Plan 06).

    Confirm the manual_override amber border (Plan 04 refresh_badge already handles it — the badge reads pagebox.manual_override and applies the #f5a623 2px border). No extra work here; just verify the Inspector QSpinBox (Plan 04) sets manual_override=True on user edit so re-auto preserves it.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_boxes.py -x -q -k "auto_number or reading_order_apply or preserve_manual"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _auto_number\b\|def _auto_number_rtl\|def _auto_number_ltr" manga_ai_studio/gui/main_window.py` is 3
    - `grep -c "from manga_ai_studio.core.reading_order import" manga_ai_studio/gui/main_window.py` is >= 1
    - `grep -c "action_auto_number_rtl\|action_auto_number_ltr" manga_ai_studio/gui/main_window.py` is >= 2
    - `grep -c 'addMenu("Auto-Number")' manga_ai_studio/gui/main_window.py` is 1
    - `python -m pytest tests/test_gui_boxes.py -x -q` exits 0
    - A test asserting Auto-Number RTL assigns 1..N right-to-left top-to-bottom passes
    - A test asserting Auto-Number LTR assigns 1..N left-to-right passes
    - A test asserting a manual_override box keeps its bubble_no across re-auto passes (preserve-manual conflict policy)
    - A test asserting ONE boxes_modified emit for the batch auto-number passes
  </acceptance_criteria>
  <done>Auto-Number RTL (Manga) / LTR (Manhwa) menu actions call assign_bubble_numbers; badges refresh; manual-override preserved; ONE batch BOXES snapshot; status-bar transient; all GUI box tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Load Translations + Auto-Number on real manga (D-15/D-16/D-17)</name>
  <files>.planning/phases/04-ocr-recognition-text-editing/04-07-SUMMARY.md</files>
  <action>
    Plan 04-07 completes the TEXT-05 translation subsystem: the Load Translations dialog (paste + file-import front-ends) + parser-result report (D-15/D-17) and the Auto-Number menu (RTL Manga / LTR Manhwa) with preserve-manual conflict policy (D-15/D-16). Boxes must have bubble numbers (run Auto-Number first) before Load Translations can match. The automated pytest-qt suite covers the parser mechanics + reading-order algorithm + GUI wiring. This checkpoint confirms the reading-order ordering + parser matching + manual-override preservation feel right on a real multi-bubble manga page.
  </action>
  <verify>
    <human-check>
      Launch the app (`python -m manga_ai_studio`) and perform these checks on a real manga page:
      1. Open a manga page, detect boxes (D), OCR All (Ctrl+R) so boxes have text.
      2. Text → Auto-Number ▸ RTL (Manga). Confirm each box gets a black badge with 1, 2, 3... at the TL corner, ordered right-to-left top-to-bottom (manga reading order).
      3. Undo (Ctrl+Z) — confirm the numbers clear (one batch undo entry). Redo — confirm they return.
      4. In the Inspector, manually change a box's Bubble # to a different number. Confirm its badge gets an amber border (manual override). Run Auto-Number RTL again — confirm that box KEEPS your manual number (preserve-manual, D-16) while the others re-auto.
      5. Text → Auto-Number ▸ LTR (Manhwa) on a fresh page — confirm left-to-right top-to-bottom ordering.
      6. Text → Load Translations…. In the paste area, type:
         [1]: first translation
         [2]: second translation
         [99]: no match for this
         [SFX -3]: *sound effect*
         Click Apply. Confirm boxes 1 and 2 get translations (canvas overlay switches to the translation per the focus rule), the parser-result report shows "Applied 2 translation(s)... 2 line(s) skipped" (the unmatched 99 + the SFX line).
      7. Undo — confirm the translations revert in one batch entry. Redo — confirm they return.
      8. Click "Load from File…" inside the dialog; pick a .txt file with a translation list. Confirm it loads into the paste area; Apply; confirm matches fill.
      9. Try loading a corrupt/non-UTF-8 file — confirm the "Couldn't read '{filename}'." error dialog appears (no crash).
    </human-check>
    <automated>python -m pytest tests/test_gui_boxes.py -x -q</automated>
  </verify>
  <done>All nine manual checks pass (RTL/LTR ordering, batch undo, manual override preservation, parser matching + report, file import, error UX) and the test_gui_boxes.py suite is green.</done>
  <resume-signal>Type "approved" or describe issues (e.g. numbers misordered, manual override not preserved, parser matching wrong boxes, report counts wrong)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pasted text / imported .txt → parser | untrusted user text crosses here (ASVS V5 — strict regex, skip+report, never crash); file-import reads user-selected files only (no programmatic path, no traversal risk) |
| Parser matches → PageBox.set_translation | the MT-seam setter (D-13); a future MTModel adapter calls the same setter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-15 | Denial of Service | oversized paste / weird unicode into parse_translations | medium | mitigate | Parser is line-by-line with strict anchored regex (Plan 02); malformed lines skipped+counted, never raise (ASVS V5/V7). Plan 02 test parse_translations_never_raises. |
| T-4-16 | Tampering | file-import path traversal / arbitrary file read | low | accept | File selected via QFileDialog.getOpenFileName (user-chosen, not programmatic); no path traversal surface. Read with open(..., "r", encoding="utf-8"); OSError/UnicodeDecodeError caught. |
| T-4-17 | Tampering | Auto-Number silently overwriting manual override numbers | medium | mitigate | assign_bubble_numbers (Plan 02) skips manual_override boxes (preserve-manual conflict policy). Test preserve_manual passes. |
</threat_model>

<verification>
- `python -m pytest tests/test_gui_boxes.py -x -q` green
- `python -m pytest tests/ -q` full suite green
- Human-verify checkpoint: Auto-Number RTL/LTR, manual override preservation, Load Translations paste + file + report, error UX, batch undo
</verification>

<success_criteria>
- LoadTranslationsDialog (paste + file-import) returns (text, page_index); MainWindow applies via Plan 02 parser
- Parser-result report shows applied/unmatched/skipped counts
- Auto-Number RTL (Manga) / LTR (Manhwa) via Plan 02 reading_order with preserve-manual
- Both push ONE batch BOXES snapshot (UI-SPEC §20); badges/overlays refresh
- Text menu complete (Run OCR / OCR All / Auto-Number ▸ / Load Translations…)
- Human checkpoint approved (ordering, override preservation, parser matching, error UX)
</success_criteria>

<output>
Create `.planning/phases/04-ocr-recognition-text-editing/04-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan creates (downstream plan-review-convergence: exclude these from drift verification):
- `manga_ai_studio.gui.load_translations_dialog.LoadTranslationsDialog(QDialog)` (paste + file-import front-ends; returns text + page_index)
- `manga_ai_studio.gui.main_window.MainWindow._open_load_translations()`
- `manga_ai_studio.gui.main_window.MainWindow._apply_translations(text, page_index)` (calls Plan 02 parse_translations + apply_translations)
- `manga_ai_studio.gui.main_window.MainWindow._auto_number(rtl)` / `_auto_number_rtl()` / `_auto_number_ltr()` (call Plan 02 assign_bubble_numbers)
- `manga_ai_studio.gui.main_window.MainWindow.action_load_translations` / `action_auto_number_rtl` / `action_auto_number_ltr` (QActions)
- `manga_ai_studio.gui.main_window.MainWindow` "Auto-Number" submenu in the Text menu
</content>
</invoke>
