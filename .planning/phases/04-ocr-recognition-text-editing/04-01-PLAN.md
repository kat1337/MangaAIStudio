---
phase: 04-ocr-recognition-text-editing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/box_model.py
  - manga_ai_studio/core/history_manager.py
  - manga_ai_studio/gui/canvas.py
  - tests/test_core/test_box_model.py
  - tests/test_core/test_history_boxes.py
  - tests/test_box_snapshot_fields.py
  - tests/test_payload_aliasing.py
autonomous: true
requirements: [TEXT-04, TEXT-05]
user_setup: []

must_haves:
  truths:
    - "PageBox carries edited/bubble_no/manual_override fields that survive undo + page-switch"
    - "set_recognized_text(str) writes payload.text and resets edited=False; set_translation(str) writes payload.translation (the D-13 MT seam)"
    - "Undoing a text edit restores the PRE-edit text, not the current text (no payload aliasing)"
    - "boxes_snapshot() round-trips edited/bubble_no/manual_override + payload text/translation"
  artifacts:
    - "manga_ai_studio/core/box_model.py with PageBox.edited / .bubble_no / .manual_override + set_recognized_text / set_translation / has_recognized_text / copy"
    - "manga_ai_studio/gui/canvas.py boxes_snapshot() passes the new fields"
    - "tests/test_box_snapshot_fields.py and tests/test_payload_aliasing.py (Pitfall 1 + 8 regression)"
  key_links:
    - "PageBox.copy() detaches payload so _materialize_snapshot (history_manager.py:274) returns a fresh instance"
    - "boxes_snapshot() (canvas.py:1276) reads the live PageBox fields, not just box/origin/payload"
---

<objective>
Lay the Phase 4 core-model foundation BEFORE any UI/consumer reads the new fields: extend PageBox with the edited/bubble_no/manual_override fields + set_recognized_text/set_translation setters + a .copy() that detaches the payload, fix canvas.boxes_snapshot() to round-trip the new fields (Pitfall 1), and land the Pitfall 1 + Pitfall 8 regression tests in Wave 0 so the rest of the phase can rely on the seam. Pure-Python core + surgical canvas edit + headless tests; no Qt widget work, no OCR model.

Purpose: Per VALIDATION.md Wave 0 + RESEARCH Pitfalls 1 & 8, the two load-bearing persistence gaps (boxes_snapshot dropping new fields; payload aliasing across undo snapshots) MUST get regression tests before feature work layers on top. Every later Phase 4 plan (overlay reads text, inline editor writes text, parser writes translation, OCR writes recognized text, undo restores text) depends on this seam being correct.

Output: Extended PageBox model with the Phase 4 fields + setters + copy(); fixed boxes_snapshot(); 4 regression test files (2 new, 2 extended) all green.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ocr-recognition-text-editing/04-CONTEXT.md
@.planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md
@.planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md
@.planning/phases/04-ocr-recognition-text-editing/04-VALIDATION.md

@manga_ai_studio/core/box_model.py
@manga_ai_studio/core/history_manager.py
@manga_ai_studio/gui/canvas.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend PageBox with Phase 4 fields + setters + copy() (TDD)</name>
  <files>manga_ai_studio/core/box_model.py, tests/test_core/test_box_model.py</files>
  <read_first>
    - manga_ai_studio/core/box_model.py (current PageBox — the file being modified; note payload: Optional[object] = None and the @dataclass)
    - .planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md (Pitfall 1 lines 424-440, Pitfall 8 lines 484-488, Open Question 2 + 6 — edited flag on PageBox, payload.text stored as str)
    - .planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md (box_model.py assignment lines 316-346 — exact field shapes + setter bodies)
    - panelcleaner/comic_text_detector/utils/textblock.py (TextBlock.__init__ signature + .text/.translation/.vertical/.language slots — the payload type; confirm the xyxy-first constructor)
    - panelcleaner/structures.py (Box accessors — confirm the xyxy getter for TextBlock construction; do NOT mutate the vendored Box)
    - tests/test_core/test_box_model.py (existing test shape — @pytest.mark.unit, lazy import inside the test fn)
  </read_first>
  <behavior>
    - PageBox has fields edited: bool = False, bubble_no: Optional[int] = None, manual_override: bool = False (peer fields, NOT derived)
    - pagebox.set_recognized_text("こんにちは") sets payload.text == "こんにちは" (stored as str per RESEARCH Open Q 6) and edited == False
    - pagebox.set_translation("hello") sets payload.translation == "hello"
    - For a user box with payload=None, set_recognized_text constructs a TextBlock (xyxy from pagebox.box) before writing .text
    - pagebox.has_recognized_text() returns True after set_recognized_text, False for payload=None or empty .text
    - pagebox.copy() returns a NEW PageBox whose payload is a different object (copy.copy(self.payload)) — mutating the copy's payload.text does NOT change the original
    - copy() preserves box/origin/edited/bubble_no/manual_override
    - The existing Phase 3 default-constructible PageBox(box=..., origin=..., payload=...) still works (new fields default)
  </behavior>
  <action>
    RED first: extend tests/test_core/test_box_model.py with the behavior cases above (test_set_recognized_text_resets_edited, test_set_translation_writes_payload, test_set_recognized_text_creates_textblock_for_user_box, test_has_recognized_text, test_pagebox_copy_detaches_payload, test_pagebox_copy_preserves_fields, test_pagebox_phase4_field_defaults). Run pytest to confirm RED.

    GREEN: add to the PageBox @dataclass in manga_ai_studio/core/box_model.py three peer fields after std_dev: `edited: bool = False`, `bubble_no: Optional[int] = None`, `manual_override: bool = False`. Add methods:

    - `set_recognized_text(self, text: str) -> None`: if self.payload is None, import TextBlock lazily inside the method (`from panelcleaner.comic_text_detector.utils.textblock import TextBlock`) and construct one with xyxy read from self.box via its xyxy accessor (confirm against panelcleaner/structures.py Box — use the x1y1x2y2 form); assign self.payload = TextBlock([x1,y1,x2,y2]). Then set self.payload.text = text (str, per RESEARCH Open Q 6) and self.edited = False.
    - `set_translation(self, text: str) -> None`: same payload-None guard + TextBlock construction; set self.payload.translation = text. (Per D-13 — this is the MT seam a future MTModel adapter calls.)
    - `has_recognized_text(self) -> bool`: return False if payload is None; else read payload.text — treat empty string / empty list as no text (TextBlock.text may be str or list per textblock.py; handle both: `t = self.payload.text; if isinstance(t, list): t = "".join(t).strip(); return bool(t)`).
    - `copy(self) -> "PageBox"`: use `dataclasses.replace(self, payload=copy.copy(self.payload))` (import copy at module top). copy.copy is a SHALLOW TextBlock copy — sufficient because Phase 4 only mutates .text/.translation (top-level attributes), per RESEARCH Assumption A3. The vendored Box is @frozen so sharing it by reference is safe (D-10).

    Do NOT subclass or mutate the vendored Box or TextBlock (D-14 anti-pattern). The new fields layer on PageBox via composition. Keep the module pure-Python (no Qt imports) so it stays headless-testable.
  </action>
  <verify>
    <automated>python -m pytest tests/test_core/test_box_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from manga_ai_studio.core.box_model import PageBox; import dataclasses; assert {f.name for f in dataclasses.fields(PageBox)} >= {'edited','bubble_no','manual_override'}"` exits 0
    - `grep -c "def set_recognized_text" manga_ai_studio/core/box_model.py` is 1
    - `grep -c "def set_translation" manga_ai_studio/core/box_model.py` is 1
    - `grep -c "def copy" manga_ai_studio/core/box_model.py` is 1
    - `python -m pytest tests/test_core/test_box_model.py -x -q` exits 0 (all new + existing tests green)
    - test_pagebox_copy_detaches_payload passes (mutating copy.payload.text does not change original.payload.text)
  </acceptance_criteria>
  <done>PageBox has edited/bubble_no/manual_override fields + set_recognized_text/set_translation/has_recognized_text/copy methods; all box_model unit tests (new + Phase 3 existing) green; copy() detaches payload.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Fix canvas.boxes_snapshot() to round-trip Phase 4 fields (Pitfall 1) + verify _materialize_snapshot detaches via PageBox.copy()</name>
  <files>manga_ai_studio/gui/canvas.py, tests/test_box_snapshot_fields.py, tests/test_payload_aliasing.py, tests/test_core/test_history_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/canvas.py (boxes_snapshot at lines ~1276-1286 — the function being modified; note the current PageBox(box=fresh_box, origin=..., payload=...) construction that DROPS the new fields)
    - manga_ai_studio/core/history_manager.py (_materialize_snapshot lines 252-275, push_boxes_state 277-302, pop_boxes_undo 304-324 — confirm the `item.copy() if hasattr(item, "copy") else item` branch now detaches because PageBox.copy exists from Task 1)
    - .planning/phases/04-ocr-recognition-text-editing/04-RESEARCH.md (Pitfall 1 lines 424-440 — the exact boxes_snapshot fix; Pitfall 8 lines 484-488 — payload aliasing)
    - .planning/phases/04-ocr-recognition-text-editing/04-PATTERNS.md (canvas.py assignment Hook 2 lines 392-408 — the exact replacement block)
    - tests/test_core/test_history_boxes.py (existing Phase 3 test shape — test_boxes_snapshot_is_detached is the analog regression guard)
    - tests/test_box_persistence.py (the page-switch persistence test file — NOT modified here but the pattern informs the new tests)
  </read_first>
  <behavior>
    - boxes_snapshot() returns PageBox instances whose edited/bubble_no/manual_override match the live BoxItem.pagebox values (Pitfall 1)
    - boxes_snapshot() preserves payload.text and payload.translation (they ride on payload)
    - Pushing a boxes snapshot then mutating a pagebox's payload.text in place does NOT change the snapshot's payload.text (Pitfall 8 — PageBox.copy detaches payload)
    - Undo of a text-edit restores the PRE-edit text (regression for the aliasing vector)
  </behavior>
  <action>
    RED first: create tests/test_box_snapshot_fields.py with a pytest-qt test that builds an EditorCanvas (or uses the existing test_gui_boxes fixture pattern), adds a BoxItem whose pagebox has edited=True, bubble_no=3, manual_override=True, payload.text="x" / payload.translation="y", calls canvas.boxes_snapshot(), and asserts each snapshot pagebox carries those exact values. Create tests/test_payload_aliasing.py with a headless HistoryManager test: push_boxes_state([pagebox_with_text("before")]); then mutate the original pagebox.payload.text = "after" in place; pop_boxes_undo — the restored pagebox.payload.text must be "before" (PageBox.copy detaches). Extend tests/test_core/test_history_boxes.py with test_pop_boxes_undo_restores_text_edit_state asserting the same at the history layer. Run pytest to confirm RED.

    GREEN: in manga_ai_studio/gui/canvas.py boxes_snapshot() (the loop body at ~lines 1279-1284), extend the PageBox construction to pass the new fields read off item.pagebox:

    `PageBox(box=fresh_box, origin=item.pagebox.origin, payload=item.pagebox.payload, edited=item.pagebox.edited, bubble_no=item.pagebox.bubble_no, manual_override=item.pagebox.manual_override)`

    Do NOT change anything else in boxes_snapshot (the fresh_box = item.current_box() read stays; the function signature stays). The history_manager.py _materialize_snapshot needs NO edit — its `item.copy() if hasattr(item, "copy") else item` branch (line 274) now detaches correctly because PageBox.copy() exists from Task 1. Verify this by re-reading _materialize_snapshot after Task 1 lands; if for any reason PageBox is not yet recognized as having .copy (it will be), do NOT add a special-case — the dataclass method satisfies hasattr.
  </action>
  <verify>
    <automated>python -m pytest tests/test_box_snapshot_fields.py tests/test_payload_aliasing.py tests/test_core/test_history_boxes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A6 "def boxes_snapshot" manga_ai_studio/gui/canvas.py | grep -c "edited="` is 1
    - `grep -A6 "def boxes_snapshot" manga_ai_studio/gui/canvas.py | grep -c "bubble_no="` is 1
    - `grep -A6 "def boxes_snapshot" manga_ai_studio/gui/canvas.py | grep -c "manual_override="` is 1
    - `grep -c "def copy" manga_ai_studio/core/box_model.py` is 1 (Task 1 dependency confirmed present)
    - `python -m pytest tests/test_box_snapshot_fields.py tests/test_payload_aliasing.py tests/test_core/test_history_boxes.py -x -q` exits 0
    - test_pop_boxes_undo_restores_text_edit_state passes (undo restores "before", not "after")
    - history_manager.py is UNCHANGED (grep diff confirms no edit to _materialize_snapshot — the existing branch handles PageBox.copy)
  </acceptance_criteria>
  <done>boxes_snapshot() round-trips edited/bubble_no/manual_override; Pitfall 1 + 8 regression tests green; _materialize_snapshot detaches PageBox via the new .copy() with no history_manager edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload (TextBlock) mutation boundary | text/translation writes cross from GUI/worker into the core model; the .copy() detachment is the trust boundary that prevents undo aliasing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Tampering (data integrity) | PageBox payload aliasing across undo snapshots | high | mitigate | PageBox.copy() shallow-copies payload so undo restores snapshot-time text, not live text (Pitfall 8). Regression test test_pop_boxes_undo_restores_text_edit_state. |
| T-4-02 | Information Disclosure | boxes_snapshot dropping edited flag → user edits silently lost on undo/page-switch | high | mitigate | boxes_snapshot() now passes edited/bubble_no/manual_override (Pitfall 1). Regression test test_box_snapshot_fields. |
</threat_model>

<verification>
- `python -m pytest tests/test_core/test_box_model.py tests/test_box_snapshot_fields.py tests/test_payload_aliasing.py tests/test_core/test_history_boxes.py -x -q` all green
- `python -m pytest tests/ -q` (full suite) still green — no Phase 1-3 regression
- PageBox.copy() detaches payload (test_pagebox_copy_detaches_payload)
- boxes_snapshot round-trips all Phase 4 fields (test_box_snapshot_fields)
</verification>

<success_criteria>
- PageBox model extended with edited/bubble_no/manual_override + set_recognized_text/set_translation/has_recognized_text/copy — all unit-tested
- Pitfall 1 (boxes_snapshot field gap) closed with regression test
- Pitfall 8 (payload aliasing) closed with regression test
- No Phase 1-3 test regression
</success_criteria>

<output>
Create `.planning/phases/04-ocr-recognition-text-editing/04-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan creates/extends (downstream plan-review-convergence: exclude these from drift verification):
- `PageBox.edited` (bool field, D-04 re-OCR gate)
- `PageBox.bubble_no` (Optional[int] field, D-15/D-16 reading-order number)
- `PageBox.manual_override` (bool field, D-16 preserve-manual conflict policy)
- `PageBox.set_recognized_text(text: str)` (writes payload.text as str, sets edited=False)
- `PageBox.set_translation(text: str)` (D-13 MT seam — writes payload.translation)
- `PageBox.has_recognized_text() -> bool`
- `PageBox.copy() -> PageBox` (dataclasses.replace + copy.copy(payload) — Pitfall 8 fix)
</content>
</invoke>
