---
phase: 04-ocr-recognition-text-editing
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - manga_ai_studio/gui/box_item.py
  - manga_ai_studio/gui/inspector_panel.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_boxes.py
  - .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md
autonomous: true
requirements: [TEXT-05]
tags: [gui, box-item, badge, inspector, bubble-number, gap-closure, uat-test-4, uat-test-6, tdd, pytest-qt]
must_haves:
  truths:
    - "The bubble badge FITS its number: after refresh_badge the badge rect equals the digit glyph bounding rect plus padding (digit line box measured with document margin 0 — 16x19 px per digit at the 12pt semibold badge font on the exec platform; badge 24x23 / 40x23 / 56x23 / 72x23 viewport px for 1/2/3/4 digits), the digit is re-centered ((badge_w - digit_w)/2, (badge_h - digit_h)/2), and the digit's local rect (pos + boundingRect) is FULLY contained in the badge rect — no top/bottom/right clipping — at any zoom (ItemIgnoresTransformations keeps both constant viewport px)"
    - "The badge stays TL-outside with the same 2px offset, computed from the ACTUAL badge size: scenePos = box sceneBoundingRect top-left minus (badge_w + 2, badge_h + 2) — a multi-digit badge sits the same 2px away as a single-digit one; the edge-flip (badge would clip off the page TL edge) still moves it inside-top-left (inset 2px), using the actual size for the decision"
    - "Bubble # 1 is manually assignable: on an unset box (bubble_no=None, spinbox showing the '—' special value) entering 1 and committing fires bubble_no_changed(1); end-to-end this sets pagebox.bubble_no=1 AND manual_override=True (D-16 amber badge) — the UAT test-6 truth"
    - "Clearing works symmetrically: committing 0 on a numbered box sets bubble_no=None and manual_override=False (unset is NOT an override); the spinbox displays '—' (setSpecialValueText) for the unset state, never a phantom '1'"
    - "WR-01 is preserved: unchanged focus cycles stay silent no-ops — 0 vs 0 on an unset box and n vs n on a numbered box emit nothing (no spurious edited/manual-override/BOXES entry); only REAL changes commit"
    - "The full suite stays green: 451 baseline + 9 new test cases = 460 passed, 0 failed, with only the pre-existing flake (test_moved_box_via_real_events_persists_round_trip) deselected — the single intentional existing-test change is test_inspector_bubble_spin_range_is_bounded (minimum 1 -> 0, the sentinel) plus non-functional comment updates in two WR-01 tests"
  artifacts:
    - "box_item.py — _BADGE_PAD_W=4.0/_BADGE_PAD_H=2.0 module constants (the fixed 20x14 badge-size constants are DELETED); digit document margin 0 set in the constructor (measurement basis = glyph line box); refresh_badge measures the digit after setPlainText, resizes the badge rect (setRect 0,0,w,h), re-centers the digit, and uses the ACTUAL size for TL-outside placement + edge-flip"
    - "inspector_panel.py — bubble_spin.setRange(0, 9999) + setSpecialValueText('\\u2014'); load_box maps bubble_no=None -> 0; clear() uses 0; _loaded_bubble sentinel is 0 (init + clear); WR-01 guard and docstrings updated to the 0-sentinel contract"
    - "main_window.py — _on_inspector_bubble_committed maps 0 -> bubble_no=None + manual_override=False, 1..9999 -> assign + manual_override=True (D-16 unchanged)"
    - "tests/test_gui_boxes.py — two 04-10 sections: badge digit-fit tests (sizing/containment, actual-size TL-outside placement, edge-flip) and bubble-sentinel tests (special value, bubble-1 assignment panel-level + end-to-end); test_inspector_bubble_spin_range_is_bounded updated to minimum()==0"
    - "04-UI-SPEC.md — amendment notes appended to the §Spacing badge-size row (superseded), §17 Widget bullet (digit-sized), §Color bullet 3 (size correction), and the §18 Bubble # row (0-sentinel deviation documented)"
  key_links:
    - "refresh_badge measurement (digit boundingRect after setPlainText, margin 0) -> badge setRect -> bx/by placement from the ACTUAL rect (box_item.py:613-614 refactored) -> edge-flip uses the same actual size"
    - "bubble_spin.editingFinished -> _emit_bubble_if_changed (WR-01 guard, now 0-vs-0 on unset) -> _on_inspector_bubble_committed (0 -> None / n -> assign+pin) -> _inspector_commit_post -> refresh_badge (amber D-16 border when manual_override)"
    - "load_box (None -> 0) <-> clear() (0) <-> _loaded_bubble (0) — the sentinel is consistent across populate/reset so the WR-01 no-op guard and the assignment path agree"
---

<objective>
Close the two remaining FIXABLE UAT gaps recorded in 04-UAT.md, as pure-fix TDD
tasks (round 3; the manga reading-order gap is DEFERRED per the user's explicit
instruction in 04-UAT.md and is NOT planned here):

- **Gap A (UAT test 4, MINOR):** "the little box at the top left of the bubble
  with the number is too small, only the top half of the number is visible, we
  need to make it fit the number". Root cause (verified in source AND by
  platform probe): the badge is a FIXED 20x14 rect (box_item.py:126-127,
  _BADGE_W/_BADGE_H) with the digit QGraphicsTextItem at a hardcoded pos
  (3.0, -1.0) (line 362). The 12pt bold digit glyph line box is 19px tall with
  the default document margin (24x27 bounding rect for '1' — probed), so the
  14px-tall badge clips the glyph vertically (the user's "top half only") and
  multi-digit numbers overflow the 20px width (probe: '12' spans local
  x 3..43 in a 0..20 badge). Fix: MEASURE the digit after setPlainText
  (document margin 0 -> the tight glyph line box, probed 16x19 px per digit,
  uniform across 0-9) and resize the badge rect to digit_w + 2x4 padding by
  digit_h + 2x2 padding, re-centering the digit; the TL-outside placement and
  the edge-flip must use the ACTUAL badge size (probed: '3' -> 24x23 at
  (-7,-6), '12' -> 40x23 at (-23,-6) for the reference box; edge-flip at the
  page TL corner -> (1,1)).
- **Gap B (UAT test 6, MINOR):** "bubble 1 can never be applied no matter what
  — even deleting and re-inputting 1 and pressing Enter, bubble 1 never gets
  assigned manually". Root cause (verified in source): sentinel collision —
  the spinbox range is 1..9999 (inspector_panel.py:166), load_box displays
  bubble_no=None as value 1 (line 221) and stores _loaded_bubble=1 (line 225),
  so the WR-01 guard _emit_bubble_if_changed (355-364) drops a user-entered 1
  as an "unchanged focus cycle" (number == _loaded_bubble) and it never
  commits. Bubble 1 is a legitimate manga number but is indistinguishable from
  the unset placeholder. Fix: 0 becomes the unset sentinel —
  setRange(0, 9999) + setSpecialValueText so 0 DISPLAYS as an em dash
  (probe-verified on PySide6 6.10.1: value 0 -> text() '\\u2014', value 1 ->
  '1', negative inputs clamp to 0), load_box maps None -> 0 (and
  _loaded_bubble=0), clear() uses 0; then 1 != 0 passes the WR-01 guard and
  commits. _on_inspector_bubble_committed (main_window.py:1414-1427) maps
  0 -> bubble_no=None + manual_override=False (clearing is not an override),
  1..9999 -> assign + manual_override=True (D-16 unchanged).

Purpose: make the UAT test-4 truth "bubble badge fits its number" and the UAT
test-6 truth "bubble # 1 is assignable" observable, each as a RED-then-GREEN
TDD task with headless pytest-qt regression tests.
Output: 2 fix tasks + 9 new test cases (3 functions / 6 cases for Gap A, 3
functions / 3 cases for Gap B) + 1 intentional existing-test change
(test_inspector_bubble_spin_range_is_bounded minimum 1 -> 0) + comment-only
updates in two WR-01 tests + four UI-SPEC amendment notes; full suite green at
460 (451 baseline + 9 new).
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ocr-recognition-text-editing/04-UAT.md
@.planning/phases/04-ocr-recognition-text-editing/04-09-SUMMARY.md
@.planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md

@manga_ai_studio/gui/box_item.py
@manga_ai_studio/gui/inspector_panel.py
@manga_ai_studio/gui/main_window.py
@tests/test_gui_boxes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap A — badge sized to the digit: measure after setPlainText (margin 0), resize badge rect + re-center digit, placement/flip use the actual size</name>
  <files>manga_ai_studio/gui/box_item.py, tests/test_gui_boxes.py, .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md</files>
  <behavior>
    - Test A (test_badge_rect_sizes_to_digit, parametrized [(3, 24.0, 23.0), (12, 40.0, 23.0), (123, 56.0, 23.0)]): a _pagebox_with_text box with bubble_no set, after refresh_badge() has item._badge.rect() == QRectF(0.0, 0.0, approx(badge_w), approx(badge_h)); the digit's document margin is 0.0 (item._badge_digit.document().documentMargin()); the digit local rect (digit.pos() + digit.boundingRect()) is FULLY contained in the badge rect (left >= 0, top >= 0, right <= badge_w, bottom <= badge_h); and the digit is centered (digit.pos().x() == approx((badge_w - digit_w)/2), y == approx((badge_h - digit_h)/2)). FAILS pre-fix: badge rect is the fixed 20x14, digit boundingRect is 24x27 (margin 4) at pos (3,-1) -> local rect (3,-1)-(27,26) is NOT contained (right 27 > 20, bottom 26 > 14) — the user's "top half only" clipping, and pos (3,-1) is not the center. NOTE: parametrize with the PLANNED digit widths 16/32/48 only as reference values — the test must not hard-code an assumption that the platform font differs; use the measured digit boundingRect for the centering assertion.
    - Test B (test_badge_tl_outside_uses_actual_badge_size, parametrized [(3, -7.0, -6.0), (12, -23.0, -6.0)]): for the reference box (rect 20,20,200,100, unselected pen 2 -> sceneBoundingRect (19,19,202,102)), after refresh_badge() the badge scenePos is exactly sceneBoundingRect.left() - badge_w - 2.0 by sceneBoundingRect.top() - badge_h - 2.0 — i.e. (-7,-6) for '3' and (-23,-6) for '12' (computed from the ACTUAL rect, not the old 20x14 constants), and the badge remains TL-outside (scenePos < box sceneBoundingRect top-left). FAILS pre-fix: scenePos is (-3,3) for BOTH digits (fixed 20x14 placement — the badge does not track its size).
    - Test C (test_badge_edge_flip_uses_actual_size): a box at Box(0,0,50,50) in a scene with setSceneRect(0,0,1000,1000), bubble_no=12 -> after refresh_badge() the outside candidate (-43,-26) would clip off the scene TL edge, so the badge flips INSIDE: scenePos == (1,1) (sceneBoundingRect.left()+2 by top()+2; sceneBoundingRect is (-1,-1,52,52) with the 2px pen) AND the digit is still fully contained in the 40x23 badge. FAILS pre-fix: scenePos is (1,1) too (the inside-flip inset is size-independent — lock only), but the digit ('12', 40x27 at margin 4, pos (3,-1)) is NOT contained in the 20x14 badge -> the containment assertion fails RED.
    - All three FAIL RED on current source (verified by probe on the exec platform, PySide6 6.10.1); existing badge tests (hidden/shows/ignores-transformations/amber-border/digit-text/auto-number-refresh) all keep passing — none asserts the fixed 20x14 size.
  </behavior>
  <action>
    In manga_ai_studio/gui/box_item.py:

    1. REPLACE the fixed badge-size constants block (lines 123-127, currently
       the two badge-size constants + the "Constant 20x14 viewport px (fits 1-2
       digits + future 3-digit padding)" comment) with two PADDING constants:
       `_BADGE_PAD_W = 4.0` and `_BADGE_PAD_H = 2.0`, and a comment stating the
       04-10 contract: the badge is DIGIT-SIZED — refresh_badge measures the
       digit glyph line box after setPlainText (document margin 0; probed
       16x19 px per digit at the 12pt semibold Liberation Sans badge font) and
       resizes the rect to digit_w + 2*_BADGE_PAD_W by digit_h + 2*_BADGE_PAD_H,
       so the badge always hugs its number (single digit ~24x23, two digits
       40x23, three 56x23, four 72x23 viewport px). The old fixed-size names
       MUST disappear from the ENTIRE file (code, comments, and docstrings) —
       delete them completely, do not leave any prose reference behind.
    2. Constructor badge construction (lines 344-362): build the rect as
       `QGraphicsRectItem(0.0, 0.0, 1.0, 1.0, self)` with a comment that
       refresh_badge sizes the rect from the digit measurement (the initial
       1x1 is never rendered with a number — refresh_badge runs in __init__
       and hides the badge while bubble_no is None). On the digit child
       (lines 355-360), add ONE line after setFont:
       `self._badge_digit.document().setDocumentMargin(0.0)` with a comment:
       the measurement basis is the tight glyph line box — with the default
       QTextDocument margin (4.0) the bounding rect is inflated by 8px per
       axis (probed: '1' is 24x27 at margin 4 vs 16x19 at margin 0), which
       would over-size the badge. Replace the hardcoded centering line
       `self._badge_digit.setPos(3.0, -1.0)` (line 362, comment "Centre the
       digit inside the badge rect") with `self._badge_digit.setPos(0.0, 0.0)`
       and a comment: the centered position is computed dynamically by
       refresh_badge from the measured digit size.
    3. Rework refresh_badge (lines 595-632) in this order:
       - Keep the early return when bubble_no is None (lines 606-608).
       - MOVE the digit text assignment to the TOP of the function (before any
         measurement): `self._badge_digit.setPlainText(str(self.pagebox.bubble_no))`
         (currently line 631 — the measurement must read the CURRENT text).
       - Measure + resize + center: read the digit's LOCAL bounding rect
         (NEVER sceneBoundingRect — the badge/digit ignore transformations, so
         local units are viewport px and the placement math below is in the
         same space): `dbr = self._badge_digit.boundingRect()`; floor the
         measured size with max(1.0, ...) on width AND height (degenerate
         measurement guard — a zero/negative rect must not produce an invalid
         badge rect, mirroring the T-4-13g floor discipline); compute
         `bw = dw + 2.0 * _BADGE_PAD_W` and `bh = dh + 2.0 * _BADGE_PAD_H`;
         `self._badge.setRect(0.0, 0.0, bw, bh)`; center the digit:
         `self._badge_digit.setPos((bw - dw) / 2.0, (bh - dh) / 2.0)`.
         The badge rect origin stays (0,0) (setPos of the badge is the scene
         placement; the rect is its local extent).
       - Placement (lines 612-614): the bx/by lines currently subtract the
         two fixed-size constants from the box's scene-space top-left —
         replace them with the ACTUAL badge size read back from the rect
         just set: `bw = self._badge.rect().width()` /
         `bh = self._badge.rect().height()` (or reuse the computed locals —
         one source of truth, so placement and the badge geometry cannot
         diverge).
       - Edge-flip (lines 619-624): UNCHANGED logic — it already compares the
         outside candidate against the scene rect and flips inside with the
         same inset; it automatically uses the new bx/by. Do not change the
         inside placement (rect.left() + _BADGE_OFFSET).
       - Keep the pen selection (626-630) and the final setVisible(True)
         (line 632) exactly as-is.
    4. Verify no other reference to the removed fixed-size constants remains
       (the constructor + placement were the only non-comment uses; the module
       docstring and section comments must not name them either).

    In tests/test_gui_boxes.py, add a section header right AFTER
    test_badge_digit_shows_bubble_number (line ~1587), BEFORE the
    "Plan 04-04 Task 2 — InspectorPanel" banner (~1591):
    "-- UAT test 4 gap closure round 3 (plan 04-10): badge digit-fit sizing --"
    with the three tests from <behavior>. Reuse `_pagebox_with_text` +
    `_scene_with_box`; set pb.bubble_no then call item.refresh_badge()
    (the existing badge-test pattern). For Test C build the scene with
    scene.setSceneRect(0.0, 0.0, 1000.0, 1000.0) BEFORE adding the item (so
    the flip decision sees the page bounds). Use pytest.approx for all
    geometry asserts; import QRectF if needed (check the existing imports at
    the top of the badge section — PySide6.QtCore QRectF may already be
    imported). For the containment assertion compute the digit's LOCAL rect
    as QRectF(digit.pos(), digit.boundingRect().size()) and compare against
    QRectF(0, 0, badge.rect().width(), badge.rect().height()) — never use
    sceneBoundingRect for the digit (transform math on an
    ignores-transformations child is not the unit under test).

    In .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md, THREE
    scoped edits (Edit tool, never Write):
    a) §Spacing row (line 68, "20x14 viewport px | Bubble-number badge size"):
       append — "04-10 amendment (gap closure round 3): SUPERSEDED — the
       badge is DIGIT-SIZED (see §17 04-10 amendment): digit glyph line box
       (measured with document margin 0) + 4px/side horizontal and 2px/side
       vertical padding; single digit ≈ 24x23 viewport px, 2 digits 40x23,
       3 digits 56x23, 4 digits (9999 max) 72x23. Still constant viewport px
       via ItemIgnoresTransformations at any zoom."
    b) §17 Widget bullet (line 267, "constant 20x14 viewport px"): append —
       "04-10 amendment (gap closure round 3): the badge is sized to its
       number — refresh_badge measures the digit's bounding rect (document
       margin 0 = the glyph line box) after setPlainText and resizes the
       badge rect to digit_w + 8 (4 px/side) by digit_h + 4 (2 px/side),
       re-centering the digit; the TL-outside placement and the edge-flip use
       the ACTUAL badge size, so the badge always hugs its number."
    c) §Color bullet 3 (line 126, "It is tiny (20x14 viewport px)"): append —
       "04-10: the badge is now digit-sized (§17 amendment) — still the one
       near-opaque element, sized to its glyph rather than a fixed asset."
    Do not touch any other spec row (the 04-08/04-09 amendment notes stay).
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_boxes.py::test_badge_rect_sizes_to_digit tests/test_gui_boxes.py::test_badge_tl_outside_uses_actual_badge_size tests/test_gui_boxes.py::test_badge_edge_flip_uses_actual_size tests/test_gui_boxes.py::test_badge_hidden_when_bubble_no_is_none tests/test_gui_boxes.py::test_badge_shows_bubble_no_when_set tests/test_gui_boxes.py::test_badge_ignores_transformations tests/test_gui_boxes.py::test_badge_manual_override_amber_border tests/test_gui_boxes.py::test_badge_digit_shows_bubble_number -q</automated>
  </verify>
  <done>
    RED commit: the three new tests fail on current source (Test A: badge
    rect 20x14, digit local rect (3,-1)-(27,26) not contained, margin 4;
    Test B: scenePos (-3,3) for both digits; Test C: digit not contained in
    the fixed 20x14 badge) — confirmed before implementation. The five
    existing badge tests pass RED by design (no size assertions). GREEN
    commit: all pass (Test A 3 parametrized cases, Test B 2 cases, Test C 1
    case);
    `grep -v '^#' manga_ai_studio/gui/box_item.py | grep -c '_BADGE_PAD_W\|_BADGE_PAD_H'`
    >= 2 (the definitions + their uses in refresh_badge),
    `grep -c '_BADGE_W\|_BADGE_H' manga_ai_studio/gui/box_item.py` == 0 (the
    fixed-size identifiers are gone from the WHOLE file — the grep has no
    comment stripping on purpose),
    `grep -v '^#' manga_ai_studio/gui/box_item.py | grep -c 'setDocumentMargin'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/box_item.py | grep -c 'self._badge_digit.boundingRect()'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/box_item.py | grep -c 'setRect(0.0, 0.0'`
    == 1, and the three UI-SPEC amendment notes (a/b/c) are present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gap B — 0 is the unset bubble sentinel: spinbox range 0..9999 + special value text, None-0 mapping, WR-01 guard keeps no-ops, handler maps 0 to None</name>
  <files>manga_ai_studio/gui/inspector_panel.py, manga_ai_studio/gui/main_window.py, tests/test_gui_boxes.py, .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md</files>
  <behavior>
    - Test A (test_inspector_bubble_spin_unset_sentinel): a fresh InspectorPanel has bubble_spin.minimum() == 0 (was 1), maximum() == 9999, value() == 0, and text() == the em dash '\u2014' (setSpecialValueText — probe-verified: at value 0 text() returns the special text, at value 1 it returns '1'). FAILS pre-fix: minimum() == 1 and text() at value 0 is '1' (the phantom unset display).
    - Test B (test_inspector_bubble_1_commit_assigns_unset_box): panel-level — an unset box (bubble_no=None) loaded into the panel, then bubble_spin.setValue(1) + editingFinished.emit() -> the bubble_no_changed capture == [1]. FAILS pre-fix: the WR-01 guard drops it (1 == _loaded_bubble 1) -> captured == [] — the exact user bug.
    - Test C (test_inspector_bubble_1_assigned_and_cleared_end_to_end): window-level (the _window_with_page + _seed_boxes_window pattern) — seed one box, load the panel (bubble_no None), setValue(1) + editingFinished.emit() -> item.pagebox.bubble_no == 1 AND manual_override is True (D-16 pin) AND the badge digit text is '1'; then load_box again (shows 1), setValue(0) + editingFinished.emit() -> bubble_no is None AND manual_override is False (clearing is not an override). FAILS pre-fix: the assignment half (bubble_no stays None, no badge) — the user's UAT test-6 report.
    - Existing WR-01 tests keep their behavior and pass pre- AND post-fix: test_inspector_unchanged_focus_cycle_is_noop (unset box: 0 vs 0 no-op; numbered box: n vs n no-op), test_inspector_bubble_changed_commit_still_emits (None -> 7 emits), test_inspector_bubble_spin_emits_bubble_no_changed (1 -> 9 emits), test_inspector_unchanged_commit_is_noop_end_to_end (editingFinished on an unset box emits nothing, no manual_override pin). ONLY test_inspector_bubble_spin_range_is_bounded changes behavior: its minimum() assertion flips 1 -> 0 (T-4-08 bounded range still holds — 0..9999).
  </behavior>
  <action>
    In manga_ai_studio/gui/inspector_panel.py:

    1. Constructor (lines 163-168): change the spinbox range call at line 166
       (currently minimum 1) to `setRange(0, 9999)`, then add
       `self.bubble_spin.setSpecialValueText("\\u2014")`
       (the em dash — the SAME placeholder character the Origin/Language
       labels use at lines 171/186, so the unset state reads consistently
       across the panel) and `self.bubble_spin.setValue(0)` (replacing
       setValue(1) — the empty/cleared state shows the dash, never a phantom
       '1'). Update the inline comment: 0 is the UNSET sentinel (probe:
       value 0 -> text() '\u2014', value 1 -> '1', negative input clamps to
       0; the special text is display-only — typing still edits normally).
    2. Module docstring (line 8, "bounded QSpinBox (1..9999)") -> "bounded
       QSpinBox (0..9999 — 0 is the unset sentinel, displayed as an em dash)".
       Class docstring security note (lines 37-38, T-4-08): "bounded
       1..9999" -> "bounded 0..9999"; append one sentence: 0 never reaches
       the model — the MainWindow handler maps the sentinel to
       bubble_no=None, so pagebox.bubble_no only ever receives None or
       1..9999 (T-4-08 mitigation unchanged).
    3. load_box (lines 220-225): `bubble = pagebox.bubble_no if
       pagebox.bubble_no is not None else 1` -> `else 0`, and update the
       inline comment ("None -> 0 as the unset sentinel; the field still
       enables"). Line 225 (`self._loaded_bubble = self.bubble_spin.value()`)
       is UNCHANGED — it reads back 0 for an unset box, which is exactly the
       WR-01 baseline the guard compares against.
    4. clear() (lines 281-287): `self._loaded_bubble = 1` -> 0 (line 281)
       and `self.bubble_spin.setValue(1)` -> `setValue(0)` (line 286), with
       the comment updated ("reset to the unset sentinel 0").
    5. _emit_bubble_if_changed docstring (lines 356-360): the sentence "The
       spinbox displays ``1`` for ``bubble_no=None``" -> "The spinbox
       displays ``0`` (the em dash) for ``bubble_no=None``" — the guard
       logic itself (number != _loaded_bubble) is UNCHANGED; with the
       sentinel at 0, a user-entered 1 now differs from the unset baseline
       and commits.

    In manga_ai_studio/gui/main_window.py — _on_inspector_bubble_committed
    (lines 1414-1427), the ONLY change in this file:
    - Replace the unconditional assign + pin with a sentinel branch: when the
      committed value equals the spinbox minimum (the unset sentinel 0),
      CLEAR — set `item.pagebox.bubble_no = None` and
      `item.pagebox.manual_override = False` (unset is not an override,
      D-16); otherwise assign — `item.pagebox.bubble_no = number` and
      `item.pagebox.manual_override = True` (D-16 unchanged for real
      numbers).
    - Update the docstring: the bounded spinbox is now 0..9999 (T-4-08) with
      0 as the unset sentinel — a 0 commit CLEARS the number (bubble_no=None,
      no override pin); 1..9999 assigns and pins manual_override. Keep the
      rest of the docstring.

    In tests/test_gui_boxes.py:
    a) UPDATE test_inspector_bubble_spin_range_is_bounded (lines 1762-1766):
       `minimum() == 1` -> `minimum() == 0`, and extend the docstring: the
       range is 0..9999 — 0 is the unset sentinel (T-4-08 bounded-range
       mitigation unchanged). This is the ONLY functional change to an
       existing test.
    b) Comment-only updates (no assertion changes): in
       test_inspector_unchanged_focus_cycle_is_noop (line ~1802) and
       test_inspector_bubble_changed_commit_still_emits (line ~1829), the
       comments "(the spin displays 1 for None (the spin default))" and
       "(displays 1; ...)" -> the 0-sentinel wording ("displays 0 — the
       unset sentinel").
    c) Add a section header AFTER test_inspector_unchanged_commit_is_noop_end_to_end
       (line ~1866), BEFORE the CR-01 undo test:
       "-- UAT test 6 gap closure round 3 (plan 04-10): bubble # 1 manually
       assignable (0-sentinel) --" with the three tests from <behavior>
       using the existing `_make_inspector(qtbot)` and `_window_with_page` +
       `_seed_boxes_window` helpers. Test B mirrors
       test_inspector_bubble_changed_commit_still_emits's wiring but enters
       1 (the previously-impossible value). Test C mirrors
       test_inspector_unchanged_commit_is_noop_end_to_end's window wiring;
       after the first commit assert the badge digit text via
       item._badge_digit.toPlainText() == "1" (the D-16 amber badge path —
       _inspector_commit_post refreshes the badge). QApplication.processEvents()
       after the commits (the existing end-to-end tests' pattern).

    In .planning/phases/04-ocr-recognition-text-editing/04-UI-SPEC.md, ONE
    scoped edit (Edit tool, never Write):
    d) §18 Bubble # row (line 284, "QSpinBox (range 1..N ...)"): append —
       "04-10 amendment (gap closure round 3): bounded 0..9999 with 0 as the
       UNSET sentinel — displayed via setSpecialValueText as an em dash (so
       an unset box never shows a phantom '1'); committing 0 CLEARS the
       number (bubble_no=None, manual_override=False — unset is not an
       override); 1..9999 assigns and pins manual_override (D-16). The
       T-4-08 bounded-range tampering mitigation is unchanged (values that
       reach the model are None or 1..9999)."
    Do not touch any other spec row.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_boxes.py::test_inspector_bubble_spin_unset_sentinel tests/test_gui_boxes.py::test_inspector_bubble_1_commit_assigns_unset_box tests/test_gui_boxes.py::test_inspector_bubble_1_assigned_and_cleared_end_to_end tests/test_gui_boxes.py::test_inspector_bubble_spin_range_is_bounded tests/test_gui_boxes.py::test_inspector_unchanged_focus_cycle_is_noop tests/test_gui_boxes.py::test_inspector_bubble_changed_commit_still_emits tests/test_gui_boxes.py::test_inspector_bubble_spin_emits_bubble_no_changed tests/test_gui_boxes.py::test_inspector_unchanged_commit_is_noop_end_to_end tests/test_gui_boxes.py::test_inspector_load_box_populates_fields -q</automated>
  </verify>
  <done>
    RED commit: Test A fails (minimum() == 1, text() at 0 is '1'), Test B
    fails (captured == [] — the guard drops 1), Test C's assignment half
    fails (bubble_no stays None, no badge) — confirmed before
    implementation; the updated range test and the existing WR-01 tests pass
    RED by design (except the intentionally-updated minimum assertion is
    part of the RED commit: change it to 0 in the SAME commit as the new
    tests, so the suite is consistent at every commit). GREEN commit: all
    pass;
    `grep -v '^#' manga_ai_studio/gui/inspector_panel.py | grep -c 'setRange(0, 9999)'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/inspector_panel.py | grep -c 'setSpecialValueText'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/inspector_panel.py | grep -c 'is not None else 0'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/inspector_panel.py | grep -c '_loaded_bubble = 0'`
    == 2 (init + clear),
    `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'if number == 0'`
    == 1,
    `grep -v '^#' manga_ai_studio/gui/inspector_panel.py | grep -c 'setRange(1, 9999)'`
    == 0, and the UI-SPEC §18 amendment note (d) is present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inspector Bubble # spinbox -> pagebox.bubble_no | Untrusted user input crosses into the reading-order model. UNCHANGED mitigation: a bounded QSpinBox (now 0..9999) — the 0 sentinel is mapped to None in the MainWindow handler, so the model only ever receives None or 1..9999 (T-4-08). |
| bubble_no (int) -> badge digit rendering | The int becomes a QGraphicsTextItem string on the canvas. Int-only input, no new text surface (the overlay's PLAIN-text ASVS V5 discipline is untouched, T-4-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-08 | Tampering | Inspector bubble_spin (range 0..9999) | high | mitigate | Bounded QSpinBox — out-of-range values cannot reach pagebox.bubble_no. The 04-10 sentinel change keeps the bound (0..9999); 0 is an INTERNAL sentinel the MainWindow maps to bubble_no=None BEFORE the model write, so no contract-violating value (0 or out-of-range) ever lands on the pagebox. Regression guard: test_inspector_bubble_spin_unset_sentinel (range + special value) + Test C's clear path (0 -> None). |
| T-4-16g | Tampering | Badge digit-measure arithmetic (boundingRect -> setRect) | low | mitigate | Measured width/height floored with max(1.0, ...) so a degenerate measurement cannot produce a zero/negative badge rect; the value is bounded by the 9999 spinbox maximum (4 digits -> 64px + 2x4 padding = 72px badge), so the sizing loop-free math cannot grow unbounded. |
| T-4-17g | Spoofing | Badge placement from the ACTUAL badge size | low | mitigate | Placement/flip use the same local values that sized the rect (one source of truth — read back from self._badge.rect() or the computed locals), so the TL-outside offset stays exactly badge+2px and the edge-flip decision matches the rendered size; covered by test_badge_tl_outside_uses_actual_badge_size + test_badge_edge_flip_uses_actual_size. |
| T-4-18g | Tampering | WR-01 no-op guard with the 0-sentinel baseline | medium | mitigate | The guard comparison is unchanged (number != _loaded_bubble); only the sentinel value moved (1 -> 0). Unchanged focus cycles (0 vs 0, n vs n) still emit nothing; a real 1 now commits. Covered by the retained WR-01 tests + the new Test B. The 0-clearing path cannot be confused with an unchanged cycle because a numbered box loads with _loaded_bubble = its number, so 0 != n always commits. |
</threat_model>

<verification>
Per-task gates: Task 1 — the three new badge tests fail RED on current source
(verified measurements: badge rect 20x14 fixed, digit 24x27 at margin 4 pos
(3,-1) -> local rect (3,-1)-(27,26) clipped; placement (-3,3) size-independent)
then pass GREEN with the digit-sized badge (24x23 / 40x23 / 56x23, digit
centered at (4,2), placement (-7,-6) / (-23,-6), edge-flip (1,1) — all
probe-verified on the exec platform). Task 2 — the sentinel tests fail RED
(minimum 1, guard drops 1, end-to-end never assigns) then pass GREEN with the
0-sentinel (probe-verified: setRange(0,9999)+setSpecialValueText renders
'\u2014' at 0 and '1' at 1). Grep gates per each task's <done>.

Suite regression (after both tasks, from the repo root):

    python -m pytest tests/ -q --deselect tests/test_gui_boxes.py::test_moved_box_via_real_events_persists_round_trip

must show 460 passed (451 baseline + 9 new cases: 6 Gap-A + 3 Gap-B), 0
failed. The pre-existing flake stays deselected — out of scope, do NOT fix it.
The deferred manga reading-order gap (UAT test 5) is NOT touched; the
04-08/04-09 deliverables (overlay fit-in-box + menu/toolbar work) are NOT
touched — Task 2's main_window.py edit is scoped to
_on_inspector_bubble_committed only.

<human-check>
The on-artwork visual judgment (the badge now fits its number over real manga
artwork, at the TL-outside corner, at production zooms) is a UAT re-test call,
deferred to the UAT test-4 re-verification (end-of-phase human gate, per the
project cadence in STATE.md). The mechanical contract is fully covered by the
automated suite above.
</human-check>
</verification>

<success_criteria>
- The UAT test-4 truth is observable: the bubble badge is sized to its number —
  the digit glyph is fully contained (no top-half clipping, no width overflow
  for multi-digit numbers) with 4px/2px padding, centered, constant viewport px
  at any zoom, TL-outside with the same 2px offset computed from the ACTUAL
  badge size, edge-flip intact at the page TL corner.
- The UAT test-6 truth is observable: an unset box displays the em-dash sentinel,
  entering 1 + Enter assigns bubble_no=1 with manual_override=True (amber D-16
  badge), and committing 0 clears the number (bubble_no=None,
  manual_override=False).
- WR-01 is preserved: unchanged focus cycles stay no-ops on both unset and
  numbered boxes (verified by the retained WR-01 tests + the new Test B/C).
- Every code change has a RED-then-GREEN TDD commit pair (2 tasks -> 2 RED +
  2 GREEN commits: `test(04-10): ...` then `fix(04-10): ...` per task).
- No new features, no new dependencies, no model downloads; only box_item.py /
  inspector_panel.py / main_window.py / test_gui_boxes.py / 04-UI-SPEC.md
  touched; the four UI-SPEC amendment notes (a/b/c/d) are in the spec; the full
  suite (451 baseline + 9 new cases) stays green with 0 failed.
</success_criteria>

<output>
Create `.planning/phases/04-ocr-recognition-text-editing/04-10-SUMMARY.md` when done
</output>
