---
phase: 06-refinement-polish-deferred-fixes-full-curve-editor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/crop_dialog.py
  - manga_ai_studio/gui/resize_dialog.py
  - manga_ai_studio/gui/load_translations_dialog.py
  - tests/test_gui_crop_tool.py
  - tests/test_gui_image_dialogs.py
  - tests/test_gui_boxes.py
autonomous: true
requirements:
  - D-10
  - D-12
estimate:
  tokens: 24000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Exactly one checkable window tool action is checked at all times (exclusive QActionGroup); the toolbar buttons mirror their default action's checked state on every entry path — V/B/R/L/E/G shortcut, Tools menu trigger, programmatic set_active_tool (UI-SPEC E3 covered)"
    - "The toolbar and the Tools dock always show the same active tool — the toolbar highlight syncs with the panel via the shared checked-state rule"
    - "The false group-membership comment at _make_tool_toolbar_button is corrected or made true (no lying comment remains)"
    - "Dialog field values and labels render at 14px Body: QFontInfo(dialog.font()).pixelSize() == 14 on Resize, Crop, LoadTranslations, and the new Curves dialog ships at 14px from birth (UI-SPEC D-12 covered)"
  artifacts:
    - manga_ai_studio/gui/main_window.py :: six window tool actions checkable + members of tools_panel.tool_group; corrected comment at _make_tool_toolbar_button
    - manga_ai_studio/gui/crop_dialog.py :: 14px Body font
    - manga_ai_studio/gui/resize_dialog.py :: 14px Body font
    - manga_ai_studio/gui/load_translations_dialog.py :: 14px Body font (Consolas 10 mono exception on paste_edit preserved)
    - tests/test_gui_crop_tool.py :: toolbar checked-state RED-gate regression + crop dialog typography test
    - tests/test_gui_image_dialogs.py :: resize dialog typography test
    - tests/test_gui_boxes.py :: LoadTranslations typography test
  key_links:
    - "set_active_tool (main_window.py:3120-3138) -> btn.setChecked(True) — functional only once the default actions are checkable + in the exclusive group"
    - "_make_tool_toolbar_button (main_window.py:3106-3118) — QToolButton mirrors its default action's checked state; the action must be checkable (Pitfall 2: making the BUTTON checkable alone is a no-op)"
    - "tools_panel.tool_group (tools_panel.py:105-106) — the exclusive group the window actions join"
  prohibitions:
    - "Window actions must NOT additionally connect toggled (panel actions already connect toggled at tools_panel.py:149-150 — double connections double-emit; RESEARCH Pitfall 1)"
    - "No new dependencies"
    - "The LoadTranslations paste_edit mono exception (Consolas 10, load_translations_dialog.py:120) stays"
---

<objective>
Apply the two remaining locked deferred fixes:

1. **D-10** — toolbar tool buttons never highlight the active tool: the six window tool actions (main_window.py:710-753) are non-checkable and outside the ToolsPanel's exclusive QActionGroup, so `set_active_tool`'s `btn.setChecked(True)` (main_window.py:3137) is a no-op. Fix = make the actions checkable + members of `tools_panel.tool_group` (Option 1 per RESEARCH Open Question 1 / UI-SPEC surface 31), correct the false comment at `_make_tool_toolbar_button`, and add a RED-gate regression test asserting toolbar button checked states across every entry path.
2. **D-12** — dialog typography at Qt default ~12px instead of the contracted 14px Body: set a 14-pixel base font on the three Phase-5/existing dialogs (Crop, Resize, LoadTranslations) — the UI-SPEC typography table row "Body | 14px | dialog field values, labels" becomes actually true. The new Curves dialog ships at 14px from birth (plan 06-04). Scope per UI-SPEC Resolved Assumptions: all four dialogs in one pass; the mono exception (Consolas 10 paste edit) preserved.

Purpose: close the deferred-items.md toolbar deferral (Phase 1 plan 04 era) and the 05-UI-REVIEW Pillar 3 typography flag; both are RED-GREEN regression contracts.

Output: checkable+grouped window tool actions with a working toolbar highlight sync; 14px Body fonts on the three existing dialogs with pixelSize tests.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-CONTEXT.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-RESEARCH.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-PATTERNS.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-UI-SPEC.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/tools_panel.py
@manga_ai_studio/gui/crop_dialog.py
@manga_ai_studio/gui/resize_dialog.py
@manga_ai_studio/gui/load_translations_dialog.py
@tests/test_gui_crop_tool.py
@tests/test_gui_image_dialogs.py
@tests/test_gui_boxes.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: D-10 — window tool actions checkable + in the exclusive group (RED-GREEN)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_crop_tool.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py — the six window tool action creations (:710-753: setData(ToolMode) + triggered lambda), `_make_tool_toolbar_button` (:3106-3118, the FALSE comment at :3109-3111 claiming group membership), `set_active_tool` (:3120-3138, the `btn.setChecked(True)` that is currently a no-op), tool shortcut wiring (:2565-2574)
    - manga_ai_studio/gui/tools_panel.py — the exclusive `tool_group` (:105-106), `_make_tool_action` (:206-221: setCheckable(True) + setData + addAction), `set_active_tool` (:244-272)
    - tests/test_gui_crop_tool.py — `test_crop_is_sixth_exclusive_tool` (:187-223, group-membership assertion shape: `crop_action.actionGroup() is panel.tool_group`) and `test_crop_action_in_tools_menu` (:227-267, toolbar-button lookup via `btn.defaultAction().data() == ToolMode.CROP` — must EXTEND, not replace)
  </read_first>
  <behavior>
    - Test 1 (RED gate): after `window.set_active_tool(ToolMode.BRUSH)` programmatically, the toolbar Brush button (looked up by `btn.defaultAction().data() == ToolMode.BRUSH`) is `isChecked()` and all other toolbar tool buttons are unchecked. MUST FAIL on current code (non-checkable action -> setChecked no-op).
    - Test 2 (shortcut path): emit the "V" QShortcut (lookup pattern: `next(sc for sc in window.findChildren(QShortcut) if sc.key().toString() == "V")`, `shortcut.activated.emit()`), assert the Move toolbar button isChecked + others unchecked.
    - Test 3 (menu path): `window.action_tool_crop.trigger()` (the menu action), assert the Crop toolbar button isChecked + others unchecked.
    - Test 4 (group membership): each window tool action `action.actionGroup() is window.tools_panel.tool_group`.
    - Test 5 (sync): after each path, `window.tools_panel.active_tool()` matches the toolbar's checked button.
  </behavior>
  <action>RED: write the tests in tests/test_gui_crop_tool.py (extend the existing crop test section — do NOT replace test_crop_action_in_tools_menu's data() assertions; add a new test function test_toolbar_buttons_track_active_tool). Run — must fail. GREEN: in `_build_tools_menu`, make each of the six window tool actions checkable and add them to the exclusive group: after each `self.action_tool_X = QAction(...)` creation (main_window.py:710-753), add `self.action_tool_X.setCheckable(True)` and `self.tools_panel.tool_group.addAction(self.action_tool_X)` (repeating for move/brush/rectangle/lasso/eraser/crop — RESEARCH Common Operation 4 pattern). Keep the existing `setData` + `triggered` lambda wiring. Correct the false comment at `_make_tool_toolbar_button` (:3109-3111) to state the actual mechanism: the buttons' default actions are checkable members of the ToolsPanel's exclusive QActionGroup, so checked state mirrors and the group's exclusivity unchecks the previous tool. Do NOT connect `toggled` on any window action (RESEARCH Pitfall 1 — the panel already connects toggled). Note the accepted side effect (UI-SPEC surface 31): the Tools-menu tool items now render a checkmark on the active tool.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All five tests pass; test_toolbar_buttons_track_active_tool fails on the pre-fix code (RED gate)
    - Grep gate: `grep -c 'setCheckable(True)' manga_ai_studio/gui/main_window.py` — at least 6 (the six tool actions; the panel's 6 are in tools_panel.py)
    - Grep gate: `grep -c 'tool_group.addAction' manga_ai_studio/gui/main_window.py` — 6 (all six window tool actions)
    - Grep gate: no new `toggled.connect` on window tool actions (only the panel's pre-existing connects in tools_panel.py:149-150)
    - The false comment at :3109-3111 no longer claims membership falsely — the comment text describes the checkable-action-in-group mechanism
  </acceptance_criteria>
  <done>Toolbar buttons highlight the active tool in sync with the dock on every entry path; the lying comment is fixed.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 — dialog field values/labels at 14px Body (Crop, Resize, LoadTranslations)</name>
  <files>manga_ai_studio/gui/crop_dialog.py, manga_ai_studio/gui/resize_dialog.py, manga_ai_studio/gui/load_translations_dialog.py, tests/test_gui_crop_tool.py, tests/test_gui_image_dialogs.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/crop_dialog.py — `__init__` (:66-75: super().__init__(parent) then setWindowTitle/setObjectName/setModal — the font set goes right after super().__init__)
    - manga_ai_studio/gui/resize_dialog.py — `__init__` (:96+: same insertion point)
    - manga_ai_studio/gui/load_translations_dialog.py — `__init__` (:98+) and the mono exception at :120 (`self.paste_edit.setFont(QFont("Consolas", 10))` — must stay)
    - 06-RESEARCH.md — Assumption A4: implement via `QFont.setPixelSize(14)` for exactness; tests assert `QFontInfo(font).pixelSize() == 14` (NOT pointSize — a 14px font reports pointSize ≈ 10.5)
    - 06-UI-SPEC.md — Typography section (D-12 enforcement; "QFont with setPixelSize(14)" — inherited by all children)
  </read_first>
  <action>In each dialog's `__init__`, immediately after `super().__init__(parent)`, set the base font with an explicit pixel size: build `f = QFont(); f.setPixelSize(14); self.setFont(f)` (do NOT use point size — `QFont("Segoe UI", 11)` ≈ 15px is NOT the contract; pixelSize(14) is). Children inherit the dialog font unless they set their own — the LoadTranslations paste_edit keeps its explicit Consolas 10 font at :120 (mono exception, untouched). The Curves dialog (plan 06-04) ships at 14px from birth — do NOT touch levels_dialog.py here. Add one typography regression test per dialog asserting `QFontInfo(dialog.font()).pixelSize() == 14`: ResizeDialog test in tests/test_gui_image_dialogs.py (near the existing dialog-contract test), CropDialog test in tests/test_gui_crop_tool.py (near the CropDialog test at :509), LoadTranslationsDialog test in tests/test_gui_boxes.py (near the existing LoadTranslations tests at :3282).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_image_dialogs.py tests/test_gui_crop_tool.py tests/test_gui_boxes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `QFontInfo(ResizeDialog().font()).pixelSize() == 14`, same for CropDialog and LoadTranslationsDialog (asserted by tests, not manual)
    - The LoadTranslations paste_edit still reports its Consolas 10 font (grep: `paste_edit.setFont` remains at load_translations_dialog.py:120; its QFontInfo(...).pixelSize() != 14 is NOT asserted — the exception is preserved, not tested away)
    - Grep gate: `grep -c 'setPixelSize(14)' manga_ai_studio/gui/crop_dialog.py manga_ai_studio/gui/resize_dialog.py manga_ai_studio/gui/load_translations_dialog.py` — 1 per dialog
    - All three touched test files pass (existing + new typography tests)
  </acceptance_criteria>
  <done>The three existing dialogs render labels and field values at 14px; the mono exception survives.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/main_window.py`:
  - Six window tool actions (`action_tool_move/brush/rectangle/lasso/eraser/crop`) — now `setCheckable(True)` + members of `tools_panel.tool_group` (the exclusive group)
  - `_make_tool_toolbar_button` — false group-membership comment corrected to describe the checkable-action mechanism
- `manga_ai_studio/gui/crop_dialog.py` / `resize_dialog.py` / `load_translations_dialog.py`: 14px Body base font (`QFont.setPixelSize(14)`) in `__init__`
- Tests: test_toolbar_buttons_track_active_tool (test_gui_crop_tool.py), test_crop_dialog_font_14px (test_gui_crop_tool.py), test_resize_dialog_font_14px (test_gui_image_dialogs.py), test_load_translations_dialog_font_14px (test_gui_boxes.py)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action group wiring → tool state | Incorrect QActionGroup wiring can leave multiple tools "checked" or the toolbar/dock out of sync (correctness, not security) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | QActionGroup exclusivity / set_active_tool sync | medium | mitigate | Exclusive group + checkable actions is the Qt contract (RESEARCH Pitfall 2 anti-pattern avoided); RED-gate tests cover shortcut/menu/programmatic paths; no toggled double-connects (RESEARCH Pitfall 1) |
| T-06-SC | Tampering | pip installs | high | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py tests/test_gui_image_dialogs.py tests/test_gui_boxes.py -x -q` — green
- Grep gates: 6x setCheckable(True) + 6x tool_group.addAction in main_window.py; no new toggled connects; 1x setPixelSize(14) per dialog file
- Full-suite spot check at wave merge: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (baseline 552 at Phase 5 close)
</verification>

<success_criteria>
- Toolbar + dock always show the same active tool across all entry paths (RED-GREEN regression)
- Dialog typography at 14px on the three dialogs; mono exception preserved
- No production code outside the four listed files touched; no new dependencies
</success_criteria>

<output>
Create `.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-03-SUMMARY.md` when done
</output>
