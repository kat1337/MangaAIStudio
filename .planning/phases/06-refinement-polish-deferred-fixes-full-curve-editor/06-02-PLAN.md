---
phase: 06-refinement-polish-deferred-fixes-full-curve-editor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/canvas.py
  - tests/test_gui_canvas.py
  - tests/test_gui_project.py
autonomous: true
requirements:
  - D-09
  - D-11
estimate:
  tokens: 22000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After ANY image display path (set_image_from_path, set_image_from_numpy, project open via _display_page_state, live preview frames), the empty-state trio (heading/body/hint, z=2000) is hidden — the empty state never persists over a loaded page (UI-SPEC E2 covered)"
    - "On a loaded page with zero boxes, the empty-box hint (z=850) is visible; on pages with boxes it is hidden (_update_empty_state already refreshes _refresh_empty_box_hint)"
    - "The first-run hint copy at canvas.py:278 is 'File \u2192 Open Folder\u2026 (Ctrl+Shift+O)   \u00b7   or drag files here' — the stale Open-Image Ctrl+O advertisement is gone (UI-SPEC D-11 covered)"
    - "The empty-state body text 'Open a single image or a folder of images to begin cleaning.' stays verbatim"
  artifacts:
    - manga_ai_studio/gui/canvas.py :: _set_image_from_numpy gains the _update_empty_state() call; hint copy at :278 rewritten
    - tests/test_gui_canvas.py :: numpy-path + preview-path + hint-copy regression tests
    - tests/test_gui_project.py :: project-open empty-state regression test
  key_links:
    - "_display_page_state (main_window.py:2292-2302) -> canvas.set_image_from_numpy -> _set_image_from_numpy — the bug site (the only display path missing the _update_empty_state call)"
    - "_update_empty_state (canvas.py:1545-1575) -> _refresh_empty_box_hint (canvas.py:1728-1751) — the one call covers both the trio and the box hint"
  prohibitions:
    - "The empty-state body text and heading text stay verbatim (only the hint copy changes)"
    - "Ctrl+O stays bound to Open Project… (main_window.py:311) — the hint must not advertise it as Open Image"
    - "No new dependencies"
---

<objective>
Apply two locked deferred fixes to `gui/canvas.py` with RED-GREEN regression tests at the real bug sites:

1. **D-09** — the empty-state overlay persists after project open: `_set_image_from_numpy` (canvas.py:710-767) never calls `_update_empty_state()`, so the z=2000 "No page open" trio stays rendered over the loaded page. Fix = call `self._update_empty_state()` at the end of `_set_image_from_numpy` (D-09 verbatim prescription). One-line fix; the regression test drives the project-open numpy path.
2. **D-11** — stale hint copy at canvas.py:278 advertises "Open Image… (Ctrl+O)" but Ctrl+O is Open Project… (Phase 5 re-binding, exactly one binding at main_window.py:311). Fix = the locked copy "File → Open Folder… (Ctrl+Shift+O) · or drag files here" (UI-SPEC Copywriting row; Open Folder binds Ctrl+Shift+O at main_window.py:305).

Purpose: close the two 05-UAT/05-UI-REVIEW deferrals whose root causes are already verified in-repo; both are RED-GREEN regression contracts (the tests must fail on current code).

Output: D-09 one-liner + D-11 copy change in canvas.py, with the regression tests in test_gui_canvas.py (numpy-path + preview-path + copy) and test_gui_project.py (project-open path).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-CONTEXT.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-RESEARCH.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-PATTERNS.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-UI-SPEC.md
@manga_ai_studio/gui/canvas.py
@tests/test_gui_canvas.py
@tests/test_gui_project.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: D-09 — empty-state overlay cleared on the numpy display path (RED-GREEN)</name>
  <files>manga_ai_studio/gui/canvas.py, tests/test_gui_canvas.py, tests/test_gui_project.py</files>
  <read_first>
    - manga_ai_studio/gui/canvas.py — `_set_image_from_numpy` (:710-767, the bug site; note `set_image_from_numpy_preview` :683-696 routes through the same shared impl), `_update_empty_state` (:1545-1575, already refreshes `_refresh_empty_box_hint`), `set_image` (:383) and `clear` (:423) which already call `_update_empty_state()`, the empty-state trio construction (:273-293)
    - tests/test_gui_project.py — `test_ctrl_o_opens_project_not_image` (:273-286) and `test_open_project_restores_session` (:309) — the project-open path shape
    - tests/test_gui_canvas.py — `test_empty_state_heading` (:206-217) — asserts text content only, NOT transitions (the gap the new test closes)
  </read_first>
  <behavior>
    - Test 1 (project-open path, test_gui_project.py): open a project session (or drive `window._display_page_state(imf)` directly per the D-11 seam) so the numpy display path runs; assert `not canvas._empty_heading.isVisible()` and `not canvas._empty_body.isVisible()` and `not canvas._empty_hint.isVisible()`; on a zero-box page assert `canvas.empty_box_hint.isVisible()`. MUST FAIL on current code (the trio stays visible).
    - Test 2 (numpy path, test_gui_canvas.py): on a fresh EditorCanvas showing the empty state, call `canvas.set_image_from_numpy(rgb)`; assert the trio is hidden and the empty-box hint is visible (zero boxes).
    - Test 3 (preview idempotence, test_gui_canvas.py): with an image displayed, call `canvas.set_image_from_numpy_preview(rgb, capture_original=False)`; assert the trio stays hidden and no exception — the `_update_empty_state` call is benign/idempotent on the preview path (RESEARCH Pitfall 1).
  </behavior>
  <action>RED: write the three tests (name them test_project_open_hides_empty_state_trio / test_numpy_display_hides_empty_state / test_preview_path_keeps_empty_state_hidden) and run them — they must fail on the current code. GREEN: add `self._update_empty_state()` at the END of `_set_image_from_numpy` (canvas.py:710-767), immediately before the final `return qimg` — the D-09 verbatim prescription. Do NOT place it in the public `set_image_from_numpy` wrapper; the shared-impl placement is the one-line fix and covers the preview path idempotently. Re-run the tests — all three must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py tests/test_gui_canvas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_project_open_hides_empty_state_trio fails before the fix (RED gate on the project-open numpy path) and passes after, asserting all three trio items not visible + empty-box hint visible on a zero-box page
    - test_numpy_display_hides_empty_state passes: fresh canvas empty state -> set_image_from_numpy -> trio hidden, empty_box_hint visible
    - test_preview_path_keeps_empty_state_hidden passes: preview frames never re-show the trio and never crash
    - The one-line call is at the end of `_set_image_from_numpy` (grep: `self._update_empty_state()` present in `_set_image_from_numpy` body, once)
  </acceptance_criteria>
  <done>The empty state never persists over a loaded page on any display path; project-open regression is green.</done>
</task>

<task type="auto">
  <name>Task 2: D-11 — hint copy references the real Open Folder binding</name>
  <files>manga_ai_studio/gui/canvas.py, tests/test_gui_canvas.py</files>
  <read_first>
    - manga_ai_studio/gui/canvas.py — the empty-state hint construction at :277-279 (current copy: "File \u2192 Open Image\u2026 (Ctrl+O)   \u00b7   or drag files here"), hint font/accent styling at :291-293
    - manga_ai_studio/gui/main_window.py — :305 (action_open_folder Ctrl+Shift+O) and :310-311 (action_open_project Ctrl+O) — the real bindings the copy must match
    - 06-UI-SPEC.md — the D-11 Copywriting row: "File → Open Folder… (Ctrl+Shift+O) · or drag files here" (the → / · / … glyphs are the existing \u2192 / \u00b7 / \u2026 escapes)
  </read_first>
  <action>Change the `_empty_hint` text at canvas.py:278 from the stale Open-Image copy to the locked D-11 copy: "File \u2192 Open Folder\u2026 (Ctrl+Shift+O)   \u00b7   or drag files here" (three spaces around the \u00b7 exactly as the current string has). Leave the heading ("No page open"), body ("Open a single image or a folder of images to begin cleaning."), fonts, and accent color untouched. Add a regression test in test_gui_canvas.py asserting `canvas._empty_hint.toPlainText()` equals the new copy exactly and that "Ctrl+O" is not a substring (grep-safe: the new copy contains "Ctrl+Shift+O" — the substring "Ctrl+O" cannot match it).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_canvas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `canvas._empty_hint.toPlainText()` == "File \u2192 Open Folder\u2026 (Ctrl+Shift+O)   \u00b7   or drag files here" (exact match incl. spacing)
    - The test asserts "Ctrl+O" not in the hint text; `! grep -Fq 'Ctrl+O' manga_ai_studio/gui/canvas.py` passes (the stale advertisement is gone from the file)
    - The body text assertion in test_empty_state_heading (starts with "Open a single image...") still passes — body untouched
  </acceptance_criteria>
  <done>The first-run hint advertises the real Open Folder binding; stale Ctrl+O copy is gone from canvas.py.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/canvas.py`:
  - `_set_image_from_numpy` — gains one `self._update_empty_state()` call at the end of the method body (D-09)
  - `_empty_hint` text — rewritten to "File \u2192 Open Folder\u2026 (Ctrl+Shift+O)   \u00b7   or drag files here" (D-11)
- `tests/test_gui_canvas.py` (extended): test_numpy_display_hides_empty_state, test_preview_path_keeps_empty_state_hidden, test_empty_hint_copy_references_open_folder
- `tests/test_gui_project.py` (extended): test_project_open_hides_empty_state_trio

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| display paths → empty-state visibility | numpy/path/preview display flows toggle overlay item visibility; a regression renders stale overlays over a loaded page (correctness, not security) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | _set_image_from_numpy empty-state visibility | low | mitigate | D-09 regression tests cover all three paths (project-open, numpy, preview); the call is idempotent with an image present (RESEARCH Pitfall 1 verified) |
| T-06-SC | Tampering | pip installs | high | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py tests/test_gui_canvas.py -x -q` — green (existing + new tests)
- Grep gate: `! grep -Fq 'Ctrl+O' manga_ai_studio/gui/canvas.py` — stale hint advertisement gone
- Grep gate: `grep -c '_update_empty_state()' manga_ai_studio/gui/canvas.py` — one call inside `_set_image_from_numpy` (plus the pre-existing calls in set_image/clear)
</verification>

<success_criteria>
- All three D-09 regression tests fail on pre-fix code and pass post-fix (RED-GREEN at the real bug site)
- Hint copy matches the UI-SPEC D-11 row verbatim; body/heading copy untouched
- No other production code changed; no new dependencies
</success_criteria>

<output>
Create `.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-02-SUMMARY.md` when done
</output>
