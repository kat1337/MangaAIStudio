---
phase: 06-refinement-polish-deferred-fixes-full-curve-editor
plan: 05
type: execute
wave: 3
depends_on:
  - 06-03
  - 06-04
files_modified:
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/levels_dialog.py
  - tests/test_gui_curves_dialog.py
  - tests/test_gui_image_dialogs.py
autonomous: true
requirements:
  - PROJ-04
estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Tools ▸ Image section shows 'Curves…' (the renamed Levels slot, Alt+T placement unchanged); the Levels dialog-opening action is gone (D-01 rename)"
    - "Apply commits ONE image-only IMAGE-stack undo entry via _apply_geometry_op('curves', geometry=False, ...) with flash 'Curves applied.'; geometry_altered is NOT set (D-15/D-22); masks/boxes untouched (D-15)"
    - "Cancel restores the pre-dialog image byte-identical with zero undo entries (Pitfall 9)"
    - "The b376f8a restore-before-Apply ordering is preserved: the detached base is re-displayed BEFORE _apply_geometry_op captures, so Ctrl+Z restores the true pre-dialog image byte-identical and leaves the stack empty (RESEARCH Pitfall 3 / UI-SPEC populated covered)"
    - "Show Original re-baselines to the post-curves image after Apply (D-14) and is never re-baselined by live previews mid-dialog (UI-SPEC populated covered)"
    - "The undo/redo flash op-name set is ('rotate', 'crop', 'curves', 'resize') — 'levels' replaced, not appended (RESEARCH Open Question 3)"
    - "Curve ops run on the GUI thread: the _op_running gate blocks re-entry during apply (no background thread touches the image mid-dialog) — the concurrency probe edge"
    - "gui/levels_dialog.py is deleted after migration; levels_lut/levels_page and their tests stay (the endpoint-math model)"
  artifacts:
    - manga_ai_studio/gui/main_window.py :: action_curves + _on_curves (renamed from the Levels slot), _undo_op_label set update, _refresh_action_states gating list update
    - manga_ai_studio/gui/levels_dialog.py :: DELETED
    - tests/test_gui_curves_dialog.py :: migrated lifecycle tests (test_curves_apply_pushes_one_entry, test_curves_cancel_restores_exactly, test_curves_preview_no_baseline_poison, test_curves_defaults_and_clamp)
    - tests/test_gui_image_dialogs.py :: Levels tests removed (rotate/resize + typography tests stay)
  key_links:
    - "_on_curves -> _apply_geometry_op('curves', geometry=False, transform_fn, flash='Curves applied.') — the ONE place the image-op contract lives (one undo entry, D-14 re-baseline)"
    - "CurvesDialog.result_values (master_points, channel_points) -> image_ops.curves_page(base, master, channels) — the transform_fn payload"
    - "lazy import swap: from manga_ai_studio.gui.curves_dialog import CurvesDialog replaces the Levels import inside the slot"
  prohibitions:
    - "The b376f8a restore-before-Apply ordering MUST NOT regress (05-UI-REVIEW-FIX; the Levels no-op undo flag is ALREADY FIXED — do not redo it)"
    - "levels_lut/levels_page and their tests are KEPT (not part of the migration)"
    - "The Curves dialog follows the Levels collector contract — no model mutation from the dialog, one undo entry per Apply"
    - "No new dependencies"
---

<objective>
Land the Curves dialog in the MainWindow: rename the Levels surface to "Curves…" (action + slot + flash + undo label), wire the full collector→preview→apply→undo lifecycle through `_apply_geometry_op("curves", geometry=False, ...)` preserving the b376f8a restore-before-Apply ordering, then delete `gui/levels_dialog.py` and migrate the Levels lifecycle tests to the Curves dialog suite (D-01, D-14, D-15; PROJ-04).

Purpose: complete the D-01 supersession — the curve editor becomes THE tones-adjustment surface; the Levels dialog is retired with its learning curve preserved inside the Curves dialog's quick-access rows (assumption-delta `promote`: the Curves dialog is the primary adjustment surface; no standalone Levels dialog surface remains).

Output: `action_curves`/`_on_curves` in main_window.py, levels_dialog.py deleted, migrated lifecycle tests green, full suite re-baselined.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-CONTEXT.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-RESEARCH.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-PATTERNS.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-UI-SPEC.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/curves_dialog.py
@tests/test_gui_curves_dialog.py
@tests/test_gui_image_dialogs.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: _on_curves — the Curves dialog wired end-to-end (RED-GREEN, tracer slice)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_curves_dialog.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py — `_on_levels` (:1227-1274, the ENTIRE slot to copy/rename: base capture with .copy(), preview_callback lambda, Cancel restore, b376f8a restore-before-Apply at :1261-1267, _apply_geometry_op call at :1272-1273), `action_levels` (:793-798), the Tools-menu Image section (:824), `_refresh_action_states` gating list (:1090), `_undo_op_label` set (:2888)
    - manga_ai_studio/gui/curves_dialog.py — the CurvesDialog from plan 06-04 (constructor signature (parent, page_image, preview_callback), result_values payload shape (master_points, channel_points))
    - tests/test_gui_image_dialogs.py — the Levels lifecycle tests to migrate (:145-288: test_levels_defaults_and_clamp, test_levels_cancel_restores_exactly, test_levels_apply_pushes_one_entry, test_levels_preview_no_baseline_poison) — the _fake_exec monkeypatch shape at :218-226
  </read_first>
  <behavior>
    - Test 1 (migrated apply contract): window with page; monkeypatch CurvesDialog.exec with a _fake_exec that drives the curve state (e.g. dlg._channel_points["RGB"] = [(0,0),(128,150),(255,255)] via widget edits or direct list mutation + dlg.result_values = (master, channels); return Accepted); window._on_curves(); assert canvas image == image_ops.curves_page(pre, master, channels) byte-exact; mask+boxes untouched; len(history._image_undo) == 1 and _mask_undo/_boxes_undo == 0; image_files[0].geometry_altered is False; status flash "Curves applied."; _original_image_numpy == post-op image (D-14)
    - Test 2 (b376f8a restore semantics): after Apply, window.on_undo() restores the PRE-DIALOG image byte-identical and the stack is empty (the before-state is the true pre-op image, never the last preview frame)
    - Test 3 (migrated cancel): _fake_exec returns Rejected after mutating the curve state (preview fired); canvas restored byte-identical; not history.can_undo(); no flash
    - Test 4 (migrated preview no-baseline-poison): preview mid-dialog never re-baselines Show Original; Cancel leaves _original_image_numpy == pre-dialog image
    - Test 5 (menu surface): window.action_curves exists with text "Curves\u2026", the Tools-menu Image section contains it, "Levels\u2026" action is gone
  </behavior>
  <action>RED: migrate the four Levels lifecycle tests from tests/test_gui_image_dialogs.py into tests/test_gui_curves_dialog.py as the Curves equivalents (rename the test functions; retarget the monkeypatch from the Levels dialog class to `CurvesDialog`; drive the curve state via the dialog's real controls — e.g. preset buttons or _channel_points — plus the result_values payload (master, channels); update the flash assertions to "Curves applied."). Run them against current code — they must fail (no _on_curves/action_curves). GREEN: in main_window.py, rename `action_levels` → `action_curves` with text "Curves\u2026" and the UI-SPEC tooltip ("Adjust tones with a draggable curve — presets, per-channel curves, and black/white/gamma quick controls, with live preview."); rename `_on_levels` → `_on_curves` and swap the lazy import to `from manga_ai_studio.gui.curves_dialog import CurvesDialog`; KEEP the slot's leading gate verbatim (`if self._op_running or self._current_page_index() is None: return` — the concurrency re-entry lock) and the `base = self.canvas.get_image_numpy(); base = base.copy()` capture (Pitfall 2); the preview lambda calls `image_ops.curves_page(base, master, channels)` where (master, channels) = dialog.result_values unpacked; KEEP the Cancel-restore and the b376f8a restore-before-Apply ordering exactly as-is (:1254-1267 shape); call `_apply_geometry_op("curves", geometry=False, transform_fn=_transform, flash="Curves applied.")` with `_transform` returning `(image_ops.curves_page(base, master, channels), None, None)` (geometry-free, D-15); update `_undo_op_label` set (:2888) to replace "levels" with "curves"; update the `_refresh_action_states` gating list (:1090) to reference action_curves. Tests must pass GREEN.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py tests/test_gui_image_dialogs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All four migrated lifecycle tests pass with CurvesDialog + _on_curves (RED→GREEN proven on the rename)
    - test_curves_apply_pushes_one_entry asserts: byte-exact curves_page output, ONE image-only entry, geometry_altered False, flash "Curves applied.", Ctrl+Z → byte-identical pre-dialog image + empty stack (b376f8a restore semantics carried over)
    - Grep gate: `grep -c 'action_curves' manga_ai_studio/gui/main_window.py` >= 3 (creation, menu add, gating list); `grep -c '_on_curves' manga_ai_studio/gui/main_window.py` >= 1; `! grep -q 'Levels\u2026' manga_ai_studio/gui/main_window.py` — the menu copy is gone
    - `_undo_op_label` set contains "curves" and not "levels" (grep the tuple)
    - The b376f8a ordering code is present in _on_curves: `set_image_from_numpy(base.copy())` appears BEFORE the `_apply_geometry_op` call (both in the Accepted path)
  </acceptance_criteria>
  <done>The Curves dialog applies end-to-end through the image-op contract with one undo entry, exact Cancel, and preserved restore-before-Apply ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Delete levels_dialog.py + finish test migration (rename sweep, no stale refs)</name>
  <files>manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/levels_dialog.py, tests/test_gui_image_dialogs.py</files>
  <read_first>
    - manga_ai_studio/gui/levels_dialog.py — the module being deleted (its _DIALOG_QSS + gamma mapping live on in curves_dialog.py)
    - tests/test_gui_image_dialogs.py — the Levels test remnants to remove (:35 the LevelsDialog import, :145-288 the four levels tests — replaced by the migrated curves tests in test_gui_curves_dialog.py), the rotate/resize tests that STAY
    - 06-RESEARCH.md — Runtime State Inventory (the in-code rename scope: test call sites test_gui_image_dialogs.py:191,229,279 call _on_levels; :196,242 assert "Levels applied.")
  </read_first>
  <action>Delete `manga_ai_studio/gui/levels_dialog.py`. Remove the Levels test remnants from tests/test_gui_image_dialogs.py (the LevelsDialog import at :35 and the four levels tests at :145-288 — they now live as the curves tests in test_gui_curves_dialog.py); keep the rotate/resize tests and any typography tests intact. Sweep for stale references: grep the repo (excluding .planning/ and the kept levels_lut/levels_page math) for the deleted module's import path and class name, and for any remaining "Levels" action/copy references in main_window.py — fix or remove each (the gating list, tooltips, status strings). Run the full suite — the baseline must be green and STRICTLY the pre-existing count plus this phase's additions (no test lost: 4 levels tests became 4 curves tests).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path manga_ai_studio/gui/levels_dialog.py` is False (file deleted)
    - `! grep -rn "levels_dialog import" manga_ai_studio tests --include="*.py"` — no module imports remain; `! grep -rn "LevelsDialog" manga_ai_studio tests --include="*.py"` — no class references remain
    - `! grep -n "Levels\u2026\|Levels applied\|_on_levels\|action_levels" manga_ai_studio/gui/main_window.py` — no stale Levels surface copy in the window
    - Full suite green; `& "...\python.exe" -m pytest -q` reports the same pass count as the pre-deletion run plus this phase's new tests (no regression, no lost tests)
  </acceptance_criteria>
  <done>The Levels dialog module and its tests are gone; the suite is green with no stale references.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/main_window.py`:
  - `action_curves` — "Curves…" action (replaces the Levels action; Alt+T Image-section slot + placement unchanged; new UI-SPEC tooltip)
  - `_on_curves` — renamed slot: CurvesDialog collector + preview via `image_ops.curves_page`, Cancel restore, b376f8a restore-before-Apply, `_apply_geometry_op("curves", geometry=False, transform_fn, flash="Curves applied.")`
  - `_undo_op_label` set — ("rotate", "crop", "curves", "resize")
  - `_refresh_action_states` — gates `action_curves`
- `manga_ai_studio/gui/levels_dialog.py` — DELETED (superseded by curves_dialog.py, D-01)
- `tests/test_gui_curves_dialog.py` (extended): test_curves_apply_pushes_one_entry, test_curves_cancel_restores_exactly, test_curves_preview_no_baseline_poison, test_curves_defaults_and_clamp, test_curves_action_in_tools_menu (migrated from the Levels suite)
- `tests/test_gui_image_dialogs.py`: Levels tests removed; rotate/resize/typography tests stay

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog exec result → undo stack | Apply path captures the undo before-state; a wrong capture (last preview frame) makes Ctrl+Z a no-op — undo-record integrity (V7 logic) |
| menu/action surface → op dispatch | The renamed action must gate and dispatch identically to the Levels slot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | _on_curves undo before-state capture | high | mitigate | b376f8a restore-before-Apply ordering preserved (set_image_from_numpy(base.copy()) before _apply_geometry_op); test_curves_apply_pushes_one_entry asserts Ctrl+Z → byte-identical pre-dialog image + empty stack (regression guard, ASVS V7) |
| T-06-08 | Tampering | action/flash/undo-label rename sweep | low | mitigate | Grep gates for stale references + migrated flash assertions ("Curves applied.") + full-suite re-baseline |
| T-06-SC | Tampering | pip installs | high | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py tests/test_gui_image_dialogs.py -x -q` — migrated lifecycle + remaining dialog tests green
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — green, count = baseline + phase additions
- Grep gates: no `levels_dialog`/`LevelsDialog`/`_on_levels`/`action_levels`/`Levels\u2026`/`Levels applied` references in manga_ai_studio/ + tests/ (levels_lut/levels_page exempt — kept math)
</verification>

<success_criteria>
- Tools ▸ Curves… opens the curve editor; Apply/Cancel/undo lifecycle matches the Levels contract with the b376f8a ordering intact
- levels_dialog.py deleted; no stale references; levels_lut/levels_page kept and tested
- Full suite green at the wave merge
</success_criteria>

<output>
Create `.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-05-SUMMARY.md` when done
</output>

<!-- planner-discipline-allow: action_levels -->
<!-- planner-discipline-allow: _on_levels -->
<!-- planner-discipline-allow: LevelsDialog -->
<!-- planner-discipline-allow: Levels applied -->

