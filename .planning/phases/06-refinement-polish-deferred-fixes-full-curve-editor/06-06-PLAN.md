---
phase: 06-refinement-polish-deferred-fixes-full-curve-editor
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - tests/test_gui_curves_dialog.py
autonomous: true
gap_closure: true
requirements:
  - PROJ-04
assumption_delta:
  decision: no-change
  rationale: "The assumption-delta scan fired on the historical ROADMAP phrase 'Levels dialog D-12 is the v1 fallback'. The promote decision is ALREADY resolved and recorded in 06-04-PLAN.md's <assumption_delta_decision> block: the Curves dialog is the primary adjustment surface, Levels demoted to quick-access rows, levels_dialog.py deleted. This gap plan introduces no new variant of that identity model — do not re-litigate."
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "On a fresh page (canvas._original_image_numpy is None at dialog open — the folder-load state), opening the Curves dialog, touching controls so live previews fire, and pressing Cancel leaves canvas._original_image_numpy None and canvas.has_inpaint_result() False, with the canvas showing the pre-dialog image byte-identical (VERIFICATION truth 25 / CR-01 restored)"
    - "The Cancel path in _on_curves restores via the capture-suppressed preview path — it never captures a baseline and never claims an inpaint result; after the next _refresh_action_states, Show Original (P) stays disabled on the fresh page (IN-03 folded into the gate)"
    - "The Apply pre-restore in _on_curves is capture-suppressed too; after Apply, Show Original still re-baselines to the post-curves image via _apply_geometry_op's tail rebaseline_original() (D-14 preserved — test_curves_apply_pushes_one_entry stays green)"
    - "The capture-enabled display paths (inpaint result display, geometry-op write-back, project page display) still set _inpainted_qimage — has_inpaint_result() stays True after a real set_image_from_numpy call (test_inpaint_gui.py:256 contract preserved)"
  artifacts:
    - manga_ai_studio/gui/main_window.py :: _on_curves Cancel restore (:1274) and apply pre-restore (:1284) both routed through set_image_from_numpy_preview(..., capture_original=False)
    - manga_ai_studio/gui/canvas.py :: _set_image_from_numpy gates _inpainted_qimage on capture_original (:769)
    - tests/test_gui_curves_dialog.py :: test_curves_cancel_fresh_page_no_baseline_poison (the no-pre-baseline variant, IN-01)
  key_links:
    - "_on_curves Cancel -> canvas.set_image_from_numpy_preview(base.copy(), capture_original=False) — the capture-suppressed restore is the ONLY display restore left in _on_curves"
    - "canvas._set_image_from_numpy capture_original gate -> _inpainted_qimage — the preview path can no longer claim an inpaint result"
    - "test_curves_cancel_fresh_page_no_baseline_poison asserts the (_original_image_numpy is None, has_inpaint_result() False) pair that CR-01 violated"
  prohibitions:
    - "The Curves dialog live preview must never re-baseline Show Original mid-dialog (D-14 / Pitfall 5)"
    - "The Cancel path must not establish or poison a Show Original baseline — no capture-enabled restore and no rebaseline_original() on Cancel"
    - "The preview path must not claim an inpaint result (_inpainted_qimage stays gated on capture_original)"
    - "The b376f8a restore-before-Apply ordering must not regress (Ctrl+Z restores the true pre-dialog image)"
---

<objective>
Close gap **CR-01 (critical)** from 06-VERIFICATION.md (truth 25 failed): the Curves dialog's Cancel path poisons the Show Original baseline on fresh pages. The Cancel restore at main_window.py:1274 uses the capture-ENABLED `set_image_from_numpy`, so on a fresh page (`_original_image_numpy is None`, reset by canvas.clear() at canvas.py:423 — the folder-load state, since on_page_selected displays via set_image_from_path which never captures) the capture at canvas.py:742-743 stores the LAST PREVIEW FRAME (curve-distorted) as the "original", and canvas.py:769 sets `_inpainted_qimage` unconditionally so `has_inpaint_result()` turns True with no inpaint ever run — enabling Show Original (P) to display the distorted frame as "the original". The phase's own regression test masks the scenario by calling `canvas.rebaseline_original()` before opening the dialog (IN-01, tests/test_gui_curves_dialog.py:726).

Purpose: restore the D-14 contract "Show Original re-baselines to the post-curves image after Apply and is never re-baselined by live previews" — Cancel must be a silent display-only restore that neither captures a baseline nor claims an inpaint result.

Output: capture-suppressed Cancel + apply-pre-restore in `_on_curves`, the `capture_original` gate on `_inpainted_qimage`, and the no-pre-baseline regression test the gap prescribes.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-VERIFICATION.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-REVIEW.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-05-PLAN.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/canvas.py
@tests/test_gui_curves_dialog.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-GREEN — fresh-page Cancel must not poison the baseline (CR-01 regression + cancel fix + canvas gate)</name>
  <files>manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/canvas.py, tests/test_gui_curves_dialog.py</files>
  <read_first>
    - tests/test_gui_curves_dialog.py — test_curves_preview_no_baseline_poison (:717-747, the IN-01 pre-baseline at :726 that masks the defect — the new variant DROPS that call), test_curves_cancel_restores_exactly (:628-650, the _fake_exec Rejected shape), _window_with_page (:96-107 — folder load leaves _original_image_numpy None: on_page_selected displays via set_image_from_path, which never enters _set_image_from_numpy)
    - manga_ai_studio/gui/main_window.py — _on_curves (:1244-1291; the Cancel restore at :1271-1275)
    - manga_ai_studio/gui/canvas.py — _set_image_from_numpy (:715-780; the None-capture at :742-743, the unconditional `_inpainted_qimage = qimg` at :769), set_image_from_numpy_preview (:688-701), rebaseline_original (:703-713), has_inpaint_result (:802-804)
  </read_first>
  <behavior>
    - Test 1 (RED gate): fresh page WITHOUT the pre-baseline call — at dialog open assert `window.canvas._original_image_numpy is None`; _fake_exec mutates black/gamma/white controls (live previews fire, canvas mutates mid-dialog) then returns Rejected; after `window._on_curves()` + processEvents: (a) `np.array_equal(window.canvas.get_image_numpy(), pre)` byte-exact, (b) `window.canvas._original_image_numpy is None`, (c) `window.canvas.has_inpaint_result() is False`, (d) after `window._refresh_action_states()`, `window.action_show_original.isEnabled() is False` (the IN-03 symptom — Show Original/Preview-hold must not light up without an inpaint). MUST FAIL on current code: the capture-enabled cancel restore stores the last preview frame as the baseline (b fails, _original_image_numpy == curved frame) and sets _inpainted_qimage (c fails).
  </behavior>
  <action>RED: add `test_curves_cancel_fresh_page_no_baseline_poison` to tests/test_gui_curves_dialog.py — clone the _fake_exec/driver shape of test_curves_preview_no_baseline_poison but OMIT the `window.canvas.rebaseline_original()` call, add the `_original_image_numpy is None` open-state assertion and the (b)/(c)/(d) assertions above. Run — must fail. GREEN, two production edits: (1) main_window.py:1274 — the Cancel restore becomes capture-suppressed: replace the capture-enabled setter call with `self.canvas.set_image_from_numpy_preview(base.copy(), capture_original=False)`. Do NOT call `canvas.rebaseline_original()` after it — deliberate deviation from the review's "+ rebaseline_original()" suggestion, because the VERIFICATION missing-test contract asserts `_original_image_numpy` STAYS None on fresh pages, and a rebaseline on Cancel would also clobber a legitimate pre-inpaint baseline on an already-baselined page (Show Original would then show the pre-dialog image instead of the true pre-inpaint original). The honest D-14 baseline is established exclusively by `_apply_geometry_op`'s tail rebaseline (main_window.py:1198) on Apply. (2) canvas.py:769 — gate the inpaint-result claim: wrap `self._inpainted_qimage = qimg` in `if capture_original:` so the preview path (capture_original=False) can never claim an inpaint result. The capture-enabled callers (inpaint display, op write-backs, project page display) keep setting it. Run — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_curves_cancel_fresh_page_no_baseline_poison FAILS on the pre-fix code (RED gate: baseline captured from the last preview frame, has_inpaint_result True) and PASSES after the two fixes
    - test_curves_preview_no_baseline_poison (the baselined-page variant) still passes unchanged
    - test_curves_cancel_restores_exactly still passes (byte-exact restore, no undo entry, no flash)
    - Grep gate: `grep -c 'if capture_original:' manga_ai_studio/gui/canvas.py` == 2 (the None-capture at :742 plus the new _inpainted_qimage gate)
    - Grep gate: `grep -n 'set_image_from_numpy_preview' manga_ai_studio/gui/main_window.py | Select-String '12[67][0-9]'` shows the Cancel restore at :1274 using the preview method (capture-suppressed)
  </acceptance_criteria>
  <done>The fresh-page Cancel path restores silently: no baseline captured, no inpaint result claimed, byte-exact image, Show Original stays disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Apply pre-restore hardening + CR-01 closure sweep (b376f8a ordering preserved)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_curves_dialog.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py — _on_curves Accepted path (:1276-1291; the b376f8a apply pre-restore at :1284), _apply_geometry_op tail (:1192-1198: _record_geometry_op_name → push_geometry_state → rebaseline_original — the D-14 re-baseline that must remain the ONLY baseline writer on the apply path)
    - manga_ai_studio/gui/canvas.py — set_image_from_numpy (:663-686, capture-enabled — still used by inpaint/op/project paths)
    - tests/test_gui_curves_dialog.py — test_curves_apply_pushes_one_entry (:654-713, the assertions that must stay green: post-op baseline at :704, restore-semantics at :709-713)
    - tests/test_inpainting/test_inpaint_gui.py — the has_inpaint_result-after-set_image_from_numpy contract at :237-256
  </read_first>
  <action>Harden the Apply pre-restore to match the review's "and on the apply pre-restore" prescription: main_window.py:1284 — swap the capture-enabled restore to the capture-suppressed preview method (same call shape as the Cancel path). On a fresh page this removes the intermediate capture of the LAST PREVIEW FRAME (previously stored at :1284 and only corrected later by the tail rebaseline); the post-Apply baseline is still established by _apply_geometry_op's tail `rebaseline_original()` (:1198), so `test_curves_apply_pushes_one_entry`'s `_original_image_numpy == expected` assertion (:704) holds unchanged. IN-03 (Cancel leaves stale action enablement) needs no separate fix — it is moot: the canvas gate keeps `_inpainted_qimage` None through the whole preview+cancel lifecycle, so `_refresh_action_states` gating (has_inpaint at :996) is correct on every path; Task 1's (d) assertion proves it. Run the affected modules, then the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py tests/test_gui_canvas.py tests/test_inpainting/test_inpaint_gui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_curves_apply_pushes_one_entry passes UNCHANGED (post-apply baseline == post-op image at :704; on_undo restores the pre-dialog image byte-identical; stack empty)
    - test_inpaint_gui.py:237-256 contract preserved: a real capture-enabled `set_image_from_numpy` still yields `has_inpaint_result() is True`
    - test_rebaseline_original_after_op (tests/test_gui_canvas.py:701) still passes
    - Grep gate: `grep -c 'set_image_from_numpy_preview' manga_ai_studio/gui/main_window.py` == 3 (the preview lambda at :1267 plus the Cancel restore and the apply pre-restore — the only display calls in _on_curves)
    - Grep gate: `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'set_image_from_numpy(base'` == 0 (no capture-enabled base restore remains in the window)
    - Full suite green at the re-baselined count (597 + the one new test = 598 passed, 0 failed)
  </acceptance_criteria>
  <done>Every display restore in _on_curves is capture-suppressed; the D-14 re-baseline happens only via the apply-path tail; the full suite is green.</done>
</task>

</tasks>

## Flagged Probe Assumptions (specless fallback — EDGE_ABSENT, PROHIB_ABSENT)

No SPEC.md exists for phase 6; the deterministic edge probe on PROJ-04 returned two UNRESOLVED edges. Neither is silent-dropped here — each maps to an existing verified truth in 06-VERIFICATION.md:

1. **idempotency** — probe: "What happens if this runs twice on the same input?" → **Resolution:** curve_lut is pure; double application is deterministic curve-of-curve composition, verified by `test_curve_lut_and_page_idempotent` (VERIFICATION truth 3, ✓ VERIFIED). The op lifecycle itself pushes exactly one image-only undo entry per Apply (truth 22) — re-applying is a fresh op with a fresh record. Flagged assumption: no additional idempotency work in this gap plan.
2. **concurrency** — probe: "If interrupted or run in parallel, what is guaranteed?" → **Resolution:** all curve ops run on the GUI thread; the `_op_running` gate blocks re-entry during apply (truth 27, ✓ VERIFIED, main_window.py:1256 kept verbatim). The preview is a synchronous display mutation on the GUI thread (Pitfall 9). Flagged assumption: no background thread touches the image mid-dialog; no additional concurrency work in this gap plan.

## Artifacts this phase produces

- `manga_ai_studio/gui/main_window.py`:
  - `_on_curves` — Cancel restore (:1274) and apply pre-restore (:1284) both via `set_image_from_numpy_preview(base.copy(), capture_original=False)`; no baseline writer on the cancel path; D-14 rebaseline stays in `_apply_geometry_op` (:1198)
- `manga_ai_studio/gui/canvas.py`:
  - `_set_image_from_numpy` — `_inpainted_qimage = qimg` gated on `capture_original` (:769); the preview path can no longer claim an inpaint result
- `tests/test_gui_curves_dialog.py` (extended): `test_curves_cancel_fresh_page_no_baseline_poison` — no-pre-baseline variant (IN-01) asserting `_original_image_numpy is None` + `has_inpaint_result() is False` + byte-exact restore + Show Original disabled after refresh

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog preview/cancel → canvas display state | The cancel restore crosses into the canvas display path; a capture-enabled restore on a fresh page stores a preview frame as the Show Original baseline (compare-integrity violation) |
| canvas display path → inpaint-result gate | `_inpainted_qimage` drives `has_inpaint_result()` → Show Original (P) + Preview-hold enablement; a false claim lights up the before/after compare with no inpaint ever run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Tampering | _on_curves Cancel/apply-pre-restore baseline capture | high | mitigate | Both restores routed through the capture-suppressed preview path (capture_original=False); no rebaseline on Cancel; regression test test_curves_cancel_fresh_page_no_baseline_poison asserts _original_image_numpy stays None + has_inpaint_result False (ASVS V7 undo/state integrity) |
| T-06-10 | Spoofing | _set_image_from_numpy _inpainted_qimage claim | high | mitigate | Gate `_inpainted_qimage` on capture_original — the preview path cannot claim an inpaint result; capture-enabled callers (inpaint/op/project display) keep the claim; test_inpaint_gui.py:256 contract re-verified |
| T-06-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py tests/test_gui_canvas.py tests/test_inpainting/test_inpaint_gui.py -x -q` — affected modules green (the CR-01 RED gate proven RED→GREEN)
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 598 passed, 0 failed (597 + the new test)
- Grep gates: `if capture_original:` x2 in canvas.py; `set_image_from_numpy_preview` x3 in main_window.py; zero capture-enabled `set_image_from_numpy(base` calls
- The VERIFICATION.md `human_verification` item (curve drag fluidity + grid/handle legibility at 150%/200% DPI) is held for the phase UAT gate via /gsd-verify-work — no plan is created for it (explicitly out of gap scope)
</verification>

<success_criteria>
- Truth 25 ("Show Original re-baselines to the post-curves image after Apply (D-14) and is never re-baselined by live previews mid-dialog") is PASS-able: fresh-page open→preview→Cancel leaves `_original_image_numpy` None, `has_inpaint_result()` False, byte-exact restore, Show Original disabled
- Apply path D-14 re-baseline + b376f8a restore ordering intact (existing lifecycle tests unchanged and green)
- Full suite green at 598; no new dependencies
</success_criteria>

<output>
Create `.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-06-SUMMARY.md` when done
</output>

<!-- planner-discipline-allow: set_image_from_numpy(base -->
<!-- planner-discipline-allow: _inpainted_qimage = qimg -->
