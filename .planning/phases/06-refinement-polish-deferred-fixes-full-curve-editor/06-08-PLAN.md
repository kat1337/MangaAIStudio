---
phase: 06-refinement-polish-deferred-fixes-full-curve-editor
plan: 08
type: execute
wave: 3
depends_on:
  - 06-07
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_crop_tool.py
autonomous: true
gap_closure: true
requirements:
  - PROJ-04
estimate:
  tokens: 20000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A dock tool-button click (the most common entry path) leaves the panel action checked, tools_panel.active_tool() == the clicked tool, the window action checked, and the toolbar button checked — including TWO consecutive dock clicks (VERIFICATION truths 9/10 / WR-02 restored; the probe's second-click total desync is gone)"
    - "The six window tool actions are checkable but NOT members of the ToolsPanel's exclusive QActionGroup (actionGroup() is None) — the panel group holds exactly its own six actions"
    - "The shortcut (V/B/R/L/E/G), Tools-menu, and programmatic set_active_tool paths still sync dock + toolbar (test_toolbar_buttons_track_active_tool passes with the flipped membership assertion)"
    - "The false group-membership claim in the _make_tool_toolbar_button docstring is corrected to describe the actual mechanism (checkable standalone actions driven by set_active_tool's explicit sync)"
  artifacts:
    - manga_ai_studio/gui/main_window.py :: six window tool actions standalone (checkable, ungrouped — the six membership lines added in 06-03 removed); set_active_tool window-action sync loop; _make_tool_toolbar_button docstring rewrite
    - tests/test_gui_crop_tool.py :: test_dock_button_click_syncs_dock_toolbar_and_window (dock-click regression); test_toolbar_buttons_track_active_tool membership assertion flipped (actionGroup() is None)
  key_links:
    - "tools_panel.tool_changed -> set_active_tool (main_window.py:2563) — the dock-click path: the panel's toggled-driven emission re-enters set_active_tool, whose explicit action-sync loop now drives the window actions and toolbar"
    - "set_active_tool action-sync loop -> the six action_tool_* checked states -> QToolButton mirror (defaultAction) — the group's exclusivity is no longer load-bearing for the toolbar"
  prohibitions:
    - "Window tool actions must not re-join the panel's exclusive QActionGroup (the 12-action group fought itself on dock clicks)"
    - "No new toggled connections on window actions (RESEARCH Pitfall 1 — the panel already connects toggled; double connections double-emit)"
    - "No lying comment: the _make_tool_toolbar_button docstring must describe the real sync mechanism"
---

<objective>
Close gap **WR-02 (warning)** from 06-VERIFICATION.md (truths 9/10 failed): dock tool-button clicks desync the dock/toolbar highlights. Plan 06-03 made the six WINDOW tool actions checkable members of the ToolsPanel's exclusive QActionGroup — which already held the panel's own six actions — so the group holds 12 mirrored checkable actions and the exclusivity fights itself. Probe-verified: after a dock Rectangle click the panel action ends up UNCHECKED (dock button loses its highlight), `tools_panel.active_tool()` falls back to ToolMode.MOVE, and a second dock click desyncs everything. The enumerated tested paths (shortcut/menu/programmatic `set_active_tool`) pass; the dock-click path is uncovered.

Purpose: restore the D-10 contract — exactly one tool checked in the panel, and the dock + toolbar always agreeing — on EVERY entry path, with the dock click explicitly regression-tested.

Output: the six window tool actions kept checkable but removed from the panel's exclusive group; `set_active_tool` drives their checked state explicitly (action-sync loop + the existing toolbar loop); the docstring tells the truth; the dock-click regression test (two consecutive clicks) locks the contract.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-VERIFICATION.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-REVIEW.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-03-PLAN.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/tools_panel.py
@tests/test_gui_crop_tool.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-GREEN — dock-button clicks sync dock + toolbar + window actions (WR-02 fix)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_crop_tool.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py — the six window tool action blocks (:714-769; each ends with the membership line added in 06-03), the Tools-menu addActions (:830-835), _make_tool_toolbar_button (:3123-3138 — the docstring claiming group membership that becomes false again), set_active_tool (:3140-3158 — the toolbar loop that only checks the matching button), the tool_changed connection (:2563)
    - manga_ai_studio/gui/tools_panel.py — the exclusive tool_group (:105-106), _make_tool_action (:206-221), _on_action_toggled (:229-242 — toggled → tool_changed), set_active_tool (:244-272), active_tool (:267-272)
    - tests/test_gui_crop_tool.py — test_toolbar_buttons_track_active_tool (:227-293, the membership loop at :259-267 that must flip), test_crop_is_sixth_exclusive_tool (:187-223 — PANEL-only, must stay untouched)
  </read_first>
  <behavior>
    - Test 1 (RED gate — the dock-click path): `window.tools_panel.action_rectangle.trigger()` (the QToolButton-click equivalent; the panel's toggled → tool_changed → set_active_tool chain is the real wiring at :2563); processEvents; assert: (a) `window.tools_panel.action_rectangle.isChecked()`, (b) `window.tools_panel.active_tool() == ToolMode.RECTANGLE`, (c) `window.action_tool_rectangle.isChecked()`, (d) `window.action_tool_move.isChecked() is False`, (e) the toolbar button whose defaultAction().data() == ToolMode.RECTANGLE is `isChecked()`, (f) `checked_tools() == [ToolMode.RECTANGLE]`. Then a SECOND dock click — `window.tools_panel.action_brush.trigger()` — and the same assertions for BRUSH (the probe showed the second click desyncs EVERYTHING pre-fix). MUST FAIL on current code: probe-verified panel action unchecked, active_tool() falls back to MOVE.
  </behavior>
  <action>RED: add `test_dock_button_click_syncs_dock_toolbar_and_window` to tests/test_gui_crop_tool.py, reusing the toolbar_btn/checked_tools helper pattern from test_toolbar_buttons_track_active_tool (:240-255). Run — must fail. GREEN in main_window.py: (1) remove the six membership lines added in 06-03 — each is the final statement of its action-creation block at lines 718, 726, 734, 742, 750, 769 — the window actions STAY `setCheckable(True)` but become standalone (outside the panel's exclusive group, so the group's exclusivity can never fight between the 12 mirrored actions). (2) In `set_active_tool` (:3140-3158), BEFORE the existing toolbar loop, add a window-action sync loop over the six `action_tool_*`: for each, `blockSignals` → `setChecked(act.data() == tool)` → unblock (explicitly check the target and uncheck the other five — the group no longer does this). Keep the existing toolbar loop (it re-checks the matching button; the QToolButtons mirror their default actions, so the buttons follow the actions). Do NOT connect toggled on any window action (RESEARCH Pitfall 1 — the panel already connects toggled at tools_panel.py:150; a double connection double-emits tool_changed). (3) Rewrite the `_make_tool_toolbar_button` docstring (:3123-3132): the group-membership claim is false again — state that the six window tool actions are standalone checkable-actions (not in the panel's exclusive group) whose checked state is driven explicitly by `set_active_tool`'s sync loop, and each toolbar button mirrors its default action. (4) Update tests/test_gui_crop_tool.py test_toolbar_buttons_track_active_tool: flip the membership loop at :259-267 to assert `action.actionGroup() is None` for all six window actions, and update the test docstring (:232-236) to describe the standalone-action + explicit-sync mechanism. test_crop_is_sixth_exclusive_tool is PANEL-only — untouched. Run — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_dock_button_click_syncs_dock_toolbar_and_window FAILS on the pre-fix code (RED gate — the probe's desync: panel action unchecked, active_tool() == MOVE) and PASSES post-fix, INCLUDING the second consecutive dock click
    - test_toolbar_buttons_track_active_tool passes with the flipped membership assertion: all six window actions `actionGroup() is None`; the shortcut/menu/programmatic sync assertions unchanged and green
    - test_crop_is_sixth_exclusive_tool passes unchanged (panel group intact with exactly its 6 actions, actionGroup() is panel.tool_group)
    - Grep gate: `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'tool_group.addAction'` == 0 (no window action joins the panel group)
    - Grep gate: `grep -c 'action_tool_.*setCheckable(True)' manga_ai_studio/gui/main_window.py` == 6 (the six window tool actions stay checkable — the unfiltered `setCheckable(True)` count is 11: 4 pre-existing non-tool actions at :519/:538/:553/:691, the 6 tool actions at :715-760, and btn.setCheckable(True) at :3135, so the gate is scoped to the action_tool_ lines)
  </acceptance_criteria>
  <done>Dock clicks, shortcuts, menus, and programmatic set_active_tool all leave dock + toolbar + window actions in agreement; the group holds only the panel's six actions.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 closure sweep — docstring truth + adjacent suites + full re-baseline</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_crop_tool.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py — the rewritten _make_tool_toolbar_button docstring (:3123-3132) — verify it describes the standalone-action + explicit-sync mechanism and contains no stale group-membership claim
    - tests/test_gui_canvas.py — test_tools_panel_tool_group_exclusive (:358-367, panel-only — must stay green)
    - tests/test_gui_image_dialogs.py — the dialog suites that share the MainWindow fixture (regression net for the set_active_tool change)
  </read_first>
  <action>Sweep-verify the WR-02 fix across the adjacent surfaces: (1) confirm the _make_tool_toolbar_button docstring now states the standalone checkable-action mechanism driven by set_active_tool's explicit sync (no group-membership claim remains — grep the exact phrase). (2) Run test_gui_crop_tool.py + test_gui_canvas.py (the panel-group exclusivity test is panel-only and must stay green) + test_gui_image_dialogs.py — all green. (3) Run the FULL suite: the count must be the 06-07 baseline plus this plan's additions (the new dock regression test), 0 failed — the wave merge gate proving no toolbar/dock behavior regressed anywhere (e.g. no test elsewhere relied on the window actions' group membership).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py tests/test_gui_canvas.py tests/test_gui_image_dialogs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Grep gate (docstring truth): `grep -c 'standalone checkable-action' manga_ai_studio/gui/main_window.py` >= 1 — the docstring names the real mechanism
    - Grep gate (no stale claim): `grep -c "members of the ToolsPanel" manga_ai_studio/gui/main_window.py` == 0 — the false group-membership claim is gone (comments filtered via grep -v '^#' if needed)
    - test_tools_panel_tool_group_exclusive (tests/test_gui_canvas.py:358) still passes — the panel's own group behavior untouched
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — the 06-07 baseline count plus this plan's additions, 0 failed
  </acceptance_criteria>
  <done>The docstring describes the real mechanism, the adjacent suites are green, and the full suite re-baselines with the dock regression test counted.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/main_window.py`:
  - Six window tool actions (`action_tool_move/brush/rectangle/lasso/eraser/crop`) — checkable, STANDALONE (the six membership lines from 06-03 removed); the panel's exclusive group holds exactly its own six actions
  - `set_active_tool` — new window-action sync loop (check target, uncheck the other five, blockSignals-guarded) before the existing toolbar loop
  - `_make_tool_toolbar_button` — docstring rewritten to the standalone-action + explicit-sync truth
- `tests/test_gui_crop_tool.py` (extended):
  - `test_dock_button_click_syncs_dock_toolbar_and_window` — dock-click regression (two consecutive clicks: panel action checked, active_tool() == clicked tool, window action checked, toolbar mirror, all-others unchecked)
  - `test_toolbar_buttons_track_active_tool` — membership loop flipped to `actionGroup() is None`; docstring updated

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dock button click / toolbar click / shortcut / menu → tool state | User activation crosses into the tool-state sync; a desync leaves the UI lying about the active tool and active_tool() reporting the wrong mode (state-integrity, not a security boundary — no untrusted data crosses) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Tampering | set_active_tool sync / QActionGroup wiring | medium | mitigate | Window actions ungrouped (panel group = its 6 actions only); set_active_tool's action-sync loop + toolbar loop drive all three surfaces explicitly; dock-click regression test (two consecutive clicks) + the flipped membership assertions lock every entry path (ASVS V7 state integrity) |
| T-06-13 | Tampering | _make_tool_toolbar_button docstring | low | mitigate | Docstring rewritten to the actual mechanism; grep gates assert the stale group-membership claim is gone and the standalone mechanism is named (no lying comment remains) |
| T-06-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py -x -q` — dock-click regression + flipped membership assertions green (RED→GREEN proven on the probe desync)
- Adjacent suites: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py tests/test_gui_canvas.py tests/test_gui_image_dialogs.py -x -q`
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 598 + this plan's additions passed, 0 failed
- Grep gates: zero membership calls on window actions; 6x `action_tool_.*setCheckable(True)`; docstring truth gates
</verification>

<success_criteria>
- Truths 9/10 ("Exactly one checkable window tool action is checked at all times; toolbar mirrors on every entry path" / "Toolbar and Tools dock always show the same active tool") are PASS-able: the dock-click path — the most common entry — is explicitly regression-tested, including the second-click desync the probe found
- The panel's exclusive group holds exactly its own six actions; window actions are checkable standalone
- Full suite green; no new dependencies
</success_criteria>

<output>
Create `.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-08-SUMMARY.md` when done
</output>

<!-- planner-discipline-allow: tool_group.addAction -->
