---
phase: 06-refinement-polish-deferred-fixes-full-curve-editor
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - manga_ai_studio/gui/curves_dialog.py
  - tests/test_gui_curves_dialog.py
autonomous: true
requirements:
  - PROJ-04
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Curves dialog opens at defaults: Linear curve (0 interior points), endpoints (0,0)/(255,255), gamma 1.00, channel RGB, In/Out at the selected point (UI-SPEC E1 populated covered)"
    - "Every curve/slider/gamma/channel/preset change fires ONE preview_callback with the composed LUT result — live in-place preview on the capture-suppressed canvas path (UI-SPEC populated covered)"
    - "No control state can diverge from the curve: black↔left-endpoint and white↔right-endpoint bidirectional sync with cross-clamp (black max = white−1, white min = black+1), gamma↔midpoint (sampled output at input 128) sync, all guarded by _updating (UI-SPEC partial covered)"
    - "Channel state is independent: RGB/R/G/B switcher edits separate point sets; the preview composes master then per-channel (out_c = channel_lut_c[master_lut[v]]) (UI-SPEC partial covered)"
    - "No interaction can produce an out-of-range point: all coordinates clamp to [0,255]², x-order preserved between neighbors (add/drag/In-edit), endpoints fixed at x=0/255, In spinbox range [prev_x+1, next_x−1], Out 0..255 (UI-SPEC overflow covered)"
    - "The widget handles 0 and N interior points uniformly; endpoints can never be deleted; exactly one point selected at a time (Tab/Shift+Tab cycles; adding selects) (UI-SPEC zero-one-many covered)"
    - "Keyboard story (D-07): arrows nudge the selected point ±1 (Shift=±10), Tab/Shift+Tab cycle selection, In/Out spinboxes drive the selected point"
    - "Histogram (D-08) computed ONCE at dialog open from the detached page_image (np.histogram, luminance for master, plane for R/G/B) — never recomputed during editing (concurrency probe edge: no background thread touches the image mid-dialog; the GUI thread owns all mutation)"
  artifacts:
    - manga_ai_studio/gui/curves_dialog.py :: CurveWidget + CurvesDialog (collector + preview driver, D-01..D-08)
    - tests/test_gui_curves_dialog.py :: dialog contract + widget interaction tests
  key_links:
    - "CurveWidget.points_changed/point_selected signals -> dialog._refresh -> preview_callback (the single driver)"
    - "curves_page(base, master_points, channel_points) from plan 06-01 — the composition the preview runs"
    - "gamma mapping: gamma = sampled output at input 128; y = round(255 * 0.5^(1/g)); back-mapping g = ln(0.5)/ln(y/255) (A3)"
  backstops:
    - statement: "curve drags are fluid and the grid/handles remain legible at all supported DPI settings"
      verification: backstop
assumption_delta: promote
  prohibitions:
    - "The dialog NEVER mutates models — collector + preview driver only (Pitfall 9); no undo pushes originate here"
    - "Pitfall-2 .copy() discipline: page_image, preview base, and histogram source detached before use"
    - "A degenerate curve must never render an inverted map (T-05-07 backstop; curve_lut already guarantees it)"
    - "No new dependencies — QPainter + numpy only"
---

<assumption_delta_decision>
**Decision: `promote`** — the Curves dialog (generalized tonal-adjustment surface) is promoted to the primary adjustment surface; the Levels black/white/gamma sliders demote to quick-access rows INSIDE the Curves dialog (06-CONTEXT D-01, which preserves the Levels learning curve). Per the assumption-delta checkpoint recommendation: do NOT add the Curves dialog alongside a still-required standalone Levels dialog — plan 06-05 deletes gui/levels_dialog.py after migration. The identity model now names the Curves dialog (`action_curves`/`_on_curves`) as the primary noun; no standalone Levels surface survives this phase (accepted debt: none — the promote is fully executed here).
</assumption_delta_decision>

<objective>
Build the full draggable curve editor dialog — `gui/curves_dialog.py` with the custom `CurveWidget` (QPainter grid/diagonal/histogram/points) and the `CurvesDialog` collector+preview-driver (D-01…D-08, PROJ-04 "curves" half). This is the dialog that REPLACES the Levels dialog (D-01): black/white/gamma quick-access rows kept above the grid, preset row, channel switcher (RGB/R/G/B), In/Out numeric row, luminance histogram, full keyboard story — with the curve as the single source of truth (D-02/D-03).

Purpose: land the curve editor surface (UI-SPEC surface 30) as a pure collector that plan 06-05 wires into `_on_curves`. The pixel math comes from plan 06-01's `curves_page`.

Output: `CurvesDialog` + `CurveWidget` in curves_dialog.py, with the dialog-contract and widget-interaction test suite in tests/test_gui_curves_dialog.py (the Wave-0 gap file).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-CONTEXT.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-RESEARCH.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-PATTERNS.md
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-UI-SPEC.md
@manga_ai_studio/gui/levels_dialog.py
@manga_ai_studio/core/image_ops.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: CurveWidget — the QPainter curve control (paint + mouse + keyboard + signals)</name>
  <files>manga_ai_studio/gui/curves_dialog.py, tests/test_gui_curves_dialog.py</files>
  <read_first>
    - 06-RESEARCH.md — Common Operation 3 (the CurveWidget skeleton: QWidget subclass, StrongFocus, paintEvent with QPainter+Antialiasing, mousePress/Move/Release, mouseDoubleClickEvent, keyPressEvent; points_changed/point_selected signals)
    - 06-UI-SPEC.md — surface 30 interaction contract (D-04 click-add/drag/double-click-delete, endpoints fixed y-only; D-07 keyboard; §Spacing exceptions: 256-unit grid, 64/16 gridlines, 10px hit radius, 8x8/10x10 handles, 2px accent curve, 280x240 min, 0.14 histogram alpha)
    - manga_ai_studio/gui/levels_dialog.py — the `_DIALOG_QSS` + dialog skeleton the new file extends (collector pattern reference)
  </read_first>
  <action>Create `manga_ai_studio/gui/curves_dialog.py` with the `CurveWidget(QWidget)` class FIRST (this task) — the file will hold both classes; the dialog class lands in Task 2/3. CurveWidget per RESEARCH Common Operation 3 + UI-SPEC surface 30: class signals `points_changed = Signal()` and `point_selected = Signal(int)`; `__init__(self, parent=None)` with `setFocusPolicy(Qt.FocusPolicy.StrongFocus)`, `self._points: list[tuple[int, int]] = [(0, 0), (255, 255)]`, `self._selected = 0`, `self._histogram: np.ndarray | None = None`, `setMinimumSize(280, 240)`. `paintEvent`: QPainter(self) + Antialiasing; plot square = largest square fitting the widget minus 8px margin; gridlines at 64-unit major / 16-unit minor intervals (1px, divider #3a3a42, minor reduced alpha); muted diagonal (#9a9aa2); faint histogram bars (rgba(232,232,234,0.14)) when _histogram is set; accent 2px curve polyline via QPainterPath through mapped points; interior point handles 8x8 (#e8e8ea fill, #3a3a42 border; selected = 2px accent #00d4ff outline); endpoints 10x10 (visually distinct, y-draggable only). Coordinate mapping helpers: unit-space [0,255]² <-> widget plot rect (invert y). Mouse: `mousePressEvent` hit-tests existing points within 10px -> select (emit point_selected) + start drag; else add a point at the clicked (input, output) clamped to [0,255]² with x strictly between neighbors (endpoints y-only) and select it (emit points_changed + point_selected). `mouseMoveEvent` while dragging: move the selected point with the same clamps. `mouseReleaseEvent`: end drag (emit points_changed). `mouseDoubleClickEvent`: delete the point under the cursor (never an endpoint) — emit points_changed. `keyPressEvent` (D-07): arrows nudge the selected point ±1 (Shift=±10) with the same clamps; Tab/Shift+Tab cycle selection over all points; update() after every mutation.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test file exists with widget tests: paint smoke (paintEvent runs without error on a shown widget), click-add creates a point at the mapped coordinate and selects it, drag-move clamps x between neighbors and y to [0,255], double-click deletes an interior point but never an endpoint, arrow keys nudge ±1 (Shift ±10), Tab cycles selection, endpoints never move horizontally (drag at an endpoint changes only y)
    - All widget tests pass (pytest-qt)
    - `setMinimumSize(280, 240)` in the constructor
  </acceptance_criteria>
  <done>CurveWidget renders the grid/diagonal/histogram/curve and supports the full D-04/D-07 interaction contract with signals.</done>
</task>

<task type="auto">
  <name>Task 2: CurvesDialog collector — quick-access rows, bidirectional sync, In/Out spins</name>
  <files>manga_ai_studio/gui/curves_dialog.py, tests/test_gui_curves_dialog.py</files>
  <read_first>
    - manga_ai_studio/gui/levels_dialog.py — the collector contract in full: `_updating` guard + `_refresh` clamp (:211-234), gamma log-slider mapping `_gamma_to_slider`/`_slider_to_gamma` (:190-208, reuse verbatim), slider↔spin direct setValue links (:164-169), `result_values` + `_on_apply` (:244-247), `_DIALOG_QSS` (:46-77, extend)
    - 06-UI-SPEC.md — surface 30 layout (preset row top, black/white quick rows ABOVE the grid, gamma + In/Out below, Cancel/Apply bottom) and the sync contract (D-02/D-03 with clamps; gamma = sampled output at input 128)
  </read_first>
  <action>Add `CurvesDialog(QDialog)` to curves_dialog.py per the Levels collector shape: `__init__(self, parent=None, page_image=None, preview_callback=None)` with `setWindowTitle("Curves")`, `setObjectName("curves_dialog")`, `setModal(True)`, `self._page_image = page_image` (the caller's detached base), `self.preview_callback = preview_callback`, `self._updating = False`, and the 14px Body base font (QFont.setPixelSize(14)) per D-12. Layout top→bottom per UI-SPEC surface 30: preset row (Linear/S-curve/Brighten/Darken QPushButtons — wiring in Task 3), channel row (RGB/R/G/B — Task 3), black-point row (QSlider+QSpinBox, 0..255 default 0, synced to the curve's left endpoint output), white-point row (0..255 default 255, right endpoint), the CurveWidget (stretch), gamma row (log-scaled QSlider + QDoubleSpinBox 0.10..4.00 default 1.00, synced to the midpoint anchor), In/Out rows (QSpinBox pair for the selected point; In range [prev_x+1, next_x−1], Out 0..255), [Cancel][Apply] buttons (Apply default). State: per-channel point sets — `self._channel_points: dict[str, list[tuple[int,int]]]` keyed "RGB"/"R"/"G"/"B", initialized to [(0,0),(255,255)]; `self._current_channel = "RGB"`. The CurveWidget edits `self._channel_points[self._current_channel]`. Bidirectional sync (D-02/D-03, ONE `_refresh` guarded by `_updating`): dragging the black/white sliders sets the endpoints' y in the current channel's point list; the In/Out spins drive the selected point; gamma g positions y = round(255 * 0.5^(1/g)) at input 128 (injecting/moving the (128,y) point); dragging the point at/near input 128 back-maps g = ln(0.5)/ln(y/255); clamps: black max = white−1 / white min = black+1 mirror on the slider/spin pairs, endpoints x fixed at 0/255. `result_values` payload: the collector returns the full state `self.result_values` = (master_points, channel_points) as `tuple[list[tuple[int,int]], dict[str, list[tuple[int,int]]]]` — plan 06-05's `_on_curves` reads it. `_on_apply` stores result_values + accept(); Cancel/close = reject() (the caller restores). `_refresh()` fires `preview_callback(self._current_values())` when set (values shape: (master, channels) — the Task 3 composition hooks it).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Dialog tests pass: defaults (0/255/1.00, Linear, RGB), black slider drag moves the left endpoint y and white min clamps to black+1 (mirror of test_levels_defaults_and_clamp), In spinbox range respects neighbors, Out range 0..255, gamma 1.00 ↔ midpoint 128, dragging the midpoint updates the gamma spin, _on_apply stores result_values and accepts, reject does not store
    - `QFontInfo(dialog.font()).pixelSize() == 14` (D-12, Curves ships at 14px from birth)
    - Preview callback fires on control changes with the (master, channels) payload (values shape asserted in the test)
  </acceptance_criteria>
  <done>CurvesDialog collects the full curve state with bidirectional slider↔endpoint and gamma↔midpoint sync, In/Out spins, and 14px typography.</done>
</task>

<task type="auto">
  <name>Task 3: Presets + channel switcher + histogram + preview composition driver</name>
  <files>manga_ai_studio/gui/curves_dialog.py, tests/test_gui_curves_dialog.py</files>
  <read_first>
    - 06-UI-SPEC.md — D-05 preset row copy + A2 preset coordinates (S-curve [(0,0),(64,40),(192,215),(255,255)], Brighten [(0,0),(128,150),(255,255)], Darken [(0,0),(128,105),(255,255)]), D-06 channel switcher (exclusive checkable QToolButtons RGB/R/G/B, accent checked state), D-08 histogram (luminance 0.299R+0.587G+0.114B for master, plane for R/G/B; np.histogram bins=256 range=(0,256); computed ONCE at open; normalized-linear bars)
    - 06-RESEARCH.md — Pitfall 5 (histogram source aliasing — compute from the detached page_image with defensive .copy()), A7 (luminance weights), A1 (composition order master→channel)
  </read_first>
  <action>Wire the remaining D-05/D-06/D-08 surfaces in CurvesDialog: (1) PRESETS — the preset row QPushButtons (copy per UI-SPEC: "Linear" tooltip "Reset the curve to a straight line.", "S-curve" "Add contrast — classic S-curve.", "Brighten" "Brighten midtones.", "Darken" "Darken midtones."); clicking replaces the CURRENT channel's point set with the A2 coordinates (Linear = [(0,0),(255,255)]) and stays fully editable afterwards (starting points, never locked — the points remain draggable); after a preset, _refresh + preview fire. (2) CHANNEL SWITCHER — four exclusive checkable QToolButtons (RGB/R/G/B) in a QButtonGroup(exclusive) (or QActionGroup mirroring the ToolsPanel pattern); label "Channel:" (Small muted 12px); switching loads the corresponding point set into the CurveWidget (per-channel state preserved in _channel_points) and re-renders. (3) HISTOGRAM (D-08) — in __init__, compute ONCE: `np.histogram(luminance_of(page_image), bins=256, range=(0, 256))` where luminance = 0.299R+0.587G+0.114B for the RGB master; use the per-channel plane when a channel is selected (recompute on channel switch? NO — D-08 "computed once at dialog open"; store the per-channel histograms + master histogram as three arrays computed at open, and pass the matching one to the widget on channel switch); normalize bins to the plot height (max bin = full height) and hand the array to CurveWidget._histogram; when page_image is None, leave the histogram unset (widget paints without it). (4) PREVIEW DRIVER — `_refresh` composes via `image_ops.curves_page(self._page_image, master_points, channel_points)` and calls `self.preview_callback(composed)` when a callback is set — the single driver every control change funnels into.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass: preset click replaces the current channel's points with the A2 coordinates and the points remain editable (drag after preset moves them), Linear restores the pure diagonal, channel switcher preserves per-channel edits (edit R, switch to G, switch back — R points intact), histogram computed once (np.histogram result captured at construction; editing never recomputes — assert the stored histogram array identity is unchanged after a point drag), preview_callback receives curves_page output composed master→channel (byte-exact vs image_ops.curves_page on the same inputs)
    - All UI-SPEC copy strings for presets + channel labels match verbatim
    - `_refresh` is the single preview driver (no other preview_callback call sites in the file)
  </acceptance_criteria>
  <done>Presets, channel independence, once-at-open histogram, and the composed preview driver are wired and tested.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/curves_dialog.py` (NEW):
  - `CurveWidget(QWidget)` — signals `points_changed = Signal()`, `point_selected = Signal(int)`; attrs `_points: list[tuple[int,int]]`, `_selected: int`, `_histogram: np.ndarray | None`; paintEvent (grid 64/16, diagonal, histogram, 2px accent curve, 8x8 interior / 10x10 endpoint handles), mousePress/Move/Release/DoubleClickEvent (10px hit-test, add/drag/delete, x-order clamps, endpoints y-only), keyPressEvent (arrows ±1/±10, Tab/Shift+Tab)
  - `CurvesDialog(QDialog)` — collector + preview driver; attrs `result_values: tuple[list, dict]`, `_channel_points: dict[str, list[tuple[int,int]]]`, `_current_channel: str`, `_updating: bool`, `preview_callback`, black/white/gamma rows, In/Out spins, preset buttons, channel QToolButtons, histogram arrays; methods `_refresh`, `_gamma_to_slider`/`_slider_to_gamma` (reused from Levels), `_on_apply`; `_DIALOG_QSS` extended
- `tests/test_gui_curves_dialog.py` (NEW): widget interaction tests (add/drag/delete/nudge/Tab/endpoint rules/paint smoke) + dialog contract tests (defaults, clamps, sync, spins, presets, channels, histogram-once, preview composition, 14px font)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curve widget mouse/key input → point state | Untrusted interaction (drag beyond bounds, double-clicks on endpoints, extreme nudge) crosses into point coordinates |
| dialog controls → preview_callback | Every control change drives the composed preview onto the canvas (display mutation only, no model writes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | CurveWidget mouse/key handlers | medium | mitigate | All mutations clamp to [0,255]² with x-order preservation; endpoints y-locked (D-04); In spinbox range [prev_x+1, next_x−1]; curve_lut's T-05-07 backstop is the last line (never NaN/inverted) |
| T-06-06 | DoS | histogram recompute / preview storm | medium | mitigate | Histogram computed ONCE at open (D-08, RESEARCH Pitfall 5); preview cost equals the Levels precedent (accepted); the dialog never mutates models (Pitfall 9) |
| T-06-SC | Tampering | pip installs | high | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_curves_dialog.py -x -q` — the full new dialog suite green
- Grep gate: preview_callback called only from `_refresh` (single driver)
- Full-suite spot check at wave merge: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
</verification>

<success_criteria>
- CurveWidget + CurvesDialog implement D-01…D-08 fully (interaction, sync, presets, channels, In/Out, keyboard, histogram)
- Collector contract honored: no model mutation, no undo pushes, single preview driver
- 14px Body typography from birth; all UI-SPEC surface-30 copy verbatim
- All tests green; no new dependencies
</success_criteria>

<output>
Create `.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-04-SUMMARY.md` when done
</output>
