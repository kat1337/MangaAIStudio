---
phase: 08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate
plan: 02
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - tests/test_gui/test_inpaint_flow.py
autonomous: true
requirements: [MASK-02, MASK-03]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Inpaint (C) does fill + LaMa in one shot: Auto low-std and forced-fill boxes are median-color filled, Auto high-std + forced-inpaint + manual-plane strokes (minus erase) are LaMa-inpainted via patched loop when page > max, all baked into one image with ONE undo entry"
    - "No mask content before/after: fill-only pages skip LaMa, inpaint-only pages skip fill, both-empty passthrough unchanged, large pages use one-patch-at-a-time ordering with progress '{n} of {N}'"
    - "boxes_snapshot snapshot-as-geometry carries fill_color; geometry ops invalidate fill_color alongside mask/std_dev so rotated/cropped boxes never reuse stale color"
  artifacts:
    - manga_ai_studio/gui/main_window.py (inpaint dispatch, _run_inpaint_task, _on_inpaint_finished single undo, progress, _INPAINT_OVERRIDE_TO_MODEL fill branch)
    - manga_ai_studio/gui/canvas.py (boxes_snapshot fill_color forward, set_image_from_numpy bbox composite already composes patch union)
  key_links:
    - "GUI extracts on GUI thread: image_rgb .copy() + boxes_snapshot() + manual/erase binaries -> worker builds fill_specs + inpaint_binary from snapshot -> inpaint_patches via inpaint_fn -> return image + union bbox -> _on_inpaint_finished captures pre-inpaint slice then push_image_action once then set_image_from_numpy(bbox) then consume_mask_display signal-silent"
    - "Threshold change path recomposes without refit: compose_fill_binary + compose_auto_binary from snapshot; snapshot-as-geometry never writes box back to live pageboxes (OCR id-routing invariant)"
---

<objective>
Wire the interactive Inpaint (C) one-shot fill+LaMa flow with OOM-safe patching and a single undo entry, expanding the headless tracer into the GUI layer.

Purpose: This is the user-visible core of D-03/D-05..D-08 — the Inpaint key must cheap-fill uniform text and AI-inpaint complex text on the same run, without OOM on large pages, with one Ctrl+Z reversal and no signal-silent-resurrect bug.
Output: Patched, single-undo Inpaint wired through Worker off the GUI thread, snapshot-as-geometry throughout, progress reporting, and the fill/never branches correct.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-CONTEXT.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-RESEARCH.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/core/detection_boxes.py
@manga_ai_studio/core/inpaint_patching.py
@manga_ai_studio/core/box_model.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inpaint single-shot worker: fill pass + patched LaMa + union bbox</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui/test_inpaint_flow.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (inpaint 4889-5005, _run_inpaint_task 4937-4964, _on_inpaint_finished 4975-5058, compute_mask_bbox 6564-6584, _INPAINT_OVERRIDE_TO_MODEL ~3745, _dispatch_batch 6074-6189)
    - manga_ai_studio/core/detection_boxes.py (compose_auto_binary, compose_fill_binary new helper, BoxMaskFit.median_color)
    - manga_ai_studio/core/inpaint_patching.py (plan_patches + inpaint_patches API from 08.1-01)
    - manga_ai_studio/gui/canvas.py (get_image_numpy 871-910 .copy(), boxes_snapshot 2141-2196, set_image_from_numpy 922-924 bbox composite, consume_mask_display 686-714, recompose_mask 582-612)
    - panelcleaner/image_ops.py (convert_mask_to_rgba 73-76, compose shape)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (main_window analog 370-469, Single Undo 812-824, .copy() 827-837)
  </read_first>
  <behavior>
    - Test: worker path split — fill_specs = [(mask,color,xy) for pb where inpaint_state is will_fill/forced_fill], inpaint_binary = (manual_bin | high-std Auto | always) & ~erase_bin: low-std Auto box goes to fill_specs not inpaint_binary, high-std Auto goes to inpaint_binary not fill, forced_fill always fill, forced_inpaint always inpaint, never neither, manual always inpaint minus erase. Assert with deterministic PIL masks and synthetic std_dev/overrides without a real model.
    - Test: fill application — per fill_spec build RGBA layer via convert_mask_to_rgba(mask, color) -> alpha_composite at (x1,y1) -> paste onto page copy (masker.py 104/138 exact sequence); assert filled pixels equal fill_color inside mask and page outside masks pixel-exact.
    - Test: patched inpaint delegation — when image max side > max_size (e.g. 3000x3000 page, max 2048), worker calls inpaint_patches with max_size, page RGB after fill, inpaint_binary, and model.inpaint as inpaint_fn; when page fits (e.g. 1000x1000), fast path calls model.inpaint once. Fake model that asserts input w/h <= max_size.
    - Test: single undo contract — worker returns {image: result_rgb, bbox: union_bbox (x,y,w,h) of fill+inpaint coverage or patch union, fill_count, inpaint_count}; _on_inpaint_finished captures pre_inpaint[y:y+h,x:x+w].copy() before set_image_from_numpy(result, bbox), calls history.push_image_action once, composites only bbox region, and status reflects fill vs inpaint vs both.
  </behavior>
  <action>
    Per D-03 one-shot fill + LaMa with ONE undo entry, per D-05..D-08 patched inpaint. Refactor MainWindow.inpaint() to extract on GUI thread: image_rgb via canvas.get_image_numpy() (already .copy()-detached), boxes_snapshot() (detached PageBoxes carrying mask/std_dev/fill_color), manual_bin/erase_bin via mask_to_numpy_binary on canvas planes (or planes_snapshot), max_size from profile_manager.config.current_profile.masker.max_inpaint_resolution as (max,max) tuple, model via backend_factory inpainting. Dispatch Worker(self._run_inpaint_task, ...). Rewrite _run_inpaint_task to run off GUI thread (no Qt): compute threshold from masker_conf, build fill_specs and inpaint_binary from snapshot using compose_fill_binary / compose_auto_binary siblings plus manual/erase numpy composition (manual|auto>& ~erase mirroring canvas.recompose_mask 582-612), run fill pass headlessly via PIL convert_mask_to_rgba+alpha_composite+paste if fill_specs non-empty, then run inpaint_patches if inpaint_binary any else skip LaMa, compute union bbox via compute_mask_bbox over fill+inpaint coverage or patch rect union, return dict with image+bbox counts. Keep load-once (model.load once), one patch live at a time, progress_cb emits (50+40*n//total, f"Inpainting {n} of {total}…") per patch. Do not emit mask_modified during fill; fill is baked into page image not overlay. Use fill per user decision D-01.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui/test_inpaint_flow.py -q</automated>
  </verify>
  <done>Worker does fill via vendored PIL sequence plus patched LaMa via inpaint_patches, respects max_size cap, partitions fill vs inpaint per inverted gate + manual/erase, and returns union bbox for single undo.</done>
  <acceptance_criteria>
    - Low-std Auto and fill override produce fill_specs and are absent from inpaint_binary; high-std Auto and always plus manual minus erase produce inpaint_binary.
    - Fill applies median_color via convert_mask_to_rgba + alpha_composite + paste, pixel-exact outside masks.
    - Large page delegates to inpaint_patches with max 2048 cap; small page fast-path calls model once; fake model never receives input exceeding cap.
    - Worker result contains image and union bbox covering fill+inpaint; no per-patch undo pushed inside worker.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Single-undo finish, progress, snapshot-as-geometry and canvas forwarding</name>
  <files>manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/canvas.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_on_inpaint_finished 4975-5058, _on_inpaint_progress, _on_inpaint_cleanup, history_manager push_image_action 211-226)
    - manga_ai_studio/gui/canvas.py (boxes_snapshot 2141-2196 materializing Box(int...), get_image_numpy, set_image_from_numpy 912-1036 bbox composite, consume_mask_display 686-714 signal-silent)
    - manga_ai_studio/core/box_model.py (copy detachment, payload/style/mask)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (Snapshot-as-Geometry 792-802, Signal-Silent Consumption 803-811)
  </read_first>
  <action>
    In _on_inpaint_finished (preserving CR-03 capture-before-overwrite): capture pre_inpaint = canvas.get_image_numpy().copy() on entry, slice original_patch = pre_inpaint[y1:y1+bh, x1:x1+bw].copy() (mandatory .copy(), Pitfall 2) where bbox is (x,y,w,h) union from worker or compute_mask_bbox fallback, push ONE history.push_image_action(x,y,original_patch) (D-03/D-08 mandate — never per patch), then canvas.set_image_from_numpy(result_rgb, bbox=bbox) so only bbox region composites (non-masked pixels untouched per canvas 922-924), persist into image_files[idx].current_image detached via .copy(), call canvas.consume_mask_display() signal-silent (must NOT emit mask_modified, CR-16 2-stack contract), update status/verdict counts (fill vs inpaint). Extend _on_inpaint_progress to render Inpainting… {percent}% ({n} of {total}) per discretion. In canvas.py extend boxes_snapshot to forward fill_color (tuple immutable) alongside mask/std_dev/inpaint_override/style; verify core/image_ops geometry ops already invalidate fill_color from Task 01 (if not, add there). Ensure no live pagebox.box is written back — recompose consumers read snapshot only (CR-01 stale geometry guard sibling). Keep _mask/_auto_bin not set for fill (fill never enters overlay; auto plane shows only inpaint-eligible content per Research Pitfall 5).
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui/test_inpaint_flow.py tests/test_gui/test_history_geometry.py -q</automated>
  </verify>
  <done>Finish handler pushes exactly one undo entry for the union bbox, composites only that bbox, leaves non-masked pixels pixel-exact, clears overlay signal-silently, and boxes_snapshot carries fill_color through undo/geometry cycles.</done>
  <acceptance_criteria>
    - _on_inpaint_finished pushes history once with (x,y,patch.copy()) of the union bbox and calls set_image_from_numpy with that bbox; history depth grows by one per Inpaint run even across multiple patches.
    - canvas boxes_snapshot returns PageBoxes whose fill_color equals the fitted median_color; after rotate/crop/resize the snapshot's fill_color is None until re-fit.
    - consume_mask_display does not emit mask_modified; patch progress status shows "{n} of {N}" when patched.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker -> GUI (image+bbox) | Patched result crosses thread boundary, bbox must be validated before slicing history patch |
| canvas -> history (patch buffers) | Undo patch must be .copy()-detached so QImage GC cannot alias numpy OWNDATA |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-02-01 | Tampering | main_window _on_inpaint_finished bbox | medium | mitigate | Validate bbox is 4-tuple ints within page bounds before slicing pre_inpaint patch; clamp and reject negative w/h as programming error (no allocation from untrusted bbox) |
| T-08.1-02-02 | Information Disclosure | worker progress callback | low | accept | Progress emits only percent + patch count strings, no paths or PII; GUI sanitizes before status bar |
| T-08.1-02-03 | Denial of Service | canvas set_image_from_numpy bbox composite | medium | mitigate | Bbox composite validates (x,y,w,h) ints and that patch shape equals (h,w,3); mismatch raises ValueError before history push, preserving 2-stack contract |
| T-08.1-02-SC | Tampering | package installs | high | mitigate | No new packages; verification uses fake inpaint_fn, not real torch model in tests |

</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui/test_inpaint_flow.py -q` passes including fake-model cap assertion (no input >2048)
- Manual: open a page with one uniform box (std ~5) and one complex box (std ~35) threshold 15 -> run Inpaint (C) -> uniform text fills with median color, complex text LaMa-inpainted, single Ctrl+Z undoes both, Redo restores, overlay cleared without mask_modified emission (check history depth +1 only)
- Manual: large page ~6000px with patching -> status shows Inpainting 1 of N / 2 of N etc., result has no visible seam where halo overlap required
</verification>

<success_criteria>
- Interactive Inpaint is one-shot fill+patched LaMa with one undo entry, snapshot-as-geometry throughout, progress per patch, and headless fill/inpaint helpers remain Qt-free; patched and whole-page paths produce identical results outside the union bbox.
</success_criteria>

<output>
Create `.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-02-SUMMARY.md` when done
</output>
