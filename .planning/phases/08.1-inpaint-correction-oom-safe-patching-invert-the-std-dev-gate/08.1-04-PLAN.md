---
phase: 08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate
plan: 04
type: execute
wave: 3
depends_on: ["08.1-01", "08.1-02", "08.1-03"]
files_modified:
  - manga_ai_studio/core/batch_runner.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_core/test_batch_runner.py
  - tests/test_gui/test_batch_inpaint_parity.py
  - tests/test_gui/test_detection_settings_tooltip.py
autonomous: true
requirements: [MASK-02, MASK-03]
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Batch Detect+Clean and Batch Clean adopt the same corrected gate and patched inpainting as interactive: same fill + patched LaMa split, same max cap, same manual/erase handling, closing the batch-interactive quality parity item per D-09"
    - "No OOM on large pages in batch or interactive: every LaMa call respects max_inpaint_resolution, one patch live at a time, whole-page fast path when fits, patched composite pixel-exact outside union bbox"
    - "Existing gate-dependent tests that encoded the old direction (~7 files) are re-based to D-01 inverted semantics and remain green; new headless purity and patch invariants are covered"
  artifacts:
    - manga_ai_studio/core/batch_runner.py (clean branches use gate+fill+inpaint_patches, max_inpaint_size param threaded)
    - manga_ai_studio/gui/main_window.py (_dispatch_batch threads masker_conf + max_size to worker, batch progress parity)
    - tests/test_core/test_batch_runner.py (re-based corrected gate + patched batch)
  key_links:
    - "batch_runner imports inpaint_patching headlessly (no Qt/torch) and reuses compose_fill/compose_auto + fill PIL sequence identical to interactive worker"
    - "_dispatch_batch threads profile.masker.max_inpaint_resolution alongside masker_conf (additive param, default 2048 keeps callers working) into _run_batch_task clean branches"
    - "Batch dirty marking covers result and cancel/abort paths; per-page failures logged and continued, never abort whole batch"
---

<objective>
Batch parity (D-09) + OOM-guard hardening across interactive and batch + re-basing the remaining gate-dependent tests, closing the phase.

Purpose: Interactive and batch must produce the same visual quality (fill vs inpaint gate) and the same memory safety (patching); the ~7 test files that locked the old gate must be flipped so CI reflects CONTEXT D-01 authority.

Output: Batch clean uses corrected gate + fill + patched loop, dispatched with the profile cap, with parity tests and full suite green.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-CONTEXT.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-RESEARCH.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md
@manga_ai_studio/core/batch_runner.py
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/core/detection_boxes.py
@manga_ai_studio/core/inpaint_patching.py
@manga_ai_studio/core/project_io.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch corrected gate + fill + patched inpaint (D-09 parity)</name>
  <files>manga_ai_studio/core/batch_runner.py, manga_ai_studio/gui/main_window.py, tests/test_core/test_batch_runner.py</files>
  <read_first>
    - manga_ai_studio/core/batch_runner.py (_run_batch_task docstring invariants 103-248, _read_image_bgr 84-101, clean site 232-241, empty-mask gate 224-226, progress/abort/cancel contracts)
    - manga_ai_studio/gui/main_window.py (_dispatch_batch 6074-6189 threading masker_conf, _on_batch_progress, _on_batch_finished, _refresh_current_page_after_batch)
    - manga_ai_studio/core/detection_boxes.py (compose_auto_binary, compose_fill_binary)
    - manga_ai_studio/core/inpaint_patching.py (inpaint_patches contract from 08.1-01)
    - panelcleaner/image_ops.py (convert_mask_to_rgba fill sequence)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (batch_runner assignment 308-367)
  </read_first>
  <behavior>
    - Test: batch_clean with two boxes (low std 5 vs high std 30, threshold 15, manual_bin empty) -> result saved: low-std region pixels equal fill_color, high-std region was processed via inpaint_fn (fake model paints with distinct color), both present in output. Uses fake inp_model that asserts input w/h <= max_size and records patch count.
    - Test: batch large-page cap: page 4000x3000 masks far apart, max 2048 -> fake model called per patch, each input <=2048, patch count >1, composite pixel-exact outside union bbox, per-page try/except continues on second page failure (failed list).
    - Test: empty-branch parity: fill-only batch page (all low-std) skips LaMa entirely (model not called); inpaint-only page skips fill; both-empty does passthrough; batch runner stays Qt-free (no PySide6 import) so worker import probe passes.
  </behavior>
  <action>
    Per D-09 thread max_inpaint_size alongside masker_conf into batch. Add additive param max_inpaint_size: tuple[int,int] = (2048,2048) to batch_clean/batch_detect_and_clean wrappers and to _run_batch_task (default keeps existing callers working, D-02 threshold unchanged). Inside _run_batch_task clean/detect_and_clean branches (220-248 invariants preserved: assert cleaned_dir.name==cleaned, abort only at loop top, progress_callback once per page at loop top, per-page try/except logs and continues, load-once): for each page load image_bgr via _read_image_bgr (non-ASCII safe) -> image_rgb, derive fill_specs from page.boxes via snapshot-like compose (page.boxes already carries persisted fill_color/mask/std_dev; use compose_fill_binary helper) and inpaint_binary from compose_auto_binary + manual/erase (numpy (manual|auto>& ~erase), mirroring canvas.recompose_mask:582-612), apply fill headlessly via PIL convert_mask_to_rgba per fill_spec + alpha_composite+paste, then if inpaint_binary any call inpaint_patches(image_with_fill, inpaint_binary, max_inpaint_size, inp_model.inpaint) with one-patch-live ordering else skip LaMa, then save via save_image_optimized. In main_window._dispatch_batch thread profile.masker.max_inpaint_resolution as (max,max) into the batch args (mirror existing masker_conf threading at 6133). No QImage crosses worker boundary; batch worker never imports PySide6.
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_batch_runner.py -q</automated>
  </verify>
  <done>Batch clean parity green: same gate split as interactive, fill via vendored PIL sequence, patched LaMa capped at max_size, one patch live at a time, empty masks handled, worker still Qt-free.</done>
  <acceptance_criteria>
    - batch_runner wrapper signatures accept max_inpaint_size with default 2048 and forward it to _run_batch_task; _run_batch_task uses it for inpaint_patches.
    - Low-std boxes in batch produce fill_color pixels, high-std produce inpaint_fn pixels, batch large page never passes input > max_size to model.
    - Per-page failure does not abort batch; abort/cancel checked only at loop top; progress_callback emitted once per page at top plus per-patch progress inside inpaint_patches.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Re-base remaining gate-dependent tests (expected breakage suite)</name>
  <files>tests/test_core/test_batch_runner.py, tests/test_gui/test_detection_settings_tooltip.py, tests/test_gui/test_batch_inpaint_parity.py, tests/test_core/test_project_io.py</files>
  <read_first>
    - tests/test_gui/test_detection_settings_tooltip.py (threshold tooltip copy)
    - tests/test_gui/test_batch_inpaint_parity.py (if exists; else test_core/test_batch_runner invariants)
    - tests/test_core/test_project_io.py (mask dims == image dims, fill_color triple)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-RESEARCH.md (Pitfall 1 list: test_detection_boxes, test_box_model, test_gui_border_states, test_gui_inspector_override, test_project_io, test_batch_runner, test_gui_detection_settings — ~7 files)
  </read_first>
  <action>
    Re-base every remaining file that encodes old gate direction to D-01 inverted semantics with CONTEXT citation in test docstring. Flip: threshold tooltip now reads fill below / inpaint above (Pitfall 5 copy), border test matrix now expects 5 visible states (will_fill/will_inpaint/forced_fill/forced_inpaint/never + gate_skipped), batch runner tests now assert low-std->fill high-std->inpaint (not the reverse), project_io override validation now includes fill. Keep probe style: real vendored pick_best_mask/border_std_deviation where applicable, no monkeypatch beyond propagation guard. Add explicit parity test: same snapshot through interactive worker fake vs batch worker fake produces identical fill/inpaint split. Do not weaken fixtures to hide semantic flip.
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_batch_runner.py tests/test_gui/test_detection_settings_tooltip.py tests/test_gui/test_batch_inpaint_parity.py -q</automated>
  </verify>
  <done>All gate-dependent tests re-based to inverted semantics and passing; suite documents CONTEXT D-01 authority in docstrings and encodes 5-state matrix + tooltip inversion + batch parity.</done>
  <acceptance_criteria>
    - Tooltip test asserts inverted copy (fill at/below, inpaint above), border test asserts 5 visible states, batch tests assert corrected split, no test still asserts low-std->inpaint.
    - Parity test asserts interactive and batch consumers produce identical fill vs inpaint partition for same snapshot+threshold.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Refresh after batch + verdict counts for combined flow</name>
  <files>manga_ai_studio/gui/main_window.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_on_batch_finished, _refresh_current_page_after_batch, _on_inpaint_finished verdict at 5062-5080 counting will_inpaint/forced, status_bar_left progress)
    - manga_ai_studio/core/box_model.py (5-state inpaint_state after 08.1-01)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (Async Worker 849-865, batch post-refresh pattern)
  </read_first>
  <action>
    Update the Inpaint verdict/status that currently counts will_inpaint+forced to count will_fill vs will_inpaint vs forced_fill vs forced_inpaint vs never per new vocabulary; ensure status after Inpaint reports fill count and inpaint count and patch count when patched. Ensure _refresh_current_page_after_batch after a patched batch still restores via set_image_from_numpy at returned dims (no planes wipe regression). Keep per-BoxItem color refresh after threshold change using snapshot-as-geometry (CR-01 guard) so slider change re-derives colors from snapshot not live box mutation. No new threads beyond existing Worker pool.
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <done>Verdict and progress reflect combined fill+inpaint flow (including patch count), batch post-refresh remains correct, threshold slider live-recomputes border states from snapshot without stale geometry.</done>
  <acceptance_criteria>
    - Inpaint finish status contains both fill and inpaint counts and, when patched, patch count N; whole-page path shows no patch suffix.
    - Full suite `pytest -q` passes (allow existing Phase 8 suite baseline as superset, no new failures).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk -> batch (page images) | Page images read via _read_image_bgr (non-ASCII path hardened),dims validated before clamp |
| profile INI -> batch max_size | Hand-edited INI may contain out-of-range or non-int max_inpaint_resolution |
| batch -> filesystem (cleaned output) | Output dir must be named cleaned (T-02-03 assert) and save via save_image_optimized with DPI/mode handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-04-01 | Denial of Service | batch_runner max_inpaint_size threading | medium | mitigate | Validate max_inpaint_size tuple ints 512..8192 in worker entry; clamp in MaskerConfig.fix(); fallback 2048 if missing/invalid (try_to_load) — never trust INI value raw |
| T-08.1-04-02 | Tampering | batch clean image read | medium | mitigate | Reuse _read_image_bgr np.fromfile+cv2.imdecode hardened path; FileNotFoundError surfaces per-page not as crash; no cv2.imread on non-ASCII paths |
| T-08.1-04-03 | Information Disclosure | batch per-page failure logging | low | mitigate | Logger records stems + error strings only (T-05-05), no full paths with PII beyond filename; worker errors to loguru not QMessageBox (T-01-08) |
| T-08.1-04-SC | Tampering | package installs | high | mitigate | No new packages; batch purity keeps worker Qt-free/torch-free, no pip install in this phase |

</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` full suite green (baseline 552 passed superset plus new/rebased tests, zero failures)
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -c "import manga_ai_studio.core.batch_runner; import sys; assert 'PySide6' not in sys.modules"` headless probe passes for batch import path
- Manual batch parity: prepare a two-page chapter with one uniform + one complex box per page, run Batch Detect+Clean with max 2048 -> cleaned outputs match interactive Inpaint (C) on same pages pixel-wise outside union bbox; large-page batch shows per-patch progress and no OOM
- Sampling: full suite via pytest 9.1.1 pinned interpreter; VALIDATION.md remains draft until validate-phase (wave-3 comprehensive verification covers batch parity + remaining manual checks)
</verification>

<success_criteria>
- Batch and interactive use identical inverted gate + patching, capped at configured max, one-patch ordering, full parity proven by fake-model tests and manual comparison; suite green with inverted expectations.
</success_criteria>

<output>
Create `.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-04-SUMMARY.md` when done
</output>
