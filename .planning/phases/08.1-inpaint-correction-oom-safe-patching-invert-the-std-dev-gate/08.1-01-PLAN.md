---
phase: 08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/box_model.py
  - manga_ai_studio/core/detection_boxes.py
  - manga_ai_studio/core/inpaint_patching.py
  - panelcleaner/config.py
  - manga_ai_studio/core/project_io.py
  - manga_ai_studio/core/image_ops.py
  - tests/test_core/test_box_model.py
  - tests/test_core/test_detection_boxes.py
  - tests/test_core/test_inpaint_patching.py
  - tests/test_core/test_project_io.py
autonomous: true
requirements: [MASK-02, MASK-03]
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Uniform box (std <= threshold, e.g. 8 <= 15) under Auto is will-fill, complex box (std > threshold, e.g. 20 > 15) under Auto is will-inpaint — gate inverted per D-01, threshold default 15 unchanged per D-02"
    - "Per-box median fill color from pick_best_mask is stored at fit time and survives through boxes_snapshot and .mas round-trip; recomputation at inpaint time is not used"
    - "Patch planner caps every LaMa input at max_inpaint_resolution (default 2048) via D-05/D-06: each patch grown to >=25% of page per side + 10% margin where room, clamped at page edges (D-07 never deferred), interior edge-straddlers deferred (D-08), one patch live at a time, union bbox drives single undo (D-03/D-08)"
  artifacts:
    - manga_ai_studio/core/box_model.py (fill_color + 5-state inpaint_state + copy detachment)
    - manga_ai_studio/core/detection_boxes.py (BoxMaskFit.median_color, compose_auto_binary inverted, compose_fill_binary new)
    - manga_ai_studio/core/inpaint_patching.py (new headless module, Qt-free/torch-free)
    - panelcleaner/config.py (MaskerConfig.max_inpaint_resolution = 2048 marked addition)
    - manga_ai_studio/core/project_io.py (accepts fill + fill_color round-trip)
  key_links:
    - "derive_page_mask_state stores honest std + median_color with gate lifted (1e9) -> compose_auto_binary (inpaint) uses std > threshold and compose_fill_binary (fill) uses std <= threshold — single derivation in PageBox.inpaint_state consumed by BoxItem and batch"
    - "plan_patches + inpaint_patches enforce max size cap and headless purity so batch_runner can import without Qt/torch"
    - "fill_color forwarded through boxes_snapshot and geometry-op invalidation, and persisted via project_io"
---

<objective>
Tracer slice: invert the std-dev gate, store per-box median fill color, add headless OOM-safe patch planner, add max-size config, and widen persistence to the 4-value override — the thin end-to-end headless path that every later GUI/batch plan builds on.

Purpose: Prove the architecture (gate inversion + fill provenance + patch cap) on the agent's best context before wiring the GUI single-undo flow and Inspector/batch consumers. All later plans depend on these seams being correct and headless-pure. Tracer exception: headless consolidation validates gate+fill+patch cap before fan-out — 10 files (6 prod + config + image_ops + 4 tests) at warning threshold but under blocker 15, consolidating avoids split overhead for the proven vertical slice.

Output: Corrected headless seams + new patch module + config key + persistence, all verified headless with hermetic purity probe and fake-model patch loop.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-CONTEXT.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-RESEARCH.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md
@manga_ai_studio/core/box_model.py
@manga_ai_studio/core/detection_boxes.py
@panelcleaner/config.py
@manga_ai_studio/core/project_io.py
@manga_ai_studio/core/image_ops.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end corrected gate: low-std -> fill, high-std -> inpaint — one path only</name>
  <files>manga_ai_studio/core/box_model.py, manga_ai_studio/core/detection_boxes.py, manga_ai_studio/core/project_io.py, panelcleaner/config.py, manga_ai_studio/core/image_ops.py, tests/test_core/test_box_model.py, tests/test_core/test_detection_boxes.py</files>
  <read_first>
    - manga_ai_studio/core/box_model.py (field block 92-116, inpaint_state 199-230, copy 232-256, _has_auto_mask_content 187-198)
    - manga_ai_studio/core/detection_boxes.py (BoxMaskFit 152-165, _fit_one_box 306-354, compose_auto_binary 357-394, derive_page_mask_state 201-303)
    - panelcleaner/config.py (MaskerConfig 553-703, mask_dilation_radius precedent 564-569)
    - manga_ai_studio/core/project_io.py (pagebox_to_json 205-240, json_to_pagebox 296-314, _pagebox_mask_from_json 357-391)
    - manga_ai_studio/core/image_ops.py (rotate/crop/resize/box ops 156,185,293,327,441,474 carrying mask/std_dev)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (box_model + detection_boxes + project_io + config analogs)
  </read_first>
  <behavior>
    - Test: PageBox copy detaches mask PIL image (copy.mask is not original.mask) while fill_color tuple is preserved by value
    - Test: inpaint_state matrix for threshold 15: Auto + std 8 + has content -> will_fill; Auto + std 20 + has content -> will_inpaint; Auto + std 15 exactly -> will_fill (<=); Auto + std None or mask None/empty -> gate_skipped; override fill -> forced_fill regardless of std; override always -> forced_inpaint; override never -> never (6 visible + gate_skipped = 7 probes, the 5 visible states per D-04)
    - Test: compose_auto_binary inverted: two boxes (low std 5, high std 30, same threshold 15, both with 10x10 PIL 1 masks) -> contributing set is high-std + forced_inpaint only; low-std excluded from auto inpaint binary, included in compose_fill_binary when new helper is called with same threshold
    - Test: derive_page_mask_state stores BoxMaskFit.median_color and PageBox.fill_color equal to MaskFittingResults.median_color (off-white rounding baked), and threshold change recomposes via compose_auto_binary/compose_fill_binary without re-fitting (fits dict unchanged)
    - Test: project_io round-trip: PageBox with inpaint_override fill and fill_color (128,200,50) serializes fill_color as [128,200,50] and loads back equal; legacy file without fill_color loads with fill_color None; file with old vocabulary always/never still loads
  </behavior>
  <action>
    Per D-01 invert the gate, per D-02 keep threshold 15, per D-04 add fill vocabulary. Wire ONE headless path end-to-end (no batch, no GUI beyond model): box_model extends PageBox with fill_color Optional[tuple[int,int,int]] = None and re-derives inpaint_state to return will_fill (Auto <=t has content) vs will_inpaint (Auto >t has content) vs forced_fill (fill) vs forced_inpaint (always) vs never vs gate_skipped; update copy() to detach mask and preserve fill_color; detection_boxes extends BoxMaskFit with median_color Optional[tuple[int,int,int]], stores it in _fit_one_box from fit.median_color alongside mask/std_dev, inverts compose_auto_binary to std_dev > threshold for inpaint, adds sibling compose_fill_binary for std_dev <= threshold (or override fill) returning either binary or list of (mask,color,(x,y)) per PATTERNS — both share never/empty guards; forwards fill_color through canvas-equivalent boxes_snapshot already carries it (but this task ensures detection_boxes callers materialize it) and invalidates fill_color alongside mask/std_dev in core/image_ops geometry transforms (six sites: rotate/crop/resize variants); panelcleaner/config adds MaskerConfig.max_inpaint_resolution int 2048 marked addition with export/import/fix clamp 512..8192; project_io widens json_to_pagebox to accept fill plus validates fill_color triple 0..255 optional. Using fill per user decision (RESEARCH suggested storing median_color). No GUI files touched — this plan is the headless tracer that proves the inversion + fill provenance + config cap before expansion.
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_box_model.py tests/test_core/test_detection_boxes.py tests/test_core/test_project_io.py -q</automated>
  </verify>
  <done>Headless tracer green: inpaint_state 5-state matrix passes, compose_auto_binary now includes high-std not low-std, compose_fill_binary covers the complement, fill_color stored at fit time and round-trips via project_io, legacy INI/.mas without the new keys load with defaults.</done>
  <acceptance_criteria>
    - PageBox has field fill_color with default None and inpaint_state returns will_fill for Auto std 8/threshold 15 and will_inpaint for Auto std 20/threshold 15, plus forced_fill/forced_inpaint/never/gate_skipped.
    - BoxMaskFit has median_color and _fit_one_box assigns pb.fill_color from fit.median_color; geometry ops clear fill_color when they clear mask/std_dev.
    - compose_auto_binary contributes only std_dev > threshold (plus always), compose_fill_binary contributes only std_dev <= threshold (plus fill), never is excluded from both, empty masks excluded.
    - MaskerConfig has max_inpaint_resolution default 2048 persisted via ConfigUpdater with 512..8192 clamp; missing key falls back to 2048.
    - project_io round-trip of fill + fill_color succeeds and legacy files without those keys load.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>New headless patch planner: plan_patches + inpaint_patches (OOM guard D-05..D-08)</name>
  <files>manga_ai_studio/core/inpaint_patching.py, tests/test_core/test_inpaint_patching.py</files>
  <read_first>
    - manga_ai_studio/core/detection_boxes.py (header 1-40 headless discipline, _uint8_binary_to_pil1 + _pil1_to_uint8, compose helpers)
    - manga_ai_studio/core/mask_planes.py (pack_binary validation, .copy() detachment)
    - panelcleaner/image_ops.py (grow_mask 811-833, compose_masks 127-142)
    - manga_ai_studio/adapters/torch_impl.py (TorchLamaModel.inpaint 230-259 PIL round-trip contract)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (inpaint_patching assignment 27-108 + No Analog section 886-894)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-RESEARCH.md (Pattern 3 API + Pitfalls 2-4)
  </read_first>
  <behavior>
    - Test: whole-page fast path: image 1000x800 (fits 2048) -> plan_patches returns single rect covering union bbox capped at page, inpaint_patches calls inpaint_fn once and returns union bbox
    - Test: large page 6000x4000 with two disjoint 100x100 masks far apart -> plan_patches returns 2 rects each >=25% page per side where room + 10% margin, each capped at 2048, interior edge-straddler deferred case: mask whose bbox crosses first patch interior edge is excluded from first patch and appears in second patch
    - Test: edge clamp D-07: mask at x=0 (page edge) grown rect is clamped at 0 with no margin that side, not deferred, mask fully included in its patch
    - Test: compositing with grow_mask halo (isolation 5) covers seam: fake inpaint_fn paints patch with solid color, result composite equals page_copy with halo-dilated mask region replaced, later patch wins overlap, untouched pixels pixel-exact outside union bbox
    - Test: headless purity: inpaint_patching source contains no PySide6/torch/gui. imports and subprocess probe `python -c "import manga_ai_studio.core.inpaint_patching; import sys; assert 'PySide6' not in sys.modules"` passes
    - Test: region larger than max_size fallback: 3000x3000 mask region on page 6000x6000 with max 2048 -> planner still returns rects capped at 2048 (margins shrunk), no input exceeds cap
  </behavior>
  <action>
    Per D-05 max-size cap and Per D-06 (25% + 10% margin) with D-07 edge clamp and D-08 interior deferral. Create manga_ai_studio/core/inpaint_patching.py headless (numpy/PIL only, optional cv2 for connectedComponents) per PATTERNS. Implement plan_patches(mask_binary (H,W) uint8 0/255, max_size (max_w,max_h) default 2048, page_size (W,H), min_context 0.25, margin 0.10) -> list[(x1,y1,x2,y2)]: source regions via connected components of mask, per region grow bbox to max(bbox_dim, 0.25*page_dim) per axis where room then add 10% margin on sides with space, clamp to page, cap w/h at max_size, apply D-07 edge rule (no margin where region touches page edge, never deferred for edge proximity), apply D-08 deferral (any OTHER region whose bbox intersects patch boundary in interior is deferred). Loop until every region covered. Implement inpaint_patches(image_rgb (H,W,3) uint8, mask_binary (H,W) uint8 0/255, max_size, inpaint_fn, isolation_radius 5, progress_cb): whole-page fast path when max(h,w) <= max(max_size), else patches = plan_patches(...), page_copy = image_rgb.copy(), for each rect crop and call inpaint_fn (SimpleLama fully convolutional valid), composite only where grow_mask(patch_mask, isolation) is set so halos overlap (Pitfall 3; panelcleaner/config inpainting_isolation_radius 5), progress_cb per patch, return (page_copy, union_bbox (x,y,w,h) or None). Memory-safe: one patch crop + one model call live at a time. Validate inputs dtype/ndim, return .copy()-detached arrays.
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_inpaint_patching.py -q</automated>
  </verify>
  <done>Patch planner is headless-pure, caps every model input at max_size, handles edge clamp vs interior deferral per D-07/D-08, halo composite covers seams, and subprocess probe confirms no Qt/torch import.</done>
  <acceptance_criteria>
    - plan_patches returns rects capped at max_size, each grown to >=25% page per side + 10% margin where room, edge-touching rects clamped without deferral, interior-straddling masks deferred to later patches.
    - inpaint_patches whole-page fast path calls model once; multi-patch path calls model per rect, composites via grow_mask halo, reports progress, returns pixel-exact result outside union bbox.
    - Module source contains no PySide6/torch/gui imports and subprocess probe passes; oversized region falls back to capped rects.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Re-base gate-dependent tests to inverted semantics (expected breakage)</name>
  <files>tests/test_core/test_detection_boxes.py, tests/test_core/test_box_model.py, tests/test_core/test_project_io.py</files>
  <read_first>
    - tests/test_core/test_detection_boxes.py (current gate assertions, especially compose_auto_binary and derive_page_mask_state expectations)
    - tests/test_core/test_box_model.py (inpaint_state matrix)
    - tests/test_core/test_project_io.py (inpaint_override validation)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-RESEARCH.md (Pitfall 1 expected breakage list: ~7 files encode old gate)
  </read_first>
  <action>
    In each listed test file, flip expectations to D-01 semantics with CONTEXT cited in docstring: low-std (<=15) now belongs to fill plane not inpaint plane; high-std (>15) now belongs to inpaint; forced maps become forced_fill/forced_inpaint per D-04; add fill_color assertions where PageBox is constructed (geometry-op tests verify fill_color invalidated). Keep probe tests using real vendored pick_best_mask/border_std_deviation (no monkeypatch, 01-07 discipline); only the propagation guard patches. Do not weaken fixtures to hide the flip. This task is deliberately paired with Task 1 so the suite stays green after the inversion.
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_detection_boxes.py tests/test_core/test_box_model.py tests/test_core/test_project_io.py -q</automated>
  </verify>
  <done>Gate-dependent headless tests re-based to inverted semantics and pass; low-std now asserts will_fill/compose_fill, high-std asserts will_inpaint/compose_auto, fill verbatim appears only as D-04 value.</done>
  <acceptance_criteria>
    - All three test files pass with corrected gate: low-std Auto contributes to fill not inpaint, high-std Auto contributes to inpaint not fill, threshold equality (15) maps to fill per D-01 (<=).
    - Tests assert fill_color round-trip and geometry-op invalidation; legacy .mas/INI without fill still loads.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk -> app (project_io) | .mas JSON/mask blobs untrusted, may contain crafted fill_color/inpaint_override/mask dims (T-08-10 family) |
| model output -> geometry (detection_boxes) | TextBlock xyxy and heatmap dims untrusted (V5 validation) |
| worker -> GUI (image arrays) | Patch-cropped arrays cross thread boundary, must be .copy()-detached |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-01-01 | Tampering | project_io json_to_pagebox fill_color | high | mitigate | Validate fill_color triple int 0..255, reject non-list or len!=3 as ProjectFormatError; wrap binascii/OSError/UnidentifiedImageError/DecompressionBombError as ProjectFormatError (existing pattern) — keep T-08-10 class handling |
| T-08.1-01-02 | Tampering | detection_boxes heatmap dims | medium | mitigate | Strict ValueError if heatmap.ndim!=2 or shape != image_rgb dims (T-08-04), no allocation sized from unvalidated dims; box xyxy clamped + zero-area drop |
| T-08.1-01-03 | Denial of Service | inpaint_patching max_size | medium | mitigate | Validate max_size tuple ints 512..8192, clamp page dims, never allocate from mask dims without validation; cap every patch at max_size, one patch live at a time (memory ordering) |
| T-08.1-01-SC | Tampering | package installs | high | mitigate | No new packages installed (RESEARCH Package Legitimacy Audit: existing stack only); if any install needed, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] (not applicable here) |

</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_box_model.py tests/test_core/test_detection_boxes.py tests/test_core/test_project_io.py tests/test_core/test_inpaint_patching.py -q` passes from a clean checkout
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -c "import manga_ai_studio.core.inpaint_patching; import sys; assert 'PySide6' not in sys.modules; assert 'torch' not in sys.modules"` passes as subprocess probe
- Manual truth: create two AUTO boxes on same page (std 5 uniform + std 30 complex, threshold 15) -> inpaint_state yields will_fill vs will_inpaint respectively, compose_auto_binary contains only the complex box's mask, compose_fill_binary contains only uniform box's mask
- Sampling: existing pytest 9.1.1 infrastructure with pinned interpreter covers all headless truths; VALIDATION.md remains draft until validate-phase per workflow (per-task verification map + manual border/seam checks deferred to validate-phase)
</verification>

<success_criteria>
- Headless tracer proves inverted gate (low-std fill, high-std inpaint) with median_color stored at fit time, patch planner caps at 2048 and handles D-07 edge clamp vs D-08 interior deferral, config key round-trips, persistence accepts fill without breaking legacy files, and headless purity probe stays green.
</success_criteria>

<output>
Create `.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-01-SUMMARY.md` when done
</output>
