---
phase: 08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate
plan: 03
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - manga_ai_studio/gui/inspector_panel.py
  - manga_ai_studio/gui/tools_panel.py
  - manga_ai_studio/gui/box_item.py
  - manga_ai_studio/gui/canvas.py
  - tests/test_gui/test_inspector_override.py
  - tests/test_gui/test_box_item_inpaint_state.py
  - tests/test_gui/test_tools_panel_masker.py
autonomous: true
requirements: [MASK-03]
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Inspector Inpaint combo shows Auto / Fill / Inpaint / Never (replacing Always) with Mixed sentinel for multi-select, and committing Fill writes inpaint_override fill per D-04"
    - "Box border 5 visible states distinct: will_fill (Auto uniform) vs will_inpaint (Auto complex) vs forced_fill vs forced_inpaint vs never, gate_skipped dashed; grey/dash palette extends minimally, zero new hex beyond existing forced/never palette, no recomputation of gate in BoxItem"
    - "Detection settings Std dev threshold tooltip says at/below is fill, above is inpaint (inverted copy), and Max inpaint size control (512..8192, default 2048, suffix px) persists via masker_values/set_masker_values to profile INI"
  artifacts:
    - manga_ai_studio/gui/inspector_panel.py (_INPAINT_DISPLAY/_INPAINT_ITEMS + _set_inpaint_combo + commit map)
    - manga_ai_studio/gui/tools_panel.py (threshold tooltip inverted + max_inpaint_spin + masker_values/set_masker_values blockSignals mirror)
    - manga_ai_studio/gui/box_item.py (_INPAINT_GREY_STATES/_INPAINT_DASHED_STATES + _inpaint_pen_color + set_inpaint_state for 5-state)
  key_links:
    - "PageBox.inpaint_state is the single derivation; BoxItem.set_inpaint_state and inspector display maps consume it, never recompute std_dev vs threshold"
    - "Inspector commit maps display Fill->fill, Inpaint->always (stored always preserved for .mas compat); never and Auto (None) map as before; Mixed never leaves widget layer (Pitfall 7) and WR-01 no-op guard prevents spurious commits"
    - "tools_panel max_inpaint_spin blockSignals mirror prevents startup re-emit; value threads to MaskerConfig.max_inpaint_resolution via Profile.safe_write (atomic)"
---

<objective>
Override vocabulary UI (D-04) + threshold copy fix (Pitfall 5) + max-size setting (D-05) + 5-state BoxItem pens, expanding the tracer's headless gate into visible, controllable UI without touching the Inpaint dispatch flow (parallel to 08.1-02).

Purpose: Users need to see and control the inverted gate (which Auto boxes will fill vs inpaint) and to cap LaMa memory via a persisted setting; the 4 pen states must carry the new fill signal without inventing new colors.
Output: Inspector/box/tool chrome updated and tested, still headless-pure at the model seam, with Mixed sentinel and signal-silent guarantees intact.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-CONTEXT.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-RESEARCH.md
@.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md
@manga_ai_studio/gui/inspector_panel.py
@manga_ai_studio/gui/tools_panel.py
@manga_ai_studio/gui/box_item.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/core/box_model.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inspector override combo Auto/Fill/Inpaint/Never (D-04) + commit maps</name>
  <files>manga_ai_studio/gui/inspector_panel.py, tests/test_gui/test_inspector_override.py</files>
  <read_first>
    - manga_ai_studio/gui/inspector_panel.py (185-190 _INPAINT_DISPLAY/_INPAINT_ITEMS, 311 inpaint_override_changed Signal, 365-379 _emit_inpaint_if_changed, 617-625 load_box, 838-851 load_multi_selection, 1016-1027 _set_inpaint_combo, 1269 _emit_inpaint_if_changed WR-01 guard)
    - manga_ai_studio/gui/main_window.py (~3745 _INPAINT_OVERRIDE_TO_MODEL)
    - manga_ai_studio/core/box_model.py (inpaint_state 5-state after 08.1-01)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (inspector_panel assignment 524-570, Shared Pitfall 6 + WR-01)
  </read_first>
  <behavior>
    - Test: single-select display mapping — PageBox inpaint_override None shows Auto, fill shows Fill, always shows Inpaint (label change from Always), never shows Never; stored value for forced-inpaint stays always for .mas compat, display labels differ.
    - Test: multi-select Mixed sentinel — two boxes Auto+fill -> combo shows Mixed, row stays ENABLED (A5 edit-all), Mixed never emitted on commit, committing Fill from Mixed writes fill to all selected boxes via on_inpaint_override callback, WR-01 guard (no-op when currentText == _loaded_inpaint) suppresses spurious signal.
    - Test: commit map — selecting Fill emits inpaint_override_changed Fill which MainWindow maps to model fill, selecting Inpaint maps to always, Never to never, Auto to None; legacy files with always still show Inpaint after load.
  </behavior>
  <action>
    Per D-04 replace tri-state with quad-state (stored vocabulary None/fill/always/never, display Auto/Fill/Inpaint/Never): edit _INPAINT_DISPLAY to {None:"Auto", "fill":"Fill", "always":"Inpaint", "never":"Never"} and _INPAINT_ITEMS to ["Auto","Fill","Inpaint","Never"] (note Always label -> Inpaint, stored always preserved for .mas compat so legacy files load unchanged). Update _set_inpaint_combo to prepend Mixed when mixed, keep blockSignals discipline; update load_box/load_multi_selection population to use new maps; update _emit_inpaint_if_changed to carry Fill/Inpaint strings and keep WR-01 guard and Mixed sentinel filter (Mixed never leaves widget layer, Pitfall 7). In main_window _INPAINT_OVERRIDE_TO_MODEL add Fill->fill branch and keep Inpaint->always mapping (display Inpaint, stored always). No new signals, reuse inpaint_override_changed.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui/test_inspector_override.py -q</automated>
  </verify>
  <done>Inspector shows Auto/Fill/Inpaint/Never plus Mixed, Fill commits as fill, Inpaint commits as always (compat), legacy always files still open as Inpaint, Mixed never committed.</done>
  <acceptance_criteria>
    - _INPAINT_DISPLAY contains four entries (Auto/Fill/Inpaint/Never) mapping to None/fill/always/never; _INPAINT_ITEMS lists exactly those four.
    - Single and multi-select load + commit paths assert Fill->fill and Inpaint->always round-trip with WR-01 no-op guard and Mixed sentinel not emitted.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BoxItem 5-state border pens (extend grey/dash, zero new hex)</name>
  <files>manga_ai_studio/gui/box_item.py, manga_ai_studio/gui/canvas.py, tests/test_gui/test_box_item_inpaint_state.py</files>
  <read_first>
    - manga_ai_studio/gui/box_item.py (95-111 _INPAINT_FORCED_HEX/_INPAINT_NEVER_HEX/_INPAINT_GREY_STATES/_INPAINT_DASHED_STATES, 571-588 _inpaint_pen_color, 598-613 pen setDashPattern branch, 623-640 set_inpaint_state, 558-562 _inpaint_state field, 709-720 _apply_origin_pen selection path)
    - manga_ai_studio/core/box_model.py (inpaint_state 5-state after 08.1-01: will_fill/will_inpaint/forced_fill/forced_inpaint/never/gate_skipped)
    - manga_ai_studio/gui/canvas.py (refresh_box_inpaint_states wiring plan 08-07 ~3077)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (box_item assignment 640-696 + 5-state mapping Option A)
  </read_first>
  <action>
    Per discretion (5 visible states, keep existing 4-pen system, extend dash/grey palette minimally, borrow no new hex): update _INPAINT_GREY_STATES to frozenset({"forced_fill","forced_inpaint","never"}) covering both forced variants plus never (forced uses near-white #e8e8ea, never uses muted #9a9aa2 via existing _inpaint_pen_color branch), extend _INPAINT_DASHED_STATES to frozenset({"gate_skipped","never"}) so fill states stay solid (will_fill solid origin hue like will_inpaint, gate_skipped dashed origin hue, both forced variants solid grey, never dashed grey) — alternatively use dotted vs solid within grey set to distinguish forced_fill vs forced_inpaint per PATTERNS option A but must not invent new hex; keep _INPAINT_FORCED_HEX/#9a9aa2 as the only greys (ZERO new hex invariant). Update _inpaint_pen_color to handle forced_fill + forced_inpaint alongside legacy forced token if kept for compat, and keep the two apply paths (_apply_origin_pen / _apply_look_for / itemChange) sharing the same frozensets (single behaviour). Ensure set_inpaint_state is the only consumer of PageBox.inpaint_state — BoxItem never recomputes gate (single-derivation rule). Wire canvas refresh_box_inpaint_states to pass through the new 5-state strings unchanged.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui/test_box_item_inpaint_state.py -q</automated>
  </verify>
  <done>Board renders five visible states with correct hue+dash: will_fill/will_inpaint solid origin hue, forced_fill/forced_inpaint solid grey (#e8e8ea), never dashed muted (#9a9aa2), gate_skipped dashed origin, no new hex introduced, pen branch shared between selected and unselected paths.</done>
  <acceptance_criteria>
    - Source contains frozenset with forced_fill and forced_inpaint in _INPAINT_GREY_STATES and gate_skipped+never in _INPAINT_DASHED_STATES; no new hex literal beyond #e8e8ea/#9a9aa2.
    - set_inpaint_state for each of the 5 visible states yields distinct hue/dash per spec; unselected and selected pen paths re-derive identically.
    - No gate recomputation exists in box_item (grep for threshold/std_dev empty in file).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ToolsPanel max LaMa size control + threshold tooltip inversion + live wiring</name>
  <files>manga_ai_studio/gui/tools_panel.py, tests/test_gui/test_tools_panel_masker.py</files>
  <read_first>
    - manga_ai_studio/gui/tools_panel.py (385-549 _build_detection_settings_section, 461-466 std_dev_threshold_spin tooltip, 569-627 masker_values/set_masker_values, 553-563 blockSignals mirror, 30-47 imports)
    - panelcleaner/config.py (MaskerConfig.max_inpaint_resolution field from 08.1-01)
    - manga_ai_studio/core/box_model.py (threshold gate semantics after inversion)
    - .planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-PATTERNS.md (tools_panel assignment 574-638 + tooltip fix 600-609)
  </read_first>
  <action>
    Per D-05 add Max inpaint size input to the Detection settings section: QSpinBox max_inpaint_spin range 512..8192 default 2048 suffix " px" tooltip "Maximum LaMa input size (largest side). Larger pages are patched; caps memory.", form row "Max inpaint size", signal max_inpaint_size_changed (or reuse existing masker param change plumbing) wired to MainWindow persisting to profile.masker.max_inpaint_resolution via Profile.safe_write (atomic). Per Pitfall 5 invert threshold tooltip from "Boxes above this are skipped" to "At or below this is color-filled (solid border); above is AI-inpainted (solid). Fill/Never overrides fill/skip. Borders update live." Extend masker_values() to return max_inpaint_resolution and set_masker_values(masker_conf, detect_boxes) to program-set the spin with blockSignals True/False mirror (prevent startup re-emit, plan 08-05 precedent) falling back to 2048 if missing. Keep vertical-only QScrollArea layout intact.
  </action>
  <verify>
    <automated>&quot;C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe&quot; -m pytest tests/test_gui/test_tools_panel_masker.py -q</automated>
  </verify>
  <done>Detection settings shows threshold tooltip with fill/inpaint semantics and a 512..8192 max size spin defaulting 2048; masker_values round-trips the new key and set_masker_values blockSignals mirror prevents re-emit at startup.</done>
  <acceptance_criteria>
    - ToolsPanel has QSpinBox max_inpaint_spin 512..8192 value 2048 suffix px with correct tooltip; std_dev_threshold_spin tooltip contains "color-filled" and "AI-inpainted".
    - masker_values includes max_inpaint_resolution and set_masker_values restores it with blockSignals; legacy masker_conf without the key falls back to 2048.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI -> profile INI (tools_panel max size) | User-controlled integer 512..8192, must be clamped before persisting and before patch planner consumes it |
| inspector -> pageboxes (override commit) | Commit string comes from QComboBox currentText, must be allowlisted to four values before writing to PageBox |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-03-01 | Tampering | tools_panel max_inpaint_spin | medium | mitigate | QSpinBox range 512..8192 + fix() clamp max(512,min(8192,int(v))) in config; reject non-int via _coerce_int pattern if INI hand-edited |
| T-08.1-03-02 | Tampering | inspector inpaint_override commit | medium | mitigate | Allowlist commit to Auto/Fill/Inpaint/Never only; Mixed and any other string dropped via WR-01/Mixed guard; stored vocabulary is None/fill/always/never (fill additive, always preserved compat) |
| T-08.1-03-03 | Spoofing | box_item border state | low | accept | Border hue/dash is visual only, no security principal; single-derivation guarantee prevents spoofed state via recomputation |
| T-08.1-03-SC | Tampering | package installs | high | mitigate | No package installs; existing stack only |

</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui/test_inspector_override.py tests/test_gui/test_box_item_inpaint_state.py tests/test_gui/test_tools_panel_masker.py -q` passes
- Manual: select one box -> Inspector shows Auto/Fill/Inpaint/Never; set Fill -> border grey solid (forced_fill), set Never -> grey dashed, set Auto with std 5 vs 30 threshold 15 -> will_fill solid hue vs will_inpaint solid hue vs gate_skipped dashed accordingly
- Manual: change Max inpaint size to 1024, restart app -> spin still 1024 (INI round-trip); tooltip for threshold mentions fill above/below
</verification>

<success_criteria>
- Override vocabulary is four-state end-to-end (display + stored + persisted + border), threshold copy matches inverted gate, max size capped and persisted, and no new colors or recomputed gates introduced outside PageBox.inpaint_state.
</success_criteria>

<output>
Create `.planning/phases/08.1-inpaint-correction-oom-safe-patching-invert-the-std-dev-gate/08.1-03-SUMMARY.md` when done
</output>
