---
phase: 01-cleaning-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - pytest.ini
  - .gitignore
  - README.md
  - manga_ai_studio/__init__.py
  - manga_ai_studio/__main__.py
  - manga_ai_studio/app.py
  - manga_ai_studio/config/__init__.py
  - manga_ai_studio/config/profile_manager.py
  - manga_ai_studio/adapters/__init__.py
  - manga_ai_studio/adapters/base.py
  - manga_ai_studio/adapters/onnx_impl.py
  - manga_ai_studio/gui/__init__.py
  - manga_ai_studio/gui/theme.py
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/core/__init__.py
  - panelcleaner/__init__.py
  - panelcleaner/config.py
  - panelcleaner/helpers.py
  - panelcleaner/cli_utils.py
  - panelcleaner/model_downloader.py
  - panelcleaner/ocr/__init__.py
  - panelcleaner/ocr/supported_languages.py
  - tests/conftest.py
  - tests/__init__.py
  - tests/test_core/__init__.py
  - tests/test_core/test_config.py
  - tests/test_core/test_adapters.py
  - tests/test_gui_canvas.py
autonomous: true
requirements:
  - CLEAN-01
  - FLOW-01
user_setup: []

must_haves:
  truths:
    - "python -m manga_ai_studio launches a PySide6 QMainWindow window without ImportError or runtime error"
    - "pytest exits 0 and pytest --collect-only exits 0"
    - "The package layout separates manga_ai_studio (our code) from panelcleaner (vendored PanelCleaner, GPL v3)"
    - "ProfileManager can load a default Profile, materialize it to Config via Config.from_config_updater, and round-trip it to an INI .profile file via Profile.safe_write / Profile.load (config.py:1015, 974, 1310)"
    - "adapters/base.py defines DetectionModel, OCRModel, InpaintModel as abc.ABC subclasses with load/detect-or-recognize-or-inpaint/preprocess/postprocess/configure/get_info abstract methods"
    - "adapters/onnx_impl.py OnnxDetectionModel and OnnxInpaintModel raise NotImplementedError on load (ONNX backend is a future add-on)"
    - "D-11: Adapter structure matches the contracted layout — adapters/base.py (ABC base classes), adapters/torch_impl.py (PanelCleaner PyTorch implementations, populated in plans 03/05), adapters/onnx_impl.py (ONNX stubs written against the ONNX Runtime API, NotImplementedError until a future phase)"
    - "D-06: The full Profile schema is vendored from PanelCleaner (GeneralConfig, TextDetectorConfig, PreprocessorConfig, Profile, MaskerConfig, DenoiserConfig, InpainterConfig, Config) so feature-parity is available; Phase 1 ships no profile-switching UI (the v1 simplification), but the schema round-trips via ProfileManager"
    - "File -> Open Image (Ctrl+O) loads a PNG/JPG file into the EditorCanvas QGraphicsView via a QGraphicsPixmapItem (the end-to-end slice)"
    - "The dark Fusion QPalette is applied once at startup with dominant #232328, canvas matte #0b0b0e, accent #00d4ff, text primary #e8e8ea"
    - "EditorCanvas.__init__ calls setTransformationAnchor(AnchorUnderMouse), setBackgroundBrush(#0b0b0e), and QImageReader.setAllocationLimit(0) (image_viewer.py:45)"
  artifacts:
    - "pyproject.toml with name = manga-ai-studio, python-requires >=3.12, [project.scripts] manga-ai-studio entry point, and dependency groups: main (PySide6, opencv-python, pillow, loguru, configupdater, attrs), torch (torch, transformers, simple-lama-inpainting, manga-ocr), dev (pytest, pytest-qt, pytest-mock)"
    - "pytest.ini registering pytest-qt plugin and testpaths = tests"
    - "manga_ai_studio/__main__.py with def main() that constructs QApplication, loads the default Profile, builds MainWindow, calls app.exec()"
    - "manga_ai_studio/adapters/base.py with DetectionModel, OCRModel, InpaintModel ABCs"
    - "manga_ai_studio/adapters/onnx_impl.py with NotImplementedError stub subclasses"
    - "manga_ai_studio/config/profile_manager.py with ProfileManager class wrapping Profile.safe_write/Profile.load/Config.from_config_updater"
    - "manga_ai_studio/gui/theme.py exposing build_dark_palette() returning a QPalette"
    - "manga_ai_studio/gui/canvas.py with EditorCanvas(QGraphicsView)"
    - "manga_ai_studio/gui/main_window.py with MainWindow(QMainWindow) showing File menu with Open Image (Ctrl+O)"
    - "panelcleaner/config.py vendored from C:\\Src\\PanelCleaner\\pcleaner\\config.py with pcleaner. -> panelcleaner. import rewrites"
    - "tests/conftest.py with qtbot fixture usage and a default Profile fixture"
    - "tests/test_core/test_config.py asserting ProfileManager round-trip"
    - "tests/test_core/test_adapters.py asserting base ABCs reject direct instantiation and onnx stubs raise NotImplementedError"
    - "tests/test_gui_canvas.py::test_load_image asserting a QImage loads into the canvas"
  key_links:
    - "__main__.main -> QApplication -> MainWindow -> EditorCanvas -> QGraphicsScene/QGraphicsPixmapItem (the launch path)"
    - "MainWindow.open_image -> ProfileManager.load + QImage load -> EditorCanvas.set_image (the open-image slice)"
    - "ProfileManager -> panelcleaner.config.Profile.safe_write / Profile.load / Config.from_config_updater (config round-trip)"
    - "adapters/base.py ABCs <- adapters/onnx_impl.py stubs <- (future) adapters/torch_impl.py in plans 03/05"
  prohibitions: []
---

<objective>
Establish the Walking Skeleton: the thinnest end-to-end slice that proves the Phase 1 stack works
and the package layout is correct. A user can run `python -m manga_ai_studio`, open one manga page
via File -> Open Image (Ctrl+O), see it on the dark canvas, and close the window.

Per D-01, D-04, D-05, D-07, D-10, D-12: vendor the PanelCleaner config system near-verbatim into
`panelcleaner/`, declare the model adapter ABCs (`DetectionModel`/`OCRModel`/`InpaintModel`) with
ONNX stubs, bootstrap the `ProfileManager`, create the `pytest` infrastructure (Wave 0), and build
a minimal `MainWindow` + `EditorCanvas` that opens one image. Plans 02–06 extend this skeleton.

Purpose: proves the frontend environment (PySide6 + Fusion dark palette), the vendored config
system, and the adapter contract before any feature work.
Output: a runnable application, a green test suite, the SKELETON.md (already written).
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cleaning-workspace/01-CONTEXT.md
@.planning/phases/01-cleaning-workspace/01-RESEARCH.md
@.planning/phases/01-cleaning-workspace/01-PATTERNS.md
@.planning/phases/01-cleaning-workspace/01-UI-SPEC.md
@.planning/phases/01-cleaning-workspace/01-VALIDATION.md
@.planning/phases/01-cleaning-workspace/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor PanelCleaner config system + create pyproject/pytest scaffolding + ProfileManager + adapter ABCs</name>
  <files>
    pyproject.toml,
    pytest.ini,
    .gitignore,
    README.md,
    panelcleaner/__init__.py,
    panelcleaner/config.py,
    panelcleaner/helpers.py,
    panelcleaner/cli_utils.py,
    panelcleaner/model_downloader.py,
    panelcleaner/ocr/__init__.py,
    panelcleaner/ocr/supported_languages.py,
    manga_ai_studio/__init__.py,
    manga_ai_studio/config/__init__.py,
    manga_ai_studio/config/profile_manager.py,
    manga_ai_studio/adapters/__init__.py,
    manga_ai_studio/adapters/base.py,
    manga_ai_studio/adapters/onnx_impl.py,
    tests/__init__.py,
    tests/conftest.py,
    tests/test_core/__init__.py,
    tests/test_core/test_config.py,
    tests/test_core/test_adapters.py
  </files>
  <read_first>
    C:\Src\PanelCleaner\pcleaner\config.py (full file — the vendoring source; pay attention to lines 1-18 imports, Profile class 935-1065, Config class 1067+, safe_write 974, load classmethod 1015, from_config_updater 1310, Config.save 1217),
    C:\Src\PanelCleaner\pcleaner\helpers.py (transitive dep of config.py:14 — provides tr()),
    C:\Src\PanelCleaner\pcleaner\cli_utils.py (transitive dep of config.py:15),
    C:\Src\PanelCleaner\pcleaner\model_downloader.py (transitive dep of config.py:17),
    C:\Src\PanelCleaner\pcleaner\ocr\supported_languages.py (transitive dep of config.py:18),
    C:\Src\PanelCleaner\requirements.txt (dependency ground truth),
    .planning\phases\01-cleaning-workspace\01-RESEARCH.md (§Standard Stack lines 51-110, §Package Legitimacy Audit lines 111-128, §Validation Architecture lines 873-914),
    .planning\phases\01-cleaning-workspace\01-PATTERNS.md (§panelcleaner/config.py lines 182-213, §adapters/base.py lines 66-101, §adapters/onnx_impl.py lines 158-178, §config/profile_manager.py lines 242-272),
    .planning\phases\01-cleaning-workspace\01-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-10, D-12),
    .planning\phases\01-cleaning-workspace\01-VALIDATION.md (§Wave 0 Requirements lines 64-76)
  </read_first>
  <action>
    Project scaffolding (per D-10 two-package layout). Create `pyproject.toml` with:
    - `[project]` name = "manga-ai-studio", version = "0.1.0", requires-python = ">=3.12"
    - `[project.scripts] manga-ai-studio = "manga_ai_studio.__main__:main"`
    - `[project.gui-scripts]` NOT used (console script is sufficient for v1)
    - `[project.dependencies]` = PySide6>=6.7, opencv-python>=4.9, pillow>=10, loguru, configupdater, attrs, numpy<2, scipy, natsort. NOTE on the three additions (declared here as the single source of truth so downstream plans do NOT re-declare them): `numpy<2` is pinned explicitly because PanelCleaner pins it (opencv/torch pull numpy transitively but a numpy 2.x install breaks the vendored stack); `scipy` is required by the vendored `panelcleaner/image_ops.py:10` (plan 05); `natsort` is required by `manga_ai_studio/gui/file_table.py` natural-sort (plan 02). All three are listed in C:\Src\PanelCleaner\requirements.txt.
    - `[project.optional-dependencies.torch]` = torch>=2.2, transformers>=4.40, simple-lama-inpainting, manga-ocr
    - `[project.optional-dependencies.dev]` = pytest, pytest-qt, pytest-mock
    Create `pytest.ini` with `[pytest]`, `qt_api = pyside6`, `testpaths = tests`, `addopts = -ra`, `filterwarnings =` empty. Create `.gitignore` ignoring `__pycache__/`, `*.pyc`, `.venv*/`, `main_env/`, `torch_env/`, `onnx_env/`, `models/`, `.pytest_cache/`, `*.egg-info/`, `build/`, `dist/`. Create `README.md` with project title, GPL v3 notice, the `python -m manga_ai_studio` run command, and a pointer to `panelcleaner/LICENSE` obligation.

    Vendor PanelCleaner config (D-04 verbatim, D-12 GPL-compat). Copy these files from `C:\Src\PanelCleaner\pcleaner\` into the new `panelcleaner/` package, performing ONLY the mechanical import rewrite `from pcleaner` -> `from panelcleaner` / `import pcleaner` -> `import panelcleaner` / `pcleaner.` -> `panelcleaner.` everywhere imports appear (do NOT refactor logic, classes, signatures, or comments):
    - `config.py` (the full ~1500-line file; keep classes GeneralConfig, TextDetectorConfig, PreprocessorConfig, Profile, MaskerConfig, DenoiserConfig, InpainterConfig, Config, and methods Profile.safe_write:974, Profile.unsafe_write:998, Profile.load:1015, Profile.bundle_config:948, Config.from_config_updater:1310, Config.save:1217 — all verbatim)
    - `helpers.py` (provides `tr()` used at config.py:14)
    - `cli_utils.py` (config.py:15 dependency)
    - `model_downloader.py` (config.py:17 dependency — provides `get_inpainting_model_path`, `download_*`)
    - `ocr/supported_languages.py` + `ocr/__init__.py` (config.py:18 dependency)
    Create `panelcleaner/__init__.py` (empty or minimal docstring). Do NOT vendor comic_text_detector/, inpainting.py, masker.py, image_ops.py, structures.py yet — they are not transitive deps of config.py and belong to plans 03/05. Do NOT copy PanelCleaner's LICENSE into panelcleaner/ (license notice goes in README + the project LICENSE file; do not duplicate the upstream GPL text inside vendored files).

    Create `manga_ai_studio/config/profile_manager.py` (D-05 INI/ConfigUpdater, per PATTERNS.md corrected wrapper lines 248-268). Class `ProfileManager` with:
    - `__init__(self, config_dir: Path)` storing `config_dir` and `self.config = Config()` (vendored Config, defaults)
    - `save_profile(self, profile: Profile, name: str) -> Path` writing `{config_dir}/{name}.profile` via `profile.safe_write(profile_path)` (NOT `profile.save` — `save` is on `Config`, not `Profile`; verified: `Config.save` at config.py:1217, `Profile.safe_write` at 974); return the path
    - `load_profile(self, name: str) -> Profile` returning `Profile.load(profile_path)` (classmethod at config.py:1015)
    - `profile_to_config(self, profile: Profile) -> Config` returning `Config.from_config_updater(profile.bundle_config())` (config.py:1310 and 948)
    - `default_profile(self) -> Profile` returning a fresh `Profile()` (vendored defaults)
    Note: `Profile.load` swallows exceptions and returns a default `Profile()` on failure (config.py:1031-1034) — do not add a try/except; preserve that behavior.

    Create `manga_ai_studio/adapters/base.py` (D-01, per PATTERNS.md §adapters/base.py lines 66-101 and RESEARCH Pattern 1 lines 237-296). Imports stdlib first, then third-party (numpy), then `panelcleaner.*` if needed. Define three `abc.ABC` subclasses:
    - `DetectionModel(ABC)`: abstract methods `load(self, model_path: Path, device: str = "cpu") -> None`, `detect(self, image: np.ndarray) -> Tuple[np.ndarray, list]` returning `(heatmap_mask, text_blocks)`, `preprocess(self, image: np.ndarray) -> np.ndarray`, `postprocess(self, model_output: np.ndarray) -> np.ndarray`, `configure(self, **kwargs) -> None`, `get_info(self) -> dict`
    - `OCRModel(ABC)`: abstract methods `load`, `recognize(self, image: np.ndarray) -> str`, `preprocess`, `postprocess`, `configure`, `get_info`
    - `InpaintModel(ABC)`: abstract methods `load(self, model_path: Path, device: str = "cpu") -> None`, `inpaint(self, image: np.ndarray, mask: np.ndarray) -> np.ndarray`, `preprocess`, `postprocess`, `configure`, `get_info`
    Each abstract method decorated with `@abstractmethod`. Docstrings on each class citing D-01.

    Create `manga_ai_studio/adapters/onnx_impl.py` (D-03/D-09, per PATTERNS.md §adapters/onnx_impl.py lines 158-178). Define `OnnxDetectionModel(DetectionModel)` and `OnnxInpaintModel(InpaintModel)` subclassing the base ABCs. Each `load()` body: `raise NotImplementedError("ONNX backend lands in a future phase; use torch_impl.")`. Other abstract methods (`detect`/`inpaint`/`preprocess`/`postprocess`) may `raise NotImplementedError`. Do NOT import `onnxruntime` (it is not a Phase 1 dependency — keep the interface honest for D-02 without pulling the dep).

    Create `tests/conftest.py` with: `import pytest`, `pytest.importorskip("PySide6")`, a fixture `default_profile()` returning `Profile()` from `manga_ai_studio.config.profile_manager`, a fixture `tmp_config_dir(tmp_path)` returning `tmp_path`, and a fixture `profile_manager(tmp_config_dir)` returning `ProfileManager(tmp_config_dir)`. Create `tests/test_core/test_config.py` with a test `test_profile_round_trip(profile_manager, tmp_config_dir)` that: builds a default Profile, saves it via `profile_manager.save_profile(profile, "default")`, reloads via `profile_manager.load_profile("default")`, and asserts the reloaded object's `.general` attribute is not None. Add `test_profile_to_config` asserting `profile_manager.profile_to_config(profile)` returns a `Config` instance with a `.current_profile` attribute. Create `tests/test_core/test_adapters.py` with `test_base_abc_rejects_instantiation` asserting `DetectionModel()`, `OCRModel()`, `InpaintModel()` each raise `TypeError` (abstract), and `test_onnx_stub_raises` asserting `OnnxDetectionModel().load(Path("x"))` and `OnnxInpaintModel().load(Path("x"))` raise `NotImplementedError`. Create `tests/__init__.py`, `tests/test_core/__init__.py` as empty files.
  </action>
  <verify>
    <automated>python -c "from panelcleaner.config import Config, Profile; from manga_ai_studio.config.profile_manager import ProfileManager; from manga_ai_studio.adapters.base import DetectionModel, OCRModel, InpaintModel; from manga_ai_studio.adapters.onnx_impl import OnnxDetectionModel, OnnxInpaintModel; print('imports ok')" && pytest tests/test_core/ -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `pyproject.toml` contains `name = "manga-ai-studio"`, `requires-python = ">=3.12"`, `manga-ai-studio = "manga_ai_studio.__main__:main"` under `[project.scripts]`, and lists PySide6, opencv-python, pillow, loguru, configupdater, attrs, numpy<2, scipy, natsort as dependencies (the single source of truth — downstream plans 02/05 use these without re-declaring)
    - Source: `pytest.ini` contains `qt_api = pyside6` and `testpaths = tests`
    - Source: `panelcleaner/config.py` contains `class Profile` and `class Config` and `def safe_write(self, path: Path) -> bool` and `def from_config_updater` and zero occurrences of `from pcleaner` / `import pcleaner` (all rewritten to `panelcleaner`)
    - Source: `manga_ai_studio/adapters/base.py` contains `class DetectionModel`, `class OCRModel`, `class InpaintModel`, each with `@abstractmethod` decorators, and `import abc` / `from abc import ABC, abstractmethod`
    - Source: `manga_ai_studio/adapters/onnx_impl.py` contains `class OnnxDetectionModel(DetectionModel)` and `raise NotImplementedError`
    - Source: `manga_ai_studio/config/profile_manager.py` contains `profile.safe_write(profile_path)` and `Profile.load(profile_path)` and `Config.from_config_updater` — and does NOT contain `profile.save(`
    - CLI: `pytest tests/test_core/ -x` exits 0
    - Behavior: `tests/test_core/test_config.py::test_profile_round_trip` passes (Profile round-trips through INI)
    - Behavior: `tests/test_core/test_adapters.py::test_base_abc_rejects_instantiation` passes (TypeError on ABC instantiation)
    - Behavior: `tests/test_core/test_adapters.py::test_onnx_stub_raises` passes (NotImplementedError)
  </acceptance_criteria>
  <done>
    pyproject.toml + pytest.ini + .gitignore + README.md exist; panelcleaner/ package contains config.py and its transitive deps with all pcleaner imports rewritten to panelcleaner; ProfileManager wraps Profile.safe_write/Profile.load/Config.from_config_updater; adapter ABCs and ONNX stubs compile; tests/test_core/ is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Walking Skeleton app — theme + EditorCanvas + MainWindow + __main__ entry point that opens one image</name>
  <files>
    manga_ai_studio/__main__.py,
    manga_ai_studio/app.py,
    manga_ai_studio/gui/__init__.py,
    manga_ai_studio/gui/theme.py,
    manga_ai_studio/gui/canvas.py,
    manga_ai_studio/gui/main_window.py,
    tests/test_gui_canvas.py
  </files>
  <read_first>
    C:\Src\PanelCleaner\pcleaner\gui\image_viewer.py (lines 1-60 for QImageReader.setAllocationLimit, 116-130 for update_smoothing + wheelEvent setup, 221-260 for zoom clamp logic; this is the GPL pan/zoom mechanics source — vendor/adapt),
    C:\Src\PanelCleaner\pcleaner\gui\mainwindow_driver.py (lines 111-165 for __init__ shape, 191-217 for set_theme/Fusion pattern — GPL, adapt),
    .planning\phases\01-cleaning-workspace\01-UI-SPEC.md (§Design System lines 25-42, §Color lines 86-114, §Surface 1 Main-window shell lines 160-181, §Surface 2 Image canvas lines 182-194),
    .planning\phases\01-cleaning-workspace\01-PATTERNS.md (§gui/main_window.py lines 274-359, §gui/canvas.py lines 362-444 — note MangaCleaner_GPU canvas is REFERENCE-ONLY per D-12, PanelCleaner image_viewer.py pan/zoom is GPL vendorable),
    .planning\phases\01-cleaning-workspace\01-SKELETON.md (capability proven end-to-end)
  </read_first>
  <behavior>
    - Test 1 (test_app_launches): constructing QApplication + MainWindow + calling window.show() then window.close() does not raise; MainWindow.windowTitle() == "Manga AI Studio" when no page is open
    - Test 2 (test_theme_applied): after app bootstrap, QApplication.palette() returns a QPalette whose Window color is #232328 (dark dominant) — asserts the dark Fusion palette is applied
    - Test 3 (test_canvas_load_image): EditorCanvas.set_image(qpixmap_or_path) adds a QGraphicsPixmapItem to the scene and the sceneRect matches the image size; QImageReader.setAllocationLimit(0) was called
    - Test 4 (test_open_image_action): MainWindow has a QAction for "Open Image…" with shortcut Ctrl+O (verified via action.text() and action.shortcut().toString())
  </behavior>
  <action>
    Create `manga_ai_studio/gui/theme.py` with `def build_dark_palette() -> QPalette` (UI-SPEC §Color, applied once at startup per PATTERNS.md §Shared Pattern 1). The returned QPalette sets: Window/WindowText = #232328 / #e8e8ea; Base/AlternateBase = #2d2d33 / #232328; Text/WindowText = #e8e8ea; Highlight = #00d4ff; Button = #2d2d33; ToolTipBase/Text = #2d2d33 / #e8e8ea. Use QColor hex literals matching UI-SPEC tokens exactly (#232328, #0b0b0e, #2d2d33, #3a3a42, #00d4ff, #e8e8ea, #9a9aa2).

    Create `manga_ai_studio/gui/canvas.py` with `class EditorCanvas(QGraphicsView)` (our own file per D-10; the pan/zoom MECHANICS are adapted from PanelCleaner `image_viewer.py` which is GPL/vendored per D-12 — say "adapted from PanelCleaner image_viewer.py" in the docstring, NOT "copied from MangaCleaner_GPU"). In `__init__`: create `self.scene = QGraphicsScene(self)`, `self.setScene(self.scene)`; add `self.image_item = QGraphicsPixmapItem()` and `self.mask_item = QGraphicsPixmapItem()` to the scene (mask_item added after image_item so it stacks above); call `self.setRenderHint(QPainter.Antialiasing)`, `self.setTransformationAnchor(QGraphicsView.AnchorUnderMouse)` (UI-SPEC surface 2, image_viewer.py:56), `self.setResizeAnchor(QGraphicsView.AnchorUnderMouse)`, `self.setBackgroundBrush(QBrush(QColor(11, 11, 14)))` (#0b0b0e canvas matte, MangaCleaner_GPU canvas.py:21 token, UI-SPEC §Color), `self.setHorizontalScrollBarPolicy(Qt.ScrollBarAsNeeded)` and same for vertical, and at module import time call `QImageReader.setAllocationLimit(0)` (image_viewer.py:45 — large-page safety). Add `def set_image(self, pixmap: QPixmap) -> None` setting `self.image_item.setPixmap(pixmap)` and `self.setSceneRect(QRectF(pixmap.rect()))` and initializing `self.mask_item` to a transparent QImage of the same size. Add `def clear(self) -> None` resetting both items. Do NOT implement pan/zoom event handlers yet (plan 02 adds wheelEvent/zoom); the skeleton only needs the image to display and fit. Add `def fit_to_window(self) -> None` calling `self.fitInView(self.image_item, Qt.KeepAspectRatio)`.

    Create `manga_ai_studio/gui/main_window.py` with `class MainWindow(QMainWindow)` (architecture adapted from PanelCleaner mainwindow_driver.py:111-165 per PATTERNS.md §gui/main_window.py; this is GPL/vendored-shape, say "adapted from PanelCleaner mainwindow_driver.py"). In `__init__(self, profile_manager: ProfileManager, parent=None)`: store `self.profile_manager`; create `self.canvas = EditorCanvas()` and `self.setCentralWidget(self.canvas)`; build the menu bar — File menu with `self.action_open_image = QAction("Open Image…", self)` shortcut `QKeySequence("Ctrl+O")` (UI-SPEC §Keyboard), connected to `self.open_image`; add a View menu with Fit to Window `Ctrl+0` connected to `self.canvas.fit_to_window`. Implement `def open_image(self) -> None`: open a `QFileDialog.getOpenFileName` with filter "Images (*.png *.jpg *.jpeg *.webp *.bmp)" (UI-SPEC surface 4); if a path is chosen, load it via `QImage(path)` (with `.copy()` discipline per RESEARCH Pitfall 2 / PATTERNS.md §Shared Pattern 5 — call `.copy()` on the QImage after load so the buffer is detached), convert to `QPixmap.fromImage`, call `self.canvas.set_image(...)`, update `self.setWindowTitle(f"Manga AI Studio — {Path(path).name}")`, then `self.canvas.fit_to_window()`. Set `self.setWindowTitle("Manga AI Studio")` initially (UI-SPEC surface 1 — no version in title bar). Window minimum size 1024x720, default 1440x900 (UI-SPEC surface 1).

    Create `manga_ai_studio/app.py` with `def create_app(argv=None) -> QApplication` that constructs `QApplication(argv or [])`, calls `QApplication.setStyle("Fusion")` (UI-SPEC §Design System, mandatory), applies `QApplication.setPalette(build_dark_palette())`, and returns the app. Create `manga_ai_studio/__main__.py` with `def main() -> None`: call `create_app()`, build a `ProfileManager(config_dir=Path.home()/".manga_ai_studio")` (creating the dir if missing via `Path.mkdir(parents=True, exist_ok=True)`), construct `MainWindow(profile_manager)`, `window.show()`, `sys.exit(app.exec())`. Add `if __name__ == "__main__": main()`.

    Create `tests/test_gui_canvas.py` (GUI smoke test per VALIDATION.md §Wave 0; uses pytest-qt `qtbot`). Implement the four behaviors above: `test_app_launches(qtbot)` (qtbot.addWidget(MainWindow(ProfileManager(tmp_path))), show+close), `test_theme_applied(qtbot)` (create_app(); assert QApplication.palette().color(QPalette.Window).name() == "#232328"), `test_canvas_load_image(qtbot)` (build a 4x4 red QPixmap, call EditorCanvas.set_image, assert scene().items() has a QGraphicsPixmapItem and sceneRect width == 4), `test_open_image_action(qtbot)` (construct MainWindow, find the QAction with text "Open Image…", assert its shortcut toString contains "Ctrl+O"). Guard each test with the qtbot fixture; use `pytest.importorskip("PySide6")` at module top. Create `manga_ai_studio/gui/__init__.py` as empty.
  </action>
  <verify>
    <automated>python -c "from manga_ai_studio.gui.canvas import EditorCanvas; from manga_ai_studio.gui.main_window import MainWindow; from manga_ai_studio.app import create_app; print('app imports ok')" && pytest tests/test_gui_canvas.py -x && pytest --collect-only -q | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `python -c "from manga_ai_studio.__main__ import main; print('entry ok')"` exits 0 (no import error)
    - CLI: `pytest tests/test_gui_canvas.py -x` exits 0
    - CLI: `pytest --collect-only` exits 0 (Wave 0 collection gate from VALIDATION.md)
    - Source: `manga_ai_studio/gui/canvas.py` contains `class EditorCanvas(QGraphicsView)`, `setTransformationAnchor(QGraphicsView.AnchorUnderMouse)`, `setBackgroundBrush` with QColor(11, 11, 14), and `QImageReader.setAllocationLimit(0)`
    - Source: `manga_ai_studio/gui/theme.py` contains `def build_dark_palette` and QColor literals matching #232328, #00d4ff, #e8e8ea
    - Source: `manga_ai_studio/app.py` contains `QApplication.setStyle("Fusion")` and `setPalette(build_dark_palette())`
    - Source: `manga_ai_studio/__main__.py` contains `def main` and `sys.exit(app.exec())`
    - Source: `manga_ai_studio/gui/main_window.py` contains `class MainWindow(QMainWindow)`, `QAction("Open Image…"`, `QKeySequence("Ctrl+O")`, and `setWindowTitle(f"Manga AI Studio` (f-string form OR concatenation — the literal "Manga AI Studio" must appear)
    - Behavior: test_theme_applied passes — QApplication palette Window color is #232328
    - Behavior: test_canvas_load_image passes — set_image puts a QGraphicsPixmapItem in the scene
    - Behavior: test_open_image_action passes — the Open Image action exists with Ctrl+O shortcut
    - D-12 compliance: gui/canvas.py docstring says "adapted from PanelCleaner image_viewer.py" (GPL source) and does NOT say "copied from MangaCleaner_GPU"
  </acceptance_criteria>
  <done>
    `python -m manga_ai_studio` would launch a QMainWindow (verified via import + smoke test, no display needed for the automated gate); the dark Fusion palette is applied; File -> Open Image action with Ctrl+O exists; EditorCanvas renders a QPixmap via QGraphicsPixmapItem with the #0b0b0e matte and AnchorUnderMouse; full pytest suite green and collectable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> app (file open) | User-selected image path crosses into QImage load; untrusted path/content |
| models/ dir -> adapter (future) | Model weight files loaded by adapters; tampering risk (stubbed in skeleton) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering (path traversal) | MainWindow.open_image / QFileDialog | medium | mitigate | QFileDialog.getOpenFileName returns an absolute, OS-validated path; no user-typed path string is accepted in the skeleton. Plan 02 adds Path.resolve() + suffix allowlist validation when folder/recent-files opens land. |
| T-01-02 | Denial of Service (large image) | EditorCanvas / QImage load | medium | mitigate | QImageReader.setAllocationLimit(0) is set at module import (image_viewer.py:45) — prevents Qt's default 128MB allocation cap from rejecting large manga pages; a 10000x10000px upper bound is enforced in plan 02's open path. |
| T-01-03 | Tampering (model weight tampering) | adapters/base.py load() contract | low | accept | Skeleton ships no concrete model loader (torch_impl lands in plans 03/05). The ABC contract documents that concrete load() implementations MUST verify model checksums on load (per RESEARCH §Security Domain T-2). No model is loaded in this plan. |
| T-01-04 | Tampering (ImageTragick-style malicious image) | QImage load | low | accept | Qt's QImage decoder is sandboxed and not ImageMagick; v1 loads PNG/JPG/WebP/BMP via Qt only. Plan 02 validates file headers and enforces the 10000x10000px cap. |
| T-01-SC | Tampering | pip/uv installs (pyproject deps) | high | mitigate | All dependencies [OK]/Approved per RESEARCH §Package Legitimacy Audit (PySide6, opencv-python, pillow, loguru, configupdater, attrs, numpy, scipy, natsort — all from official sources). No [ASSUMED]/[SUS] packages; no blocking human checkpoint needed. |
</threat_model>

<verification>
- `python -m manga_ai_studio` launches without ImportError (manual or smoke-tested via `python -c "from manga_ai_studio.__main__ import main"`)
- `pytest` exits 0 (full suite: test_core + test_gui_canvas)
- `pytest --collect-only` exits 0 (Wave 0 collection gate)
- `python -c "import panelcleaner.config; import manga_ai_studio.adapters.base"` exits 0 (cross-package import integrity)
</verification>

<success_criteria>
- Two-package layout (manga_ai_studio/ + panelcleaner/) compiles and imports cleanly
- Vendored PanelCleaner config round-trips through Profile.safe_write / Profile.load (test_profile_round_trip green)
- Adapter ABCs (DetectionModel, OCRModel, InpaintModel) reject direct instantiation; ONNX stubs raise NotImplementedError
- Walking Skeleton app launches with the dark Fusion palette and an Open Image (Ctrl+O) action
- EditorCanvas renders a QPixmap on the #0b0b0e matte via QGraphicsPixmapItem
- pytest infrastructure (pytest.ini, conftest.py, test directories) established for Wave 0
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 — Walking Skeleton)

**Packages / modules:**
- `manga_ai_studio` (top-level package, our code)
- `manga_ai_studio.__main__:main()` — application entry point (also `[project.scripts] manga-ai-studio`)
- `manga_ai_studio.app:create_app(argv=None) -> QApplication` — QApplication factory applying Fusion + dark palette
- `manga_ai_studio.config.ProfileManager(config_dir)` — wraps `Profile.safe_write(path)`, `Profile.load(path)` (classmethod), `Config.from_config_updater(profile.bundle_config())`
- `manga_ai_studio.adapters.base.DetectionModel` (ABC) — abstract methods: `load(model_path, device="cpu")`, `detect(image) -> (mask, blocks)`, `preprocess`, `postprocess`, `configure`, `get_info`
- `manga_ai_studio.adapters.base.OCRModel` (ABC) — `load`, `recognize(image) -> str`, `preprocess`, `postprocess`, `configure`, `get_info`
- `manga_ai_studio.adapters.base.InpaintModel` (ABC) — `load`, `inpaint(image, mask) -> image`, `preprocess`, `postprocess`, `configure`, `get_info`
- `manga_ai_studio.adapters.onnx_impl.OnnxDetectionModel(DetectionModel)` — stub, `load` raises NotImplementedError
- `manga_ai_studio.adapters.onnx_impl.OnnxInpaintModel(InpaintModel)` — stub, `load` raises NotImplementedError
- `manga_ai_studio.gui.theme.build_dark_palette() -> QPalette`
- `manga_ai_studio.gui.canvas.EditorCanvas(QGraphicsView)` — `set_image(pixmap)`, `clear()`, `fit_to_window()`; scene stack `image_item` -> `mask_item`; matte #0b0b0e; AnchorUnderMouse; QImageReader.setAllocationLimit(0)
- `manga_ai_studio.gui.main_window.MainWindow(QMainWindow)` — `__init__(profile_manager)`, `open_image()`; File menu with Open Image… (Ctrl+O); View menu with Fit to Window (Ctrl+0)
- `panelcleaner` (top-level package, vendored PanelCleaner GPL v3)
- `panelcleaner.config.Config` (vendored, verbatim) — `from_config_updater(conf_updater)` classmethod (config.py:1310), `save(config_path=None)` (1217)
- `panelcleaner.config.Profile` (vendored, verbatim) — `safe_write(path) -> bool` (974), `unsafe_write(path)` (998), `load(path)` classmethod (1015), `bundle_config(gui_mode=False) -> ConfigUpdater` (948); fields `general`, `text_detector`, `preprocessor`, `masker`, `denoiser`, `inpainter`
- `panelcleaner.config.{GeneralConfig, TextDetectorConfig, PreprocessorConfig, MaskerConfig, DenoiserConfig, InpainterConfig}` (vendored, verbatim)
- `panelcleaner.helpers`, `panelcleaner.cli_utils`, `panelcleaner.model_downloader`, `panelcleaner.ocr.supported_languages` (vendored transitive deps of config.py)

**Config files:**
- `pyproject.toml` — name=manga-ai-studio, python>=3.12, [project.scripts] manga-ai-studio, dependency groups main/torch/dev
- `pytest.ini` — qt_api=pyside6, testpaths=tests
- `.gitignore` — Python + venv + models/ ignores
- `README.md` — GPL v3 notice, run command

**Test files:**
- `tests/conftest.py` — `default_profile`, `tmp_config_dir`, `profile_manager` fixtures
- `tests/test_core/test_config.py` — `test_profile_round_trip`, `test_profile_to_config`
- `tests/test_core/test_adapters.py` — `test_base_abc_rejects_instantiation`, `test_onnx_stub_raises`
- `tests/test_gui_canvas.py` — `test_app_launches`, `test_theme_applied`, `test_canvas_load_image`, `test_open_image_action`

**Config keys introduced (read by later plans):**
- `detection_backend`, `ocr_backend`, `inpainting_backend` (CONTEXT.md §Specific Ideas; keys consumed by adapter factory in plans 03/05)
</artifacts_produced>

<output>
Create `.planning/phases/01-cleaning-workspace/01-01-SUMMARY.md` when done
</output>
