---
phase: 01-cleaning-workspace
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/file_table.py
  - manga_ai_studio/core/image_file.py
  - tests/test_gui_canvas.py
  - tests/test_gui_file_table.py
autonomous: true
requirements:
  - CLEAN-01
  - FLOW-01
user_setup: []

must_haves:
  truths:
    - "Ctrl+wheel on an open page zooms toward the cursor (AnchorUnderMouse) by a half-step factor of sqrt(1.25); zoom clamps between half-viewport and 100x"
    - "update_smoothing disables SmoothPixmapTransform when zoom factor > 1 (pixel-accurate) and enables it when zoom <= 1 (image_viewer.py:116)"
    - "Opening a folder loads all supported images (flat, non-recursive, natural-sorted) into the FileTable sidebar"
    - "Single-clicking a sidebar row loads that page into the canvas; the sidebar row shows an 80px row with a 64x64 thumbnail and 1-indexed page number"
    - "The current page row is highlighted with rgba(0,212,255,0.18) background and a 2px #00d4ff left border"
    - "Ctrl+O opens a single image; Ctrl+Shift+O opens a folder; drag-drop images or a folder onto the window loads them"
    - "Empty canvas shows centered heading 'No page open' and body 'Open a single image or a folder of images to begin cleaning.'"
    - "QImage objects created from numpy/file buffers are .copy()-detached before storage (RESEARCH Pitfall 2)"
    - "File-open path validation: Path.resolve() is called and only png/jpg/jpeg/webp/bmp suffixes are accepted (T-01-02 mitigation)"
    - "Image size limit enforced: images exceeding 10000x10000px are rejected with a 'file unreadable' dialog (T-01-03 mitigation)"
  artifacts:
    - "manga_ai_studio/gui/canvas.py extended with wheelEvent, zoom, zoom_in, zoom_out, zoom_reset, zoom_fit (fit_to_window), update_smoothing, pan helpers (Space+drag, middle-mouse), empty-state overlay"
    - "manga_ai_studio/gui/file_table.py with class FileTable(QListView) emitting file_clicked(Path), files_dropped(list[Path]), folder_dropped(Path)"
    - "manga_ai_studio/core/image_file.py with class ImageFile(path: Path, thumbnail: QPixmap | None, mask: QImage | None, dirty: bool) and load_thumbnail()"
    - "manga_ai_studio/gui/main_window.py extended with File menu (Open Image Ctrl+O, Open Folder Ctrl+Shift+O, Recent Files, Quit Ctrl+Q), View menu (Fit Ctrl+0, 100% Ctrl+1, Zoom In Ctrl++, Zoom Out Ctrl+-), Pages dock + Tools dock placeholders, status bar (3 fields), empty-state"
    - "tests/test_gui_file_table.py with test_load_folder and test_navigation"
  key_links:
    - "MainWindow.open_folder -> ImageFile.scan -> FileTable.set_pages -> FileTable.file_clicked -> EditorCanvas.set_image"
    - "EditorCanvas.wheelEvent (Ctrl modifier) -> EditorCanvas.zoom(sqrt(1.25) or 1/sqrt(1.25))"
    - "FileTable -> DropEvent -> files_dropped/folder_dropped signals -> MainWindow handler"
  prohibitions:
    - "Must NOT vendor MangaCleaner_GPU frontend/widgets.py source verbatim (all-rights-reserved per D-12); the FileTable is our own reimplementation patterned after the QListView shape"
    - "Must NOT run image load/decode on the GUI thread for batch folder scans (use QThreadPool; RESEARCH Pitfall 3)"
---

<objective>
Deliver the full image-viewer vertical slice (CLEAN-01, FLOW-01): a user can open a single image
or a folder of images, navigate between pages via a file-list sidebar, and pan/zoom the canvas.

Per UI-SPEC surfaces 2 (canvas), 3 (file-list sidebar), 4 (open workflow), and the §Keyboard
Shortcut Reference. Pan/zoom mechanics are adapted from PanelCleaner `image_viewer.py` (GPL,
vendored per D-12). The FileTable is our own reimplementation patterned after the QListView
shape — MangaCleaner_GPU `frontend/widgets.py` is reference-only (no LICENSE, D-12).

Purpose: complete viewing/navigation parity so the user can move through a chapter.
Output: pan/zoomable canvas + file-list sidebar + open workflow + empty state.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-cleaning-workspace/01-CONTEXT.md
@.planning/phases/01-cleaning-workspace/01-RESEARCH.md
@.planning/phases/01-cleaning-workspace/01-PATTERNS.md
@.planning/phases/01-cleaning-workspace/01-UI-SPEC.md
@.planning/phases/01-cleaning-workspace/01-VALIDATION.md
@.planning/phases/01-cleaning-workspace/01-SKELETON.md
@.planning/phases/01-cleaning-workspace/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canvas pan/zoom mechanics + image-size/path validation + empty state</name>
  <files>
    manga_ai_studio/gui/canvas.py,
    manga_ai_studio/core/image_file.py,
    tests/test_gui_canvas.py
  </files>
  <read_first>
    C:\Src\PanelCleaner\pcleaner\gui\image_viewer.py (lines 1-60 for QImageReader.setAllocationLimit + __init__, 116-160 for update_smoothing + wheelEvent + zoom_in/out, 221-260 for zoom() clamp logic, 160-220 for zoom_fit; GPL, vendor/adapt the mechanics verbatim into our canvas),
    .planning\phases\01-cleaning-workspace\01-UI-SPEC.md (§Surface 2 Image canvas lines 182-194, §Color lines 86-114, §Spacing lines 44-66, §Keyboard lines 280-308, §Copywriting lines 118-141),
    .planning\phases\01-cleaning-workspace\01-PATTERNS.md (§gui/canvas.py lines 362-444, §core/image_file.py lines 539-546, §Shared Pattern 5 image/mask buffer lifetime lines 673-685),
    .planning\phases\01-cleaning-workspace\01-RESEARCH.md (§Common Pitfalls P2/P4 lines 659-678, §Security Domain lines 916-940),
    manga_ai_studio/gui/canvas.py (the skeleton file from plan 01 — extend it, do not rewrite)
  </read_first>
  <behavior>
    - Test 1 (test_zoom_wheel_half_step): calling canvas.zoom(factor) with a starting zoom of 1.0 and factor sqrt(1.25) results in zoom_factor ~1.118 (half-step); the transform scale matches
    - Test 2 (test_zoom_clamp_100x): repeated zoom_in past 100x clamps at exactly 100.0; zoom_factor never exceeds 100.0
    - Test 3 (test_zoom_clamp_min): zooming out below half-viewport size is a no-op (zoom_factor unchanged when factor<1 and proposed size < viewport/2)
    - Test 4 (test_update_smoothing_pixel_accurate): when zoom_factor > 1, render hint SmoothPixmapTransform is OFF; when <= 1 it is ON
    - Test 5 (test_image_size_limit_rejects_huge): set_image or a helper validate_image_size(path) raises/returns failure for a synthetic 10001x10001px image (T-01-03)
    - Test 6 (test_path_validation_rejects_bad_suffix): validate_image_path rejects a .txt or .exe path; accepts .png/.jpg/.jpeg/.webp/.bmp (T-01-02)
    - Test 7 (test_empty_state_heading): when no image is loaded, the canvas shows the text "No page open"
  </behavior>
  <action>
    Extend `manga_ai_studio/gui/canvas.py` (the skeleton from plan 01). Add module constant `ZOOM_TICK_FACTOR = 1.25` (image_viewer.py:14). In `__init__` add: `self.zoom_factor = 1.0`, `self.min_zoom = None` (computed dynamically), max 100.0; `self._panning = False`, `self._last_pan_pos = QPointF()`, `self._space_held = False`; connect no signals here (pan is via mouse events). Keep the existing #0b0b0e background, AnchorUnderMouse, and QImageReader.setAllocationLimit(0).

    Add `def wheelEvent(self, event)` adapted from image_viewer.py:125-142 (GPL, vendor the logic): if `Qt.ControlModifier & event.modifiers()`: positive angleDelta.y -> `self.zoom_in(wheel=True)`, negative -> `self.zoom_out(wheel=True)`; elif `Qt.ShiftModifier & event.modifiers()`: horizontal scroll (adjust horizontalScrollBar value by -angleDelta.y); else call `super().wheelEvent(event)` (default vertical pan).

    Add `def zoom_in(self, wheel=False)` and `def zoom_out(self, wheel=False)` from image_viewer.py:144-154: wheel=True uses half-step `ZOOM_TICK_FACTOR ** 0.5` (zoom_in) or `1 / (ZOOM_TICK_FACTOR ** 0.5)` (zoom_out); full uses `ZOOM_TICK_FACTOR` / `1 / ZOOM_TICK_FACTOR`. Each calls `self.zoom(factor)`.

    Add `def zoom(self, factor, *, suppress_signals=False)` adapted from image_viewer.py:221-255 (GPL). Compute `proposed = min(self.zoom_factor * factor, 100.0)` (max 100x clamp). Compute proposed_width/height in viewport coords; if `proposed_width < view_width/2 and proposed_height < view_height/2 and factor < 1` -> return (min = half viewport, no-op). Else set `self.zoom_factor = proposed`, call `self.update_smoothing()`, `self.setTransform(QTransform().scale(self.zoom_factor, self.zoom_factor))`. Emit a `zoom_changed = Signal(float)` (declared on the class) with the new factor.

    Add `def update_smoothing(self)` from image_viewer.py:116: if `self.zoom_factor > 1`: `self.setRenderHint(QPainter.SmoothPixmapTransform, False)` (pixel-accurate); else `True` (smooth).

    Add `def zoom_fit(self)` (alias for the skeleton's `fit_to_window`) from image_viewer.py:160: reset zoom to fit image in viewport keeping aspect ratio, then set `self.zoom_factor` to the resulting scale and call `update_smoothing()`. Keep the existing `fit_to_window` as the public name; `zoom_fit` is the alias. Add `def zoom_reset(self)` -> set zoom_factor to 1.0 and reapply transform (image_viewer.py:156, Ctrl+1 Actual Size). Add `def actual_size(self)` alias.

    Add pan mechanics: `def mousePressEvent(self, event)` — if middle button OR (`self._space_held` and left button): set `self._panning = True`, `self._last_pan_pos = event.position()`, set cursor ClosedHandCursor; else if tool is Move/no-tool pass through. `def mouseMoveEvent` — if panning, compute delta from last_pan_pos, scroll by delta (use `self.horizontalScrollBar().setValue(value - dx)` etc.), update last_pan_pos. `def mouseReleaseEvent` — clear panning, restore cursor. `def keyPressEvent`/`keyReleaseEvent` — track Space (set `self._space_held`, set OpenHandCursor while held) — only when the canvas has focus. (UI-SPEC surface 2 pan: middle-drag OR Space+left-drag OR scrollbars.)

    Add the empty-state overlay: a `QGraphicsTextItem` (or a paintEvent overlay) showing heading "No page open" (16px semibold, muted #9a9aa2), body "Open a single image or a folder of images to begin cleaning." (14px regular muted), hint "File → Open Image… (Ctrl+O) · or drag files here" (12px accent #00d4ff), centered, shown when `self.image_item.pixmap().isNull()` (UI-SPEC §Surface 9 Empty state, §Copywriting). Add `def _update_empty_state(self)` toggling the overlay visibility whenever set_image/clear is called.

    Add image-size validation: `def validate_image_size(self, width: int, height: int) -> bool` returning False if `width > 10000 or height > 10000` (T-01-03 DoS mitigation, RESEARCH §Security Domain). Add a module-level (or staticmethod) `def validate_image_path(path: Path) -> bool` that calls `path.resolve()` (T-01-02 path traversal mitigation) and checks `path.suffix.lower() in {".png", ".jpg", ".jpeg", ".webp", ".bmp"}` — returns True only if both pass. Modify `set_image` to accept either a QPixmap or (via a new overload `set_image_from_path(path: Path)`) validate the path via `validate_image_path`, load via `QImage(path)`, call `.copy()` on the QImage (RESEARCH Pitfall 2 / PATTERNS.md §Shared Pattern 5 — QImage lifetime safety), check `validate_image_size(qimage.width(), qimage.height())` and raise `ValueError` (or return False) if too large, then convert to QPixmap and call the existing set_image.

    Create `manga_ai_studio/core/image_file.py` with `class ImageFile` (role-match PanelCleaner `gui/image_file.py:ImageFile` per PATTERNS.md §core/image_file.py lines 539-546; keep minimal). Fields (use `@dataclass` or plain `__init__`): `path: Path`, `thumbnail: QPixmap | None = None`, `mask: QImage | None = None`, `dirty: bool = False`. Methods: `load_thumbnail(self, size: int = 64) -> None` that loads the image via QImage, scales to a 64x64 letterboxed QPixmap (aspect-preserved, padded on #2d2d33 per UI-SPEC surface 3) storing it in `self.thumbnail`; `clear_mask(self) -> None`. Note: UI-SPEC overrides the thumbnail size to 64 (PanelCleaner's THUMBNAIL_SIZE is larger).

    Extend `tests/test_gui_canvas.py` with the 7 behaviors listed above. For test_zoom_clamp_100x: start zoom_factor=1.0, call zoom_in() repeatedly, assert zoom_factor never exceeds 100.0 and reaches exactly 100.0. For test_image_size_limit_rejects_huge: construct a QImage(10001, 10001, QImage.Format_RGB32), call canvas.validate_image_size(10001, 10001), assert False. For test_path_validation_rejects_bad_suffix: assert validate_image_path(Path("x.txt")) is False and validate_image_path(Path("a/b.png")) is True. Use `qtbot` fixtures; if a real display is unavailable, gate heavy zoom tests behind `pytest.mark.skipif` on `not QApplication.instance()`.
  </action>
  <verify>
    <automated>pytest tests/test_gui_canvas.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `manga_ai_studio/gui/canvas.py` contains `ZOOM_TICK_FACTOR = 1.25`, `def wheelEvent`, `def zoom(self, factor`, `min(self.zoom_factor * factor, 100.0)`, `def update_smoothing`, `setRenderHint(QPainter.SmoothPixmapTransform, False)`, `def zoom_fit`, `def zoom_reset`
    - Source: canvas contains `def validate_image_size` with `10000` bound, `def validate_image_path` with `path.resolve()` and the suffix allowlist {png,jpg,jpeg,webp,bmp}
    - Source: canvas contains `Signal(float)` named `zoom_changed` and empty-state text "No page open" and "Open a single image or a folder of images to begin cleaning."
    - Source: `manga_ai_studio/core/image_file.py` contains `class ImageFile` with fields `path`, `thumbnail`, `mask`, `dirty` and `def load_thumbnail`
    - Behavior: test_zoom_wheel_half_step passes (sqrt(1.25) half-step)
    - Behavior: test_zoom_clamp_100x passes (max 100.0)
    - Behavior: test_zoom_clamp_min passes (half-viewport min)
    - Behavior: test_update_smoothing_pixel_accurate passes (SmoothPixmapTransform toggles at zoom 1)
    - Behavior: test_image_size_limit_rejects_huge passes (T-01-03)
    - Behavior: test_path_validation_rejects_bad_suffix passes (T-01-02)
    - Behavior: test_empty_state_heading passes
    - CLI: `pytest tests/test_gui_canvas.py -x` exits 0
  </acceptance_criteria>
  <done>
    Canvas supports Ctrl+wheel half-step zoom (sqrt(1.25)), 100x max / half-viewport min clamp, AnchorUnderMouse, pixel-accurate smoothing toggle, pan via middle-mouse/Space+drag, empty-state overlay, and validates image paths (Path.resolve + suffix allowlist) and size (10000x10000 cap). ImageFile data model exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FileTable sidebar (QListView) + folder open workflow + drag-drop + MainWindow wiring</name>
  <files>
    manga_ai_studio/gui/file_table.py,
    manga_ai_studio/gui/main_window.py,
    tests/test_gui_file_table.py
  </files>
  <read_first>
    C:\Users\Stella\Downloads\MangaCleaner_GPU\_internal\src\frontend\widgets.py (lines 11-19 for FileListWidget QListView shape — REFERENCE ONLY per D-12, reimplement do not copy),
    C:\Src\PanelCleaner\pcleaner\gui\file_table.py (lines 1-100 for natural-sort via natsort + setIconSize pattern; GPL, may vendor the natsort usage and icon-size constant reference),
    .planning\phases\01-cleaning-workspace\01-UI-SPEC.md (§Surface 3 File-list sidebar lines 195-206, §Surface 4 Open workflow lines 208-214, §Surface 1 Main-window shell lines 160-181, §Keyboard lines 280-308, §Copywriting empty states lines 124-130),
    .planning\phases\01-cleaning-workspace\01-PATTERNS.md (§gui/file_table.py lines 448-469, §gui/main_window.py lines 274-359),
    .planning\phases\01-cleaning-workspace\01-VALIDATION.md (test_gui_file_table.py rows lines 47-48),
    manga_ai_studio/gui/main_window.py (the plan-01 skeleton — extend File/View menus, add docks and status bar)
  </read_first>
  <behavior>
    - Test 1 (test_load_folder_populates): FileTable.set_pages(list_of_paths) creates one row per path, natural-sorted, with the filename text; rowCount/itemCount equals the number of paths
    - Test 2 (test_navigation_click_emits_signal): clicking/selecting a row emits file_clicked(Path) with the correct path; selecting row index N returns the N-th natural-sorted path
    - Test 3 (test_thumbnail_64): each row's decoration role is a QPixmap whose dimensions are <= 64x64 (letterboxed)
    - Test 4 (test_current_row_highlight): setting the current row applies the selected-row style (rgba(0,212,255,0.18) bg + 2px accent border via QSS) — assertable via the QSS string set on the widget
    - Test 5 (test_open_folder_action): MainWindow has a QAction "Open Folder…" with shortcut Ctrl+Shift+O
    - Test 6 (test_drop_images_signal): dropping mimedata with image file urls emits files_dropped(list[Path])
    - Test 7 (test_status_bar_page_progress): after loading N pages and selecting page i, the status bar right field reads "Page {i+1} / {N}"
  </behavior>
  <action>
    Create `manga_ai_studio/gui/file_table.py` with `class FileTable(QListView)` (our own reimplementation patterned after the QListView shape from MangaCleaner_GPU widgets.py:11-19 — D-12 REFERENCE-ONLY, do not copy; natural-sort pattern from PanelCleaner file_table.py:12 natsort — GPL, vendor the import). NOTE: `natsort` is already declared in `pyproject.toml [project.dependencies]` by plan 01-01 (single source of truth) — do NOT re-declare it here, and do NOT touch pyproject.toml in this plan. In `__init__`: `super().__init__()`, set `self.setViewMode(QListView.ListMode)`, `self.setMovement(QListView.Static)`, `self.setResizeMode(QListView.Adjust)`, `self.setIconSize(QSize(64, 64))`, `self.setUniformItemSizes(True)`, natural-sort via `from natsort import natsorted` (natsort is a declared runtime dep from plan 01-01; no fallback branch needed). Apply QSS (UI-SPEC surface 3 + §Color): `background: #2d2d33; border: none;` for the view, `QListView::item { height: 80px; padding: 8px; border-left: 2px solid transparent; }` and `QListView::item:selected { background: rgba(0,212,255,0.18); border-left: 2px solid #00d4ff; color: #e8e8ea; }`. Set a `QStandardItemModel` as the model. Each item: decoration role = the 64x64 letterboxed thumbnail QPixmap (use `ImageFile.load_thumbnail(64)`), display role = filename, UserRole = the Path. Show a 1-indexed page number in muted text appended to the filename (e.g., "page01.jpg   ·   1"). Define signals `file_clicked = Signal(Path)`, `files_dropped = Signal(list)`, `folder_dropped = Signal(Path)`. Connect `self.clicked` to a slot emitting file_clicked with the item's UserRole path. Override `dragEnterEvent`/`dragMoveEvent` to accept proposed actions when mimedata has urls; override `dropEvent` to inspect urls — if exactly one url and it `isDir()`, emit `folder_dropped(Path)`; else collect all url local paths, filter to image suffixes via the canvas's `validate_image_path` (import from manga_ai_studio.gui.canvas), emit `files_dropped(list[Path])`. Add `def set_pages(self, paths: list[Path]) -> None` that clears the model, natural-sorts paths, builds one item per path. Add `def current_path(self) -> Path | None` returning the selected row's UserRole. Add placeholder text "No pages loaded — open a folder (Ctrl+Shift+O)." when empty (UI-SPEC surface 3 empty).

    Extend `manga_ai_studio/gui/main_window.py`. Build out the menu bar per UI-SPEC surface 1:
    - File: Open Image… `Ctrl+O` (already exists from plan 01 — keep), Open Folder… `Ctrl+Shift+O` (new, `action_open_folder` connected to `self.open_folder`), Recent Files submenu (max 8 via QSettings — wire a QMenu, populate from `QSettings("MangaAIStudio").value("recentFiles", [])`), separator, Quit `Ctrl+Q` (close the window).
    - View: Fit to Window `Ctrl+0` (existing), Actual Size `Ctrl+1` (new, connect to `self.canvas.zoom_reset`), Zoom In `Ctrl++` (connect `self.canvas.zoom_in`), Zoom Out `Ctrl+-` (connect `self.canvas.zoom_out`), separator, Toggle Sidebar, Toggle Tools (placeholder toggles for the docks), separator, Toggle Mask Overlay `M` (no-op stub here — wired in plan 03/04), Show Original `P` (no-op stub — wired in plan 05).
    - Edit menu (placeholder for plan 06 undo/redo): Undo Image Ctrl+Z, Redo Image Ctrl+Shift+Z, Undo Mask Alt+Z, Redo Mask Alt+Shift+Z (all disabled until their plans wire them), separator, Clear Mask… (disabled).
    - Tools menu (placeholder): Detect Text `D` (disabled — plan 03), Inpaint `C` (disabled — plan 05), separator, Move/Pan `V`, Brush `B`, Rectangle `R`, Lasso `L`, Eraser `E` (disabled — plan 04).
    - Help: About (QMessageBox with version + GPL v3 notice).
    Add a single top toolbar (UI-SPEC surface 1): Open, (sep), Fit / 100% / Zoom Out / Zoom In, (sep), Toggle Mask Overlay. Other toolbar sections (Detect/Inpaint/Tools/Undo) added by their plans. Sections separated by `addSeparator()`.

    Add the docks: `self.dock_pages = QDockWidget("Pages", self)` containing `self.file_table = FileTable()`; add to `LeftDockWidgetArea`. `self.dock_tools = QDockWidget("Tools", self)` (placeholder widget for plan 04); add to `RightDockWidgetArea`. Both docks user-movable/closable; restore default layout on launch.

    Add the 3-field status bar (UI-SPEC surface 1): `self.status_bar_left = QLabel("")` (operation/status), `self.status_bar_center = QLabel("")` (cursor coords + zoom %, mono font Consolas), `self.status_bar_right = QLabel("")` (page progress "Page {n} / {total}"). Add them to `self.statusBar()`. Connect `self.canvas.zoom_changed` to a slot updating the center field's zoom %.

    Implement `def open_folder(self) -> None`: `QFileDialog.getExistingDirectory`, then scan the directory non-recursively for image files (`[p for p in dir.iterdir() if p.is_file() and validate_image_path(p)]`), build `list[ImageFile]`, call `self.file_table.set_pages([p for imf in ...])` storing the ImageFile list on the window (`self.image_files`). Connect `self.file_table.file_clicked` to `self.on_page_selected(Path)` which finds the ImageFile, loads its full pixmap into the canvas via `set_image_from_path` (with .copy() discipline and size validation), updates the window title, the status bar right field, and selects the row. Handle `files_dropped`/`folder_dropped` signals by routing to the same scan+set_pages path. Add `def on_page_selected(self, path: Path) -> None`.

    Extend `tests/test_gui_file_table.py` (VALIDATION.md rows 47-48). Build a temp folder with 3 fake images (small PNGs named page2.png, page10.png, page1.png to test natural sort -> page1, page2, page10). Implement the 7 behaviors: test_load_folder_populates asserts model row count == 3 and item(0).text() contains "page1"; test_navigation_click_emits_signal uses `qtbot` and `QTest`/`file_table.setCurrentIndex` then asserts the file_clicked signal carried the page1 path; test_thumbnail_64 asserts item(0).data(Qt.DecorationRole).size() is within 64x64; test_current_row_highlight asserts the FileTable stylesheet contains "rgba(0,212,255,0.18)" and "#00d4ff"; test_open_folder_action asserts MainWindow's Open Folder action has shortcut Ctrl+Shift+O; test_drop_images_signal builds a QMimeData with image urls and asserts files_dropped emitted; test_status_bar_page_progress loads 3 pages, selects index 1, asserts status_bar_right.text() == "Page 2 / 3".
  </action>
  <verify>
    <automated>pytest tests/test_gui_file_table.py tests/test_gui_canvas.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `manga_ai_studio/gui/file_table.py` contains `class FileTable(QListView)`, `QListView.ListMode`, `setIconSize(QSize(64, 64))`, `natsorted` (or local natural_key), signals `file_clicked = Signal(Path)` / `files_dropped = Signal(list)` / `folder_dropped = Signal(Path)`, and QSS containing "rgba(0,212,255,0.18)" and "#00d4ff"
    - Source: `manga_ai_studio/gui/main_window.py` contains `QAction("Open Folder…"` with `QKeySequence("Ctrl+Shift+O")`, `QAction("Open Image…"`, docks `dock_pages`/`dock_tools`, `LeftDockWidgetArea`/`RightDockWidgetArea`, status bar with `Page {` ... `/ {total}` pattern, and "No pages loaded" appears in the file_table placeholder or main window empty state
    - Source: main_window contains `QAction("Quit"` with `Ctrl+Q`, View menu items Fit `Ctrl+0`, Actual Size `Ctrl+1`, Zoom In `Ctrl++`, Zoom Out `Ctrl+-`, Help -> About
    - Behavior: test_load_folder_populates passes (natural sort page1, page2, page10)
    - Behavior: test_navigation_click_emits_signal passes (file_clicked emitted with correct path)
    - Behavior: test_thumbnail_64 passes (<= 64x64)
    - Behavior: test_current_row_highlight passes (QSS applied)
    - Behavior: test_open_folder_action passes (Ctrl+Shift+O)
    - Behavior: test_drop_images_signal passes
    - Behavior: test_status_bar_page_progress passes ("Page 2 / 3")
    - CLI: `pytest tests/test_gui_file_table.py tests/test_gui_canvas.py -x` exits 0
    - D-12 compliance: gui/file_table.py docstring says "reimplemented patterned after MangaCleaner_GPU widgets.py" and does NOT say "copied"/"vendored from MangaCleaner_GPU"
  </acceptance_criteria>
  <done>
    FileTable QListView sidebar loads a folder (natural-sorted, 64x64 thumbnails, 80px rows, accent highlight); clicking a row loads the page; Open Folder (Ctrl+Shift+O) and drag-drop wired; MainWindow has the full menu/toolbar/dock/status-bar shell from UI-SPEC surface 1; canvas pan/zoom from Task 1 works end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> app (folder open, drag-drop, recent files) | User-selected directory or dropped files cross into scan + QImage load |
| drag-drop mimedata -> app | External drop payload parsed into paths |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering (path traversal) | MainWindow.open_folder / FileTable.dropEvent / recent files | high | mitigate | All file-open paths flow through `validate_image_path(path)` which calls `path.resolve()` and enforces the suffix allowlist {png,jpg,jpeg,webp,bmp}. Folder scan uses `Path.iterdir()` + `is_file()` + suffix filter — symlink resolution is handled by resolve(). Recent-files paths re-validated on open. |
| T-01-03 | Denial of Service (large image) | EditorCanvas.set_image_from_path | medium | mitigate | `validate_image_size(width, height)` rejects images > 10000x10000px; failure shows the UI-SPEC "file unreadable" dialog. QImageReader.setAllocationLimit(0) is already set for Qt's internal cap. |
| T-01-05 | Tampering (drag-drop path injection) | FileTable.dropEvent | medium | mitigate | dropEvent inspects `QMimeData.urls()`, takes only `url.toLocalFile()`, filters via `validate_image_path`, and ignores non-local/non-file URLs entirely. No remote URL fetching. |
| T-01-06 | Spoofing (filename display injection) | FileTable row text / status bar | low | mitigate | Filenames are displayed via QListWidgetItem text (Qt escapes markup) and QLabel text — Qt does not interpret HTML by default in these widgets. No setRichText/setHtml on user-controlled strings. |
</threat_model>

<verification>
- `pytest tests/test_gui_canvas.py tests/test_gui_file_table.py -x` green
- Open a folder of manga pages -> sidebar populates, click navigates, pan/zoom responsive (manual check per VALIDATION.md §Manual-Only)
- Empty state shows when no page is open
</verification>

<success_criteria>
- Canvas: Ctrl+wheel half-step zoom, 100x max / half-viewport min, AnchorUnderMouse, pixel-accurate smoothing, pan (middle-mouse / Space+drag), empty state
- FileTable: QListView, 64x64 thumbnails, natural-sort, accent-highlighted current row, click-to-load, drag-drop
- Open workflow: Open Image (Ctrl+O), Open Folder (Ctrl+Shift+O), recent files, drag-drop onto window
- MainWindow shell: full menu bar (File/Edit/View/Tools/Help), toolbar, Pages + Tools docks, 3-field status bar
- Security: path validation (resolve + suffix allowlist) and image-size cap (10000x10000) enforced on every open path
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 — Image Viewer)

**Modules / classes:**
- `manga_ai_studio.gui.canvas.EditorCanvas` (extended): `wheelEvent`, `zoom(factor)`, `zoom_in(wheel=False)`, `zoom_out(wheel=False)`, `zoom_reset()` / `actual_size()`, `zoom_fit()` / `fit_to_window()`, `update_smoothing()`, pan via `mousePressEvent`/`mouseMoveEvent`/`mouseReleaseEvent`/`keyPressEvent`/`keyReleaseEvent`, `validate_image_size(w,h)`, `validate_image_path(path)` (staticmethod/module), `set_image_from_path(path)`, `_update_empty_state()`; signal `zoom_changed = Signal(float)`; constant `ZOOM_TICK_FACTOR = 1.25`
- `manga_ai_studio.gui.file_table.FileTable(QListView)`: `set_pages(paths)`, `current_path()`, signals `file_clicked(Path)`, `files_dropped(list)`, `folder_dropped(Path)`; QSS with rgba(0,212,255,0.18) selected-row style
- `manga_ai_studio.core.image_file.ImageFile`: fields `path: Path`, `thumbnail: QPixmap|None`, `mask: QImage|None`, `dirty: bool`; methods `load_thumbnail(size=64)`, `clear_mask()`
- `manga_ai_studio.gui.main_window.MainWindow` (extended): `open_folder()`, `on_page_selected(path)`, drop handlers; menus File (Open Image Ctrl+O, Open Folder Ctrl+Shift+O, Recent Files, Quit Ctrl+Q), Edit (Undo/Redo placeholders, disabled), View (Fit Ctrl+0, Actual Size Ctrl+1, Zoom In Ctrl++, Zoom Out Ctrl+-, Toggle Sidebar/Tools, Toggle Mask Overlay M, Show Original P), Tools (Detect Text D, Inpaint C, Move V, Brush B, Rectangle R, Lasso L, Eraser E — disabled until plans 03/04/05), Help (About); toolbar; docks `dock_pages`/`dock_tools`; status bar 3 fields `status_bar_left`/`status_bar_center`/`status_bar_right`

**Config / QSS:**
- FileTable QSS (selected-row style: rgba(0,212,255,0.18) bg + 2px #00d4ff border)
- QSettings key "recentFiles" (list of recent file paths, max 8)

**Test files:**
- `tests/test_gui_canvas.py` (extended): zoom/clamp/smoothing/empty-state/size-limit/path-validation tests
- `tests/test_gui_file_table.py` (new): `test_load_folder_populates`, `test_navigation_click_emits_signal`, `test_thumbnail_64`, `test_current_row_highlight`, `test_open_folder_action`, `test_drop_images_signal`, `test_status_bar_page_progress`
</artifacts_produced>

<output>
Create `.planning/phases/01-cleaning-workspace/01-02-SUMMARY.md` when done
</output>
