---
phase: 01-cleaning-workspace
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - manga_ai_studio/adapters/__init__.py
  - manga_ai_studio/adapters/torch_impl.py
  - manga_ai_studio/adapters/factory.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/canvas.py
  - panelcleaner/comic_text_detector/__init__.py
  - panelcleaner/comic_text_detector/inference.py
  - panelcleaner/comic_text_detector/basemodel.py
  - panelcleaner/comic_text_detector/LICENSE
  - panelcleaner/comic_text_detector/utils/__init__.py
  - panelcleaner/comic_text_detector/utils/textmask.py
  - panelcleaner/comic_text_detector/utils/textblock.py
  - panelcleaner/comic_text_detector/utils/imgproc_utils.py
  - panelcleaner/comic_text_detector/utils/io_utils.py
  - panelcleaner/comic_text_detector/utils/db_utils.py
  - panelcleaner/comic_text_detector/utils/yolov5_utils.py
  - tests/test_detection/__init__.py
  - tests/test_detection/test_ctd_adapter.py
autonomous: true
requirements:
  - CLEAN-02
user_setup:
  - service: comic_text_detector_model
    why: "CTD (Comic Text Detector) PyTorch model weights are required to run text detection. The vendored model_downloader fetches them from HuggingFace on first use."
    env_vars: []
    dashboard_config:
      - task: "On first run of Tools -> Detect Text, allow the model download (~100-200MB) to complete. If behind a proxy, set HF endpoint env vars per model_downloader.py."
        location: "Triggered automatically by panelcleaner.model_downloader on first detection run"

must_haves:
  truths:
    - "Tools -> Detect Text (D) runs CTD text detection asynchronously and does NOT freeze the UI (RESEARCH Pitfall 3)"
    - "TorchCTDModel.detect() unpacks the TextDetector.__call__ return as a 3-tuple: mask, mask_refined, blk_list = self.detector(image, refine_mode=REFINEMASK_ANNOTATION, keep_undetected_mask=True) (inference.py:166 signature, :210 return — VERIFIED 3-tuple, NOT 5-tuple)"
    - "REFINEMASK_ANNOTATION is imported from panelcleaner.comic_text_detector.utils.textmask (the vendored location; inference.py imports it from utils.textmask and re-exports)"
    - "The detected mask is composited into the canvas mask layer using rgba(255,0,0,0.63) (UI-SPEC §Color mask overlay)"
    - "Detection runs in a Worker(QRunnable) per gui/worker_thread.py; the worker emits progress(percent, message) and result; the main thread mutates Qt only in signal handlers (RESEARCH Pitfall 3)"
    - "If a mask already exists when Detect Text is pressed, the 'Replace the current mask with a new detection?' confirmation dialog is shown (UI-SPEC §Copywriting)"
    - "Model-load failure shows a QMessageBox::Critical 'Couldn't load the detection model.' and a persistent #7a1f1f error chip in the status bar (UI-SPEC surface 9, §Copywriting)"
    - "backend selection: config key detection_backend resolves 'torch' -> TorchCTDModel via the adapter factory (D-02); 'onnx' -> OnnxDetectionModel (raises NotImplementedError)"
    - "Model weight paths are validated (FileNotFoundError propagated from TextDetector if the model path is missing — inference.py pattern; T-01-04)"
  artifacts:
    - "panelcleaner/comic_text_detector/ vendored (inference.py, basemodel.py, utils/*, LICENSE) with pcleaner. -> panelcleaner. import rewrites"
    - "manga_ai_studio/adapters/torch_impl.py with class TorchCTDModel(DetectionModel) implementing load() and detect() against the VERIFIED 3-tuple return"
    - "manga_ai_studio/adapters/factory.py with backend_factory(kind: str, backend: str) -> DetectionModel|InpaintModel|OCRModel"
    - "manga_ai_studio/gui/main_window.py extended: Tools -> Detect Text (D) enabled, worker wiring, on_detection_finished handler, replace-mask confirmation, error chip"
    - "manga_ai_studio/gui/canvas.py extended: set_mask(qimage), toggle_mask_overlay(), mask overlay visibility state"
    - "tests/test_detection/test_ctd_adapter.py asserting the 3-tuple unpack and the adapter contract (with a mock TextDetector)"
  key_links:
    - "MainWindow.detect_text -> Worker(run_detection_task) -> TorchCTDModel.detect (3-tuple unpack) -> Worker.finished(result) -> MainWindow.on_detection_finished -> EditorCanvas.set_mask"
    - "adapters/factory.backend_factory('detection', config.detection_backend) -> TorchCTDModel (D-02)"
    - "TorchCTDModel.detect -> self.detector(image, refine_mode=REFINEMASK_ANNOTATION, keep_undetected_mask=True) -> 3-tuple (mask, mask_refined, blk_list)"
  prohibitions:
    - "Must NOT unpack the TextDetector.__call__ return as a 5-tuple (CONTEXT.md/RESEARCH.md claim a 5-tuple — that is INCORRECT; VERIFIED source returns a 3-tuple at inference.py:210). A 5-tuple unpack will raise ValueError."
    - "Must NOT import torch or TextDetector directly in gui/main_window.py — all model access goes through the adapter (RESEARCH §Don't Hand-Roll)"
    - "Must NOT run TextDetector inference on the GUI thread (RESEARCH Pitfall 3)"
---

<objective>
Deliver the text-detection vertical slice (CLEAN-02): a user can run heatmap text detection on a
page and see an auto-generated mask of text regions composited onto the canvas.

Per D-01/D-02: implement the concrete `TorchCTDModel` adapter in `adapters/torch_impl.py` and a
`backend_factory` resolver. **CRITICAL — contract against the VERIFIED 3-tuple return** of
`TextDetector.__call__` at `inference.py:210` (`return mask, mask_refined, blk_list`), NOT the
5-tuple claimed in CONTEXT.md/RESEARCH.md. The orchestrator's pattern-mapper correction is
binding.

Vendor PanelCleaner's `comic_text_detector/` (GPL, D-12). Wire async detection via the
`Worker(QRunnable)` pattern; the worker is the frontend-env side of the D-07/D-08 boundary —
Phase 1 runs detection in-process via QThreadPool (single-env fallback, D-09b); the subprocess
IPC split is a future optimization if it becomes necessary.

Purpose: gives the user auto-detection as the starting point for mask editing (plan 04).
Output: CTD adapter, backend factory, async detection worker, mask-overlay display.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-cleaning-workspace/01-CONTEXT.md
@.planning/phases/01-cleaning-workspace/01-RESEARCH.md
@.planning/phases/01-cleaning-workspace/01-PATTERNS.md
@.planning/phases/01-cleaning-workspace/01-UI-SPEC.md
@.planning/phases/01-cleaning-workspace/01-VALIDATION.md
@.planning/phases/01-cleaning-workspace/01-01-PLAN.md
@.planning/phases/01-cleaning-workspace/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor comic_text_detector + implement TorchCTDModel adapter against the VERIFIED 3-tuple + backend factory</name>
  <files>
    panelcleaner/comic_text_detector/__init__.py,
    panelcleaner/comic_text_detector/inference.py,
    panelcleaner/comic_text_detector/basemodel.py,
    panelcleaner/comic_text_detector/LICENSE,
    panelcleaner/comic_text_detector/utils/__init__.py,
    panelcleaner/comic_text_detector/utils/textmask.py,
    panelcleaner/comic_text_detector/utils/textblock.py,
    panelcleaner/comic_text_detector/utils/imgproc_utils.py,
    panelcleaner/comic_text_detector/utils/io_utils.py,
    panelcleaner/comic_text_detector/utils/db_utils.py,
    panelcleaner/comic_text_detector/utils/yolov5_utils.py,
    manga_ai_studio/adapters/torch_impl.py,
    manga_ai_studio/adapters/factory.py,
    manga_ai_studio/adapters/__init__.py,
    tests/test_detection/__init__.py,
    tests/test_detection/test_ctd_adapter.py
  </files>
  <read_first>
    C:\Src\PanelCleaner\pcleaner\comic_text_detector\inference.py (FULL FILE — load-bearing: line 166 `def __call__(self, img, refine_mode=REFINEMASK_INPAINT, keep_undetected_mask=False)` signature, line 210 `return mask, mask_refined, blk_list` 3-tuple return; line 20 `from ...utils.textmask import REFINEMASK_*`; lines 130-163 TextDetector.__init__ device/input_size/act params),
    C:\Src\PanelCleaner\pcleaner\comic_text_detector\utils\textmask.py (REFINEMASK_INPAINT, REFINEMASK_ANNOTATION constants + refine_mask/refine_undetected_mask functions),
    C:\Src\PanelCleaner\pcleaner\comic_text_detector\ (directory listing — identify basemodel.py, utils/textblock.py, imgproc_utils.py, io_utils.py, db_utils.py, yolov5_utils.py for vendoring),
    .planning\phases\01-cleaning-workspace\01-PATTERNS.md (§adapters/torch_impl.py lines 105-156 — the CRITICAL CORRECTION at lines 19-34 and the verified 3-tuple unpack at 140-142; §adapters/onnx_impl.py lines 158-178),
    .planning\phases\01-cleaning-workspace\01-RESEARCH.md (§Pattern 1 lines 230-358 — NOTE lines 320-329 and 722-731 unpack a 5-tuple; these are the INCORRECT upstream claims — do NOT follow them; use the 3-tuple from PATTERNS.md verification),
    .planning\phases\01-cleaning-workspace\01-CONTEXT.md (D-01, D-02, D-12),
    manga_ai_studio/adapters/base.py (the plan-01 ABCs to subclass),
    manga_ai_studio/adapters/onnx_impl.py (the plan-01 OnnxDetectionModel stub the factory resolves to for backend='onnx')
  </read_first>
  <action>
    Vendor PanelCleaner's `comic_text_detector/` package (D-12 GPL-compat, near-verbatim). Copy the full directory tree from `C:\Src\PanelCleaner\pcleaner\comic_text_detector\` into `panelcleaner/comic_text_detector/`, including: `__init__.py`, `inference.py`, `basemodel.py`, `LICENSE` (keep the upstream GPL v3 license file in this dir), and `utils/` (`__init__.py`, `textmask.py`, `textblock.py`, `imgproc_utils.py`, `io_utils.py`, `db_utils.py`, `yolov5_utils.py`). Perform ONLY the mechanical import rewrite `pcleaner.` -> `panelcleaner.` / `from pcleaner` -> `from panelcleaner` everywhere imports appear. Do NOT refactor the `TextDetector.__call__` signature (line 166) or its return statement (line 210) — both stay exactly as upstream. Verify after copy that `inference.py` line ~210 still reads `return mask, mask_refined, blk_list` (3-tuple).

    Create `manga_ai_studio/adapters/torch_impl.py` (D-01/D-02; per PATTERNS.md §adapters/torch_impl.py lines 105-156). Imports: stdlib (os, pathlib.Path, typing), then numpy, then `from panelcleaner.comic_text_detector.inference import TextDetector` and `from panelcleaner.comic_text_detector.utils.textmask import REFINEMASK_ANNOTATION` (the vendored location; inference.py imports REFINEMASK_* from utils.textmask at line ~20 and re-exports — either import path works post-vendoring; use the utils.textmask path for explicitness), then `from manga_ai_studio.adapters.base import DetectionModel, InpaintModel`. Define:
    - `class TorchCTDModel(DetectionModel)`:
      - `__init__(self, config=None)`: store config; `self.detector = None`; `self.device = "cpu"`
      - `load(self, model_path: Path, device: str = "cpu") -> None`: probe `cuda = device == "cuda" or (device == "auto" and torch.cuda.is_available())` (import torch lazily inside load to keep the adapter importable without torch in the main_env — D-07); validate `model_path` exists (raise `FileNotFoundError(f"Model not found: {model_path}")` matching PanelCleaner inpainting.py:21 pattern — T-01-04 model path validation); construct `self.detector = TextDetector(model_path=str(model_path), input_size=1024, device="cuda" if cuda else "cpu", act="leaky")` (params from inference.py:115-116); store `self.device`.
      - `detect(self, image: np.ndarray) -> tuple[np.ndarray, list]`: assert `self.detector is not None` (raise RuntimeError("Model not loaded") if None); call `mask, mask_refined, blk_list = self.detector(image, refine_mode=REFINEMASK_ANNOTATION, keep_undetected_mask=True)` — **THIS IS THE VERIFIED 3-TUPLE UNPACK** (inference.py:166 signature, :210 return; do NOT use 5 targets — `img`/`refine_mode` are INPUTS not returns). Return `(mask_refined, blk_list)` (mask_refined is the heatmap mask for display; blk_list is the TextBlock list). Emit progress via the Worker's injected progress_callback (the adapter itself does not emit; the Worker wraps it — see Task 2).
      - `preprocess(self, image) -> np.ndarray`: return image (TextDetector handles preprocessing internally — RESEARCH Pattern 1 line 331).
      - `postprocess(self, model_output) -> np.ndarray`: return `(model_output > 128).astype(np.uint8) * 255` (binary mask threshold, RESEARCH Pattern 1 line 337).
      - `configure(self, **kwargs) -> None`: store kwargs (input_size, conf_thresh, etc.) for use in load().
      - `get_info(self) -> dict`: return `{"backend": "torch", "device": self.device, "model": "comic_text_detector", "input_size": 1024}`.
    - Do NOT implement `TorchLamaModel` here (it belongs to plan 05). Leave a module-level comment `# TorchLamaModel(InpaintModel) is implemented in plan 05 (CLEAN-06).`.

    Create `manga_ai_studio/adapters/factory.py` (D-02 backend selection). Imports: `from manga_ai_studio.adapters.base import DetectionModel, OCRModel, InpaintModel`. Define `def backend_factory(kind: str, backend: str) -> DetectionModel | OCRModel | InpaintModel`: dispatch on kind in {"detection","ocr","inpainting"} and backend in {"torch","onnx"}: detection+torch -> `TorchCTDModel()`; detection+onnx -> `OnnxDetectionModel()` (the plan-01 stub); ocr+torch -> raise NotImplementedError("OCR adapter lands in Phase 4"); inpainting+torch -> raise NotImplementedError("Inpainting torch adapter lands in plan 05"); inpainting+onnx -> `OnnxInpaintModel()`. Import concrete classes lazily inside the function bodies to avoid importing torch when the factory is imported. Raise `ValueError(f"Unknown backend: {kind}/{backend}")` for unrecognized combos. Update `manga_ai_studio/adapters/__init__.py` to re-export `backend_factory`, `DetectionModel`, `OCRModel`, `InpaintModel`.

    Create `tests/test_detection/__init__.py` (empty) and `tests/test_detection/test_ctd_adapter.py`. Use `pytest` with `unittest.mock` to avoid requiring torch + model weights in CI. Tests:
    - `test_3_tuple_unpack_contract`: create a fake `detector` object whose `__call__` returns a 3-tuple `(np.zeros((4,4),dtype=uint8), np.zeros((4,4),dtype=uint8), ["blk1"])`. Monkeypatch `TorchCTDModel` to use this fake (set `instance.detector = fake`). Call `instance.detect(np.zeros((4,4,3),dtype=uint8))` and assert it returns a 2-tuple `(mask_refined, blk_list)` with `blk_list == ["blk1"]` — this is the regression guard against the 5-tuple mistake.
    - `test_load_missing_model_raises`: `TorchCTDModel().load(Path("nonexistent.pt"))` raises `FileNotFoundError` (guard before torch import — assert the FileNotFoundError is raised before any torch import attempt).
    - `test_factory_torch_detection`: `backend_factory("detection", "torch")` returns a `TorchCTDModel` instance.
    - `test_factory_onnx_detection_raises_on_load`: `backend_factory("detection", "onnx")` returns an `OnnxDetectionModel`; calling `.load(Path("x"))` raises `NotImplementedError`.
    - `test_factory_unknown_raises`: `backend_factory("detection", "wat")` raises `ValueError`.
    Mark the fake-detector test with `pytest.mark.unit`; no torch import needed.
  </action>
  <verify>
    <automated>python -c "from panelcleaner.comic_text_detector.inference import TextDetector; import inspect; src=inspect.getsource(TextDetector.__call__); assert 'return mask, mask_refined, blk_list' in src, '3-tuple return missing'; print('3-tuple contract verified')" && pytest tests/test_detection/ -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `panelcleaner/comic_text_detector/inference.py` contains `def __call__(self, img, refine_mode=REFINEMASK_INPAINT, keep_undetected_mask=False)` and `return mask, mask_refined, blk_list` (3-tuple) and zero occurrences of `from pcleaner` (all rewritten to panelcleaner)
    - Source: `manga_ai_studio/adapters/torch_impl.py` contains `class TorchCTDModel(DetectionModel)`, `mask, mask_refined, blk_list = self.detector(` with EXACTLY 3 unpack targets, `refine_mode=REFINEMASK_ANNOTATION`, `keep_undetected_mask=True`, `input_size=1024`, `act="leaky"`, and `from panelcleaner.comic_text_detector.utils.textmask import REFINEMASK_ANNOTATION`
    - Source: `manga_ai_studio/adapters/torch_impl.py` does NOT contain a 5-target unpack of `self.detector(` (grep `self.detector(` returns lines with exactly 3 left-side names)
    - Source: `manga_ai_studio/adapters/factory.py` contains `def backend_factory(kind`, `TorchCTDModel`, `OnnxDetectionModel`, and `raise ValueError`
    - CLI: the verify command's 3-tuple assertion passes (regression guard on the vendored source)
    - Behavior: test_3_tuple_unpack_contract passes (the regression guard against the 5-tuple mistake)
    - Behavior: test_load_missing_model_raises passes (FileNotFoundError, T-01-04)
    - Behavior: test_factory_torch_detection passes
    - Behavior: test_factory_onnx_detection_raises_on_load passes
    - Behavior: test_factory_unknown_raises passes
    - CLI: `pytest tests/test_detection/ -x` exits 0
  </acceptance_criteria>
  <done>
    comic_text_detector vendored with the verified 3-tuple return intact; TorchCTDModel adapter unpacks exactly 3 values; backend_factory resolves detection/torch and detection/onnx; all detection adapter tests green; the 3-tuple regression guard passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Async detection worker + mask overlay display + Detect Text (D) wiring + replace-mask confirmation + error chip</name>
  <files>
    manga_ai_studio/gui/worker_thread.py,
    manga_ai_studio/gui/main_window.py,
    manga_ai_studio/gui/canvas.py,
    tests/test_detection/test_ctd_adapter.py
  </files>
  <read_first>
    C:\Src\PanelCleaner\pcleaner\gui\worker_thread.py (FULL 165-line file — GPL, vendor near-verbatim per PATTERNS.md §gui/worker_thread.py lines 497-536: WorkerSignals, WorkerError, SharableFlag, Abort, Worker(QRunnable); the run loop at 126-161 with try/except Abort/error/result/finally finished; the progress_callback/abort_flag auto-injection at 97-124),
    .planning\phases\01-cleaning-workspace\01-UI-SPEC.md (§Surface 5 Text detection lines 215-221, §Surface 9 Feedback states lines 268-275, §Copywriting lines 118-141, §Color error chip #7a1f1f line 102),
    .planning\phases\01-cleaning-workspace\01-PATTERNS.md (§gui/main_window.py worker wiring lines 334-359, §Shared Pattern 2 worker dispatch lines 636-648, §Shared Pattern 5 buffer lifetime lines 673-685),
    .planning\phases\01-cleaning-workspace\01-RESEARCH.md (§Common Pitfalls P2 QImage lifetime 659-664, P3 touching Qt from worker 666-671, §Don't Hand-Roll QThread pattern line 631),
    manga_ai_studio/gui/main_window.py (plan-02 file — enable the Tools -> Detect Text D action, add worker + handlers),
    manga_ai_studio/gui/canvas.py (plan-02 file — add set_mask, toggle_mask_overlay)
  </read_first>
  <behavior>
    - Test 1 (test_detect_text_action_enabled_with_page): when a page is open, the Detect Text action (Tools menu, shortcut D) is enabled; when no page is open it is disabled
    - Test 2 (test_detection_worker_emits_result): a Worker wrapping a fake detect function emits result signal with the expected payload; progress signal fires; finished signal fires last
    - Test 3 (test_set_mask_composites_overlay): EditorCanvas.set_mask(qimage) sets the mask_item pixmap and makes the mask layer visible; mask overlay uses the rgba(255,0,0,0.63) token
    - Test 4 (test_toggle_mask_overlay): toggle_mask_overlay flips the mask_item visibility
    - Test 5 (test_replace_mask_confirmation): calling MainWindow._confirm_replace_mask() returns True when a mask exists (the confirmation dialog text contains "Replace the current mask"); the dialog is NOT shown when no mask exists
    - Test 6 (test_detection_error_shows_chip): on a worker error (simulated), MainWindow shows the #7a1f1f error chip in the status bar (assertable via the chip widget's stylesheet containing "#7a1f1f")
  </behavior>
  <action>
    Vendor `manga_ai_studio/gui/worker_thread.py` from PanelCleaner `C:\Src\PanelCleaner\pcleaner\gui\worker_thread.py` (GPL, D-12 vendored near-verbatim per PATTERNS.md §gui/worker_thread.py). Copy the full file; rewrite `pcleaner.` -> `panelcleaner.` only if any imports reference pcleaner (PanelCleaner's worker_thread.py is largely self-contained — verify imports). Preserve exactly: `class WorkerError` (line 18), `class WorkerSignals(QObject)` (29) with signals `finished=Signal(tuple)`, `error=Signal(WorkerError)`, `result=Signal(object)`, `progress=Signal(object)`, `aborted=Signal(tuple)` (52-56), `class SharableFlag` (59), `class Abort(Exception)` (70), `class Worker(QRunnable)` (78) with `__init__` (97) auto-injecting `progress_callback`/`abort_flag` into kwargs unless `no_progress_callback=True`, and `run()` (127) doing the try/except Abort/error/else result/finally finished pattern. Keep `WorkerError` carrying `exception_type, value, traceback, args, kwargs`. Docstring cites PanelCleaner source (GPL).

    Extend `manga_ai_studio/gui/canvas.py`: add `def set_mask(self, mask_qimage: QImage) -> None` that converts the mask to a QPixmap, applies the rgba(255,0,0,0.63) overlay tint (UI-SPEC §Color — the mask overlay is semantic red at ~160/255 alpha; use `QColor(255, 0, 0, 160)`), sets `self.mask_item.setPixmap(...)`, makes `self.mask_item.setVisible(True)`, and stores `self._mask_visible = True`. IMPORTANT — QImage buffer discipline (RESEARCH Pitfall 2, PATTERNS.md §Shared Pattern 5): if the mask QImage came from a numpy buffer, ensure it was `.copy()`-detached at the producer side before reaching set_mask; set_mask itself may call `mask_qimage.copy()` defensively before storing. Add `def toggle_mask_overlay(self) -> None` flipping `self.mask_item.isVisible()` and toggling `self._mask_visible`. Add `def has_mask(self) -> bool` returning whether `self.mask_item.pixmap()` is non-null. Add `def clear_mask(self) -> None`.

    Extend `manga_ai_studio/gui/main_window.py`. Enable the plan-02 Tools -> Detect Text `D` action (`action_detect_text`): set enabled iff a page is open AND no async op is currently running (`self._op_running` flag). Connect `action_detect_text.triggered` to `def detect_text(self) -> None`. In `detect_text`: if `self.canvas.has_mask()` -> call `self._confirm_replace_mask()` and abort if rejected; build a `TorchCTDModel` via `backend_factory("detection", self.profile.config... detection_backend or "torch")`; build `worker = Worker(self._run_detection_task, image_path, model)` where `_run_detection_task(image_path, model, progress_callback=None, abort_flag=None)` (the contract from worker_thread.py auto-injection) loads the image via cv2.imread, calls `model.detect(image)` -> `(mask_refined, blk_list)`, returns a dict `{"mask": mask_refined, "blocks": blk_list}`; connect `worker.signals.progress` to `self._on_detection_progress(percent, message)` updating status_bar_left to "Detecting text… {percent}%" (UI-SPEC §Copywriting) and the thin 3px progress bar; connect `worker.signals.result` to `self._on_detection_finished(result)`; connect `worker.signals.error` to `self._on_detection_error(worker_error)`; connect `worker.signals.finished` to `self._on_detection_cleanup`; set `self._op_running = True`, disable Detect/Inpaint actions, set the Detect button to spinner state; `QThreadPool.globalInstance().start(worker)`. Phase 1 uses the D-09b in-process fallback (QThreadPool); the D-07/D-08 subprocess split is deferred until a dependency conflict forces it (so inference runs in-process despite D-08 stating a frontend/backend subprocess goal).

    Implement `def _on_detection_finished(self, result)`: take `result["mask"]` (numpy HxW), convert to QImage (Format_RGBA8888 or Format_Grayscale8 then tinted), apply `.copy()` discipline, call `self.canvas.set_mask(qimage)`, update status_bar_left to "Detection complete", reset `_op_running`, re-enable actions. Implement `def _on_detection_error(self, worker_error)`: show `QMessageBox.critical(self, "Couldn't load the detection model.", "Check that the model files exist in the `models/` folder and see the log for details.")` (UI-SPEC §Copywriting model-load error), show the persistent #7a1f1f error chip in the status bar (a QLabel with stylesheet `background-color: #7a1f1f; color: #ffffff;` per UI-SPEC §Color / PanelCleaner mainwindow_driver.py:2417 OOM banner pattern), log via loguru. Implement `def _confirm_replace_mask(self) -> bool`: `QMessageBox.question(self, "Detect Text", "Replace the current mask with a new detection? Your manual edits will be lost — undo is available via mask undo (Alt+Z).", QMessageBox.Cancel | QMessageBox.Replace)` returning True only on Replace (UI-SPEC §Copywriting destructive re-detect). Wire the View -> Toggle Mask Overlay `M` action to `self.canvas.toggle_mask_overlay()`.

    Extend `tests/test_detection/test_ctd_adapter.py` with the 6 behaviors above (some may go in a new `tests/test_detection/test_detection_gui.py` if cleaner — but keep within this plan's files). For test_detection_worker_emits_result: build a `Worker(lambda progress_callback=None, abort_flag=None: ("ok",))`, connect Qt signals via `qtbot.waitSignal`, start on QThreadPool, assert result/progress/finished. For test_replace_mask_confirmation: mock QMessageBox.question to return Replace, assert True; mock to return Cancel, assert False; and when canvas.has_mask() is False assert no dialog is shown (use a sentinel). For test_detection_error_shows_chip: build a Worker that raises, connect error, assert the status-bar chip label stylesheet contains "#7a1f1f" after the error handler runs. Use `monkeypatch` to stub `backend_factory` and avoid real model loads. Mark GUI tests with `@pytest.mark.usefixtures("qtbot")`.
  </action>
  <verify>
    <automated>pytest tests/test_detection/ -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `manga_ai_studio/gui/worker_thread.py` contains `class Worker(QRunnable)`, `class WorkerSignals(QObject)`, `finished = Signal(tuple)`, `error = Signal(WorkerError)`, `result = Signal(object)`, `progress = Signal(object)`, `aborted = Signal(tuple)`, `class Abort(Exception)`, `class WorkerError`, and a `def run` with the try/except Abort/error/finally-finished structure (vendored from PanelCleaner GPL)
    - Source: `manga_ai_studio/gui/canvas.py` contains `def set_mask`, `def toggle_mask_overlay`, `def has_mask`, `QColor(255, 0, 0, 160)` (the rgba(255,0,0,0.63) overlay), and a `.copy()` call in the QImage/numpy path
    - Source: `manga_ai_studio/gui/main_window.py` contains `def detect_text`, `Worker(self._run_detection_task`, `QThreadPool.globalInstance().start(worker)`, `_on_detection_finished`, `_on_detection_error`, `_confirm_replace_mask`, the literal "Detecting text… {percent}%" and "Replace the current mask with a new detection" and "Couldn't load the detection model." copy, and the `#7a1f1f` error chip stylesheet
    - Source: main_window does NOT import `torch` or `TextDetector` directly (all model access via the adapter) — `grep -E "import torch|TextDetector" main_window.py` returns nothing
    - Behavior: test_detect_text_action_enabled_with_page passes
    - Behavior: test_detection_worker_emits_result passes (Worker signals fire in order)
    - Behavior: test_set_mask_composites_overlay passes
    - Behavior: test_toggle_mask_overlay passes
    - Behavior: test_replace_mask_confirmation passes
    - Behavior: test_detection_error_shows_chip passes (#7a1f1f chip)
    - CLI: `pytest tests/test_detection/ -x` exits 0
  </acceptance_criteria>
  <done>
    gui/worker_thread.py vendored (GPL Worker/WorkerSignals/WorkerError/Abort); Detect Text (D) runs TorchCTDModel.detect via Worker without freezing UI; detected mask composited as rgba(255,0,0,0.63) overlay; replace-mask confirmation and model-load error chip (#7a1f1f) implemented; mask overlay toggle (M) wired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models/ dir -> adapter (model load) | PyTorch model weight file loaded by TextDetector; tampering/corruption risk |
| backend worker thread -> GUI thread | numpy mask result crosses thread boundary via Qt signal (must not touch Qt in worker) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering (model weight tampering) | TorchCTDModel.load / TextDetector | high | mitigate | TorchCTDModel.load validates `model_path.is_file()` and raises FileNotFoundError before constructing TextDetector (PanelCleaner inpainting.py:21 pattern). The vendored model_downloader.fetch uses sha256 verification on download (PATTERNS.md §model_downloader line 236). A known-good model path is stored in config; user-supplied paths are resolve()'d. RESEARCH §Security Domain T-2. |
| T-01-07 | Denial of Service (thread-safety crash) | Worker -> Qt mutation | high | mitigate | Workers touch only numpy/Python and emit signals; ALL Qt mutation happens in main-thread signal handlers (_on_detection_finished/_on_detection_error). This is enforced by the vendored Worker(QRunnable) pattern (RESEARCH Pitfall 3). The detect() call returns plain numpy arrays, never QImage/QPixmap. |
| T-01-08 | Information disclosure (traceback leakage) | WorkerError -> QMessageBox | low | mitigate | WorkerError carries the traceback but the QMessageBox shows only user-friendly copy ("Couldn't load the detection model."); the full traceback goes to loguru logs, not the dialog. |
</threat_model>

<verification>
- `pytest tests/test_detection/ -x` green
- The 3-tuple regression guard passes (vendored inference.py still returns `mask, mask_refined, blk_list`)
- Detect Text (D) on a sample page produces a red mask overlay without freezing the UI (manual check per VALIDATION.md)
- Model-load failure path shows the error chip + critical dialog
</verification>

<success_criteria>
- comic_text_detector vendored with the VERIFIED 3-tuple return intact (no 5-tuple unpack anywhere)
- TorchCTDModel adapter subclass of DetectionModel with the 3-tuple unpack
- backend_factory resolves detection/torch and detection/onnx
- Async detection via Worker(QRunnable); UI stays responsive; mask composited as rgba(255,0,0,0.63)
- Replace-mask confirmation + model-load error chip + mask overlay toggle (M) wired
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 — Text Detection)

**Modules / classes:**
- `panelcleaner.comic_text_detector` (vendored package, GPL v3): `inference.TextDetector` (`__call__(img, refine_mode=REFINEMASK_INPAINT, keep_undetected_mask=False)` returning 3-tuple `(mask, mask_refined, blk_list)` at inference.py:210), `basemodel`, `utils.textmask` (`REFINEMASK_INPAINT`, `REFINEMASK_ANNOTATION`, `refine_mask`, `refine_undetected_mask`), `utils.textblock.TextBlock`, `utils.imgproc_utils`, `utils.io_utils`, `utils.db_utils`, `utils.yolov5_utils`
- `manga_ai_studio.adapters.torch_impl.TorchCTDModel(DetectionModel)`: `load(model_path, device="cpu")`, `detect(image) -> (mask_refined, blk_list)` (3-tuple unpack with `refine_mode=REFINEMASK_ANNOTATION, keep_undetected_mask=True`), `preprocess`, `postprocess`, `configure`, `get_info`
- `manga_ai_studio.adapters.factory.backend_factory(kind, backend) -> DetectionModel|OCRModel|InpaintModel`
- `manga_ai_studio.gui.worker_thread.Worker(QRunnable)`, `WorkerSignals`, `WorkerError`, `SharableFlag`, `Abort` (vendored from PanelCleaner GPL)
- `manga_ai_studio.gui.canvas.EditorCanvas` (extended): `set_mask(qimage)`, `toggle_mask_overlay()`, `has_mask()`, `clear_mask()`; `_mask_visible` state
- `manga_ai_studio.gui.main_window.MainWindow` (extended): `detect_text()`, `_run_detection_task(image_path, model, progress_callback, abort_flag)`, `_on_detection_finished(result)`, `_on_detection_error(worker_error)`, `_on_detection_progress(percent, message)`, `_on_detection_cleanup()`, `_confirm_replace_mask() -> bool`; `_op_running` flag; `#7a1f1f` error chip; Tools -> Detect Text (D) enabled

**Config keys consumed:**
- `detection_backend` ("torch" default; "onnx" -> OnnxDetectionModel stub) — read via Profile/Config in MainWindow.detect_text

**Test files:**
- `tests/test_detection/test_ctd_adapter.py`: `test_3_tuple_unpack_contract` (regression guard), `test_load_missing_model_raises`, `test_factory_torch_detection`, `test_factory_onnx_detection_raises_on_load`, `test_factory_unknown_raises`, `test_detect_text_action_enabled_with_page`, `test_detection_worker_emits_result`, `test_set_mask_composites_overlay`, `test_toggle_mask_overlay`, `test_replace_mask_confirmation`, `test_detection_error_shows_chip`
</artifacts_produced>

<output>
Create `.planning/phases/01-cleaning-workspace/01-03-SUMMARY.md` when done
</output>
