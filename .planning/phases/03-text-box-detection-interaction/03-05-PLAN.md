---
phase: 03-text-box-detection-interaction
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_box_persistence.py
autonomous: false
requirements: [TEXT-01, TEXT-03]
tags: [persistence, undo-collapse, d-11, surface-13, per-page, alt-z-strings]

must_haves:
  truths:
    - "Per-page box persistence mirrors Phase 2 D-11: on page-leave the outgoing page's boxes are snapshotted to ImageFile.boxes (.copy()-detached); on page-select the incoming page's boxes are restored (.copy()-detached at both boundaries)"
    - "The unified Ctrl+Z / Ctrl+Shift+Z pop the merged MASK/IMAGE/BOXES timeline (plan 03-02's HistoryManager.undo/redo) and MainWindow routes the (kind, value) result to the matching apply method"
    - "The 4-button undo toolbar collapses to 2 ([Undo][Redo]); the 4 edit-menu items collapse to 2; Alt+Z and Alt+Shift+Z shortcuts are removed (Surface 13)"
    - "The two orphaned Phase 1 confirm-dialog strings referencing Alt+Z are updated: main_window.py:1104 (Clear Mask) -> Ctrl+Z; main_window.py:1342 (Replace Mask) -> Ctrl+Z"
    - "After an undo/redo, the status bar shows a transient 'Undo: {op}' / 'Redo: {op}' message naming the op type (the which-stack-was-popped indication the unified timeline requires)"
    - "Page-switching resets all three HistoryManager stacks (clear() extended in plan 03-02)"
  artifacts:
    - "manga_ai_studio/gui/main_window.py (on_page_selected Step 1 + Step 4 extended for boxes .copy()-detach; unified on_undo/on_redo handlers routing (kind, value) to apply_undo_mask/apply_undo_image/apply_undo_boxes; toolbar collapse 4->2; edit-menu collapse 4->2; Alt+Z/Alt+Shift+Z shortcuts removed; action_undo_mask/action_redo_mask removed; orphaned Alt+Z strings at :1104 and :1342 updated; transient undo/redo status feedback)"
    - "tests/test_box_persistence.py (pytest-qt: per-page box save/restore across navigation; .copy()-detach at both boundaries — mirrors Phase 2 test_mask_persistence_uses_copy)"
  key_links:
    - "on_page_selected Step 1 (outgoing save) + Step 4 (incoming restore) boxes extension — mirrors the Phase 2 D-11 mask seam exactly (the load-bearing persistence wiring)"
    - "The unified on_undo/on_redo routing (kind, value) -> apply_undo_{mask,image,boxes} — the UI half of plan 03-02's unified timeline"
    - "The two Alt+Z string fixes at main_window.py:1104 + :1342 — the dead-shortcut cleanup UI-SPEC §Copywriting flagged as a PLANNER TODO"
---

<objective>
Land the final Phase 3 wiring: (a) per-page box persistence mirroring Phase 2 D-11's mask seam (.copy() at both boundaries), (b) the Surface 13 undo collapse — route the unified timeline (plan 03-02's HistoryManager.undo/redo) into MainWindow, collapse the toolbar/menu/shortcuts from 4 to 2, remove Alt+Z, and fix the two orphaned confirm-dialog strings. After this plan, boxes persist across page navigation and Ctrl+Z is the single undo over mask + image + box ops. The phase is then feature-complete: TEXT-01 (detect boxes, plan 03-04) + TEXT-03 (correct boxes, plan 03-03) + persistence + unified undo.

Purpose: This plan closes two open seams. Persistence is required so a user's box corrections survive flipping to another page and back (otherwise every page-switch loses their work — the same problem Phase 2 D-11 solved for masks). The undo collapse is the UI half of D-11 — plan 03-02 built the headless algorithm; this plan wires it into the toolbar/menu/shortcuts and removes the now-redundant Alt+Z mask-undo path (Surface 13 superseding Phase 1 surface 8). The two Alt+Z string fixes are the dead-shortcut cleanup UI-SPEC explicitly flagged.

Output: Extended on_page_selected + unified undo/redo handlers + collapsed toolbar/menu + removed Alt+Z + fixed strings; one persistence test file. This is the last plan of the phase.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-text-box-detection-interaction/03-CONTEXT.md
@.planning/phases/03-text-box-detection-interaction/03-RESEARCH.md
@.planning/phases/03-text-box-detection-interaction/03-PATTERNS.md
@.planning/phases/03-text-box-detection-interaction/03-UI-SPEC.md
# The plans this plan wires together:
@.planning/phases/03-text-box-detection-interaction/03-02-SUMMARY.md
@.planning/phases/03-text-box-detection-interaction/03-03-SUMMARY.md
@.planning/phases/03-text-box-detection-interaction/03-04-SUMMARY.md
# The Phase 2 persistence pattern this plan mirrors:
@.planning/phases/02-cleaning-output-batch/02-CONTEXT.md
# The main_window being extended + the exact seam sites:
@manga_ai_studio/gui/main_window.py
# The canvas API (plan 03-03) + HistoryManager API (plan 03-02):
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/core/history_manager.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-page box persistence in on_page_selected (Phase 2 D-11 mirror, .copy() both boundaries)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_box_persistence.py</files>
  <read_first>
    - `manga_ai_studio/gui/main_window.py` lines 653-747 (`on_page_selected` — the 5-step Phase 2 D-11 seam; Step 1 outgoing mask save at ~695-705 with the MANDATORY .copy(); Step 4 incoming mask restore at ~724-734 with the boundary .copy(); the _last_page_index vs _current_page_index distinction at ~663-672)
    - `manga_ai_studio/core/image_file.py` (from plan 03-01: the boxes slot + has_boxes method)
    - `manga_ai_studio/gui/canvas.py` (from plan 03-03: has_boxes, boxes_snapshot, set_boxes — the API this seam calls)
    - `.planning/phases/03-text-box-detection-interaction/03-RESEARCH.md` §Code Examples "Per-page box persistence (mirrors Phase 2 D-11, T-02-04)" + §Common Pitfalls Pitfall 3 (snapshot aliasing) + Pitfall 6 (int at QRectF boundary)
    - `.planning/phases/03-text-box-detection-interaction/03-PATTERNS.md` Shared Pattern 2 (Per-page persistence seam — the exact 5-step shape + the belt-and-suspenders .copy())
    - `.planning/STATE.md` accumulated decisions: "02-02: Boundary .copy() at BOTH the OUTGOING save and INCOMING restore" + "02-02: OUTGOING page index read from _last_page_index NOT _current_page_index"
    - `tests/test_history.py` + the Phase 2 test_mask_persistence_uses_copy pattern (the regression-guard shape this plan's test_box_persistence_uses_copy mirrors)
  </read_first>
  <behavior>
    - On page-switch WITH boxes on the outgoing page: the outgoing ImageFile.boxes is set to a fresh snapshot from canvas.boxes_snapshot() (detached — Pitfall 3)
    - On page-switch TO a page WITH persisted boxes: the incoming boxes are restored via canvas.set_boxes (the plan 03-03 API rebuilds BoxItems)
    - The OUTGOING index is read from self._last_page_index (NOT _current_page_index — it has already flipped; Phase 2 lesson)
    - Mutating the canvas boxes AFTER the outgoing save does NOT change the persisted ImageFile.boxes (Pitfall 3 — test_box_persistence_uses_copy regression guard)
    - Switching away and back restores the boxes (round-trip)
    - Page-switching resets all three HistoryManager stacks (clear() from plan 03-02 — already extended; this task just confirms on_page_selected calls reset_history which calls clear)
  </behavior>
  <action>
    Extend `on_page_selected` (main_window.py:653-747). In Step 1 (persist OUTGOING, ~lines 695-705): after the existing mask-save block (`if self.canvas.has_mask(): self.image_files[outgoing_idx].mask = self.canvas.get_mask().copy()`), add the boxes-save block: `if self.canvas.has_boxes(): self.image_files[outgoing_idx].boxes = self.canvas.boxes_snapshot()`. The OUTGOING index MUST be `self._last_page_index` (the existing variable in Step 1 — NOT `_current_page_index()`, which has already flipped to the incoming page; this is the Phase 2 lesson logged in STATE.md). `boxes_snapshot()` (plan 03-03) already materializes fresh int Box tuples per item from the current rects — so the snapshot is detached from live BoxItems (Pitfall 3); no additional `.copy()` is needed on the boxes path (unlike QImage which needs an explicit `.copy()`, the boxes snapshot is a fresh list of fresh tuples by construction). Store the snapshot as `list[PageBox]` or the (box, origin, payload) tuple shape plan 03-01's ImageFile.boxes expects — match the type.

    In Step 4 (restore INCOMING, ~lines 724-734): after the existing mask-restore block, add the boxes-restore block: read `incoming_boxes = self.image_files[incoming_idx].boxes`; if `incoming_boxes`: call `self.canvas.set_boxes(user_boxes=incoming_boxes, detected_boxes=[])` — but NOTE: set_boxes takes a user/detected split; on restore ALL boxes come back as-is with their original origin (the snapshot preserved origin). Either (a) extend set_boxes to accept a single combined list and split internally by origin, or (b) split here: `user = [b for b in incoming_boxes if b.origin == USER]; detected = [b for b in incoming_boxes if b.origin == DETECTED]; self.canvas.set_boxes(user, detected)`. Prefer (b) to keep set_boxes's signature stable. The incoming boxes are already detached (they were snapshotted on the outgoing side); a belt-and-suspenders re-materialization is harmless but not required (Phase 2's mask path does `self.image_files[incoming_idx].mask.copy()` because QImage buffers can alias — boxes are plain tuples/dataclasses with immutable Box, no aliasing risk, but for symmetry you MAY re-snapshot). Update the status-bar box count after restore.

    Confirm `reset_history` (called in Step 2 / ~line 708) calls `self.history.clear()` which plan 03-02 extended to clear all six lists (mask/image/boxes undo + redo) — so switching pages resets the BOXES stack too (Phase 1's per-page reset invariant, extended to the 3rd stack).

    Create `tests/test_box_persistence.py` (pytest-qt, mirroring the Phase 2 test_mask_persistence_uses_copy shape). Cover: `test_box_persistence_round_trip` (page A has user+detect boxes; switch to page B; switch back to A; assert canvas.has_boxes() and the boxes match the originals — origin preserved); `test_box_persistence_uses_copy` (the Pitfall-3 regression guard — page A has boxes; trigger on_page_selected to page B; mutate the canvas boxes on page B (add/remove); assert `image_files[A].boxes` is UNCHANGED — the outgoing snapshot was detached); `test_outgoing_index_uses_last_page_index` (the Phase 2 lesson regression — switch A->B, assert the OUTGOING save wrote to image_files[A] not image_files[B]; this catches the _current_page_index-vs-_last_page_index bug); `test_page_switch_resets_boxes_stack` (push a boxes snapshot, switch pages, assert `history.can_undo()` is False — the BOXES stack cleared with the other two). Mark `@pytest.mark.gui`. Construct the multi-page scenario via the shared MainWindow fixture + two test images (reuse Phase 2's test fixture pattern).
  </action>
  <verify>
    <automated>python -m pytest tests/test_box_persistence.py -q -m gui</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "has_boxes()" manga_ai_studio/gui/main_window.py` >= 2 (Step 1 outgoing + at least one other reference)
    - `grep -c "boxes_snapshot\|\.boxes =\|\.boxes\b" manga_ai_studio/gui/main_window.py` >= 3 (the persistence wiring in on_page_selected)
    - `grep -c "_last_page_index" manga_ai_studio/gui/main_window.py` >= 1 in the boxes-save context (read on_page_selected to confirm the outgoing boxes save uses _last_page_index, NOT _current_page_index)
    - The boxes-save block sits in Step 1 (outgoing) and the boxes-restore block sits in Step 4 (incoming) of on_page_selected — verify by reading the function
    - `python -m pytest tests/test_box_persistence.py -q -m gui` exits 0
    - `python -m pytest tests/ -m gui -q` exits 0 (no regression to Phase 1/2 GUI tests)
  </acceptance_criteria>
  <done>Boxes persist per-page across navigation (Phase 2 D-11 mirror); outgoing save uses _last_page_index + boxes_snapshot() (Pitfall 3 detached); incoming restore rebuilds BoxItems via set_boxes with origin preserved; page-switch resets the BOXES stack; test_box_persistence.py green (round-trip + detachment + outgoing-index + stack-reset).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface 13 undo collapse — unified on_undo/on_redo + toolbar/menu/shortcut collapse + Alt+Z removal + orphaned string fixes</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_box_persistence.py</files>
  <read_first>
    - `manga_ai_studio/gui/main_window.py` lines 263-284 (the Edit menu — where Undo Mask/Redo Mask items are removed) + lines 443-459 (the toolbar undo section — where the 4 buttons collapse to 2) + lines 920-942 (the shortcut wiring — where Alt+Z/Alt+Shift+Z are removed and Ctrl+Z/Ctrl+Shift+Z are repointed to the unified handlers) + lines 1102-1104 (the Clear Mask confirm dialog body — the first orphaned Alt+Z string) + lines 1340-1343 (the Replace Mask confirm dialog body — the second orphaned Alt+Z string)
    - `manga_ai_studio/core/history_manager.py` (from plan 03-02: undo(current_mask, current_img, current_boxes) -> (kind, value) | None, redo(...), can_undo(), can_redo())
    - `manga_ai_studio/gui/canvas.py` (from plan 03-03: boxes_snapshot, set_boxes — used by apply_undo_boxes)
    - `.planning/phases/03-text-box-detection-interaction/03-UI-SPEC.md` §13 (Undo/redo surface — the CHANGED contract: 3 stacks, 2 buttons, 2 shortcuts, unified timeline; the "which-stack-was-popped" status feedback) + §Copywriting (the undo/redo status feedback copy "Undo: {op}" / "Redo: {op}"; the two orphaned Alt+Z PLANNER TODOs)
    - `.planning/phases/03-text-box-detection-interaction/03-PATTERNS.md` Group C Seam 5 (the undo collapse) + Seam 6 (the two orphaned Alt+Z strings at main_window.py:1104, 1342)
    - `.planning/phases/03-text-box-detection-interaction/03-CONTEXT.md` D-11 (unified Ctrl+Z timeline)
  </read_first>
  <behavior>
    - Ctrl+Z pops the unified timeline (mask/image/boxes most-recent-first) and applies the result; Ctrl+Shift+Z redoes
    - Alt+Z and Alt+Shift+Z no longer trigger mask undo/redo (removed)
    - The toolbar has exactly 2 undo buttons ([Undo][Redo]); the Edit menu has exactly 2 undo items
    - action_undo_mask and action_redo_mask no longer exist (removed)
    - main_window.py:1104 Clear Mask body says "Ctrl+Z" not "Alt+Z"; main_window.py:1342 Replace Mask body says "Ctrl+Z" not "Alt+Z"
    - After undo/redo, status_bar_left shows "Undo: {op}" / "Redo: {op}" transiently (op in {mask edit, inpaint, box create/move/resize/delete})
    - Undo/Redo buttons disable when can_undo()/can_redo() are False (union over all three stacks)
  </behavior>
  <action>
    Extend `manga_ai_studio/gui/main_window.py`. Implement unified handlers `def on_undo(self):` and `def on_redo(self):`. on_undo: gather the current mask (canvas.get_mask() or None), current image (the current image state — reuse whatever apply_undo_image reads), current boxes (canvas.boxes_snapshot() or []); call `result = self.history.undo(current_mask, current_img, current_boxes)`; if result is None, return; unpack `kind, value = result`; route: if kind == "mask", apply the mask (reuse the existing apply_undo_mask logic — set the canvas mask from value); if kind == "image", apply the image patch (reuse apply_undo_image); if kind == "boxes", apply the boxes snapshot via `self.canvas.set_boxes(...)` splitting by origin (or extend set_boxes to take a combined list). After applying, emit the transient status feedback: `self.status_bar_left.setText(f"Undo: {op_label(kind, value)}")` and start a ~3s QTimer to revert to the box-count status (UI-SPEC §Copywriting — the which-stack-was-popped indication). The op_label maps kind to the UI-SPEC copy: mask edit / inpaint / box create|move|resize|delete (the box op-type lives in the snapshot metadata if plan 03-02 stored it; if not, default to "box edit"). on_redo mirrors on_undo across the redo lists. Update `_refresh_action_states` to set `action_undo.setEnabled(self.history.can_undo())` and `action_redo.setEnabled(self.history.can_redo())` (the union flags from plan 03-02).

    Collapse the UI surface (Surface 13). In the toolbar section (~lines 449-457): remove `action_undo_mask` and `action_redo_mask` (the Phase 1 mask-undo buttons); keep `action_undo` and `action_redo` (the image-undo buttons become the unified buttons — repoint their text/tooltips to "Undo"/"Redo" with tooltips "Undo last action (Ctrl+Z)" / "Redo last action (Ctrl+Shift+Z)" per UI-SPEC §13). The 4-button section becomes 2 buttons. In the Edit menu (~lines 263-284): remove the `Undo Mask`/`Redo Mask` items; keep `Undo`/`Redo` (repointed). In the shortcut wiring (~lines 920-942): REMOVE the two Phase 1 mask-undo QShortcut entries (the ones binding the Alt-modifier Z / Alt-modifier Shift-Z keysequences to the mask-undo handlers) and REPPOINT the existing Ctrl+Z / Ctrl+Shift+Z keysequence entries from the image-only handlers to the unified `self.on_undo`/`self.on_redo`. (Name the exact keysequence strings to remove by reading the ~920-942 block; they are the Phase 1 mask-undo bindings being subsumed.) Remove the `on_undo_mask`/`on_redo_mask` method definitions (dead after the shortcut/menu/toolbar removal). Remove the `action_undo_mask`/`action_redo_mask` QAction declarations.

<!-- planner-discipline-allow: Alt+Z -->
    Fix the two orphaned confirm-dialog strings that reference the now-removed mask-undo shortcut (UI-SPEC §Copywriting PLANNER TODO — PATTERNS Seam 6). At main_window.py:1102-1104 (the `_confirm_clear_mask` body): change the parenthetical that currently names the Alt-modifier-Z mask-undo shortcut to name `Ctrl+Z` instead — final text "You can undo with Ctrl+Z." (UI-SPEC §Copywriting exact replacement). At main_window.py:1340-1343 (the `_confirm_replace_mask` body): change the trailing clause that currently names the Alt-modifier-Z mask-undo shortcut to name `Ctrl+Z` instead — final text "undo is available via Ctrl+Z." (UI-SPEC §Copywriting exact replacement). These are string-level edits — the dead mask-undo shortcut must never ship in a confirm dialog (UI-SPEC flagged this explicitly).

    Extend `tests/test_box_persistence.py` (or a sibling test file) with the undo-collapse tests: `test_unified_undo_pops_across_stacks` (push a mask edit, a box move, an image action; Ctrl+Z three times; assert the pops reverse in chronological order — mirrors plan 03-02's headless test but at the MainWindow level via the shortcut or on_undo); `test_mask_undo_shortcut_removed` (send the legacy Alt-modifier-Z keysequence to the MainWindow, assert on_undo_mask is NOT called — the shortcut is gone; or assert the QShortcut no longer exists); `test_orphaned_strings_fixed` (read the two confirm-dialog bodies via grep or by constructing the dialogs and assert they contain "Ctrl+Z" and do NOT contain the legacy mask-undo shortcut name); `test_toolbar_has_two_undo_buttons` (count the undo-related actions in the toolbar — assert exactly 2). Mark `@pytest.mark.gui`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_box_persistence.py -q -m gui</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def on_undo\b\|def on_redo\b" manga_ai_studio/gui/main_window.py` >= 2 (the unified handlers)
    - `grep -c "history.undo\|history.redo" manga_ai_studio/gui/main_window.py` >= 2 (the handlers call the unified pop)
    - `grep -c "Alt+Z\|Alt\+Z" manga_ai_studio/gui/main_window.py` == 0 (the dead shortcut fully removed — no shortcut, no dialog string, no tooltip)
    - `grep -c 'QKeySequence("Alt' manga_ai_studio/gui/main_window.py` == 0 (the Alt+Z/Alt+Shift+Z QShortcut entries removed)
    - `grep -c "action_undo_mask\|action_redo_mask\|on_undo_mask\|on_redo_mask" manga_ai_studio/gui/main_window.py` == 0 (the Phase 1 mask-undo actions + methods fully removed)
    - The two confirm dialogs reference Ctrl+Z — `grep -c "You can undo with Ctrl+Z" manga_ai_studio/gui/main_window.py` >= 1 (the Clear Mask fix at :1104) AND `grep -c "undo is available via Ctrl+Z" manga_ai_studio/gui/main_window.py` >= 1 (the Replace Mask fix at :1342)
    - `grep -c "Undo: {\|Redo: {" manga_ai_studio/gui/main_window.py` >= 1 (the transient status feedback per UI-SPEC §Copywriting)
    - `grep -c "can_undo()\|can_redo()" manga_ai_studio/gui/main_window.py` >= 2 (the unified enable flags drive the 2 buttons)
    - `python -m pytest tests/test_box_persistence.py -q -m gui` exits 0
    - `python -m pytest tests/ -q` exits 0 (FULL suite green — phase is feature-complete)
  </acceptance_criteria>
  <done>Unified Ctrl+Z/Ctrl+Shift+Z pop the merged timeline and route (kind, value) to the matching apply; Alt+Z/Alt+Shift+Z removed; toolbar 4->2 buttons; Edit menu 4->2 items; action_undo_mask/action_redo_mask removed; the two orphaned Alt+Z confirm-dialog strings fixed to Ctrl+Z; transient "Undo: {op}" status feedback; Undo/Redo enable on the union flags; test_box_persistence.py undo-collapse tests green; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify persistence + unified undo + Alt-shortcut removal end-to-end</name>
  <files>.planning/phases/03-text-box-detection-interaction/03-05-SUMMARY.md</files>
  <action>
    Plan 03-05 closes the phase: per-page box persistence (boxes survive page navigation, mirroring Phase 2 mask persistence) + the Surface 13 undo collapse (one Ctrl+Z over mask + image + box ops; the legacy Alt-modifier mask-undo shortcuts removed; 4 toolbar buttons collapsed to 2; the two orphaned confirm-dialog strings that referenced the removed shortcut are fixed to Ctrl+Z). Combined with plans 03-01..04, Phase 3 is feature-complete: TEXT-01 (detect boxes) + TEXT-03 (correct boxes) + persistence + unified undo. This checkpoint confirms the persistence round-trip, the unified undo order across interleaved ops, and the dead-shortcut cleanup on the real running app.
  </action>
  <verify>
    <human-check>
      Launch the app (`python -m manga_ai_studio`) and perform these checks on a multi-page chapter:
      1. (Persistence) On page 1: run Detect (mode on) -> green boxes appear; Alt+drag an amber user box; delete a false-positive green box. Switch to page 2. Switch back to page 1. Confirm ALL your box work survived (the user box, the deletions, the detected boxes) — D-11 per-page persistence works for boxes.
      2. (Unified undo) On a page with a mask: paint a mask stroke (mask edit). Move a box (box move). Run an inpaint (image action). Now press Ctrl+Z three times. Confirm the ops reverse in chronological order (inpaint -> box move -> mask edit) and the status bar shows "Undo: inpaint" / "Undo: box move" / "Undo: mask edit" transiently.
      3. (Removed shortcut) Press the legacy Alt-modifier-Z keysequence. Confirm NOTHING happens (the mask-undo shortcut is gone — subsumed by Ctrl+Z). Press Ctrl+Z — that is the only undo now.
      4. (Toolbar collapse) Confirm the toolbar has exactly 2 undo buttons ([Undo][Redo]), not 4. Confirm the Edit menu has exactly 2 undo items.
      5. (Orphaned strings fixed) Trigger Edit -> Clear Mask (with a mask present). Confirm the confirm dialog says "You can undo with Ctrl+Z." (NOT the legacy mask-undo shortcut name). Trigger a re-Detect over an existing mask. Confirm the Replace Mask dialog says "undo is available via Ctrl+Z." (NOT the legacy mask-undo shortcut name).
      6. (Undo recovers a deleted box) Delete a box (silent). Press Ctrl+Z. Confirm the box reappears (BOXES stack + unified pop recover it — D-12 safety).
    </human-check>
    <automated>cd "C:/Src/Manga AI Studio" &amp;&amp; python -m pytest tests/test_box_persistence.py -q -m gui 2>&1 | findstr /C:"passed" /C:"failed"</automated>
  </verify>
  <done>All six manual checks pass (persistence round-trip; unified undo order across interleaved mask/box/image ops; legacy mask-undo shortcut inert; toolbar collapsed to 2; both confirm dialogs name Ctrl+Z; Ctrl+Z recovers a deleted box) and the test_box_persistence.py suite is green.</done>
  <resume-signal>Type "approved" or describe issues (e.g. "undo order feels wrong after interleaved ops", "persistence lost my boxes on the third page-switch", "Ctrl+Z status message disappears too fast").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas live state -> ImageFile.boxes (persistence) | Box rects cross here on page-leave; snapshot must detach (Pitfall 3) |
| HistoryManager -> canvas (undo apply) | The (kind, value) pop result crosses here; the apply must not alias the history's internal list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-03-08 | Tampering | on_page_selected outgoing box save (main_window.py) | medium | mitigate | boxes_snapshot() materializes fresh int Box tuples (plan 03-03, Pitfall 3+6); the persisted ImageFile.boxes does not alias live BoxItems. Regression guard: test_box_persistence_uses_copy (mirrors Phase 2 test_mask_persistence_uses_copy). OUTGOING index uses _last_page_index not _current_page_index (Phase 2 lesson). |
| T-03-09 | Repudiation | unified undo/redo status feedback (main_window.py) | low | mitigate | The "Undo: {op}" / "Redo: {op}" transient status message names which stack was popped (UI-SPEC §13 "which-stack-was-popped indication") — necessary because the user can no longer target a stack by modifier after the Alt+Z removal. |

No new trust boundaries beyond what plans 03-01..04 already covered. Single-user offline desktop app; no network/auth/untrusted-input surface added by persistence or the undo collapse.
</threat_model>

<verification>
- `python -m pytest tests/test_box_persistence.py -q -m gui` exits 0
- `python -m pytest tests/ -q` exits 0 (FULL suite green — Phase 3 feature-complete)
- `grep -c "Alt+Z" manga_ai_studio/gui/main_window.py` == 0 (dead shortcut fully purged — UI-SPEC §Copywriting PLANNER TODO closed)
- Manual checkpoint passed (persistence round-trip, unified undo order, Alt+Z removed, toolbar collapsed, orphaned strings fixed, undo recovers deleted box)
</verification>

<success_criteria>
- Per-page box persistence mirrors Phase 2 D-11 (save on leave, restore on select, .copy()-detach, _last_page_index for outgoing).
- Surface 13 undo collapse shipped: 3 stacks, 2 toolbar buttons, 2 edit-menu items, unified Ctrl+Z/Ctrl+Shift+Z; Alt+Z/Alt+Shift+Z removed; the two orphaned Alt+Z confirm-dialog strings fixed (UI-SPEC PLANNER TODO closed).
- D-11 unified timeline fully wired end-to-end (plan 03-02 algorithm + this plan's UI).
- Phase 3 feature-complete: TEXT-01 (detect) + TEXT-03 (correct) + persistence + unified undo. Both Phase 3 success criteria met.
</success_criteria>

<output>
Create `.planning/phases/03-text-box-detection-interaction/03-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

**MainWindow extensions (gui/main_window.py):**
- `on_page_selected` Step 1 + Step 4 boxes wiring (per-page box persistence, Phase 2 D-11 mirror)
- `MainWindow.on_undo()` / `on_redo()` (unified handlers routing (kind, value) -> apply_undo_{mask,image,boxes})
- `MainWindow.apply_undo_boxes(boxes_snapshot)` (the BOXES apply — mirrors apply_undo_mask/apply_undo_image)
- Transient "Undo: {op}" / "Redo: {op}" status feedback (QTimer-backed)
- Toolbar collapse: 4 undo buttons -> 2 (`action_undo`/`action_redo` repointed to unified handlers)
- Edit-menu collapse: 4 undo items -> 2
- Removed: `action_undo_mask`, `action_redo_mask`, `on_undo_mask`, `on_redo_mask`, the Alt+Z/Alt+Shift+Z QShortcut entries
- Fixed strings: main_window.py:1104 (Clear Mask body "Ctrl+Z") + :1342 (Replace Mask body "Ctrl+Z")

**Wave 0 test file (per VALIDATION.md):**
- `tests/test_box_persistence.py` (pytest-qt: round-trip, .copy()-detach, _last_page_index outgoing, stack-reset on page-switch, unified undo across stacks, Alt+Z removed, orphaned strings fixed, toolbar 2 buttons)
