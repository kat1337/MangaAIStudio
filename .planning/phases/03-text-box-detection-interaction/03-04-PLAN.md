---
phase: 03-text-box-detection-interaction
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_detection_boxes.py
autonomous: false
requirements: [TEXT-01]
tags: [gui, detection-seam, d-01, d-03, d-04, v5-validation]

must_haves:
  truths:
    - "_on_detection_finished reads result['blocks'] (the blk_list ALREADY in the worker result dict at main_window.py:1217) and, when Detect Boxes mode is on, builds BoxItems from it — Phase 1's discard is replaced"
    - "Each TextBlock.xyxy is coerced to int and bounds-clamped against the current image rect at build-time (V5: model output treated as untrusted — out-of-range boxes are clamped or dropped, never trusted)"
    - "The Detect Boxes mode toggle (D-01) gates box creation: on = mask + boxes, off = mask only (Phase 1 behavior preserved)"
    - "Re-running detection replaces detected boxes but preserves user boxes (D-03 origin split — user boxes survive re-detect)"
    - "A confirm gate fires BEFORE replacing detected boxes when >= 1 detected box exists and Detect Boxes mode is on (D-04, mirrors _confirm_replace_mask)"
    - "On first detect with mode on, the box overlay auto-shows (toggle checked) so the boxes the user asked for are visible"
    - "A BOXES snapshot is pushed onto HistoryManager after the build (D-10) so the detection itself is undoable via the unified Ctrl+Z"
  artifacts:
    - "manga_ai_studio/gui/main_window.py (_on_detection_finished extended to build boxes from result['blocks']; _build_detected_boxes helper; Detect Boxes mode toggle action + Tools menu wiring; _confirm_replace_boxes mirroring _confirm_replace_mask; auto-show box overlay on first detect; push_boxes_state call)"
    - "tests/test_gui_detection_boxes.py (pytest-qt: blk_list -> boxes when mode on; discarded when off; D-03 re-detect keeps user; D-04 confirm gate; V5 clamp/drop out-of-range; auto-show overlay)"
  key_links:
    - "_on_detection_finished:1284 stop discarding result['blocks'] — the load-bearing one-line thread-through (no new worker, no new model)"
    - "The Detect Boxes mode toggle (action_detect_boxes_mode, checkable, default checked per UI-SPEC §Copywriting A1) — gates the box-build branch"
    - "V5 bounds-clamp at the TextBlock->Box boundary — model xyxy treated as untrusted (the ASVS L1-relevant control for this plan)"
    - "_confirm_replace_boxes mirroring _confirm_replace_mask:1328 — the D-04 confirm-gate pattern"
---

<objective>
Wire the TEXT-01 vertical slice end-to-end: when the user runs Detect Text with the Detect Boxes mode toggle on, the `blk_list` that the Phase 1 worker ALREADY returns (main_window.py:1217) is surfaced as editable BoxItems on the canvas instead of discarded. This is the "nearly free" seam (RESEARCH: no new worker, no new model, no new adapter) — the change is `_on_detection_finished` stops discarding `result["blocks"]` and builds BoxItems, gated by the D-01 mode toggle, with the D-03 re-detect rule (replace detected, keep user) and the D-04 confirm gate. Plus the V5 input-validation control: model-produced xyxy is bounds-clamped against the image rect before becoming a BoxItem.

Purpose: This completes TEXT-01 ("run text-box detection across a page to create editable text-box objects"). Combined with plan 03-03 (TEXT-03 select/move/resize/delete), the user can now detect boxes AND correct them — the full Phase 3 success criteria. The seam is small because Phase 1 already did the model work; Phase 3 just stops throwing the boxes away.

Output: Extended `_on_detection_finished` + mode toggle + confirm gate + V5 validator; one GUI test file. No persistence yet (plan 03-05) and no undo-UI collapse yet (plan 03-05).
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-text-box-detection-interaction/03-CONTEXT.md
@.planning/phases/03-text-box-detection-interaction/03-RESEARCH.md
@.planning/phases/03-text-box-detection-interaction/03-PATTERNS.md
@.planning/phases/03-text-box-detection-interaction/03-UI-SPEC.md
# The box layer this plan feeds (plan 03-03):
@.planning/phases/03-text-box-detection-interaction/03-03-SUMMARY.md
# The main_window being extended + the exact seam sites:
@manga_ai_studio/gui/main_window.py
# The model/validator (plan 03-01) + the canvas API (plan 03-03):
@manga_ai_studio/core/box_model.py
@manga_ai_studio/gui/canvas.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire _on_detection_finished to build boxes from result["blocks"] (D-01/D-03) + V5 bounds-clamp</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_detection_boxes.py</files>
  <read_first>
    - `manga_ai_studio/gui/main_window.py` lines 1284-1303 (`_on_detection_finished` — the seam; currently reads only result["mask"], discards result["blocks"]) + lines 1165-1217 (`_run_detection_task` — CONFIRMS result["blocks"] = blk_list is already in the dict at line 1217; NO worker change needed) + lines 1328-1348 (`_confirm_replace_mask` — the confirm-gate pattern to mirror for D-04) + lines 1093-1110 (`_confirm_clear_mask` — the second confirm-gate analog) + lines 304-308 (action_toggle_mask_overlay — the checkable-action pattern to mirror for action_toggle_box_overlay AND action_detect_boxes_mode) + lines 337-403 (_build_tools_menu — where the Detect Boxes mode toggle wires)
    - `manga_ai_studio/core/box_model.py` (from plan 03-01: PageBox, textblock_to_box, DETECTED, USER)
    - `manga_ai_studio/gui/canvas.py` (from plan 03-03: set_boxes(user_pageboxes, detected_pageboxes), set_box_overlay_visible, boxes_snapshot, has_boxes, box_origin_counts)
    - `manga_ai_studio/core/history_manager.py` (from plan 03-02: push_boxes_state)
    - `.planning/phases/03-text-box-detection-interaction/03-RESEARCH.md` §Code Examples "TextBlock -> BoxItem -> QRectF (TEXT-01 build step)" + §Security Domain V5 (xyxy treated as untrusted) + §Common Pitfalls Pitfall 6 (int at QRectF boundary)
    - `.planning/phases/03-text-box-detection-interaction/03-CONTEXT.md` D-01 (mode toggle), D-03 (replace detected keep user), D-04 (confirm gate)
    - `.planning/phases/03-text-box-detection-interaction/03-UI-SPEC.md` §10 (detection surface extended), §Copywriting (mode-toggle copy + confirm-gate copy)
    - `tests/test_core/conftest.py` (FakeDetectionModel — already returns (mask, blk_list); extend it for the box-build tests)
  </read_first>
  <behavior>
    - With Detect Boxes mode ON and a non-empty blk_list, _on_detection_finished builds one BoxItem per TextBlock (origin=DETECTED) and shows them on the canvas
    - With Detect Boxes mode OFF, _on_detection_finished builds NO boxes (Phase 1 behavior — mask only)
    - A TextBlock with xyxy partially out of the image rect is bounds-clamped (xyxy clamped to [0, img_w]/[0, img_h]); a TextBlock with non-positive area after clamping is dropped (V5)
    - Re-running detection (mode on, >= 1 detected box present) replaces the detected boxes and KEEPS the user boxes (D-03)
    - Re-running detection fires the confirm gate (D-04) BEFORE replacing when >= 1 detected box exists and mode is on; Cancel aborts (no replace)
    - On first detect with mode on, the box overlay auto-shows (action_toggle_box_overlay checked = True)
    - After the build, push_boxes_state(canvas.boxes_snapshot()) is called so the detection is undoable
  </behavior>
  <action>
    Extend `manga_ai_studio/gui/main_window.py`. Add the Detect Boxes mode toggle action in the Tools menu (D-01): in `_build_tools_menu` (or wherever checkable actions are created), add `self.action_detect_boxes_mode = QAction("Detect Boxes", self, checkable=True)`; set checked=True by default (UI-SPEC §Copywriting A1 — default checked); set tooltip/status tip per UI-SPEC ("When on, Detect Text also creates editable text boxes (in addition to the mask). Turn off for mask-only behaviour."); add to the Tools menu. Add `self.action_toggle_box_overlay = QAction("Toggle Box Overlay", self, checkable=True)` mirroring `action_toggle_mask_overlay` (main_window.py:304-308); shortcut Shift+M (UI-SPEC §11 — M is mask overlay; Shift+M is the sibling); place in `_build_view_menu` immediately AFTER Toggle Mask Overlay; connect toggled to `self.canvas.set_box_overlay_visible(checked)` + refresh the empty-box-hint visibility; enabled iff a page is open. (The View menu wiring can land in this task or be split — keep it here since the auto-show-on-detect depends on the action existing.)

    Modify `_on_detection_finished` (line 1284). Keep the existing mask handling (lines 1294-1302 unchanged). AFTER `self.canvas.set_mask(qimage.copy())`, add the box-build branch: `if self.action_detect_boxes_mode.isChecked():` then `blk_list = result.get("blocks") or []`; call `self._build_detected_boxes(blk_list)`. The branch is gated by the mode toggle (D-01) — when off, Phase 1 behavior is preserved exactly (mask only, no box work). result["blocks"] is ALREADY in the dict (main_window.py:1217 — `_run_detection_task` returns `{"mask": mask_refined, "blocks": blk_list}`); NO worker change, NO adapter change, NO new model call.

    Implement `_build_detected_boxes(self, blk_list)`: D-04 confirm gate first — if `self.canvas.has_boxes()` AND any existing box has origin == DETECTED (check via `box_origin_counts()`), call `self._confirm_replace_boxes()`; if it returns False, return early (Cancel — no replace). Then build the detected PageBoxes from blk_list with V5 validation: get the current image rect (width/height from `self.canvas` — the image_item's scene bbox or the loaded image dims). For each blk in blk_list: coerce via `textblock_to_box(blk)` (plan 03-01 — int coercion), then bounds-clamp the resulting Box against `[0, img_w]` / `[0, img_h]` (clamp x1/x2 to [0, img_w], y1/y2 to [0, img_h]); if the clamped box has non-positive area (x2 <= x1 or y2 <= y1), DROP it (V5 — log a debug via loguru "dropped zero-area detected box"); otherwise build a `PageBox(box=clamped_box, origin=DETECTED, payload=blk)` (payload = the TextBlock, preserved untouched for Phase 4/5 OCR + Phase 5 export per CONTEXT). Then apply the D-03 rule: collect the existing USER boxes from the canvas (`[(b.box, b.origin, b.payload) for b in canvas boxes if origin == USER]` — use `canvas.boxes_snapshot()` filtered, or add a canvas helper); call `self.canvas.set_boxes(user_boxes, detected_pageboxes)` (the plan 03-03 API rebuilds the layer with user + detected). Auto-show the overlay: `self.action_toggle_box_overlay.setChecked(True)` (which fires the toggled signal -> set_box_overlay_visible(True)). Push the BOXES snapshot: `self.history.push_boxes_state(self.canvas.boxes_snapshot())` (D-10 — detection is undoable). Update the status bar box count.

    Implement `_confirm_replace_boxes(self) -> bool` mirroring `_confirm_replace_mask` (main_window.py:1328-1348) verbatim in structure: `box = QMessageBox(self)`, `setIcon(Question)`, `setWindowTitle("Detect Text")`, `setText(...)` with the UI-SPEC §Copywriting body ("Replace the detected text boxes with a new detection? Boxes you drew yourself are kept. Undo is available via Ctrl+Z."), custom buttons `[Cancel] [Replace Detected Boxes]` (RejectRole/AcceptRole), `setDefaultButton(replace_btn)`, `box.exec()`, return `clickedButton() is replace_btn`. D-04 fires this AFTER the existing mask-replace gate if a mask is also present (the mask gate stays first — both gates can fire on a page with both a mask and detected boxes; order: mask gate -> boxes gate -> proceed).

    Create `tests/test_gui_detection_boxes.py` mirroring `tests/test_gui_canvas.py:1-58` header (`pytest.importorskip("PySide6")`, qtbot, `@pytest.mark.gui`, MainWindow fixture via shared conftest). Extend the FakeDetectionModel in `tests/test_core/conftest.py` (or a local fake) to return a non-empty blk_list of fake TextBlocks (a `SimpleNamespace(xyxy=[x1,y1,x2,y2], text=[], ...)` — duck-typed; the box-build only reads .xyxy). Cover: `test_builds_boxes_when_mode_on` (run detect with mode on + non-empty blk_list, assert `canvas.has_boxes()` and box_count matches blk_list length, all origin DETECTED); `test_no_boxes_when_mode_off` (toggle mode off, run detect, assert `not canvas.has_boxes()`); `test_redetect_replaces_detected_keeps_user` (Alt+drag a user box, run detect, assert the user box survives AND detected boxes are the new blk_list — D-03); `test_redetect_confirm_gate` (with detected boxes present, run detect, mock the QMessageBox to click Cancel, assert the detected boxes are NOT replaced — D-04); `test_v5_clamps_and_drops` (fake a TextBlock with xyxy partially out of image bounds -> clamped; xyxy entirely out/inverted -> dropped; assert box_count reflects only valid boxes); `test_auto_show_overlay_on_first_detect` (overlay off, run detect with mode on, assert `action_toggle_box_overlay.isChecked()` is True); `test_detection_pushes_boxes_snapshot` (run detect, assert `history.can_undo()` is True via the BOXES stack — the unified flag from plan 03-02). Mark `@pytest.mark.gui`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gui_detection_boxes.py -q -m gui</automated>
  </verify>
  <acceptance_criteria>
    - `_on_detection_finished` reads `result["blocks"]` — `grep -c 'result\["blocks"\]\|result.get("blocks")' manga_ai_studio/gui/main_window.py` >= 1 (no longer discarded)
    - `_on_detection_finished` gates the box build on the mode toggle — `grep -c "action_detect_boxes_mode.isChecked" manga_ai_studio/gui/main_window.py` >= 1
    - `grep -c "def _build_detected_boxes\|def _confirm_replace_boxes" manga_ai_studio/gui/main_window.py` == 2
    - `grep -c "action_detect_boxes_mode\|action_toggle_box_overlay" manga_ai_studio/gui/main_window.py` >= 4 (both actions declared + wired)
    - `grep -c "Shift+M\|Shift.M" manga_ai_studio/gui/main_window.py` >= 1 (the box overlay shortcut per UI-SPEC §11)
    - V5 clamp present — `grep -c "img_w\|image.width\|clamp\|max(0\|min(" manga_ai_studio/gui/main_window.py` shows the bounds-clamp logic in `_build_detected_boxes` (read the function to confirm xyxy is clamped against image rect and zero-area boxes dropped)
    - The confirm-gate body matches UI-SPEC §Copywriting — `grep -c "Boxes you drew yourself are kept" manga_ai_studio/gui/main_window.py` == 1 (the D-04 copy)
    - `_run_detection_task` is UNCHANGED — `grep -c '"blocks": blk_list' manga_ai_studio/gui/main_window.py` == 1 (still in the result dict at line ~1217; no worker change)
    - `python -m pytest tests/test_gui_detection_boxes.py -q -m gui` exits 0
    - `python -m pytest tests/ -q` exits 0 (full suite green at the plan boundary — plan 03-05 not yet merged)
  </acceptance_criteria>
  <done>_on_detection_finished builds BoxItems from result["blocks"] when Detect Boxes mode is on (TEXT-01); Phase 1 discard replaced; V5 clamps/drops out-of-range xyxy; D-03 re-detect keeps user boxes; D-04 confirm gate fires before replacing detected boxes; box overlay auto-shows on first detect; BOXES snapshot pushed (detection undoable); test_gui_detection_boxes.py green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify TEXT-01 detection -> boxes on a real page (green/amber distinction)</name>
  <files>.planning/phases/03-text-box-detection-interaction/03-04-SUMMARY.md</files>
  <action>
    Plan 03-04 delivers the TEXT-01 vertical slice: run Detect Text with Detect Boxes mode on and the model's blk_list becomes editable green BoxItems on the canvas. Combined with plan 03-03's interaction loop, the user can now detect boxes AND correct them (move misaligned boxes, delete false positives, draw missed boxes via Alt+drag, re-detect which keeps user corrections). The automated suite covers the mechanics; this checkpoint confirms detection works on a real page with the real CTD model and the green/amber distinction reads correctly on real artwork.
  </action>
  <verify>
    <human-check>
      Launch the app (`python -m manga_ai_studio`) on the project venv (CTD model weights present) and perform these checks:
      1. Open a manga chapter folder; navigate to a text-heavy page.
      2. Confirm Detect Boxes is checked in the Tools menu (default on). Confirm View -> Toggle Box Overlay (Shift+M) exists.
      3. Press D (or Tools -> Detect Text). Watch the detection progress; on completion, confirm: the mask overlay appears (Phase 1) AND green boxes appear over each detected text region AND the box overlay auto-toggled on.
      4. Visually confirm the boxes align with actual text regions. Note any false positives (boxes over non-text) and false negatives (text with no box).
      5. Correct a FALSE POSITIVE: select a green box over non-text, press Delete. Confirm it vanishes instantly (silent — D-12).
      6. Correct a FALSE NEGATIVE: Alt+drag to draw an amber user box over missed text. Confirm amber colour (distinct from green detected).
      7. Re-run Detect Text (D again). Confirm the D-04 confirm dialog appears ("Replace the detected text boxes... Boxes you drew yourself are kept"). Click Replace Detected Boxes. Confirm: the green detected boxes are replaced with the fresh detection; your amber user box SURVIVES (D-03).
      8. Toggle Detect Boxes OFF in the Tools menu. Run Detect Text again. Confirm: only the mask appears (Phase 1 behavior) — NO boxes built (D-01 mode toggle gates).
    </human-check>
    <automated>cd "C:/Src/Manga AI Studio" &amp;&amp; python -m pytest tests/test_gui_detection_boxes.py -q -m gui 2>&1 | findstr /C:"passed" /C:"failed"</automated>
  </verify>
  <done>All eight manual checks pass (detection produces green boxes over text; auto-show overlay; delete false positive; Alt+drag amber user box; re-detect replaces detected + keeps user; mode-off = mask only) and the test_gui_detection_boxes.py suite is green.</done>
  <resume-signal>Type "approved" or describe issues (e.g. "boxes are slightly offset from text", "confirm dialog feels redundant", "green hard to distinguish on green artwork").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output (blk_list) -> app data model | TextBlock.xyxy crosses here at _build_detected_boxes; this is the ASVS V5 control point (model output treated as untrusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | `_build_detected_boxes` xyxy handling (main_window.py) | high | mitigate | Coerce xyxy to int (textblock_to_box, plan 03-01) AND bounds-clamp against the current image rect ([0, img_w]/[0, img_h]) AND drop boxes with non-positive area after clamping. Model output is NOT trusted — a malformed xyxy (negative, out-of-image, inverted) is clamped or dropped, never allowed to corrupt the canvas or crash Qt. This is the ASVS L1-relevant control the planner guidance flagged. Regression guard: test_v5_clamps_and_drops. |
| T-03-07 | Repudiation | D-04 confirm gate (_confirm_replace_boxes) | low | mitigate | The confirm dialog fires before replacing detected boxes (mirrors _confirm_replace_mask); Cancel aborts. Boxes are also undoable via the BOXES stack (D-10/D-11) — belt-and-suspenders: the gate prevents accidental loss, undo recovers if the gate is bypassed. |

The realistic Phase 3 threat per the planner guidance is exactly T-03-06: model output treated as trusted. Single-user offline desktop app; no network, no auth, no untrusted input beyond image files (Phase 1 already sandboxes via validate_image_path/validate_image_size).
</threat_model>

<verification>
- `python -m pytest tests/test_gui_detection_boxes.py -q -m gui` exits 0
- `python -m pytest tests/ -q` exits 0 (full suite green at the plan boundary)
- Manual checkpoint passed (detection -> green boxes; D-03 re-detect keeps user; D-04 confirm gate; D-01 mode toggle gates; V5 robustness on edge cases)
- `_run_detection_task` is byte-identical to Phase 1 (no worker change — the seam is purely in the finished handler)
</verification>

<success_criteria>
- TEXT-01 deliverable: user runs Detect Text with mode on and editable box objects appear (not a pixel mask). Phase 1's `blk_list` discard is replaced.
- D-01 (mode toggle gates), D-03 (re-detect replaces detected, keeps user), D-04 (confirm gate) — all honored.
- V5 input validation: model xyxy coerced + bounds-clamped + zero-area dropped (the ASVS L1 control).
- The seam is the small change RESEARCH predicted: one branch in _on_detection_finished + one helper + one confirm gate + one toggle action. No new worker/model/adapter.
- Combined with plan 03-03, the user can detect AND correct boxes — both Phase 3 success criteria met (TEXT-01 + TEXT-03).
</success_criteria>

<output>
Create `.planning/phases/03-text-box-detection-interaction/03-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

**MainWindow extensions (gui/main_window.py):**
- `MainWindow.action_detect_boxes_mode` (checkable QAction, default checked, Tools menu — D-01 mode toggle)
- `MainWindow.action_toggle_box_overlay` (checkable QAction, Shift+M, View menu after Toggle Mask Overlay — D-02 layer toggle)
- `MainWindow._build_detected_boxes(blk_list)` (D-03 replace-detected-keep-user + V5 bounds-clamp/drop + PageBox build + set_boxes + auto-show overlay + push_boxes_state)
- `MainWindow._confirm_replace_boxes() -> bool` (D-04 confirm gate mirroring _confirm_replace_mask)
- `_on_detection_finished` extended: reads result["blocks"], gated by action_detect_boxes_mode.isChecked()

**Wave 0 test file (per VALIDATION.md):**
- `tests/test_gui_detection_boxes.py` (pytest-qt: builds-when-on, discarded-when-off, D-03 re-detect, D-04 gate, V5 clamp/drop, auto-show overlay, pushes boxes snapshot)
