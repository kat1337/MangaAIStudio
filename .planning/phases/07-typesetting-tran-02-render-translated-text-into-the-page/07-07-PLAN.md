---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 07
type: execute
wave: 2
depends_on: [07-06]
files_modified:
  - manga_ai_studio/gui/box_item.py
  - manga_ai_studio/gui/text_renderer.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/inspector_panel.py
  - tests/test_core/test_typeset_layout.py
  - tests/test_gui_boxes.py
  - tests/test_gui_inspector_styling.py
autonomous: true
gap_closure: true
gap_ids: [G-07-1]
requirements:
  - TRAN-02
assumption_delta:
  decision: no-change
  rationale: "The user OVERRIDES the W3C/D-11 rotated-Latin convention in vertical mode (UAT G-07-1, user report): roman text renders upright, one letter above the other, and rendering is horizontal by default regardless of detected orientation. The override is user-locked and recorded in the UAT — do not re-litigate D-11; implement the override. payload.vertical remains pure export metadata (project_io/ocr_export/image_ops preserve it untouched)."
estimate:
  tokens: 27000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Rendering flips vertical ONLY when the box's style.vertical is set — the detector's payload.vertical metadata never auto-flips rendering (horizontal by default for detected-vertical AND detected-horizontal boxes)"
    - "The Inspector Vertical checkbox reads and writes style.vertical (single- and multi-select); payload.vertical is no longer written by the toggle and a never-OCR'd box's payload is never constructed by the toggle"
    - "In vertical mode, Latin letters and digits stack upright (rotate=False) one above the other; the bracket/dash set still rotates; the exported payload.vertical metadata is unchanged (project_io 'vertical' key, ocr_export block 'vertical', image_ops transform preservation)"
    - "Canvas overlay, bake, and the rendered-size probe agree on ONE render flag expression: bool(style.vertical)"
  artifacts:
    - manga_ai_studio/gui/box_item.py :: refresh_text_overlay vertical flag (:673-680) — bool(style.vertical) only
    - manga_ai_studio/gui/text_renderer.py :: bake_typeset_page vertical flag (:981-988) — bool(style.vertical) only; char_rotates (:136-146) — letters/digits upright, ASCII punctuation + _ROTATE_EXTRA rotate; classification constants (:112-132) + module docstring (:34-47) + layout_vertical docstring updated
    - manga_ai_studio/gui/main_window.py :: _style_rendered_size vertical flag (:3069-3076) — bool(style.vertical) only; _on_inspector_vertical_committed (:3227-3261) — writes style.vertical via _replace_style, drops the payload write + _ensure_payload construction
    - manga_ai_studio/gui/inspector_panel.py :: single-select vertical read (:540-551) + multi-select vertical read (:695-714) — style.vertical instead of payload.vertical; vertical_check tooltip wording updated (must still contain 'tategaki', no 'Coming soon')
    - tests/test_core/test_typeset_layout.py :: test_vertical_classification + test_vertical_rotated_advance updated to the upright-Latin contract
    - tests/test_gui_boxes.py :: test_vertical_checkbox_live, test_vertical_toggle_constructs_payload_on_never_ocrd_box (renamed + re-asserted), test_vertical_preflagged_box_renders_vertical (renamed + re-asserted), test_inspector_load_box_populates_fields updated
    - tests/test_gui_inspector_styling.py :: test_load_multi_selection_bare_payload_defensive updated (style.vertical basis)
  key_links:
    - "refresh_text_overlay <-> bake_typeset_page <-> _style_rendered_size: the SAME single expression bool(style.vertical) at all three render sites — no divergence window (the old OR comment is deleted with the OR)"
    - "checkbox commit (writes style.vertical) <-> inspector loads (read style.vertical): the toggle path and the panel population agree on the style field"
    - "char_rotates classification <-> _vertical_placements advance: upright letters/digits flow along the WIDTH-based column advance — geometry already supports upright stacking"
  prohibitions:
    - "payload.vertical must never influence rendering anywhere (the three render sites + the inspector reads are the complete render-side surface)"
    - "The checkbox toggle must not write payload.vertical nor construct payloads (WR-01's _ensure_payload call is removed with the payload write)"
    - "Export metadata sites (project_io.py 'vertical', ocr_export.py block 'vertical', image_ops.py transform preservation) stay untouched"
    - "Tests that locked the old behavior are UPDATED to the new contract, never deleted and never left asserting rotated Latin"
---

<objective>
Close **G-07-1 (major)** from 07-UAT.md, two mechanisms: (1) the render-vertical flag is `style.vertical OR payload.vertical` at box_item.py:673-680, text_renderer.py:981-988 (bake), main_window.py:3069-3076 (rendered-size probe), and the Inspector checkbox writes `payload.vertical` (main_window.py:3227-3261) while the panel reads it (inspector_panel.py:540-551 single / 695-714 multi) — the detector's `payload.vertical` metadata auto-flips rendering; (2) `char_rotates` classifies ALL halfwidth ASCII as rotate=True (text_renderer.py:112-146, the W3C convention the user overrides). User contract: roman text renders horizontal by default regardless of detected orientation, and when vertical is ENABLED it renders upright one letter above the other — not rotated 90°; `payload.vertical` stays pure export metadata.

Purpose: make the render flag `bool(style.vertical)` ONLY at all three render sites, move the checkbox to read/write `style.vertical`, flip Latin/digits to upright (geometry already supports upright stacking), and update every test that locked the old behavior to the new contract.

Output: the single-expression render flag, the style-driven checkbox, the upright-Latin classification, and the re-asserted test suite.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UAT.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-CONTEXT.md
@manga_ai_studio/gui/box_item.py
@manga_ai_studio/gui/text_renderer.py
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/inspector_panel.py
@tests/test_core/test_typeset_layout.py
@tests/test_gui_boxes.py
@tests/test_gui_inspector_styling.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN — the render flag is bool(style.vertical) ONLY at all three render sites</name>
  <files>manga_ai_studio/gui/box_item.py, manga_ai_studio/gui/text_renderer.py, manga_ai_studio/gui/main_window.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/box_item.py — refresh_text_overlay (:666-687; the OR at :673-680 and its D-13 comment at :668-672)
    - manga_ai_studio/gui/text_renderer.py — bake_typeset_page (:964-992; the OR at :981-988 and its comment at :974-980)
    - manga_ai_studio/gui/main_window.py — _style_rendered_size (:3059-3078; the OR at :3069-3076)
    - tests/test_gui_boxes.py — test_vertical_preflagged_box_renders_vertical (:4284-4300 — locks the OLD auto-vertical behavior: a payload.vertical=True box renders through the vertical path)
  </read_first>
  <behavior>
    - Test 1 (RED gate, new contract): a box that arrives with `payload.vertical=True` (CTD pre-flagged) and NO style renders HORIZONTALLY by default — `vertical_placements == []` and the layout is the horizontal document layout. Rename `test_vertical_preflagged_box_renders_vertical` to `test_preflagged_box_renders_horizontal_by_default` and re-assert: payload.vertical True + style None → horizontal placements. MUST FAIL on current code (the OR flips it vertical).
    - Test 2 (bake spy): bake_typeset_page renders a preflagged box (payload.vertical=True, style None) with `vertical=False` captured in the layout spy.
  </behavior>
  <action>RED: update tests/test_gui_boxes.py per the behavior block — re-assert the preflagged-box scenario to the new contract (payload.vertical alone never flips rendering; the rename reflects the contract, the scenario stays). Run — must fail at the new assertions. GREEN, three production edits, ALL the same shape: replace the OR expression with `vertical = bool(style.vertical)` — (1) box_item.py:673-680 (delete the D-13 OR comment at :668-672 and the payload getattr branch), (2) text_renderer.py:981-988 in bake_typeset_page (delete the OR comment at :974-980; the note about the falsy vendored TextBlock becomes moot and is removed), (3) main_window.py:3069-3076 in _style_rendered_size. The export-metadata sites (project_io.py:201 'vertical' key, ocr_export block 'vertical', image_ops.py:150/277/407 payload preservation) are UNTOUCHED — payload.vertical remains the exported/detector metadata. Run the vertical GUI tests — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py -k "vertical or preflagged or checkbox" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_preflagged_box_renders_horizontal_by_default FAILS on the pre-fix code (the OR renders it vertical) and PASSES after the three-site change
    - Grep gate (render-side OR gone): `rg -n "payload\.vertical" manga_ai_studio/gui/box_item.py manga_ai_studio/gui/text_renderer.py` returns NO matches (both render-site files are payload.vertical-free after the edit)
    - Grep gate (probe site): `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'style.vertical'` >= 2 (the _style_rendered_size read + the vertical-commit write added in Task 2 — at minimum the read at :3069 now present)
    - The export-metadata payload.vertical sites (project_io.py, ocr_export.py, image_ops.py) are byte-unchanged
  </acceptance_criteria>
  <done>All three render sites use the single bool(style.vertical) expression; a preflagged box renders horizontal by default; the bake agrees via spy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-GREEN — the Inspector checkbox reads/writes style.vertical (single + multi)</name>
  <files>manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/inspector_panel.py, tests/test_gui_boxes.py, tests/test_gui_inspector_styling.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py — _on_inspector_vertical_committed (:3227-3261: writes payload.vertical at :3255 + _ensure_payload at :3254; the op-name 'style change' at :3256; _replace_style (:3028-3031) is the style-write mechanism)
    - manga_ai_studio/gui/inspector_panel.py — single-select vertical load (:540-551 reads payload.vertical at :545), multi-select vertical load (:695-714 reads payload.vertical at :698-703), the vertical_check tooltip
    - tests/test_gui_boxes.py — test_vertical_checkbox_live (:4196-4252: asserts payload.vertical writes at :4218/:4226 + the bake spy at :4237-4252), test_vertical_toggle_constructs_payload_on_never_ocrd_box (:4256-4280: asserts the payload IS constructed at :4270-4273 and undo restores payload None at :4278-4280), test_inspector_load_box_populates_fields (:1876-1892: sets payload.vertical at :1881, asserts checked at :1892)
    - tests/test_gui_inspector_styling.py — test_load_multi_selection_bare_payload_defensive (:374-395: builds the vertical-True box via payload.vertical at :393)
  </read_first>
  <behavior>
    - Test 1 (RED gate): toggling the checkbox writes `item.pagebox.style.vertical` (NOT payload.vertical); on a never-OCR'd box (payload None) the toggle does NOT construct the payload — payload stays None and undo restores style to the pre-toggle state. Update `test_vertical_toggle_constructs_payload_on_never_ocrd_box` to the new contract (rename to `test_vertical_toggle_writes_style_and_never_constructs_payload`): assert payload stays None after the toggle, `style.vertical is True`, one Ctrl+Z restores `style` to None (defaults). MUST FAIL on current code (the toggle constructs the payload and writes payload.vertical).
    - Test 2: `test_vertical_checkbox_live` re-asserts via style: toggle ON -> `item.pagebox.style.vertical is True` + overlay vertical_placements non-empty; toggle OFF -> horizontal; the bake spy still captures vertical=True (style-driven); tooltip contains 'tategaki', no 'Coming soon'.
    - Test 3: `test_inspector_load_box_populates_fields` seeds `pb.style = TextStyle(vertical=True)` (not payload.vertical) and asserts the checkbox reads it; `test_load_multi_selection_bare_payload_defensive` builds the differing box via `style = TextStyle(vertical=True)`.
  </behavior>
  <action>RED: update the four tests per the behavior block (assertions on style.vertical; the payload-construction assertions removed). Run — must fail. GREEN, two production edits: (1) main_window.py `_on_inspector_vertical_committed` — replace the per-item payload write (the `_ensure_payload()` call plus the payload-flag assignment pair at :3254-3255, which the acceptance grep asserts is gone) with `self._replace_style(item.pagebox, vertical=vertical)`; the before-snapshot, ONE boxes_modified emit, op-name 'style change', overlay refresh, and reload stay exactly as-is. (2) inspector_panel.py — single-select load (:545): read `vertical = bool(pagebox.style.vertical) if pagebox.style is not None else False` (the style fallback matches `_load_style_section`'s `style = pagebox.style if pagebox.style is not None else TextStyle()` at :555); multi-select load (:698-703): `verticals = {(pb.style.vertical if pb.style is not None else False) for pb in pageboxes}`; refresh the vertical_check tooltip copy so it describes the style-driven flag (keep the word 'tategaki'; no 'Coming soon'). Run — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py tests/test_gui_inspector_styling.py -k "vertical or inspector_load or mixed_state or load_multi" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_vertical_toggle_writes_style_and_never_constructs_payload FAILS pre-fix (payload gets constructed + written) and PASSES post-fix with payload staying None
    - test_vertical_checkbox_live passes with the style-driven assertions (overlay flips, bake spy vertical=True, tooltip 'tategaki' present)
    - test_inspector_load_box_populates_fields + test_load_multi_selection_bare_payload_defensive pass with style.vertical seeding
    - Grep gate (no payload write in the toggle): `grep -c 'payload.vertical = vertical' manga_ai_studio/gui/main_window.py` == 0
    - Grep gate (single payload.vertical read left in the panel is none): `rg -n "payload\.vertical" manga_ai_studio/gui/inspector_panel.py` returns no matches
  </acceptance_criteria>
  <done>The checkbox commit writes style.vertical and never constructs/writes payloads; both panel load paths read style.vertical; the four tests lock the new contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED-GREEN — Latin letters/digits upright in vertical mode (user override of W3C rotation)</name>
  <files>manga_ai_studio/gui/text_renderer.py, tests/test_core/test_typeset_layout.py</files>
  <read_first>
    - manga_ai_studio/gui/text_renderer.py — the classification constants (:105-133: _ASCII_ROTATE = 0x21..0x7E, _ROTATE_EXTRA, _ALIGN_CENTER), char_rotates (:136-146 + docstring), the module docstring vertical paragraph (:34-47), the layout_vertical docstring (:440-460 — 'size_px' + classification references), _vertical_placements' use of char_rotates (:384)
    - tests/test_core/test_typeset_layout.py — test_vertical_classification (:202-219: asserts ALL of 0x21..0x7E rotate + the extra set + the upright set), test_vertical_rotated_advance (:293-311: asserts 'A'/'1' rotate with height>width and the height-based advance)
  </read_first>
  <behavior>
    - Test 1 (RED gate): `char_rotates` classification — ASCII letters (A-Z, a-z) and digits (0-9) are UPRIGHT (False); halfwidth ASCII punctuation (e.g. '!', '?', '.', ',', ':', ';') still rotates; the _ROTATE_EXTRA bracket/dash set still rotates; CJK/kana/vertical-form punctuation upright as before. Update test_vertical_classification: split the 0x21..0x7E loop into letters+digits (upright) vs ASCII punctuation (rotate); keep the extra-set and upright-set loops. MUST FAIL pre-fix (letters/digits assert rotate).
    - Test 2 (RED gate): test_vertical_rotated_advance — 'A1' now place upright (rotate False, square-ish w >= h - tolerance) and a rotated punctuation sample (e.g. '!') keeps the height>width + height-based-advance assertions; the CJK assertion unchanged.
  </behavior>
  <action>RED: update the two tests per the behavior block. Run — must fail at the letter/digit assertions. GREEN in text_renderer.py: (1) replace the `_ASCII_ROTATE` definition with a letters+digits upright set and a punctuation-only rotate set — e.g. `_ASCII_UPRIGHT = set(chr(i) for i in range(0x30, 0x3A)) | set(chr(i) for i in range(0x41, 0x5B)) | set(chr(i) for i in range(0x61, 0x7B))` and `_ASCII_ROTATE = set(chr(i) for i in range(0x21, 0x7F)) - _ASCII_UPRIGHT` (halfwidth ASCII punctuation keeps rotating; _ROTATE_EXTRA unchanged — brackets/dashes/ellipsis rotate per the typographic convention); (2) update char_rotates' docstring: letters/digits are UPRIGHT — the user override of the W3C mixed-orientation convention (one letter above the other); (3) update the module docstring vertical paragraph (:36-37) and the layout_vertical docstring (:449-451) to state the upright-Roman rule. The geometry (column advance, centering) needs NO change — upright stacking is already supported. Run the full vertical layout test set — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_layout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_vertical_classification FAILS pre-fix at the letters/digits assertions and PASSES post-fix
    - test_vertical_rotated_advance passes: upright A/1 with the width-based advance; the rotated punctuation sample keeps the height-based advance coverage
    - test_vertical_rtl_column_flow, test_vertical_wrap_at_height, test_vertical_centering_and_alignment, test_vertical_code_point_indexing pass UNCHANGED (CJK upright behavior untouched)
    - Grep gate: `grep -c '_ASCII_UPRIGHT' manga_ai_studio/gui/text_renderer.py` >= 2 (definition + the _ASCII_ROTATE difference — the classification split is present)
    - Full suite green at the re-baselined count (687 baseline from 07-VERIFICATION.md + 0 new = 689 passed, 0 failed — all changes are updates)
  </acceptance_criteria>
  <done>Latin letters and digits stack upright in vertical mode; the punctuation/bracket rotation convention is preserved; all vertical layout tests pass under the new contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/box_item.py`:
  - `refresh_text_overlay` — `vertical = bool(style.vertical)` (:673-680, the OR deleted)
- `manga_ai_studio/gui/text_renderer.py`:
  - `bake_typeset_page` — `vertical = bool(style.vertical)` (:981-988, the OR deleted)
  - `char_rotates` — letters/digits upright; `_ASCII_UPRIGHT` set + punctuation-only `_ASCII_ROTATE` (:112-146); docstrings updated (module :34-47, char_rotates, layout_vertical)
- `manga_ai_studio/gui/main_window.py`:
  - `_style_rendered_size` — `vertical = bool(style.vertical)` (:3069-3076)
  - `_on_inspector_vertical_committed` — writes `style.vertical` via `_replace_style`; `_ensure_payload` + payload write removed (:3254-3255)
- `manga_ai_studio/gui/inspector_panel.py`:
  - single-select vertical load (:545) + multi-select vertical load (:698-703) — read `style.vertical`; tooltip copy refreshed
- Tests updated (never deleted) to the new contract: `test_vertical_classification`, `test_vertical_rotated_advance`, `test_vertical_checkbox_live`, `test_vertical_toggle_writes_style_and_never_constructs_payload` (renamed), `test_preflagged_box_renders_horizontal_by_default` (renamed), `test_inspector_load_box_populates_fields`, `test_load_multi_selection_bare_payload_defensive`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector metadata → render flag | `payload.vertical` (CTD-detected orientation metadata) crosses into the render decision; the old OR let untrusted detector metadata override the user's explicit style choice |
| Inspector checkbox → style model | The toggle writes per-box state; the old path constructed payloads on never-OCR'd boxes (side-effect mutation beyond the user's intent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Spoofing | refresh_text_overlay / bake_typeset_page / _style_rendered_size render-flag OR | high | mitigate | Render flag = `bool(style.vertical)` ONLY — detector metadata can no longer spoof the render orientation; all three sites share the one expression; preflagged-box regression test pins horizontal-by-default (ASVS V1-style UI integrity, no trust in metadata) |
| T-07-11 | Tampering | _on_inspector_vertical_committed payload construction | medium | mitigate | Toggle writes style.vertical via `_replace_style` (dataclasses.replace — fresh instance, Pitfall 1); the `_ensure_payload()` side effect is removed; the never-OCR'd regression re-asserts payload stays None (no spurious undo entries, WR-01 preserved) |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_layout.py tests/test_gui_boxes.py tests/test_gui_inspector_styling.py -x -q` — affected modules green
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 689 passed, 0 failed (687 baseline from 07-VERIFICATION.md, no new tests)
- Grep gates: no `payload.vertical` matches in box_item.py / text_renderer.py / inspector_panel.py; no `payload.vertical = vertical` write in main_window.py; export-metadata sites byte-unchanged
</verification>

<success_criteria>
- Vertical rendering is driven exclusively by the user's per-box style flag; detected orientation is metadata only
- Vertical mode renders roman text upright, one letter above the other (user override), with punctuation/brackets still rotated
- Every test that locked the old behavior asserts the new contract; full suite green at 689
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-07-SUMMARY.md` when done
</output>
