---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 05
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - manga_ai_studio/gui/inspector_panel.py
  - manga_ai_studio/gui/box_item.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/text_renderer.py
  - tests/test_gui_inspector_styling.py
  - tests/test_gui_boxes.py
autonomous: true
requirements:
  - TRAN-02
estimate:
  tokens: 60000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Inspector gains a Style section (D-05) BELOW the existing text fields: 'Style' 12px semibold muted header + 1px divider; Font (QFontComboBox), Style (QComboBox from QFontDatabase.styles, refreshed on font change), Size (QSpinBox 0..200, 0 = 'Auto' sentinel) + Auto-fit checkbox (checked → spin disabled at 0; unchecked → spin shows the current rendered size rounded), Color (24×24 QToolButton swatch → QColorDialog), Align (Left/Center/Right) + Align V (Top/Middle/Bottom) combos, and Outline/Glow/Shadow rows (enable checkbox + swatch + spin; disabled row greys swatch+spin); the styling controls join the existing QSS token set and the WR-01 no-op guards"
    - "Empty state (UI-SPEC empty covered row): no selection → the whole panel (text fields AND styling section) disables with the extended copy 'Select a text box to edit its text, translation, and style.'"
    - "Common-value/Mixed (D-10, UI-SPEC partial covered row): differing values across the selection show 'Mixed' (font/style combo entries, size-spin special text, split swatch #e8e8ea/#9a9aa2, tri-state checkboxes); the per-box text fields (Bubble #, Origin, Recognized, Translation, Language) disable at N>1 while the styling section stays enabled; the hint 'Style edits apply to all {n} selected boxes.' shows; ONE override applies to ALL selected boxes with ONE BOXES snapshot + ONE overlay refresh; the Mixed sentinel never persists into a TextStyle (Pitfall 7)"
    - "Uniform selections show real values, never Mixed (UI-SPEC populated covered row)"
    - "The Vertical text checkbox is LIVE (D-13): 'Coming soon' tooltip replaced by 'Render this box's text vertically, top-to-bottom (tategaki).'; toggling writes payload.vertical AND re-renders the overlay + bake vertically; the render vertical flag = pb.style.vertical OR payload.vertical, read by BOTH the overlay and bake_typeset_page (atomic flip — no divergence window); CTD pre-flagged vertical boxes render tategaki; the inline editor stays horizontal (D-12)"
    - "Increase/Decrease Font Size (D-16): Text menu Typesetting section, Ctrl+] / Ctrl+[ (UI-SPEC-locked pair — Ctrl+- is taken by zoom-out), ±1 px; an Auto-fit box converts to MANUAL at its current rendered size before the delta (A11); applies to ALL selected boxes in ONE snapshot; undo flash op name 'font size'; gated on page_open + ≥1 box selected + no async op"
    - "Every style commit (incl. the vertical toggle) pushes ONE BOXES snapshot with the recorded op name 'style change' — one Ctrl+Z reverses the whole commit (D-10/surface 13)"
    - "Manual-size overflow (UI-SPEC overflow covered row): manual-size text wraps at the box inner width and overflows UNCLIPPED on both canvas and bake (clipped only at the page edge) — the WYSIWYG truth, D-01"
  artifacts:
    - manga_ai_studio/gui/inspector_panel.py :: Style section widgets + signals + Mixed logic + live vertical checkbox + hint label + extended empty state
    - manga_ai_studio/gui/main_window.py :: style-commit routing to ALL selected (one snapshot, op name 'style change') + action_increase/decrease_font_size (Ctrl+] / Ctrl+[)
    - manga_ai_studio/gui/box_item.py :: overlay refresh reads the vertical OR + renders through the 07-03 vertical path
    - manga_ai_studio/gui/text_renderer.py :: bake_typeset_page vertical flag = the same OR expression
    - tests/test_gui_inspector_styling.py (NEW) + tests/test_gui_boxes.py (extended)
  key_links:
    - "style commit → boxes_modified emitted ONCE → ONE BOXES snapshot + ONE overlay refresh (the _inspector_commit_pre/post pair, main_window.py:2794-2834)"
    - "vertical flag OR expression shared verbatim by box_item refresh and bake_typeset_page (canvas ≡ bake vertical flip)"
    - "Size +/- handler → per-box dataclasses.replace on TextStyle (never in-place mutation — Pitfall 1)"
  prohibitions:
    - "OCR/translation text is never rendered via setHtml or rich-text — plain-text rendering only (ASVS V5; QTextLayout plain runs / setPlainText)"
    - "TextStyle is never mutated in place — every style change assigns a fresh instance via dataclasses.replace; snapshots detach style (Pitfall 8/1); the Mixed sentinel never persists into a TextStyle"
    - "Vertical text is never whole-block rotated — tategaki is per-run layout; the bake composites text only (no chrome) onto a detached page copy (Pitfall 2)"
    - "Group/style ops push exactly ONE BOXES snapshot; overlay reposition never re-layouts during mousemove (RC-1 — setPos-only)"
---

<objective>
Make styling user-controllable: the Inspector's **Style section** (D-05) with full per-box controls — font family/style, size + Auto-fit, color, H/V alignment, outline/glow/shadow effects — the **common-value/Mixed behavior** for multi-select (D-10), the **live Vertical text checkbox** (D-13) that flips canvas AND bake to the 07-03 tategaki path, and the **Increase/Decrease Font Size actions** (D-16). Every commit is one BOXES snapshot + one overlay refresh, undoable in one Ctrl+Z.

Purpose: this is the phase's user-facing surface — the deferred styling list (04-CONTEXT 2026-08-04) becomes real controls. It consumes the multi-select model (07-02), the tategaki + effects renderer (07-03), and the style model + persistence (07-01/07-04). Per-page styling = Select All Boxes + apply (D-06 flat — no cascade).

Output: the styling section + Mixed logic in `inspector_panel.py`, the apply-to-all commit routing + size actions in `main_window.py`, the vertical-flag wiring in `box_item.py`/`text_renderer.py`, and the GUI test battery `tests/test_gui_inspector_styling.py` plus `test_gui_boxes.py` extensions.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-CONTEXT.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-RESEARCH.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-PATTERNS.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UI-SPEC.md
@manga_ai_studio/gui/inspector_panel.py
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/box_item.py
@manga_ai_studio/gui/text_renderer.py
@manga_ai_studio/core/text_style.py
</context>
<tasks>

<task type="auto">
  <name>Task 1: Inspector Style section — widgets, signals, QSS, empty-state (D-05)</name>
  <files>manga_ai_studio/gui/inspector_panel.py, tests/test_gui_inspector_styling.py</files>
  <read_first>
    - manga_ai_studio/gui/inspector_panel.py — `_INSPECTOR_QSS` :64-89 (the token set to extend), the QFormLayout rows :164-205 (the section joins below the Language/vertical rows), class-scope Signal declarations :142-146 (the `vertical_changed = Signal(bool)` precedent), `load_box` :220-281 (blockSignals population + `_loaded_*` memory), `clear()` :283-307 + `_set_fields_enabled` :309-317 (the empty-state gate the styling controls join), the commit guards :351-372 (WR-01 no-op discipline), the vertical checkbox :199-203 ("Coming soon" tooltip — replaced in Task 3)
    - 07-PATTERNS.md — §`gui/inspector_panel.py` (QSS rows, one signal per commit-able control, load/commit/no-op patterns) and Shared Pattern 10 (the panel pattern)
    - 07-UI-SPEC.md — surface 33 (the full styling-section layout sketch: Font/Style/Size+Auto-fit/Color/Align/Align V/Outline/Glow/Shadow rows), the spacing exceptions table (0..200 size with Auto sentinel, 0..10 outline, 0..20 glow, 0..10 shadow, 24×24 swatch), the Copywriting rows (section header "Style", row labels, "Auto-fit" + tooltips)
    - 07-RESEARCH.md — Don't-Hand-Roll (QFontComboBox / QColorDialog — never hand-rolled lists)
  </read_first>
  <action>In `gui/inspector_panel.py` extend `_INSPECTOR_QSS` (:64-89) with `QFontComboBox`, `QComboBox`, `QSpinBox`, `QToolButton`, `QCheckBox` rows reusing the existing tokens (`#2d2d33` bg, `#3a3a42` border, `#e8e8ea` fg, `:disabled` → `#25252b`/`#6a6a72`). Add the Style section BELOW the existing form rows (after the vertical checkbox row): a 12px semibold muted `#9a9aa2` "Style" header QLabel + a 1px QFrame HLine divider, then the rows per UI-SPEC surface 33: Font — QFontComboBox (default "Liberation Sans"); Style — QComboBox populated from `QFontDatabase.styles(family)`, repopulated on Font change; Size — QSpinBox 0..200 with `setSpecialValueText("Auto")` at 0 (the bubble_spin em-dash pattern) plus an "Auto-fit" QCheckBox beside it (checked → spin setValue(0) + setEnabled(False); unchecked → spin enabled with the current rendered size rounded); Color — 24×24 QToolButton swatch painting the current color, click → `QColorDialog.getColor` seeded with it; Align / Align V — QComboBoxes (Left/Center/Right; Top/Middle/Bottom); Outline / Glow / Shadow — three rows each with an enable QCheckBox + 24×24 swatch + QSpinBox (outline 0..10, glow radius 0..20, shadow offset 0..10), a disabled checkbox greys the row's swatch+spin. Add class-scope Signals following the `vertical_changed = Signal(bool)` shape: `style_font_changed = Signal(str)`, `style_font_style_changed = Signal(str)`, `style_size_changed = Signal(int)` (0 = Auto sentinel), `style_auto_fit_changed = Signal(bool)`, `style_color_changed = Signal(str)`, `style_align_changed = Signal(str, str)` (h, v), `style_effect_changed = Signal(str, dict)` (effect key + {enabled, color, value}). Extend `load_box` (:220-281) population with `blockSignals(True)` around every setValue/setChecked and record each `_loaded_*`; extend `clear()`/`_set_fields_enabled` (:283-317) so the styling controls join the empty-state gate; extend the empty-state label copy to "Select a text box to edit its text, translation, and style."; wire every control through the WR-01 no-op-guard pattern (:351-372 shape — an unchanged focus cycle never emits). Create `tests/test_gui_inspector_styling.py` (header mirrors test_gui_boxes.py: importorskip PySide6, `@pytest.mark.gui`, qtbot, real InspectorPanel + a scene with one box): `test_styling_section_present` (all widgets exist and populate from the box's style), `test_style_commit_signal_fires` (changing Font emits style_font_changed with the new family; the no-op guard suppresses re-emission on focus cycles), `test_auto_fit_toggles_size_spin` (checked → spin 0 + disabled; unchecked → spin enabled at the rendered size rounded), `test_empty_state_disables_styling` (no selection → styling controls disabled + extended copy).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_styling_section_present asserts the Font/Style/Size/Auto-fit/Color/Align/Align V/Outline/Glow/Shadow controls exist and their values match the box's TextStyle
    - test_style_commit_signal_fires asserts exactly one emission per real change and zero on a no-op focus cycle (WR-01)
    - test_auto_fit_toggles_size_spin asserts the spin 0/disabled ↔ enabled/rounded-size transition
    - test_empty_state_disables_styling asserts the styling controls follow `_set_fields_enabled` and the extended empty-state copy is present
    - The pre-existing inspector tests (text fields, bubble, vertical checkbox) stay green
  </acceptance_criteria>
  <done>Users see the full Style section in the Inspector with working per-box controls and no-op-safe commit signals — committed.</done>
</task>
<task type="auto">
  <name>Task 2: Mixed common-value state + apply-to-all commit routing (D-10, Pitfall 7)</name>
  <files>manga_ai_studio/gui/inspector_panel.py, manga_ai_studio/gui/main_window.py, tests/test_gui_inspector_styling.py</files>
  <read_first>
    - manga_ai_studio/gui/inspector_panel.py — the Task 1 styling widgets + `load_box`/`_loaded_*`/no-op guards, the class-scope signals, `_set_fields_enabled` :309-317 (the per-box-fields disable target), the multi-select hint-label insertion point above the form
    - manga_ai_studio/gui/main_window.py — `_inspector_commit_pre` :2794-2819 (before-snapshot + push-side payload detach), `_inspector_commit_post` :2821-2834 (overlay refresh + `boxes_modified.emit(snapshot)` + panel reload), `_on_boxes_modified` :2741-2776, the 06-WR-01 op-name recording (:2925-2935)
    - 07-PATTERNS.md — §`gui/inspector_panel.py` (Mixed presentation + the sentinel-never-leaves-the-widget-layer rule, RESEARCH Pitfall 7) and §`gui/main_window.py` change site 3 (the commit pair reused for ALL selected boxes)
    - 07-UI-SPEC.md — surface 33 Mixed presentation rows (combo "Mixed" entries, spin "Mixed" special text, split swatch, tri-state checkboxes, hint label) + the multi-select field-disabling row + surface 13 ("style change" op name, one snapshot per commit)
    - 07-RESEARCH.md — Pitfall 7 (Mixed state corrupting boxes on commit — the sentinel rule)
  </read_first>
  <action>In `gui/inspector_panel.py` add the D-10 common-value layer: when the panel is loaded for a MULTI-selection, compute the per-control value set across the selection — all equal → show the value; differing → the Mixed sentinel per widget (Font/Style combos get a non-editable "Mixed" entry; Size spin shows its sentinel value with `setSpecialValueText("Mixed")`; Color swatch paints the split fill — left `#e8e8ea` / right `#9a9aa2`, 1px `#3a3a42` border — per UI-SPEC §Color; Auto-fit/effect/vertical checkboxes become tri-state via `setTristate(True)` with indeterminate = mixed). The sentinel NEVER leaves the widget layer (RESEARCH Pitfall 7): a commit only fires when the user actively changes a control, and the emitted value is always a real value. Disable the per-box text fields (Bubble #, Origin, Recognized, Translation, Language) while the styling section stays enabled; show the muted hint label "Style edits apply to all {n} selected boxes." above the form (n = selection size). In `gui/main_window.py` route the new styling signals through the existing `_inspector_commit_pre`/`_inspector_commit_post` pair (:2794-2834) extended for style: capture ONE before-snapshot (`boxes_snapshot()` — full list with style forwarding), apply the commit to EVERY selected PageBox via `dataclasses.replace(box.style, **changes)` (fresh instance — NEVER in-place mutation), record the op name "style change" (the 06-WR-01 pattern), then `_inspector_commit_post` (one overlay refresh per selected item + ONE `boxes_modified.emit(before)` + panel reload). Extend tests/test_gui_inspector_styling.py: `test_mixed_state_presented` (two boxes with different colors → split swatch + Mixed combos/spin; uniform selection → real values, never Mixed), `test_style_commit_applies_to_all` (change color on a Mixed selection → BOTH boxes' styles update; exactly ONE BOXES entry; one Ctrl+Z restores both previous styles), `test_mixed_sentinel_never_persists` (after a commit on a Mixed selection, no TextStyle contains a sentinel value), `test_multi_text_fields_disabled` + `test_hint_label_count`.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_mixed_state_presented asserts the split swatch / Mixed entries / tri-state on differing values and real values on uniform selections
    - test_style_commit_applies_to_all asserts both boxes carry the new style, the history holds exactly one entry, and one undo restores both pre-edit styles (Pitfall 1 — the restored styles are detached)
    - test_mixed_sentinel_never_persists asserts no sentinel string in any TextStyle after commits (Pitfall 7)
    - test_multi_text_fields_disabled asserts the text-field controls are disabled at N>1 and enabled at N==1
    - test_hint_label_count asserts the hint shows the exact selection count
  </acceptance_criteria>
  <done>Multi-select styling works: Mixed until overridden, one override applies to all selected with ONE undoable snapshot — committed.</done>
</task>
<task type="auto">
  <name>Task 3: Live vertical checkbox + Size +/- actions + atomic vertical-flag wiring (D-13/D-16)</name>
  <files>manga_ai_studio/gui/box_item.py, manga_ai_studio/gui/inspector_panel.py, manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/text_renderer.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/inspector_panel.py — the vertical checkbox :199-203 (the "Coming soon" tooltip + metadata-only commit to replace), the `vertical_changed` signal :146
    - manga_ai_studio/gui/box_item.py — `refresh_text_overlay` (the 07-01 renderer-driven refresh — the vertical flag read lands here), `set_text_overlay_visible` :670-686
    - manga_ai_studio/gui/text_renderer.py — `bake_typeset_page` (the vertical flag expression flips here too), the 07-03 vertical layout/paint entry points
    - manga_ai_studio/gui/main_window.py — `_build_text_menu` (the Typesetting section joins after Load Translations…), `_refresh_action_states` :966-1112 (the size-action gates: page_open + selection + not _op_running — the :1067-1069 shape), the shortcut map (Ctrl+] / Ctrl+[ verified free by the UI-SPEC conflict audit — Pitfall 4), the 06-WR-01 op-name recording
    - 07-RESEARCH.md — Pitfall 9 (toggle must re-render the overlay — D-13), Pitfall 4 (shortcut collisions — Ctrl+- is taken; the UI-SPEC-locked bracket pair), Assumption A11 (size +/- converts Auto-fit to manual first)
    - 07-UI-SPEC.md — surface 33 vertical-checkbox row + surface 1 Text-menu Typesetting section + the shortcut table (Ctrl+] / Ctrl+[) + Copywriting rows (tooltips for the vertical checkbox and the size actions)
  </read_first>
  <action>Live vertical (D-13): in `inspector_panel.py` replace the vertical checkbox tooltip ("Coming soon…" → "Render this box's text vertically, top-to-bottom (tategaki)."); the commit path keeps writing `payload.vertical` via the existing `vertical_changed` routing AND now triggers the box's overlay refresh with the vertical flag (Pitfall 9 — a toggle must re-render, not just write metadata). In `box_item.py` `refresh_text_overlay`: compute `vertical = bool(pb.style.vertical or (pb.payload.vertical if pb.payload else False))` and call `renderer.layout(text, style, rect, vertical=vertical)` + `renderer.paint(...)` (the 07-03 tategaki path — upright CJK, RTL columns, rotated Latin); the inline editor (04-05 QGraphicsProxyWidget) stays horizontal — untouched (D-12). In `text_renderer.py` `bake_typeset_page`: use the SAME OR expression per box so canvas and bake flip atomically (no divergence window). Size actions (D-16): in `main_window.py` add `action_increase_font_size` ("Increase Font Size", `QKeySequence("Ctrl+]")`) and `action_decrease_font_size` ("Decrease Font Size", `QKeySequence("Ctrl+[")`) in a Text-menu Typesetting section after Load Translations… (zero-arg-lambda triggered wiring; both `setEnabled(False)` initially); the handler: for each selected box, if the box's style is Auto-fit (`font_size_px is None or auto_fit`), FIRST convert to manual at the current rendered size (A11 — renderer.layout's auto-fit result), then apply the ±1 px delta (floor at 1); capture ONE before-snapshot, apply via `dataclasses.replace` per box, record the op name "font size", emit `boxes_modified` ONCE + one overlay refresh; add both actions to `_refresh_action_states` gated on `page_open and box_selected and not _op_running`. Extend tests/test_gui_boxes.py: `test_vertical_checkbox_live` (toggle → the overlay layout mode flips — assert via the renderer's mode or a cached-mode marker; the "Coming soon" tooltip is gone; bake_typeset_page renders the box vertically too), `test_size_plus_minus_actions` (Ctrl+] / Ctrl+[ fire exactly their own handlers — single-binding, no Ambiguous-shortcut overload; ±1 px applied to all selected in ONE snapshot), `test_size_action_converts_auto_fit` (an Auto-fit box converts to manual at its rendered size, then applies the delta per A11), `test_vertical_preflagged_box_renders_vertical` (a box with payload.vertical=True renders through the vertical path without any inspector interaction — D-13 CTD-pre-flag).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py tests/test_gui_inspector_styling.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_vertical_checkbox_live asserts the overlay's layout mode flips after toggling (and back), the tooltip holds the new copy, and the bake renders the same vertical layout
    - test_size_plus_minus_actions asserts each shortcut triggers exactly one action (grep the shortcut map: each sequence bound once) and the delta applies to every selected box with exactly one BOXES entry
    - test_size_action_converts_auto_fit asserts the converted manual size equals the rendered auto-fit size ±1 (A11)
    - test_vertical_preflagged_box_renders_vertical asserts a payload.vertical box uses the vertical path with default style (D-13)
    - The inline editor still opens horizontally (D-12 — no changes to the editor path)
  </acceptance_criteria>
  <done>Users can flip any box (or selection) to true tategaki with the checkbox and nudge font sizes with Ctrl+] / Ctrl+[ — every commit one undoable snapshot; committed.</done>
</task>

</tasks>
## Artifacts this phase produces

- `manga_ai_studio/gui/inspector_panel.py` (MOD): Style section (header + divider + Font QFontComboBox / Style QComboBox / Size QSpinBox 0..200 "Auto" / Auto-fit QCheckBox / Color 24×24 swatch QToolButton / Align + Align V QComboBoxes / Outline-Glow-Shadow rows with enable+swatch+spin); signals `style_font_changed(str)`, `style_font_style_changed(str)`, `style_size_changed(int)`, `style_auto_fit_changed(bool)`, `style_color_changed(str)`, `style_align_changed(str, str)`, `style_effect_changed(str, dict)`; D-10 common-value/Mixed layer (Mixed combo entries, "Mixed" spin special text, split swatch, tri-state checkboxes, sentinel-never-persists); multi-select hint label; multi-select field disabling; extended empty-state copy; live vertical checkbox (tooltip replaced, commit triggers overlay refresh)
- `manga_ai_studio/gui/main_window.py` (MOD): style-commit routing through `_inspector_commit_pre`/`_inspector_commit_post` applied to ALL selected (one snapshot, op name "style change"); `action_increase_font_size` (Ctrl+]) / `action_decrease_font_size` (Ctrl+[) + handler (Auto-fit → manual conversion per A11, ±1 px, one snapshot, op name "font size"); `_refresh_action_states` gates for both
- `manga_ai_studio/gui/box_item.py` (MOD): `refresh_text_overlay` vertical flag = `pb.style.vertical or payload.vertical` → vertical layout/paint path
- `manga_ai_studio/gui/text_renderer.py` (MOD): `bake_typeset_page` uses the same OR expression (atomic canvas ≡ bake vertical flip)
- `tests/test_gui_inspector_styling.py` (NEW): test_styling_section_present, test_style_commit_signal_fires, test_auto_fit_toggles_size_spin, test_empty_state_disables_styling, test_mixed_state_presented, test_style_commit_applies_to_all, test_mixed_sentinel_never_persists, test_multi_text_fields_disabled, test_hint_label_count
- `tests/test_gui_boxes.py` (extended): test_vertical_checkbox_live, test_size_plus_minus_actions, test_size_action_converts_auto_fit, test_vertical_preflagged_box_renders_vertical

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inspector styling widgets → PageBox.style | Widget values (incl. a "Mixed" sentinel) cross into the model on commit; a sentinel write would corrupt box data |
| Shortcut actions → handlers | Ctrl+] / Ctrl+[ bindings must not collide with the locked shortcut map |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Tampering | Inspector Mixed commit path | medium | mitigate | The Mixed sentinel never leaves the widget layer (RESEARCH Pitfall 7) — commits emit real values only, applied via dataclasses.replace (never in-place); test_mixed_sentinel_never_persists locks it |
| T-07-12 | Spoofing (UI) | Ctrl+] / Ctrl+[ / Ctrl+Shift+B bindings | low | mitigate | UI-SPEC conflict audit verified the bracket pair + Ctrl+Shift+B free (Ctrl+- is taken by zoom-out — Pitfall 4); the size-action test asserts one handler per sequence (the CR-14 single-binding discipline) |
| T-07-13 | DoS | per-mousemove style/effect re-render | medium | mitigate | Layout + effect caches keyed (text, style, rect, vertical); reposition stays setPos-only (RC-1) — test_group_move_no_relayout (plan 07-02) plus the effects backstop gate |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py tests/test_gui_boxes.py -q`
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (600-test baseline green)
- Grep gates: `rg "Coming soon" manga_ai_studio/gui/inspector_panel.py` → nothing (the D-13 tooltip is gone); each of Ctrl+], Ctrl+[, Ctrl+Shift+B appears exactly once in the main_window shortcut map
- End-of-phase human gate (config human_verify_mode=end-of-phase): Inspector styling session with a real font + color; tategaki + effects visual backstops (the 07-03 backstop rows); bake WYSIWYG at 100%
</verification>

<success_criteria>
- Full styling control surface live: font/style/size/Auto-fit/color/alignments/effects per box and per selection
- Mixed common-value behavior correct (presentation + apply-to-all + sentinel never persists)
- Vertical checkbox live with atomic canvas≡bake flip; inline editor stays horizontal
- Size +/- actions work on selections with Auto-fit conversion; no shortcut collisions
- Every style commit = ONE undoable snapshot with the "style change"/"font size" op-name flash
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-05-SUMMARY.md` when done
</output>




