---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 10
type: execute
wave: 4
depends_on: [07-07]
files_modified:
  - manga_ai_studio/gui/text_renderer.py
  - tests/test_core/test_typeset_layout.py
autonomous: true
gap_closure: true
gap_ids: [G-07-4]
requirements:
  - TRAN-02
assumption_delta:
  decision: no-change
  rationale: "G-07-4 changes the Auto-fit contract per the user report ('Autofit should make the text big enough to fit not just tiny text in a massive box'): the [10,28] base clamp stops being an unintended hard max — a grow phase is added while text fits, capped at min(inner_w, inner_h), keeping the 5 px floor and the 12-iteration bound. The affected tests (test_auto_fit_uses_box_adaptive_base, test_vertical_auto_fit) are UPDATED to the new contract; shrink tests stay green. Manual-size rendering (D-15) is untouched."
estimate:
  tokens: 16500
  raw_tokens: 11000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Auto-fit grows short text to fill a large box — the rendered size can exceed the old 28 px clamp max, bounded by the per-box growth cap min(inner_w, inner_h)"
    - "Auto-fit still shrinks text that does not fit at the base — the shrink path, the 5 px floor, and the 12-iteration bound behave exactly as before (all shrink tests stay green)"
    - "The vertical Auto-fit loop grows under the same cap/iteration rules (column-count + vertical-extent checks unchanged); a long text still floors at 5 px with overflow reported"
  artifacts:
    - manga_ai_studio/gui/text_renderer.py :: the horizontal auto-fit loop in layout (:536-556) — grow-while-fits phase with cap min(inner_w, inner_h) + a _OVERLAY_FIT_GROW_STEP constant; _vertical_fit_size (:463-486) — the same grow phase on the vertical fit checks
    - tests/test_core/test_typeset_layout.py :: test_auto_fit_uses_box_adaptive_base updated (growth above the old clamp, cap respected); test_vertical_auto_fit updated (growth above the base); test_auto_fit_grows_short_text_to_fit (new, horizontal); test_auto_fit_shrinks_wrapped_text_to_fit / floor / budget tests unchanged and green
  key_links:
    - "the grow loop's fit check <-> the cap: growth stops at the cap OR the first non-fitting candidate (the last fitting candidate is kept — never a shrink fall-through)"
    - "layout auto-fit branch <-> _vertical_fit_size: both loops share the grow-cap semantics so horizontal and vertical boxes grow consistently"
  prohibitions:
    - "The 5 px floor and the 12-iteration bound must be preserved in both loops (floor checked at the loop TOP)"
    - "A text that never fits at the base must follow the EXACT old shrink path (shrink tests must pass byte-identical assertions)"
    - "Manual-size rendering (font_size_px set, auto_fit False) is untouched — growth applies to Auto-fit only"
---

<objective>
Close **G-07-4 (minor)** from 07-UAT.md: "Autofit should make the text big enough to fit not just tiny text in a massive box". Root cause (already diagnosed): the Auto-fit loops are shrink-only — base = 14·min(w,h)/100 clamped [10,28], the loop only multiplies down by 0.9 (text_renderer.py:536-556 horizontal; 463-486 vertical). The [10,28] base clamp acts as an unintended hard max — large boxes never grow text. Fix: add a grow phase (multiply up while the text fits, capped at min(inner_w, inner_h)), keep the 5 px floor and the iteration bound.

Purpose: Auto-fit fills the box — big enough to be legible, never overflowing — with the shrink behavior preserved for text that cannot fit.

Output: the grow-to-fit phase in both auto-fit loops and the re-asserted fit tests.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UAT.md
@manga_ai_studio/gui/text_renderer.py
@tests/test_core/test_typeset_layout.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN — horizontal Auto-fit grows short text to fill the box (cap min(inner_w, inner_h))</name>
  <files>manga_ai_studio/gui/text_renderer.py, tests/test_core/test_typeset_layout.py</files>
  <read_first>
    - manga_ai_studio/gui/text_renderer.py — the horizontal auto-fit loop in layout (:536-556: base at :538-543, the shrink-only loop at :547-556), the fit constants (find _OVERLAY_FIT_MAX_ITERS / _OVERLAY_FIT_STEP / _OVERLAY_FIT_FLOOR_PX / _OVERLAY_BOX_REF_DIM / _BASE_CLAMP_MIN / _BASE_CLAMP_MAX / _OVERLAY_FONT_BASE), _build_document's signature
    - tests/test_core/test_typeset_layout.py — test_auto_fit_uses_box_adaptive_base (:119-129: the 200x100 -> 14.0, 300x300 -> 28.0, 100x40 -> 10.0 assertions — the first two LOCK the clamp-as-max behavior and must be updated), test_auto_fit_shrinks_wrapped_text_to_fit (:133-142), test_auto_fit_floor_terminates_for_huge_text (:151-164), test_auto_fit_fits_long_text_within_loop_budget (:168-176), the _inner helper
  </read_first>
  <behavior>
    - Test 1 (RED gate): short text ('hello') in a 300x300 box renders ABOVE the old clamp max — `used_font_size_px > 28.0` — and never above the cap `min(inner_w, inner_h)` with overflow False. On current code this FAILS (the size is clamped at 28 — the hard max).
    - Test 2 (RED gate): the 200x100 'hello' case grows above the 14 px base (range assertion `14.0 < used <= min(inner_w, inner_h)`), and the 100x40 case grows above the 10 px floor (range assertion, cap = min(96, 36) = 36).
    - Test 3 (unchanged): the wrapped-text shrink case (40 words) still renders below 14 (shrink path — the base does not fit), the huge-text floor case still floors at 5 with overflow True, the wide-box budget case still fits with overflow False.
  </behavior>
  <action>RED: update `test_auto_fit_uses_box_adaptive_base` per the behavior block — replace the exact `== 28.0` and `== 14.0` assertions with range-based growth assertions (the project's platform-robust convention: no magic exact sizes for grown text; assert `used > <old value>` and `used <= min(inner_w, inner_h)` and overflow False); add `test_auto_fit_grows_short_text_to_fit` for the 300x300 case. Run — must fail at the growth assertions (RED). GREEN in text_renderer.py: add `_OVERLAY_FIT_GROW_STEP = 1.1` next to the other fit constants; restructure the horizontal auto-fit branch (:536-556) into grow-while-fits-with-cap + the unchanged shrink path: start at the clamped base; each iteration builds the candidate at target; if the candidate's height fits inner_h — record it as doc/size with overflow False, then if target < cap (`grow_cap = min(inner_w, inner_h)`) advance `target = min(grow_cap, target * _OVERLAY_FIT_GROW_STEP)` and continue (break when the cap is reached); if the candidate does NOT fit — if a fitting candidate was already recorded, break (keep the last fit); otherwise record the candidate (doc/size), set overflow True, and multiply target down by _OVERLAY_FIT_STEP (the old shrink path, byte-equivalent behavior). The floor check stays at the loop TOP. Run the horizontal fit tests — must pass, including the shrink/floor/budget tests UNCHANGED.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_layout.py -k "auto_fit" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_auto_fit_grows_short_text_to_fit + the updated test_auto_fit_uses_box_adaptive_base FAIL pre-fix (clamp-as-max) and PASS post-fix (growth > 28 / > base, bounded by the cap, overflow False)
    - test_auto_fit_shrinks_wrapped_text_to_fit, test_auto_fit_floor_terminates_for_huge_text, test_auto_fit_fits_long_text_within_loop_budget pass UNCHANGED (the shrink path is behavior-identical)
    - Grep gate: `grep -c '_OVERLAY_FIT_GROW_STEP' manga_ai_studio/gui/text_renderer.py` >= 2 (definition + the growth advance)
  </acceptance_criteria>
  <done>Horizontal Auto-fit grows short text beyond the old clamp, bounded by min(inner_w, inner_h); the shrink/floor/budget behavior is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-GREEN — vertical Auto-fit grows under the same cap/iteration rules</name>
  <files>manga_ai_studio/gui/text_renderer.py, tests/test_core/test_typeset_layout.py</files>
  <read_first>
    - manga_ai_studio/gui/text_renderer.py — _vertical_fit_size (:463-486: base at :474-477, the shrink-only loop at :478-485, the fit check `ncols <= max_cols and block_h <= inner_h + _EPS` at :483), _vertical_placements' return shape (ncols, block_w, block_h)
    - tests/test_core/test_typeset_layout.py — test_vertical_auto_fit (:369-387: 5 CJK chars in 200x100 -> 14.0 — LOCKED to the clamp; 2000 chars in 200x60 -> floor 5 + overflow True)
  </read_first>
  <behavior>
    - Test 1 (RED gate): 5 CJK chars in a 200x100 box render ABOVE the 14 px base — range assertion `14.0 < used <= min(inner_w, inner_h)` with overflow False (the vertical fit checks: column count <= floor(inner_w / size) and block_h <= inner_h — the growth stops when 5 x size exceeds inner_h). On current code this FAILS (== 14.0).
    - Test 2 (unchanged): the 2000-char case still floors at 5 px with overflow True (the base never fits → the shrink path is exact).
  </behavior>
  <action>RED: update `test_vertical_auto_fit` per the behavior block — replace the `== 14.0` assertion with the range-based growth assertion (platform-robust convention); the 2000-char assertions stay. Run — must fail at the growth assertion (RED). GREEN in text_renderer.py: restructure `_vertical_fit_size` (:463-486) with the same grow-while-fits-with-cap shape as Task 1 — cap `min(inner_w, inner_h)`, growth step `_OVERLAY_FIT_GROW_STEP`, fit check unchanged (`ncols <= max_cols and block_h <= inner_h + _EPS`), the floor check at the loop TOP, and the shrink path byte-equivalent when the base never fits. Run the vertical fit tests — must pass. Then run the full vertical layout file and the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_layout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_vertical_auto_fit FAILS pre-fix at the growth assertion and PASSES post-fix (5-char case grows above 14, bounded by the cap; 2000-char case floors at 5 with overflow True)
    - All other vertical layout tests (RTL flow, wrap, centering, classification, code points) pass UNCHANGED
    - Grep gate (both loops grow): `grep -c 'grow_cap' manga_ai_studio/gui/text_renderer.py` >= 2 (the horizontal branch + _vertical_fit_size)
    - Full suite green at the re-baselined count (687 baseline from 07-VERIFICATION.md + 1 new test = 694 passed, 0 failed)
  </acceptance_criteria>
  <done>Vertical Auto-fit grows under the same cap/iteration rules; the floor case is unchanged; the full suite is green.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/text_renderer.py`:
  - `_OVERLAY_FIT_GROW_STEP = 1.1` — the growth multiplier (next to the existing fit constants)
  - `layout`'s horizontal auto-fit branch (:536-556) — grow-while-fits-with-cap (`grow_cap = min(inner_w, inner_h)`), last-fitting-candidate retention, shrink path byte-equivalent when the base never fits
  - `_vertical_fit_size` (:463-486) — the same grow phase on the vertical fit checks
- `tests/test_core/test_typeset_layout.py` (updated/new):
  - `test_auto_fit_uses_box_adaptive_base` — range-based growth assertions (the clamp-as-max lock removed)
  - `test_auto_fit_grows_short_text_to_fit` (new)
  - `test_vertical_auto_fit` — range-based growth assertion
  - shrink/floor/budget tests unchanged

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auto-fit loop → rendered size | The loop's output font size feeds both canvas and bake; an unbounded growth path would overflow the box or blow up layout cost (the cap + iteration bound are the bounds) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-16 | DoS | layout auto-fit grow phase | medium | mitigate | Growth capped at min(inner_w, inner_h) AND bounded by the 12-iteration budget — no unbounded size path (a crafted huge box cannot allocate a runaway document); the floor check stays at the loop top (ASVS V7 resource bounds) |
| T-07-17 | Spoofing | fit-loop overflow contract | medium | mitigate | The last-FITTING candidate is always retained on growth overshoot; a text that never fits at the base follows the exact old shrink path (overflow True + floor), so the canvas/bake overflow rendering contract (UI-SPEC A6, unclipped) is unchanged |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_layout.py -x -q` — the fit tests green (RED gates proven RED→GREEN)
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 694 passed, 0 failed (687 baseline from 07-VERIFICATION.md + 1 new)
- Grep gates: `_OVERLAY_FIT_GROW_STEP` x2+, `grow_cap` x2+ in text_renderer.py
</verification>

<success_criteria>
- Auto-fit makes text big enough to fill the box (growth beyond the old clamp, capped), while shrink/floor behavior is byte-identical for text that cannot fit
- Vertical Auto-fit grows under the same rules; full suite green at 694
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-10-SUMMARY.md` when done
</output>
