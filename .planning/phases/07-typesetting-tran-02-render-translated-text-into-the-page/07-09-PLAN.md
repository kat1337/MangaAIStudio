---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 09
type: execute
wave: 3
depends_on: [07-07]
files_modified:
  - manga_ai_studio/gui/inspector_panel.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_inspector_styling.py
autonomous: true
gap_closure: true
gap_ids: [G-07-7]
requirements:
  - TRAN-02
assumption_delta:
  decision: no-change
  rationale: "G-07-7 mirrors the WR-02 effect-row model that already works (inspector_panel.py:1210-1242 _effect_payload None-sentinel + main_window.py:3033-3057 _replace_effect per-key preservation). The plan reuses that proven pattern for the align combos; no new assumptions about the style model."
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "In a multi-select with differing aligns, the align combos show a leading 'Mixed' entry AND keep the real options (Top/Middle/Bottom, Left/Center/Right) selectable — the user can override Mixed"
    - "Picking a value in one align combo commits ONLY that axis: the other axis per box is preserved (a Mixed align_v box set to Bottom keeps each box's own align_h)"
    - "The Mixed sentinel maps to None on commit and never persists into any TextStyle; an unchanged focus cycle on a Mixed combo is still a WR-01 no-op (no spurious BOXES snapshot)"
  artifacts:
    - manga_ai_studio/gui/inspector_panel.py :: load_multi_selection align loads (:671-688) — `["Mixed", ...real items]` with Mixed selected when differing; _emit_style_align_if_changed (:1163-1169) — Mixed→None translation, per-axis change detection; style_align_changed signal type relaxed to object-typed for None payloads
    - manga_ai_studio/gui/main_window.py :: _replace_align (per-axis, None = untouched — the _replace_effect mirror) + _on_inspector_style_align_committed (:3129-3132) routed through it
    - tests/test_gui_inspector_styling.py :: test_mixed_align_override_commits_per_axis (the Mixed→override commit: real items present, per-axis preservation, one BOXES snapshot)
  key_links:
    - "load_multi_selection combo items <-> _emit_style_align_if_changed: the leading-Mixed presentation and the Mixed→None mapping are one contract — the sentinel is presentation-only and becomes None the moment it crosses to a commit"
    - "_emit_style_align_if_changed (Mixed→None, per-axis) <-> _on_inspector_style_align_committed (_replace_align per-axis): the None axes are preserved per box at the consumer, mirroring _replace_effect's enabled-None handling"
  prohibitions:
    - "The 'Mixed' sentinel must never reach a TextStyle (it is translated to None at the commit boundary)"
    - "A commit must never overwrite the untouched axis on any box"
    - "The combos must keep the real items selectable while Mixed is shown — the old items-replaced-with-['Mixed'] behavior is gone"
---

<objective>
Close **G-07-7 (major)** from 07-UAT.md: "align V doesn't allow me to override, it just says mixed". Three stacked defects (already diagnosed): (1) `load_multi_selection` replaces the combo items with only `['Mixed']` (inspector_panel.py:686-688 via `_select_combo` :870-877) — no Bottom/Middle/Top to pick; (2) `_emit_style_align_if_changed` (:1163-1169) returns if EITHER axis is 'Mixed' — a real change on one combo is discarded; (3) the consumer `_on_inspector_style_align_committed` (main_window.py:3129-3132) overwrites BOTH axes on every box. Fix: mirror the WR-02 effect pattern — keep real items + a leading "Mixed" entry, map "Mixed"→None on commit, per-axis `_replace_align` preserving untouched axes.

Purpose: make Mixed align state overridable per axis, with one BOXES snapshot per commit and the sentinel never persisting.

Output: the item-preserving Mixed presentation, the sentinel→None commit mapping, and the per-axis `_replace_align` consumer.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UAT.md
@manga_ai_studio/gui/inspector_panel.py
@manga_ai_studio/gui/main_window.py
@tests/test_gui_inspector_styling.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN — item-preserving Mixed align combos + sentinel→None commit mapping</name>
  <files>manga_ai_studio/gui/inspector_panel.py, tests/test_gui_inspector_styling.py</files>
  <read_first>
    - manga_ai_studio/gui/inspector_panel.py — load_multi_selection align loads (:668-688: `_select_combo(self.align_v_combo, ["Mixed"], "Mixed")` at :687, the align_h twin at :678), _select_combo (:870-877: clear + addItems + select), _emit_style_align_if_changed (:1163-1169: the either-axis-Mixed return at :1166-1167), the style_align_changed signal declaration (:282 — Signal(str, str)), the align combo connections (:1055-1058), the WR-02 _effect_payload None-sentinel model (:1210-1242 — the pattern to mirror), the _ALIGN_H_TO_MODEL/_ALIGN_V_TO_MODEL maps
    - tests/test_gui_inspector_styling.py — the connect_commit_handlers driver helper (:54-68 area, the on_style_align capture), test_mixed_state_presented (:328-370, the D-10 sentinel test that must stay green), the style-commit-to-window tests (:398+, _window_with_box helpers)
  </read_first>
  <behavior>
    - Test 1 (RED gate): `load_multi_selection` over two boxes with differing align_v shows the 'Mixed' entry AND keeps the real items — `align_v_combo` item texts == ['Mixed', 'Top', 'Middle', 'Bottom'] with 'Mixed' current; the uniform case shows the real value with NO Mixed entry (['Top', 'Middle', 'Bottom']). On current code the differing case FAILS (the combo holds only ['Mixed']).
    - Test 2 (RED gate): with the Mixed align_v selection, picking 'Bottom' in align_v while align_h stays 'Mixed' fires `on_style_align` with `(None, 'bottom')` — the untouched axis arrives as None, the changed axis as the model value; an unchanged Mixed cycle fires nothing (WR-01).
  </behavior>
  <action>RED: update/extend tests/test_gui_inspector_styling.py per the behavior block — add `test_mixed_align_keeps_real_items_and_maps_sentinel` driving the panel through the connect_commit_handlers driver. Run — must fail (combo items / no-op / mapping). GREEN in inspector_panel.py: (1) load_multi_selection — build `items = ["Mixed"] + real_display_items` for each axis (e.g. `["Mixed", "Left", "Center", "Right"]` / `["Mixed", "Top", "Middle", "Bottom"]`), select 'Mixed' when the axis differs and the real display value when uniform (the uniform case keeps the plain item list — no Mixed entry); (2) `_emit_style_align_if_changed` — translate each axis independently: `h_model = None if h == "Mixed" else _ALIGN_H_TO_MODEL[h]` (same for v), fire `on_style_align(h_model, v_model)` only when at least one axis differs from its loaded display value — the WR-01 no-op guard must be per-axis now, and a Mixed-untouched axis never suppresses the changed axis anymore; (3) relax the `style_align_changed` declaration to `Signal(object, object)` so the None payloads pass through the typed signal (the class-scope-signal tests assert presence only — check tests/test_gui_inspector_styling.py's signal test and the panel's has_class_scope_signals assertions stay green). Run — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_mixed_align_keeps_real_items_and_maps_sentinel FAILS pre-fix (combo holds only ['Mixed']; the change is swallowed) and PASSES post-fix
    - The uniform case shows the real value without a Mixed entry; test_mixed_state_presented passes UNCHANGED
    - The Mixed→None mapping reaches the consumer: (None, 'bottom') for a changed vertical axis with untouched horizontal axis
    - An unchanged Mixed focus cycle emits nothing (WR-01 per-axis no-op)
    - Grep gate (leading-Mixed item lists): `grep -c '"Mixed", "Left"' manga_ai_studio/gui/inspector_panel.py` == 1 and `grep -c '"Mixed", "Top"' manga_ai_studio/gui/inspector_panel.py` == 1
  </acceptance_criteria>
  <done>The align combos present Mixed alongside the real options; the commit maps Mixed→None per axis and fires on real changes only.</done>
</task>

<task type="auto">
  <name>Task 2: per-axis _replace_align consumer + the Mixed-override commit test (one snapshot)</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_inspector_styling.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py — _on_inspector_style_align_committed (:3129-3132: the both-axes `_replace_style(item.pagebox, align_h=h, align_v=v)`), _replace_effect (:3033-3057: the per-key preservation model — `dict(getattr(style, key))` + targeted changes + dataclasses.replace), _replace_style (:3028-3031), _inspector_style_commit (:3001+: ONE snapshot + ONE emit per commit)
    - tests/test_gui_inspector_styling.py — the window-level style-commit tests (test_style_commit_applies_to_all :398+, the boxes_modified ONE-emit assertions), the align commit driver
  </read_first>
  <action>Add `_replace_align(self, pb, *, align_h=None, align_v=None)` in main_window.py next to `_replace_effect`: build the changes dict from the non-None axes only (`if align_h is not None: changes["align_h"] = align_h`, same for v) and delegate to `_replace_style(pb, **changes)` — a both-None call is a no-op (defensive; the panel's WR-01 guard already prevents it). Rewire `_on_inspector_style_align_committed` to `self._inspector_style_commit(lambda item: self._replace_align(item.pagebox, align_h=h, align_v=v))`. Then add `test_mixed_align_override_commits_per_axis` at the window level: seed two boxes with differing align_v (and differing align_h), select both, drive the panel commit for align_v='Bottom' with align_h untouched (Mixed) — assert BOTH boxes end with align_v 'bottom', EACH box keeps its OWN align_h, exactly ONE `boxes_modified` emission (the existing one-snapshot assertion pattern), and one Ctrl+Z restores both boxes' pre-commit aligns (the snapshot carries the detached styles). Also assert no TextStyle anywhere holds the string 'Mixed'. Run the affected modules, then the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py tests/test_gui_boxes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_mixed_align_override_commits_per_axis passes: per-axis preservation (each box keeps its own untouched axis), ONE boxes_modified emit, one Ctrl+Z restores both boxes
    - _replace_align with a single non-None axis never touches the other axis on any box
    - No 'Mixed' string lands in any TextStyle (the sentinel→None mapping is airtight at the commit boundary)
    - Grep gate: `grep -c 'def _replace_align' manga_ai_studio/gui/main_window.py` == 1
    - Full suite green at the re-baselined count (687 baseline from 07-VERIFICATION.md + ~2 new tests = 693 passed, 0 failed)
  </acceptance_criteria>
  <done>Mixed align overrides commit per axis with one snapshot; untouched axes are preserved per box; the sentinel never persists.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/inspector_panel.py`:
  - `load_multi_selection` — align combos populated as `["Mixed", <real items>]` (Mixed current when differing; plain real items when uniform)
  - `_emit_style_align_if_changed` — per-axis Mixed→None translation + per-axis WR-01 no-op guard
  - `style_align_changed` — relaxed to `Signal(object, object)` for None payloads
- `manga_ai_studio/gui/main_window.py`:
  - `_replace_align(pb, *, align_h=None, align_v=None)` — per-axis style replacement (None = untouched; the `_replace_effect` mirror)
  - `_on_inspector_style_align_committed` — routed through `_replace_align`
- `tests/test_gui_inspector_styling.py` (extended): `test_mixed_align_keeps_real_items_and_maps_sentinel` + `test_mixed_align_override_commits_per_axis`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inspector Mixed sentinel → style model | The "Mixed" presentation sentinel crosses the commit boundary; if it leaks into a TextStyle it corrupts alignment state (Pitfall 7) |
| commit payload → selected boxes | The per-axis commit payload crosses to every selected box; an overwrite of the untouched axis would clobber per-box values on a Mixed selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-14 | Tampering | _emit_style_align_if_changed sentinel translation | high | mitigate | Mixed→None at the commit boundary; the consumer `_replace_align` skips None axes; the regression asserts no 'Mixed' string reaches any TextStyle (Pitfall 7 — the WR-02 effect pattern already proven at :1210-1242) |
| T-07-15 | Tampering | _on_inspector_style_align_committed both-axes overwrite | high | mitigate | Per-axis `_replace_align` preserves the untouched axis per box (the old both-axes `_replace_style(align_h=h, align_v=v)` is replaced); the Mixed-override test asserts per-box preservation + ONE boxes_modified emission + one-snapshot undo |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py tests/test_gui_boxes.py -x -q` — affected modules green (RED gate proven RED→GREEN)
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 693 passed, 0 failed (687 baseline from 07-VERIFICATION.md + ~2 new)
- Grep gates: leading-Mixed item lists x1 per axis in inspector_panel.py; `def _replace_align` x1 in main_window.py
</verification>

<success_criteria>
- A Mixed align state is overridable: the real options stay selectable under the leading Mixed entry
- Commits apply the changed axis only, preserving each box's untouched axis; one Ctrl+Z reverses the whole commit
- The sentinel never persists; full suite green at 693
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-09-SUMMARY.md` when done
</output>
