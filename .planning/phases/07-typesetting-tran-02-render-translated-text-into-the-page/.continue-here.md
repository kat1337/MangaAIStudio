---
context: phase
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
task: 0
total_tasks: 0
status: complete
last_updated: 2026-08-12T05:36:26.280Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **QPainterPath.addText / raw QTextLayout glyph paths CRASH this PySide6 6.10.1 + Python 3.14.2 stack** (native 0xC0000409, pure-virtual abort) — any future plan touching text rendering MUST use the QTextDocument + `setTextOutline` path (07-01/07-03 deviations). Do not reintroduce glyph-path painting.
- [ ] CONSTRAINT: **Ctrl+Z overlay teardown UAF — the graveyard is MANDATORY** — every BoxItem removal site MUST route through `_retire_boxes` (canvas.py); never drop the last Python wrapper ref mid-event-loop while scene updates are pending (the 0xC0000409 mechanism fixed in 07-06). Keep the `Shiboken.isValid` guards.
- [ ] CONSTRAINT: **D-11 render contract is USER-OVERRIDDEN** — Roman text renders UPRIGHT stacked in vertical mode (never rotated 90°); rendering is horizontal-by-default for ALL boxes; the render flag is `bool(style.vertical)` ONLY; `payload.vertical` is pure export metadata. Do not "fix" this back to the W3C convention.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Glyph-path text painting | `QPainterPath.addText`/QTextLayout glyph paths abort the interpreter (0xC0000409) | blocking | QTextDocument + setTextOutline only; probe mechanisms on the pinned interpreter before committing to them |
| Mid-event-loop item deletion | Dropping the last Python ref of a QGraphicsItem while pending scene updates reference it → freed-object paint → native crash | blocking | `_retire_boxes` graveyard + `QTimer.singleShot(0)` release + `Shiboken.isValid` guards; regression tests must drop refs before undo |
| W3C vertical convention applied to user contract | The research/UI-SPEC locked rotated-Latin (Japanese convention); the user overrode it in UAT | blocking | render flag = `bool(style.vertical)` only; `_ASCII_UPRIGHT` for Latin/digits; horizontal-by-default tests |
| Empty subagent returns | gsd-executor/gsd-researcher sometimes return empty with ZERO work product | advisory | Check disk (SUMMARY.md/commits) first; if nothing exists, RESUME the same session with a "continue" prompt (07-03/07-05 pattern) before re-spawning fresh |
| gsd commit tool vs dirty tree | `gsd_run query commit` fails when unrelated files are dirty (dumps status) | advisory | Use plain `git add <explicit paths> && git commit -m ...`; never `git add -A` (would sweep the user's 01-RESEARCH.md change) |
| LF→CRLF status noise | Tooling touches mark files "modified" with ZERO textual diff | advisory | Verify with `git diff --numstat` before `git checkout --`; restore only zero-diff files |
| Stray QObjects in production code | The cancelled session's parentless `QAction("Bisect")` probe corrupted the C++ heap (teardown UAF trigger) | advisory | Never leave diagnostic probes/widgets in production code; remove parentless QObjects before commit |
| Suite-count claims drift | Plans anchored counts at the stale 600 baseline; executors correctly re-baselined (714→717) | advisory | Treat the live `pytest -q` count as ground truth; plan counts are approximate |

## Current State

Phase 7 (Typesetting, TRAN-02) is **COMPLETE**: 12/12 plans executed (5 main + 7 gap-closure), code review clean (both rounds), re-verification **passed** (44/46 — the 2 unverified are held-out human visual gates), 717 tests green, ROADMAP/STATE/PROJECT.md updated, phase.complete recorded. This is the **last phase** of milestone v1.1 (`next_phase: null`).

Remaining work is user-facing only:
1. `/gsd-verify-work 7` — run the 2 remaining visual UAT gates + prohibition sign-off; reconciles the 7 UAT gaps (G-07-1..G-07-7) to `resolved` (their fix plans executed).
2. Optional: `/gsd-secure-phase 7` (security enforcement active, ASVS L1 — no SECURITY.md yet), `/gsd-ui-review 7`.
3. Milestone closeout: `/gsd-complete-milestone` (v1.1).

## Required Reading (in order)

1. `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UAT.md` — the 7 gaps + diagnoses (G-07-1..G-07-7, all fixed); tests 1-2 are the pending visual gates
2. `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-VERIFICATION.md` — final verification (status: passed; 44/46)
3. `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-CONTEXT.md` — locked decisions D-01..D-16 + the user's D-11 override
4. `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-RESEARCH.md` — Patterns 1-5, Pitfalls 1-9 (esp. Pitfall 5 effect bounds, Pitfall 8 copy-detach)
5. `.planning/debug/ctrl-z-undo-crash.md` — the UAF analysis the 07-06 fix implements
6. `.planning/WINDOWS.md` — Windows/PySide6 stack deviation ledger (appended by executors)

## Infrastructure State

- Python: pinned interpreter `C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe` (AGENTS.md) — never bare `python`
- Stack: PySide6 6.10.1 / PIL 12.0.0 / numpy 2.3.5 / torch loaded (app heap is part of the UAF trigger conditions)
- Git: branch `master`; worktree isolation disabled (`use_worktrees: false`) — sequential execution
- No servers/watchers/async jobs running
- Dirty tree: `.planning/phases/01-cleaning-workspace/01-RESEARCH.md` (USER's pre-existing change — do NOT touch); untracked pre-existing artifacts (AGENTS.md, start.bat, .python-version, .planning/ui-reviews/, research/.cache/, .claude/MCP cache, phase .gitkeep/PATTERNS stragglers)

## Decisions Made

- **OCR_JSON_VERSION → "2"** (user, one-way-door checkpoint) — D-07 style block; consumers can branch on version
- **D-11 override** (user) — upright-stacked Roman in vertical mode; horizontal-by-default; `style.vertical` only
- **G-07-2 stopgap** (user) — contains-search filter on the font dropdown
- **G-07-6 fix** — graveyard + `Shiboken.isValid` guards (delayed C++ deletion past the pending-update flush)
- **G-07-3 fix** — `default_style()` Qt-free factory + QSettings `defaultFontFamily` + Set-as-Default button; no-key boxes keep `style None`
- **G-07-4 fix** — grow-to-fit with cap `min(inner_w, inner_h)`; shrink path byte-identical

## Blockers

- None technical. 2 visual backstop gates + prohibition sign-off await the user via `/gsd-verify-work 7`.

## Next Action

Start with: `/gsd-verify-work 7` — run UAT tests 1-2 (real-CJK tategaki visual check; effects quality/fluidity at 100/150/200% + drag fluidity) and the prohibition confirmation; verify-work reconciles the 7 gaps to resolved via their executed fix plans. Then `/gsd-secure-phase 7`, then `/gsd-complete-milestone` for v1.1 closeout.
