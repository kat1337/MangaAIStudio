---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 12
type: execute
wave: 5
depends_on: [07-11]
files_modified:
  - manga_ai_studio/gui/inspector_panel.py
  - tests/test_gui_inspector_styling.py
autonomous: true
gap_closure: true
gap_ids: [G-07-2]
requirements:
  - TRAN-02
assumption_delta:
  decision: no-change
  rationale: "G-07-2 is the user-accepted stopgap for the QFontComboBox start-of-name-only incremental search ('Wild Words' cannot find 'CC Wild Words'): a contains/substring filter for the font dropdown in inspector_panel.py. The plan pins the QSortFilterProxyModel approach as primary (roles pass through — the preview delegate keeps working) with a documented fallback; the acceptance contract is substring findability, not the mechanism."
estimate:
  tokens: 15000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Typing part of a font name finds fonts that CONTAIN it — 'Wild Words' surfaces 'CC Wild Words' in the font dropdown (the start-of-name-only search is replaced by a contains match)"
    - "Clearing the filter restores the full family list; the filter is case-insensitive"
    - "The dropdown still functions as before for loading ('Mixed' display text, family selection, the font-preview delegate) — filtering never breaks the load/commit paths"
  artifacts:
    - manga_ai_studio/gui/inspector_panel.py :: a filter QLineEdit above/next to the Font row + a QSortFilterProxyModel wired to the QFontComboBox model (case-insensitive substring filter on the family column), cleared on load
    - tests/test_gui_inspector_styling.py :: test_font_filter_contains_match (the 'Wild Words' -> 'CC Wild Words' scenario, driven on the real font database) + test_font_filter_clears_and_loads_untouched
  key_links:
    - "font_filter_edit.textChanged <-> proxy.setFilterRegularExpression: the live contains filter; escaping the input (QRegularExpression.escape) keeps user input safe as a regex"
    - "font_combo.setModel(proxy) <-> the existing setCurrentText loads (Mixed/family): the proxy passes the source model's roles through, so the preview delegate and the load paths keep working"
  prohibitions:
    - "The QFontComboBox's native family list must never be permanently filtered or polluted (the proxy filters a VIEW of the source model; the source model stays intact; setCurrentText on the combo must keep working for any text incl. 'Mixed')"
    - "No hand-rolled font list rebuild (Don't-Hand-Roll — QFontDatabase stays the single source)"
    - "The filter state must not leak into load_box/load_multi_selection population (the panel clears the filter on load)"
---

<objective>
Close **G-07-2 (minor)** from 07-UAT.md: the font dropdown's built-in incremental search matches from the START of the family name only — 'CC Wild Words' is not found by 'Wild Words'. Root cause (already diagnosed): QFontComboBox's built-in search is start-of-name-only. Stopgap: add a contains/substring search to the font dropdown.

Purpose: typing any part of a font name finds it — the substring filter — without replacing the native font picker.

Output: the filter control + proxy-model wiring in inspector_panel.py and the substring-findability tests.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UAT.md
@manga_ai_studio/gui/inspector_panel.py
@tests/test_gui_inspector_styling.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN — contains-match filter for the font dropdown (filter line-edit + proxy model)</name>
  <files>manga_ai_studio/gui/inspector_panel.py, tests/test_gui_inspector_styling.py</files>
  <read_first>
    - manga_ai_studio/gui/inspector_panel.py — the Font row construction (:367-371: `self.font_combo = QFontComboBox()`, `form.addRow("Font", self.font_combo)`), the __init__ QSS/container context, the load paths that call `self.font_combo.setCurrentText` (:602-609 multi, :735-738 single — the 'Mixed' display text at :607 must keep working through the proxy), the QSS block for QFontComboBox (:125-133)
    - tests/test_gui_inspector_styling.py — _make_inspector fixture, the multi-selection load tests that exercise the font combo (:344-348: 'Mixed' entries)
    - PySide6 facts to verify in a probe before implementing: QFontComboBox.model() returns the QFontDatabaseModel; QSortFilterProxyModel wrapping it preserves the FontRole/decoration roles the preview delegate reads; QComboBox.setModel(proxy) keeps setCurrentText working for arbitrary text with the default (NoInsert) insert policy
  </read_first>
  <behavior>
    - Test 1 (RED gate): a family whose name contains the query mid-string is found — set the filter to a substring of a real installed family (probe the font DB for a longest-name family, e.g. any family with a space+word tail; the test picks the longest installed family name and uses its LAST space-delimited word as the query) — the combo's model rowCount drops to the contains-matching families and the target family is present among the visible rows; typing the SAME query without the fix leaves the rowCount unchanged (the built-in search is start-of-name-only). Fails pre-fix (no filter control exists).
    - Test 2: the filter is case-insensitive; clearing the filter restores the FULL family rowCount; setCurrentText('Mixed') still works with the proxy installed (the load path is unbroken).
  </behavior>
  <action>RED: add `test_font_filter_contains_match` to tests/test_gui_inspector_styling.py — probe the installed font database for the longest family name, derive a mid-name substring query from it, drive the filter line-edit, and assert the filtered rowCount < full rowCount AND the target family is a visible row (via the combo's model, role FontFamilyRole or the item text at the family column); add the clear-restores and case-insensitivity assertions; add a 'Mixed' load-path assertion (load_multi_selection over differing fonts still shows 'Mixed' with the proxy installed). Run — must fail (no filter widget). GREEN in inspector_panel.py: (1) add `from PySide6.QtCore import QSortFilterProxyModel, QRegularExpression` (QRegularExpression lives in QtCore — verify the import path on this PySide6 6.10.1 stack); (2) create `self.font_filter_edit = QLineEdit()` with a 'Filter fonts…' placeholder + clear button, inserted directly above the Font row (a compact row — the QSS tokens for QLineEdit already exist in the panel); (3) create `self._font_proxy = QSortFilterProxyModel(self)` with case-insensitive matching on column 0, `setSourceModel(self.font_combo.model())`, and `self.font_combo.setModel(self._font_proxy)`; (4) `textChanged` -> `self._font_proxy.setFilterRegularExpression(QRegularExpression(QRegularExpression.escape(text), QRegularExpression.PatternOption.CaseInsensitiveOption))` (escape the input — user text is untrusted, ASVS V5); (5) clear the filter in `clear()` and at the top of `load_box`/`load_multi_selection` (blockSignals around setText('') so loads never emit the filter signal). If the probe shows the proxy breaks the preview delegate on this stack, fall back (documented in the SUMMARY): keep the native model and implement find-as-you-type — on textChanged, setCurrentText to the FIRST family containing the substring (the same contains contract; the acceptance test's rowCount assertions are then adapted to the fallback's observable behavior — the acceptance contract is substring findability, pinned by the target-family assertion in both variants). Run — must pass.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_font_filter_contains_match FAILS pre-fix (no filter) and PASSES post-fix (mid-name substring finds the target family)
    - Clearing the filter restores the full family rowCount; the filter is case-insensitive
    - The 'Mixed' load path + the font-preview path keep working with the proxy installed (the existing styling tests pass UNCHANGED)
    - Grep gate: `grep -c 'setFilterRegularExpression' manga_ai_studio/gui/inspector_panel.py` == 1
    - Grep gate: `grep -c 'QRegularExpression.escape' manga_ai_studio/gui/inspector_panel.py` >= 1 (the input is escaped — no regex injection)
  </acceptance_criteria>
  <done>Typing any part of a font name filters the dropdown to contains-matches (case-insensitive, escapable, restorable); the load/commit paths are unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: filter interaction polish + the 'CC Wild Words'-style scenario + full suite</name>
  <files>manga_ai_studio/gui/inspector_panel.py, tests/test_gui_inspector_styling.py</files>
  <read_first>
    - manga_ai_studio/gui/inspector_panel.py — the filter row placement (verify the layout: the filter sits above the Font row and below the Style header, QSS-consistent with the disabled/empty-state handling — `_set_fields_enabled` must disable the filter with the other styling controls)
    - tests/test_gui_inspector_styling.py — the empty-state disable test conventions (the panel.clear() disable assertions at test_gui_boxes.py:1865-1872 shape)
  </read_first>
  <action>Complete the interaction contract: (1) the filter line-edit joins `_set_fields_enabled`/the empty-state disable path (disabled with no selection, like the rest of the styling section); (2) when the filter has text, the font combo popup opens with the filtered rows (verify the proxy view reflects on popup — if the popup needs `combo.showPopup()` after a filter change to refresh the view, wire that); (3) add `test_font_filter_contains_match_scenario` reproducing the user's literal report shape: pick any installed family with a multi-word name (probe `QFontDatabase.families()`), filter by its trailing word, assert the family is visible; (4) run the full styling module, then the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py tests/test_gui_boxes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The filter is disabled in the empty state and enabled with a selection (mirrors the other styling controls)
    - test_font_filter_contains_match_scenario passes: a trailing-word substring query surfaces its multi-word family ('CC Wild Words' / 'Wild Words' shape)
    - The existing styling + boxes modules pass UNCHANGED (no load/commit regression from the proxy or the disable wiring)
    - Full suite green at the re-baselined count (687 baseline from 07-VERIFICATION.md + ~2 new tests = 700 passed, 0 failed)
  </acceptance_criteria>
  <done>The filter behaves like a first-class styling control, the user's exact substring scenario is pinned by test, and the full suite is green.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/inspector_panel.py`:
  - `font_filter_edit` — the 'Filter fonts…' QLineEdit (clear button, disabled in the empty state)
  - `_font_proxy` — the QSortFilterProxyModel (case-insensitive, column 0, source = the QFontComboBox model) installed via `font_combo.setModel(proxy)`
  - `_on_font_filter_changed` — `QRegularExpression.escape`d contains filter
  - filter clearing in `clear()`/`load_box`/`load_multi_selection` (blockSignals-guarded)
- `tests/test_gui_inspector_styling.py` (extended): `test_font_filter_contains_match` + `test_font_filter_contains_match_scenario` (+ clear/case/load-path assertions)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filter input → regex filter expression | The user's filter text crosses into a QRegularExpression; unescaped user input would be a regex-injection hazard (input-validation boundary, ASVS V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-20 | Tampering | font_filter_edit -> setFilterRegularExpression | medium | mitigate | `QRegularExpression.escape(text)` on every filter keystroke — user input is a literal substring, never regex syntax (ASVS V5); the filter is a view-level proxy, the source font model is never mutated |
| T-07-21 | DoS | filtered-dropdown interaction | low | mitigate | The proxy filters a bounded model (the installed-family list, hundreds of rows); no unbounded allocation; the clear button + load-time clearing guarantee the filter can never wedge the dropdown |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py tests/test_gui_boxes.py -x -q` — affected modules green (RED gate proven RED→GREEN)
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 700 passed, 0 failed (687 baseline from 07-VERIFICATION.md + ~2 new)
- Grep gates: `setFilterRegularExpression` x1 + `QRegularExpression.escape` x1+ in inspector_panel.py
</verification>

<success_criteria>
- Typing part of a font name finds every family that CONTAINS it (the 'Wild Words' -> 'CC Wild Words' shape pinned by test)
- The filter restores, disables, and coexists with the load/commit paths; full suite green at 700
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-12-SUMMARY.md` when done
</output>
