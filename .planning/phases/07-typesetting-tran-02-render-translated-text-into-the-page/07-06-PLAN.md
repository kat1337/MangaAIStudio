---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/box_item.py
  - tests/test_gui_boxes.py
autonomous: true
gap_closure: true
gap_ids: [G-07-6]
requirements:
  - TRAN-02
assumption_delta:
  decision: no-change
  rationale: "The G-07-6 diagnosis (UAT) is authoritative and already debugged live (.planning/debug/phase7-ctrl-z-crash.md): the Ctrl+Z native abort is the latent text-overlay teardown UAF (deferred-items.md) manifesting via the undo-rebuild path. No new assumptions; this plan implements the prescribed graveyard + Shiboken.isValid guard + refs-dropped regression test."
estimate:
  tokens: 22500
  raw_tokens: 15000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Ctrl+Z after a style commit never crashes the app: the undo-rebuild path (on_undo -> apply_undo_boxes -> canvas.set_boxes) no longer drops the last Python references to removed BoxItems mid-event-loop while pending scene updates still reference their overlay items"
    - "Removed BoxItem wrappers stay alive through the pending UpdateRequest flush and are released only after the next event-loop iteration (the graveyard contract, observable via weakrefs)"
    - "TypesetOverlayItem.paint is a defensive no-op for any wrapper whose C++ object is gone (Shiboken.isValid guard) — a stale paint can never touch a freed QPixmap"
    - "The Delete-key path (_remove_box) retires its item through the same graveyard — no second drop-the-last-ref site remains in the canvas"
    - "Undo semantics are unchanged: one Ctrl+Z still restores the pre-commit snapshot (existing style-commit + undo tests stay green)"
  artifacts:
    - manga_ai_studio/gui/canvas.py :: _retire_boxes + _release_graveyard (the graveyard pair, singleShot(0)-scheduled), wired into set_boxes (:1650-1652) and _remove_box (:2134); QTimer import added to the QtCore import line
    - manga_ai_studio/gui/box_item.py :: TypesetOverlayItem.paint gains the Shiboken.isValid guard (:331-334)
    - tests/test_gui_boxes.py :: test_undo_style_commit_with_dropped_refs_no_crash (the refs-dropped-before-on_undo regression) + the delete-path variant
  key_links:
    - "canvas.set_boxes -> _retire_boxes -> QTimer.singleShot(0) -> _release_graveyard: the deletion is deferred past the scene's UpdateRequest flush (the scene's update timer is queued BEFORE our release timer, so paint dispatches to live objects)"
    - "on_undo -> apply_undo_boxes -> set_boxes: the only path the regression test drives — every prior test held wrappers alive, which is why the suite never reproduced the crash"
    - "Shiboken.isValid in paint: the belt-and-suspenders half — any path that reaches Python paint with a stale wrapper no-ops instead of drawing from a freed pixmap"
  prohibitions:
    - "Removed BoxItems must never be dropped to zero Python references inside set_boxes/_remove_box while the scene still holds queued updates"
    - "The graveyard release must be deferred (singleShot(0)), never synchronous inside set_boxes"
    - "paint() must not draw from _pixmap when the wrapper's C++ object is invalid"
---

<objective>
Close **G-07-6 (blocker)** from 07-UAT.md: pressing Ctrl+Z after a style commit natively crashes the app (0xC0000409). Root cause (already diagnosed — do not re-diagnose): style commits queue scene updates holding pointers to overlay items (box_item.py:396 `update()`), then Ctrl+Z → `on_undo` → `apply_undo_boxes` → `canvas.set_boxes` (canvas.py:1622) removes every BoxItem and drops the last Python refs (canvas.py:1650-1652: `_scene.removeItem(item)` loop + `self._box_items = []`), so shiboken synchronously deletes the C++ BoxItems mid-event-loop while pending updates still reference them; the next event-loop flush dispatches `paint()` to a freed TypesetOverlayItem (box_item.py:331-334 `drawPixmap`) → pure-virtual call → abort. Heap-layout dependent (real-app heap: torch/cudnn/QColorDialog/font DB); test helpers hold wrappers alive, which is why the suite is green.

Purpose: eliminate the teardown UAF — retire removed wrappers to a graveyard released via `QTimer.singleShot(0)` (after the pending UpdateRequest flush), guard `paint()` with `Shiboken.isValid`, and pin the lifetime contract with a regression test that drops item refs before `on_undo()` exactly as the app does.

Output: the graveyard pair in `EditorCanvas`, the paint guard in `TypesetOverlayItem`, and the refs-dropped regression tests.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UAT.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/deferred-items.md
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/box_item.py
@tests/test_gui_boxes.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-GREEN — undo-rebuild teardown UAF: graveyard + paint guard + refs-dropped regression</name>
  <files>manga_ai_studio/gui/canvas.py, manga_ai_studio/gui/box_item.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/canvas.py — set_boxes (:1622-1675; the remove+drop at :1650-1652, the boxes_modified emit at :1675), the QtCore import line (:36 — QPointF, QRectF, Qt, Signal; QTimer must join it), _box_items lifecycle
    - manga_ai_studio/gui/box_item.py — TypesetOverlayItem.paint (:331-334), set_content's `self.update()` (:396 — the queued UpdateRequest that outlives the item), the overlay construction (:315-322)
    - tests/test_gui_boxes.py — _seed_boxes_window / _window_with_page helpers (the existing helpers HOLD the item wrappers — the regression must deliberately drop them), the existing style-commit + undo tests (test_style_commit_* and the Ctrl+Z tests the UAT says must stay green), the QApplication.processEvents() driving convention
    - manga_ai_studio/gui/main_window.py — on_undo (:3382-3402) + apply_undo_boxes (:3455-3482) — the undo-rebuild path the test drives
  </read_first>
  <behavior>
    - Test 1 (RED gate, deterministic mechanism contract): after a style commit + `del items` (all wrapper refs dropped except the canvas's own `_box_items`), `window.on_undo()` WITHOUT an event-loop flush leaves every removed item's weakref ALIVE (`wr() is not None` — the graveyard holds the wrappers through the pending UpdateRequest flush); the FOLLOWING `QApplication.processEvents()` releases them (`wr() is None`). On the current code assertion 1 FAILS deterministically: set_boxes drops `_box_items` synchronously, the wrappers die inside on_undo, and shiboken deletes the C++ items mid-event-loop — the exact teardown the crash diagnosis describes.
    - Test 2 (crash-path regression): the full app lifetime cycle — style-commit a box (queues overlay updates), drop every wrapper ref, `on_undo()`, `processEvents()` — repeated for several undo/redo cycles — never crashes and the layer rebuilds to the correct pre-commit state (box count + pagebox text/style assertions).
  </behavior>
  <action>RED: add `test_undo_style_commit_with_dropped_refs_no_crash` to tests/test_gui_boxes.py. Mirror the style-commit-then-undo shape of the existing style tests but (a) capture `weakref.ref(item)` for every seeded item BEFORE dropping, (b) `del items` (and any other local wrapper refs) so the canvas's `_box_items` is the ONLY strong ref, (c) `window.on_undo()`, then assert every weakref is still ALIVE (the graveyard contract), (d) `QApplication.processEvents()`, then assert every weakref is DEAD (released after the flush) + the rebuilt layer matches the pre-commit snapshot (box count, style fields) + `window.canvas._box_items` holds the restored wrappers. Run — assertion (c) fails on current code (RED). GREEN, two production edits: (1) canvas.py — add `QTimer` to the QtCore import; add the graveyard pair: `_retire_boxes(items)` appends to `self._box_graveyard` and schedules `QTimer.singleShot(0, self._release_graveyard)` guarded by a `_graveyard_pending` flag (never two outstanding timers); `_release_graveyard` clears the pending flag then clears the graveyard list (dropping the refs happens in the timer callback — after the scene's update flush, because the scene's 0-timeout update timer was queued BEFORE our release timer at update()-time). In `set_boxes`, replace the remove+drop (:1650-1652) with: detach `removed = self._box_items`, set `self._box_items = []`, loop `self._scene.removeItem(item)` per removed item, then `self._retire_boxes(removed)`. (2) box_item.py — add `from PySide6 import Shiboken` at module top; in `TypesetOverlayItem.paint`, no-op when `not Shiboken.isValid(self)` before touching `self._pixmap`. Run — must pass. Note: `_graveyard_pending`/`_box_graveyard` must be initialized in `__init__` BEFORE any set_boxes call (defensive `getattr` in `_retire_boxes` is acceptable if an init-order hazard exists, but prefer explicit init near `_box_items`).</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_undo_style_commit_with_dropped_refs_no_crash FAILS on the pre-fix code at the weakref-alive assertion (RED gate) and PASSES after the graveyard + guard
    - The weakref transitions are deterministic: alive immediately after on_undo (no event-loop flush in between), dead after the next processEvents
    - The undo rebuild restores the exact pre-commit snapshot (existing style-commit + undo assertions in the same test remain, incl. payload detachment)
    - Grep gate (single graveyard schedule): `grep -c 'QTimer.singleShot(0' manga_ai_studio/gui/canvas.py` == 1 (the graveyard release — the only zero-delay timer in the canvas)
    - Grep gate (guard present): `grep -c 'Shiboken.isValid(self)' manga_ai_studio/gui/box_item.py` == 1
  </acceptance_criteria>
  <done>The undo-rebuild path no longer drops the last BoxItem refs synchronously; the graveyard holds them through the update flush; paint guards against stale wrappers; the refs-dropped regression is RED on the old code and GREEN on the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Delete-key path retires through the graveyard (no second drop-the-last-ref site)</name>
  <files>manga_ai_studio/gui/canvas.py, tests/test_gui_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/canvas.py — _remove_box (:2134-2160: `_scene.removeItem(item)` + `self._box_items.remove(item)` — the same synchronous drop-the-last-ref shape as the pre-fix set_boxes)
    - tests/test_gui_boxes.py — the existing delete-key tests (the Delete/Backspace handler path) to extend with the dropped-refs variant
  </read_first>
  <action>Route the single-delete path through the same graveyard: in `_remove_box`, after `self._scene.removeItem(item)` and `self._box_items.remove(item)`, call `self._retire_boxes([item])` so the wrapper's release is deferred to the next event-loop iteration — the Delete key is hit mid-event-loop with the same queued-update precondition as the undo path (a style commit queues overlay updates; a subsequent Delete then removes the item; the deferred flush must not find a freed overlay). The `boxes_modified(before)` emission and the WR-04/CR-01 snapshot discipline stay byte-identical. Extend the regression coverage: add `test_delete_with_dropped_refs_no_crash` — seed a box, style-commit it, drop the wrapper refs, `del` the local item ref, drive the Delete handler (the existing key-press driving convention), assert the weakref is alive immediately after (graveyard) and dead after `processEvents()`, with the layer rebuilt to the pre-delete state on undo. Then run the affected modules and the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py tests/test_gui_canvas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - _remove_box retires through _retire_boxes — no second synchronous drop-the-last-ref site remains in canvas.py
    - test_delete_with_dropped_refs_no_crash passes: weakref alive post-delete, dead post-flush, undo restores the deleted box
    - Existing delete tests (silent delete, one snapshot, undo recovers) pass UNCHANGED
    - Grep gate: `grep -v '^#' manga_ai_studio/gui/canvas.py | grep -c '_retire_boxes('` == 2 (set_boxes + _remove_box — every removal site)
    - Full suite green at the re-baselined count (687 baseline from 07-VERIFICATION.md + 2 new tests = 689 passed, 0 failed)
  </acceptance_criteria>
  <done>Every BoxItem removal in the canvas retires through the graveyard; the full suite is green; the Ctrl+Z crash path is closed by test at the wrapper-lifetime level.</done>
</task>

</tasks>

## Artifacts this phase produces

- `manga_ai_studio/gui/canvas.py`:
  - `_retire_boxes(items)` — graveyard enqueue + singleShot(0) scheduling (guarded by `_graveyard_pending`)
  - `_release_graveyard()` — the timer callback that clears the graveyard (the refs drop happens here, after the update flush)
  - `_box_graveyard` / `_graveyard_pending` — instance state initialized in `__init__`
  - `set_boxes` (:1650-1652) and `_remove_box` (:2134-2160) — both route removal through `_retire_boxes`
  - `QTimer` added to the QtCore import
- `manga_ai_studio/gui/box_item.py`:
  - `TypesetOverlayItem.paint` — `Shiboken.isValid(self)` guard before `drawPixmap` (:331-334)
  - `from PySide6 import Shiboken` module-top import
- `tests/test_gui_boxes.py` (extended):
  - `test_undo_style_commit_with_dropped_refs_no_crash` — the refs-dropped-before-on_undo regression (weakref-alive/weakref-dead + rebuilt-layer assertions)
  - `test_delete_with_dropped_refs_no_crash` — the Delete-path variant

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| undo-rebuild / delete teardown → event-loop paint | `set_boxes`/`_remove_box` drop the last Python refs to BoxItems (and their TypesetOverlayItem children) mid-event-loop; the pending scene UpdateRequest flush then dispatches paint to freed C++ objects → native abort (0xC0000409) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | DoS | canvas.set_boxes / _remove_box teardown (undo-rebuild + delete paths) | critical | mitigate | Graveyard pattern: removed wrappers held in `_box_graveyard`, released only via `QTimer.singleShot(0)` after the scene's update flush; both removal sites route through `_retire_boxes`; refs-dropped regression tests pin the lifetime contract (ASVS V7 — the crash is a state-integrity failure of the undo path) |
| T-07-09 | DoS | TypesetOverlayItem.paint on a stale wrapper | high | mitigate | `Shiboken.isValid(self)` guard — paint no-ops before touching `_pixmap` when the C++ object is gone; defense-in-depth behind the graveyard (the load-bearing fix) |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages (RESEARCH Package Legitimacy Audit: none installed). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py tests/test_gui_canvas.py -x -q` — affected modules green (RED gate proven RED→GREEN on the weakref contract)
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — 689 passed, 0 failed (687 baseline from 07-VERIFICATION.md + 2 new)
- Grep gates: `QTimer.singleShot(0` x1 in canvas.py; `Shiboken.isValid(self)` x1 in box_item.py; `_retire_boxes(` x2 in canvas.py (every removal site)
- The remaining G-07 gaps are separate plans (07-07..07-12) — do not address them here
</verification>

<success_criteria>
- Ctrl+Z after a style commit no longer crashes the app — the native UAF path (synchronous wrapper release mid-event-loop with pending scene updates) is structurally closed and pinned by the refs-dropped regression
- Delete-key removal is equally protected; no second drop-the-last-ref site remains
- Undo semantics unchanged: existing style-commit + undo tests green, full suite at 689
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-06-SUMMARY.md` when done
</output>
