---
phase: 07-typesetting-tran-02-render-translated-text-into-the-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/text_style.py
  - manga_ai_studio/core/box_model.py
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/text_renderer.py
  - manga_ai_studio/core/ocr_export.py
  - manga_ai_studio/gui/box_item.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_core/test_text_style.py
  - tests/test_core/test_typeset_layout.py
  - tests/test_core/test_typeset_bake.py
  - tests/test_core/test_history_boxes.py
  - tests/test_gui_export.py
autonomous: true
requirements:
  - TRAN-02
estimate:
  tokens: 65000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A fresh box renders OPAQUE typeset text on the canvas — default TextStyle (Liberation Sans, #e8e8ea opaque fill, 2px #0b0b0e outline, Auto-fit on, center/middle, horizontal) — replacing the Phase 4 translucent overlay look (D-01); the hardcoded _OVERLAY_FILL/_OVERLAY_OUTLINE/_OVERLAY_FONT constants are gone from box_item.py"
    - "Export Typeset… (Ctrl+Shift+B, File menu, after Export OCR JSON) bakes the current page image (detached copy — Pitfall 2) with per-box styled text composited at 1:1 through the SAME renderer functions as the canvas — canvas ≡ bake (D-01/D-02); boxes with neither translation nor recognized text render/bake nothing (D-04 current-focus rule)"
    - "The sidecar lands next to the source page when pristine and in cleaned/ when geometry-altered (D-03 via ocr_json_target_dir), written by save_image_optimized (PNG compress_level=9 / JPG quality=95, DPI preserved — the PROJ-02 writer contract)"
    - "Auto-fit (the default) preserves the 04-09 machinery at scene px: box-adaptive base (14 × min(w,h)/100), [10,28] base clamp, bounded shrink (12 iters × 0.9, 5 px floor) — long translations fit or floor (UI-SPEC long-text covered row)"
    - "Bake failure surfaces 'Couldn't save '{filename}'.' QMessageBox::Critical with the writable-folder copy and never mutates the canvas (UI-SPEC error covered row)"
    - "PageBox.copy() detaches style (undo snapshots never alias — Pitfall 8); canvas.boxes_snapshot() forwards style into rebuilt PageBoxes"
    - "TextStyle.to_dict()/from_dict() round-trip; from_dict type-checks + clamps numerics (size 1..1024, widths/radii 0..256, opacity 0..1), ignores unknown keys, None → defaults (Pitfall 8)"
    - "The layout reports overflow for text exceeding the box inner rect and paint() never clips (unclipped WYSIWYG contract; the manual-size control that makes overflow reachable lands with plan 07-05)"
  artifacts:
    - manga_ai_studio/core/text_style.py :: TextStyle dataclass + to_dict/from_dict
    - manga_ai_studio/gui/text_renderer.py :: layout() + paint() + bake_typeset_page() + current_focus_text()
    - manga_ai_studio/core/ocr_export.py :: default_typeset_path()
    - manga_ai_studio/core/box_model.py :: PageBox.style field + copy() detachment
    - manga_ai_studio/gui/box_item.py :: renderer-driven opaque typeset overlay child
    - manga_ai_studio/gui/main_window.py :: action_export_typeset + _on_export_typeset
    - tests/test_core/test_text_style.py, test_typeset_layout.py, test_typeset_bake.py (new)
  key_links:
    - "BoxItem overlay ↔ bake compositor share ONE renderer path (D-01 canvas ≡ bake; the equivalence pixel test is the Pitfall 2 guard)"
    - "canvas.boxes_snapshot() → PageBox.style forwarding (every undo round-trip must preserve style)"
    - "bake_typeset_page → current_focus_text rule (D-04) — boxes with neither text skip"
  flagged_assumptions:
    - "Empty-text boxes render nothing and bake nothing (D-04 current-focus rule; probe edge: empty input)"
    - "Text length/width math operates on Unicode code points (Python str semantics), never bytes — no multi-byte truncation in the tategaki layout or fit-in-box loops (probe edge: encoding)"
  prohibitions:
    - "OCR/translation text is never rendered via setHtml or rich-text — plain-text rendering only (ASVS V5; QTextLayout plain runs / setPlainText)"
    - "TextStyle is never mutated in place — every style change assigns a fresh instance via dataclasses.replace; snapshots detach style (Pitfall 8/1); the Mixed sentinel never persists into a TextStyle"
    - "Vertical text is never whole-block rotated — tategaki is per-run layout; the bake composites text only (no chrome) onto a detached page copy (Pitfall 2)"
    - "Group/style ops push exactly ONE BOXES snapshot; overlay reposition never re-layouts during mousemove (RC-1 — setPos-only)"
---

<objective>
Land the phase's end-to-end tracer slice — **one text box with the default style renders opaquely on the canvas AND bakes to a PNG sidecar** — by wiring the four layers D-01 depends on: the `TextStyle` model (`core/text_style.py` + `PageBox.style`), the shared renderer (`gui/text_renderer.py` — horizontal layout + fill/outline paint + the bake compositor), the opaque canvas overlay (`gui/box_item.py`), and the Export Typeset… action (`gui/main_window.py`). This is the phase's single-visual-truth proof: the same renderer functions serve canvas and bake, so the export cannot drift from what the user sees.

Purpose: every later plan (multi-select, tategaki, effects, Inspector styling, persistence) builds on a proven pipeline. The model→render→disk path is verified end-to-end here — the architecture is proven before any styling control exists.

Output: `TextStyle` + `PageBox.style` (composition, `copy()` detachment), `gui/text_renderer.py` (layout/paint/bake headless-testable), `default_typeset_path` in `ocr_export.py`, the opaque overlay rework in `box_item.py`, the `action_export_typeset` handler in `main_window.py`, and the test files `test_text_style.py` / `test_typeset_layout.py` / `test_typeset_bake.py` plus extensions to `test_history_boxes.py` / `test_gui_export.py`.
</objective>

> **Scope note (checker scope_sanity, accepted):** the 12-file `files_modified` concentration is the deliberate tracer-first foundation — the model → renderer → overlay → bake pipeline every later plan (07-02..07-05) builds on. It sits within the estimate budget (65k/100k tokens, 3 tasks) and is bounded by per-task test-then-implement commits, each carrying its own test run before implementation.


<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-CONTEXT.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-RESEARCH.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-PATTERNS.md
@.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-UI-SPEC.md
@manga_ai_studio/core/box_model.py
@manga_ai_studio/gui/box_item.py
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/core/ocr_export.py
@manga_ai_studio/core/image_io.py
</context>
<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TextStyle model + shared renderer (horizontal) + bake-to-disk pipeline — RED-GREEN</name>
  <files>manga_ai_studio/core/text_style.py, manga_ai_studio/core/box_model.py, manga_ai_studio/gui/canvas.py, manga_ai_studio/gui/text_renderer.py, manga_ai_studio/core/ocr_export.py, tests/test_core/test_text_style.py, tests/test_core/test_typeset_layout.py, tests/test_core/test_typeset_bake.py</files>
  <read_first>
    - manga_ai_studio/core/box_model.py — PageBox dataclass :53-178, `copy()` :166-178 (the composition + copy-detach discipline to extend: `replace(self, payload=_copy.copy(self.payload))` gains `style=_copy.copy(self.style)`)
    - 07-PATTERNS.md — §`core/text_style.py` (module-header/dataclass/serialization patterns mirroring box_model.py:53-94 + the V5 coercion shape of project_io.py:182-271) and §`core/box_model.py` (change sites 1-3 incl. `boxes_snapshot` style forwarding, canvas.py:1710-1746)
    - 07-RESEARCH.md — Code Example 1 (TextStyle fields/defaults), Common Operation 7 (bake composite), Pattern 1 (scene-px sizing + auto-fit constants), the V5 bounds (size 1..1024, widths/radii 0..256, opacity 0..1)
    - manga_ai_studio/gui/box_item.py — `_current_focus_text` :720-746 (the D-04 rule `current_focus_text` extracts), `_OVERLAY_*` fit constants :108-122 (the Auto-fit machinery the renderer takes over)
    - manga_ai_studio/core/ocr_export.py — `ocr_json_target_dir` :199-212 + `default_ocr_json_path` :215-222 (the D-22 shape `default_typeset_path` mirrors)
    - manga_ai_studio/core/image_io.py — `save_image_optimized` :47-107 (the PROJ-02 writer contract the bake tests assert)
  </read_first>
  <behavior>
    - Test 1 (defaults): TextStyle() defaults match UI-SPEC A1 — font_family "Liberation Sans", bold False, italic False, font_size_px None, auto_fit True, color "#e8e8ea", align_h "center", align_v "middle", vertical False, outline {enabled True, color "#0b0b0e", width_px 2.0}, glow {enabled False}, shadow {enabled False}
    - Test 2 (round-trip): TextStyle.to_dict()/from_dict() round-trip preserves every field; from_dict(None) and from_dict({}) return defaults
    - Test 3 (V5 clamps): from_dict with font_size_px -5/0/2000 → clamped into 1..1024 (or None when auto_fit), width_px 300 → 256, opacity 1.5 → 1.0, non-numeric → defaults; unknown keys ignored; wrong effect-key shape → defaults
    - Test 4 (copy detaches): push boxes snapshot → mutate live pb.style → HistoryManager pop restores the PRE-edit style; restored style object is not the live one (Pitfall 1/8)
    - Test 5 (layout horizontal): layout("hello world", default_style, QRectF(0,0,100,50), vertical=False) wraps at inner width (two lines for a long string), align_h/align_v shift the ink rect, overflow flag False when text fits, True when a fixed-size text exceeds inner height
    - Test 6 (bake content): bake_typeset_page on a page with one box with translation only bakes the translation; with recognized only bakes recognized; with neither leaves the page pixels unchanged (D-04); result does not share memory with the input (Pitfall 2)
    - Test 7 (placement): default_typeset_path returns {stem}_typeset.png in source dir when geometry_altered False, in cleaned/ when True (dir created if missing); save_image_optimized writes a PNG at compress_level=9 with DPI preserved
  </behavior>
  <action>RED: write the behavior tests — new `tests/test_core/test_text_style.py` (defaults, round-trip, clamps, copy-detach via a real HistoryManager per the test_history_boxes shape), new `tests/test_core/test_typeset_layout.py` (horizontal layout geometry), new `tests/test_core/test_typeset_bake.py` (composite pixels + D-04 + placement + writer contract) — and run them (they must fail on missing modules). GREEN: (1) create `core/text_style.py` with the `TextStyle` dataclass exactly per RESEARCH Code Example 1 with the UI-SPEC-pinned defaults (outline width 2.0, glow radius 4.0, shadow dx/dy 2.0 absolute px); `to_dict()` returns the hand-picked field projection (never raw dataclass dict); `from_dict(d)` type-checks + clamps every numeric (font_size_px 1..1024 or None, widths/radii 0..256, opacity 0..1), ignores unknown keys, None → defaults. (2) In `core/box_model.py` add `style: Optional["TextStyle"] = None` next to the Phase 4 peer fields and extend `copy()` to `replace(self, payload=_copy.copy(self.payload), style=_copy.copy(self.style))` (import TextStyle; no vendored-type changes — D-14 composition). (3) In `gui/canvas.py` `boxes_snapshot()` forward `style=item.pagebox.style` into each rebuilt PageBox (PATTERNS §box_model change site 3). (4) Create `gui/text_renderer.py` (QtGui/QtWidgets imports only — no widget deps; ASVS V5: plain runs only, never setHtml): `layout(text, style, box_rect, vertical=False) -> LayoutResult` — horizontal mode wraps at inner width (inner = rect shrunk by 2×_OVERLAY_INSET), aligns per align_h/align_v, renders at font_size_px when set else the Auto-fit bounded loop (box-adaptive base 14 × min(w,h)/100, [10,28] base clamp, 12 iters × 0.9, 5 px floor — the 04-09 constants), reports ink bounds + overflow flag; `paint(painter, result, style)` — opaque fill pass (QColor(style.color)) + outline pass via QPainterPath.addText + strokePath (RoundCap/RoundJoin, width_px from style, skip when width 0); `bake_typeset_page(page_np, boxes, renderer) -> np.ndarray` per Common Operation 7 (detached QImage copy, per-box current-focus text, skip empty, renderer.layout + paint at 1:1, detached numpy out); `current_focus_text(pb)` — translation else recognized else "" (the box_item.py:720-746 rule). (5) In `core/ocr_export.py` add `default_typeset_path(page_path, geometry_altered) -> Path` mirroring `default_ocr_json_path` with the `_typeset.png` suffix (D-03 locked spelling). Run the three new test files; then the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_text_style.py tests/test_core/test_typeset_layout.py tests/test_core/test_typeset_bake.py tests/test_core/test_history_boxes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_text_style.py: defaults test asserts every field value; round-trip asserts dict equality after from_dict(to_dict()); clamp test asserts font_size_px 2000 → 1024, width_px 300 → 256, opacity 1.5 → 1.0, and non-numeric inputs fall back to defaults without raising
    - test_typeset_bake.py: the D-04 test asserts the baked page is byte-identical to the source page (np.array_equal) when no box has text; the detach test asserts not np.shares_memory(result, page_np)
    - test_typeset_layout.py: the wrap test asserts the LayoutResult carries ≥2 line rects for a long string in a narrow rect; the overflow test asserts overflow True for fixed-size text taller than inner_h
    - test_typeset_bake.py placement test asserts default_typeset_path(...).name == "{stem}_typeset.png" and parent == source dir / "cleaned" when geometry_altered; the writer test asserts PNG bytes written with compress_level=9 and DPI within 1 of the original (the Phase 2 DPI-tolerance discipline)
    - Grep gate: `rg "setHtml" manga_ai_studio/gui/text_renderer.py` returns nothing
  </acceptance_criteria>
  <done>The style model detaches on copy, the renderer produces correct horizontal geometry + opaque fill/outline paint, and the bake writes a D-03-placed sidecar through save_image_optimized — all locked by green tests, committed as test-then-implement commits.</done>
</task>
<task type="auto">
  <name>Task 2: Opaque canvas overlay rework + Export Typeset… action (the user-facing half of D-01)</name>
  <files>manga_ai_studio/gui/box_item.py, manga_ai_studio/gui/main_window.py, tests/test_gui_export.py</files>
  <read_first>
    - manga_ai_studio/gui/box_item.py — overlay construction :340-349, `refresh_text_overlay` :487-565 (the hardcoded style this task replaces), `_reposition_text_overlay` :566-580 (setPos-only — the RC-1 discipline to preserve), `set_text_overlay_visible` :670-686 (T toggle — unchanged contract), `apply_overlay_zoom` :591-608 (scene-px sizing supersedes the [10,28] viewport clamp for opaque text), `_current_focus_text` :720-746
    - manga_ai_studio/gui/main_window.py — `_build_file_menu` :296-445 (Export section: Export Page Ctrl+E :382-385, Export OCR JSON :657-658), `_refresh_action_states` :966-1112 (the gate shape `page_open and not self._op_running` :1040/1091-1093), `export_page` :4534-4575 (the handler template incl. save-failure dialog :4637-4645 + status flash :4646), the `_snapshot_current_page` flush seam :4605, `_on_boxes_modified` :2741-2776
    - 07-PATTERNS.md — §`gui/box_item.py` (overlay construction + D-04 rule + inset discipline) and §`gui/main_window.py` (change sites 1, 2, 5 — the action shape, the gate, the handler recipe with default_typeset_path + save_image_optimized + flash + failure dialog)
    - 07-UI-SPEC.md — surface 35 (bake export contract: Ctrl+Shift+B, filter, default name, D-03 default dir, silent overwrite, inline execution, flash copy) and the Copywriting rows (Export Typeset…, "Typeset exported → {filename}", "Couldn't save '{filename}'.")
  </read_first>
  <action>In `gui/box_item.py`: replace the `QGraphicsTextItem` overlay child with a renderer-driven typeset overlay — a custom QGraphicsItem child (or pixmap-cached child) at the same z=120 that (a) refreshes through `gui/text_renderer.layout` + `paint` into a cached QPixmap keyed (text, style, box_size, vertical), (b) renders OPAQUE per the box's TextStyle (defaults for boxes without one — D-06 flat), (c) draws from `current_focus_text` (D-04 — translation else recognized else nothing), (d) keeps `_reposition_text_overlay` setPos-only (RC-1 — a move/resize drag must never re-layout), (e) keeps the T-toggle (`set_text_overlay_visible`) and the box-visibility inheritance, (f) sizes in scene px (the style size or the Auto-fit loop — the [10,28] viewport clamp applies only inside Auto-fit's base computation per UI-SPEC surface 34; the 2/zoom viewport outline is gone — outline width is the style's scene-px width), and (g) removes the hardcoded `_OVERLAY_FILL`/`_OVERLAY_OUTLINE`/`_OVERLAY_FONT` constants (their values move into the TextStyle defaults; keep the `_OVERLAY_FIT_*` + `_OVERLAY_INSET` constants for the Auto-fit loop). The overlay reads `pb.style.vertical` only (the payload.vertical OR arrives with the live checkbox in plan 07-05 — both canvas and bake flip together, no divergence window). In `gui/main_window.py`: add `action_export_typeset = QAction("Export Typeset…", self)` with `setShortcut(QKeySequence("Ctrl+Shift+B"))`, `triggered.connect(self._on_export_typeset)`, `setEnabled(False)`, placed in the File menu Export section AFTER Export OCR JSON… (PATTERNS §main_window change site 1); add `_on_export_typeset` mirroring `export_page` :4534-4575: `_op_running` guard first, `_snapshot_current_page()` flush, page copy via `canvas.get_image_numpy()` (detached — Pitfall 2), default path via `default_typeset_path(page_path, imf.geometry_altered)` where imf is the current ImageFile, `QFileDialog.getSaveFileName(self, "Export Typeset", str(default_path), "PNG Image (*.png);;JPEG Image (*.jpg *.jpeg)")` (cancel → return silently), composite via `bake_typeset_page(page_np, canvas boxes list, text_renderer)`, write via `save_image_optimized(result, dest, original=page_path)` (image_io.py:47-107 — never a new encode path), success → `_show_transient_status(f"Typeset exported → {dest.name}")`, OSError → the Phase 5 save-failure critical dialog "Couldn't save '{filename}'." + "Check that the folder is writable and see the log for details."; add `action_export_typeset` to `_refresh_action_states` with `page_open and not self._op_running` (PATTERNS §main_window change site 2). Extend `tests/test_gui_export.py` with `test_typeset_export_action`: build a scene with one box carrying a translation, assert the overlay child is visible and painted with the fill color (scene render or item.grab() pixel probe), invoke the action handler, assert the sidecar exists at the D-03 path, decodes, and contains fill-color pixels inside the box rect (and no box-border chrome pixels), and that a failure path (unwritable dest) shows the critical dialog without touching the canvas image.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_export.py::test_typeset_export_action tests/test_core/test_typeset_bake.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_typeset_export_action passes: the sidecar decodes with fill-color pixels inside the box rect; the page outside the box keeps the source pixels (no chrome, no drift)
    - The overlay pixel probe asserts opaque rendering (fill alpha == 255 at glyph pixels — the translucent 0.85 is gone)
    - The failure-path assertion triggers the "Couldn't save" critical dialog and the canvas image object is unchanged (np.array_equal before/after)
    - `rg "_OVERLAY_FILL|_OVERLAY_OUTLINE|_OVERLAY_FONT\b" manga_ai_studio/gui/box_item.py` returns nothing (constants removed — their values live in TextStyle defaults)
    - The full suite runs green at plan end (existing overlay/gui tests keep passing — the T toggle, box visibility, and reposition behaviors are preserved)
  </acceptance_criteria>
  <done>A real user can see opaque typeset text on the canvas and export it — File ▸ Export Typeset… writes the D-03 sidecar with exactly the styled text the canvas shows; committed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: D-01 fidelity locks — canvas≡bake equivalence, overflow contract, style-undo regression</name>
  <files>manga_ai_studio/gui/text_renderer.py, tests/test_core/test_typeset_bake.py, tests/test_core/test_typeset_layout.py, tests/test_core/test_text_style.py, tests/test_core/test_history_boxes.py</files>
  <read_first>
    - manga_ai_studio/gui/text_renderer.py — the Task 1 implementations (re-read only the sections under test)
    - 07-RESEARCH.md — Pitfall 2 (the bake and canvas diverge — the equivalence guard), Pitfall 1 (style aliasing across undo snapshots — the HistoryManager regression), Pattern 1 note (scene-px sizing; overflow unclipped)
    - 07-UI-SPEC.md — the overflow covered row (unclipped on both canvas and bake) and the long-text row (Auto-fit floor)
    - tests/test_core/test_history_boxes.py — the existing BOXES undo test shape to extend with the style variant
  </read_first>
  <behavior>
    - Test 1 (equivalence): for the same (text, style, rect), pixels painted via renderer.paint into a QImage at scale 1.0 are byte-identical to the corresponding bake region (same inputs → same pixels — the Pitfall 2 guard)
    - Test 2 (overflow): a fixed-size text taller than the box inner rect yields overflow True and the painted glyphs extend beyond the rect unclipped (no painter clip on the canvas path)
    - Test 3 (auto-fit floor): a 2000-char translation auto-fits or floors at 5 px scene — the loop terminates (12 iters max) and the ink rect never exceeds the box inner rect at floor
    - Test 4 (style undo): real HistoryManager — push boxes with style A, mutate a box's style to B via dataclasses.replace assignment, pop undo → the restored PageBox carries style A and its style object is not aliased (Pitfall 1 regression, mirroring test_payload_aliasing.py)
    - Test 5 (V5 edges): from_dict absorbs a style dict with non-dict outline, list-valued color, and a font_size_px of "big" — always yielding a valid TextStyle without raising
  </behavior>
  <action>RED: write the five tests (equivalence in test_typeset_bake.py, overflow + floor in test_typeset_layout.py, V5 edges in test_text_style.py, style-undo in test_history_boxes.py) — the equivalence test fails if the paint path and the bake path diverge (the probe's job); the others must pass immediately. GREEN: fix any divergence found in gui/text_renderer.py (e.g. a bake-only inset or scale difference — the layout/paint functions are the single path; do NOT add bake-only code paths). Ensure the paint pass never installs a clip on the page/canvas painter (overflow unclipped per the locked A6 policy). Run the four test files, then the full suite.</action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_typeset_bake.py tests/test_core/test_typeset_layout.py tests/test_core/test_text_style.py tests/test_core/test_history_boxes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The equivalence test asserts np.array_equal(canvas_style_pixels, bake_pixels) for the same inputs — the D-01 contract pinned by test, not prose
    - The overflow test asserts ink pixels exist at y beyond the rect bottom (unclipped) and layout reports overflow True
    - The auto-fit floor test asserts the loop terminates and the ink rect stays within the box inner rect (fits or floors at 5 px scene)
    - The style-undo test asserts the restored PageBox's style equals style A (field-wise) and `restored.style is not live.style`
    - The V5-edges test asserts from_dict never raises and always returns a TextStyle with clamps applied
  </acceptance_criteria>
  <done>The D-01 canvas≡bake guarantee, the unclipped-overflow contract, and the style-undo regression are all locked by green tests; the full suite passes.</done>
</task>

</tasks>
## Artifacts this phase produces

- `manga_ai_studio/core/text_style.py` (NEW):
  - `TextStyle` dataclass — fields: `font_family: str = "Liberation Sans"`, `bold: bool = False`, `italic: bool = False`, `font_size_px: float | None = None` (None = Auto per D-15), `auto_fit: bool = True`, `color: str = "#e8e8ea"`, `align_h: str = "center"` (left|center|right), `align_v: str = "middle"` (top|middle|bottom), `vertical: bool = False`, `outline: dict` (enabled True / color "#0b0b0e" / width_px 2.0), `glow: dict` (enabled False / color "#e8e8ea" / radius_px 4.0 / opacity 0.8), `shadow: dict` (enabled False / color "#000000" / radius_px 4.0 / dx 2.0 / dy 2.0 / opacity 0.6)
  - `TextStyle.to_dict() -> dict` (hand-picked projection) + `TextStyle.from_dict(d: dict | None) -> TextStyle` (V5 clamps: size 1..1024, widths/radii 0..256, opacity 0..1; unknown keys ignored; None → defaults)
- `manga_ai_studio/core/box_model.py` (MOD): `PageBox.style: Optional[TextStyle] = None` field; `copy()` extended to detach both `payload` and `style`
- `manga_ai_studio/gui/canvas.py` (MOD): `boxes_snapshot()` forwards `style=item.pagebox.style` into rebuilt PageBoxes
- `manga_ai_studio/core/ocr_export.py` (MOD): `default_typeset_path(page_path, geometry_altered) -> Path` — `{stem}_typeset.png` via `ocr_json_target_dir` (D-03)
- `manga_ai_studio/gui/text_renderer.py` (NEW): `layout(text, style, box_rect, vertical=False) -> LayoutResult` (line/column rects, ink bounds, overflow flag; horizontal wrap + alignment + Auto-fit bounded loop), `paint(painter, result, style)` (opaque fill + glyph-path stroke outline), `bake_typeset_page(page_np, boxes, renderer) -> np.ndarray` (detached composite, D-04 content rule, 1:1), `current_focus_text(pb) -> str` (translation else recognized else "")
- `manga_ai_studio/gui/box_item.py` (MOD): renderer-driven opaque typeset overlay child (z=120, cached pixmap keyed (text, style, box_size, vertical)), `_OVERLAY_FILL/_OVERLAY_OUTLINE/_OVERLAY_FONT` removed; `_OVERLAY_FIT_*`/`_OVERLAY_INSET` kept
- `manga_ai_studio/gui/main_window.py` (MOD): `action_export_typeset` ("Export Typeset…", Ctrl+Shift+B, File ▸ Export after Export OCR JSON), `_on_export_typeset` handler (flush → dialog → composite → save_image_optimized → flash/error), `_refresh_action_states` gate
- Tests: `tests/test_core/test_text_style.py` (NEW), `tests/test_core/test_typeset_layout.py` (NEW), `tests/test_core/test_typeset_bake.py` (NEW), `tests/test_core/test_history_boxes.py` (extended: style-undo regression), `tests/test_gui_export.py` (extended: `test_typeset_export_action`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCR/translation text → renderer | Untrusted text (Phase 4 OCR + manual input) crosses into the painter; must never become rich text |
| `.mas`/`_ocr.json` style dict → TextStyle | Untrusted file-boundary data (huge sizes, wrong types) crosses into the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | text_renderer/box_item text paths | medium | mitigate | Plain-text rendering ONLY — `setPlainText`/QTextLayout plain runs, never `setHtml` on OCR/translation content (ASVS V5; Phase 4 Pattern 3 discipline); the Task 1 grep gate + renderer tests lock it |
| T-07-02 | Tampering | TextStyle.from_dict | medium | mitigate | Strict type-check + clamp every numeric (size 1..1024, widths/radii 0..256, opacity 0..1), unknown keys ignored, non-numeric → defaults without raising (Pitfall 8 backward compat); test_text_style.py clamps lock it |
| T-07-03 | DoS | bake compositor memory | low | accept | Works on a detached page copy; page size bounded by MAX_IMAGE_DIMENSION at load (canvas.py:79, existing); inline single-page composite is sub-second (A4); no Worker required |
| T-07-04 | Tampering | loguru bake logs | low | accept | Logs stems + error strings only, never raw OCR/translation text (Phase 2 batch-report discipline, unchanged) |
| T-07-SC | Tampering | pip installs | high | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit: all pre-installed, no install step). No checkpoint needed. |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_text_style.py tests/test_core/test_typeset_layout.py tests/test_core/test_typeset_bake.py tests/test_core/test_history_boxes.py tests/test_gui_export.py -q`
- Full suite: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (600-test baseline green at phase start)
- Grep gates: `rg "setHtml" manga_ai_studio/gui/text_renderer.py` → nothing; `rg "_OVERLAY_FILL|_OVERLAY_OUTLINE|_OVERLAY_FONT\b" manga_ai_studio/gui/box_item.py` → nothing
</verification>

<success_criteria>
- One box with default styling renders opaque on canvas AND bakes to a D-03-placed sidecar (end-to-end tracer slice proven)
- TextStyle model: defaults, round-trip, V5 clamps, copy detachment all test-locked
- canvas ≡ bake equivalence pinned by a pixel test; overflow unclipped; Auto-fit floor preserved
- No new dependencies; no vendored-type changes; full suite green
</success_criteria>

<output>
Create `.planning/phases/07-typesetting-tran-02-render-translated-text-into-the-page/07-01-SUMMARY.md` when done
</output>




