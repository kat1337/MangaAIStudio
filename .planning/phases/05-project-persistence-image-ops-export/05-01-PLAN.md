---
phase: 05-project-persistence-image-ops-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/project_io.py
  - manga_ai_studio/core/image_file.py
  - manga_ai_studio/core/image_io.py
  - tests/test_core/test_project_io.py
autonomous: true
requirements: [PROJ-01]
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - ".mas page files round-trip: PageBox (box/origin/edited/bubble_no/manual_override + TextBlock text/translation/lines/vertical/language/font_size) survives save_page_file -> load_page_file; PageBox.mask/std_dev stay None (D-15 seam)"
    - "All JSON content (manifest.json, .mas meta entries) is written and read as UTF-8"
    - "Re-saving a project overwrites only the owned manifest.json + per-page .mas files; foreign folder content is never deleted (D-02 non-destructive)"
    - "A save interrupted mid-write cannot corrupt the existing project on disk (atomic temp-file + os.replace) -- held-out test"
    - "Load rejects corrupt/newer-version input with ProjectFormatError (bad magic, memlimit breach, oversized dims); ints coerced; no partial session"
    - "D-06: original verified iff the referenced path resolves with an image suffix AND its sha256 matches -> ImageFile.original_verified=True; otherwise False (embedded image is the base)"
    - "A page .mas with a sibling manifest.json is detectable via find_sibling_manifest (D-09)"
  artifacts:
    - "manga_ai_studio/core/project_io.py -- LZMA2 container, manifest, PageBox<->JSON mapping, sha256, sibling detection, validation"
    - "manga_ai_studio/core/image_file.py with original_verified + geometry_altered dataclass fields"
    - "manga_ai_studio/core/image_io.py with save_image_bytes helper (in-memory PNG encode)"
    - "tests/test_core/test_project_io.py (Wave 0 file for PROJ-01)"
  key_links:
    - "load_page_file decompression bounded by memlimit -- entry table sizes vs lzma FORMAT_XZ blobs (T-05-01)"
    - "json_to_pagebox int coercion mirrors Phase 3 V5 textblock_to_box discipline (T-05-02)"
    - "manifest page order feeds the session sidebar rebuild (plan 05-05)"
---

<objective>
**As a** scanlator, **I want to** save and resume my full workspace, apply basic image operations, and export OCR data, **so that** the editor is a resumable, interoperable tool. *(User story derived from the ROADMAP Phase 5 Goal line — the goal is not literally in user-story format; this framing is for plan orientation only.)*

Wave 1 of PROJ-01: the headless `.mas` serialization core. Build `core/project_io.py` — the LZMA2 (FORMAT_XZ) page-file container (D-04), the plain-JSON chapter manifest (D-01/D-02), the PageBox↔JSON mapping (D-03/D-17/D-15-seam), the sha256 original-verification rule (D-06), sibling-manifest detection (D-09), and untrusted-file validation (T-05-01/T-05-02). Extend `ImageFile` with the `original_verified` and `geometry_altered` flags, and `image_io.py` with an in-memory PNG encoder. Pure stdlib + numpy + PIL — no Qt — so the module is worker-safe and headless-testable (mirrors `core/image_io.py`).

Purpose: every other plan in the phase reads or writes this module — plan 05-05 (Save/Open Project GUI), 05-08 (export uses the same PageBox state), and the geometry-altered flag consumed by D-22. The container format is a one-way published layout (D-04); the tracer proves the full round-trip architecture before any GUI work layers on.

Output: `core/project_io.py`, extended `image_file.py` + `image_io.py`, `tests/test_core/test_project_io.py` all green (baseline measured at plan time 2026-08-08: 463 collected / 462 passed / 1 failed — the sole failure is the pre-existing tests/test_gui_boxes.py::test_moved_box_via_real_events_persists_round_trip regression, remediated by plan 05-09 in Wave 1; re-measure at execution).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@.planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md
@.planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md
@.planning/phases/05-project-persistence-image-ops-export/05-VALIDATION.md

@manga_ai_studio/core/image_io.py
@manga_ai_studio/core/image_file.py
@manga_ai_studio/core/box_model.py
@panelcleaner/comic_text_detector/utils/textblock.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): .mas page-file container + PageBox↔JSON mapping — one full round-trip</name>
  <reversibility rating="one-way">D-04 locked the LZMA2 container; the entry-table layout is the saved-project format a reader migration would be needed to change. (User decision D-04 already walked this door — no checkpoint:decision inserted; rating kept for the record.)</reversibility>
  <files>manga_ai_studio/core/project_io.py, tests/test_core/test_project_io.py</files>
  <read_first>
    - manga_ai_studio/core/image_io.py (the module template: docstring source/dependency contract, validation-before-I/O, pure-stdlib discipline — mimic its shape exactly)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pattern 1 lines 238-283 — the container layout; Common Operation 1 lines 464-491 — the PageBox→JSON mapping; Pitfall 6 lines 430-434 — memlimit; Anti-Pattern 1 — never TextBlock.to_dict())
    - .planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md (project_io assignment lines 33-73 — the module contract)
    - manga_ai_studio/core/box_model.py (PageBox dataclass fields + the DETECTED/USER origin constants at lines 49-50; D-15 seam: mask/std_dev are NOT serialized)
    - panelcleaner/comic_text_detector/utils/textblock.py (TextBlock field set — xyxy/lines/vertical/language/font_size/text/translation; the payload to map, NOT to_dict())
    - tests/test_core/test_image_io.py (the test-file shape to mirror: module docstring, tmp_path usage, @pytest.mark.unit)
  </read_first>
  <action>
    Create `manga_ai_studio/core/project_io.py` with a module docstring stating origin (our own module, no vendoring — PROJ-04 greenfield per 05-CONTEXT canonical refs), the dependency contract ("imports ONLY stdlib + numpy + Pillow — no Qt, no torch, no models — safe from a worker thread, headless-testable"), and GPL v3 attribution. Imports: json, lzma, struct, hashlib, os, tempfile, pathlib, numpy as np, PIL Image.

    Implement per RESEARCH Pattern 1 verbatim:
    - Module constants `_MAGIC = b"MAS\x00"` and `_FORMAT_VERSION = 1`.
    - `_pack_entry(name: str, payload: bytes) -> bytes` — LZMA2-compress with `lzma.compress(payload, format=lzma.FORMAT_XZ, preset=6)` (preset 6 = default; preset 9's ~800 MiB compressor overhead is a regression, A6), then `struct.pack("<HQ", len(name_b), len(data)) + name_b + data`.
    - `save_page_file(path: Path, entries: dict[str, bytes]) -> None` — header `_MAGIC + struct.pack("<II", _FORMAT_VERSION, len(entries))` + concatenated entries. Validate `path` parent exists or create it (mkdir parents=True exist_ok=True — image_io.py:105 pattern). Atomic write: write to a temp file in the same directory then `os.replace` (the interrupted-save backstop; also used by Task 2's save_project).
    - `load_page_file(path: Path) -> dict[str, bytes]` — read bytes, raise `ProjectFormatError` (define this exception in this module) if the magic is wrong or the header is truncated. Unpack the entry table; decompress each blob with `lzma.decompress(blob, format=lzma.FORMAT_XZ, memlimit=MAX_ENTRY_DECOMPRESSED)` where `MAX_ENTRY_DECOMPRESSED = 512 * 1024 * 1024` (512 MiB) — an `lzma.LZMAError` on memlimit breach propagates as ProjectFormatError (T-05-01). Wrap the ENTIRE header + entry-table unpack in try/except (struct.error, IndexError) → ProjectFormatError — a truncated header, an entry count that overruns the blob, or a garbage name/length prefix must never escape as a raw struct.error/IndexError (T-05-01 entry-table size sanity; covered by test_malformed_entry_table_rejected). Do NOT validate dims here (that needs meta.json — Task 3).

    PageBox mapping per Common Operation 1 (hand-picked fields; NEVER TextBlock.to_dict()):
    - `pagebox_to_json(pb: PageBox) -> dict` — box `list(pb.box.as_tuple)` [x1,y1,x2,y2]; origin; edited; bubble_no; manual_override; payload: None if pb.payload is None else {xyxy: list(payload.xyxy), lines: payload.lines, vertical, language, font_size, text, translation}. Never write mask/std_dev (D-15 seam).
    - `json_to_pagebox(d: dict) -> PageBox` — int-coerce every numeric field with `int()` (Phase 3 V5 discipline); construct a fresh `Box(int,int,int,int)` (never mutate — Box is @frozen; build new); payload None if `d["payload"] is None`; else construct `TextBlock([x1,y1,x2,y2], lines=..., vertical=..., language=...)` then assign text/translation/font_size directly (constructor shape per textblock.py — confirm the xyxy-first signature in read_first). Preserve origin/edited/bubble_no/manual_override. Unknown keys are ignored; missing required keys raise ProjectFormatError.

    Tests (RED-first shape is not required here — standard tracer): `test_page_file_round_trip` (write a page file with meta.json/image.png/mask.bin/original.json entries incl. a non-trivial binary blob; read back; assert entries equal byte-for-byte) and `test_pagebox_json_round_trip` (a PageBox with a full TextBlock payload — multi-line text with a newline, translation, 2 line quads, vertical=True, bubble_no=3, manual_override=True, edited=True, origin="user" — must survive pagebox_to_json → json_to_pagebox with equal fields; assert payload.lines equality element-wise). Also `test_bad_magic_rejected` (a file with a wrong 4-byte prefix raises ProjectFormatError) and `test_malformed_entry_table_rejected` (3 sub-cases: a truncated header; an entry count claiming more entries than the blob holds; a garbage length prefix that overruns the remaining bytes — EACH raises ProjectFormatError, never struct.error/IndexError).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_project_io.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from manga_ai_studio.core.project_io import save_page_file, load_page_file, pagebox_to_json, json_to_pagebox, ProjectFormatError"` exits 0
    - test_page_file_round_trip passes (entries byte-identical after round-trip; mask.bin binary blob preserved exactly)
    - test_pagebox_json_round_trip passes (text with a literal newline round-trips; lines quads element-equal; bubble_no/origin/edited/manual_override/vertical preserved)
    - test_bad_magic_rejected passes (wrong magic raises ProjectFormatError, not a generic exception)
    - test_malformed_entry_table_rejected passes (all 3 sub-cases raise ProjectFormatError — no raw struct.error/IndexError escapes load_page_file)
    - `grep -c "FORMAT_XZ" manga_ai_studio/core/project_io.py` is >= 3 (compress + decompress + docstring)
    - Full suite still green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (baseline measured at plan time: 463 collected / 462 passed / 1 failed — the sole pre-existing test_gui_boxes.py move regression is fixed by plan 05-09, same wave; re-measure at execution)
  </acceptance_criteria>
  <done>The .mas page container round-trips and PageBox↔JSON mapping round-trips with full payload fidelity; corrupt magic rejected; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Chapter manifest + page-state assembly + atomic non-destructive save + ImageFile flags</name>
  <reversibility rating="one-way">D-01/D-02/D-03 locked the chapter-split layout (manifest + per-page self-contained .mas beside the source); changing it later needs migration of saved projects. (User decisions D-01/D-02/D-03 already walked this door — no checkpoint:decision inserted; rating kept for the record.)</reversibility>
  <files>manga_ai_studio/core/project_io.py, manga_ai_studio/core/image_file.py, manga_ai_studio/core/image_io.py, tests/test_core/test_project_io.py</files>
  <read_first>
    - manga_ai_studio/core/project_io.py (this plan's Task 1 output — extend it)
    - manga_ai_studio/core/image_file.py (the ImageFile dataclass at lines 54-76 — add the two new fields; the .copy() discipline in load_thumbnail)
    - manga_ai_studio/core/image_io.py (save_image_optimized at lines 46-106 — the PNG kwargs to mirror: compress_level=9, DPI/mode preservation)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pattern 1 example JSON; Assumptions A3/A9 — manifest schema fields; Common Operation 3 resize encode style)
  </read_first>
  <action>
    Extend `core/image_io.py` with `save_image_bytes(image_rgb: np.ndarray) -> bytes`: encode the (H,W,3) uint8 RGB array to PNG bytes in memory via `PIL.Image.fromarray(image_rgb, "RGB")` + `BytesIO` + `pil.save(buf, format="PNG", compress_level=9)` (mirror save_image_optimized's PNG kwargs and its pre-write (H,W,3) uint8 validation — T-02-01 boundary). Return buf.getvalue(). No disk I/O.

    Extend `core/image_file.py` ImageFile dataclass with two peer fields after `dirty: bool = False`: `original_verified: bool = False` (D-06: set True during .mas load when path-ref + sha256 match; normal image/folder open also sets it True — the source file was loaded from that path) and `geometry_altered: bool = False` (D-22: set True when a crop/rotate/resize op applies; levels does NOT set it — A4). Update the dataclass docstring for both.

    Extend `core/project_io.py`:
    - `build_page_entries(page_state) -> dict[str, bytes]` — a per-page save projection. Input is a simple dataclass/dict carrying: boxes (list[PageBox]), image_rgb (np.ndarray (H,W,3) uint8), mask_bin (np.ndarray (H,W) uint8 or None), geometry_altered (bool), original_path (Path or None), original_sha256 (str or None). Returns 4 entries: `meta.json` (UTF-8 JSON: {"version": 1, "img": {"w", "h"}, "mask": {"h", "w"} or null, "geometry_altered": bool, "original": {"path": str, "sha256": str} or null, "boxes": [pagebox_to_json...]}), `image.png` (save_image_bytes), `mask.bin` (mask_bin.tobytes() raw, or omitted when no mask), `original.json` (UTF-8 JSON {"path", "sha256"} — the D-06 ref, present even when the source no longer exists on disk). mask shape is recorded in meta.json so load can reshape without trusting blob length alone.
    - `save_project(project_dir: Path, project_name: str, page_files: list[tuple[str, dict[str, bytes]]]) -> Path` — writes `manifest.json` (PLAIN JSON, UTF-8, human-readable, diffable per D-04: {"version": 1, "name": project_name, "pages": [{"name": stem, "file": "<stem>.mas"}]}) then each `<stem>.mas` via save_page_file. Non-destructive overwrite (D-02 collision copy): only manifest.json and *.mas files in project_dir are ever written or replaced; other content is untouched. Every file write is atomic (temp + os.replace). Returns the manifest path.
    - `load_project(manifest_path: Path) -> dict` — validate magic-free plain JSON: {"version", "name", "pages": [...]}; int-coerce version; pages entries must have str name + file; raise ProjectFormatError on malformed structure. Returns the parsed manifest dict (callers rebuild the session — plan 05-05).
    - `parse_page_entries(entries: dict[str, bytes]) -> dict` — the load-side mirror of build_page_entries: decode meta.json (UTF-8), reshape mask.bin via meta["mask"] dims, decode original.json. Raw dict out; typed model construction happens at the GUI boundary (plan 05-05) so this module stays Qt-free.

    Tests: `test_manifest_round_trip` (save_project → load_project: order + names preserved), `test_non_destructive_overwrite` (put a foreign file `notes.txt` in an existing .mas-project dir, save_project again → notes.txt still present and unchanged; owned manifest.json + .mas replaced), `test_save_is_atomic` (backstop held-out test: monkeypatch the write of one page's .mas to raise mid-write, assert the pre-existing project files are byte-identical afterward — the temp-file scheme never corrupts the target), `test_image_file_flags_default` (ImageFile() defaults original_verified False, geometry_altered False), `test_save_image_bytes_round_trip` (bytes decode back to the same dims via PIL open).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_project_io.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_manifest_round_trip passes (manifest order == save order; name preserved; UTF-8 — non-ASCII project name round-trips)
    - test_non_destructive_overwrite passes (foreign file untouched after re-save)
    - test_save_is_atomic passes (the interrupted-write backstop — mid-write failure leaves the prior project intact)
    - test_save_image_bytes_round_trip passes
    - `grep -v '^#' manga_ai_studio/core/image_file.py | grep -c "original_verified"` is >= 1 and `grep -v '^#' manga_ai_studio/core/image_file.py | grep -c "geometry_altered"` is >= 1
    - `python -c "from manga_ai_studio.core.image_io import save_image_bytes"` exits 0
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Manifest + page-state assembly round-trips; re-save is non-destructive and atomic; ImageFile carries both flags; committed.</done>
</task>

<task type="auto">
  <name>Task 3: D-06 sha256 checksum rule + D-09 sibling-manifest detection + untrusted-file validation</name>
  <files>manga_ai_studio/core/project_io.py, tests/test_core/test_project_io.py</files>
  <read_first>
    - manga_ai_studio/core/project_io.py (this plan's Tasks 1-2 output — extend it)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Security Domain lines 725-737 — the threat mitigations; Open Question 6 — original_verified flag; A7 — chunked sha256; Pitfall 6 — memlimit)
    - manga_ai_studio/gui/canvas.py lines 79 and 114-126 (MAX_IMAGE_DIMENSION = 10000 and validate_image_path — the suffix-allowlist pattern to mirror; read the surrounding function once)
  </read_first>
  <action>
    Extend `core/project_io.py` (still stdlib+numpy+PIL only — the suffix check below is pure pathlib, no Qt):
    - `sha256_file(path: Path) -> str` — chunked (1 MiB reads) sha256 hexdigest (A7; stdlib hashlib).
    - `_IMAGE_SUFFIXES = {".png", ".jpg", ".jpeg", ".webp", ".bmp", ".tif", ".tiff"}` — the D-06 allowlist mirroring canvas.validate_image_path's suffixes.
    - `verify_original(original_path_str: str, expected_sha256: str) -> bool` — returns False (never raises) when the path resolves to nothing, its suffix is not in the allowlist, or the checksum mismatches. Resolve via `Path(...).resolve()` before suffix check (path-traversal guard T-05-03 — a crafted manifest ref pointing at an arbitrary file fails the suffix gate; a matching-suffix wrong file fails the checksum).
    - `find_sibling_manifest(page_mas_path: Path) -> Path | None` — return `page_mas_path.parent / "manifest.json"` iff it exists AND `load_project` on it succeeds (a corrupt sibling manifest must NOT silently open the page — the D-09 climb relies on a valid manifest); else None. Raise ProjectFormatError when the sibling manifest exists but is malformed (the caller decides the dialog vs error — plan 05-05).
    - Load-side validation in `load_page_file`'s meta path (add a `validate_meta(meta: dict) -> None` helper called by parse_page_entries): img w/h must be ints within [1, MAX_IMAGE_DIMENSION] where `MAX_IMAGE_DIMENSION = 10000` (canvas.py:79 constant — duplicate the literal with a comment naming the source); mask dims must equal img dims when both present; geometry_altered must be bool; raise ProjectFormatError otherwise. Every int field in meta coerces via int() with a try/except → ProjectFormatError on TypeError/ValueError (T-05-02 — JSON type confusion; the textblock_to_box discipline).
    - Bounded chapter: `load_project` rejects `len(pages) > MAX_PROJECT_PAGES` (1000 — an oversized chapter is a DoS signal, T-05-04) with ProjectFormatError.

    Tests: `test_original_checksum_rule` (3 cases: matching sha256 → True; wrong-sha256 file at the same path → False; missing path → False; also a non-image-suffix path pointing at an existing file → False), `test_sibling_manifest_detection` (page .mas next to a valid manifest.json → returns it; no manifest → None; manifest.json present but malformed → ProjectFormatError), `test_d15_seam_preserved` (a PageBox with mask/std_dev set to non-None objects serializes via pagebox_to_json WITHOUT those fields in the dict — assert the keys are absent — and json_to_pagebox yields mask/std_dev None), `test_meta_validation` (img dims > 10000 or non-int → ProjectFormatError; mask dims ≠ img dims → ProjectFormatError; geometry_altered non-bool → ProjectFormatError), `test_oversized_chapter_rejected` (manifest with 1001 pages → ProjectFormatError).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_project_io.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_original_checksum_rule passes (all 4 sub-cases)
    - test_sibling_manifest_detection passes (3 sub-cases incl. the corrupt-sibling error)
    - test_d15_seam_preserved passes (mask/std_dev never serialized, always None after round-trip)
    - test_meta_validation + test_oversized_chapter_rejected pass
    - `grep -c "def sha256_file" manga_ai_studio/core/project_io.py` is 1 and `grep -c "def find_sibling_manifest" manga_ai_studio/core/project_io.py` is 1 and `grep -c "memlimit" manga_ai_studio/core/project_io.py` is >= 1
    - `grep -v '^#' manga_ai_studio/core/project_io.py | grep -c "MAX_IMAGE_DIMENSION"` is >= 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Checksum rule, sibling detection, and untrusted-input validation complete with regression coverage; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05-01)

- `manga_ai_studio/core/project_io.py` (NEW) — `ProjectFormatError`, `_MAGIC`, `_FORMAT_VERSION`, `MAX_ENTRY_DECOMPRESSED`, `MAX_IMAGE_DIMENSION`, `MAX_PROJECT_PAGES`, `_IMAGE_SUFFIXES`, `_pack_entry`, `save_page_file`, `load_page_file`, `pagebox_to_json`, `json_to_pagebox`, `build_page_entries`, `parse_page_entries`, `save_project`, `load_project`, `validate_meta`, `sha256_file`, `verify_original`, `find_sibling_manifest`
- `manga_ai_studio/core/image_file.py` (EXTEND) — `ImageFile.original_verified: bool = False`, `ImageFile.geometry_altered: bool = False`
- `manga_ai_studio/core/image_io.py` (EXTEND) — `save_image_bytes(image_rgb) -> bytes`
- `tests/test_core/test_project_io.py` (NEW) — 10+ test functions

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → app (`.mas` / `manifest.json` open) | Untrusted saved-project files cross here: crafted magic, malformed entries, oversized blobs/dims, hostile `original` path refs |
| app → disk (`.mas` save) | Mid-write failure must not corrupt an existing project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | DoS | `load_page_file` (lzma decompress) | high | mitigate | `memlimit=MAX_ENTRY_DECOMPRESSED` (512 MiB) on every decompress + header/entry-table unpack wrapped (struct.error/IndexError → ProjectFormatError — test_malformed_entry_table_rejected); LZMAError → ProjectFormatError (RESEARCH Pitfall 6 / T-01-16 analog) |
| T-05-02 | Tampering | `json_to_pagebox` / `validate_meta` | medium | mitigate | int() coercion + bounds clamp per field; required-key checks; ProjectFormatError on type confusion — no partial session (Phase 3 V5 `textblock_to_box` discipline, box_model.py:181-204) |
| T-05-03 | Tampering / Information disclosure | `verify_original` (manifest `original` ref) | medium | mitigate | `Path.resolve()` + image-suffix allowlist before use; checksum mismatch falls back to embedded image — the ref never opens arbitrary files (canvas.py:114-126 pattern) |
| T-05-04 | DoS | `load_project` (oversized chapter) | medium | mitigate | `MAX_PROJECT_PAGES` (1000) bound + per-page meta dims ≤ MAX_IMAGE_DIMENSION (10000); error path keeps the previous session intact (plan 05-05 owns the intact-session UX) |
| T-05-05 | Tampering | `sha256_file` integrity (D-06) | low | mitigate | stdlib hashlib chunked sha256 is the standard control; used for integrity, not confidentiality (ASVS V6 n/a) |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this phase — RESEARCH Package Legitimacy Audit reviewed (all `[SUS-unknown-downloads]`, already-installed, zero new deps); no checkpoints needed |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_project_io.py -x -q`
- Per wave merge: full suite `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (~1-2 min; baseline measured at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09, same wave; re-measure at execution)
- The atomic-save backstop is held-out in this plan's test suite (test_save_is_atomic) — REQUIRED to pass, per the probe's backstop item.
</verification>

<success_criteria>
- `.mas` page files + manifest save/load round-trip at the bytes and model level
- D-06 checksum rule, D-09 sibling detection, and the full untrusted-input validation suite green
- `ImageFile.original_verified` / `geometry_altered` fields exist with documented semantics
- All new tests pass with the full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-01-SUMMARY.md` when done.
</output>
