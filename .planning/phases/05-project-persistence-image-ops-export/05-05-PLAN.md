---
phase: 05-project-persistence-image-ops-export
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-04]
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_project.py
autonomous: true
requirements: [PROJ-01]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Save Project… (Ctrl+S) / Save Project As… (Ctrl+Shift+S) / Open Project… (Ctrl+O) exist in the File menu; Open Image… no longer binds Ctrl+O (the old binding removed — Qt never fires both)"
    - "Save Project… is disabled when no page is open; saving a clean session flashes the transient no-changes message and writes nothing"
    - "Dirty tracking: any page mutation marks the session dirty; the window title appends * as the last character; Save clears the dirty state"
    - "The Unsaved Changes prompt [Save][Discard][Cancel] runs on Quit, window close, Open Project…, Open Image…, and Open Folder… when the session is dirty (D-07)"
    - "Recent Projects submenu: 'No recent projects yet.' empty item when the list is empty; 'Project — {folder-name}' entries with full-path tooltips, max 8, QSettings-persisted, plus Clear Menu"
    - "Open Project… rebuilds the session from the manifest: sidebar order restored, per-page masks/boxes/text restored via the Phase 2 D-11 seam, EVERY page's embedded image decoded into ImageFile.current_image (D-05/D-08 — batch export and re-save read per-page dims from it; originals may be missing), fresh undo history (D-05), originals re-found via path + sha256 (D-06 -> ImageFile.original_verified)"
    - "Opening a page .mas with a sibling manifest pops the Chapter Detected prompt (Open Project default / Open Page Only; Esc cancels); a corrupt/missing manifest errors with the corrupt-project copy and never mutates the current session"
    - "Menu actions gate on _op_running (disabled during async ops)"
  artifacts:
    - "manga_ai_studio/gui/main_window.py: _save_project/_save_project_as/_open_project/_load_project_session/_confirm_discard_changes/_add_recent_project/_refresh_recent_projects_menu + _snapshot_current_page + title/dirty wiring"
    - "tests/test_gui_project.py (pytest-qt, real MainWindow + tmp_path)"
  key_links:
    - "save-side flush reads _last_page_index + _flush_current_canvas_mask_to_data_model + canvas.boxes_snapshot + get_image_numpy (Pitfall 7 — never _current_page_index())"
    - "core/project_io (plan 05-01) save_project/load_project/build_page_entries/parse_page_entries — the only disk boundary"
    - "load-side mask/boxes restore mirrors the Phase 2 D-11 seam with .copy() at both boundaries (Pitfall 2)"
    - "per-page ImageFile.current_image population at load is the batch-export dims fallback (plan 05-08) — without it, non-current pages of reopened projects with missing originals have no dims source (D-08)"
---

<objective>
The Save/Open Project session layer of PROJ-01 (D-07/D-08/D-09): wire the File-menu project actions — Save Project… (Ctrl+S), Save Project As… (Ctrl+Shift+S), Open Project… (Ctrl+O with the old Open Image… binding removed per Pitfall 8), Recent Projects (QSettings, max 8), the dirty-`*` window title, the Unsaved Changes [Save][Discard][Cancel] gate, and the D-09 chapter-climb dialog — orchestrating `core/project_io` (plan 05-01) against the live canvas via the Phase 2 D-11 flush/restore seam.

Purpose: this is the "resumable workspace" user-facing core of PROJ-01 — the tracer path (Save → disk → Open → restored session) is Task 1; the gate/Recent/prompt surfaces expand from it. Plan 05-06's image ops and 05-08's export consume the session this plan establishes.

Output: `main_window.py` session wiring + `tests/test_gui_project.py` green (baseline measured at plan time 2026-08-08: 463 collected / 462 passed / 1 failed — sole pre-existing test_gui_boxes.py regression fixed by plan 05-09, Wave 1; re-measure at execution).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@.planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md
@.planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md
@.planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md
@.planning/phases/05-project-persistence-image-ops-export/05-VALIDATION.md

@manga_ai_studio/gui/main_window.py
@manga_ai_studio/core/project_io.py
@manga_ai_studio/core/mask_editor.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): Save Project… end-to-end — flush canvas → disk → title/dirty, one path</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_project.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_build_file_menu at 258-321 — the QAction creation pattern + menu assembly; _refresh_action_states at 724-804 — the enablement gate; setWindowTitle call at 1017 + line 203; _flush_current_canvas_mask_to_data_model at 3123-3142 + _last_page_index usage; on_page_selected D-11 seam at 916-946; action_open_image shortcut at line 261 — THE line to change)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pitfall 7 lines 436-440 — the outgoing-index rule; Common Operation 4 lines 554-566 — _snapshot_current_page shape; Open Question 5 — non-destructive folder collision)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 21 File menu structure + §Copywriting save/open/no-changes rows + gating rules — copy is asserted via tests, not grepped)
    - manga_ai_studio/core/project_io.py (build_page_entries/save_project signatures — the disk boundary)
  </read_first>
  <action>
    In `main_window.py`:

    - `_build_file_menu` additions per UI-SPEC surface 21 (executor-authoritative structure): `Save Project…` (QAction, shortcut Ctrl+S, tooltip per UI-SPEC §Copywriting, disabled-init True) → slot `_save_project`; `Save Project As…` (Ctrl+Shift+S) → `_save_project_as`; `Open Project…` (Ctrl+O) → `_open_project`; a `Recent Projects ▸` submenu (structure wired in Task 3). Insert order: Open Image… / Open Folder… / separator / Open Project… / Recent Projects ▸ / Recent Files ▸ / separator / Save Project… / Save Project As… / separator / Export Page… / Batch ▸ / separator / Quit. IMPORTANT (Pitfall 8): remove `self.action_open_image.setShortcut(QKeySequence("Ctrl+O"))` at line 261 — Open Image… keeps its action but loses the binding; Open Project… takes Ctrl+O. Verify no other action binds Ctrl+O.
    - `_project_dir: Path | None = None` attribute (None = unsaved session; Save Project… routes to the As… flow).
    - `_snapshot_current_page() -> None` — per RESEARCH Common Operation 4: `idx = self._last_page_index` (NEVER _current_page_index() — Pitfall 7); when in range: flush `self.image_files[idx].mask = self.canvas.get_mask().copy()` when canvas.has_mask() (else leave the slot as-is), `self.image_files[idx].boxes = self.canvas.boxes_snapshot()`, and capture `self.image_files[idx].current_image = self.canvas.get_image_numpy()` — add an `ImageFile.current_image: np.ndarray | None = None` dataclass field in core/image_file.py (plan 05-01 added the flags; this runtime slot holds the canvas numpy at save time, .copy() detached).
    - `_save_project(force_as: bool = False) -> None` — gate: no page open → return; session not dirty AND _project_dir set → transient no-changes flash (UI-SPEC §Copywriting status row) and return; flush current page (`_snapshot_current_page`); when force_as or _project_dir None → `_save_project_as` flow: `QFileDialog.getExistingDirectory`-style folder dialog defaulting to `<source-parent>/<chapter-name>.mas-project` (single-image session: `<image-parent>/<image-stem>.mas-project`); then build `page_files` from image_files with the PER-PAGE IMAGE SOURCE rule per RESEARCH Assumption A3: the CURRENT page (index == `self._last_page_index`) uses the just-flushed `current_image`; each NON-CURRENT page's image is `cleaned/<stem>` if that file exists in the page's source dir, ELSE the source file at `ImageFile.path` (read via `np.asarray(PIL.Image.open(path)).copy()` — Pitfall 2; the canvas holds only the live page, A3) — each page: build_page_entries with boxes/mask_bin via `mask_to_numpy_binary` (mask_editor.py:186-187) or None/geometry_altered/original path+sha256 via sha256_file; then call `project_io.save_project(project_dir, name, page_files)`; set `_project_dir`; clear all `ImageFile.dirty`; `_add_recent_project`; update window title (dirty suffix removed); transient saved flash (UI-SPEC §Copywriting status row). Wrap the disk call: on OSError → critical dialog with the save-failure copy + loguru traceback (T-05-12).
    - Wire the dirty-tracking seed for THIS plan's scope: connect the existing mutation signals the current page already emits (canvas.mask_modified, canvas.boxes_modified) + the inpaint-finish handler + OCR commit handlers to a `_set_session_dirty()` helper that marks the current ImageFile.dirty and refreshes the title. Image-op/crop dirtying arrives with plans 05-06/05-07.
    - Window title format (D-07): `Manga AI Studio — {project-name} — {page-filename}*` (project open) / `Manga AI Studio — {page-filename}*` (no project); `*` is the LAST character; Save clears it.

    Tests (`tests/test_gui_project.py` — pytest-qt, real MainWindow, tmp_path): `test_save_project_writes_project_folder` (open a 2-page folder session, dirty it, _save_project → the .mas-project folder exists with manifest.json + 2 .mas files; manifest page order matches the sidebar), `test_save_clean_session_no_changes_flash` (fresh session, Ctrl+S → no folder written, transient flash text shown), `test_save_disabled_with_no_page` (fresh MainWindow → action disabled), `test_ctrl_o_opens_project_not_image` (only ONE action binds Ctrl+O), `test_dirty_title_suffix` (mutation → title ends with *; save → suffix gone).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_save_project_writes_project_folder passes (manifest + 2 page files on disk; order preserved)
    - test_save_clean_session_no_changes_flash, test_save_disabled_with_no_page, test_ctrl_o_opens_project_not_image, test_dirty_title_suffix pass
    - `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'Ctrl+O'` is exactly 1 (only Open Project… binds it)
    - `grep -c "def _snapshot_current_page" manga_ai_studio/gui/main_window.py` is 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Save Project… writes a valid .mas-project folder end-to-end with the flush seam and title/dirty wiring; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Open Project… — session rebuild via the D-11 seam + chapter-climb (D-09) + corrupt-file handling</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_project.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (on_page_selected steps 1/1b at 916-946 — the restore seam mirror; _set_pages (sidebar rebuild); _build_file_menu recent structure; the error-dialog pattern from load_translations_dialog.py:168-176; canvas.set_image_from_numpy/set_mask/set_boxes call shapes)
    - manga_ai_studio/core/project_io.py (load_project/parse_page_entries/verify_original/find_sibling_manifest — the read boundary; ProjectFormatError)
    - manga_ai_studio/core/mask_editor.py (numpy_binary_to_mask_qimage at 206-207 — the mask bytes→QImage boundary)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 22 — the climb dialog semantics; §Copywriting corrupt-project + open-status rows; surface 21 open contract)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pitfall 2 — .copy() on every load boundary; D-05 fresh-history contract)
  </read_first>
  <action>
    In `main_window.py`:

    - `_open_project() -> None` — `QFileDialog.getOpenFileName(self, title, "", "Manga AI Studio Project (manifest.json *.mas)")`; empty path → return. Route: if the chosen file's name is `manifest.json` → `_load_project_session(manifest_path)`. If it ends with `.mas` → `sibling = find_sibling_manifest(path)`; sibling found → Chapter Detected prompt (UI-SPEC surface 22): modal QMessageBox with the chapter-detected copy, buttons [Open Project] (default) → `_load_project_session(sibling)` and [Open Page Only] → `_load_single_page_mas(path)`; Esc → cancel the action entirely (previous session untouched). No sibling → `_load_single_page_mas(path)` directly. Wrap the whole flow: ProjectFormatError/OSError → critical dialog with the corrupt-project copy + loguru traceback; the CURRENT session is never mutated on failure (build the new session fully BEFORE swapping).
    - `_load_project_session(manifest_path) -> None` — run the Unsaved-Changes gate first — `_confirm_discard_changes()` and its `_session_dirty()` helper are created HERE in this task (both `_load_project_session` and `_load_single_page_mas` need them; Task 3 only wires the gate into the remaining entry points). Parse via load_project; per page (in manifest order): parse_page_entries → build `ImageFile` (path = original ref when verify_original passes, else the page stem path inside the project — a placeholder `Path` with the original stem so the sidebar shows the right name; `original_verified` per the D-06 result); restore mask via `numpy_binary_to_mask_qimage(mask_bin)` with `.copy()` (Pitfall 2); restore boxes via `json_to_pagebox` list (Phase 3 setter path); set `geometry_altered` from meta; decode the `image.png` entry into `ImageFile.current_image` for EVERY page — `np.asarray(PIL.Image.open(BytesIO(entries["image.png"]))).copy()` (Pitfall 2) — NOT only the first (D-08 contract: plan 05-08's batch export and any re-save read per-page image dims from `current_image`; originals may be missing, so the embedded image is the only dims source for non-current pages). A corrupt blob raises → per-page failure handling per RESEARCH Security Domain (corrupt-image-blob row). Then rebuild the session: `_set_pages` (sidebar in manifest order), load the first page's embedded image onto the canvas via `canvas.set_image_from_numpy(image_files[0].current_image.copy())` (the D-05 current-image contract — reuse the already-decoded slot, no re-decode), `set_mask`, `set_boxes`, `self.history = HistoryManager(limit=20)` (fresh undo — D-05), `_project_dir = manifest_path.parent`, clear dirty, update title with the project name, transient opened flash (append the original-not-found note when any page's original_verified is False — UI-SPEC §Copywriting).
    - `_load_single_page_mas(path) -> None` — same Unsaved-Changes gate; load_page_file + parse_page_entries for the ONE page; decode the `image.png` entry into `ImageFile.current_image` (same `np.asarray(PIL.Image.open(BytesIO(...))).copy()` path as above — the standalone page's embedded image is its dims source for export/re-save) and display it via `canvas.set_image_from_numpy(current_image.copy())`; build a 1-page session exactly like a folder-open (sidebar single entry); Save Project… on it creates a single-page project (UI-SPEC surface 22).
    - `_refresh_action_states` — no change in this plan beyond what Task 1 added (Show Original gating lands in 05-06), but Open Project… / Save actions must be gated by `not self._op_running` here.

    Tests: `test_open_project_restores_session` (save a 2-page session via the Task-1 helper, open it: sidebar order + masks + boxes + text restored — assert ImageFile.mask non-null with content, boxes round-trip incl. text/translation, title shows the project name), `test_open_project_populates_all_current_images` (BOTH pages' ImageFile.current_image non-null with dims matching meta.json — including the non-displayed second page whose original was deleted — the 05-08 batch dims fallback), `test_open_page_mas_with_sibling_prompt` (page .mas + valid manifest → the prompt appears; Open Page Only loads one page), `test_open_corrupt_project_keeps_session` (corrupt manifest → critical dialog, previous session pages still present), `test_open_verified_original_flag` (page .mas whose original exists+matches → original_verified True; original deleted → False and the embedded image is used — assert set_image_from_numpy received the embedded image).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_open_project_restores_session passes (mask/boxes/text restored; order preserved; fresh history)
    - test_open_page_mas_with_sibling_prompt + test_open_corrupt_project_keeps_session pass
    - test_open_verified_original_flag passes (both D-06 branches)
    - `grep -c "def _load_project_session" manga_ai_studio/gui/main_window.py` is 1 and `grep -c "def _load_single_page_mas" manga_ai_studio/gui/main_window.py` is 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Open Project… rebuilds the session with D-06/D-08/D-09 semantics and never corrupts the previous session; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Unsaved Changes gate + Recent Projects menu + action gating</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_project.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_settings at 1051-1054, _add_recent_file at 1068-1074, _refresh_recent_menu at 1080-1099, _make_recent_opener at 1095-1099, MAX_RECENT_FILES = 8 at line 72 — the exact Recent Files pattern to duplicate for projects; _refresh_action_states at 724-804; closeEvent if present)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 21 Recent Projects contract + §Copywriting discard-prompt + empty-state rows)
  </read_first>
  <action>
    In `main_window.py`:

    - `_session_dirty()` and `_confirm_discard_changes()` were created in Task 2 (any `image_file.dirty` True → "save" | "discard" | "cancel" flow). `_set_session_dirty()` (Task 1) sets the current page's dirty + refreshes the title. THIS task wires the gate into: `closeEvent`, the Quit action, `_open_project` (before the file dialog), `_open_folder`/`_open_image` handlers (before they replace the session) — UI-SPEC surface 21's dirty-gate list.
    - `original_verified = True` wiring on normal image/folder open (D-06; the missing-owner fix): in the folder/image open paths where `ImageFile` objects are constructed (`_load_folder`/`_open_image` handlers — touched here by the gate wiring), set each page's `original_verified = True` — the page was loaded from its own path, so Show Original is valid (D-06 semantics). This is consumed by plan 05-06 Task 3's Show Original gating, whose `test_show_original_gating` folder-open branch requires it.
    - `_add_recent_project(project_dir: Path)` + `_refresh_recent_projects_menu()` + `MAX_RECENT_PROJECTS = 8` — duplicate the Recent Files machinery exactly (QSettings key `"recentProjects"`, entries "Project — {folder-name}" with the full path in the tooltip, max 8 drop-off, Clear Menu, empty-state disabled item "No recent projects yet." with muted styling). Entry click → `_open_project` at that manifest path (bypass the dialog).
    - `_refresh_action_states` — extend the gating: Save Project…/Save Project As… enabled iff ≥1 page open AND not _op_running; Open Project… + Recent Projects entries enabled iff not _op_running; (image-op + export actions gate in plans 05-06/05-08).

    Tests: `test_unsaved_changes_prompt_save_discard_cancel` (dirty session + window close → prompt appears; Discard proceeds; Cancel aborts the close), `test_recent_projects_menu` (after save, the submenu shows 'Project — {folder-name}' with tooltip; 9 saves → max 8 entries; Clear Menu empties it; fresh QSettings → the disabled empty item), `test_menu_gating_during_op` (simulate _op_running True → Save/Open actions disabled), `test_folder_open_sets_original_verified` (a normal folder-open session → every `ImageFile.original_verified` is True — the D-06 normal-open wiring; Show Original enabled).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_unsaved_changes_prompt_save_discard_cancel passes (all three buttons)
    - test_recent_projects_menu passes (label shape, cap 8, Clear Menu, empty item)
    - test_menu_gating_during_op passes
    - `grep -c "recentProjects" manga_ai_studio/gui/main_window.py` is >= 2 (read + write)
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>The dirty gate, Recent Projects, and action gating are complete and tested; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05-05)

- `manga_ai_studio/gui/main_window.py` (EXTEND) — `_project_dir`, `_set_session_dirty`, `_session_dirty` (Task 2), `_snapshot_current_page`, `_save_project`, `_save_project_as`, `_open_project`, `_load_project_session`, `_load_single_page_mas`, `_confirm_discard_changes` (Task 2), `_add_recent_project`, `_refresh_recent_projects_menu`, `MAX_RECENT_PROJECTS`; menu/action wiring + Ctrl+O remap; title/dirty format; folder/image-open `original_verified = True` wiring (D-06)
- `manga_ai_studio/core/image_file.py` (EXTEND) — `ImageFile.current_image: np.ndarray | None = None` (runtime save-time capture slot; populated for EVERY page at project load — Task 2)
- `tests/test_gui_project.py` (NEW) — 11+ pytest-qt test functions

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → session (project open) | Untrusted saved-project files rebuild the live session here — validation is core-side (plan 05-01); this layer must keep the previous session intact on failure |
| session → disk (project save) | Mid-write failures must surface without losing the in-memory session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | DoS / availability | `_save_project` / `_open_project` disk I/O | medium | mitigate | OSError → critical dialog with the save-failure copy + loguru traceback; the session is never partially swapped (build-then-swap load) — RESEARCH Security Domain corrupt-project row |
| T-05-13 | Tampering | corrupt/newer-version project files | high | mitigate | core-side ProjectFormatError on every open (plan 05-01 T-05-01/T-05-02); GUI shows the corrupt-project copy and leaves the previous session byte-identical (test_open_corrupt_project_keeps_session) |
| T-05-14 | Spoofing | Recent Projects entries (stale/wrong paths) | low | accept | QSettings-persisted path list is user-local convenience data; a stale entry errors with the standard open-error copy |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit); no checkpoints needed |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py -x -q`
- Per wave merge: full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</verification>

<success_criteria>
- Save/Open round-trip end-to-end (tracer), session rebuild with D-06/D-08/D-09 semantics, dirty gate, Recent Projects — all tested
- Ctrl+O collision resolved (exactly one binding)
- Full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-05-SUMMARY.md` when done.
</output>
