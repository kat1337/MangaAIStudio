---
phase: 05-project-persistence-image-ops-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/image_ops.py
  - tests/test_core/test_image_ops.py
autonomous: true
requirements: [PROJ-04]
user_setup: []

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Rotate 90° CW/CCW/180° is pixel-exact for image + binary mask (np.rot90 k=-1/1/2, one convention — no PIL/Qt direction ambiguity) and transforms every box bbox AND TextBlock.lines polygon (D-15/D-17/D-18)"
    - "90° rotations compose: two 180° rotations return the identity geometry; the transform functions never mutate the frozen Box or the caller's line lists (fresh payload copies)"
    - "Crop is an exact slice; fully-outside boxes are dropped (count returned), partially-inside boxes are clipped to the crop rect — bbox AND lines polygons (D-16)"
    - "Resize: LANCZOS image / NEAREST mask (no soft alpha drift, D-18); boxes scaled to int coords; levels: 256-entry numpy LUT, white>black clamped (never an inverted map), geometry-free (D-15)"
    - "Every transform returns .copy()-detached arrays (Pitfall 2)"
  artifacts:
    - "manga_ai_studio/core/image_ops.py -- rotate_page/rotate_boxes/crop_page/resize_page/resize_boxes/levels_page/levels_lut (pure numpy+PIL, no Qt)"
    - "tests/test_core/test_image_ops.py (Wave 0 file for PROJ-04)"
  key_links:
    - "mask boundary: mask_to_numpy_binary (mask_editor.py:186-187) in, numpy_binary_to_mask_qimage (206-207) out — image_ops works on the binary form only"
    - "geometry transforms consume PageBox.box.as_tuple + payload.lines quads and produce NEW PageBox objects (Pitfall 3: never in-place before undo push)"
---

<objective>
Wave 1 of PROJ-04: the headless pixel + geometry math module `core/image_ops.py` — rotate (90° steps, D-10), crop (D-11 slice semantics), resize (D-13), levels (D-12 LUT) plus the D-15/D-17/D-18 geometry transforms that carry masks and box geometry (bbox AND `TextBlock.lines` polygons) along with the image. Pure numpy + PIL, no Qt — the same headless discipline as `core/mask_editor.py`, so every transform is unit-testable without a display and safe to call from the apply path (plan 05-06/05-07) and any worker.

Purpose: PROJ-04's GUI plans (05-06 rotate/levels/resize, 05-07 crop) orchestrate; this module owns ALL pixel and geometry math. The geometry transforms are the phase's genuinely novel work (RESEARCH "Key insight") — bbox + line-polygon transforms must be correct here once, with the "never mutate the frozen Box / never mutate payload.lines in place" rules baked into the API shape (fresh PageBox per box).

Output: `core/image_ops.py` + `tests/test_core/test_image_ops.py` green (baseline measured at plan time 2026-08-08: 463 collected / 462 passed / 1 failed — sole pre-existing test_gui_boxes.py regression fixed by plan 05-09, same wave; re-measure at execution).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@.planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md
@.planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md
@.planning/phases/05-project-persistence-image-ops-export/05-VALIDATION.md

@manga_ai_studio/core/mask_editor.py
@manga_ai_studio/core/box_model.py
@panelcleaner/structures.py
@panelcleaner/comic_text_detector/utils/textblock.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): Rotate 90° CW/CCW/180° — image + mask + box/line geometry, one path end-to-end</name>
  <reversibility rating="costly">D-15's transform contract (geometry rides with the image) is the phase's core data invariant; every later op and the export share this shape.</reversibility>
  <files>manga_ai_studio/core/image_ops.py, tests/test_core/test_image_ops.py</files>
  <read_first>
    - manga_ai_studio/core/mask_editor.py (the pure-mutation-ops module template: docstring shape, `from __future__ import annotations`, numpy imports, clamp helper at 72-79; mask_to_numpy_binary/numpy_binary_to_mask_qimage at 162-207 — the binary-mask boundary, NOT to be re-implemented)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Common Operation 2 lines 493-520 — the transform math; Common Operation 3 lines 528-531 — rotate_page; Pitfall 4 lines 404-422 — ONE rotation convention; Pitfall 3 lines 412-417 — never mutate lines in place)
    - .planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md (image_ops assignment lines 110-128)
    - manga_ai_studio/core/box_model.py (PageBox fields + copy() at 166-178 — shallow payload copy; the fresh-payload rule)
    - panelcleaner/structures.py (Box @frozen at 39-44 — build NEW Box instances, never mutate)
    - panelcleaner/comic_text_detector/utils/textblock.py (TextBlock constructor + .lines quad shape [[x,y]x4])
  </read_first>
  <action>
    Create `manga_ai_studio/core/image_ops.py` — pure functions ONLY, module docstring mirroring mask_editor.py's (state the separation: pixel/geometry math lives here, Qt event dispatch lives in gui/canvas.py). Imports: numpy as np, PIL Image, copy, and `from manga_ai_studio.core.box_model import PageBox` + `from panelcleaner.comic_text_detector.utils.textblock import TextBlock` + `from panelcleaner.structures import Box` (all pure modules — verify no Qt transitively).

    Rotation (the TRACER — ONE op, full depth):
    - `_rotate_point(x, y, w, h, k) -> tuple[int, int]` per RESEARCH Common Operation 2: k=-1 (90 CW): (h-1-y, x); k=1 (90 CCW): (y, w-1-x); k=2 (180): (w-1-x, h-1-y); else identity.
    - `transform_box(box: Box, w, h, k) -> Box` — rotate both corners, normalize order: `Box(min(nx1,nx2), min(ny1,ny2), max(nx1,nx2), max(ny1,ny2))`. NEVER mutate the input Box.
    - `transform_lines(lines: list, w, h, k) -> list` — a NEW list of NEW quad lists; each quad's 4 points transformed via _rotate_point. NEVER touch the input lists in place.
    - `transform_box_payload(pagebox: PageBox, w, h, k) -> PageBox` — builds a fresh PageBox: `PageBox(box=transform_box(...), origin=same, payload=fresh TextBlock)` — the fresh TextBlock gets `xyxy = list(transform_box(...).as_tuple)`, `lines = transform_lines(payload.lines, ...)`, and copies vertical/language/font_size/text/translation from the original payload (text may be a str or list — copy whichever it is, do not join). Carry edited/bubble_no/manual_override unchanged. If payload is None, produce a payload-None PageBox with the transformed box.
    - `rotate_page(image_rgb: np.ndarray, mask_bin: np.ndarray, k: int) -> tuple[np.ndarray, np.ndarray]` — `np.rot90(image_rgb, k=k).copy(), np.rot90(mask_bin, k=k).copy()` (trailing .copy() = Pitfall 2). Validate image (H,W,3) uint8 and mask_bin (H,W) uint8 BEFORE the op (image_io.py:65-72 discipline); raise ValueError otherwise.
    - `rotate_boxes(boxes: list[PageBox], w: int, h: int, k: int) -> list[PageBox]` — map transform_box_payload over the list; w/h are the PRE-rotation image dims (each rotated box's quad math uses them).

    Tests (`tests/test_core/test_image_ops.py`, shape mirroring test_core/test_box_model.py): `test_rotate_transforms_all` — build a 4x6 (H,W) uint8 RGB image with a distinctive asymmetric pattern (e.g. a bright 2x1 block at a known corner), a binary mask with a nonzero pixel at a known offset, and two PageBoxes (one with a 2-line payload, one payload-None). Apply k=-1 (CW): assert image dims swap (6x4), the corner block lands at the CW-expected position (verify against np.rot90 on the pattern — assert `new_img == np.rot90(img, k=-1)` elementwise), mask equals np.rot90(mask, k=-1), each box's bbox equals the research math, each line quad element-equals the transformed quad, and the ORIGINAL boxes/lines are byte-identical afterward (the no-in-place-mutation guard). Repeat k=1 and k=2. `test_rotate_composes_identity` — two k=2 applications return the identity box geometry and dims (the probe's rotation-composition truth). `test_rotate_rejects_bad_shapes` — non-(H,W,3) image or non-(H,W) mask raises ValueError.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_image_ops.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from manga_ai_studio.core.image_ops import rotate_page, rotate_boxes, _rotate_point"` exits 0
    - test_rotate_transforms_all passes (pixel-exact image+mask; bbox and line-quad transforms verified per k; originals untouched)
    - test_rotate_composes_identity passes (two 180° = identity)
    - test_rotate_rejects_bad_shapes passes
    - `grep -c "def transform_lines" manga_ai_studio/core/image_ops.py` is 1 (fresh-list transform, no in-place path)
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Rotate transforms image + mask + box/line geometry pixel-exactly with fresh objects; compositions verified; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Crop — exact slice + drop-fully-outside/clip-partial policy (D-16) with dropped count</name>
  <files>manga_ai_studio/core/image_ops.py, tests/test_core/test_image_ops.py</files>
  <read_first>
    - manga_ai_studio/core/image_ops.py (this plan's Task 1 output — extend it; reuse _rotate_point/transform patterns for the clip math)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Common Operation 3 lines 532-536 — crop_page slice; Pitfall 10 lines 454-458 — the drop/clip policy + count; D-16 contract in 05-CONTEXT.md decisions)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 24a — apply semantics; the status copy shape is asserted by tests, not grepped)
  </read_first>
  <action>
    Extend `core/image_ops.py`:
    - `crop_page(image_rgb, mask_bin, x, y, w, h) -> tuple[np.ndarray, np.ndarray]` — exact slices `image_rgb[y:y+h, x:x+w].copy()` / `mask_bin[y:y+h, x:x+w].copy()` (D-18; Pitfall 2 .copy()). Validate (H,W,3)/(H,W) shapes and that x/y/w/h are ints with x>=0, y>=0, w>=1, h>=1, x+w<=W, y+h<=H; raise ValueError otherwise.
    - `_clip_quad(quad, x, y, w, h) -> list | None` — clamp each point to the crop rect [x, x+w] x [y, y+h] via per-coordinate min/max; if every point of the quad lies fully OUTSIDE the rect in a way that makes the clamped quad degenerate (zero width or height), return None (the caller drops it). Points outside clamp to the rect edge — partial boxes CLIP (D-16), their quads clamp to the new page boundary.
    - `_clip_box(pagebox: PageBox, x, y, w, h) -> PageBox | None` — compute the intersection of the bbox with the crop rect: `nx1 = max(bx1, x)`, `ny1 = max(by1, y)`, `nx2 = min(bx2, x+w)`, `ny2 = min(by2, y+h)`. If nx2<=nx1 or ny2<=ny1 → the box is fully outside → return None (DROP, counted by the caller). Else build a fresh PageBox: new Box(nx1, ny1, nx2, ny2) and, when a payload exists, a fresh TextBlock with xyxy = [nx1, ny1, nx2, ny2] and lines = [quad for quad in payload.lines if (clipped := _clip_quad(quad, x, y, w, h)) is not None] (dropped line quads are removed; remaining quads are translated by (-x, -y) so they sit in post-crop page coordinates, clamped to [0, w] x [0, h]). Copy vertical/language/font_size/text/translation/edited/bubble_no/manual_override from the original.
    - `crop_boxes(boxes: list[PageBox], x, y, w, h) -> tuple[list[PageBox], int]` — returns (kept_boxes, dropped_count) where dropped_count is the number of fully-outside boxes (D-16 status count).
    - `crop_page_with_boxes(image_rgb, mask_bin, boxes, x, y, w, h) -> tuple[np.ndarray, np.ndarray, list[PageBox], int]` — the one-call apply entry: crop_page + crop_boxes, so the GUI apply path (plan 05-07) has a single seam.

    Tests: `test_crop_drop_and_clip` — image+mask slice exactness (a 10x8 page cropped to x=2,y=1,w=4,h=3 yields the exact sub-arrays); boxes: one fully outside → dropped, count==1; one fully inside → translated bbox; one partial → bbox clamped to [2,1,6,4] with a line quad clamped and translated; one payload-None partial box → clipped bbox, payload still None. `test_crop_rejects_bad_input` — out-of-range or non-int coords raise ValueError. `test_crop_zero_area_dropped` — a box whose bbox touches the crop edge but has zero intersection area (nx2==nx1) is dropped, counted.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_image_ops.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_crop_drop_and_clip passes (slice exactness; drop count; clip of bbox AND line quads; post-crop coordinates)
    - test_crop_rejects_bad_input and test_crop_zero_area_dropped pass
    - `grep -c "def crop_boxes" manga_ai_studio/core/image_ops.py` is 1 and `grep -c "dropped_count" manga_ai_studio/core/image_ops.py` is >= 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Crop slices exactly and applies the D-16 drop/clip policy with a count; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Resize (LANCZOS/NEAREST + int box scaling) + Levels (numpy LUT + white&gt;black clamp)</name>
  <files>manga_ai_studio/core/image_ops.py, tests/test_core/test_image_ops.py</files>
  <read_first>
    - manga_ai_studio/core/image_ops.py (this plan's Tasks 1-2 output — extend it)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Common Operation 3 lines 538-551 — resize_page/levels_page; Pattern 4 lines 361-375 — levels_lut + the 768-entry PIL point() pitfall; A8 — LANCZOS/NEAREST)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 25 — Levels black≥white clamp contract; surface 26 — Resize ranges 1..100000)
  </read_first>
  <action>
    Extend `core/image_ops.py`:
    - `resize_page(image_rgb, mask_bin, new_w, new_h) -> tuple[np.ndarray, np.ndarray]` — image via `Image.fromarray(image_rgb, "RGB").resize((new_w, new_h), Image.Resampling.LANCZOS)` then np.asarray + .copy(); mask via `Image.fromarray(mask_bin, "L").resize((new_w, new_h), Image.Resampling.NEAREST)` + .copy() (D-18 — no soft alpha drift; A8). Validate: new_w/new_h ints within [1, 100000] (UI-SPEC surface 26 range), image (H,W,3), mask (H,W); ValueError otherwise.
    - `resize_boxes(boxes: list[PageBox], w, h, new_w, new_h) -> list[PageBox]` — scale = (new_w/w, new_h/h); each box's bbox coords scale via `int(round(coord * scale))` (Pitfall 6 int discipline) and normalize order; each line quad's points scale the same way. Fresh PageBox/TextBlock per box (same copy discipline as Task 1 — never mutate inputs). `w`/`h` are the pre-resize dims.
    - `levels_lut(black: int, white: int, gamma: float) -> np.ndarray` — per Pattern 4: `lut = np.arange(256, dtype=np.float64); lut = (lut - black) / max(white - black, 1); lut = np.clip(lut, 0.0, 1.0) ** (1.0 / gamma); return (lut * 255.0).round().astype(np.uint8)` (256-entry — NEVER the 768-entry PIL point() form, Pitfall 1). Clamp: internally normalize so the LUT never renders an inverted map — when `white <= black`, treat the range as [min(black, white), max(black, white)] (the white>black clamp; the GUI also cross-clamps slider ranges per UI-SPEC surface 25, this is the math-level backstop).
    - `levels_page(image_rgb, black, white, gamma) -> np.ndarray` — `levels_lut(black, white, gamma)[image_rgb].copy()` (geometry-free — D-15: mask/boxes untouched by design; the function signature takes only the image).
    - All four functions keep the pre-op shape validation discipline (image_io.py:65-72 analog).

    Tests: `test_resize_and_levels` — resize a 10x8 image to 5x4: dims correct; mask resized with NEAREST (assert the resized mask contains ONLY 0/255 values — no interpolated grays); boxes scaled by 0.5 with int coords (a box [2,2,6,6] → [1,1,3,3]); levels: a known ramp array (0..255 across the last axis) through levels_page(black=64, white=192, gamma=1.0) → values below 64 become 0, above 192 become 255, midpoint maps to 128 (byte-exact); `levels_lut(white=100, black=100, gamma=1.0)` never yields a descending map (monotone non-decreasing — the white>black clamp); levels leaves the image shape unchanged and is purely pixel-wise (no geometry args in the signature). `test_resize_rejects_bad_input` — new_w=0 or >100000 raises ValueError.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_image_ops.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_resize_and_levels passes (NEAREST mask purity; int box scaling; byte-exact LUT math; monotone clamp)
    - test_resize_rejects_bad_input passes
    - `grep -c "def levels_lut" manga_ai_studio/core/image_ops.py` is 1 and `grep -c "def resize_boxes" manga_ai_studio/core/image_ops.py` is 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Resize and levels complete with the D-18 mask purity and clamp contracts; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05-02)

- `manga_ai_studio/core/image_ops.py` (NEW) — `_rotate_point`, `transform_box`, `transform_lines`, `transform_box_payload`, `rotate_page`, `rotate_boxes`, `_clip_quad`, `_clip_box`, `crop_page`, `crop_boxes`, `crop_page_with_boxes`, `resize_page`, `resize_boxes`, `levels_lut`, `levels_page`
- `tests/test_core/test_image_ops.py` (NEW) — 6 test functions

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI numeric input → transforms | Spinbox/dialog values cross here; clamps below keep the math total regardless of caller |
| live canvas buffers → transforms | Aliasing would corrupt the undo snapshot chain — .copy() at every return |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | DoS | `levels_page` / `resize_page` on oversized arrays | medium | mitigate | pre-op (H,W,3)/(H,W) validation + resize bounds [1, 100000]; page dims already capped by MAX_IMAGE_DIMENSION at load (canvas.py:79) |
| T-05-07 | Tampering | levels white&gt;black inversion | low | mitigate | math-level clamp in `levels_lut` (monotone non-decreasing LUT) + GUI slider cross-clamping (plan 05-06) — no inverted map ever renders (UI-SPEC surface 25) |
| T-05-08 | Tampering | in-place mutation of payload.lines / frozen Box | medium | mitigate | API shape returns fresh PageBox/TextBlock per box; no-mutation regression tests (`test_rotate_transforms_all` originals-untouched assertion); never touch the @frozen Box (Pitfall 3, D-14 composition) |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: all already-installed, zero new deps); no checkpoints needed |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_image_ops.py -x -q`
- Per wave merge: full suite `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</verification>

<success_criteria>
- Rotate/crop/resize/levels all verified pixel-exact or per-contract (LANCZOS/NEAREST, LUT math, drop/clip)
- Geometry transforms proven non-mutating with fresh objects (undo-snapshot-safe for plan 05-04)
- All new tests pass; full suite green
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-02-SUMMARY.md` when done.
</output>
