---
phase: 05-project-persistence-image-ops-export
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/ocr_export.py
  - tests/test_core/test_ocr_export.py
autonomous: true
requirements: [PROJ-03]
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "build_page_ocr_json produces the D-19 snake_case shape: version/img_width/img_height/blocks[] with per-block box [x1,y1,x2,y2]/vertical/text/translation/bubble_no/origin + lines[] with per-line box+text (published contract — spelling pinned once)"
    - "D-20: whole-text split by \n onto the TextBlock.lines polygons — line N gets segment N; unmatched polygons export empty text; zero-box pages export empty blocks[] (no gate, no confirm)"
    - "D-22: pristine page (geometry_altered False) -> source-folder sidecar <stem>_ocr.json; geometry-altered page -> <source>/cleaned/<stem>_ocr.json, created if missing"
    - "Batch export is interruptible via the abort flag (checked at the loop top only); each page writes independently; per-page failures are isolated and reported -> {ok, failed, total}"
    - "Exported coordinates + img dims always describe the CURRENT page state; PageBox.mask/std_dev are never exported (D-15 seam)"
  artifacts:
    - "manga_ai_studio/core/ocr_export.py -- build_page_ocr_json/split_text_onto_lines/line_box/ocr_json_target_dir/write_page_ocr_json/batch_export_ocr (pure stdlib+PIL, no Qt)"
    - "tests/test_core/test_ocr_export.py (Wave 0 file for PROJ-03)"
  key_links:
    - "origin values read from box_model.DETECTED/USER constants (box_model.py:49-50)"
    - "batch_export_ocr's last-two-kwargs contract (progress_callback=None, abort_flag=None) matches Worker auto-injection (worker_thread.py:103-140) — plan 05-08 dispatches it"
    - "geometry_altered flag (plan 05-01) drives the D-22 target dir"
---

<objective>
Wave 1 of PROJ-03: the headless `_ocr.json` exporter `core/ocr_export.py` — the D-19 custom shape (mokuro vocabulary + our fields + per-line `lines[]`), the D-20 `\n`-split mapping onto `TextBlock.lines` polygons, the D-22 state-dependent output location rule, and the batch loop contract (`batch_export_ocr(pages, progress_callback=None, abort_flag=None) -> {"ok", "failed", "total"}` mirroring `core/batch_runner.py`'s per-page-isolation shape). Pure projection of `PageBox`/`TextBlock` state — no models, no Qt — so plan 05-08 can dispatch it via the existing Worker with zero threading concerns.

Purpose: the JSON shape is a published contract consumed by downstream typesetting tools (D-19 one-way). This module pins the exact field spelling and split semantics in one place, under test, before any GUI surface exists.

Output: `core/ocr_export.py` + `tests/test_core/test_ocr_export.py` green (baseline measured at plan time 2026-08-08: 463 collected / 462 passed / 1 failed — sole pre-existing test_gui_boxes.py regression fixed by plan 05-09, same wave; re-measure at execution).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@.planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md
@.planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md
@.planning/phases/05-project-persistence-image-ops-export/05-VALIDATION.md

@manga_ai_studio/core/box_model.py
@manga_ai_studio/core/batch_runner.py
@panelcleaner/comic_text_detector/utils/textblock.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): build_page_ocr_json — the D-19 shape + D-20 \n-split, one page end-to-end</name>
  <reversibility rating="one-way">D-19 locked the JSON shape as a published contract downstream typesetting tools will read; changing the field spelling later breaks consumers. (User decision D-19 already walked this door — no checkpoint:decision inserted; rating kept for the record.)</reversibility>
  <files>manga_ai_studio/core/ocr_export.py, tests/test_core/test_ocr_export.py</files>
  <read_first>
    - manga_ai_studio/core/image_io.py (the pure-module template: docstring dependency contract, validation-before-I/O)
    - manga_ai_studio/core/box_model.py (PageBox fields at 85-94; DETECTED/USER origin constants at 49-50 — the exact strings the export must emit)
    - panelcleaner/comic_text_detector/utils/textblock.py (TextBlock.lines quad shape [[x1,y1],[x2,y2],[x3,y3],[x4,y4]]; .text may be str or list — handle both)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pattern 3 lines 330-358 — the exact JSON shape + the verified mokuro vocabulary; Common Operation 5 lines 568-581 — split_text_onto_lines + line_box; Anti-Pattern 1 — never TextBlock.to_dict())
    - .planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md (ocr_export assignment lines 76-107)
  </read_first>
  <action>
    Create `manga_ai_studio/core/ocr_export.py` — pure stdlib + numpy + PIL only (module docstring per the image_io template; state the D-19 shape is a published contract). Imports: json, pathlib, `from manga_ai_studio.core.box_model import DETECTED, USER`.

    - `OCR_JSON_VERSION = "1"` (the version string — pinned once; the plan documents it as the published value).
    - `split_text_onto_lines(text: str, lines: list) -> list[str]` — `segments = text.split("\n")`; return `[segments[n] if n < len(segments) else "" for n in range(len(lines))]` (line N gets segment N; unmatched polygons get empty text — D-20; when text is a list, join with "\n" first — the Phase 4 storage is single-str but the vendored TextBlock accepts lists; defensive join keeps the export correct either way).
    - `line_box(quad: list) -> list[int]` — `[min(xs), min(ys), max(xs), max(ys)]` from the 4-point polygon (D-19 per-line box).
    - `build_page_ocr_json(page_boxes: list, img_w: int, img_h: int) -> dict` — returns the D-19 shape: `{"version": OCR_JSON_VERSION, "img_width": img_w, "img_height": img_h, "blocks": [...]}`. Per block: `box` = `list(pagebox.box.as_tuple)` [x1,y1,x2,y2]; `vertical` = payload.vertical when a payload exists else False; `text` = payload.text as str (list → "\n".join; None payload → ""); `translation` = payload.translation or ""; `bubble_no` = pagebox.bubble_no (None → null — the model never holds 0, per the 04-10 0-sentinel contract); `origin` = pagebox.origin (the DETECTED/USER strings from box_model). `lines` = per polygon: `{"box": line_box(quad), "text": segment}` via split_text_onto_lines; when the payload is None or has no lines, `lines` = [] and the whole text (if any) is NOT split (empty lines list exports no per-line entries — the block-level text still exports). Zero-box pages → `"blocks": []` (no gate, no confirm — UI-SPEC surface 27). int-coerce img_w/img_h (ValueError on non-int — T-05-02 discipline). NEVER export PageBox.mask/std_dev (D-15 seam).
    - `page_ocr_json_dumps(data: dict) -> str` — `json.dumps(data, ensure_ascii=False, indent=2)` (UTF-8-safe; the export file writes this string encoded utf-8).

    Tests (`tests/test_core/test_ocr_export.py`): `test_json_shape` — a page with two boxes (one full payload: text "First line\nSecond line", translation, bubble_no=3, origin "detected", vertical False, two line quads; one payload-None user box with bubble_no=None): assert the exact dict keys at every level (version/img_width/img_height/blocks/box/vertical/text/translation/bubble_no/origin/lines + per-line box/text), the snake_case spelling, per-line boxes = line_box of the quads, and the payload-None box exports text "" + lines []. `test_newline_split` — 3-line text onto 2 quads → segments ["First line", "Second line"] with the third segment dropped... (per D-20 the EXTRA segments beyond polygon count are dropped — the split maps onto polygons); 1-line text onto 3 quads → ["text", "", ""]; zero-box page → blocks []. `test_d15_seam_never_exported` — a PageBox with mask/std_dev set to dummy objects exports no such keys.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_ocr_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from manga_ai_studio.core.ocr_export import build_page_ocr_json, OCR_JSON_VERSION; assert OCR_JSON_VERSION == '1'"` exits 0
    - test_json_shape passes (exact key set + snake_case spelling at every level)
    - test_newline_split passes (segment mapping + empty-polygon fill + zero-box page)
    - test_d15_seam_never_exported passes
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>The D-19/D-20 export dict builds correctly with pinned spelling; committed.</done>
</task>

<task type="auto">
  <name>Task 2: D-22 location rule + write_page_ocr_json + batch_export_ocr (Worker-contract loop)</name>
  <reversibility rating="costly">D-22's state-dependent placement is the contract downstream tooling relies on; the batch loop's kwargs shape is consumed by the Worker in plan 05-08.</reversibility>
  <files>manga_ai_studio/core/ocr_export.py, tests/test_core/test_ocr_export.py</files>
  <read_first>
    - manga_ai_studio/core/ocr_export.py (this plan's Task 1 output — extend it)
    - manga_ai_studio/core/batch_runner.py (lines 56-66 imports + 129-188 loop shape — the per-page isolation + abort-at-loop-top + {ok,failed,total} contract to mirror; the cleaned/ folder convention at 124-127)
    - manga_ai_studio/gui/worker_thread.py (lines 103-140 — the Worker auto-injection contract: progress_callback and abort_flag are the LAST TWO kwargs with None defaults)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Open Question 4 — the batch shape; D-22 in 05-CONTEXT.md decisions)
  </read_first>
  <action>
    Extend `core/ocr_export.py` (still no Qt):
    - `ocr_json_target_dir(source_dir: Path, geometry_altered: bool) -> Path` — D-22: pristine → source_dir; altered → `source_dir / "cleaned"` (the Phase 2 sibling convention). Callers create it via `mkdir(parents=True, exist_ok=True)` (the UI-SPEC surface 27 "created if missing" contract — batch_runner's folder-creation helper is not reused; os-level mkdir is the A5/UI-SPEC Open Q5 resolution).
    - `default_ocr_json_path(page_path: Path, geometry_altered: bool) -> Path` — `<target_dir> / f"{page_path.stem}_ocr.json"` (mokuro naming convention, D-22).
    - `write_page_ocr_json(page_boxes, img_w, img_h, page_path: Path, geometry_altered: bool, path_override: Path | None = None) -> Path` — build via build_page_ocr_json, target dir per D-22 unless path_override (the GUI's Save As dialog overrides — UI-SPEC surface 27), mkdir parents=True exist_ok=True, write `page_ocr_json_dumps(data).encode("utf-8")` (UTF-8 file write — the probe encoding truth), atomic temp+os.replace, return the written path.
    - `batch_export_ocr(pages: list, progress_callback=None, abort_flag=None) -> dict` — mirror batch_runner's loop: `failed: list[tuple[Path, str]] = []`, `total = len(pages)`; per page (page is an object with `.path`, `.boxes`, `.img_w`/`.img_h` — or a tuple — pick ONE input shape and document it: recommend a lightweight `ExportPage` dataclass with `path: Path`, `boxes: list`, `img_w: int`, `img_h: int`, `geometry_altered: bool`); abort check at the loop TOP ONLY (`if abort_flag is not None and abort_flag.get(): raise Abort()`); progress emit per page `(percent, page.path.name)`; per-page try/except → `logger.error` + failed.append + continue (D-04 per-page isolation); return `{"ok": total - len(failed), "failed": failed, "total": total}`. Import `Abort` from manga_ai_studio.gui.worker_thread and `logger` from loguru at module top (batch_runner.py:56,66 precedent — the gui import is fine because worker_thread has no Qt widget deps; verify that before committing to it, else raise a local Abort subclass).
    - Log only page stems/names — never raw text content (T-05-05 log-injection discipline).

    Tests: `test_d22_location_rule` — pristine page → sidecar in source_dir with the {stem}_ocr.json name; geometry_altered page → <source>/cleaned/ (directory created); a source file named `chapter-01/page-03.png` exports to `chapter-01/cleaned/page-03_ocr.json` when altered. `test_write_round_trip_utf8` — write_page_ocr_json with a Japanese text payload → file bytes decode as UTF-8 and json.loads → blocks[0]["text"] equals the source. `test_batch_export_ocr_isolates_failures` — 3 pages, one with an invalid page path that raises at write: result {"ok": 2, "failed": 1, "total": 3}, the two good JSON files exist, the failure is recorded with the page stem; abort_flag set → batch raises Abort before writing any further page (write one page first to prove loop-top ordering). `test_batch_progress_emits` — progress_callback collects (percent, name) pairs; first emit has the first page's name.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_ocr_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_d22_location_rule passes (both branches + cleaned/ creation)
    - test_write_round_trip_utf8 passes (UTF-8 decode + JSON parse round-trip of Japanese text)
    - test_batch_export_ocr_isolates_failures passes ({ok, failed, total} exact; loop-top abort)
    - test_batch_progress_emits passes
    - `grep -c "def batch_export_ocr" manga_ai_studio/core/ocr_export.py` is 1 and `grep -c "abort_flag" manga_ai_studio/core/ocr_export.py` is >= 2
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>D-22 placement, UTF-8 writes, and the interruptible batch loop are complete and tested; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05-03)

- `manga_ai_studio/core/ocr_export.py` (NEW) — `OCR_JSON_VERSION`, `split_text_onto_lines`, `line_box`, `build_page_ocr_json`, `page_ocr_json_dumps`, `ocr_json_target_dir`, `default_ocr_json_path`, `write_page_ocr_json`, `batch_export_ocr` (incl. the `ExportPage` input shape)
- `tests/test_core/test_ocr_export.py` (NEW) — 6 test functions

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → disk (`_ocr.json` writes) | Exported files land beside source pages or in `cleaned/`; writes must be atomic and UTF-8 |
| model state → export dict | PageBox payload text crosses into a published JSON contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | DoS | `build_page_ocr_json` (hostile line counts) | low | mitigate | per-block lines bounded by the actual payload.lines polygons (no unbounded loops); img dims int-coerced — T-05-02 discipline |
| T-05-10 | Tampering | JSON encoding of user text | low | mitigate | json.dumps(ensure_ascii=False) + UTF-8 file write — no manual string concatenation into the JSON body (the shape IS the contract; Don't-Hand-Roll JSON export) |
| T-05-05 | Tampering | log injection via crafted text content | low | mitigate | batch loop logs only page stems/names + error strings, never raw OCR/translation text (Phase 2 batch-report discipline) |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: zero new deps); no checkpoints needed |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_ocr_export.py -x -q`
- Per wave merge: full suite `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</verification>

<success_criteria>
- D-19 shape + D-20 split + D-22 location + batch isolation all green under test
- `batch_export_ocr` signature ready for the Worker dispatch (plan 05-08)
- Full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-03-SUMMARY.md` when done.
</output>
