---
phase: 05-project-persistence-image-ops-export
plan: 07
type: execute
wave: 5
depends_on: [05-06]
files_modified:
  - manga_ai_studio/core/mask_editor.py
  - manga_ai_studio/gui/tools_panel.py
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/crop_dialog.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_crop_tool.py
autonomous: true
requirements: [PROJ-04]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Crop is the 6th exclusive tool: ToolMode.CROP added to the enum, ToolsPanel text button after Eraser, shortcut G, active-tool accent highlight, Tools-menu action in the exclusive group (D-11)"
    - "Crop-tool drag defines a crop rect with CrossCursor; the outside region dims via the z=880 overlay at rgba(0,0,0,0.45) (1px inset); the rect border reuses the inherited cyan dashed preview pen; a drag below 8x8 scene px is a no-op"
    - "The rect stays armed after release (drag-then-decide): Enter applies, Esc cancels (overlay+preview clear, Crop tool stays active); box hit-testing is preserved while the tool is active (press on a box still selects/moves it)"
    - "Apply crops image + mask as exact slices, drops fully-outside boxes with the count flash, clips partial boxes (bbox AND lines), pushes ONE geometry undo entry, re-baselines, marks geometry_altered (D-16/D-18)"
    - "Crop… dialog (Edit menu): X/Y/W/H spinboxes initialized to the full page bounds; Width range 1..W−x recomputed when X changes (Height analog); apply-time re-validation keeps the dialog open with corrected values"
  artifacts:
    - "manga_ai_studio/core/mask_editor.py: ToolMode.CROP"
    - "manga_ai_studio/gui/tools_panel.py: 6th tool button"
    - "manga_ai_studio/gui/canvas.py: crop armed-rect state machine + dim-out overlay (z=880) + Enter/Esc dispatch"
    - "manga_ai_studio/gui/crop_dialog.py (numeric x/y/w/h)"
    - "manga_ai_studio/gui/main_window.py: Edit-menu Crop… + Tools-menu crop action + _apply_crop"
    - "tests/test_gui_crop_tool.py (pytest-qt)"
  key_links:
    - "crop apply reuses _apply_geometry_op (plan 05-06) with crop_page_with_boxes — the one-call core seam"
    - "preview_item (z=900) reused as the crop border; the dim-out overlay sits at z=880 between the box layer and the preview"
    - "dispatch order preserved: inline-editor check → pan → box hit-test → tool branch (canvas.py:881-948)"
---

<objective>
The crop surface of PROJ-04 (D-11/D-16/D-18): the Canvas Crop tool — the 6th exclusive tool in the QActionGroup — with its armed-rect interaction (drag defines the rect, dim-out overlay at rgba(0,0,0,0.45) z=880, Enter applies, Esc cancels, 8×8 scene-px minimum) and the numeric Crop… dialog (Edit menu, x/y/w/h spinboxes with recomputed bounds). Both apply paths share `core/image_ops.crop_page_with_boxes` (plan 05-02) through the `_apply_geometry_op` orchestration (plan 05-06).

Purpose: crop is the phase's only canvas-gesture image op — the armed-rect state machine is new interaction territory (unlike mask-rect, it must survive release until Enter/Esc). The D-16 dropped-box count and the dim-out overlay are the user-visible contracts.

Output: tool + canvas state machine + dialog + wiring + tests green (baseline measured at plan time 2026-08-08: 462 collected / 461 passed / 1 failed — sole pre-existing test_gui_boxes.py regression fixed by plan 05-09, Wave 1; re-measure at execution).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@.planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md
@.planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md
@.planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md
@.planning/phases/05-project-persistence-image-ops-export/05-VALIDATION.md

@manga_ai_studio/core/mask_editor.py
@manga_ai_studio/gui/tools_panel.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/core/image_ops.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ToolMode.CROP + the 6th tool button + G shortcut + Tools-menu action</name>
  <files>manga_ai_studio/core/mask_editor.py, manga_ai_studio/gui/tools_panel.py, manga_ai_studio/gui/main_window.py, tests/test_gui_crop_tool.py</files>
  <read_first>
    - manga_ai_studio/core/mask_editor.py (ToolMode enum at 56-69 — the enum being extended; docstring "5 exclusive" -> 6)
    - manga_ai_studio/gui/tools_panel.py (_make_tool_action at 194-209, _action_to_tool dict at 127-133, tool row assembly at 145-161, docstring/UI-SPEC surface-6 note at 1-23)
    - manga_ai_studio/gui/main_window.py (_build_tools_menu at 521-562 — tool actions carry setData(ToolMode) at 556 + connect via lambda set_active_tool at 557; the QActionGroup pattern; _refresh_action_states)
    - .planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md (tools_panel + mask_editor assignments lines 257-279)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 6 + 24a — 6th tool contract, shortcut G, tooltip copy)
  </read_first>
  <action>
    - `core/mask_editor.py`: add `CROP = "crop"` to ToolMode (first enum extension — the Phase 3 "no 6th tool" stance is superseded by D-11). Update the enum docstring ("The 5 exclusive mask-editing tools" → 6).
    - `gui/tools_panel.py`: add the CROP tool button after Eraser in the exclusive QActionGroup — `_make_tool_action("Crop", ToolMode.CROP, shortcut="G", tooltip per UI-SPEC §Copywriting)`; register in `_action_to_tool`; docstring surface-6 note updated to "6 exclusive tools". No brush-size row for crop (UI-SPEC surface 6 — crop needs no size control).
    - `gui/main_window.py` `_build_tools_menu`: add the Crop tool action to the existing exclusive tool group (setData(ToolMode.CROP), shortcut G, tooltip per §Copywriting) after Eraser — same wiring as the other tool actions; `_refresh_action_states` leaves it enabled while a page is open (tool actions follow the existing tool gating).

    Tests (test_gui_crop_tool.py): `test_crop_is_sixth_exclusive_tool` (ToolsPanel exposes a Crop action in the exclusive group with data == ToolMode.CROP; activating it deactivates Brush and vice versa; shortcut G triggers it), `test_crop_action_in_tools_menu` (the menu action exists with the same data and is exclusive with the others).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_crop_is_sixth_exclusive_tool + test_crop_action_in_tools_menu pass
    - `grep -c 'CROP = "crop"' manga_ai_studio/core/mask_editor.py` is 1 and `grep -v '^#' manga_ai_studio/gui/tools_panel.py | grep -c "ToolMode.CROP"` is >= 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>The 6th tool exists everywhere (enum, panel, menu, shortcut) with exclusive-group behavior; committed.</done>
</task>

<task type="tracer">
  <name>Task 2 (TRACER): Crop-tool armed-rect state machine + dim-out overlay — drag → Enter applies / Esc cancels, end-to-end</name>
  <files>manga_ai_studio/gui/canvas.py, tests/test_gui_crop_tool.py</files>
  <read_first>
    - manga_ai_studio/gui/canvas.py (mousePressEvent dispatch at 858-949 — inline-editor guard FIRST (881-889), pan (891-899), box hit-test (903-938), mask-tool branch (940-948); keyPressEvent at ~1146; the existing preview_item (mask-rect/lasso/box-create reuse) + its pen; the z-order constants; scene rect/page bounds; _begin_create_box/_commit_create flow for the anchor pattern; MAX_IMAGE_DIMENSION at 79)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 24a — armed-rect contract, dim-out z=880 rgba(0,0,0,0.45) 1px inset, 8x8 minimum, Enter/Esc semantics; Z-order section lines 251-271)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pitfall 10 — the 8x8 no-op; UI-SPEC Open Question 3 — armed-rect confirmation)
  </read_first>
  <action>
    In `canvas.py`, implement the crop-tool state machine (current_tool == ToolMode.CROP branch):

    - State: `_crop_rect: QRectF | None` (armed rect, scene coords), `_crop_drag_active: bool`, `_crop_dim_items: list[QGraphicsRectItem]` (4 composited dim rects) or one even-odd QGraphicsPathItem — executor picks; the LOOK is the contract (4 rects compositing the outside-of-crop region at rgba(0,0,0,0.45), z=880, 1px inset so the dim meets the crop edge exactly).
    - mousePressEvent: after the pan + box-hit-test checks (a press on a box still selects/moves it — box interaction unchanged while Crop is active), when current_tool == CROP and the press is on empty canvas: begin a crop drag — capture the anchor, hide any armed rect, set CrossCursor.
    - mouseMoveEvent while dragging: update the rect (clamp to page bounds — a drag beyond the canvas clamps, no container overflow); when the rect is >= 8x8 scene px, show the preview border (reuse preview_item with the inherited cyan dashed 2px pen at z=900) + the dim overlay (z=880, below the preview); below 8x8: nothing shows (no-op contract).
    - mouseReleaseEvent: keep the rect ARMED (dim + preview persist — drag-then-decide; unlike mask-rect there is no commit-on-release). A release with a <8x8 rect clears the overlay/preview and arms nothing.
    - keyPressEvent: after the inline-editor check, when a crop rect is armed AND current_tool == CROP: Enter → `self._apply_armed_crop()`; Esc → cancel (clear overlay + preview, drop the armed rect, tool stays active). When no rect is armed, Esc falls through to the inherited dispatch (deselect box → batch cancel).
    - `_apply_armed_crop()` emits a new signal `crop_committed(QRectF)` (scene coords → converted to image-pixel ints by the handler) and clears the armed state + overlay + preview; the tool STAYS active (user may crop again; switching tools is the exit).
    - Remove the armed rect/overlay on: tool switch, page switch, and set_image_from_numpy (stale geometry).

    Tests (test_gui_crop_tool.py): `test_drag_arms_rect_enter_applies_esc_cancels` — activate Crop, drag a 100x80 rect: on release the rect stays armed (dim + preview items exist at the right z); Enter emits crop_committed with the rect; Esc instead cancels (items removed, no signal, tool still active). `test_small_drag_noop` — a 5x5 drag produces no armed rect, no overlay, no signal. `test_dim_outlayers_geometry` — the 4 dim rects composite exactly the outside region (union == page rect minus crop rect; 1px inset at the crop edges). `test_box_press_still_selects` — pressing on a box while Crop is active selects/moves the box, does NOT start a crop. `test_clamp_to_page` — dragging beyond the page clamps the rect to page bounds.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_drag_arms_rect_enter_applies_esc_cancels + test_small_drag_noop pass
    - test_dim_outlayers_geometry + test_box_press_still_selects + test_clamp_to_page pass
    - `grep -c "crop_committed" manga_ai_studio/gui/canvas.py` is >= 1 and `grep -v '^#' manga_ai_studio/gui/canvas.py | grep -c "0.45"` is >= 1 (the dim alpha)
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>The crop gesture works end-to-end (drag → armed → Enter/Esc) with the dim-out contract; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Crop apply (drop/clip + count flash + one undo) + numeric Crop… dialog</name>
  <files>manga_ai_studio/gui/canvas.py, manga_ai_studio/gui/crop_dialog.py, manga_ai_studio/gui/main_window.py, tests/test_gui_crop_tool.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_apply_geometry_op from plan 05-06 — the orchestration to reuse; _build_edit_menu structure — where Crop… lands between the undo pair and Clear Mask; _build_tools_menu; the dialog-collector consumption pattern from _on_levels)
    - manga_ai_studio/gui/load_translations_dialog.py (dialog template as plans 05-06)
    - manga_ai_studio/core/image_ops.py (crop_page_with_boxes — the one-call core seam)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 24a apply semantics + 24b dialog contract; §Copywriting crop + dropped-box count rows — copy asserted via tests)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pitfall 10 — the count + 8x8 minimum; D-16)
  </read_first>
  <action>
    - main_window.py: connect `canvas.crop_committed(QRectF)` → `_apply_crop(x, y, w, h)` (int-convert via math.floor on the scene rect, clamped to the image dims — never beyond). `_apply_crop(x, y, w, h)`: validate w>=1, h>=1, in-bounds (a degenerate call is a silent no-op — the dialog ranges and the 8x8 tool minimum make it near-impossible; the guard covers programmatic edges); run through `_apply_geometry_op("crop", geometry=True, fn=lambda: crop_page_with_boxes(img, mask, boxes, x, y, w, h))` — dropped_count comes back from the core seam; after the op, when dropped_count > 0 flash the dropped-box count copy (UI-SPEC §Copywriting crop row); geometry_altered=True via the shared path. Add the Edit-menu action `Crop…` (no shortcut, dialog-opening ellipsis per §Copywriting) → `_on_crop_dialog`.
    - Create `crop_dialog.py` — `CropDialog(QDialog)` per surface 24b: title "Crop", QFormLayout with X/Y/Width/Height QSpinBoxes initialized to the FULL current page bounds (x=0, y=0, w=W, h=H — never empty); ranges: X 0..W−1, Y 0..H−1, Width 1..W−x (recomputed when X changes), Height 1..H−y (recomputed when Y changes); [Cancel][Apply] (Apply default, accent border per the QSS pattern); collector `self.result = (x, y, w, h)`; apply-time re-validation (out-of-range values corrected in-place and the dialog stays open — guards programmatic edges only).
    - main_window.py `_on_crop_dialog() -> None`: get current image dims, open the dialog; on accept → `_apply_crop(*dialog.result)`.

    Tests: `test_crop_apply_drop_clip_count` (page with 3 boxes: one inside, one fully outside, one partial; _apply_crop(10, 10, 100, 80) → image+mask are the exact slices; inside box translated; outside box GONE; partial box clipped (bbox + line quads); dropped_count flash contains the count; ONE geometry undo entry; one Ctrl+Z restores ALL THREE incl. the dropped box; geometry_altered True), `test_crop_dialog_contract` (opens at full bounds; editing X to 50 recomputes Width max to W−50; editing Y analog; out-of-range programmatic value → dialog stays open with corrected values), `test_crop_dialog_apply_end_to_end` (dialog Apply → the same crop semantics as the tool path), `test_crop_degenerate_noop` (_apply_crop with w=0 or out-of-bounds → no change, no undo entry, no crash).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_crop_apply_drop_clip_count passes (slices, drop/clip, count flash, ONE entry, one-press undo restores the dropped box)
    - test_crop_dialog_contract + test_crop_dialog_apply_end_to_end + test_crop_degenerate_noop pass
    - `grep -c "class CropDialog" manga_ai_studio/gui/crop_dialog.py` is 1 and `grep -c "def _apply_crop" manga_ai_studio/gui/main_window.py` is 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Crop applies with the full D-16/D-18 semantics and the numeric dialog; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05-07)

- `manga_ai_studio/core/mask_editor.py` (EXTEND) — `ToolMode.CROP`
- `manga_ai_studio/gui/tools_panel.py` (EXTEND) — 6th tool action (Crop, shortcut G)
- `manga_ai_studio/gui/canvas.py` (EXTEND) — `_crop_rect`/`_crop_drag_active`/`_crop_dim_items` state, `_apply_armed_crop`, `crop_committed(QRectF)` signal, dim-out overlay compositing, Enter/Esc dispatch
- `manga_ai_studio/gui/crop_dialog.py` (NEW) — `CropDialog` (x/y/w/h spinboxes, W−x/H−y recompute, result collector)
- `manga_ai_studio/gui/main_window.py` (EXTEND) — `_apply_crop`, `_on_crop_dialog`, Edit-menu Crop… action, Tools-menu crop action wiring
- `tests/test_gui_crop_tool.py` (NEW) — 9+ pytest-qt test functions

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas gesture → crop rect | Scene-px drags cross here — clamped to page bounds, 8x8 minimum, int-converted at apply |
| dialog spinboxes → apply | Numeric bounds recompute live; the apply path re-validates (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-17 | Tampering | degenerate crop rects (w=0 / out-of-bounds) | low | mitigate | tool-side 8x8 minimum + page-bounds clamp (UI-SPEC surface 24a); dialog ranges 1..W−x / 1..H−y (24b); apply-side guard is a silent no-op — no degenerate slice ever executes (Pitfall 10) |
| T-05-18 | DoS | dim-out overlay item count | low | accept | fixed 4 rects (or one even-odd path) per armed drag — constant item count, removed on apply/cancel/tool-switch; scene-px geometry is page-bounded (UI-SPEC dismissal rationale) |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit); no checkpoints needed |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_crop_tool.py -x -q`
- Per wave merge: full suite green (baseline at plan time: 462 collected / 461 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</verification>

<success_criteria>
- Crop tool gesture contract (armed rect, dim-out, Enter/Esc, 8x8) verified end-to-end
- Apply path drops/clips boxes with the count flash, ONE undo entry, one-press restore
- Numeric dialog bounds contract + apply-time re-validation green
- Full suite green (baseline at plan time: 462 collected / 461 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-07-SUMMARY.md` when done.
</output>
