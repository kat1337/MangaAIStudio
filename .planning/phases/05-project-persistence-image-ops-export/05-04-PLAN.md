---
phase: 05-project-persistence-image-ops-export
plan: 04
type: tdd
wave: 2
depends_on: [05-02]
files_modified:
  - manga_ai_studio/core/history_manager.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_history.py
autonomous: true
requirements: [PROJ-04]
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One Ctrl+Z reverses image + mask + boxes together: push_geometry_state stamps ALL THREE stores with ONE monotonic stamp; undo()/redo() pop every store whose tail stamp equals the max and return a list of (kind, value) tuples"
    - "Every image op pushes exactly ONE geometry entry — no double-push, no empty entries (the probe's PROJ-04 idempotency truth)"
    - "Ordinary single-store pushes/pops behave exactly as before (mask/image/boxes edits unchanged); per-type pop methods are untouched"
    - "MainWindow.on_undo/on_redo apply the list in kind order and flash the Phase 3 'Undo: {op}'/'Redo: {op}' transient; the op-name set extends with rotate/crop/levels/resize"
    - "The IMAGE side of a geometry entry is a full-frame patch at (0,0) — pop_image_undo's existing machinery handles it (redo stash + limit=20 bounds memory, T-01-16)"
  artifacts:
    - "manga_ai_studio/core/history_manager.py with push_geometry_state + list-returning undo()/redo()"
    - "manga_ai_studio/gui/main_window.py on_undo/on_redo/_apply_undo_result accepting list[(kind, value)]"
    - "tests/test_history.py regression: test_geometry_undo_reverses_all_three (ONE undo restores image+mask+boxes)"
  key_links:
    - "stamp comparison: _stamp() monotonic counter (history_manager.py:113-122) shared by all three stores"
    - "_materialize_snapshot (251-275) detaches the boxes side; push-side .copy() detaches image/mask (Pitfall 2)"
    - "MainWindow._current_undo_state (1467-1485) gathers current mask/image/boxes for the undo call — unchanged shape"
---

<objective>
The geometry-op undo record (PROJ-04, D-14 + Claude's Discretion): extend `HistoryManager` with the stamp-shared triple push — `push_geometry_state(image_patch, mask_qimage, boxes)` stamps IMAGE+MASK+BOXES with one monotonic stamp, and `undo()`/`redo()` pop EVERY store whose tail stamp equals the max, returning `list[(kind, value)]` so ONE Ctrl+Z reverses an image op's image + mask + boxes together (UI-SPEC surface 28, "one press per op, never two"). Then update `MainWindow.on_undo`/`on_redo`/`_apply_undo_result` in the SAME plan (the contract change is atomic — the GUI is the only consumer of `undo()`'s return shape).

Purpose: RESEARCH Pattern 2 — this is the least-surprising unified-timeline behavior (Phase 3 D-11); it preserves the "three logical stacks, one timeline" invariant, keeps the per-type pop methods untouched, and bounds memory with the existing limit=20. Plan 05-06's rotate/levels/resize and 05-07's crop push through this record.

Output: `history_manager.py` + `main_window.py` changes + regression tests, suite green (baseline measured at plan time 2026-08-08: 463 collected / 462 passed / 1 failed — sole pre-existing test_gui_boxes.py regression fixed by plan 05-09, Wave 1, which MUST land before this Wave-2 plan's Task 2 verify targets tests/test_gui_boxes.py; re-measure at execution).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@.planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md
@.planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md
@.planning/phases/05-project-persistence-image-ops-export/05-VALIDATION.md

@manga_ai_studio/core/history_manager.py
@manga_ai_studio/gui/main_window.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: push_geometry_state — stamp-shared triple push (TDD)</name>
  <reversibility rating="costly">The record shape is the phase's undo contract (Claude's Discretion, RESEARCH A2); every image-op plan pushes through it.</reversibility>
  <files>manga_ai_studio/core/history_manager.py, tests/test_history.py</files>
  <read_first>
    - manga_ai_studio/core/history_manager.py (the whole file — _stamp at 113-122, push_mask_state/push_image_action at 125-200 (the .copy() + redo-clear + limit discipline to mirror), pop methods at 140-340, undo at 344-378, _materialize_snapshot at 251-275, WR-01 null guards at 159-165)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pattern 2 lines 285-327 — the exact push_geometry_state + pop-all-with-max-stamp shape)
    - .planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md (history_manager assignment lines 283-325)
    - tests/test_history.py (the existing test shape — instantiate HistoryManager without widgets; extend, do not restructure)
  </read_first>
  <behavior>
    - push_geometry_state(image_patch: np.ndarray, mask_qimage, boxes) appends (stamp, value) to _image_undo / _mask_undo / _boxes_undo with the SAME stamp value
    - The image entry is (stamp, (0, 0, image_patch.copy())); the mask entry (stamp, mask_qimage.copy()) — detached (Pitfall 2); the boxes entry (stamp, self._materialize_snapshot(boxes)) — detached by construction
    - mask_qimage=None or boxes=None omits that store from the push (levels pushes image-only)
    - The push clears each touched store's redo branch and applies the limit cap per store
    - test_geometry_push_stamps_all_stores: after a push, all three (or two, when mask None) stores share one tail stamp; the next ordinary push_mask_state gets a HIGHER stamp
  </behavior>
  <action>
    RED first: add to tests/test_history.py the behavior cases (test_geometry_push_stamps_all_stores, test_geometry_push_omits_none_stores, test_geometry_push_clears_redo, test_geometry_push_detaches_patch — mutating the caller's array after push does not change the stored entry). Run pytest to confirm RED (push_geometry_state does not exist yet).

    GREEN: add to HistoryManager:

    - `push_geometry_state(self, image_patch: np.ndarray, mask_qimage=None, boxes=None) -> None` exactly per RESEARCH Pattern 2: `stamp = self._stamp()`; `self._image_undo.append((stamp, (0, 0, image_patch.copy())))`; `self._image_redo.clear()`; when mask_qimage is not None: `self._mask_undo.append((stamp, mask_qimage.copy()))` + clear redo; when boxes is not None: `self._boxes_undo.append((stamp, self._materialize_snapshot(boxes)))` + clear redo; apply the existing `if len(...) > self.limit: pop(0)` cap per touched store. Docstring states the one-stamp contract + that only geometry ops use it.

    Do NOT change any existing push/pop method bodies. Keep WR-01 null guards untouched.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_history.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def push_geometry_state" manga_ai_studio/core/history_manager.py` is 1
    - test_geometry_push_stamps_all_stores passes (single shared stamp; ordinary push afterwards gets a higher stamp)
    - test_geometry_push_omits_none_stores passes (mask_qimage=None touches only image+boxes)
    - test_geometry_push_clears_redo + test_geometry_push_detaches_patch pass
    - All pre-existing test_history.py tests still pass (unchanged behavior for ordinary pushes)
  </acceptance_criteria>
  <done>push_geometry_state lands with the one-stamp contract and detachment discipline; committed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: undo()/redo() pop-all-with-max-stamp returning lists + MainWindow list-apply (TDD)</name>
  <files>manga_ai_studio/core/history_manager.py, manga_ai_studio/gui/main_window.py, tests/test_history.py</files>
  <read_first>
    - manga_ai_studio/core/history_manager.py (undo at 344-378 + redo at 380-403 — the functions being changed; keep their parameter shape (current_mask, current_img, current_boxes))
    - manga_ai_studio/gui/main_window.py (_current_undo_state at 1467-1485, on_undo/on_redo at 1487-1519, _apply_undo_result at 1521-1535, _show_transient_status at 1566-1583, the Phase 3 op-name set for the Undo/Redo flash — read once)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pattern 2 lines 305-327 — the pop-all algorithm + on_undo list apply + flash op names)
  </read_first>
  <behavior>
    - undo() returns list[(kind, value)] — EVERY store whose tail stamp equals the max tail stamp pops; a single-store tail (ordinary edits) returns a one-element list [(kind, value)]
    - redo() is the symmetric mirror over the redo stores
    - Empty stacks return [] (was None)
    - test_geometry_undo_reverses_all_three: push image+mask+boxes via push_geometry_state, then a second ordinary image push; ONE undo() returns [image-entry] for the ordinary push; the NEXT undo() returns a list of ALL THREE geometry entries (mask+image+boxes); a third undo() returns []
    - MainWindow._apply_undo_result handles a list: applies each (kind, value) in order (image→mask→boxes), refreshes the canvas + overlay once, flashes 'Undo: {op}' where {op} is drawn from the record/context (rotate|crop|levels|resize set added)
  </behavior>
  <action>
    RED first: extend tests/test_history.py with test_geometry_undo_reverses_all_three (+ a redo mirror test_geometry_redo_restores_all_three and a single-store-list test — an ordinary push_mask_state then undo() returns [("mask", ...)]). Run pytest — RED (undo still returns a bare (kind, value)).

    GREEN, history_manager.py: rewrite `undo(self, current_mask, current_img, current_boxes)` per RESEARCH Pattern 2: gather `candidates = [(kind, store[-1][0]) for kind, store in (("mask", self._mask_undo), ("image", self._image_undo), ("boxes", self._boxes_undo)) if store]`; if empty return []; `max_stamp = max(stamp for _, stamp in candidates)`; for each store whose tail stamp == max_stamp, append `(kind, pop_<kind>_undo(current_<kind>))` to a result list (skip stores whose tail stamp differs — ordinary single-store behavior); return the list. Mirror in `redo()`. The per-type pop methods stay untouched; WR-01 null-current guards inside them are preserved. Docstrings updated: return type is now `list[tuple[str, object]]`.

    GREEN, main_window.py: `_apply_undo_result` accepts `list[(kind, value)]` — iterate, applying per kind exactly as today (apply_undo_image with (0, 0, patch) bounds-checked against the current image — canvas.py:522-565 handles full-frame; apply_undo_mask; apply_undo_boxes), then a single refresh + `_show_transient_status` flash. `on_undo`/`on_redo` pass the list through; the op-name set for the flash extends with the four image-op names (rotate/crop/levels/resize) while keeping the existing names. Update the WR-01 null guards at the call sites if the current-state gathering needs it (read _current_undo_state first — it already tolerates None mask).

    IMPORTANT: this plan is the atomic contract change — do not leave main_window consuming a bare tuple. The full-suite GUI undo tests (test_gui_boxes.py etc.) must stay green after this task.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_history.py tests/test_gui_boxes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_geometry_undo_reverses_all_three passes (ONE undo pops image+mask+boxes together; the ordinary-push undo before it returns exactly one entry)
    - test_geometry_redo_restores_all_three + test_undo_returns_single_element_list pass
    - Existing test_history.py tests pass unchanged or with only the return-shape assertion updated (list-of-one)
    - test_gui_boxes.py passes (MainWindow undo/redo still works end-to-end after the list-apply change)
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>One-press geometry undo/redo works end-to-end through the GUI; no regressions; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05-04)

- `manga_ai_studio/core/history_manager.py` (EXTEND) — `push_geometry_state(image_patch, mask_qimage=None, boxes=None)`; `undo()`/`redo()` return `list[tuple[str, object]]` (pop-all-with-max-stamp)
- `manga_ai_studio/gui/main_window.py` (EXTEND) — `_apply_undo_result` list iteration; `on_undo`/`on_redo` passthrough; Undo/Redo flash op-name set + rotate/crop/levels/resize
- `tests/test_history.py` (EXTEND) — 3+ new geometry-undo regression tests

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MainWindow undo/redo → HistoryManager | The unified-timeline contract crosses here; a shape mismatch (tuple vs list) is a crash-class defect — covered by the atomic in-plan GUI update |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | DoS | geometry undo memory | low | mitigate | full-frame IMAGE patches bounded by the existing limit=20 cap (T-01-16); full-frame at (0,0) is the only correct pre-state for whole-canvas ops (RESEARCH State of the Art) |
| T-05-11 | Tampering | undo snapshot aliasing (Pitfall 3) | medium | mitigate | push-side .copy() (image/mask) + _materialize_snapshot detachment (boxes); regression tests assert mutation-after-push does not alter the entry |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit); no checkpoints needed |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_history.py -x -q`
- Task 2 adds the GUI undo smoke: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_history.py tests/test_gui_boxes.py -x -q`
- Per wave merge: full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</verification>

<success_criteria>
- push_geometry_state + pop-all-with-max-stamp verified under TDD (RED→GREEN committed separately)
- MainWindow applies the list contract with zero GUI regressions
- One-press-reverses-all-three proven by test_geometry_undo_reverses_all_three
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-04-SUMMARY.md` when done.
</output>
