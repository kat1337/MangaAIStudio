---
phase: 05-project-persistence-image-ops-export
plan: 06
type: execute
wave: 4
depends_on: [05-02, 05-04, 05-05]
files_modified:
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/levels_dialog.py
  - manga_ai_studio/gui/resize_dialog.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_image_dialogs.py
  - tests/test_gui_canvas.py
  - tests/test_gui_project.py
autonomous: true
requirements: [PROJ-04]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Rotate 90° CW/CCW/180° applies silently to the current page: image + mask + boxes transform together via core/image_ops, ONE geometry undo entry pushes, Show Original re-baselines to the post-op image (D-14), status flashes the rotate copy"
    - "Levels dialog: black 0 / white 255 / gamma 1.00 defaults; live in-place canvas preview on every control change; Cancel restores the pre-dialog image exactly with no undo entry; Apply pushes ONE image entry and re-baselines; white>black clamped so no inverted map ever renders"
    - "Resize dialog: width/height 1..100000 px initialized to current dims, aspect lock checked by default (editing one field recomputes the other, rounded >= 1), px/% unit toggle, live 'Result: {w} × {h} px' label; apply = LANCZOS image / NEAREST mask + scaled boxes, one undo entry"
    - "Levels is geometry-free (D-15): masks/boxes untouched; ImageFile.geometry_altered is set ONLY by crop/rotate/resize — never by levels (A4/UI-SPEC surface 25)"
    - "Show Original (P): disabled with tooltip per UI-SPEC when the current page is .mas-loaded without a verified original (original_verified False); enabled otherwise; the _original_image_numpy cache re-baselines after EVERY image op"
    - "Image-op actions are gated: enabled iff >= 1 page open AND no async op running; the levels live preview never pushes undo entries while the dialog is open (Pitfall 9)"
  artifacts:
    - "manga_ai_studio/gui/canvas.py: rebaseline_original() + capture-suppressed display path for previews"
    - "manga_ai_studio/gui/levels_dialog.py + manga_ai_studio/gui/resize_dialog.py (QDialog, _DIALOG_QSS pattern, collector + live-preview contract)"
    - "manga_ai_studio/gui/main_window.py: Tools-menu Image section + _apply_geometry_op orchestration + Show Original gating"
    - "tests/test_gui_image_dialogs.py (NEW) + test_gui_canvas.py/test_gui_project.py extensions"
  key_links:
    - "canvas.get_image_numpy / set_image_from_numpy / set_mask / set_boxes + mask_to_numpy_binary boundary (Pitfall 2 .copy() at every bridge)"
    - "push_geometry_state (plan 05-04) — the one-press undo record; _suppress_boxes_push guard around set_boxes during ops"
    - "_original_image_numpy capture-once pitfall (canvas.py:640-641) — rebaseline_original() after every op + capture-suppressed preview path"
---

<objective>
The image-op GUI layer of PROJ-04 (D-10/D-12/D-13/D-14/D-15): wire the Tools-menu Rotate actions and the Levels + Resize dialogs end-to-end — the apply orchestration that reads the canvas (get_image_numpy/mask_to_numpy_binary/boxes_snapshot), transforms via `core/image_ops` (plan 05-02), writes back through set_image_from_numpy/set_mask/set_boxes with the `_suppress_boxes_push` guard, pushes ONE geometry undo entry (plan 05-04), re-baselines Show Original, marks `ImageFile.geometry_altered` (never for levels), and flashes the status copy. Plus the D-06/D-14 Show Original gating in `_refresh_action_states`.

Purpose: after this plan, the user can rotate and run levels/resize with full undo fidelity and correct Show Original semantics. The crop surface (tool + dialog) follows in plan 05-07; the apply path here is its template.

Output: canvas + 2 dialogs + main_window wiring + tests green (baseline measured at plan time 2026-08-08: 463 collected / 462 passed / 1 failed — sole pre-existing test_gui_boxes.py regression fixed by plan 05-09, Wave 1; re-measure at execution).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@.planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md
@.planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md
@.planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md
@.planning/phases/05-project-persistence-image-ops-export/05-VALIDATION.md

@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/load_translations_dialog.py
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/core/image_ops.py
@manga_ai_studio/core/mask_editor.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): Rotate end-to-end — menu action → transform → canvas → one undo → rebaseline</name>
  <reversibility rating="costly">D-14's silent+re-baseline contract means the pre-op image is recoverable only via Ctrl+Z; the apply-path shape is reused by crop/resize.</reversibility>
  <files>manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/canvas.py, tests/test_gui_canvas.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_build_tools_menu at 521-562 — where the Rotate submenu + Image section land; _build_file_menu 258-321 pattern; _refresh_action_states 724-804; _show_transient_status 1566-1583; _suppress_boxes_push guard at 1554-1564; on_undo list-apply from plan 05-04; setWindowTitle at 1017)
    - manga_ai_studio/gui/canvas.py (get_image_numpy 568-607 — trailing .copy(); set_image_from_numpy 609-670 — _original_image_numpy capture-once at 640-641 (THE re-baseline pitfall); set_mask 389-419; set_boxes 1271-1318; boxes_snapshot 1382+; MAX_IMAGE_DIMENSION at 79)
    - manga_ai_studio/core/image_ops.py (rotate_page/rotate_boxes signatures — the transform seam)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pitfall 5 lines 424-428 — rebaseline_original + capture-suppressed preview; Pitfall 7 — flush seam; Pattern 2 — push_geometry_state consumption)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 23 rotate contract + §Copywriting image-op + undo-feedback rows — copy asserted via tests, not grepped)
  </read_first>
  <action>
    canvas.py first:
    - `rebaseline_original(self) -> None` — set `self._original_image_numpy = self.get_image_numpy()` and `self._showing_original = False` (D-14; RESEARCH Pitfall 5).
    - `set_image_from_numpy_preview(self, image_rgb: np.ndarray, capture_original: bool) -> None` — the capture-suppressed display path: identical to set_image_from_numpy except the `_original_image_numpy` capture is skipped when capture_original=False (used by the Levels live preview so opening the dialog never poisons the baseline — RESEARCH Pitfall 5).

    main_window.py:
    - `_build_tools_menu`: add the Image section per UI-SPEC surface 21/23: after the existing tool group, a separator + `Rotate ▸` submenu (three QActions: Rotate 90° CW / Rotate 90° CCW / Rotate 180°, no shortcuts, tooltips per §Copywriting) → `_rotate_page(k=-1/1/2)`; + `Levels…` → `_on_levels`; + `Resize…` → `_on_resize`; + separator + existing Cancel Batch.
    - `_apply_geometry_op(op_name, geometry: bool, transform_fn) -> None` — the shared orchestration: gate (≥1 page open, not _op_running); flush via `_snapshot_current_page` (plan 05-05); capture PRE-op state: `pre_image = self.canvas.get_image_numpy().copy()`, `pre_mask = self.canvas.get_mask().copy() if self.canvas.has_mask() else None`, `pre_boxes = self.canvas.boxes_snapshot()`; run transform_fn (returns new image + new mask_bin + new boxes); write back: `set_image_from_numpy(new_image)` (DETACHED — pass the array; the setter copies), `set_mask(numpy_binary_to_mask_qimage(new_mask_bin))` when a mask existed, `set_boxes(new_boxes)` under the `_suppress_boxes_push` guard (the geometry undo entry is the ONLY record — one press per op, PROJ-04 probe truth); `self.history.push_geometry_state(pre_image, pre_mask, pre_boxes)`; `self.canvas.rebaseline_original()`; current `ImageFile.geometry_altered = True` when geometry=True (rotate/resize/crop; NEVER for levels); `_set_session_dirty()`; transient flash (rotate/resize copy per §Copywriting — e.g. rotated / resized-dims forms); `_refresh_action_states()`.
    - `_rotate_page(k: int) -> None` — `_apply_geometry_op("rotate", geometry=True, fn=lambda: image_ops.rotate_page(img, mask, k) + rotate_boxes(...))` with the current image dims passed to rotate_boxes. Menu action → `_rotate_page(-1)` / `_rotate_page(1)` / `_rotate_page(2)`.
    - Gating in `_refresh_action_states`: the three rotate actions + Levels… + Resize… enabled iff page_open AND not _op_running (disabled-init on creation).

    Tests (test_gui_canvas.py): `test_rebaseline_original_after_op` (set an image, mutate it via a second set, call rebaseline_original → Show Original toggles to the POST-op image), `test_preview_path_does_not_capture_original` (set_image_from_numpy_preview(capture_original=False) leaves _original_image_numpy at the pre-preview value). test_gui_image_dialogs.py: `test_rotate_90cw_end_to_end` (real MainWindow + page: invoke _rotate_page(-1) → canvas image dims swapped pixel-exact vs np.rot90 of the pre image; mask rotated; box bbox rotated; ONE undo entry (history._image_undo tail stamp equals _mask_undo/_boxes_undo tail); Ctrl+Z restores image+mask+boxes together; geometry_altered True; status flash contains the rotated copy; Show Original after op shows the POST-op image).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_canvas.py tests/test_gui_image_dialogs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_rebaseline_original_after_op + test_preview_path_does_not_capture_original pass
    - test_rotate_90cw_end_to_end passes (pixel-exact dims/mask/box; single stamp across the three stores; one Ctrl+Z restores all three; geometry_altered True; post-op Show Original)
    - `grep -c "def rebaseline_original" manga_ai_studio/gui/canvas.py` is 1 and `grep -c "def _apply_geometry_op" manga_ai_studio/gui/main_window.py` is 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Rotate works end-to-end with one-press undo, re-baseline, and geometry_altered marking; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Levels dialog — live preview + Cancel-restores-exactly + Apply-pushes-one</name>
  <files>manga_ai_studio/gui/levels_dialog.py, manga_ai_studio/gui/main_window.py, tests/test_gui_image_dialogs.py</files>
  <read_first>
    - manga_ai_studio/gui/load_translations_dialog.py (the _DIALOG_QSS block at 47-72 + QDialog skeleton at 83-149 + collector pattern at 147-192 — the exact template)
    - manga_ai_studio/core/image_ops.py (levels_page/levels_lut — the transform)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pitfall 9 lines 448-452 — the preview-must-not-push contract; Pattern 4 — the white>black guard)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 25 — control ranges, defaults, clamp contract; §Copywriting status rows)
  </read_first>
  <action>
    Create `manga_ai_studio/gui/levels_dialog.py` per UI-SPEC surface 25 (QDialog, _DIALOG_QSS pattern, QFormLayout, title "Levels"):
    - Black point: QSlider + QSpinBox, range 0..255, default 0. White point: QSlider + QSpinBox, range 0..255, default 255. Gamma: log-scaled QSlider + QDoubleSpinBox, range 0.10..4.00, default 1.00, singleStep 0.01 (UI-SPEC spacing exception ±0.01).
    - white>black cross-clamp: white slider minimum follows black+1; black slider maximum follows white−1 (one mechanism — the preview never renders an inverted map; image_ops.levels_lut has the math-level backstop too).
    - Constructor takes the page image (np.ndarray) and a preview callback — the dialog is a COLLECTOR + preview driver, it never mutates models: every control change → `self.preview_callback(self._current_values())`; [Cancel] → `reject()`; [Apply] → store `self.result = self._current_values()` and `accept()`. The pre-dialog image is detached (.copy()) by the CALLER before opening (Pitfall 2 — the restore base).
    - No undo pushes from inside the dialog (Pitfall 9).

    main_window.py `_on_levels() -> None`:
    - `base = self.canvas.get_image_numpy().copy()` (detached restore/apply base); open the dialog with a preview callback `lambda values: self.canvas.set_image_from_numpy_preview(image_ops.levels_page(base, *values), capture_original=False)` (silent — the capture-suppressed path keeps the baseline pristine).
    - Cancel → `self.canvas.set_image_from_numpy(base.copy())` silently (exact pre-dialog image — no entry, no flash).
    - Apply → `self._apply_geometry_op("levels", geometry=False, ...)` with transform_fn = levels_page(base, black, white, gamma) — pushes ONE image-only entry (push_geometry_state with mask_qimage=None, boxes=None), re-baselines, flashes the levels copy; geometry_altered stays UNCHANGED (A4 — levels never moves the D-22 export target).

    Tests (test_gui_image_dialogs.py): `test_levels_cancel_restores_exactly` (open the dialog on a page, drag sliders (preview mutates the canvas), Cancel → canvas image byte-identical to the pre-dialog image; history has NO new entry), `test_levels_apply_pushes_one_entry` (Apply → exactly one IMAGE entry; mask/boxes untouched — geometry-free; the flash fires; Show Original shows the post-levels image), `test_levels_defaults_and_clamp` (dialog opens at 0/255/1.00; dragging black to 200 moves white's minimum to 201; the preview never receives white<=black — assert the callback's last received values keep white>black), `test_levels_preview_no_baseline_poison` (open dialog, preview, Cancel → Show Original still shows the PRE-dialog image).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_image_dialogs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_levels_cancel_restores_exactly passes (byte-identical restore, zero undo entries)
    - test_levels_apply_pushes_one_entry passes (one IMAGE entry; mask/boxes untouched; flash)
    - test_levels_defaults_and_clamp + test_levels_preview_no_baseline_poison pass
    - `grep -c "def _on_levels" manga_ai_studio/gui/main_window.py` is 1 and `grep -c "class LevelsDialog" manga_ai_studio/gui/levels_dialog.py` is 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Levels dialog delivers live preview with exact Cancel restore and one-entry Apply; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Resize dialog + Show Original gating (D-06/D-14)</name>
  <files>manga_ai_studio/gui/resize_dialog.py, manga_ai_studio/gui/main_window.py, tests/test_gui_image_dialogs.py, tests/test_gui_project.py</files>
  <read_first>
    - manga_ai_studio/gui/load_translations_dialog.py (the dialog template — as Task 2)
    - manga_ai_studio/core/image_ops.py (resize_page/resize_boxes — the transform)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 26 — ranges, aspect lock, px/%, live label; surface 29 — the Show Original gating contract; §Copywriting Show Original tooltip row)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Open Question 6 — original_verified consumption in _refresh_action_states; A8 — LANCZOS/NEAREST)
    - manga_ai_studio/gui/main_window.py (_refresh_action_states at 724-804 — the has_inpaint gating at 753-754 to extend; action_show_original; ImageFile.original_verified from plan 05-01)
  </read_first>
  <action>
    Create `manga_ai_studio/gui/resize_dialog.py` per UI-SPEC surface 26 (QDialog, _DIALOG_QSS, QFormLayout, title "Resize"):
    - Width/Height QSpinBox pairs, range 1..100000 px (percent mode: 1..1000), initialized to the current page dims; QComboBox unit toggle "pixels" (default) / "percent" (percent applies to the corresponding current dimension, rounded int, ≥1); QCheckBox "Maintain aspect ratio" checked by default (D-13) — while locked, editing the dominant field recomputes the other (rounded, ≥1); muted QLabel "Result: {w} × {h} px" updating on every change (in % mode it shows resulting pixels). Buttons [Cancel][Apply]. Collector: `self.result = (new_w, new_h)` on accept.
    - main_window.py `_on_resize() -> None`: base capture like levels; on Apply → `_apply_geometry_op("resize", geometry=True, ...)` with resize_page/resize_boxes (LANCZOS image / NEAREST mask, boxes scaled int — image_ops) + the resized-dims flash copy; geometry_altered=True; re-baseline; one entry.
    - `_refresh_action_states` Show Original gating (surface 29): the action is disabled with the not-found tooltip when the current page's `ImageFile.original_verified` is False AND the page came from a project load — read the current ImageFile from `self.image_files[self._last_page_index]`; normal sessions (original_verified True — set on folder/image open by plan 05-05 Task 3) keep the inherited behavior. `canvas.rebaseline_original()` after every op keeps the cache honest (Task 1).

    Tests: `test_resize_dialog_contract` (opens at current dims; aspect lock recomputes the other field on width edit (rounded, ≥1); unlocking frees the other field; % mode applies to the current dimension and the label shows resulting px; ranges clamp 1..100000), `test_resize_apply_end_to_end` (Apply → image dims match, mask resized NEAREST (0/255 only), boxes scaled int, ONE geometry entry, geometry_altered True, flash), `test_show_original_gating` (test_gui_project.py: a .mas-loaded page with original_verified False → action disabled + the not-found tooltip; a folder-open page → enabled — relies on plan 05-05 Task 3's folder-open original_verified=True wiring; after an image op on the folder page → Show Original shows the post-op image — the re-baseline truth).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_image_dialogs.py tests/test_gui_project.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_resize_dialog_contract + test_resize_apply_end_to_end pass
    - test_show_original_gating passes (disabled+tooltip on unverified project page; enabled otherwise; post-op re-baseline)
    - `grep -c "class ResizeDialog" manga_ai_studio/gui/resize_dialog.py` is 1 and `grep -c "original_verified" manga_ai_studio/gui/main_window.py` is >= 2
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Resize dialog and Show Original gating complete with the D-06/D-13/D-14 contracts; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05-06)

- `manga_ai_studio/gui/canvas.py` (EXTEND) — `rebaseline_original()`, `set_image_from_numpy_preview(image_rgb, capture_original)`
- `manga_ai_studio/gui/levels_dialog.py` (NEW) — `LevelsDialog` (black/white/gamma controls, white>black cross-clamp, preview callback, result collector)
- `manga_ai_studio/gui/resize_dialog.py` (NEW) — `ResizeDialog` (W/H spinboxes, px/% toggle, aspect-lock checkbox, live label, result collector)
- `manga_ai_studio/gui/main_window.py` (EXTEND) — `_apply_geometry_op`, `_rotate_page`, `_on_levels`, `_on_resize`, Tools-menu Image section, Show Original gating in `_refresh_action_states`
- `tests/test_gui_image_dialogs.py` (NEW) + `tests/test_gui_canvas.py` + `tests/test_gui_project.py` (EXTEND)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog controls → transforms | Spinbox/slider values cross here — ranges and the white>black clamp are the controls (UI-SPEC surfaces 25/26) |
| canvas cache → Show Original | The `_original_image_numpy` cache is the D-14 trust point — capture-once + preview-suppression keep it honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | DoS | levels live preview flood | medium | mitigate | preview is a synchronous numpy transform in the modal's nested event loop — no history pushes (Pitfall 9), bounded by page dims ≤ MAX_IMAGE_DIMENSION |
| T-05-16 | Tampering | Show Original baseline poisoning (Pitfall 5) | medium | mitigate | `rebaseline_original()` after EVERY op + `set_image_from_numpy_preview(capture_original=False)` for dialog previews — regression-tested (test_levels_preview_no_baseline_poison) |
| T-05-07 | Tampering | levels inversion | low | mitigate | slider cross-clamp (white min = black+1) + image_ops math-level clamp — no inverted map ever renders (UI-SPEC surface 25) |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit); no checkpoints needed |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_image_dialogs.py -x -q` (plus the canvas/project files per task)
- Per wave merge: full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</verification>

<success_criteria>
- Rotate/levels/resize all apply silently with ONE-press undo and correct re-baseline
- Levels preview never pushes, Cancel restores exactly, Apply pushes exactly one
- Show Original gating + tooltip per D-06; geometry_altered excludes levels (A4)
- Full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-06-SUMMARY.md` when done.
</output>
