---
phase: 05-project-persistence-image-ops-export
plan: 08
type: execute
wave: 6
depends_on: [05-03, 05-07]
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_export.py
autonomous: true
requirements: [PROJ-03]
user_setup: []

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Export OCR JSON… (Ctrl+Shift+E, Text menu after Load Translations…): Save As dialog with the *_ocr.json filter, default filename {stem}_ocr.json, default directory per D-22 (pristine -> source folder; geometry-altered -> cleaned/, created if missing); success flashes the single-page copy; write failure shows the save-failure dialog copy"
    - "Batch Export OCR JSON (Batch menu): runs on the existing Worker + _op_running + _batch_active machinery with the phase-2 batch progress surface (status-left text + 3px determinate bar + Cancel Batch); per-page D-22 placement; completion flashes the n/m copy with the failure count; no per-page modals"
    - "Both surfaces exist and are distinct (single-page dialog vs batch action) with distinct feedback (UI-SPEC surface 27)"
    - "Zero-box pages export without a gate or confirm (empty blocks[]); exports describe the CURRENT page state"
    - "The save-side flush runs before both exports (Pitfall 7: _last_page_index + _snapshot_current_page) so the exported JSON matches the live canvas"
  artifacts:
    - "manga_ai_studio/gui/main_window.py: _export_ocr_json / _dispatch_batch_ocr_export / _on_batch_ocr_export_finished + menu actions + gating"
    - "tests/test_gui_export.py (pytest-qt)"
  key_links:
    - "core/ocr_export.write_page_ocr_json + batch_export_ocr (plan 05-03) — the only disk boundary; Worker auto-injection kwargs contract"
    - "ImageFile.geometry_altered (plans 05-01/05-06/05-07) drives the D-22 default dir per page"
    - "_dispatch_batch (main_window.py:3013-3121) — the Worker dispatch template"
---

<objective>
The `_ocr.json` export GUI of PROJ-03 (D-21/D-22): wire Export OCR JSON… (Ctrl+Shift+E, Text menu — Save As dialog with the D-22 default directory and {stem}_ocr.json default name) and Batch Export OCR JSON (Batch menu — Worker + progress + Cancel + mixed-result copy) onto `core/ocr_export` (plan 05-03), with the save-side flush seam (Pitfall 7) so exports always describe the live canvas.

Purpose: this closes the phase's interoperability story — downstream typesetting tools consume the published D-19 shape. The batch path mirrors the Phase 2 batch structure exactly (Worker + _op_running + batch progress), so the UI contract (UI-SPEC surface 27 + §UI Considerations E6/E9) rides tested machinery.

Output: `main_window.py` export wiring + `tests/test_gui_export.py` green (baseline measured at plan time 2026-08-08: 463 collected / 462 passed / 1 failed — sole pre-existing test_gui_boxes.py regression fixed by plan 05-09, Wave 1; re-measure at execution).
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-persistence-image-ops-export/05-CONTEXT.md
@.planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md
@.planning/phases/05-project-persistence-image-ops-export/05-PATTERNS.md
@.planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md
@.planning/phases/05-project-persistence-image-ops-export/05-VALIDATION.md

@manga_ai_studio/gui/main_window.py
@manga_ai_studio/core/ocr_export.py
@manga_ai_studio/gui/worker_thread.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): Export OCR JSON… — Text-menu action → Save As dialog (D-22 default) → write → flash, one page end-to-end</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_export.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_build_text_menu at 448-519 — where Export OCR JSON… lands after Load Translations…; _show_transient_status 1566-1583; the error-dialog pattern (load_translations_dialog.py:168-176 analog); _snapshot_current_page from plan 05-05; _last_page_index; _dispatch_batch at 3013-3121 for the Worker template)
    - manga_ai_studio/core/ocr_export.py (write_page_ocr_json/default_ocr_json_path — the disk boundary; ExportPage shape)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Pitfall 7 — the outgoing-index rule applies to export too; D-21/D-22)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 27 single-page contract; §Copywriting export rows — copy asserted via tests, not grepped)
  </read_first>
  <action>
    In `main_window.py`:

    - `_build_text_menu`: add `Export OCR JSON…` after Load Translations… — QAction, shortcut Ctrl+Shift+E (conflict audit clean: Ctrl+E is Export Page, the Shift-modified sequence is distinct), tooltip per UI-SPEC §Copywriting, disabled-init, → `_export_ocr_json`.
    - `_export_ocr_json() -> None`: gate (page open, not _op_running); `self._snapshot_current_page()` (Pitfall 7 — the flush makes the export describe the live canvas); read the current page's `ImageFile` (`self._last_page_index`): boxes, current image dims (from the canvas — the CURRENT page state per D-22), and `geometry_altered`. Build the default target via `default_ocr_json_path(page_path, geometry_altered)`; open `QFileDialog.getSaveFileName(self, title, str(default_target), "OCR JSON (*_ocr.json)")` — the dialog's default directory follows D-22 and the user may override (UI-SPEC surface 27); empty path → return. Call `write_page_ocr_json(boxes, img_w, img_h, page_path, geometry_altered, path_override=chosen)`. On OSError → critical dialog with the save-failure copy + loguru traceback; on success → transient single-page copy flash (UI-SPEC §Copywriting status row; page number 1-indexed matching the status-bar convention).
    - `_refresh_action_states`: the action enabled iff page open AND not _op_running.

    Tests (`tests/test_gui_export.py` — pytest-qt, real MainWindow + tmp_path): `test_export_single_pristine_page` (folder session with boxes/text; invoke _export_ocr_json with a stubbed dialog path → the JSON lands NEXT TO the source page as {stem}_ocr.json; json.loads → version "1", img dims == current canvas dims, blocks[0] carries text/translation/bubble_no/origin + lines[]; flash fires), `test_export_single_altered_page_default_dir` (page with geometry_altered True → the dialog's default path is inside cleaned/ (created); user override to a custom path wins), `test_export_zero_box_page` (no boxes → blocks [] written, no gate, no confirm), `test_export_write_failure_dialog` (unwritable target → critical dialog, no crash), `test_export_action_gating` (disabled with no page; disabled while _op_running).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_export_single_pristine_page passes (sidecar placement, shape, flash)
    - test_export_single_altered_page_default_dir + test_export_zero_box_page pass
    - test_export_write_failure_dialog + test_export_action_gating pass
    - `grep -c "def _export_ocr_json" manga_ai_studio/gui/main_window.py` is 1 and `grep -v '^#' manga_ai_studio/gui/main_window.py | grep -c 'Ctrl+Shift+E'` is 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Single-page export works end-to-end with D-22 placement and feedback; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Batch Export OCR JSON — Worker dispatch + progress + Cancel + mixed-result copy</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_export.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_dispatch_batch at 3013-3121 — the Worker dispatch template: _flush_current_canvas_mask_to_data_model before dispatch, Worker(task_fn, *args, abort_signal=self.batch_abort_requested), progress/result/error/aborted/finished connections, _op_running gate, file_table disable, progress_bar setup, QThreadPool start; _build_batch_menu structure; _batch_mode/_batch_active/_batch_cancelled semantics)
    - manga_ai_studio/gui/worker_thread.py (Worker contract at 103-169: last-two-kwargs injection, Abort raise, finished always emitted)
    - manga_ai_studio/core/ocr_export.py (batch_export_ocr + ExportPage — the task function shape)
    - .planning/phases/05-project-persistence-image-ops-export/05-RESEARCH.md (Open Question 4 — Worker path recommendation; Pitfall 7 — flush before batch dispatch)
    - .planning/phases/05-project-persistence-image-ops-export/05-UI-SPEC.md (surface 27 batch contract + §UI Considerations E6-loading/E9-loading rows — the progress surface)
  </read_first>
  <action>
    In `main_window.py`:

    - `_build_batch_menu`: add `Batch Export OCR JSON` after the three cleaning batch actions — QAction, no ellipsis (starts immediately, mirrors Phase 2), tooltip per §Copywriting, disabled-init, → `_dispatch_batch_ocr_export`.
    - `_dispatch_batch_ocr_export() -> None`: gate (≥1 page open, not _op_running, not _batch_active); `self._snapshot_current_page()` + `_flush_current_canvas_mask_to_data_model()` (Pitfall 7 — the flush seam, Bug-D pattern); build `pages = [ExportPage(path=image_file.path, boxes=image_file.boxes or [], img_w=..., img_h=..., geometry_altered=image_file.geometry_altered) ...]` — dims read per page: for the current page from the canvas, for others from the saved `current_image`/re-read source (a page whose image is unavailable at batch time exports from its stored dims — keep it simple and deterministic: dims from the ImageFile's known current-image numpy when present, else read the source file dims via PIL); dispatch exactly like `_dispatch_batch` (Worker + `abort_signal=self.batch_abort_requested`, `_op_running = True`, `file_table.setEnabled(False)`, progress_bar determinate 0..100, status-left progress copy from the progress handler, `_batch_active` gate for Cancel Batch enablement). The worker task fn = `partial(ocr_export.batch_export_ocr, pages)` (last-two-kwargs auto-injection).
    - `_on_batch_ocr_export_finished(result: dict) -> None`: `_op_running = False`, `_batch_active = False`, re-enable the table, clear the progress bar, status-left revert to the idle line; flash the completion copy — with the failure-count form when `failed` non-empty (UI-SPEC §Copywriting batch rows); log per-page failures via loguru (stems + error strings only — T-05-05). The aborted handler mirrors the Phase 2 cancel path (partial report with the pages completed so far).
    - `_refresh_action_states`: the action enabled iff ≥1 page open AND not _op_running AND not _batch_active.

    Tests: `test_batch_export_writes_all_pages` (3-page session, one page altered (geometry_altered True) → 3 JSON files: 2 sidecars in the source folder + 1 in cleaned/; each parses with the right dims; completion flash shows the ok count), `test_batch_export_mixed_failure_count` (make one page's source unreadable/missing at batch time → completion flash carries the failure count; the other files still written; no modal), `test_batch_export_cancel` (abort_flag set mid-batch → Cancel Batch path: partial files written, completion flash for the completed count, no crash, _op_running False after), `test_batch_export_gating_and_progress` (action disabled while another batch runs; the progress handler emits {done}/{total} text + the determinate bar advances).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_batch_export_writes_all_pages passes (per-page D-22 placement; dims per page)
    - test_batch_export_mixed_failure_count + test_batch_export_cancel pass
    - test_batch_export_gating_and_progress passes
    - `grep -c "def _dispatch_batch_ocr_export" manga_ai_studio/gui/main_window.py` is 1 and `grep -c "batch_export_ocr" manga_ai_studio/gui/main_window.py` is >= 1
    - Full suite green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
  </acceptance_criteria>
  <done>Batch export is fully wired with progress, cancel, and failure reporting; committed.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05-08)

- `manga_ai_studio/gui/main_window.py` (EXTEND) — `_export_ocr_json`, `_dispatch_batch_ocr_export`, `_on_batch_ocr_export_finished` (+ aborted handler), Text-menu Export OCR JSON… action, Batch-menu action, gating in `_refresh_action_states`
- `tests/test_gui_export.py` (NEW) — 9 pytest-qt test functions

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → disk (`_ocr.json` writes) | Sidecar/cleaned-folder writes must be atomic and never clobber foreign files (same-named JSON from another tool is only overwritten via the user's explicit Save As choice) |
| worker → GUI (batch results) | The {ok, failed, total} dict crosses the thread boundary — signal-only, no shared state (T-01-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Tampering | sidecar overwrite of an existing {stem}_ocr.json | low | accept | single-page export always goes through the Save As dialog (user sees the target); batch writes the D-22 default location — the published placement contract downstream tools rely on (D-22 one-way); no silent deletion |
| T-05-05 | Tampering | log injection via crafted text content | low | mitigate | batch failures log page stems + error strings only, never OCR/translation text (Phase 2 discipline) |
| T-05-20 | DoS | batch on a huge session | medium | mitigate | model-free serialization is fast; abort checked at the loop top per page; per-page failure isolation keeps one bad page from stalling the batch |
| T-05-SC | Tampering | package installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit); no checkpoints needed |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_export.py -x -q`
- Per wave merge: full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</verification>

<success_criteria>
- Single-page and batch export both live with distinct surfaces and feedback
- D-22 placement honored per page (pristine → source, altered → cleaned/)
- Cancel + mixed-failure reporting work through the Phase 2 batch machinery
- Full suite green (baseline at plan time: 463 collected / 462 passed / 1 failed — sole pre-existing failure fixed by 05-09; re-measure at execution)
</success_criteria>

<output>
Create `.planning/phases/05-project-persistence-image-ops-export/05-08-SUMMARY.md` when done.
</output>
