---
phase: 02-cleaning-output-batch
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_batch.py
autonomous: false
requirements:
  - PROJ-02
  - FLOW-03
user_setup:
  - service: manga-models
    why: "The manual batch smoke test needs the real CTD (~80MB) + LaMa (~200MB) model weights cached from Phase 1's first run, plus a real manga chapter folder (5+ pages) to confirm cleaned/ outputs are visually clean."
    manual_steps:
      - "Confirm the models are cached (Phase 1 first-run already downloaded them)."
      - "Have a real manga chapter folder with 5+ pages available for the smoke test."
must_haves:
  truths:
    - "A File menu exposes Export Page (Ctrl+E), Batch Detect, Batch Clean, Batch Detect+Clean, and Cancel Batch actions"
    - "Export Page writes the DISPLAYED canvas image (not a re-clean) to a user-chosen PNG/JPG path via QFileDialog.getSaveFileName"
    - "Each batch action dispatches a single Worker(QRunnable) on the global QThreadPool that runs the Plan 03 batch entry point, with progress_callback writing to status_bar_left + progress_bar (D-10)"
    - "Launching any batch sets _op_running=True, disabling Detect/Inpaint and all three batch actions for the duration (D-08); page navigation is also disabled to avoid the D-11 write race (T-02-05 mitigation)"
    - "_op_running is cleared unconditionally on batch finish/abort/error so the editor is never stuck disabled (Pitfall 7)"
    - "Cancel Batch (Tools menu + Esc QShortcut) emits the abort signal, which sets the SharableFlag; the worker stops cleanly after the current page (D-09)"
    - "On batch completion the status bar shows a summary like 'Cleaned 28/30 pages — 2 failed, see log' (D-04)"
  artifacts:
    - "manga_ai_studio/gui/main_window.py with export_page(), three batch dispatch handlers, batch progress/result/cleanup handlers, a _batch_abort_signal, the five File/Tools menu actions, and navigation-disable-while-running"
    - "tests/test_gui_batch.py with the PROJ-02 export test, the D-08 op_running gate test, and the Pitfall 7 cleanup test passing"
  key_links:
    - "export_page imports save_image_optimized from core.image_io (Plan 01)"
    - "the three batch handlers import batch_detect/batch_clean/batch_detect_and_clean from core.batch_runner (Plan 03) and pass self.image_files + the resolved model paths + cleaned_dir = source_folder.parent/'cleaned'"
    - "the cancel signal connects to Worker via the abort_signal constructor param (worker_thread.py:138-140)"
---

<objective>
Wire the Phase 2 UI into MainWindow: the File-menu Export Page action (Ctrl+E, PROJ-02), the three batch actions (Batch Detect / Batch Clean / Batch Detect+Clean, FLOW-03 D-01), the Cancel affordance (Tools menu + Esc, D-09 Open Question Q1 → A4), the batch progress/result/cleanup signal handlers (D-10, D-04, Pitfall 7), and the navigation-disable-while-running mitigation (T-02-05 / Open Question Q2 → A5). This plan connects Plans 01 (image_io) + 02 (mask persistence) + 03 (batch_runner) to the user.

Purpose: Complete the phase — the user can export a cleaned page and batch-process a chapter with visible progress + cancel.

Output: `gui/main_window.py` (modified — File/Tools menu actions, handlers, abort signal, navigation gate), `tests/test_gui_batch.py` (extended with the PROJ-02/D-08/Pitfall-7 tests). This plan is `autonomous: false` because the manual batch smoke test (real models, real chapter) is a blocking human checkpoint per VALIDATION §Manual-Only.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cleaning-output-batch/02-CONTEXT.md
@.planning/phases/02-cleaning-output-batch/02-RESEARCH.md
@.planning/phases/02-cleaning-output-batch/02-PATTERNS.md
@.planning/phases/02-cleaning-output-batch/02-VALIDATION.md

# The wiring site + the patterns to mirror + the Plan 01/03 callables:
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/worker_thread.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/file_table.py
@manga_ai_studio/core/image_io.py
@manga_ai_studio/core/batch_runner.py
@manga_ai_studio/core/image_file.py

# The GUI test module extended by this plan:
@tests/test_gui_batch.py
@tests/test_inpainting/test_inpaint_gui.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend test_gui_batch.py with the UI-wiring tests (RED)</name>
  <files>tests/test_gui_batch.py</files>
  <read_first>
    - tests/test_gui_batch.py (the module created in Plan 02 — its _make_window/_open_page/_paint_mask_on_canvas helpers, the @pytest.mark.gui marker convention; this task ADDS tests to it)
    - tests/test_inpainting/test_inpaint_gui.py (the _FakeInpaintModel lines 86-115 — the fake to inject so batch dispatch does not hit real torch; the monkeypatch-on-backend_factory or model-injection pattern)
    - manga_ai_studio/gui/main_window.py (the File menu at _build_file_menu line 157, detect_text dispatch at 955, _on_detection_progress at 1108, _on_detection_cleanup at 1155, _refresh_action_states at 451, _current_page_index at 490 — the patterns the new actions/handlers mirror)
    - manga_ai_studio/gui/canvas.py (get_image_numpy at 468 — what export_page calls; has_inpaint_result — to gate export on a displayed result if appropriate)
    - manga_ai_studio/core/batch_runner.py (the three entry-point signatures from Plan 03 — what the dispatch handlers call)
  </read_first>
  <behavior>
    - test_export_writes_displayed_image: Export writes canvas.get_image_numpy() to the chosen path; the (monkeypatched) inpaint/detect models are NOT called by export (Pitfall 5 — export is not a re-clean).
    - test_batch_sets_op_running: while a batch Worker is dispatched, _op_running is True and the detect/inpaint/batch actions report isEnabled()==False (D-08).
    - test_op_running_cleared_after_batch: after the batch Worker finishes (or aborts), _op_running is False and the actions are re-enabled (Pitfall 7 — no stuck-disabled state).
  </behavior>
  <action>
    Extend the existing `tests/test_gui_batch.py` (created in Plan 02) with three new `@pytest.mark.gui` tests. Reuse its existing `_make_window`/`_open_page`/`_paint_mask_on_canvas` helpers. Add a fake-injection seam: monkeypatch `manga_ai_studio.adapters.factory.backend_factory` (or monkeypatch the batch entry points in `manga_ai_studio.core.batch_runner` to a fake that records calls and returns `{"ok":N,"failed":[],"total":N}`) so the batch dispatch does not load real torch models. Drive the Worker to completion synchronously via `qtbot.waitUntil(lambda: not window._op_running)`.

    - `test_export_writes_displayed_image(qtbot, tmp_path, monkeypatch)`: build a window, open a page, paint+apply an inpaint result so the canvas shows a non-original image (use the Phase 1 `_FakeInpaintModel` injection pattern from test_inpaint_gui.py, or set the canvas image directly via `canvas.set_image_from_numpy`), then monkeypatch `QFileDialog.getSaveFileName` to return `(str(tmp_path/"exported.png"), "")` and call `window.export_page()`. Assert `Path(tmp_path/"exported.png").exists()`; assert the export did NOT call the detect or inpaint models (inspect the fake's call list — Pitfall 5). Compare the exported file's pixel data to `canvas.get_image_numpy()` to confirm it is the displayed image, not a re-clean.
    - `test_batch_sets_op_running(qtbot, tmp_path, monkeypatch)`: build a window, load a two-page folder, monkeypatch the batch entry point to a fake that sleeps briefly or records the call, trigger `window._run_batch_detect()` (or the public menu handler). Assert `window._op_running is True` immediately after dispatch and that `window.action_detect_text.isEnabled() is False` (D-08 gate).
    - `test_op_running_cleared_after_batch(qtbot, tmp_path, monkeypatch)`: same setup; `qtbot.waitUntil(lambda: window._op_running is False, timeout=5000)`; then assert `window._op_running is False` AND `window.action_detect_text.isEnabled() is True` (Pitfall 7 — the finished handler cleared the gate). Repeat the assertion for the aborted path if feasible (monkeypatch the abort).

    Do not implement the handlers in this task. Run the suite and confirm the three new tests FAIL (RED — export_page, _run_batch_detect do not exist yet).
  </action>
  <verify>
    <automated>cd "C:/Src/Manga AI Studio" &amp;&amp; python -m pytest tests/test_gui_batch.py::test_export_writes_displayed_image tests/test_gui_batch.py::test_batch_sets_op_running tests/test_gui_batch.py::test_op_running_cleared_after_batch -x 2>&1 | findstr /C:"passed" /C:"failed" /C:"AttributeError"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_export_writes_displayed_image\|test_batch_sets_op_running\|test_op_running_cleared_after_batch" tests/test_gui_batch.py` returns 3 (all three new tests present).
    - The three new tests carry `@pytest.mark.gui`.
    - `python -m pytest tests/test_gui_batch.py::test_export_writes_displayed_image -x` is RED (export_page does not exist — AttributeError or similar).
  </acceptance_criteria>
  <done>Three UI-wiring tests added; the suite is RED because the handlers do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Export Page + batch actions + handlers + cancel + nav-gate (GREEN)</name>
  <files>manga_ai_studio/gui/main_window.py</files>
  <read_first>
    - manga_ai_studio/gui/main_window.py (_build_file_menu lines 157-184 — the QAction→setShortcut→triggered.connect→addAction pattern; detect_text lines 955-996 — the Worker dispatch shape to mirror for batches; _on_detection_progress 1108 + _on_detection_finished 1117 + _on_detection_error 1138 + _on_detection_cleanup 1155 — the four handler shapes to mirror; _refresh_action_states 451 — where to add batch/export action enable/disable logic; _resolve_detection_model_path 1052 + _resolve_inpainting_model_path 1198 — model path resolution to pass to the batch entry points; _detection_backend / inpainting backend accessor; the _build_tools_menu or equivalent if one exists, else _build_file_menu is where batch actions attach; open_image 501 — the getOpenFileName sibling for getSaveFileName)
    - manga_ai_studio/gui/worker_thread.py (Worker constructor lines 113-140 — the abort_signal param auto-injects abort_flag + connects to Worker.abort; WorkerSignals.aborted line 64)
    - manga_ai_studio/gui/file_table.py (select_path 136, setEnabled / interaction toggling — how to disable navigation during a batch)
    - manga_ai_studio/gui/canvas.py (get_image_numpy 468 — the export source; has_inpaint_result — whether export should require a displayed result)
    - manga_ai_studio/core/image_io.py (save_image_optimized — the export write call)
    - manga_ai_studio/core/batch_runner.py (batch_detect/batch_clean/batch_detect_and_clean signatures from Plan 03 — the task fns; the summary dict shape {ok, failed, total})
    - tests/test_gui_batch.py (the test contract from Task 1)
  </read_first>
  <behavior>
    - export_page(): guard on _op_running + current path; read canvas.get_image_numpy(); QFileDialog.getSaveFileName (PNG or JPEG filter by original suffix); save_image_optimized(image_rgb, Path(path), original=current). Does NOT call any model (Pitfall 5).
    - _run_batch_detect / _run_batch_clean / _run_batch_detect_and_clean(): guard on _op_running + open folder; resolve model paths via the existing _resolve_*_model_path helpers; derive cleaned_dir = first_page.path.parent / "cleaned"; build a Worker on the Plan 03 entry point with abort_signal=self._batch_abort_signal; connect progress/result/error/aborted/finished; set _op_running; disable navigation; start on QThreadPool.
    - _on_batch_progress((percent, name)): progress_bar.setValue + status_bar_left text "Cleaning {percent}% — {name}".
    - _on_batch_finished(summary): status_bar_left summary text (D-04); refresh actions.
    - _on_batch_error(worker_error): loguru + error_chip (T-01-08 mirror).
    - _on_batch_cleanup(_args): _op_running=False; re-enable navigation; hide progress_bar; refresh actions (Pitfall 7 — unconditional).
    - _cancel_batch(): emit self._batch_abort_signal.
    - _refresh_action_states extended: batch + export actions enabled iff a folder is open AND not _op_running; cancel enabled iff _op_running for a batch.
  </behavior>
  <action>
    Edit `manga_ai_studio/gui/main_window.py`. Multiple coordinated additions:

    1. ABORT SIGNAL + NAV-STATE FIELD in __init__: add `self._batch_abort_signal = Signal()` — but note `Signal` must be a class attribute on a QObject, not an instance attribute. So declare it as a class-level `batch_abort_requested = Signal()` on MainWindow (place it near any existing class-level Signal declarations; if MainWindow has none yet, add `from PySide6.QtCore import Signal` is already imported at top — verify). Also add a flag `self._batch_active = False` (distinct from `_op_running`, to gate the Cancel action specifically) initialized in __init__ alongside `_last_page_index` (Plan 02) and `_op_running`.

    2. EXPORT PAGE handler `export_page(self) -> None` (Claude's Discretion in CONTEXT — the displayed canvas, NOT a re-clean): guard `if self._op_running: return`; `current = self.file_table.current_path()`; if None return; `image_rgb = self.canvas.get_image_numpy()`; if None return; compute the filter from `current.suffix.lower()` (PNG filter when `.png`, else JPEG); call `path, _ = QFileDialog.getSaveFileName(self, "Export Page", str(current.name), filt)` (mirror open_image's getOpenFileName family at lines 508-513); if not path return; `from manga_ai_studio.core.image_io import save_image_optimized`; `save_image_optimized(image_rgb, Path(path), original=current)`. NO model call (Pitfall 5 — test_export_writes_displayed_image asserts this).

    3. BATCH DISPATCH handler (one private method parameterized by mode, or three thin handlers each calling a shared `_dispatch_batch(mode)` helper — prefer the shared helper to avoid triplication). `_dispatch_batch(self, mode: str) -> None`:
       - `if self._op_running: return`.
       - `if not self.image_files: return` (D-06: batch operates on the open folder; no folder picker).
       - Resolve model paths: `det_path = self._resolve_detection_model_path()` if mode needs detect; `inp_path = self._resolve_inpainting_model_path()` if mode needs inpaint (reuse the existing helpers — CR-10/CR-11 cache short-circuit prevents re-download). Resolve backends via the existing `_detection_backend()` and the inpainting backend accessor (mirror how detect_text/inpaint resolve them).
       - Derive output dir: `first = self.image_files[0]; cleaned_dir = first.path.parent / "cleaned"` (D-07). (The T-02-03 name-guard inside batch_runner double-checks the name.)
       - Pick the task fn + args by mode: detect → `batch_detect` with (pages, det_path, det_backend, cleaned_dir); clean → `batch_clean` with (pages, inp_path, inp_backend, cleaned_dir); detect_and_clean → `batch_detect_and_clean` with (pages, det_path, inp_path, det_backend, inp_backend, cleaned_dir). Import these from `manga_ai_studio.core.batch_runner`.
       - `worker = Worker(task_fn, *args, abort_signal=self.batch_abort_requested)` (the abort_signal auto-injects abort_flag + connects to Worker.abort per worker_thread.py:138-140).
       - Connect: `worker.signals.progress.connect(self._on_batch_progress)`; `worker.signals.result.connect(self._on_batch_finished)`; `worker.signals.error.connect(self._on_batch_error)`; `worker.signals.aborted.connect(self._on_batch_cleanup)`; `worker.signals.finished.connect(self._on_batch_cleanup)` (ALWAYS connect finished to cleanup — Pitfall 7; aborted+finished both go to cleanup, which is idempotent).
       - `worker.setAutoDelete(True)`.
       - Set UI state: `self._op_running = True; self._batch_active = True`; `self.file_table.setEnabled(False)` (NAVIGATION DISABLE — T-02-05 mitigation, Open Question Q2 → A5; the canvas stays read-only-visible); `self._refresh_action_states()`; `self.error_chip.hide()`; `self.progress_bar.setRange(0, 100); self.progress_bar.setValue(0); self.progress_bar.show()`; `self.status_bar_left.setText("Batch\u2026 0%")`.
       - `QThreadPool.globalInstance().start(worker)`.
       - Public handlers: `def batch_detect(self): self._dispatch_batch("detect")` etc. (so menu actions can connect to named methods).

    4. SIGNAL HANDLERS (mirror _on_detection_*):
       - `_on_batch_progress(self, payload)`: if `isinstance(payload, tuple) and len(payload)==2:` unpack `(percent, name)`; `self.progress_bar.setValue(int(percent))`; `self.status_bar_left.setText(f"Cleaning\u2026 {int(percent)}% \u2014 {name}")` (D-10 — page count + name).
       - `_on_batch_finished(self, summary)`: `ok = summary.get("ok",0); total = summary.get("total",0); failed = summary.get("failed",[])`; if failed: `self.status_bar_left.setText(f"Cleaned {ok}/{total} pages \u2014 {len(failed)} failed, see log")` else `f"Cleaned {ok}/{total} pages"` (D-04). `self._refresh_action_states()`.
       - `_on_batch_error(self, worker_error)`: `logger.error(f"Batch failed: {worker_error}")` (T-01-08 mirror of _on_detection_error); `self.error_chip.setText("Batch error"); self.error_chip.show()`; `self.status_bar_left.setText("Batch failed")`.
       - `_on_batch_cleanup(self, _args) -> None` (Pitfall 7 — unconditional): `self._op_running = False; self._batch_active = False`; `self.file_table.setEnabled(True)` (RE-ENABLE navigation); `self.progress_bar.hide()`; `self._refresh_action_states()`. This must be safe to call twice (aborted then finished) — guard with `if not self._op_running and not self._batch_active: return` OR make it idempotent (setting False twice is harmless; setEnabled(True) twice is harmless).

    5. CANCEL handler `_cancel_batch(self) -> None`: `if self._batch_active: self.batch_abort_requested.emit()` (D-09 — sets the SharableFlag; the worker checks it at the next page boundary and raises Abort → signals.aborted → _on_batch_cleanup).

    6. MENU ACTIONS — extend `_build_file_menu` (or add a `_build_batch_menu`/Tools menu). Add QActions mirroring the open_image/open_folder pattern (lines 159-166):
       - `self.action_export_page = QAction("Export Page\u2026", self); setShortcut(QKeySequence("Ctrl+E")); triggered.connect(self.export_page)`.
       - `self.action_batch_detect = QAction("Batch Detect", self); triggered.connect(self.batch_detect)`.
       - `self.action_batch_clean = QAction("Batch Clean", self); triggered.connect(self.batch_clean)`.
       - `self.action_batch_detect_and_clean = QAction("Batch Detect + Clean", self); triggered.connect(self.batch_detect_and_clean)`.
       - `self.action_cancel_batch = QAction("Cancel Batch", self); triggered.connect(self._cancel_batch)`.
       - Add a "Batch" submenu (or a File-menu section with addSeparator) containing the three batch actions; add Cancel to a Tools menu (or the same Batch submenu, disabled unless a batch is running). Wire an Esc `QShortcut` to `_cancel_batch` mirroring the _wire_history_actions QShortcut pattern (lines 686-765 region): `QShortcut(QKeySequence("Esc"), self).activated.connect(self._cancel_batch)`.

    7. EXTEND `_refresh_action_states` (line 451): after the existing detect/inpaint enable lines, add: `folder_open = bool(self.image_files)`; `self.action_export_page.setEnabled(page_open and not self._op_running)`; `self.action_batch_detect.setEnabled(folder_open and not self._op_running)`; same for batch_clean + batch_detect_and_clean; `self.action_cancel_batch.setEnabled(self._batch_active)`.

    Run the Task 1 tests + the Plan 02 GUI tests and confirm all pass (GREEN). Run the full suite `python -m pytest tests/ -x`.
  </action>
  <verify>
    <automated>cd "C:/Src/Manga AI Studio" &amp;&amp; python -m pytest tests/test_gui_batch.py -x 2>&1 | findstr /C:"passed" /C:"failed" /C:"error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def export_page\|def batch_detect\|def batch_clean\|def batch_detect_and_clean\|def _dispatch_batch\|def _on_batch_progress\|def _on_batch_finished\|def _on_batch_error\|def _on_batch_cleanup\|def _cancel_batch" manga_ai_studio/gui/main_window.py` returns all the new method definitions.
    - `grep -n "QFileDialog.getSaveFileName" manga_ai_studio/gui/main_window.py` returns the export_page line (PROJ-02 dialog — T-01-01 sibling, OS-validated path).
    - `grep -n "get_image_numpy" manga_ai_studio/gui/main_window.py` returns the export_page line writing the DISPLAYED image (Pitfall 5 — not a re-clean).
    - `grep -n "abort_signal=" manga_ai_studio/gui/main_window.py` returns the Worker construction passing `abort_signal=self.batch_abort_requested` (D-09 cancel wiring).
    - `grep -n "self.file_table.setEnabled(False)\|self.file_table.setEnabled(True)" manga_ai_studio/gui/main_window.py` returns BOTH (navigation disabled at dispatch, re-enabled at cleanup — T-02-05 mitigation).
    - `grep -c "self._op_running = False" manga_ai_studio/gui/main_window.py` is at least 2 (the existing _on_detection_cleanup/_on_inpaint_cleanup plus the new _on_batch_cleanup — Pitfall 7 unconditional clear).
    - `grep -n "Ctrl+E\|Batch Detect\|Cancel Batch" manga_ai_studio/gui/main_window.py` returns the menu action definitions.
    - `grep -n 'failed, see log\|Cleaned {' manga_ai_studio/gui/main_window.py` returns the D-04 summary status text.
    - `python -m pytest tests/test_gui_batch.py -x` reports the Plan 02 + Plan 04 tests passing (test_export_writes_displayed_image, test_batch_sets_op_running, test_op_running_cleared_after_batch, plus the 3 from Plan 02).
    - `python -m pytest tests/ -x` full suite green (no Phase 1 regression).
  </acceptance_criteria>
  <done>Export Page, the three batch actions, cancel, progress/result/cleanup handlers, the navigation gate, and the menu wiring all pass their tests with no Phase 1 regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke test of the real-model output path (blocking)</name>
  <files>.planning/phases/02-cleaning-output-batch/02-04-SUMMARY.md</files>
  <action>
    The complete Phase 2 output path is now automated-green using fake adapters (Plans 01-04 unit/integration/GUI suites). This task is the VALIDATION.md §Manual-Only smoke test, which requires the real CTD (~80MB) + LaMa (~200MB) model weights and a real manga chapter folder that no unit test can exercise. The human verifies what was built: single-page Export (File -> Export Page, Ctrl+E) writes the displayed canvas image as PNG/JPG; the three batch actions (Batch Detect, Batch Clean, Batch Detect + Clean) process the open folder through the cleaning pipeline writing to cleaned/; per-page progress shows in the status bar; Cancel via Tools -> Cancel Batch / Esc stops the run between pages. No code change is made in this task — it is the visual/functional gate before phase close.
  </action>
  <verify>
    <human-check>
      Perform the six manual checks below on the project venv (Phase 1 env, Python 3.12, numpy<2), launched via `python -m manga_ai_studio` (or the project's documented entry point):
      1. EXPORT PAGE (PROJ-02): Open a single manga page; run Detect + Inpaint so the canvas shows a cleaned result; File -> Export Page (or Ctrl+E); in the Save dialog choose a .png path; open the exported file and confirm it MATCHES the displayed canvas (text removed, artwork restored) and does NOT differ from the screen (Pitfall 5).
      2. BATCH DETECT + CLEAN (FLOW-03): File -> Open Folder on a real chapter (5+ pages); run Batch Detect + Clean; watch the status bar cycle "Cleaning N% - page-name" and the progress bar advance; on finish confirm the summary reads like "Cleaned N/N pages" (or "Cleaned N/M - K failed, see log").
      3. CLEANED/ OUTPUT (D-03/D-07): Open the cleaned/ subfolder next to the chapter; confirm each page has text removed + artwork restored, no-text pages are byte-identical to source (copy2 passthrough), and NO file was written to the chapter folder itself (only into cleaned/).
      4. CANCEL (D-09): Start a Batch Detect + Clean on a 5+ page folder; during page 2-3 press Esc (or Tools -> Cancel Batch); confirm the run stops AFTER the current page, cleaned/ holds the pages done before cancel, no file is truncated (Pitfall 4), and Detect/Inpaint menu items re-enable afterward (Pitfall 7).
      5. REVIEW-IN-BETWEEN (D-01/D-02): Open a folder; run Batch Detect; navigate to a page and confirm its detected mask is shown (D-11 survived navigation); hand-edit the mask with the brush; run Batch Clean; confirm the edited mask (not a fresh re-detect) was used - the cleaned output reflects the edit (edits are sacred, D-02).
      6. CONCURRENCY GATE (D-08): While a batch runs, confirm page navigation is disabled (sidebar non-interactive) and the canvas stays read-only-visible.
    </human-check>
    <automated>cd "C:/Src/Manga AI Studio" &amp;&amp; python -m pytest tests/ 2>&1 | findstr /C:"passed" /C:"failed"</automated>
  </verify>
  <done>All six manual checks pass (Export matches canvas; Batch Detect+Clean produces visually-clean cleaned/ outputs; no-text pages are byte-identical; Cancel stops cleanly between pages with no truncation; review-in-between uses the edited mask; navigation is gated during a run) and the full automated suite is green.</done>
  <resume-signal>Type "approved" if all six checks pass, or describe which step failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → Worker dispatch | Menu/shortcut triggers cross from the GUI thread into batch dispatch; the `_op_running` gate is the concurrency guard. |
| user save path → filesystem | Export's `getSaveFileName` path crosses into `save_image_optimized` write. |
| batch worker thread → ImageFile.mask / cleaned/ | The Plan 03 loop writes masks + outputs off the GUI thread; the navigation gate prevents the concurrent on_page_selected write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | export save path | medium | mitigate | Export uses `QFileDialog.getSaveFileName` (OS-validated path, T-01-01 sibling of open_image's getOpenFileName). The path is never derived from untrusted input. The write goes through Plan 01's `save_image_optimized` which calls `path.parent.mkdir(parents=True, exist_ok=True)` but writes only to the handed path. |
| T-02-02 | Tampering / Destruction | originals via batch | high | mitigate | The batch output dir is derived (`first.path.parent / "cleaned"`, D-06/D-07) and double-guarded by batch_runner's `cleaned_dir.name == "cleaned"` assert (T-02-03, Plan 03). The dispatch handler passes only that derived dir; originals are read-only inputs. |
| T-02-05 | Tampering / DoS | concurrent write on ImageFile.mask (Q2 race) | high | mitigate | THIS PLAN closes the race: `_dispatch_batch` calls `self.file_table.setEnabled(False)` so on_page_selected cannot fire during the run (Open Question Q2 → A5; consistent with D-08 "batch blocks the editor"). `_on_batch_cleanup` re-enables it. The canvas stays visible (read-only viewing preserved per D-08). |
| T-02-06 | DoS | editor stuck disabled (Pitfall 7) | high | mitigate | `_on_batch_cleanup` is connected to BOTH `aborted` and `finished` signals and unconditionally clears `_op_running` + `_batch_active` + re-enables `file_table`. Worker.run's `finally` ALWAYS emits `finished` (worker_thread.py:164-165), so cleanup always runs. test_op_running_cleared_after_batch is the regression guard. |
| T-02-09 | Spoofing / Tampering | Esc shortcut hijack | low | accept | Esc is a conventional cancel affordance; wiring it to `_cancel_batch` (guarded by `if self._batch_active`) cannot trigger when no batch runs. No credential or destructive action is bound to Esc. |
| T-02-SC | Tampering | package installs | low | accept | No new packages. No install checkpoint. |
</threat_model>

<verification>
- `python -m pytest tests/test_gui_batch.py -x` green (Plan 02 + Plan 04 GUI tests).
- `python -m pytest tests/ -x` full suite green (no Phase 1 regression).
- Manual smoke (the blocking checkpoint above): real chapter Batch Detect+Clean produces visually-clean `cleaned/` outputs; Export writes the displayed image; Cancel stops cleanly between pages; review-in-between uses the edited mask.
</verification>

<success_criteria>
- File menu exposes Export Page (Ctrl+E), Batch Detect, Batch Clean, Batch Detect + Clean; Tools exposes Cancel Batch; Esc also cancels.
- Export writes the displayed canvas image (not a re-clean) as PNG/JPG via getSaveFileName.
- Batch dispatch reuses the Plan 03 entry points, sets _op_running, disables navigation + model actions, shows per-page progress, and clears _op_running on finish/abort/error.
- Cancel stops the batch cleanly after the current page; no half-written output; the editor re-enables.
- The manual smoke test confirms real-model output quality on a real chapter.
</success_criteria>

<output>
Create `.planning/phases/02-cleaning-output-batch/02-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `MainWindow.batch_abort_requested` | class-level Signal | `manga_ai_studio/gui/main_window.py` |
| `MainWindow._batch_active` | instance field | `manga_ai_studio/gui/main_window.py` (MainWindow.__init__) |
| `MainWindow.export_page(self)` | method | `manga_ai_studio/gui/main_window.py` |
| `MainWindow._dispatch_batch(self, mode)` | method | `manga_ai_studio/gui/main_window.py` |
| `MainWindow.batch_detect(self)` / `batch_clean(self)` / `batch_detect_and_clean(self)` | methods | `manga_ai_studio/gui/main_window.py` |
| `MainWindow._on_batch_progress(self, payload)` | method | `manga_ai_studio/gui/main_window.py` |
| `MainWindow._on_batch_finished(self, summary)` | method | `manga_ai_studio/gui/main_window.py` |
| `MainWindow._on_batch_error(self, worker_error)` | method | `manga_ai_studio/gui/main_window.py` |
| `MainWindow._on_batch_cleanup(self, _args)` | method | `manga_ai_studio/gui/main_window.py` |
| `MainWindow._cancel_batch(self)` | method | `manga_ai_studio/gui/main_window.py` |
| `action_export_page` / `action_batch_detect` / `action_batch_clean` / `action_batch_detect_and_clean` / `action_cancel_batch` | QAction attributes | `manga_ai_studio/gui/main_window.py` (_build_file_menu / batch submenu) |
| Esc → Cancel Batch `QShortcut` | shortcut wiring | `manga_ai_studio/gui/main_window.py` |
| `test_export_writes_displayed_image` | test | `tests/test_gui_batch.py` |
| `test_batch_sets_op_running` | test | `tests/test_gui_batch.py` |
| `test_op_running_cleared_after_batch` | test | `tests/test_gui_batch.py` |
</content>
</invoke>
