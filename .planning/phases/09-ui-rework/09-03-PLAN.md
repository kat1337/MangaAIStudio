---
phase: 09-ui-rework
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - manga_ai_studio/gui/side_panel.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_edit_section.py
  - tests/test_gui_curves_dialog.py
  - tests/test_gui_crop_tool.py
autonomous: true
requirements: [UI-05]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The panel's fourth section 'Edit' renders a 2-column grid of six text QToolButtons bound via setDefaultAction to the LIVE window actions: Curves…, Crop… (numeric dialog), Resize…, Rotate 90° CW, Rotate 90° CCW, Rotate 180° (D-08)."
    - "Clicking an Edit button triggers exactly the same handler as the old menu entry did (curves dialog, numeric crop dialog, resize dialog, page rotation) — zero new op logic; the section is pure relocation of proven entry points (UI-05)."
    - "Edit buttons' enabled state tracks _refresh_action_states gating automatically through default-action binding — disabled with no page open, disabled while an async op runs, icons/labels grey automatically."
    - "The Crop TOOL (G, armed on-canvas rect) remains a strip button only; the Edit section hosts the numeric Crop… dialog entry — the two stay distinct (D-08)."
    - "The Tools menu's Image section no longer lists Rotate ▸ / Curves… / Resize…, and the Edit menu no longer lists Crop… — while every QAction object stays ALIVE as a state holder (D-09)."
    - "Keyboard reachability is unchanged: rotate/curves/resize/crop-dialog carried no shortcuts before and gain none; tool shortcuts V/B/R/L/E/G and D/C live on window-level actions independent of menus and still fire (D-09)."
  artifacts:
    - "EditSection body widget inside side_panel.py (2-column QGridLayout of six text QToolButtons; class name at executor's discretion) added as the panel's fourth CollapsibleSection titled 'Edit' with settings_key sidePanel/editExpanded"
    - "tests/test_gui_edit_section.py — default-action identity + trigger parity + gating + menu-slimming membership assertions"
    - "Updated tests/test_gui_curves_dialog.py (menu-membership assertion re-targeted to Edit-section binding) and tests/test_gui_crop_tool.py (crop-dialog menu test re-targeted)"
  key_links:
    - "Edit buttons ↔ existing window QActions via setDefaultAction — enablement/tooltip/status-tip inheritance for free"
    - "Edit CollapsibleSection ↔ QSettings sidePanel/editExpanded — same persistence mechanism as the other three sections"
    - "Removed menu addAction lines ↔ kept-alive QAction objects — the action_detect_boxes_mode state-holder precedent (main_window.py:807-818)"
  prohibitions:
    - "NEVER delete or recreate the QAction objects removed from menus — remove only the menu addAction membership; shortcuts/state/tests die with deleted actions. [flagged-unverified]"
    - "NEVER remove or alter any setShortcut call during menu slimming — the keyboard-reachability contract; the warning sign is any removed setShortcut rather than a removed addAction line. [flagged-unverified]"
    - "NEVER put the Crop TOOL (G) into the Edit section or the numeric Crop… dialog into the strip — D-08 assigns the interactive tool to the strip and the dialog entry to Edit. [flagged-unverified]"
    - "NEVER implement Edit buttons as lambda-buttons calling .trigger() on the actions — default-action binding is the contract; lambda buttons need manual gating mirrors and drift. [flagged-unverified]"
  assumptions:
    - "{ statement: UI-05 probe returned 'unclassified — review manually' — flagged-unverified. Planner assumption: 'levels' in the UI-05 wording reads as the curves dialog that superseded it (Phase 6 D-01, user-locked in 09-CONTEXT D-08); no separate Levels entry is created.", verification: flagged-unverified }
---

<objective>
Complete the panel with the "Edit" section — six default-action buttons opening the existing Curves/Crop-dialog/Resize dialogs plus instant rotates — and slim the Tools/Edit menus accordingly, with zero new image-op logic and zero keyboard loss.

Purpose: UI-05 + D-08/D-09 — image-editing entry points get one panel home; menus stop duplicating them.
Output: Edit section in the unified panel, slimmed menus, `tests/test_gui_edit_section.py`, updated curves/crop tests.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-rework/09-CONTEXT.md
@.planning/phases/09-ui-rework/09-RESEARCH.md
@.planning/phases/09-ui-rework/09-PATTERNS.md
@.planning/phases/09-ui-rework/09-UI-SPEC.md

# Build surfaces
@manga_ai_studio/gui/side_panel.py
@manga_ai_studio/gui/main_window.py
@tests/test_gui_curves_dialog.py
@tests/test_gui_crop_tool.py

# Prior plan summaries (SidePanel/CollapsibleSection API from 09-02; strip ownership from 09-01)
@.planning/phases/09-ui-rework/09-01-SUMMARY.md
@.planning/phases/09-ui-rework/09-02-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols/files created by THIS plan (exclude from drift verification — they are new):

- **New class (inside side_panel.py):** `EditSection(QWidget)` — 2-column QGridLayout of six text QToolButtons; constructor receives the six live QActions (curves, crop-dialog, resize, rotate CW/CCW/180 — exact MainWindow attribute names confirmed from main_window.py:903-947 region); each button `setDefaultAction(act)`, ToolButtonTextOnly, secondary chrome, sm (8px) grid gaps
- **New panel section:** fourth CollapsibleSection titled "Edit" with `settings_key="sidePanel/editExpanded"` appended by SidePanel assembly in `_build_docks`
- **New test file:** `tests/test_gui_edit_section.py`
- **Menu membership changes (D-09):** Rotate submenu + Curves… + Resize… removed from the Tools menu's Image section; Crop… removed from the Edit menu — addAction lines only
- **No new QActions, no new dialogs, no new signals, no new settings keys beyond the existing editExpanded key**

<tasks>

<task type="auto">
  <name>Task 1: EditSection body — six default-action buttons as the panel's fourth section</name>
  <files>manga_ai_studio/gui/side_panel.py, manga_ai_studio/gui/main_window.py, tests/test_gui_edit_section.py</files>
  <read_first>
    manga_ai_studio/gui/main_window.py lines 555-570 (action_crop_dialog creation + Edit-menu membership), 903-971 (rotate/curves/resize action creation + tooltips + Tools-menu Image section), 1095-1130 (_refresh_action_states gating of these actions)
    manga_ai_studio/gui/side_panel.py (SidePanel.add_section / CollapsibleSection from 09-02)
    .planning/phases/09-ui-rework/09-UI-SPEC.md §40 (Edit section contract + button labels/tooltips) + §Copywriting (labels table)
    .planning/phases/09-ui-rework/09-RESEARCH.md Pattern 3 (setDefaultAction rationale)
    .planning/phases/09-ui-rework/09-PATTERNS.md §EditSection body
  </read_first>
  <action>
Write the failing Edit-section tests FIRST (RED), then implement.

1. Add `EditSection(QWidget)` to side_panel.py: a 2-column QGridLayout of six text QToolButtons, each bound with `setDefaultAction(<live window action>)` — Curves… (action_curves), Crop… numeric dialog (action_crop_dialog), Resize… (action_resize), Rotate 90° CW / 90° CCW / 180° (the three rotate actions created around main_window.py:903-947 — confirm exact attribute names before wiring). Labels/tooltips/status-tips inherit from the actions automatically; do not re-set them. No lambda wiring.
2. In MainWindow panel assembly, append the fourth section: CollapsibleSection("Edit", EditSection(...)) with settings_key sidePanel/editExpanded — after Typesetting, completing the D-02 workflow order.
3. Gating: verify no extra code is needed — _refresh_action_states already recomputes the six actions' enabled state on page/op events and default-action buttons track it; if any button does NOT track (e.g. an action gated by a mechanism default-actions miss), fix the action's gating, not the button.
4. Tests (tests/test_gui_edit_section.py): (a) the panel's fourth section is titled "Edit" and its body contains exactly six QToolButtons; (b) each button's defaultAction() identity-equals the corresponding window action; (c) trigger parity — triggering a button fires the same slot effect as the action (use a spy/monkeypatch on the MainWindow handler, e.g. _on_curves, and assert the button click reaches it); (d) gating parity — with no page open all six buttons are disabled, and they enable after a page load per the existing gating tests' fixture pattern.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_edit_section.py tests/test_gui_side_panel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Panel section order is Detection settings, Brush, Typesetting, Edit; the Edit body is a 2-column grid of exactly 6 buttons
    - Every button's defaultAction() identity-equals its live window action (curves, crop-dialog, resize, rotate x3)
    - A button click invokes the same MainWindow handler the old menu entry invoked (spy assertion)
    - All six buttons start disabled on a fresh window and follow the action gating after page load
    - Quick command above exits green
  </acceptance_criteria>
  <done>Edit section live with six inherited-gating dialog/rotate entry points; zero new op logic.</done>
</task>

<task type="auto">
  <name>Task 2: Menu slimming (D-09) — remove Image-section and Crop… entries, keep actions alive, verify shortcuts</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_edit_section.py, tests/test_gui_curves_dialog.py, tests/test_gui_crop_tool.py</files>
  <read_first>
    manga_ai_studio/gui/main_window.py lines 540-570 (_build_edit_menu — Crop… entry), 903-971 (_build_tools_menu Image section — Rotate submenu / Curves… / Resize… addAction lines), 807-818 (action_detect_boxes_mode keep-alive precedent + its NOTE comment)
    tests/test_gui_curves_dialog.py lines 575-595 (menu-membership assertion to re-target; wrapper-lifetime handling)
    tests/test_gui_crop_tool.py around line 358 (test_crop_action_in_tools_menu)
    tests/test_gui_detection_settings.py lines 275-288 + 346-354 (_tools_menu_texts helper + membership assertion pattern — reuse for the slimming assertions)
    .planning/phases/09-ui-rework/09-RESEARCH.md Pitfall 5 (shortcut audit — verified: these actions carry NO shortcuts; V/B/R/L/E/G/D/C live on window-level actions/shortcuts independent of menus)
  </read_first>
  <action>
1. In `_build_tools_menu`: remove the Image-section addAction lines for the Rotate submenu, Curves…, and Resize… (the submenu object and its three rotate addAction lines go; the four QAction objects stay constructed exactly as today). In `_build_edit_menu`: remove the Crop… addAction line (action_crop_dialog stays constructed). Delete NO QAction construction and NO setShortcut call — menu-membership removal only, mirroring the action_detect_boxes_mode state-holder precedent (keep a one-line NOTE comment there citing D-09).
2. Extend tests/test_gui_edit_section.py with membership assertions using the _tools_menu_texts pattern (hold menu-bar action wrappers while resolving — PySide6 wrapper-lifetime lesson): the Tools menu's action texts no longer include the Curves/Rotate/Resize entries and the Edit menu's texts no longer include the Crop… dialog entry; the window still exposes all four action attributes.
3. Update tests/test_gui_curves_dialog.py: replace the assertion that the curves action is a member of the Tools menu with the new contract (Edit-section defaultAction binding + action object alive on the window). Update tests/test_gui_crop_tool.py's crop-menu test to assert the strip hosts the crop TOOL and the Edit section hosts the numeric dialog entry (two distinct buttons, distinct actions).
4. Shortcut audit in tests: the existing tool-shortcut tests (V/B/R/L/E/G, D, C) must pass UNCHANGED — run them explicitly; if any fails, a shortcut was touched in error (fix the removal, never re-add shortcuts to the slimmed actions).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_edit_section.py tests/test_gui_curves_dialog.py tests/test_gui_crop_tool.py tests/test_gui_detection_settings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Tools-menu text list contains neither the Curves entry, nor the Resize entry, nor a Rotate submenu; Edit-menu text list contains neither the numeric Crop… entry
    - action_curves, action_resize, action_crop_dialog and the three rotate actions still exist on the window with unchanged tooltips and no shortcut changes
    - test_gui_curves_dialog.py and test_gui_crop_tool.py pass against the new contract; all tool/detect/inpaint shortcut tests pass unchanged
    - Quick command above exits green
  </acceptance_criteria>
  <done>Menus slimmed per D-09 with all actions alive as state holders, keyboard reachability proven intact, Edit section is the single mouse-driven home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan binds existing QActions to buttons; no new input parsing, network, or persistence surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09c-01 | Tampering | Edit-section button labels/tooltips | low | accept | Labels/tooltips inherited verbatim from existing actions (plain text); no new user-input surface; dialog behavior unchanged |
| T-09c-02 | Denial of Service | Menu slimming dropping keyboard access | medium | mitigate | Shortcut audit encoded in tests: all existing tool/detect/inpaint shortcut tests must pass unchanged; prohibition against any setShortcut removal |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_edit_section.py -x -q` plus touched suites
- Wave/phase close: full suite `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` green before /gsd-verify-work
- Manual UAT deferral: visual pass over the final layout (strip accent highlight, panel feel) → end-of-phase human gate
</verification>

<success_criteria>
- Edit section present with six default-action buttons, inherited gating, correct D-02 position — UI-05/D-08
- Tools/Edit menus slimmed with all QActions alive; zero shortcut loss — D-09
- Crop tool (strip) vs Crop… dialog (Edit) distinction asserted
- Full suite green (strict superset of the 552-pass baseline)
</success_criteria>

<output>
Create `.planning/phases/09-ui-rework/09-03-SUMMARY.md` when done
</output>
