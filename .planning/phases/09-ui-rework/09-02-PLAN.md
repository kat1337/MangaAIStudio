---
phase: 09-ui-rework
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - manga_ai_studio/gui/side_panel.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/tools_panel.py
  - manga_ai_studio/gui/inspector_panel.py
  - tests/test_gui_side_panel.py
  - tests/test_gui_canvas.py
  - tests/test_gui_detection_settings.py
autonomous: true
requirements: [UI-01, UI-02, UI-04]
estimate:
  tokens: 65000
  raw_tokens: 32500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One unified right-side dock titled 'Panel' replaces the tabified two-dock pair; its body is a vertical stack of four independently collapsible sections in workflow order per D-01/D-02: Detection settings → Brush → Typesetting → Edit (the Edit section body itself lands in plan 09-03 — this plan delivers the section shell for it)."
    - "Each section collapses/expands INDEPENDENTLY via a checkable header row toggling its body with plain setVisible — no animation; QToolBox is not used anywhere in the panel."
    - "The Typesetting section body IS the existing InspectorPanel content (text fields + Style section + inpaint override combo, Mixed-state machinery intact) and its signals survive relocation unchanged (UI-04 rename does not touch class name or signal names)."
    - "A chevron toggle QToolButton sits at the very top of the panel ('Toggle panel', ▸/▾); clicking it collapses/expands the whole scroll-area body while the dock STAYS DOCKED; toggle state is session-transient and NOT persisted (UI-02)."
    - "The View menu has ONE 'Toggle Panel' action driving the same panel-body toggle; 'Toggle Sidebar' is unchanged; no View-menu action references a dead dock."
    - "Every user-visible string that named the old inspector chrome now reads 'Typesetting' (section) / 'Panel' (dock); no persisted state was keyed on the old name so nothing else restores stale chrome."
    - "Per-section collapse state persists across sessions via QSettings keys sidePanel/detectionExpanded, sidePanel/brushExpanded, sidePanel/typesettingExpanded, sidePanel/editExpanded — written on every toggle, restored after build with the tolerant bool parse, all four EXPANDED on first run (D-02)."
    - "When the four expanded sections exceed panel height at 1024×720, ONE vertical-only scroll wrap scrolls the stack; the horizontal scrollbar is permanently off (A11 rule carried over)."
    - "All relocated control signals keep their exact names and semantics: brush_size_changed, detect_boxes_changed, dilation_changed, std_dev_threshold_changed, masker_params_changed — MainWindow wiring and direct-emitting tests keep working (access-path churn updated in the same commit)."
    - "Section controls keep mirroring _refresh_action_states gating (page-open / op-running) exactly as they do today."
  artifacts:
    - "manga_ai_studio/gui/side_panel.py with CollapsibleSection(QWidget) (checkable header + body visibility toggle + optional settings_key) and SidePanel(QWidget) (header chevron row + single vertical-only QScrollArea wrap + add_section API)"
    - "MainWindow._build_docks rework: self.dock_panel = QDockWidget('Panel', self) hosting the SidePanel; dock_tools/dock_inspector/tabify removed"
    - "tools_panel.py slimmed: tool row/group/set_active_tool machinery REMOVED (relocated to ToolsStrip in 09-01); Brush rows + detection-settings form preserved as separately embeddable section bodies with blockSignals mirror pairs verbatim"
    - "self.action_toggle_panel = QAction('Toggle Panel', self) replacing action_toggle_tools/action_toggle_inspector"
    - "tests/test_gui_side_panel.py covering independent collapse, Typesetting section identity, chevron toggle, persistence round-trip"
  key_links:
    - "CollapsibleSection.header.toggled ↔ body.setVisible — the UI-01 mechanism"
    - "SidePanel sections ↔ QSettings sidePanel/*Expanded keys — tolerant parse copied from _read_detect_boxes_mode (accept true/1/yes/on)"
    - "action_toggle_panel.triggered ↔ SidePanel panel-body toggle — one action, one behavior, dock stays docked"
    - "MainWindow signal consumers ↔ relocated bodies' signals — names unchanged (tool_changed consumption moved to the strip in 09-01; brush/masker signals stay on the relocated bodies)"
  prohibitions:
    - "NEVER use QToolBox for the panel sections — it shows only its current item and violates the independent-collapse requirement. [flagged-unverified]"
    - "NEVER animate collapse (QPropertyAnimation on maximumHeight) — plain setVisible is the stable form; animation fights QScrollArea sizing. [flagged-unverified]"
    - "NEVER nest per-section scroll areas or turn the horizontal scrollbar on — exactly ONE vertical-only widgetResizable wrap around all four sections. [flagged-unverified]"
    - "NEVER persist the panel-header chevron state to QSettings — only per-section collapse persists (D-02 contract line). [flagged-unverified]"
    - "NEVER rename the InspectorPanel class or any of its public signal names during the rename sweep — the rename reach is user-visible strings only; internal churn breaks tests and wiring for zero user value. [flagged-unverified]"
  assumptions:
    - "{ statement: UI-01 probe returned 'unclassified — review manually' — flagged-unverified. Planner assumption: 'modular' means exactly the four D-02 sections, independently collapsible, per D-01/D-02; final visual judgment deferred to end-of-phase UAT.", verification: flagged-unverified }
    - "{ statement: UI-02 idempotency probe ('What happens if this runs twice on the same input?') is unresolved — flagged-unverified. Planner assumption: the chevron toggle is a plain boolean setVisible flip; invoking it twice returns the panel to its prior visibility by construction; no other state mutates.", verification: flagged-unverified }
    - "{ statement: UI-02 concurrency probe ('If interrupted or run in parallel, what is guaranteed?') is unresolved — flagged-unverified. Planner assumption: the Qt event loop serializes all toggle handling on the main thread; no parallel-entry guarantee exists beyond that.", verification: flagged-unverified }
    - "{ statement: UI-04 probe returned 'unclassified — review manually' — flagged-unverified. Planner assumption: the rename reach is every USER-VISIBLE occurrence (dock title, menu action label, docstrings opportunistically, status/tooltips if any); the Python class name InspectorPanel stays.", verification: flagged-unverified }
---

<objective>
Replace the tabified Tools+Inspector docks with ONE unified right-side panel of four independently collapsible sections (Detection settings, Brush, Typesetting, Edit-shell), move the inspector toggle to the top of that panel, complete the Inspector→Typesetting rename, and persist per-section collapse state — without renaming any signal or losing any control.

Purpose: UI-01, UI-02, UI-04 — the modular panel is the structural core of the phase; everything else (strip done in 09-01, Edit body in 09-03) hangs off it.
Output: `side_panel.py` (CollapsibleSection + SidePanel), reworked `_build_docks` + View menu, slimmed `tools_panel.py`, new `tests/test_gui_side_panel.py`.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-rework/09-CONTEXT.md
@.planning/phases/09-ui-rework/09-RESEARCH.md
@.planning/phases/09-ui-rework/09-PATTERNS.md
@.planning/phases/09-ui-rework/09-UI-SPEC.md

# Build surfaces
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/tools_panel.py
@manga_ai_studio/gui/inspector_panel.py

# Prior plan summary (ToolsStrip API this plan's set_active_tool rewiring depends on)
@.planning/phases/09-ui-rework/09-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols/files created by THIS plan (exclude from drift verification — they are new):

- **New file:** `manga_ai_studio/gui/side_panel.py` — class `CollapsibleSection(QWidget)` (members `header` (checkable QToolButton), `body` (QWidget), constructor params `title: str`, `body: QWidget`, optional `settings_key: str | None`; method `set_expanded(bool)`); class `SidePanel(QWidget)` (members `toggle_button` (chevron QToolButton, tooltip "Toggle panel"), `body_scroll` (QScrollArea), methods `add_section(section: CollapsibleSection)`, `set_body_visible(visible: bool)`, `is_body_visible() -> bool`)
- **New MainWindow attribute:** `self.dock_panel` (QDockWidget titled "Panel", objectName "dock_panel"); `self.side_panel` (SidePanel instance)
- **New QAction:** `self.action_toggle_panel = QAction("Toggle Panel", self)` (replaces action_toggle_tools + action_toggle_inspector)
- **New QSettings keys:** `sidePanel/detectionExpanded`, `sidePanel/brushExpanded`, `sidePanel/typesettingExpanded`, `sidePanel/editExpanded`
- **Refactored classes in tools_panel.py:** separately embeddable Detection-settings and Brush section bodies (class naming at executor's discretion; ALL existing signal names preserved)
- **New test file:** `tests/test_gui_side_panel.py`
- **Removed attributes (by design):** `dock_tools`, `dock_inspector`, `action_toggle_tools`, `action_toggle_inspector`

<tasks>

<task type="tracer">
  <name>Task 1: Unified Panel dock end-to-end — CollapsibleSection + SidePanel shell + Typesetting section + View-menu retarget</name>
  <files>manga_ai_studio/gui/side_panel.py, manga_ai_studio/gui/main_window.py, manga_ai_studio/gui/inspector_panel.py, tests/test_gui_side_panel.py, tests/test_gui_canvas.py</files>
  <read_first>
    manga_ai_studio/gui/main_window.py lines 296-343 (_build_docks — the two-dock + tabify structure being replaced), 572-668 (_build_view_menu incl. :647-653 toggle actions and :667-668 addAction lines), and every later reference to dock_tools/dock_inspector (grep first)
    manga_ai_studio/gui/tools_panel.py lines 149-171 (the A11 vertical-only scroll-wrap pattern to copy verbatim)
    manga_ai_studio/gui/inspector_panel.py (whole file — becomes the Typesetting body unchanged; docstring line ~4 carries the rename reach)
    .planning/phases/09-ui-rework/09-RESEARCH.md Pattern 1 (CollapsibleSection skeleton) + Pattern 4 (rename reach + toggle semantics recommendation)
    .planning/phases/09-ui-rework/09-UI-SPEC.md §38 (panel contract: titles, copy, chevron, scroll rule, signal survival)
    tests/test_gui_canvas.py around line 561 (dock_tools isinstance assertion to update)
  </read_first>
  <action>
Write the failing panel test file FIRST (RED), then implement.

1. Create `manga_ai_studio/gui/side_panel.py`: `CollapsibleSection` — QVBoxLayout with zero margins/spacing; checkable QToolButton header showing the title plus a ▸/▾ arrow indicator (text prefix or QSS property), objectName reused from the established muted-header token style (`_detection_section_header` visual language: 12px Semibold muted #9a9aa2), min-height 28px; `header.toggled` → `body.setVisible(checked)` — plain setVisible, NO animation; 1px #3a3a42 bottom divider per section; expose `set_expanded(bool)` that setChecked(s) with signals delivered normally. `SidePanel` — top header row containing the chevron QToolButton ("Toggle panel" tooltip/accessibility name; ▸ collapsed / ▾ expanded), then ONE QScrollArea wrapping a body VBox (copy tools_panel.py:149-171 verbatim: setWidgetResizable(True), horizontal scrollbar ScrollBarAlwaysOff, vertical AsNeeded, FrameShape NoFrame); `add_section()` appends CollapsibleSections in call order; chevron clicked → `set_body_visible(not is_body_visible())` — the DOCK STAYS DOCKED, only the scroll body hides.
2. Rework `_build_docks`: remove dock_tools + dock_inspector construction and the tabifyDockWidget line (:311-334). Add `self.dock_panel = QDockWidget("Panel", self)` (objectName "dock_panel", right area) hosting a new `SidePanel`. Add the Typesetting section FIRST in this task: `CollapsibleSection(title="Typesetting", body=self.inspector_panel)` — the existing InspectorPanel instance, unmodified except its docstring. Keep constructing `self.tools_panel` in this task (its Brush/Detection bodies move INTO the panel in Task 2; until then it is simply not docked — acceptable intra-plan transient).
3. Grep main_window.py for remaining references to the removed dock attributes and fix each (e.g. selection-follower raising the inspector dock → raise/focus the Typesetting section instead: expand it and scroll to it; do not leave dead attribute reads).
4. `_build_view_menu`: delete action_toggle_tools and action_toggle_inspector (:647-653) and their addAction lines (:667-668); add `self.action_toggle_panel = QAction("Toggle Panel", self)` whose triggered drives `side_panel.set_body_visible(...)`. Keep "Toggle Sidebar" untouched.
5. Rename reach (UI-04): dock title "Panel"; section title "Typesetting"; update inspector_panel.py docstring occurrences opportunistically; grep gui/ for user-visible occurrences of the old chrome name and update any status messages/tooltips found.
6. Tests: new tests/test_gui_side_panel.py — (a) window.dock_panel exists titled "Panel" and hosts a SidePanel; (b) the Typesetting section's body IS window.inspector_panel (identity); (c) independent collapse: toggling one section's header flips only that body's isVisible; (d) chevron toggle hides/shows the whole scroll body while `window.dock_panel.isVisible()` stays governed by docking (not hidden by the toggle path); (e) View menu contains "Toggle Panel" and drives the same body visibility. Update test_gui_canvas.py:561 region to assert the new dock/panel shape.

NOTE for acceptance phrasing discipline: assertions about the rename are POSITIVE ("section title equals 'Typesetting'", "View menu holds 'Toggle Panel'") — no negative-grep on literals used inside task actions.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_side_panel.py tests/test_gui_canvas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - window.dock_panel is a QDockWidget with windowTitle "Panel" whose widget() is a SidePanel containing a Typesetting section whose body identity-equals window.inspector_panel
    - Toggling the Detection-settings... (post-Task-2; here: the Typesetting) section header changes only that section body's visibility
    - Clicking the chevron flips body_scroll visibility and leaves the dock itself visible/docked
    - The View menu contains an action labelled "Toggle Panel" whose trigger flips body visibility; no View action targets a removed dock attribute (suite would AttributeError otherwise)
    - Both quick-command test files exit green
  </acceptance_criteria>
  <done>Single "Panel" dock live with independently collapsible Typesetting section, working chevron + Toggle Panel action, rename applied, suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate Detection-settings + Brush bodies into the panel; slim tools_panel.py</name>
  <files>manga_ai_studio/gui/tools_panel.py, manga_ai_studio/gui/side_panel.py, manga_ai_studio/gui/main_window.py, tests/test_gui_detection_settings.py, tests/test_gui_canvas.py</files>
  <read_first>
    manga_ai_studio/gui/tools_panel.py lines 129-138 (signals), 173-254 (tool row — DELETE after confirming 09-01 relocated it), 256-283 (brush rows + blockSignals mirror), 386-559 (_build_detection_settings_section body), 561-644 (dilation mirror + set_masker_values bulk guard)
    tests/test_gui_detection_settings.py (helper :275+, emissions :299/:316/:332-333, menu-texts helper)
    tests/test_gui_canvas.py tool-group/brush unit tests targeting the old panel tool row (:350-414)
    .planning/phases/09-ui-rework/09-PATTERNS.md §"tools_panel.py slimmed" + §Shared Patterns (blockSignals mirror pairs, dark QSS tokens)
    .planning/phases/09-ui-rework/09-01-SUMMARY.md (what already moved to ToolsStrip — do not duplicate)
  </read_first>
  <action>
1. Refactor tools_panel.py into two separately embeddable body widgets (names at executor's discretion, e.g. `BrushBody` and `DetectionSettingsBody`): the Brush body carries the label + slider/spinbox rows with the blockSignals mirror pairs moved VERBATIM and the `brush_size_changed` signal; the Detection-settings body carries the full `_build_detection_settings_section` content (Detect Boxes checkbox, dilation slider↔spin mirror, std-dev spin, max-inpaint spin, seven fit params, `set_masker_values` bulk blockSignals guard) with signals `detect_boxes_changed`, `dilation_changed`, `std_dev_threshold_changed`, `masker_params_changed`. Remove the tool row + group + `_make_tool_action`/`_on_action_toggled`/`set_active_tool`/`active_tool` machinery (it lives in ToolsStrip since 09-01) and the now-dead tool-row QSS.
2. Preserve signal NAMES and emission semantics EXACTLY — relocation must not rename or re-route them. In MainWindow, keep `self.tools_panel` bound to whichever object carries the masker/detection signals today so existing emitters (`tests emit tools_panel.dilation_changed`) keep working OR update those access paths in the same commit (RED-GREEN discipline allows the test-side update; never leave a silently renamed signal).
3. In `_build_docks`/panel assembly, add the remaining three sections in D-02 order BEFORE Edit: CollapsibleSection("Detection settings", <detection body>), CollapsibleSection("Brush", <brush body>), keeping Typesetting second-to-last and reserving the last slot for Edit (plan 09-03 adds that section; this task may pass a placeholder-free ordering by simply appending Edit last when it arrives — the SidePanel must NOT hard-code a count of four).
4. Section enablement: carry over whatever gating _refresh_action_states applied to these controls (page-open / op-running) so behavior is byte-identical post-move.
5. Update tests: test_gui_detection_settings.py access paths if the emitting object changed; test_gui_canvas.py brush/tool-row unit tests re-target the strip (exclusivity/emission) or drop assertions now duplicated by tests/test_gui_tools_strip.py — no orphaned references to the removed panel tool row may remain.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_side_panel.py tests/test_gui_detection_settings.py tests/test_gui_canvas.py tests/test_gui_tools_strip.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The panel shows sections in order: Detection settings, Brush, Typesetting (Edit appended later by 09-03); each collapses independently
    - Emitting dilation_changed / detect_boxes_changed / std_dev_threshold_changed / masker_params_changed / brush_size_changed from the relocated bodies reaches MainWindow handlers identically (existing handler-driven tests pass)
    - set_masker_values programmatic population still suppresses intermediate signals (bulk blockSignals guard intact)
    - tools_panel.py no longer contains tool-row/group/set_active_tool machinery; the strip owns exclusivity (test_gui_tools_strip.py green)
    - Quick command above exits green
  </acceptance_criteria>
  <done>All four workflow sections but Edit live in the unified panel with identical signal contracts; tools_panel is slimmed; suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse persistence via QSettings + rename sweep verification</name>
  <files>manga_ai_studio/gui/side_panel.py, manga_ai_studio/gui/main_window.py, tests/test_gui_side_panel.py</files>
  <read_first>
    manga_ai_studio/gui/main_window.py lines 2004-2007 (_settings accessor), 2997-3006 (_read_detect_boxes_mode tolerant parse), 2986-2992 (seed-with-blockSignals precedent)
    manga_ai_studio/gui/side_panel.py (from Tasks 1-2)
    .planning/phases/09-ui-rework/09-UI-SPEC.md §38 (persistence keys, defaults, session-transient chevron)
    .planning/phases/09-ui-rework/09-PATTERNS.md §QSettings view-state persistence
  </read_first>
  <action>
1. Give CollapsibleSection an optional `settings_key: str | None` param. On every header toggle, if a key is set, write the checked state to QSettings via the MainWindow `_settings()` accessor pattern (org/app constants imported or passed in — keep the accessor single-sourced).
2. Keys per D-02/UI-SPEC: sidePanel/detectionExpanded, sidePanel/brushExpanded, sidePanel/typesettingExpanded, sidePanel/editExpanded (write the editExpanded key lazily when the section exists; the mechanism is generic). Restore after build using the tolerant parse copied from _read_detect_boxes_mode (bool passthrough; else lowercase membership in true/1/yes/on); missing key ⇒ expanded (all four EXPANDED on first run). Seed restored states with the build-time signals handled so no spurious writes occur during restore (blockSignals seed precedent).
3. The chevron/panel-body toggle writes NOTHING to QSettings — session-transient per UI-SPEC §38.
4. Tests (extend tests/test_gui_side_panel.py): (a) default first-run — fresh QSettings ⇒ all sections expanded; (b) round-trip — collapse Brush, rebuild/re-read through the same parse path ⇒ Brush restored collapsed, others expanded; (c) malformed stored value (e.g. "banana") ⇒ expanded (tolerant parse fallback); (d) toggling the chevron leaves the sidePanel/*Expanded keys untouched.
5. Rename sweep verification: extend the suite with a POSITIVE assertion set — dock title equals "Panel"; Typesetting section title equals "Typesetting"; View menu holds "Toggle Panel". Then manually grep the gui package for any remaining user-visible string using the retired chrome name and fix findings (docstrings opportunistic).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_side_panel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Fresh-settings launch yields all four sections expanded; collapsing a section and re-running the restore path yields exactly that section collapsed
    - Stored value outside the accepted truthy set falls back to expanded
    - Chevron interactions produce zero writes to any sidePanel/* key (asserted by comparing QSettings values before/after)
    - Positive rename assertions (dock title, section title, View-menu label) pass
    - Quick command above exits green
  </acceptance_criteria>
  <done>Collapse state survives restarts per D-02, chevron stays session-transient, rename sweep verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QSettings store→GUI | Persisted collapse-state strings are read back and parsed at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09b-01 | Tampering | sidePanel/*Expanded QSettings values | low | mitigate | Tolerant bool parse accepts only true/1/yes/on (case-insensitive); anything else falls back to expanded — malformed/crafted values cannot inject behavior beyond a section state |
| T-09b-02 | Tampering | Section headers / chevron tooltips | low | accept | Plain-text labels with fixed contracted copy; no rich-text format introduced |
</threat_model>

<verification>
- Per task: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_side_panel.py -x -q` plus the touched files' suites
- Wave close: full suite `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q`
- Manual UAT deferral: collapse/expand feel + restart persistence feel → end-of-phase human gate (human_verify_mode=end-of-phase)
</verification>

<success_criteria>
- One "Panel" dock with four ordered, independently collapsible sections (Edit shell pending 09-03) — UI-01/D-01/D-02
- Chevron toggle atop the panel + single "Toggle Panel" View action, dock stays docked, session-transient — UI-02
- User-visible chrome says Typesetting/Panel everywhere; class and signal names unchanged — UI-04
- Collapse persistence via sidePanel/*Expanded keys with tolerant parse and expanded defaults — D-02
- Zero signal renames; detection/brush/masker handlers and their tests behave identically
</success_criteria>

<output>
Create `.planning/phases/09-ui-rework/09-02-SUMMARY.md` when done
</output>
