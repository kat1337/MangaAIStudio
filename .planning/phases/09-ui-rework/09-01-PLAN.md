---
phase: 09-ui-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/tools_strip.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/assets/icons/move.svg
  - manga_ai_studio/gui/assets/icons/brush.svg
  - manga_ai_studio/gui/assets/icons/rectangle.svg
  - manga_ai_studio/gui/assets/icons/lasso.svg
  - manga_ai_studio/gui/assets/icons/eraser.svg
  - manga_ai_studio/gui/assets/icons/crop.svg
  - manga_ai_studio/gui/assets/icons/detect-text.svg
  - manga_ai_studio/gui/assets/icons/inpaint.svg
  - pyproject.toml
  - tests/test_gui_tools_strip.py
  - tests/test_gui_canvas.py
autonomous: true
requirements: [UI-03]
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The tools strip is a vertical icon-only column embedded in the central widget, positioned BETWEEN the Pages dock and the canvas — window x-order left→right is Pages | strip | canvas | side panel (D-05; probe-verified central-container embedding, NOT addToolBar on a left toolbar area)."
    - "The strip holds exactly 8 buttons top→bottom per D-04: Move/Pan (V), Brush (B), Rectangle (R), Lasso (L), Eraser (E), Crop (G), then a visual divider, then Detect Text (D) and Inpaint (C)."
    - "Strip tool buttons are exclusive — checking one unchecks the others — and exactly ONE tool_changed emission fires per selection from every entry path (strip click / keyboard V-B-R-L-E-G / programmatic set_active_tool), preserving the 06 D-10 / WR-02 contract."
    - "set_active_tool drives the strip's checked state across all entry paths via its explicit blockSignals sync loop; the window action_tool_* actions stay standalone checkable and are driven explicitly (never merged into the strip's group)."
    - "Detect Text and Inpaint strip buttons are plain non-checkable default-action buttons whose enabled/running state mirrors the window actions' gating from _refresh_action_states (disabled with no page open, disabled while an op runs)."
    - "All 8 strip buttons are icon-only with tooltips carrying name + shortcut (e.g. 'Move/Pan tool (V)', 'Crop tool (G): drag a rectangle on the page, Enter applies, Esc cancels.'); icons load module-relative and render as non-null QIcon under the offscreen test platform."
    - "The shrunken top toolbar contains exactly: Open Folder | Fit · 100% · Out · In | Undo · Redo | Mask Overlay toggle | Preview (hold) — the 6 tool buttons, Detect Text, and Inpaint have LEFT the toolbar (D-07); Preview (hold) keeps its inherited enablement (disabled until an inpaint result exists)."
    - "Icon load failure containment: icons resolve ONLY via Path(__file__).parent / 'assets' / 'icons' inside the gui package — tests assert non-null QIcon plus button wiring, never pixels (RESEARCH A5)."
  backstop:
    - "{ statement: The checked strip tool button renders the 1px #00d4ff accent border on its :checked state at 100% zoom, and unchecked buttons sit flat on #2d2d33 with a 1px #3a3a42 border (visual check on the running app, not pixel-unit-testable)., verification: backstop }"
  artifacts:
    - "manga_ai_studio/gui/tools_strip.py with a ToolsStrip widget exposing tool_changed (Signal(object)), set_active_tool(ToolMode), active_tool(), and tool_group (exclusive QActionGroup holding EXACTLY the strip's own 6 tool actions)"
    - "8 SVG assets at manga_ai_studio/gui/assets/icons/{move,brush,rectangle,lasso,eraser,crop,detect-text,inpaint}.svg — monochrome #e8e8ea strokes, stroke-width 2, round caps/joins, fill none, transparent background, 24×24 viewBox"
    - "pyproject.toml [tool.setuptools.package-data] entry covering gui/assets/icons/*.svg"
    - "tests/test_gui_tools_strip.py covering order/membership/divider/exclusivity/single-emission/geometry/icon-non-null"
    - "Updated tests/test_gui_canvas.py toolbar-button mirror assertions reflecting the slimmed toolbar"
  key_links:
    - "ToolsStrip buttons ↔ strip-owned QActions via setDefaultAction + setCheckable(True) (the _make_tool_toolbar_button pattern) — checked-state sync follows set_active_tool's loop"
    - "MainWindow.set_active_tool ↔ ToolsStrip.set_active_tool — replaces the old tools_panel.set_active_tool call so keyboard/menu/programmatic paths drive the strip"
    - "_refresh_action_states ↔ strip Detect/Inpaint default-action buttons — enablement gating inherited free"
    - "_icon loader ↔ module-relative assets dir — never CWD-relative (Pitfall 6)"
  prohibitions:
    - "NEVER add the strip via addToolBar(Qt.LeftToolBarArea) — QMainWindow lays toolbar columns OUTSIDE dock columns and setCorner() cannot fix it; the strip would land left of Pages. Probe-verified. [flagged-unverified]"
    - "NEVER put the standalone window action_tool_* actions into the strip's exclusive QActionGroup — a mirrored multi-widget group fights itself (the Phase 6 WR-02 bug shape); the group holds exactly the strip's own actions and set_active_tool drives window actions explicitly. [flagged-unverified]"
    - "NEVER connect both triggered and toggled for tool-selection emission — keep exactly one emission path (toggled firing only on checked=True). [flagged-unverified]"
    - "NEVER resolve icon paths relative to CWD or any user-controlled input — module-relative Path(__file__).parent resolution only. [flagged-unverified]"
  assumptions:
    - "{ statement: UI-03 concurrency probe ('If interrupted or run in parallel, what is guaranteed?') is unresolved — flagged-unverified. Planner assumption: the Qt GUI event loop serializes all button/state mutations on the main thread; no parallel-entry guarantee exists or is required beyond that serialization. No lock/check machinery is added.", verification: flagged-unverified }
---

<objective>
Relocate the 6 paint/crop tools + Detect Text + Inpaint off the top toolbar into a small vertical icon-only strip between the Pages file list and the canvas (D-04/D-05/D-06), shrink the top toolbar to its final D-07 contents, and introduce the repo's first bundled icon assets — without regressing the WR-02 single-emission active-tool sync contract.

Purpose: UI-03 (as corrected by D-05) — the user's chosen final layout puts the tools within mouse reach of the canvas, not stacked in the top chrome.
Output: `tools_strip.py`, 8 SVG icons + loader + package-data, slimmed `_build_toolbar`, new `tests/test_gui_tools_strip.py`, updated `test_gui_canvas.py`.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-rework/09-CONTEXT.md
@.planning/phases/09-ui-rework/09-RESEARCH.md
@.planning/phases/09-ui-rework/09-PATTERNS.md
@.planning/phases/09-ui-rework/09-UI-SPEC.md

# Build surfaces (read the ranges, don't guess)
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/tools_panel.py
@tests/test_gui_canvas.py

# Prior-phase contracts this plan must not regress
@.planning/phases/06-refinement-polish-deferred-fixes-full-curve-editor/06-CONTEXT.md
</context>

## Artifacts this phase produces

Symbols/files created by THIS plan (exclude from drift verification — they are new):

- **New file:** `manga_ai_studio/gui/tools_strip.py` — class `ToolsStrip` (widget; QToolBar-based vertical strip recommended per RESEARCH skeleton), members `tool_changed` (Signal(object)), `tool_group` (QActionGroup), `action_move`, `action_brush`, `action_rectangle`, `action_lasso`, `action_eraser`, `action_crop` (strip-owned QActions relocated verbatim from ToolsPanel's tool row), `set_active_tool(tool: ToolMode)`, `active_tool() -> ToolMode`
- **New files:** `manga_ai_studio/gui/assets/icons/{move,brush,rectangle,lasso,eraser,crop,detect-text,inpaint}.svg` (8 hand-authored SVGs)
- **New function:** `_icon(name: str) -> QIcon` module-level helper (module-relative resolution)
- **New MainWindow wiring:** central-widget container (`QWidget` + QHBoxLayout `[strip][canvas]`) replacing `setCentralWidget(self.canvas)`; attribute `self.tools_strip`
- **Modified:** `MainWindow._build_toolbar` (D-07 slim-down); `MainWindow._refresh_action_states` (strip action gating carried over)
- **New test file:** `tests/test_gui_tools_strip.py`
- **No new QActions in menus, no new dialogs, no new settings keys** (UI-02/D-02 persistence belongs to plan 09-02)

<tasks>

<task type="tracer">
  <name>Task 1: Vertical tools strip wired end-to-end — central container swap + 8 default-action buttons + divider</name>
  <files>manga_ai_studio/gui/tools_strip.py, manga_ai_studio/gui/main_window.py, tests/test_gui_tools_strip.py</files>
  <read_first>
    manga_ai_studio/gui/main_window.py lines 125-149 (setCentralWidget seam), 299-343 (_build_docks), 794-893 (action_detect_text / action_inpaint / action_tool_* creation incl. tooltips), 4240-4309 (_make_tool_toolbar_button + set_active_tool blockSignals sync loop)
    manga_ai_studio/gui/tools_panel.py lines 129-138 (signal declarations), 173-254 (tool row + QActionGroup + toggled wiring + set_active_tool — RELOCATE this machinery verbatim), 314-327 (_on_action_toggled checked-only emission)
    tests/test_gui_canvas.py lines 350-414 (toolbar mirror + exclusivity test templates) and around line 561
    .planning/phases/09-ui-rework/09-RESEARCH.md Pattern 2 + Code Examples (probe-verified embedding skeleton)
    .planning/phases/09-ui-rework/09-PATTERNS.md sections "tools_strip.py" and "WR-02 sync loop"
    .planning/phases/09-ui-rework/09-UI-SPEC.md §39 (strip construction + tooltip copy)
  </read_first>
  <action>
Write the failing strip test file FIRST (RED), then implement.

1. Create `manga_ai_studio/gui/tools_strip.py`: a vertical `ToolsStrip` widget (QToolBar subclass with `setOrientation(Qt.Vertical)`, `setFixedWidth(44)`, `setIconSize(QSize(20, 20))` — or an equivalent plain-QWidget column; the LOOK/behavior is the contract, QToolBar is recommended). Relocate ToolsPanel's tool-row machinery verbatim (tools_panel.py:173-254): six strip-owned checkable QActions carrying `setData(ToolMode)` and the same tooltips as the corresponding window actions ("Move/Pan tool (V)", "Brush tool (B)", "Rectangle tool (R)", "Lasso tool (L)", "Eraser tool (E)", and the crop tooltip "Crop tool (G): drag a rectangle on the page, Enter applies, Esc cancels."), one exclusive QActionGroup holding EXACTLY these six actions, per-action `toggled` connected to a checked-only handler emitting `tool_changed` once per selection. Expose `set_active_tool(tool)` / `active_tool()` mirroring the relocated ToolsPanel API (blockSignals discipline preserved).
2. Buttons: for each of the six strip actions create a square (36×36) checkable QToolButton with `setDefaultAction(strip_action)`; then `addSeparator()` (the D-04 divider); then two plain NON-checkable QToolButtons with `setDefaultAction(self.action_detect_text)` and `setDefaultAction(self.action_inpaint)` — the WINDOW actions, so enablement/running-state gating follows _refresh_action_states automatically.
3. In `main_window.py`: replace `self.setCentralWidget(self.canvas)` (line 135) with the probe-verified container: `central = QWidget()` → QHBoxLayout with zero margins/spacing → `[self.tools_strip, self.canvas (stretch 1)]` → `setCentralWidget(central)`. Construct `self.tools_strip` BEFORE this call. NEVER pass the strip through `addToolBar`.
4. In `MainWindow.set_active_tool`: replace the `self.tools_panel.set_active_tool(tool)` call with `self.tools_strip.set_active_tool(tool)` (keep the window-action blockSignals loop untouched).
5. In `_refresh_action_states`: gate the six strip actions' enabled state identically to how the panel's tool actions are gated today (carry the mechanism over — read how tools_panel actions track page-open/op-running before writing).
6. Tests (`tests/test_gui_tools_strip.py`): (a) geometry order — with window shown, strip's window-x sits strictly between dock_pages right edge and canvas left edge; (b) membership — 8 buttons, 6 in `tools_strip.tool_group.actions()`, group `isExclusive()`; (c) divider present between crop and detect buttons; (d) single emission — `qtbot.waitSignal(window.tools_strip.tool_changed)` yields exactly 1 arg (ToolMode.BRUSH) when a strip action is checked, and NO second emission when `window.set_active_tool(ToolMode.MOVE)` runs programmatically; (e) Detect/Inpaint strip buttons' `defaultAction()` IS `window.action_detect_text` / `window.action_inpaint` and they start disabled with no page open.
Update any test_gui_canvas.py assertions that referenced the old top-toolbar tool-button mirrors so the suite is green at task end (assert action-list membership / default-action identity, never widget parents — Phase-4 lesson).

Per D-05 (user decision, supersedes UI-03's literal right-side wording): the strip goes LEFT of the canvas. Do not "fix" the placement back to the right.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_tools_strip.py tests/test_gui_canvas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_gui_tools_strip.py passes: geometry order assertion proves Pages | strip | canvas ordering by window x-coordinates
    - tools_strip.tool_group.actions() has length 6 and isExclusive() is True; strip button count including Detect/Inpaint is 8; at least one separator exists before the Detect button
    - Checking Brush emits tool_changed exactly once with ToolMode.BRUSH; programmatic set_active_tool emits zero additional signals
    - The Detect/Inpaint strip buttons' defaultAction() identity-equals the window actions, both disabled on a fresh window
    - Full quick command above exits green
  </acceptance_criteria>
  <done>The strip renders between Pages and canvas with all 8 buttons live, exclusivity + single-emission contract holds from every entry path, and the touched GUI suites are green.</done>
</task>

<task type="auto">
  <name>Task 2: Icon assets — 8 SVGs, module-relative loader, package-data</name>
  <files>manga_ai_studio/gui/assets/icons/move.svg, manga_ai_studio/gui/assets/icons/brush.svg, manga_ai_studio/gui/assets/icons/rectangle.svg, manga_ai_studio/gui/assets/icons/lasso.svg, manga_ai_studio/gui/assets/icons/eraser.svg, manga_ai_studio/gui/assets/icons/crop.svg, manga_ai_studio/gui/assets/icons/detect-text.svg, manga_ai_studio/gui/assets/icons/inpaint.svg, manga_ai_studio/gui/tools_strip.py, pyproject.toml, tests/test_gui_tools_strip.py</files>
  <read_first>
    .planning/phases/09-ui-rework/09-UI-SPEC.md §Icon Asset Contract + §Color (icon glyph color declaration)
    .planning/phases/09-ui-rework/09-RESEARCH.md Code Examples §Icon loading + Assumption A3/A5
    manga_ai_studio/gui/tools_strip.py (from Task 1)
    pyproject.toml (~line 52, existing [tool.setuptools.package-data] section)
  </read_first>
  <action>
1. Hand-author 8 SVG files named move.svg, brush.svg, rectangle.svg, lasso.svg, eraser.svg, crop.svg, detect-text.svg, inpaint.svg under `manga_ai_studio/gui/assets/icons/`. Art direction per UI-SPEC: 24×24 viewBox, transparent background, fill none, stroke #e8e8ea, stroke-width 2 (viewBox units), round caps/joins, simple recognizable glyphs legible at 20px (e.g. move = four-direction arrows; brush = diagonal brush; rectangle/lasso/eraser/crop = standard outline shapes; detect-text = magnifier over text lines; inpaint = droplet/band over text). Original work — no fetched assets.
2. Add a module-relative loader in tools_strip.py: `_ICONS = Path(__file__).parent / "assets" / "icons"` and `_icon(name) -> QIcon` returning `QIcon(str(_ICONS / f"{name}.svg"))`. Never CWD-relative, never user-controlled paths. Apply `btn.setIcon(_icon(...))` to all 8 buttons; buttons stay text-free (icon-only).
3. pyproject.toml: extend the existing `[tool.setuptools.package-data]` section with a pattern covering `gui/assets/icons/*.svg` (RESEARCH Runtime State Inventory — prevents a silent asset drop in future packaging).
4. Extend tests/test_gui_tools_strip.py: for each of the 8 buttons assert `not btn.icon().isNull()` (offscreen-safe per RESEARCH A5 — no pixel assertions) and that the icon source path resolves under the gui package directory.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_tools_strip.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 8 SVG files exist under manga_ai_studio/gui/assets/icons/ with viewBox 0 0 24 24 and stroke color #e8e8ea
    - Every strip button reports a non-null QIcon in the offscreen test run
    - The icon directory constant derives from the module file location (Path(__file__).parent), verified by the test asserting paths under the gui package
    - pyproject.toml package-data includes the svg glob
    - Quick command above exits green
  </acceptance_criteria>
  <done>All 8 strip buttons are icon-only with bundled module-relative SVGs, packaged via package-data, tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the top toolbar to D-07 contents</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_canvas.py</files>
  <read_first>
    manga_ai_studio/gui/main_window.py lines 981-1035 (_build_toolbar — remove :1002-1014 additions)
    tests/test_gui_canvas.py toolbar-related assertions (search for toolbar button-mirror tests referencing the tool buttons / Detect / Inpaint)
    .planning/phases/09-ui-rework/09-UI-SPEC.md §41 (final toolbar contents, exact order)
  </read_first>
  <action>
1. In `_build_toolbar`: delete the `addAction(self.action_detect_text)` / `addAction(self.action_inpaint)` lines and the six `_make_tool_toolbar_button(...)` calls (:1002-1014 region). Keep separators tidy so the remaining groups read: Open Folder | Fit · 100% · Out · In | Undo · Redo | Mask Overlay | Preview (hold). Keep text-label style (ToolButtonTextOnly behavior unchanged) — icons on the top toolbar were explicitly DECLINED (deferred idea).
2. Do NOT delete or alter the QAction objects themselves (action_detect_text, action_inpaint, action_tool_*) — they remain the state/gating holders the strip mirrors. Do NOT touch any setShortcut call (keyboard-reachability contract; audit warning sign = any removed setShortcut).
3. Update tests/test_gui_canvas.py toolbar assertions: toolbar mirror tests now target the strip (or assert the top toolbar's remaining action membership via the toolbar actions list). Assert action-list membership, never widget parents.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_canvas.py tests/test_gui_tools_strip.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Top toolbar action/widget list equals exactly: action_open_folder, fit_to_window, actual_size, zoom_out, zoom_in, undo, redo, mask overlay toggle, preview-hold button (with internal separators) — Detect/Inpaint/tool entries absent
    - action_detect_text / action_inpaint / all six action_tool_* objects still exist on the window with their shortcuts intact (existing shortcut tests still pass unchanged)
    - Quick command above exits green
  </acceptance_criteria>
  <done>Top toolbar shows only the D-07 set; every removed entry remains reachable (strip button or unchanged shortcut).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→QIcon | Bundled SVG files are read from disk and rendered by the Qt SVG plugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09a-01 | Tampering | `_icon()` loader | low | mitigate | Icons resolve ONLY from `Path(__file__).parent / "assets" / "icons"` (repo-bundled, module-relative); no CWD-relative or user-controlled path ever reaches QIcon; test asserts non-null QIcon + path containment |
| T-09a-02 | Tampering | Strip tooltips | low | accept | Tooltips are plain-text copies of existing static action texts; no rich-text format is introduced |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_tools_strip.py tests/test_gui_canvas.py -x -q` green after each task
- Full suite at wave close: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (baseline 552 passed, strict superset expected)
</verification>

<success_criteria>
- Strip geometry/order/divider/membership/exclusivity/single-emission all asserted and green (UI-03 per D-05, D-04)
- 8 bundled module-relative SVG icons load non-null; package-data covers them (D-06)
- Top toolbar reduced to the D-07 set with zero shortcut loss (D-07)
- WR-02 contract intact: one tool_changed emission per selection, window actions driven explicitly by set_active_tool
</success_criteria>

<output>
Create `.planning/phases/09-ui-rework/09-01-SUMMARY.md` when done
</output>
