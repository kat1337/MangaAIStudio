---
phase: quick-260822-vk7-fix-translation-field-bug-resizing-a-box
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/inspector_panel.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_boxes.py
autonomous: true
requirements: [QUICK-VK7-FIX]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Typing in the Inspector translation field immediately after resizing a box never loses keystrokes or resets the field (no reload lands mid-edit).
    - The stationary-grace auto detect-fit + OCR does not run while the user has an active edit session in an Inspector text field or the canvas inline editor; it re-arms instead.
    - When the user finishes editing (focus-out), the pending grace work still runs exactly once and normal behavior resumes.
    - Type-then-resize order continues to work as today (no regression to existing grace/D-04 contracts).
  artifacts:
    - manga_ai_studio/gui/inspector_panel.py with is_text_edit_active()
    - manga_ai_studio/gui/main_window.py with guarded _on_canvas_selection_changed and deferring _on_stationary_grace_timeout
    - New regression guards in tests/test_gui_boxes.py
  key_links:
    - _on_stationary_grace_timeout -> is_text_edit_active() -> timer re-arm (freeze removed from typing path)
    - _on_canvas_selection_changed -> is_text_edit_active() -> skip load_box (clobber removed from OCR-finish path)
---

<objective>
Fix the "resize box then type translation freezes briefly and resets the translation status" bug by (a) deferring the stationary-grace auto re-detect while the user has an active text-editing session, and (b) suppressing Inspector reloads that would clobber an in-progress field edit.

Purpose: The 5 s stationary-grace timer (quick-260822-gnq) fires mid-typing after a resize: the synchronous full-page refit freezes the GUI thread, then the OCR-finished handler emits boxes_modified -> _on_canvas_selection_changed -> InspectorPanel.load_box which setPlainText-clobbers the user's uncommitted translation text.

Output: Guarded MainWindow slots + one InspectorPanel probe method + regression tests; full suite green under the pinned interpreter.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@manga_ai_studio/gui/main_window.py (lines 122-167 STATIONARY_GRACE_MS + timer wiring; 3548-3610 _on_boxes_modified; 3690-3787 _mark_geometry_changed/_on_stationary_grace_timeout/_redetect_single_box; 3790-3814 _on_canvas_selection_changed; 6093-6140 _dispatch_ocr_for_box; 6304-6341 _on_ocr_finished)
@manga_ai_studio/gui/inspector_panel.py (lines 258-284 _CommitTextEdit; 590-686 load_box blockSignals population)
@manga_ai_studio/gui/canvas.py (line 291 self._inline_editor; InlineEditor.is_active())
@tests/test_gui_boxes.py (lines 4910-4971 stationary-grace guards — new tests go alongside)
@.planning/quick/260822-gnq-fix-box-move-auto-re-detect-annoyance-br/260822-gnq-SUMMARY.md

Key facts established during investigation:
- Translation commits are DEFERRED: _CommitTextEdit commits on focus-out / Ctrl+Return only, so mid-typing the pagebox payload still holds the OLD translation; any load_box reload visibly wipes in-progress keystrokes.
- _on_stationary_grace_timeout -> _redetect_single_box -> _refit_changed_boxes runs derive_page_mask_state over ALL boxes synchronously on the GUI thread (the freeze). Its D-04 gate only covers hand-edited recognized text, NOT translations, so OCR still dispatches.
- _on_ocr_finished emits canvas.boxes_modified (main_window.py:6339); _on_boxes_modified calls _on_canvas_selection_changed() which calls load_box -> the clobber.
- Type-first works because focus-out already committed the translation to the payload before the resize arms the timer.
- Existing pattern to mirror: canvas.box_interaction_started -> self._stationary_timer.stop (drag cancels grace); module constant read at start() time for monkeypatched tests.
- Pinned interpreter (AGENTS.md): C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InspectorPanel.is_text_edit_active() + selection-follower reload guard</name>
  <files>manga_ai_studio/gui/inspector_panel.py, manga_ai_studio/gui/main_window.py, tests/test_gui_boxes.py</files>
  <behavior>
    - Test 1: is_text_edit_active() returns False with no field focused (fresh panel, load_box populated).
    - Test 2: after translation_edit.setFocus(), is_text_edit_active() returns True; same for recognized_edit.
    - Test 3: with translation_edit focused, calling window._on_canvas_selection_changed() leaves the field's typed text untouched (load_box skipped); after clearing focus and re-running it, the field shows the pagebox value again (guard releases).
    - Test 4 (end-to-end shape): drive _on_ocr_finished-equivalent via emitting canvas.boxes_modified while translation_edit holds focus + typed text — typed text survives.
  </behavior>
  <action>
    In InspectorPanel add a public probe method `is_text_edit_active(self) -> bool`: True iff `self.translation_edit.hasFocus() or self.recognized_edit.hasFocus()` (docstring: "an in-progress commit-deferred edit session lives in one of the two _CommitTextEdit fields; external reloads must not land while this is True").
    In MainWindow._on_canvas_selection_changed (line ~3790), FIRST check `self.inspector_panel.is_text_edit_active()` -> early return with a debug log ("Inspector reload skipped: text edit session active"). This suppresses every reload caller (selectionChanged follower, _on_boxes_modified WR-05 sync, style-commit syncs) while the user types — per the root cause: _on_ocr_finished -> boxes_modified -> load_box clobbers the commit-deferred translation field. Do NOT touch the model-write side (_on_ocr_finished still writes recognized text + edited=False to the pagebox; only the panel refresh is suppressed — the panel resyncs on the next unfocused event).
    Do NOT add v1/simplified variants of the guard — cover both text fields in one probe (per D-04 symmetry: recognized field is equally exposed).
    Add the four tests above near the existing inspector tests (~line 2048+) using the file's existing qtbot/window fixtures.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py -q -k "text_edit_active or selection_changed or inspector"</automated>
  </verify>
  <done>is_text_edit_active exists and is consulted by _on_canvas_selection_changed; focused-field reloads are suppressed and resume after focus-out; new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stationary-grace timeout defers while an edit session is active</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_boxes.py</files>
  <behavior>
    - Test 1: with a shortened (monkeypatched) grace, stale-marked box, and translation_edit focused when the timer fires — NO refit/OCR dispatch happens and window._stationary_timer.isActive() is True again (re-armed), geometry_stale still True.
    - Test 2: same setup but canvas._inline_editor active (monkeypatch is_active->True) — timer re-arms instead of dispatching.
    - Test 3: same setup, no edit session — existing single-dispatch contract still holds (mirror test_stationary_grace_dispatches_once assertions).
    - Test 4: after the deferred case, clearing focus and letting the re-armed timer fire again results in exactly ONE dispatch (deferred, not dropped).
  </behavior>
  <action>
    In MainWindow._on_stationary_grace_timeout (line ~3725), BEFORE the `_op_running` gate, add the edit-session deferral: if `self.inspector_panel.is_text_edit_active() or self.canvas._inline_editor.is_active()` -> log debug ("stationary re-detect deferred: edit session active"), re-arm with `self._stationary_timer.start(STATIONARY_GRACE_MS)` (module constant read at start-time — the established monkeypatch seam), and return WITHOUT touching stale markers.
    Rationale (root cause): the timeout's refit leg runs derive_page_mask_state over the whole page synchronously on the GUI thread — that IS the reported freeze mid-typing; deferring removes heavy work from the typing path entirely and also prevents the subsequent OCR result from landing mid-edit (defense in depth on top of Task 1's reload guard).
    Keep the existing `_op_running` silent-skip AFTER the deferral check (order matters: deferral re-arms, op-running skips permanently until affordance click — preserve T-QG-02 semantics).
    Update the docstring: the grace now also waits out Inspector/inline edit sessions; drag-start cancel (box_interaction_started) unchanged.
    Do not describe this as a placeholder or partial fix — the deferral fully removes the freeze from the typing path.
    Add the four tests beside the existing stationary-grace guards (~line 4910+), following their monkeypatch-the-shortened-interval pattern.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_boxes.py -q -k "stationary or grace"</automated>
  </verify>
  <done>Grace timeout re-arms instead of running refit/OCR while any text-edit session is active; fires exactly once once the session ends; all pre-existing grace guards still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Reported-repro regression guard + full suite under pinned interpreter</name>
  <files>tests/test_gui_boxes.py</files>
  <action>
    Add one end-to-end regression test reproducing the EXACT report order: create/select a box, emit a committed resize (canvas.boxes_modified with a changed tuple so _mark_geometry_changed arms the timer), shorten STATIONARY_GRACE_MS, give keyboard focus to inspector_panel.translation_edit and type text into it (insertPlainText), let the grace timer fire AND simulate the OCR-finished landing (call window._on_ocr_finished({"box_id": id(item.pagebox), "text": "recognized"}) directly — no model needed). Assert: (a) the translation field still contains the typed text verbatim, (b) no exception/freeze path was hit (timer re-armed rather than dispatched), (c) after clearing focus and firing the re-armed timer + OCR finish again, the field still shows the typed translation and the pagebox payload carries BOTH the recognized text (from OCR) and the typed translation. Then run the FULL suite with the pinned interpreter and record the counts in the summary (baseline: 1031 passed).
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <done>New repro guard passes; full suite green (>= baseline count, 0 failed) under python.exe 3.14.2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async-worker-result -> GUI state | OCR/detection worker results land on the main thread and mutate UI/model state that may be mid-user-edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-vk7-01 | Tampering | _on_ocr_finished -> _on_canvas_selection_changed -> load_box | medium | mitigate | Focus-guard suppresses the reload while a commit-deferred edit session is active (Task 1); grace deferral prevents the result landing mid-typing at all (Task 2) |
| T-vk7-02 | Denial of Service | _refit_changed_boxes synchronous full-page derive on GUI thread | low | accept | Pre-existing accepted cost on explicit affordance click; removed from the automatic typing path by Task 2 deferral (threading the derive is out of scope for this quick task) |
| T-vk7-SC | Tampering | npm/pip installs | n/a | accept | No package installs in this task |
</threat_model>

<verification>
- All new tests pass; existing stationary-grace, inspector, and D-04 contract tests unchanged and green.
- Manual shape: resize a box, immediately type in the translation field — no freeze mid-typing, no status reset; after clicking away, the pending re-detect still runs once.
</verification>

<success_criteria>
- Resize-then-type no longer loses keystrokes or resets the translation field/state.
- Stationary-grace work is deferred (not dropped) during Inspector/inline edit sessions and completes exactly once afterwards.
- Full suite passes under the pinned interpreter.
</success_criteria>

<output>
Create .planning/quick/260822-vk7-fix-translation-field-bug-resizing-a-box/260822-vk7-SUMMARY.md when done
</output>
