---
phase: quick-260903-lm6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - panelcleaner/comic_text_detector/utils/textblock.py
  - manga_ai_studio/core/box_model.py
  - manga_ai_studio/core/detection_boxes.py
  - manga_ai_studio/core/project_io.py
  - panelcleaner/config.py
  - manga_ai_studio/adapters/torch_impl.py
  - manga_ai_studio/core/batch_runner.py
  - manga_ai_studio/gui/main_window.py
  - manga_ai_studio/gui/tools_panel.py
  - manga_ai_studio/gui/inspector_panel.py
autonomous: true
requirements: [quick-260903-lm6]

must_haves:
  truths:
    - The Detection settings panel has a "Min confidence" spinbox whose value persists to the profile masker and is applied as the CTD detector's conf_thresh on the NEXT Detect Text run — both interactive and batch.
    - Each detected box's Inspector (Typesetting body) shows a read-only "Confidence" row with the per-box YOLO confidence as a percentage; unknown shows an em dash.
    - User-drawn boxes show "—" confidence (never a fake value); scattered-line blocks show "—" (sentinel −1, not the upstream prob=1 default).
    - Per-box confidence and the Min confidence setting both survive project save/reload (.mas) and app restart.
  artifacts:
    - panelcleaner/comic_text_detector/utils/textblock.py (group_output sets blk.prob; scattered blocks get −1.0 sentinel)
    - manga_ai_studio/core/box_model.py (PageBox.confidence field)
    - manga_ai_studio/core/detection_boxes.py (build_detected_pageboxes sets normalized confidence)
    - manga_ai_studio/core/project_io.py ("confidence" JSON key both directions, legacy-safe)
    - panelcleaner/config.py (MaskerConfig.detection_conf_thresh + INI round-trip + clamp)
    - manga_ai_studio/core/batch_runner.py + manga_ai_studio/gui/main_window.py (configure(conf_thresh=...) at all three det-model creation sites)
    - manga_ai_studio/gui/tools_panel.py (conf_thresh_spin wired into masker_values/set_masker_values/_on_fit_param_changed)
    - manga_ai_studio/gui/inspector_panel.py (Confidence row mirroring Std dev)
  key_links:
    - DetectionSettingsBody.masker_values() key "detection_conf_thresh" == MaskerConfig field name (setattr persistence loop in MainWindow._on_masker_params_changed)
    - model.configure(conf_thresh=...) happens BEFORE model.load() at every detect site (load forwards conf_thresh into TextDetector; fresh-adapter-per-run makes it effective)
    - PageBox.confidence ← getattr(blk, "prob") range-checked to [0, 1] in build_detected_pageboxes
    - project_io "confidence" key: None-safe on save, tolerant on load (absent/None → None)
---

# Quick Task 260903-lm6 — Detection confidence: Min-confidence setting + per-box Inspector value

**User decisions (locked):** (1) "Min confidence" is a user-facing spinbox in `DetectionSettingsBody`, not just a default bump. (2) Per-box confidence is a read-only Inspector row in the Typesetting body, styled like the Std dev row. (3) Auto-pruning of false positives is OUT of scope.

**Out of scope (do not implement):** auto-pruning, `nms_thresh` / `input_size` exposure, re-filtering of already-placed boxes (the setting applies on next detect only), ONNX detection wiring (`OnnxDetectionModel` is a stub — `configure`/`load` raise `NotImplementedError`; torch is the only Phase-1 detection backend).

**Verified integration points (line numbers from working tree at plan time):**
- `manga_ai_studio/` never calls `.configure(` — the torch adapter's conf_thresh knob (:69, :154-155, applied in `load()` :105) is plumbed but dead.
- Interactive detect: `main_window.py` `detect_text()` :5281; `model = backend_factory("detection", ...)` at :5305; Worker spawned :5308; `model.load()` happens INSIDE the worker (`_run_detection_task` :5327, load ~:5371).
- Batch detect: the det model is created in `batch_runner.py` — `batch_detect` :430-431 and `batch_detect_and_clean` :514-515 — and `masker_conf` is already a parameter of both functions (threaded from `main_window.py` ~:8069).
- Vendored: `postprocess_yolo` returns `(blines, cls, confs)` (`inference.py` :112-127); `group_output` (`textblock.py` :446) discards `conf` in its zip loop and creates scattered blocks fresh at ~:474; `TextBlock.prob = 1` default at :66; `split_textblk` deepcopies (sub-blocks inherit prob) and merges go into the first block (inherits its prob) — both automatic, no code needed there.

<tasks>

<task type="auto">
  <name>Task 1: Capture per-box confidence — vendored prob deviation, PageBox field, normalization, .mas persistence</name>
  <files>panelcleaner/comic_text_detector/utils/textblock.py, manga_ai_studio/core/box_model.py, manga_ai_studio/core/detection_boxes.py, manga_ai_studio/core/project_io.py, tests/test_core/test_detection_boxes.py, tests/test_core/test_project_io.py</files>
  <action>
    1. Vendored deviation in `group_output` (panelcleaner/comic_text_detector/utils/textblock.py, ~:446-475):
       - In the `for bbox, cls, conf in zip(*blks):` loop, after constructing `blk_list.append(TextBlock(bbox, language=LANG_LIST[cls]))`, set the block's prob to the YOLO confidence: build the TextBlock into a local, assign `blk.prob = float(conf)`, then append. Mark with a brief `# Manga AI Studio deviation (quick-260903-lm6): keep per-block detector confidence (upstream discards it).` comment — match the deviation-note style used in panelcleaner/config.py / panelcleaner/masker.py.
       - For the scattered-line block created fresh at ~:474 (`blk = TextBlock([bx1, by1, bx2, by2], [line])`), set `blk.prob = -1.0` right after construction with a one-line deviation comment noting the sentinel means "unknown" (so unknown never masquerades as the upstream prob=1 default). Leave the `TextBlock.__init__` `self.prob = 1` default untouched.
       - split/merge inheritance needs NO code: `split_textblk` uses `copy.deepcopy` (sub-blocks carry the parent's prob) and merges go INTO the first block (keeps its prob).
    2. `manga_ai_studio/core/box_model.py`: add `confidence: Optional[float] = None` to the `PageBox` dataclass after `fill_color` (~:121), with a short comment: detector confidence in [0, 1]; None = unknown (user-drawn, scattered, or legacy). `copy()` uses `dataclasses.replace`, so the new plain field survives undo snapshots automatically — no copy() change.
    3. `manga_ai_studio/core/detection_boxes.py` `build_detected_pageboxes` (PageBox construction ~:98-105): before the append, normalize `raw = getattr(blk, "prob", None)` and pass `confidence=float(raw) if raw is not None and 0.0 <= float(raw) <= 1.0 else None` into the PageBox constructor. Per the locked sentinel rule: real confs (0..1) pass through, the −1.0 scattered sentinel and any missing attr normalize to None. (The upstream default prob=1 would land as 1.0 under this locked range rule — acceptable; our deviation sites guarantee YOLO and scattered blocks never carry the default. Document this in a one-line comment.)
    4. `manga_ai_studio/core/project_io.py`, additive and legacy-safe both ways:
       - `pagebox_to_json` (~:207-295): add `"confidence": None if pagebox.confidence is None else float(pagebox.confidence),` next to the other optional keys (std_dev / inpaint_override / fill_color block, before `"payload"`).
       - `json_to_pagebox` (~:298-445): tolerant read mirroring the std_dev precedent (:348-356) — `confidence = None`; if `d.get("confidence") is not None`, `float()`-coerce, raising `ProjectFormatError("confidence must be a number")` on TypeError/ValueError. Pass `confidence=confidence` into the PageBox construction. Absent key (legacy .mas) → None. Do NOT read confidence from the payload TextBlock dict — it does not persist prob.
    5. Tests:
       - tests/test_core/test_detection_boxes.py (extend): duck-typed blk with prob=0.87 → PageBox.confidence == 0.87; prob=-1 → None; blk without a prob attribute → None; prob=1 → 1.0 (documents the locked range rule); existing no-prob fakes keep passing (confidence defaults None).
       - tests/test_core/test_project_io.py (extend): roundtrip confidence=0.87 and None; a legacy dict with no "confidence" key loads to None; `"confidence": "high"` raises ProjectFormatError.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_detection_boxes.py tests/test_core/test_project_io.py tests/test_box_persistence.py -x -q   (drop the leading "& " when running from Git Bash)</automated>
  </verify>
  <done>group_output sets blk.prob for YOLO blocks and −1.0 for scattered blocks; PageBox has a confidence field populated by build_detected_pageboxes under the locked range rule; .mas save/load round-trips confidence and tolerates legacy files without the key; targeted tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Min-confidence plumbing — MaskerConfig field, INI round-trip, configure() at all three detect sites</name>
  <files>panelcleaner/config.py, manga_ai_studio/core/batch_runner.py, manga_ai_studio/gui/main_window.py, tests/test_core/test_masker_config_roundtrip.py, tests/test_core/test_batch_runner.py</files>
  <action>
    1. `panelcleaner/config.py` `MaskerConfig` (@define, :554) — mirror exactly how `max_inpaint_resolution` (:573) was added as a vendoring deviation:
       - Field `detection_conf_thresh: float = 0.4` after `max_inpaint_resolution`, with a `# Manga AI Studio addition (quick-260903-lm6)` comment: minimum YOLO detection confidence forwarded to the CTD detector; NOT an upstream key (upstream import skips unknown options gracefully).
       - `export_to_conf` template (~:646-651 area): add a commented `detection_conf_thresh = {self.detection_conf_thresh}` line in the same f-string style as max_inpaint_resolution.
       - `import_from_conf` (~:681-682): `try_to_load(self, config_updater, section, float, "detection_conf_thresh")`.
       - `fix()` clamp (~:710-715, next to the max_inpaint_resolution clamp): coerce via float() with try/except fallback 0.4, then clamp to 0.05..0.95.
    2. `manga_ai_studio/core/batch_runner.py` — at BOTH det-model creation sites (`batch_detect` :430-431 and `batch_detect_and_clean` :514-515), insert immediately BEFORE `det_model.load(...)`:
       `det_model.configure(conf_thresh=float(getattr(masker_conf, "detection_conf_thresh", 0.4)))` — configure-before-load is required (load forwards conf_thresh into TextDetector); `masker_conf` is already a parameter of both functions. Use getattr-with-default to match the existing max_inpaint_resolution tolerance pattern.
    3. `manga_ai_studio/gui/main_window.py` `detect_text()` — immediately after `model = backend_factory("detection", self._detection_backend())` (:5305) and BEFORE the Worker spawn (:5308), add the same configure call reading `self.profile_manager.config.current_profile.masker`. The load happens inside the worker, so configure-before-load holds; the fresh-adapter-per-run makes the value effective on every run.
    4. Do NOT touch `manga_ai_studio/adapters/onnx_impl.py` — `OnnxDetectionModel` is a stub whose configure/load raise NotImplementedError (torch is the only Phase-1 detection backend).
    5. Tests:
       - tests/test_core/test_masker_config_roundtrip.py (extend): default is 0.4; export_to_conf → import_from_conf round-trips 0.55; fix() clamps 3.0 → 0.95 and 0.0 → 0.05; a conf file without the key loads to the default.
       - tests/test_core/test_batch_runner.py (extend): give the existing `FakeDetectionModel` a `configure(**kwargs)` method that records kwargs, then in a batch_detect (or batch_detect_and_clean) test assert the fake received `conf_thresh` equal to the masker_conf value threaded into the entry point.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_masker_config_roundtrip.py tests/test_core/test_batch_runner.py tests/test_core/test_adapters.py -x -q   (drop the leading "& " when running from Git Bash)</automated>
  </verify>
  <done>MaskerConfig.detection_conf_thresh exists (default 0.4, clamped 0.05-0.95, INI round-trips); batch_detect and batch_detect_and_clean configure the det model from masker_conf before load; interactive detect_text configures the fresh adapter before the worker loads it; targeted tests green.</done>
</task>

<task type="auto">
  <name>Task 3: GUI surfaces — Min confidence spinbox in Detection settings + Confidence row in Inspector</name>
  <files>manga_ai_studio/gui/tools_panel.py, manga_ai_studio/gui/inspector_panel.py, tests/test_gui_detection_settings.py, tests/test_gui/test_inspector_override.py</files>
  <action>
    1. `manga_ai_studio/gui/tools_panel.py` `DetectionSettingsBody` (class :165):
       - Create `self.conf_thresh_spin = QDoubleSpinBox(self)` near `improvement_spin` (~:315): setRange(0.05, 0.95), setSingleStep(0.05), setDecimals(2), setValue(0.40), setWrapping(False). Label the form row "Min confidence". Tooltip in the wording style of the neighboring fit-param widgets, e.g. "Minimum detector confidence for Detect Text. Lower values keep more boxes, including possible false positives. Applies on the next Detect Text run."
       - Connect: `self.conf_thresh_spin.valueChanged.connect(self._on_fit_param_changed)` (joins the shared :363-365 signal path).
       - `masker_values()` (:367-383): add `"detection_conf_thresh": self.conf_thresh_spin.value(),` — the key MUST equal the MaskerConfig field name so MainWindow's setattr persistence loop (:3988-3998) picks it up for free.
       - `set_masker_values()` (:385-432): add `self.conf_thresh_spin` to the blockSignals widgets list and populate with `self.conf_thresh_spin.setValue(float(getattr(masker_conf, "detection_conf_thresh", 0.4)))` (getattr mirrors the max_inpaint tolerance precedent).
       - Update the class/signal docstrings that say "seven next-detect fit" to eight (docstring ~:173 and `_on_fit_param_changed` :363).
       - Persistence is then free: existing wiring (`masker_params_changed` → `_on_masker_params_changed` :3773/:3988, startup population :3783-3785) needs no changes.
    2. `manga_ai_studio/gui/inspector_panel.py` — mirror the Std dev row exactly:
       - QSS near :110: add `QLabel#confidenceLabel { color: #9a9aa2; }`.
       - After the Std dev row (~:382-388): `self.confidence_label = QLabel("\u2014")`, `setObjectName("confidenceLabel")`, tooltip along the lines of "Detector confidence reported for this box. \u2014 means unknown (user-drawn or legacy box).", then `form.addRow("Confidence", self.confidence_label)`.
       - Add `_set_confidence_text(self, conf)` next to `_set_std_dev_text` (:1128-1137): None → em dash; else `self.confidence_label.setText(f"{float(conf):.0%}")` (e.g. 0.87 → "87%").
       - `load_box`: right after `self._set_std_dev_text(pagebox.std_dev)` (:690), call `self._set_confidence_text(getattr(pagebox, "confidence", None))`.
       - `clear()`: after `self._set_std_dev_text(None)` (:1216), call `self._set_confidence_text(None)`.
       - Add `self.confidence_label` to the enable/disable widget list that contains `self.std_dev_label` (~:1241) so its enabled state tracks the same sections.
    3. Tests (headless QWidget patterns — follow the existing fixtures in each file):
       - tests/test_gui_detection_settings.py (extend): default spin value 0.40; changing it emits `masker_params_changed`; `set_masker_values` populates the spin from a MaskerConfig and does not re-emit (blockSignals contract).
       - tests/test_gui/test_inspector_override.py or the nearest inspector test file (extend): `load_box` with a PageBox whose confidence=0.87 shows "87%"; confidence=None shows the em dash; `clear()` resets to the em dash.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_detection_settings.py tests/test_gui/test_inspector_override.py tests/test_gui/test_detection_settings_tooltip.py tests/test_gui/test_tools_panel_masker.py -x -q   (drop the leading "& " when running from Git Bash)</automated>
  </verify>
  <done>Detection panel shows a persistent "Min confidence" spinbox wired into the existing masker persistence path; the Inspector shows a "Confidence" row (percentage or em dash) that loads with the box, clears, and matches the Std dev styling; targeted GUI tests green.</done>
</task>

</tasks>

<verification>
- Full suite stays green: `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (baseline 552 passed, 0 failed; expect +~10 new tests, 0 failures).
- Grep sanity: `grep -rn "detection_conf_thresh" manga_ai_studio/ panelcleaner/ --include="*.py"` shows the field defined in config.py, exported/imported, read at all three configure sites, and keyed in tools_panel masker_values.
- No vendored behavior change beyond the two prob assignments in group_output (TextBlock defaults untouched).
</verification>

<success_criteria>
- User can set Min confidence (0.05-0.95) in the Detection settings panel; the value persists across restart and is applied by the detector on the next interactive Detect Text AND batch detect — verifiable by lowering it and seeing more (lower-confidence) boxes appear.
- Every detected box exposes its YOLO confidence in the Inspector as a percentage; user-drawn and scattered-line boxes show "—".
- Confidence and the setting both survive .mas save/reload; legacy projects load unchanged.
- Out-of-scope items (auto-prune, nms/input_size exposure, re-filtering placed boxes, ONNX wiring) untouched.
</success_criteria>

<output>
Commit per task with Conventional Commits using the quick-task scope, e.g.:
1. `feat(quick-260903-lm6): capture per-box detector confidence on PageBox + .mas persistence`
2. `feat(quick-260903-lm6): wire Min confidence setting into MaskerConfig and all detect paths`
3. `feat(quick-260903-lm6): Min confidence spinbox + Inspector Confidence row`
Do NOT commit STATE.md/PLAN.md/SUMMARY.md — the orchestrator handles docs.
</output>
