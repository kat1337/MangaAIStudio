---
phase: quick-260828-l3l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/mask_editor.py
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/tools_strip.py
  - manga_ai_studio/gui/assets/icons/restore.svg
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_canvas.py
  - tests/test_gui_tools_strip.py
  - tests/test_history.py
autonomous: true
requirements: [QUICK-260828-L3L]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Dragging the Restore brush over the page composites the D-06 original-baseline pixels inside the brush radius onto the working image (mangled inpaint results revert to original artwork by hand).
    - One stroke = one undo entry; Ctrl+Z reverts the whole stroke via the existing bbox-patch image-undo path.
    - A committed stroke syncs ImageFile.current_image, marks the session dirty (title *), and is embedded in navigation/save.
    - Restore strokes never rebaseline `canvas._original_image_numpy` (D-14) — Show Original (P) still compares against the true disk original.
    - Restore is a 7th exclusive strip tool with its own icon, tooltip, page-open gating, and the free-letter shortcut O.
  artifacts:
    - ToolMode.RESTORE in manga_ai_studio/core/mask_editor.py
    - Restore stroke state machine + `restore_committed` signal in manga_ai_studio/gui/canvas.py
    - restore.svg + ToolsStrip button/action in manga_ai_studio/gui/tools_strip.py
    - action_tool_restore + `_on_restore_committed` write-back in manga_ai_studio/gui/main_window.py
    - New tests in tests/test_gui_canvas.py, tests/test_gui_tools_strip.py, tests/test_history.py
  key_links:
    - canvas._original_image_numpy (canvas.py:441, seeded at canvas.py:1040) -> restore stamp source
    - canvas.restore_committed -> main_window._on_restore_committed -> set_image_from_numpy(bbox) -> current_image -> _set_session_dirty -> history.push_image_action
    - tools_strip.action_restore -> MainWindow.set_active_tool -> canvas.set_tool
---

<objective>
Add a "Restore" brush tool: dragging over the page paints the ORIGINAL page's pixels (the Show Original baseline, `canvas._original_image_numpy`) inside the brush radius onto the working image, for hand-fixing mangled inpaint results. Per-stroke bbox-patch undo; brush size shared with the other brush tools; strip button + O shortcut; no baseline rebaseline (D-14).

Purpose: inpaint sometimes mangles artwork; today the only fix is re-running inpaint. This gives a pixel-level "eraser back to original" like an eraser that returns the image to its original state.

Output: Working 7th canvas tool, end-to-end (enum -> canvas stroke -> strip button -> undo/dirty write-back), with tests. All interpreter invocations use the pinned interpreter from AGENTS.md (`C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe -m pytest ...`).
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@manga_ai_studio/core/mask_editor.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/gui/tools_strip.py
@manga_ai_studio/gui/main_window.py
@tests/test_gui_canvas.py
@tests/test_gui_crop_tool.py

Key verified anchors (line numbers from planning probe; re-grep if drifted):
- ToolMode enum: mask_editor.py:56 (MOVE/BRUSH/RECTANGLE/LASSO/ERASER/CROP).
- PAINT_TOOLS frozenset: canvas.py:102-104 (drives Alt-gated box interaction + cursor visibility).
- Baseline slot: canvas.py:441 `_original_image_numpy`; seeded per page at :1040 (set_image_from_numpy_page, quick-260826-u9m D-06 semantics); `set_image_from_numpy` only captures when the slot is None (canvas.py:1086) — it can never overwrite an existing baseline. `rebaseline_original()` :1047 unchanged.
- Stroke machinery: `_begin_paint` :1727, `_advance_paint` :1742, `_end_paint` :1757; dispatch already routes any tool not in (MOVE, CROP) to `_begin_paint` when a mask plane exists (canvas.py:1514-1521). Cursor visuals `_update_cursor_visuals` :1207 (PAINT_TOOLS visibility, eraser cyan / paint red branches).
- numpy accessors: `get_image_numpy` :912 (detached copy, stride-safe), `set_image_from_numpy(rgb, bbox)` :953.
- Inpaint write-back order to mirror: main_window.py `_on_inpaint_finished` (~:6276-6360): patch slice -> set_image_from_numpy(result, bbox) -> current_image write-back -> _set_session_dirty -> push_image_action.
- `_set_session_dirty`: main_window.py:2409.
- Tools strip: tools_strip.py `_TOOL_ICONS` :45-52, exclusive 6-action group, `_make_tool_action`, buttons loop, `set_active_tool`. MainWindow wiring: `action_tool_*` creation :1033-1079, Tools menu :1152, `_refresh_action_states` gating lists :1350-1370, tool QShortcut loop ("V","B","R","L","E","G") in `_wire_tool_actions` :3594-3605, `set_active_tool` sync tuple :5058-5067.
- Test patterns: `_canvas_with_image` / `_press` / `_move` / `_release` helpers in tests/test_gui_canvas.py; 7th-tool enumeration assertions in tests/test_gui_crop_tool.py:203-312; MainWindow image-undo fixtures in tests/test_history.py:515; strip tests in tests/test_gui_tools_strip.py; shortcut assertions in test_gui_canvas.py:525 (`test_main_window_tool_shortcuts`).
- Payload-aliasing discipline: numpy crossing a Qt signal must be `.copy()`-detached (tests/test_payload_aliasing.py; RESEARCH Pitfall 2).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canvas Restore tool core — ToolMode.RESTORE, pixel-stamp stroke, green cursor, restore_committed signal (+ unit tests)</name>
  <files>manga_ai_studio/core/mask_editor.py, manga_ai_studio/gui/canvas.py, tests/test_gui_canvas.py</files>
  <action>
1. Add `RESTORE = "restore"` to ToolMode in mask_editor.py (after ERASER, keep CROP last or place RESTORE after CROP — match the enum's docstring update: the 7th tool).
2. In canvas.py add ToolMode.RESTORE to the PAINT_TOOLS frozenset (canvas.py:102) — this inherits Alt-gated box interaction under the tool (D-15) and cursor-circle visibility for free. Do NOT touch `_effective_eraser` (RESTORE is never an eraser).
3. In `_update_cursor_visuals` add a RESTORE branch BEFORE the eraser/paint color pick: green pen QColor(95, 208, 104, 200) + brush QColor(95, 208, 104, 60) (the inpaint palette's #5fd068), same circle rect math as the other branches.
4. Implement the Restore stroke in the three paint hooks (all pixel work, no mask planes):
   - `_begin_paint`: when current_tool is RESTORE, first gate: require `self._original_image_numpy is not None` AND `not self._showing_original` AND dims of `_original_image_numpy` equal the displayed image dims; otherwise return WITHOUT setting `_is_painting` (silent no-op, same style as the other guards). DECISION (documented): a press while the P-preview (`_showing_original`) is active is suppressed rather than force-exiting preview — force-exiting would fire an accidental stroke on the same click the user used to leave the compare view, and painting onto the hidden working image mid-preview would be invisible feedback. On a passing press: snapshot `pre = self.get_image_numpy()` (the detached pre-stroke copy), initialize a stroke bbox accumulator, stamp a disc at the start point (a click restores a dot), refresh display, set `_is_painting`.
   - New private `_restore_stamp(curr: QPointF)`: copy the disc of radius `brush_size / 2` from `_original_image_numpy` into a per-stroke working numpy array. Compute the integer disc bbox clipped to image dims, build a boolean circular mask for the clipped region, and assign through it (numpy boolean-mask assignment — no PIL, no QImage painting). Grow the stroke bbox accumulator with the disc bbox.
   - New private `_refresh_restore_display()`: build a QImage from the working array exactly the way `show_original` does (QImage from the array buffer with Format_RGB888, `.copy()` to detach, `image_item.setPixmap(QPixmap.fromImage(...))`). State the perf choice in a comment: full-frame numpy->QImage per move event at page scale (~1500x2000) is acceptable and keeps the code on one display path; do NOT route through `set_image_from_numpy`/`_set_image_from_numpy` per move — that path mutates `_inpainted_qimage` / baseline-capture state (canvas.py:1086 gate) and would poison the P-preview claim mid-stroke.
   - `_advance_paint`: RESTORE branch — interpolate along the segment from `self._last_pt` to curr at step `max(1.0, self.brush_size / 4)` scene px (mirrors the continuous RoundCap coverage of `paint_mask_stroke` at 4x-sub-brush spacing so fast strokes stay gapless), stamping at each interpolated point; update `_last_pt`; refresh display.
   - `_end_paint`: RESTORE branch — finalize display, then compute the stroke bbox (clamped to image dims, at least 1x1) and emit a new signal ONCE per stroke; clear stroke state. New canvas signal next to `crop_committed` (canvas.py:220): `restore_committed = Signal(object)` carrying a dict with integer x, y, w, h and `pre_patch` = the pre-stroke snapshot sliced to that bbox, `.copy()`-detached (payload-aliasing discipline). A press+release with no move still commits (the dot).
5. Tests in tests/test_gui_canvas.py reusing `_canvas_with_image`, `_press`, `_move`, `_release`:
   - stamps original pixels: load image A via set_image_from_numpy (seeds `_original_image_numpy` per the canvas.py:1086 gate), then mutate the display to B via `set_image_from_numpy(b_img, bbox=...)`; stroke RESTORE over the mutated region; assert `get_image_numpy()` at stroked points equals A and at unstroked points equals B.
   - no baseline -> no-op: clear `_original_image_numpy` to None; stroke leaves the display unchanged and emits nothing.
   - suppressed during `_showing_original=True` (set via `show_original(True)`): stroke leaves the working image unchanged.
   - cursor green: `set_tool(ToolMode.RESTORE)` shows `cursor_item` with the green pen color.
   - one emission per stroke: qtbot signal spy on `restore_committed`; one drag -> exactly one payload whose w/h cover the drag path and whose pre_patch shape is (h, w, 3).
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_canvas.py -x -q</automated>
  </verify>
  <done>ToolMode.RESTORE exists; a canvas-level restore stroke replaces stroked pixels with baseline pixels (dot and drag), is a no-op without a baseline or during P-preview, shows a green brush cursor, emits exactly one detached-payload restore_committed per stroke; full test_gui_canvas.py passes.</done>
</task>

<task type="auto">
  <name>Task 2: Toolbar wiring + main_window write-back — strip button, O shortcut, per-stroke undo/dirty/current_image integration (+ tests)</name>
  <files>manga_ai_studio/gui/assets/icons/restore.svg, manga_ai_studio/gui/tools_strip.py, manga_ai_studio/gui/main_window.py, tests/test_gui_tools_strip.py, tests/test_history.py, tests/test_gui_canvas.py</files>
  <action>
1. Create `manga_ai_studio/gui/assets/icons/restore.svg` mirroring eraser.svg's format (24x24 viewBox, stroke #e8e8ea, stroke-width 2, round caps/joins, fill none) with a simple restore glyph (e.g. a counter-clockwise arrow curling into a brush tip — keep it minimal).
2. tools_strip.py: add `ToolMode.RESTORE: "restore"` to `_TOOL_ICONS`; create `self.action_restore` via `_make_tool_action` with tooltip "Restore tool (O) — paints the original page pixels to undo inpaint damage; hold Alt to select or move a box." (mirror the brush-family Alt clause); add it to `_action_to_tool`, the checkable-buttons loop (place Restore after Eraser, before Crop to keep destructive/canvas-geometry tools last), and keep the QActionGroup exclusive — the group now holds exactly seven actions.
3. main_window.py — follow the Crop (6th tool) precedent everywhere it appears:
   - `action_tool_restore` QAction block after `action_tool_crop` (main_window.py:1070-1079): checkable, setData(ToolMode.RESTORE), triggered -> set_active_tool(ToolMode.RESTORE), tooltip matching the strip copy.
   - `tools_menu.addAction(self.action_tool_restore)` next to the Crop entry (:1152).
   - `_refresh_action_states`: add `action_tool_restore` to the window-action page-open gating tuple (:1350-1357) and `self.tools_strip.action_restore` to the strip gating tuple (:1361-1370). Gating is page-open like the sibling brush tools (NOT baseline-gated — the baseline may legitimately exist before any inpaint, and the canvas Task 1 guard keeps a missing-baseline stroke a safe no-op).
   - `set_active_tool` sync tuple (:5058-5067): add `action_tool_restore`.
   - `_wire_tool_actions` shortcut loop (:3594-3605): add ("O", ToolMode.RESTORE). "O" is free — the audit of window shortcuts shows bare-letter registrations only V B R L E G M T P D C F (O mnemonic: Original).
   - New slot `_on_restore_committed(self, payload)` connected next to the crop_committed connect (:3580). Mirror `_on_inpaint_finished`'s order EXACTLY: (a) unpack x, y, w, h, pre_patch from the payload; (b) `composite = self.canvas.get_image_numpy()`; (c) `self.canvas.set_image_from_numpy(composite, bbox=(x, y, w, h))` — refreshes `_inpainted_qimage`/display over the mutated region so the P-preview side stays coherent; this never overwrites the existing baseline (capture is gated on None at canvas.py:1086) so D-14 holds with no extra code; (d) write `self.image_files[idx].current_image = self.canvas.get_image_numpy()` for the open page (guard index like the inpaint handler); (e) `self._set_session_dirty()`; (f) `self.history.push_image_action(x, y, pre_patch)` when history is wired (no bare except). Existing Ctrl+Z image-undo (pop_image_undo + apply_undo_image) then reverts the stroke with zero new history code.
4. Tests:
   - tests/test_gui_tools_strip.py: strip exposes action_restore carrying ToolMode.RESTORE; the exclusive group has exactly 7 actions; `set_active_tool(ToolMode.RESTORE)` checks the restore action and emits tool_changed with RESTORE; restore button follows page-open gating.
   - tests/test_gui_canvas.py `test_main_window_tool_shortcuts`: extend the key-set assertion with "O" and the disabled-without-page assertion with action_tool_restore.
   - tests/test_history.py (MainWindow fixtures near :515): open a page, seed a differing baseline (same setup as Task 1's stamps test), perform a restore stroke on window.canvas; assert (a) exactly one image-undo entry was pushed, (b) `image_files[idx].current_image` equals the post-stroke display, (c) the window title carries the dirty suffix, (d) `pop_image_undo` returns a patch equal to the pre-stroke region and applying it restores the pre-stroke pixels; also assert `window.canvas._original_image_numpy` is UNCHANGED by the stroke (D-14 regression guard).
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_canvas.py tests/test_gui_tools_strip.py tests/test_history.py -x -q && "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <done>Restore appears as the 7th exclusive strip tool (icon, tooltip, O shortcut, page-open gating); a stroke composites baseline pixels, pushes one bbox-patch undo entry, syncs current_image, marks the session dirty, and leaves `_original_image_numpy` untouched; Ctrl+Z reverts the stroke; the full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| icon path -> QIcon | Restore button loads a bundled SVG; resolution must stay module-relative |
| numpy payload across Qt signal | restore_committed carries an ndarray from canvas to MainWindow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l3l-01 | Tampering | restore.svg icon load | low | mitigate | Reuse tools_strip `_icon()` module-relative resolution (`Path(__file__).parent / "assets" / "icons"`) — never CWD/user paths (existing T-09a-01 pattern) |
| T-l3l-02 | Tampering | restore_committed ndarray payload | medium | mitigate | `pre_patch` is `.copy()`-detached in canvas before emission (payload-aliasing discipline, tests/test_payload_aliasing.py) — no views into the live canvas buffer cross the signal |
| T-l3l-03 | Denial of Service | per-move full-frame numpy->QImage refresh | low | accept | Bounded by page size (~3MP) and Qt mouse-event rate; matches the accepted cost of the inpaint display path; document in `_refresh_restore_display` |
</threat_model>

<verification>
- Pinned-interpreter full suite passes: `"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (baseline 1205 passed).
- grep confirms ToolMode.RESTORE appears in mask_editor.py, canvas.py PAINT_TOOLS, tools_strip.py, main_window.py (shortcut loop, action creation, gating lists, set_active_tool sync).
- Manual: open a page, inpaint, select Restore (O), drag over the mangled region — original artwork returns under the green circle; Ctrl+Z undoes the whole stroke in one step; P still shows the true disk original.
</verification>

<success_criteria>
- Restore brush composites `_original_image_numpy` pixels inside the brush radius (dot and interpolated drag) onto the working image.
- Per-stroke undo via the existing bbox-patch image stack; stroke commits sync current_image + session dirty.
- D-14 intact: restore strokes never rebaseline the original baseline; P-preview suppresses stroking.
- Strip button + icon + O shortcut + page-open gating present; exclusive group holds exactly 7 tools.
- All new and existing tests pass under the pinned interpreter.
</success_criteria>

<output>
Create `.planning/quick/260828-l3l-restore-eraser-tool-paint-original-pixel/260828-l3l-SUMMARY.md` when done
</output>
