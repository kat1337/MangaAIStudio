---
phase: quick
plan: 260825-u9q
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/project_io.py
  - tests/test_core/test_project_io.py
autonomous: true
requirements: [QUICK-STALE-MASK]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - A saved project where a masked box was resized after fitting LOADS successfully — the stale per-box mask is silently dropped (mask=None), not rejected as corrupt
    - A std_dev/fill_color measured against different geometry never survives the drop (all three nulled together)
    - A future SAVE can never produce an unloadable file — stale masks serialize as null
    - Genuinely corrupt mask data (bad base64, non-PNG bytes) still raises ProjectFormatError
  artifacts:
    - manga_ai_studio/core/project_io.py (_pagebox_mask_from_json sanitize branch, json_to_pagebox coherent nulling, pagebox_to_json save-side guard)
    - tests/test_core/test_project_io.py (updated mismatch test + new stale-mask tests)
  key_links:
    - json_to_pagebox mask-decode result -> std_dev/fill_color invalidation (parse order: std_dev/fill_color are parsed BEFORE mask decode at L321-353 vs L361-363, so the nulling happens after decode)
---

<objective>
Fix project-load failure for projects where a masked box was resized after its mask was fitted: the loader treats a size-mismatched (stale) per-box mask as absent data instead of raising ProjectFormatError, and the saver stops serializing stale masks so new saves are always loadable. Decode-level garbage keeps raising.

Purpose: any real project where a user resized a detected box before saving is currently unloadable ("per-box mask size (269, 753) does not match the box dims 519x901" -> corrupt-project dialog). The data is intact; only the size cross-check (T-08-06) makes the contract asymmetric (save accepts what load rejects).

Output: sanitized load/save paths in core/project_io.py + TDD tests proving the exact user scenario, the save-side guard, and the garbage-rejection regression.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Src/Manga AI Studio/manga_ai_studio/core/project_io.py (pagebox_to_json L205-272 esp. mask/std_dev/fill_color emission L254-257; json_to_pagebox L275-405 esp. std_dev parse L321-326, fill_color parse L341-353, box build L355-357, mask decode L361-363; _pagebox_mask_from_json L408-442 esp. strict size check L437-441)
@C:/Src/Manga AI Studio/tests/test_core/test_project_io.py (test_per_box_mask_size_mismatch_rejected L432-450 — encodes the OLD reject-on-mismatch contract and MUST be rewritten; test_per_box_mask_garbage_rejected L453-471 — regression anchor that stays green)
@C:/Src/Manga AI Studio/manga_ai_studio/core/box_model.py (PageBox.mask is a box-cropped mode-"1" PIL image; std_dev/fill_color are D-15 seam fields populated together by the per-box fit)

Facts established during planning:
- `json_to_pagebox` (project_io.py:363) is the SOLE caller of `_pagebox_mask_from_json` — no other call site relies on it raising on mismatch. The GUI clipboard-paste path (main_window.py:2771) goes through `json_to_pagebox`, so it inherits the fix with zero changes.
- Parse order inside `json_to_pagebox`: std_dev (L321) and fill_color (L341) are parsed BEFORE the mask decode (L361). The cleanest coherent implementation: have the decode path signal staleness (return None for a decodes-fine-but-wrong-size mask) and let `json_to_pagebox` null std_dev/fill_color when a mask VALUE was present but decoded stale. A mask key of None already yields mask/std_dev-independent defaults.
- Root cause chain (probe-confirmed): quick-260822-gnq made refit manual-only; resizing a fitted box leaves PageBox.mask at old cropped dims while PageBox.box takes new geometry; pagebox_to_json serializes both verbatim; the loader's T-08-06 check rejects the whole file.
- Interpreter: ALWAYS `C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe` (AGENTS.md — bare python/pytest resolves to an unrelated venv).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load-side stale-mask sanitization — mismatched-size masks decode to None, std_dev/fill_color nulled coherently</name>
  <files>manga_ai_studio/core/project_io.py, tests/test_core/test_project_io.py</files>
  <behavior>
    - Test A (user scenario, RED first): build a PageBox with a REAL fitted state — box [0,0,100,40], a mode-"1" PIL mask sized (100,40), std_dev=12.5, fill_color=(10,20,30) — simulate the resize by constructing the JSON dict from pagebox_to_json output then overriding "box" to [0,0,80,60] (new geometry, mask still 100x40). json_to_pagebox SUCCEEDS today? NO — assert it succeeds after the fix, with out.mask is None, out.std_dev is None, out.fill_color is None, inpaint_override PRESERVED (user intent), style/payload untouched.
    - Test B (rewrite of test_per_box_mask_size_mismatch_rejected): the crafted-dict form — base dict with box [0,0,100,40] + a valid 4x4 PNG mask b64 + std_dev 12.3 + fill_color [1,2,3] loads with all three None (not ProjectFormatError). Also the matching-size case still returns a real mode-"1" image (existing behavior unchanged).
    - Test C (regression, unchanged): test_per_box_mask_garbage_rejected still passes — bad base64 and valid-b64-non-PNG STILL raise ProjectFormatError. Do not weaken the decode wrap.
    - Test D (matching-size round-trip stays exact): existing round-trip tests (test_pagebox_json_round_trip family) stay green without modification.
  </action>
  <action>RED: write Tests A/B into tests/test_core/test_project_io.py (replace the body of test_per_box_mask_size_mismatch_rejected with Test B's new expectations; add Test A as a new test function using the pinned interpreter's pytest). Run them — they MUST fail against current code (Test A/B raise ProjectFormatError today). GREEN:
  1. In `_pagebox_mask_from_json` (project_io.py:408-442): keep the decode try/except byte-identical (binascii/PIL garbage still raises ProjectFormatError). Change the size cross-check (L437-441): when decoded.size != (box_w, box_h), log a warning via loguru (`from loguru import logger` — already a project dependency) describing the stale-mask drop with both sizes, and return None INSTEAD of raising. Update the docstring to document the stale-tolerant contract (decodes-fine + wrong size -> None; decode garbage -> ProjectFormatError).
  2. In `json_to_pagebox` (L361-363): distinguish "no mask value" from "mask value decoded stale" — capture `mask_raw = d.get("mask")`; `mask = _pagebox_mask_from_json(mask_raw, box) if mask_raw is not None else None`. When `mask_raw is not None and mask is None` (stale), also force `std_dev = None` and `fill_color = None` before the PageBox constructor call (a std-dev/median-color measured against different geometry is meaningless). Leave inpaint_override as-is (explicit user intent, cheap to carry).
  3. Do NOT touch the std_dev float-coercion, fill_color triple validation, or override enum validation blocks — they run before the mask decode and stay authoritative.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_project_io.py -q</automated>
  </verify>
  <done>Tests A-D pass: stale-mask dicts load with mask/std_dev/fill_color all None and override preserved; matching-size masks still return mode-"1" images; decode-garbage still raises ProjectFormatError; the whole test_project_io.py file is green.</done>
</task>

<task type="auto">
  <name>Task 2: Save-side stale-mask guard + full-suite green</name>
  <files>manga_ai_studio/core/project_io.py, tests/test_core/test_project_io.py</files>
  <action>
    Add ONE test first (save-side contract): build the same fitted-then-resized PageBox shape as Task 1 Test A but call pagebox_to_json directly on the live object — construct a PageBox whose mask.size != (pb.box.x2-pb.box.x1, pb.box.y2-pb.box.y1) (e.g. box [0,0,80,60], mask (100,40)) with std_dev=12.5, fill_color=(10,20,30); assert the emitted dict has mask=null, std_dev=null, fill_color=null, and inpaint_override/style/payload emitted normally. Also assert the happy case: a matching-size mask serializes to a non-null base64 string (existing behavior).
    Then implement in `pagebox_to_json` (project_io.py:246-272): before building the return dict, compute `box_w = pb.box.x2 - pb.box.x1`, `box_h = pb.box.y2 - pb.box.y1`, `mask_stale = pb.mask is not None and pb.mask.size != (box_w, box_h)`. When mask_stale: emit "mask": None, "std_dev": None, "fill_color": None (mirroring the load-side semantics exactly — the three fit-derived fields die together, override survives). Otherwise emit verbatim as today. Update the docstring with one line noting the guard exists so a resized-after-fit box can never produce an unloadable file. Keep everything else in the projection byte-identical.
    Finally run the FULL suite — this is the release gate for the quick task (baseline: 1138 passed).
    Verify with: & "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <done>pagebox_to_json emits null mask/std_dev/fill_color for a stale mask and normal values otherwise; full suite passes with 0 failures (>= 1138 passed baseline plus the new tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved .mas file -> loader | Untrusted persisted data crosses into json_to_pagebox on every project open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QKN-01 | Tampering | _pagebox_mask_from_json decode wrap | high | mitigate | KEEP the existing binascii/Error/ValueError/OSError/UnidentifiedImageError/DecompressionBombError -> ProjectFormatError wrap byte-identical; only the SIZE-MISMATCH branch softens (decode-level garbage still rejects); PIL MAX_IMAGE_PIXELS bomb guard stays active |
| T-QKN-02 | DoS | stale-mask tolerance abuse | low | accept | A crafted wrong-size mask now degrades to mask=None (a benign empty-fit state) instead of rejecting — same blast radius as a file legitimately written before Phase 8; no allocation sized from untrusted dims occurs |
| T-QKN-03 | Repudiation | silent mask drop observability | low | mitigate | loguru warning on every stale-mask drop names both sizes, so users/support can tell a stale-save from genuine corruption |
</threat_model>

<verification>
- Stale-mask dict loads with mask=None, std_dev=None, fill_color=None, override preserved (the exact console error scenario reversed)
- Decode garbage (non-base64, non-PNG) still raises ProjectFormatError
- Matching-size masks round-trip unchanged
- Save-side: stale mask serializes as null trio; fresh fits serialize as before
- Full suite green on the pinned interpreter
</verification>

<success_criteria>
A project saved after resizing a masked box opens without the corrupt-project dialog; re-saving it produces a file with the stale masks already stripped; no legitimate project data other than the stale per-box fit fields (mask/std_dev/fill_color) is lost.
</success_criteria>

<output>
Create C:/Src/Manga AI Studio/.planning/quick/260825-u9q-fix-project-load-failure-valid-v1-manife/260825-u9q-SUMMARY.md when done
</output>
