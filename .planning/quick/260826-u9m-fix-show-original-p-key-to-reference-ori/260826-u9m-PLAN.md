---
phase: quick-260826-u9m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/canvas.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_canvas.py
  - tests/test_gui_project.py
autonomous: true
requirements: [QUICK-FIX-SHOW-ORIGINAL-D06-DISK-REFERENCE]
estimate:
  tokens: 60000
  raw_tokens: 32000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - After Save Project + reopen of a page whose D-06 original.json verifies (and whose pristine file dims match the embedded image), pressing P displays the PRISTINE ON-DISK original pixels — never the saved/inpainted embedded image; toggling off restores the saved state.
    - A page whose original ref fails verification, fails to decode, or has mismatched dims keeps today's fallback (in-memory embedded capture as baseline); P-gating and the "original file not found" tooltip contract are unchanged.
    - A page switch through ANY display path can no longer carry a previous page's Show Original baseline or a stale _showing_original flag into the next page (cross-page bleed dead).
    - D-14 semantics untouched — op write-backs and geometry ops still rebaseline to the post-op image via rebaseline_original(); Levels previews still never rebaseline; inpaint bbox compositing still reads the live pixmap (CR-13).
  artifacts:
    - manga_ai_studio/gui/canvas.py with a new set_image_from_numpy_page(rgb, original_baseline=None) seam that FULLY redefines per-page preview state
    - manga_ai_studio/gui/main_window.py with _decode_original_reference(imf) and both page-display sites wired to it (on_page_selected Step 3 ~line 2087, _display_page_state ~line 2903)
    - New regression tests in tests/test_gui_canvas.py (seam unit tests) and tests/test_gui_project.py (reopen/toggle/dims-fallback/no-bleed)
  key_links:
    - imf.original_verified + imf.path (D-06 verified ref from _build_image_file_from_parsed) → _decode_original_reference → canvas.set_image_from_numpy_page(original_baseline=...)
    - on_page_selected Step 3 / _display_page_state → set_image_from_numpy_page replaces set_image_from_numpy on ONLY those two call sites
    - canvas._original_image_numpy → show_original(True) pixmap swap
---

<objective>
Fix Show Original (P) after Save Project + reopen: P currently shows the SAVED (already inpainted/edited) embedded image as the "original" because `_display_page_state` seeds `_original_image_numpy` by capture-if-None from `imf.current_image`. The D-06 disk reference (`original.json` {path, sha256}, verified at load into `imf.path` + `imf.original_verified`) exists but was only ever used for enable-gating — never for pixels.

Two verified defects fixed:
1. **Missing disk reference:** when the D-06 ref verifies, the Show Original baseline must be decoded FROM DISK, lazy, only for the displayed page. Fallback to today's in-memory capture when unverified/unreadable/dims-mismatched.
2. **Cross-page stale-baseline bleed:** `EditorCanvas._set_image_from_numpy` captures `_original_image_numpy` ONLY-if-None (canvas.py:1032-1033), so a page switch through the numpy display paths (on_page_selected Step 3 with `current_image`, or `_display_page_state`) preserves the OUTGOING page's baseline AND possibly its stale `_showing_original=True` flag. P on page B can display page A's pixels.

Purpose: P is the before/after trust anchor; showing the edited result as "original" defeats its purpose, and cross-page bleed corrupts it further.
Output: one new canvas seam method + MainWindow decode helper wiring both page-display sites + regression tests in two test files.
</objective>

<execution_context>
@C:\Users\Stella\.zcode\gsd-core\workflows\execute-plan.md
@C:\Users\Stella\.zcode\gsd-core\templates\summary.md
</execution_context>

<context>
@C:\Src\Manga AI Studio\.planning\STATE.md
@C:\Src\Manga AI Studio\AGENTS.md

Canvas preview state machinery:
@C:\Src\Manga AI Studio\manga_ai_studio\gui\canvas.py lines 436-443 (state fields), 476-479 (set_image reset), 522-524 (clear reset), 953-1096 (_set_image_from_numpy family + rebaseline_original), 1098-1120 (show_original + has_inpaint_result)

MainWindow pages:
@C:\Src\Manga AI Studio\manga_ai_studio\gui\main_window.py lines 2061-2107 (on_page_selected Step 3 numpy branch), 2807-2889 (_build_image_file_from_parsed — D-06 verify sets imf.path/original_verified), 2891-2910 (_display_page_state), 1280-1304 (_refresh_action_states P gating)

Persistence side (reference only — NO changes there):
@C:\Src\Manga AI Studio\manga_ai_studio\core\project_io.py lines 495-568 (build_page_entries writes original.json), 779-800 (verify_original)

Test conventions (helpers + neighboring Show Original tests):
@C:\Src\Manga AI Studio\tests\test_gui_project.py lines 35-134 (_make_pages/_make_window/_stub_open_dialog/_save_as/_dirty), 710-758 (test_show_original_gating pattern)
@C:\Src\Manga AI Studio\tests\test_gui_canvas.py lines 660-720 (D-14/Pitfall-5 rebaseline test block)
</context>

<tasks>

<task type="auto">
  <name>Task 1: EditorCanvas.set_image_from_numpy_page — page-level preview-state rebase seam + unit tests</name>
  <files>manga_ai_studio/gui/canvas.py, tests/test_gui_canvas.py</files>
  <action>
Add ONE public method to EditorCanvas near the other set_image_from_numpy variants (~after line 991):

`def set_image_from_numpy_page(self, rgb: np.ndarray, original_baseline: np.ndarray | None = None) -> QImage`

Behavior contract (this seam exists because `_set_image_from_numpy`'s capture-if-None semantic at line 1032 CANNOT express "a page display defines its OWN complete preview state"):
1. Validate `rgb` AND, when not None, `original_baseline` with the SAME ValueError contract `_set_image_from_numpy` enforces ((H, W, 3) uint8; reuse the same message shape).
2. Run the shared display machinery once: `qimg = self._set_image_from_numpy(rgb, None, capture_original=False)` — this gets the crop-state clear (plan 05-07), the different-dims plane re-seed (plan 08-02), the Pitfall-2 `.copy()`-detached pixmap, and the empty-state refresh for free.
3. Then EXPLICITLY redefine all three preview-state slots — never relying on capture-if-None:
   - `_original_image_numpy = original_baseline.copy()` when provided, else `rgb.copy()` (fallback = the incoming page's own displayed pixels — reproduces today's observable post-reopen outcome where P showed the saved state, but WITHOUT ever inheriting a foreign page's baseline; also removes the fresh-canvas-captures-None edge at line 1032-1033 where get_image_numpy() returns None).
   - `_inpainted_qimage = qimg` (the same claim capture_original=True would set — makes has_inpaint_result() True so the P action / btn_preview_hold gating works on freshly loaded project pages).
   - `_showing_original = False`.
4. Return qimg.

Docstring must state: callers are PAGE DISPLAYS only (page switch via numpy branch, project-open display); op write-backs and undo stay on set_image_from_numpy (D-14 governs those); original_baseline comes from the MainWindow D-06 disk decode and is None whenever unverified/unreadable/dims-mismatched.

Do NOT touch: set_image_from_numpy, set_image_from_numpy_preview, rebaseline_original, show_original, _set_image_from_numpy internals, or the capture-if-None logic itself.

Tests (tests/test_gui_canvas.py, add a block next to the existing D-14/Pitfall-5 section around line 660+; follow neighbor style — @pytest.mark.gui, qtbot fixture, tiny hand-built (H,W,3) uint8 arrays with distinct constant colors so pixel equality asserts are exact):
- test_set_image_from_numpy_page_seeds_disk_baseline_and_inpaint_claim: display A via the ordinary set_image_from_numpy(arrA); then set_image_from_numpy_page(arrB, arrB_orig) with distinct arrays; assert _original_image_numpy equals arrB_orig, _showing_original False, has_inpaint_result() True; show_original(True) then get_image_numpy() equals arrB_orig; show_original(False) restores arrB pixels.
- test_set_image_from_numpy_page_rebases_no_foreign_bleed: build the EXACT bleed state (display A via set_image_from_numpy, run show_original(True) so a foreign baseline AND _showing_original=True exist), then set_image_from_numpy_page(arrB) WITHOUT baseline; assert baseline now equals arrB (NOT A's pixels), _showing_original False, displayed image is arrB.
- test_set_image_from_numpy_page_invalid_baseline_raises: wrong-rank baseline raises ValueError; a subsequent get of the displayed pixmap is unchanged (error raised BEFORE any display mutation — validate both arrays before calling the shared impl).
- test_set_image_from_numpy_page_fresh_canvas_has_inpaint_claim: on a freshly cleared canvas, set_image_from_numpy_page(arr, arr_orig) yields has_inpaint_result() True and enabled-capable P state inputs (this locks the _inpainted_qimage assignment).

Per repo discipline: PIL/QImage buffers outlived by storage must be `.copy()`-detached (Pitfall 2) — `.copy()` on every stored array covers it; keep tests platform-robust (no hard-coded font/geometry values needed here).
  </action>
  <verify>
    <automated>"C:/Users/Stella/.pyenv/pyenv-win/versions/3.14.2/python.exe" -m pytest tests/test_gui_canvas.py -x -q</automated>
  </verify>
  <done>All four new tests pass; every pre-existing canvas test still passes (capture-once semantics of the OLD methods proven unchanged by the surviving rebaseline/preview-capture tests).</done>
</task>

<task type="auto">
  <name>Task 2: MainWindow wires the D-06 disk reference into both page-display sites + GUI regression tests</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_project.py</files>
  <action>
1. Add a private helper near `_build_image_file_from_parsed` (~line 2807):
   `def _decode_original_reference(self, imf: ImageFile) -> np.ndarray | None`
   - Return None unless `imf.original_verified` is True AND `imf.current_image is not None` (the current_image provides the expected dims).
   - `resolved = Path(imf.path).resolve()` inside try catching (OSError, ValueError) -> None.
   - Decode lazily with PIL exactly like the embedded-blob decode at ~2859: `np.asarray(Image.open(resolved).convert("RGB")).copy()` inside try catching (OSError, ValueError, Image.DecompressionBombError) -> None. (.copy() detaches + guarantees contiguous C-order — Pitfall 2; show_original builds an RGB888 QImage straight off `arr.data`, which requires contiguity.)
   - Dims guard: if decoded.shape[:2] != imf.current_image.shape[:2] return None. Rationale (put in docstring): a geometry-altered page's pristine file legitimately mismatches the embedded dims; swapping a different-size pixmap onto `image_item` during a P toggle would desync sceneRect/mask overlay dims, so such references degrade to the in-memory fallback like unreadable ones do.
   - Perf note in docstring: synchronous single-file decode PER PAGE SWITCH has direct precedent (`set_image_from_path` decodes per switch); lazy = only the displayed page decodes, never eager-decoding all pages.
   - Do NOT re-run sha256 verification here — verify_original already gated load time (a mid-session corruption surfaces as an OSError -> graceful None fallback, which is acceptable).
2. Wire site 1 — `on_page_selected` Step 3 (~line 2087): replace
   `self.canvas.set_image_from_numpy(imf.current_image.copy())`
   with `baseline = self._decode_original_reference(imf)` followed by
   `self.canvas.set_image_from_numpy_page(imf.current_image.copy(), baseline)`.
   Extend the surrounding CR-01 comment block with the D-06 note: when the persisted original verifies, ITS decoded pixels seed the Show Original baseline; the fallback keeps today's behavior (P shows the saved state) for unverified/unreadable/dims-mismatched pages, so `_refresh_action_states`' gating contract needs no changes.
3. Wire site 2 — `_display_page_state` (~line 2903): identical replacement (plus one added docstring sentence pointing at the D-06 seeding).
4. Leave main_window.py:1486 (geometry-op write-back) and :5958 (inpaint bbox composite) UNTOUCHED — they are op/undo paths governed by D-14 rebaseline semantics and CR-13 live-pixmap compositing, not page displays.
5. Intended semantic upgrade to document in the code comment (do NOT try to preserve old behavior): after reopen + a NEW inpaint (no geometry op), P now shows the PRISTINE DISK ORIGINAL rather than the pre-inpaint embedded frame — that IS the requested "real reference to the original image file on disk". Inpaint must NOT rebaseline; only _apply_geometry_op's tail rebaseline_original() (line 1525) may move the baseline mid-session (D-14, unchanged).
6. Tests (tests/test_gui_project.py, new section next to test_show_original_gating ~line 758; reuse helpers _make_pages/_make_window/_save_as/_dirty/_stub_open_dialog and the reopen pattern of test_show_original_gating at lines 723-734):
   - test_show_original_uses_persisted_original_after_reopen: folder-open chapter with page_01.png; read its pristine pixels via PIL in the test; simulate an edit by setting window.image_files[0].current_image to np.flipud(pristine).copy() (same dims — flip preserves 60x60), mark dirty, _save_as(project_dir); open window2 via stubbed manifest dialog + processEvents; assert original_verified True, canvas._original_image_numpy equals pristine (and NOT the flipped embed), displayed get_image_numpy() equals the flipped embed, action_show_original enabled, then canvas.show_original(True) displays pristine pixels and show_original(False) restores the embed.
   - test_show_original_dims_mismatch_falls_back_to_embedded: save with current_image cropped (pristine[:, :40].copy()) against a square pristine file; reopen; assert baseline EQUALS the cropped embed (disk ref rejected by dims guard), no exception during open/navigation, action stays enabled.
   - test_page_switch_seeds_baseline_per_page_no_bleed: two-page chapter (_make_pages gives distinct per-page colors); give each page its own distinct edited current_image before saving; reopen; navigate to page 2 through the REAL seam (window2.file_table.select_path(page_02_path) + QApplication.processEvents, mirroring sibling navigation tests); assert canvas._original_image_numpy equals page 2's pristine decode, _showing_original False, and displayed image equals page 2's edit — locks the cross-page bleed fix end-to-end.
   Keep assertions pixel-exact on flipud/crop arrays (deterministic); reuse QApplication.processEvents() cadence from neighbors.
  </action>
  <verify>
    <automated>"C:/Users/Stella/.pyenv/pyenv-win/versions/3.14.2/python.exe" -m pytest tests/test_gui_project.py tests/test_gui_canvas.py -q && "C:/Users/Stella/.pyenv/pyenv-win/versions/3.14.2/python.exe" -m pytest -q</automated>
  </verify>
  <done>The three new GUI regressions pass; test_show_original_gating and every other pre-existing test still pass (full suite green under the pinned interpreter — prior baseline 1161 passed plus the new tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mas container → decoder | A crafted original.json path already passed verify_original (resolve + suffix + sha256) before any decode |
| disk file → numpy baseline | The referenced original file content becomes canvas pixel data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-u9m-01 | Tampering | original.json path → PIL decode | low | accept | Path already cleared verify_original (resolve + image-suffix + sha256 match) at load time; helper adds OSError/ValueError/DecompressionBombError guards mirroring the embedded-blob decode (T-05-03 lineage) |
| T-u9m-02 | Denial of Service | decompression bomb via original file | medium | mitigate | DecompressionBombError caught -> None fallback (same catch family as _build_image_file_from_parsed line ~2862); decode is per displayed page, not eager for all pages |
| T-u9m-03 | Information Disclosure | decoded original kept in memory | low | accept | Single transient ndarray on canvas per session page — same lifetime class as the existing _original_image_numpy cache |
| T-u9m-SC | Tampering | pip installs | n/a | accept | No package installs in this plan (stdlib + Pillow already vendored dependencies) |

This is a desktop app operating on user-owned files; no network boundary is crossed. Dispositions align with the project's established ASVS posture (validated input contracts + fail-graceful fallbacks).
</threat_model>

<verification>
1. `"C:/Users/Stella/.pyenv/pyenv-win/versions/3.14.2/python.exe" -m pytest tests/test_gui_canvas.py -q` green.
2. `"C:/Users/Stella/.pyenv/pyenv-win/versions/3.14.2/python.exe" -m pytest tests/test_gui_project.py -q` green (includes pre-existing test_show_original_gating, project-load, plane-restoration suites).
3. Full suite `"C:/Users/Stella/.pyenv/pyenv-win/versions/3.14.2/python.exe" -m pytest -q` green under the pinned interpreter (strict superset of the 1161-passing baseline).
4. Static sanity: the ONLY canvas call sites switched to set_image_from_numpy_page are main_window.py:~2087 and ~2903; :1486 and :5958 still call set_image_from_numpy.
</verification>

<success_criteria>
- Reopen a saved project whose originals verify: P displays the pristine on-disk original; P-off restores the saved state (test-proven).
- Unverified / unreadable / dims-mismatched pages behave exactly as today (fallback baseline, "original file not found" tooltip intact).
- Page switches never leak a previous page's baseline or stale toggle flag (unit + GUI proven).
- D-14 rebaseline-after-op semantics, Levels-preview suppression, and CR-13 composite-base rules byte-for-byte preserved (existing tests untouched and passing).
- Executor commits code+tests atomically per task; PLAN/SUMMARY/STATE docs are committed by the orchestrator.
</success_criteria>

<output>
Create `.planning/quick/260826-u9m-fix-show-original-p-key-to-reference-ori/260826-u9m-SUMMARY.md` when done
</output>
