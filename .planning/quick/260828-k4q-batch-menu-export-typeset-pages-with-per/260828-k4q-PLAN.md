---
phase: quick-260828-k4q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/page_select_dialog.py
  - manga_ai_studio/core/ocr_export.py
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_page_select_dialog.py
  - tests/test_core/test_ocr_export.py
  - tests/test_gui_export.py
autonomous: true
requirements: [QUICK-BATCH-TYPESET-EXPORT]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 62000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "The Batch menu carries an 'Export Typeset Pages…' entry, gated exactly like Batch Export OCR JSON: enabled iff a folder is open AND no async op is running AND no batch is running"
    - "Triggering it opens a MODAL page-selection dialog: one checkable row per page in file order, ALL CHECKED by default; the user may uncheck arbitrary rows"
    - "Export writes ONLY the checked pages; Cancel (or Esc) writes nothing; the dialog is a pure collector (no canvas/model mutation)"
    - "No per-page Save As dialog: each exported page is written to its default_typeset_path sidecar — {stem}_typeset.png beside a pristine source, <source>/cleaned/ when geometry_altered"
    - "Baked output matches the single-page Ctrl+Shift+B pipeline: same bake_typeset_page compositor + save_image_optimized writer; the CURRENT page reflects the live canvas because the flush seam (_snapshot_current_page + _flush_current_canvas_mask_to_data_model) runs before projection"
    - "The batch runs on ONE Worker off the GUI thread with the Phase 2 batch surface: determinate progress bar, {done}/{total} status, per-page failure isolation, Cancel Batch + Esc support, and completion copy 'Exported typeset pages for N page(s).' plus 'M page(s) failed — see the log.' when failures occur"
  artifacts:
    - manga_ai_studio/gui/page_select_dialog.py (PageSelectionDialog pure collector: checkable QListWidget, Select All/None, Export/Cancel, selected_indices())
    - manga_ai_studio/core/ocr_export.py (TypesetPage dataclass + batch_export_typeset loop, Abort/Qt lazy imports, {"ok","failed","total"} contract)
    - manga_ai_studio/gui/main_window.py (action_batch_export_typeset in batch_menu, _dispatch_batch_typeset_export + 4 handler mirrors, _refresh_action_states gating, _batch_typeset_total)
    - tests/test_gui_page_select_dialog.py (dialog defaults, subset selection, empty-selection Export disable, Select All/None)
    - tests/test_core/test_ocr_export.py (batch_export_typeset: real-bake writes, None-image failure, save-conflict failure, progress shape, abort)
    - tests/test_gui_export.py (batch typeset end-to-end, flush-seam parity, mixed failure copy, cancel, gating)
  key_links:
    - "main_window._dispatch_batch_typeset_export -> PageSelectionDialog.selected_indices() -> .copy()-detached pixel projection (current page = canvas.get_image_numpy, others = _page_image_source) -> TypesetPage(dest=default_typeset_path(path, geometry_altered)) -> partial(batch_export_typeset, items) on ONE Worker with abort_signal=batch_abort_requested"
    - "worker signals progress/result/error/aborted/finished -> the 4 _on_batch_typeset_* handlers -> _op_running/_batch_active/_batch_typeset_total state -> _refresh_action_states re-enablement (finished ALWAYS fires — Pitfall 7)"
---

<objective>
Add batch typeset export: a Batch-menu "Export Typeset Pages…" entry that opens a modal page-selection popup (one checkable row per page, ALL CHECKED by default, user may uncheck), then bakes + writes only the checked pages with no per-page Save As dialogs. Today the only way to export a typeset page is Ctrl+Shift+B one page at a time.

Purpose: a scanlator finishing a chapter needs all (or a subset of) pages exported in one action instead of N manual Ctrl+Shift+B + Save As rounds.
Output: new `PageSelectionDialog` (pure collector), `batch_export_typeset` core loop (mirrors `batch_export_ocr`'s abort/progress/failure contract), MainWindow dispatch + handlers + gating mirroring `_dispatch_batch_ocr_export`, and tests at all three layers. Full pinned suite green.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/core/ocr_export.py
@manga_ai_studio/gui/text_renderer.py
@manga_ai_studio/gui/worker_thread.py

Anchor facts (verified):
- Single-page export `_on_export_typeset` (main_window.py:7197-7260): `_op_running` gate -> `_snapshot_current_page()` flush -> `canvas.get_image_numpy()` (detached) -> `bake_typeset_page(image_np, imf.boxes or [])` -> Save As defaulting to `default_typeset_path(page_path, imf.geometry_altered)` -> `save_image_optimized(baked, path, original=page_path)` -> T-05-12 failure dialog / transient success.
- Batch template `_dispatch_batch_ocr_export` (main_window.py:7262-7349) + handlers (7351-7420): gates `_op_running or _batch_active` -> flush seam (`_snapshot_current_page()` + `_flush_current_canvas_mask_to_data_model()`) -> per-page projection -> `Worker(partial(...), abort_signal=self.batch_abort_requested)` -> progress/result/error/aborted/finished handlers -> `_op_running/_batch_active/_batch_*_total/_batch_cancelled` + file_table disable + determinate bar + status "Exporting OCR JSON… 0/N".
- Gating to mirror (main_window.py:1419-1421): `self.action_batch_export_ocr.setEnabled(folder_open and not self._op_running and not self._batch_active)`.
- `_page_image_source(idx)` (main_window.py:2546-2568): `imf.current_image` -> `cleaned/<name>` -> source path -> None. NOTE: the `current_image` branch returns the LIVE array without `.copy()` despite the docstring — the dispatch MUST apply `.copy()` itself at handoff (T-QHH-01 immutable-payload rule, quick-260826-vhh precedent).
- `bake_typeset_page(page_np, boxes)` (text_renderer.py:1332-1370): paints EXCLUSIVELY on a detached QImage copy (the one Qt paint device legal off the GUI thread) and returns a detached numpy copy — worker-safe by construction; renders each box's translation-else-recognized text through the same layout/paint as the canvas overlay (D-01 canvas ≡ bake).
- `batch_export_ocr` (ocr_export.py:326-394): abort check at loop TOP ONLY raising `Abort` (lazy-imported from gui.worker_thread INSIDE the function body — keeps the module-top import graph Qt-free while preserving the exact exception identity the Worker catches); progress `(percent, page_name)` emitted once per page at loop top; per-page `except Exception` isolation, stems + error strings only (T-05-05); returns `{"ok", "failed", "total"}`.
- `default_typeset_path(page_path, geometry_altered)` (ocr_export.py:244-262): pristine -> `{stem}_typeset.png` beside source; altered -> `cleaned/{stem}_typeset.png`; `save_image_optimized` mkdirs the parent and picks format by suffix (.png here).
- Dialog conventions: ResizeDialog (resize_dialog.py:77-158) is the shape — pure collector storing `result_values` (NEVER `result`; shadows QDialog.result()), 14px base QFont, `setModal(True)`, per-file `_DIALOG_QSS` stylesheet block (every dialog file carries its own copy).
- Menu block to extend: batch_menu actions at main_window.py:598-628 (action_batch_export_ocr is the last entry).
- Test conventions: tests/test_gui_export.py has `_make_window` / `_write_pages` / `_load_folder_session` / `_seed_box_on_canvas` helpers and batch OCR tests (test_batch_export_writes_all_pages :410, mixed :452, cancel :481 — blocking-real-write until abort flips, gating/progress :532) to mirror; failures injected by conflicting filesystem state, NOT by faking the loop.
- Interpreter: pinned per AGENTS.md — bare `python`/`pytest` resolve to an unrelated venv; always use `"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: PageSelectionDialog — modal checkable page list (pure collector)</name>
  <files>manga_ai_studio/gui/page_select_dialog.py, tests/test_gui_page_select_dialog.py</files>
  <action>
Create `manga_ai_studio/gui/page_select_dialog.py` with `PageSelectionDialog(QDialog)`, modeled on ResizeDialog's pure-collector shape (resize_dialog.py:77-158):

- `__init__(self, parent=None, page_names: list[str] = ())`. `setWindowTitle("Export Typeset Pages")`, `setModal(True)`, 14px base QFont (`QFont().setPixelSize(14)`, children inherit — the D-12 precedent), `setObjectName("page_select_dialog")`, and a per-file `_DIALOG_QSS` block copied from resize_dialog.py's (per-file QSS is the established convention — crop/curves/load_translations/resize each carry their own).
- One checkable `QListWidget` row per entry of `page_names`, in order; row index == the caller's page index (MainWindow passes names in `image_files` order, which is already natural-sorted — decision 01-02). Every item starts `Qt.CheckState.Checked` with `Qt.ItemFlag.ItemIsUserCheckable | ItemIsEnabled | ItemIsSelectable` (user contract: ALL CHECKED by default).
- A word-wrapped helper label disclosing the output contract (the "no Save As" disclosure): "Each page is written as {stem}_typeset.png beside its source page (pages with altered geometry go to cleaned/). Existing files are overwritten."
- A "Select All" / "Select None" QPushButton pair (small convenience for the deselect-some flow).
- A `QDialogButtonBox`: Export as an AcceptRole button made `setDefault(True)`, Cancel as RejectRole. Export's enabled state tracks checked-count > 0 (recompute on the list's `itemChanged`; blockSignals while bulk-checking in Select All/None so one recompute suffices).
- `selected_indices(self) -> list[int]`: ascending indices of checked rows — the ONLY result surface. Store nothing in a field named `result` (shadows QDialog.result(); the `result_values` convention applies to constructor-carried state only — here `selected_indices()` is the accessor, matching CropDialog's method-style reads).

Tests in `tests/test_gui_page_select_dialog.py` (module `pytest.importorskip("PySide6")`, `@pytest.mark.gui`, qtbot.addWidget; drive methods directly — never `exec()`):
1. All rows checked by default; `selected_indices()` == [0..n-1] for 3 names.
2. Unchecking row 1 (setChecked(False) on the item) -> `selected_indices()` == [0, 2].
3. Select None -> `selected_indices()` == [] AND the Export button is disabled; Select All -> all checked + Export enabled again.
4. Single-page list and empty-list construction do not raise (empty list leaves Export disabled).
5. Modality is ApplicationModal and the dialog never touches MainWindow state (pure collector — construct with parent=None).
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_page_select_dialog.py -x -q</automated>
  </verify>
  <done>PageSelectionDialog lists pages all-checked, reports checked row indices via selected_indices(), disables Export on empty selection, and is a modal pure collector; its test file passes under the pinned interpreter.</done>
</task>

<task type="auto">
  <name>Task 2: batch_export_typeset core loop + TypesetPage projection</name>
  <files>manga_ai_studio/core/ocr_export.py, tests/test_core/test_ocr_export.py</files>
  <action>
In `manga_ai_studio/core/ocr_export.py`, add (below `batch_export_ocr`, same file — the batch contract lives together):

- `@dataclass class TypesetPage:` with `path: Path` (the source page — used as `original=` for DPI/mode preservation), `image` (annotated as the detached `(H, W, 3)` uint8 RGB array or None; do NOT import numpy — the module keeps its pure-stdlib module-top graph, string annotation via `from __future__ import annotations`), `boxes: list` (live PageBox list — the 05-08 precedent passes them read-only across the thread boundary), `dest: Path` (the pre-computed `default_typeset_path` sidecar).
- `def batch_export_typeset(items: list, progress_callback=None, abort_flag=None) -> dict:` — a byte-for-byte structural mirror of `batch_export_ocr`'s contract (ocr_export.py:326-394): `progress_callback`/`abort_flag` stay the LAST two kwargs with None defaults (Worker auto-injection, worker_thread.py:103-140). Lazy-import INSIDE the function body, never at module top: `Abort` from `manga_ai_studio.gui.worker_thread` (exact exception identity — a locally-defined subclass would surface cancel as an error), `bake_typeset_page` from `manga_ai_studio.gui.text_renderer`, `save_image_optimized` from `manga_ai_studio.core.image_io`.
- Loop per item: (1) abort check at loop TOP ONLY — flag set -> `raise Abort()` (a page already started always finishes writing; no half-written sidecar); (2) emit `(percent, name)` once per page at loop top — the D-10 shape, percent = int(i / total * 100); (3) per-page `try`: if `page.image is None` raise ValueError("no image source") so it lands in `failed` honestly (a checked page with no pixels is a reported failure, never a silent skip); else `baked = bake_typeset_page(page.image, page.boxes)` then `save_image_optimized(baked, page.dest, original=page.path)`; (4) `except Exception` -> loguru error with stem + error string ONLY (T-05-05 log-injection discipline), append `(page.path, str(exc))`, continue; (5) return `{"ok": total - len(failed), "failed": failed, "total": total}`.
- Bake is off-thread-safe because bake_typeset_page paints exclusively on its own detached QImage (no QPixmap, no live-object reads); note that one-line rationale in the docstring.

Tests appended to `tests/test_core/test_ocr_export.py` (real loop called directly — no Worker; add a module-level `pytest.importorskip("PySide6")` guard if the file lacks one, since bake needs Qt):
1. Writes all pages: 3 items (real bake; boxes may be []) -> 3 `{stem}_typeset.png` files exist at the `default_typeset_path` locations for their geometry_altered flags; result dict `{"ok": 3, "failed": [], "total": 3}`.
2. None-image page -> `failed` carries (path, message) and the other two pages still write.
3. Save-conflict failure: pre-create one `dest` as a DIRECTORY -> OSError lands in `failed`; ok counts the rest (mirrors the "conflicting filesystem state" injection discipline).
4. Progress shape: a collector callback receives [(0, name1), (50, name2), (100, name3)] for 3 items.
5. Abort: a `SharableFlag(True)` (from gui.worker_thread) passed as abort_flag raises the EXACT `worker_thread.Abort` before any file is written (assert no sidecars exist).
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_ocr_export.py -x -q</automated>
  </verify>
  <done>batch_export_typeset bakes + writes TypesetPage items to their dest sidecars, isolates per-page failures (including None-image and OSError), emits the D-10 progress shape, raises worker_thread.Abort on a set flag, and returns {"ok","failed","total"}; the ocr_export module-top import graph stays Qt/numpy-free.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Batch menu entry + dispatch flow + progress surface + tests</name>
  <files>manga_ai_studio/gui/main_window.py, tests/test_gui_export.py</files>
  <action>
Main window wiring (main_window.py), mirroring the batch-OCR flow as the template:

1. Menu (after the action_batch_export_ocr block, ~line 628): `self.action_batch_export_typeset = QAction("Export Typeset Pages\u2026", self)` — the ellipsis is REQUIRED (opens a dialog, unlike the no-ellipsis immediate-start batch actions per UI-SPEC copywriting). setStatusTip "Export typeset pages for selected pages of the open folder ({stem}_typeset.png sidecars)." `triggered.connect(self._dispatch_batch_typeset_export)`; `setEnabled(False)`; `self.batch_menu.addAction(...)` after the OCR batch entry.
2. Gating in `_refresh_action_states`, directly beside the action_batch_export_ocr gate (~line 1419): `self.action_batch_export_typeset.setEnabled(folder_open and not self._op_running and not self._batch_active)` — identical conditions (the user's one locked ask: same availability as the other batch export).
3. State: `self._batch_typeset_total = 0` initialized next to `_batch_ocr_total`; reset in the dispatch + cleanup.
4. `_dispatch_batch_typeset_export(self) -> None`:
   a. Gates: `if self._op_running or self._batch_active: return`; `if not self.image_files: return`.
   b. Dialog FIRST (names-only, so no state churn on cancel): `names = [imf.path.name for imf in self.image_files]`; `dialog = PageSelectionDialog(self, names)`; `if dialog.exec() != QDialog.DialogCode.Accepted: return`; `indices = dialog.selected_indices()`; `if not indices: return` (defensive — Export is disabled on empty).
   c. THEN the Pitfall-7 flush seam, exactly as the OCR dispatch: `self._snapshot_current_page()` + `self._flush_current_canvas_mask_to_data_model()`.
   d. Project ONLY checked pages to `TypesetPage`: current index (via `self._current_page_index()`) -> `img_np = self.canvas.get_image_numpy()`; `if img_np is None: return` (mirror OCR); others -> `self._page_image_source(i)` (None allowed). EVERY image gets an explicit `.copy()` at handoff — `_page_image_source`'s current_image branch returns the LIVE array (Pitfall 2 / T-QHH-01: the worker must never alias live GUI state). `boxes = imf.boxes if imf.boxes is not None else []`; `dest = default_typeset_path(imf.path, imf.geometry_altered)`.
   e. Dispatch: `task_fn = partial(batch_export_typeset, items)`; `worker = Worker(task_fn, abort_signal=self.batch_abort_requested)`; connect `signals.progress/result/error/aborted/finished` to the four NEW handlers below (aborted AND finished both to cleanup — Pitfall 7); `worker.setAutoDelete(True)`; set `_op_running = True`, `_batch_active = True`, `_batch_typeset_total = len(items)`, `_batch_cancelled = False`; `self.file_table.setEnabled(False)`; `self._refresh_action_states()`; `self.error_chip.hide()`; determinate bar 0..100 shown at 0; status `f"Exporting typeset pages\u2026 0/{len(items)}"`; `QThreadPool.globalInstance().start(worker)`. (Import `PageSelectionDialog` at module top beside LoadTranslationsDialog; `QDialog` is already imported.)
5. Four handlers, structural mirrors of the OCR four (7351-7420): `_on_batch_typeset_progress` (same percent->done derivation against `_batch_typeset_total`, copy "Exporting typeset pages… {done}/{total} — {name}"); `_on_batch_typeset_finished` (clean: `f"Exported typeset pages for {ok} page(s)."`; mixed: append `f" {len(failed)} page(s) failed \u2014 see the log."`; failures already logged by the loop); `_on_batch_typeset_error` (loguru + error chip "Batch export error" + "Batch export failed"); `_on_batch_typeset_cleanup` (unconditionally clear `_op_running/_batch_active/_batch_typeset_total`, re-enable file_table, hide bar, `_refresh_action_states()`, render "Cancelled" on the cancelled path).

Tests appended to `tests/test_gui_export.py` (reuse `_load_folder_session` / `_seed_box_on_canvas`; real Worker + real loop, stub only the modal):
- Stub pattern: monkeypatch `manga_ai_studio.gui.main_window.PageSelectionDialog` with a fake whose `exec()` returns `QDialog.DialogCode.Accepted` and `selected_indices()` returns a fixed list (mirror the QFileDialog stubbing discipline).
1. `test_batch_typeset_writes_checked_pages`: 3 pages, indices [0, 2] -> page_01_typeset.png + page_03_typeset.png exist beside the source, page_02_typeset.png does NOT; success copy flashed.
2. `test_batch_typeset_default_targets`: one pristine + one geometry-altered page (set `imf.geometry_altered = True`) -> altered page's sidecar lands in `cleaned/`, pristine's beside the source (the D-22 mirror).
3. `test_batch_typeset_flushes_canvas_boxes`: `_seed_box_on_canvas` on page 1, all pages exported -> page_01_typeset.png differs from its plain source pixels (text baked — flush seam + D-01 parity), page_02/03 sidecars equal their plain sources.
4. `test_batch_typeset_mixed_failure_count`: pre-create `page_01_typeset.png` as a directory -> completion copy carries "1 page(s) failed", ok == 2.
5. `test_batch_typeset_cancel`: wrap the REAL `bake_typeset_page` (monkeypatch the `gui.text_renderer` attribute — the loop resolves it lazily per call) with an Event-wait, trigger the action, then `window.action_cancel_batch.trigger()` / emit `batch_abort_requested`; join via `qtbot.waitUntil` on `not window._batch_active`; assert "Cancelled" status + editor re-enabled (mirror test_batch_export_cancel).
6. `test_batch_typeset_gating`: action enabled iff action_batch_export_ocr is enabled (folder open; disabled with no pages); while the blocked batch holds, `action_batch_typeset` isEnabled() is False (mirror test_batch_export_gating_and_progress).
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_export.py tests/test_gui_page_select_dialog.py tests/test_core/test_ocr_export.py -x -q</automated>
  </verify>
  <done>Batch menu shows "Export Typeset Pages…"; accepting the dialog batches bake+write for exactly the checked pages to their default sidecars off the GUI thread with progress/cancel/failure-count surface; the action's gating matches action_batch_export_ocr; all new + existing test_gui_export tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI thread -> Worker (QThreadPool) | Page pixels, live PageBox list, and destination Paths cross into a non-GUI thread |
| Batch loop -> filesystem | Loop writes PNG sidecars next to user source files and into cleaned/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-K4Q-01 | Tampering | Sidecar writes (`save_image_optimized` to `default_typeset_path`) | low | accept | Overwriting an existing `{stem}_typeset.png` is the established D-03 single-page behavior; disclosed in the dialog helper text — no new surface |
| T-K4Q-02 | Information disclosure | loguru per-page failure logs | low | mitigate | Stems + error strings ONLY (T-05-05 discipline carried into batch_export_typeset; no OCR/translation text logged) |
| T-K4Q-03 | Denial of service | Large batch hogging one Worker | low | accept | Determinate progress + Cancel Batch/Esc (abort at loop top); decode+bake is bounded per page |
| T-K4Q-04 | Tampering | Cross-thread aliasing of live canvas/ImageFile arrays | medium | mitigate | Dispatch applies `.copy()` to EVERY image at handoff (Pitfall 2 / T-QHH-01); bake_typeset_page paints only its own detached QImage copy |
</threat_model>

<verification>
- `"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_export.py tests/test_gui_page_select_dialog.py tests/test_core/test_ocr_export.py tests/test_core/test_typeset_bake.py -x -q` — new tests + the existing bake/export suites green.
- Full suite regression: `"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` (baseline 1159 passed; known pre-existing full-run box-test flakes per STATE.md — judge failures against that note).
- Module-top purity: `python -c "import manga_ai_studio.core.ocr_export"` still succeeds with no PySide6 import at module top (grep the file — no module-top `PySide6`/`text_renderer`/`numpy` imports).
</verification>

<success_criteria>
- Batch menu entry "Export Typeset Pages…" opens the modal all-checked page picker; Export writes exactly the checked pages as `{stem}_typeset.png` sidecars (pristine beside source, altered in cleaned/) with zero Save As dialogs; Cancel/Esc writes nothing.
- Current-page canvas edits (boxes/text) are reflected in the exported current page (flush seam before projection).
- Worker-driven batch with determinate progress, per-page failure isolation, failure-count completion copy, and functional Cancel; `_refresh_action_states` gates the new action identically to Batch Export OCR JSON.
- All three test layers pass under the pinned interpreter.
</success_criteria>

<output>
Create `.planning/quick/260828-k4q-batch-menu-export-typeset-pages-with-per/260828-k4q-SUMMARY.md` when done. Commit CODE + TESTS only (the orchestrator commits planning docs).
</output>
