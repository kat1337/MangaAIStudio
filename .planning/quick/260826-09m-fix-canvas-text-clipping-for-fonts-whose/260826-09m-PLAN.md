---
phase: quick-260826-09m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/box_item.py
  - tests/test_gui_sfx_editing.py
autonomous: true
requirements: [QUICK-CANVAS-CLIP]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Canvas overlay pixmaps never clip glyph paint for ANY font: the cached alpha channel's bounding box has >=1 transparent pixel of margin on every side (non-empty text)"
    - "Absolute canvas placement stays identical to what bake_typeset_page paints (D-01) at rotation 0 AND at non-zero rotation — placement offsets derive from the MEASURED box, keeping the composite pixel-equivalent within the established AA envelopes"
    - "Rotation-0 behavior changes ONLY by no-longer-clipping: _ink_offset math differs from the legacy formula by the measured-vs-analytic delta alone; _render_offset/_baked_rotation contracts unchanged"
    - "Empty text still clears the cache (pixmap None, offsets reset)"
    - "RC-1 unaffected: refresh_position stays setPos-only; set_content still runs only on text/style/box-size changes"
  artifacts:
    - manga_ai_studio/gui/box_item.py (TypesetOverlayItem.set_content + _render_rotated use the two-pass measured render)
    - tests/test_gui_sfx_editing.py (Mango regression + generic margin guarantee + placement equivalence at 0/40 deg + empty-text guard)
  key_links:
    - "Scratch translate -> renderer_paint: the cancel-translate keeps its exact form, only shifted by the safety margin S"
    - "Measured bbox -> _ink_offset / _render_offset: the crop rectangle IS the new offset source"
    - "_qimage_argb_to_numpy (text_renderer.py) -> alpha-channel measurement"
---

<objective>
Fix straight-edge canvas text clipping for fonts whose glyphs paint outside the QTextDocument-derived ink rect (e.g. Mango): replace BOTH analytic surface-size derivations in TypesetOverlayItem (unrotated path in set_content and _render_rotated) with a TWO-PASS MEASURED render — pass 1 paints into an oversized scratch surface exactly as today's transform math dictates, pass 2 crops the cached pixmap to the TRUE painted bounding box measured from the scratch alpha channel and derives the placement offsets from that measured box.

Purpose: The bake never clips (full-page QImage); the canvas sized its cache analytically from `result.ink + effect_padding`, and most fonts overshoot that rect (probe: ~10px above ink.top() for Mango AND Arial at 100px; tall display fonts visibly so). Anything above the surface is chopped — the reported straight top-edge cut. Rotation re-derived the surface through a different analytic mapping, revealing different subsets of the same missing pixels.

Output: A single production change in TypesetOverlayItem + regression tests in tests/test_gui_sfx_editing.py; full suite green under the pinned interpreter.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@manga_ai_studio/gui/box_item.py
  # TypesetOverlayItem: __init__ (~518-537), boundingRect (~540-560), set_content unrotated path (~591-663), _render_rotated (~665-744), refresh_position (~746-761)
@manga_ai_studio/gui/text_renderer.py
  # layout/paint/effect_padding signatures; _qimage_argb_to_numpy (~1104-1115); bake_typeset_page (~1165-1203)
@tests/test_gui_sfx_editing.py
  # helpers (_scene_with_box ~57, _canvas_with_image ~64); test_rotation_rendered_overlay_matches_bake_pixels (~286-348) — THE placement-equivalence template incl. the double-AA envelope rationale; test_zero_rotation_keeps_legacy_overlay_path (~352)
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-pass measured render in TypesetOverlayItem (both paths)</name>
  <files>manga_ai_studio/gui/box_item.py</files>
  <read_first>manga_ai_studio/gui/box_item.py lines 498-761; manga_ai_studio/gui/text_renderer.py lines 173-250 (effect_padding, LayoutResult fields incl. whatever carries the effective font px) and 1104-1115 (_qimage_argb_to_numpy)</read_first>
  <action>
Replace the ANALYTIC surface derivation in BOTH render paths of TypesetOverlayItem with a shared two-pass measured scheme. Do NOT touch renderer_layout/renderer_paint, refresh_position's signature, boundingRect, preview_rotation/clear_preview_rotation, or the _baked_rotation contract. RC-1 holds: nothing here runs per-mousemove.

Safety margin: define a module-level helper `_measure_margin(style_or_px)` returning S = max(64, ceil(effective font px)) — read the effective font pixel size from the LayoutResult/style field used by _style_font (font_size_px, or the auto-fit-resolved size the LayoutResult carries — check LayoutResult's actual fields first). S must comfortably exceed any metric overshoot AND, combined with the existing `pad`, the full effect padding.

UNROTATED PATH (set_content, lines ~632-663): keep the exact same cancel-translate formula but shift it by S: build a scratch QImage of (w + 2S, h + 2S) Format_ARGB32_Premultiplied filled transparent, then painter.translate(-result.origin.x() - ink.left() + pad + S, -result.origin.y() - ink.top() + pad + S) before renderer_paint. Pass 2: convert the scratch via tr._qimage_argb_to_numpy (import from gui.text_renderer — reuse, do NOT hand-roll an alpha scan), take alpha > 0, np.nonzero for min/max row/col. Defensive retry: if the measured bbox touches ANY scratch border, redo pass 1 once with S doubled (should never trigger with adequate S — log nothing, just do it). Crop with scratch.copy(bx, by, bw, bh) (QRect overload — returns a detached image per Pitfall 2 discipline), QPixmap.fromImage it. Derive _ink_offset from the measured box: a scratch pixel (bx, by) equals old-surface pixel (bx - S, by - S), and the old surface origin sat at box-relative (ink.left() - pad, ink.top() - pad), so _ink_offset = QPointF(ink.left() - pad + bx - S, ink.top() - pad + by - S + dy) with dy unchanged (result.origin.y() - box_rect.y() - _OVERLAY_INSET). Keep _render_offset = None and _baked_rotation = 0.0 exactly as today. Update the class/set_content docstrings: the pixmap now covers the MEASURED painted extent; note that set_content performs up to two renders per content change (acceptable — it runs only on text/style/box-size changes, never per-mousemove).

ROTATED PATH (_render_rotated, lines ~692-744): keep the corner-mapping through _g UNCHANGED to get the analytic (left, top, w, h), then inflate: left' = left - S, top' = top - S, w' = w + 2S, h' = h + 2S for the scratch. Translate by (t0x - left', t0y - top') (same formula, inflated anchor). Measure the scratch alpha identically (shared private helper `_measured_bbox(qimg) -> tuple[int,int,int,int] | None`, None when fully empty — fall back to the full scratch if None). Retry-once-with-doubled-S on border contact. Crop the measured sub-region; self._pixmap from it. _render_offset = QPointF(left' + bx - t0x - box_rect.x(), top' + by - t0y - box_rect.y()) — the measured box replaces the analytic min-corner in the existing P = min_g - t0 expression. _baked_rotation assignment stays in set_content, untouched.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_sfx_editing.py tests/test_gui_boxes.py -q</automated>
  </verify>
  <done>Existing overlay/rotation/bake-equivalence suites pass unchanged against the new measured-render implementation (the D-01 pixel-parity tests are the load-bearing proof placement did not move beyond the AA envelopes).</done>
</task>

<task type="auto">
  <name>Task 2: Regression tests — Mango no-clip, generic margin guarantee, placement equivalence, empty-text</name>
  <files>tests/test_gui_sfx_editing.py</files>
  <read_first>tests/test_gui_sfx_editing.py lines 1-130 (helpers/conventions) and 285-365 (equivalence-test template + envelope rationale)</read_first>
  <action>
Append a new section to tests/test_gui_sfx_editing.py following the file's conventions (pytest.importorskip("PySide6") header already present; @pytest.mark.gui; reuse _scene_with_box). Add a module-level helper `_mango_style()` that loads C:\Users\Stella\AppData\Local\Microsoft\Windows\Fonts\Mango.otf via QFontDatabase.addApplicationFont (returns the loaded font ID's first applicationFontFamilies entry + builds TextStyle(font_family=<family>, font_size_px=100.0, auto_fit=False)); mark Mango-specific tests with @pytest.mark.skipif(not Path(MANGO_PATH).is_file(), ...) so they skip cleanly on machines without the font.

Test 1 (Mango regression, skipif): set_content('M', mango style, box_rect) → assert NO pixel with alpha > 0 lies on the pixmap's border rows/columns (the old analytic surface clipped these; the new one must have margin) AND pixmap height is strictly greater than ceil(result.ink.height() + 2 * effect_padding(style)) using item.layout_result.

Test 2 (generic guarantee, any font): with a plain TextStyle(auto_fit=False, font_size_px=24) and non-empty text, after set_content assert the alpha bounding box does NOT touch any pixmap border (>=1px transparent margin on all four sides). Use a default available family (TextStyle's default).

Test 3 (placement equivalence, D-01 spot-check): parameterize over angles [0.0, 40.0]. Mirror test_rotation_rendered_overlay_matches_bake_pixels: bake_typeset_page over a colored page vs compositing overlay.pixmap() at overlay.pos() onto numpy_to_qimage(page) — compare changed-pixel percentile/max deltas with the SAME envelope shape as the existing test (angle 0 should be near-exact; 40 deg admits the double-AA phase noise). This proves the measured-vs-analytic offset swap kept absolute placement identical.

Test 4 (empty-text path): after a valid set_content, call set_content("") → assert pixmap() is None, layout_result is None, _render_offset is None, _baked_rotation == 0.0.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_sfx_editing.py -q --no-header && & "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <done>New tests pass (Mango tests exercise the real user-installed font on this machine); full suite green under the pinned interpreter (baseline 1138 passed + new tests, strict superset).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced — pure rendering-path fix inside the local Qt GUI; no new inputs, services, or packages.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q09M-01 | DoS | TypesetOverlayItem.set_content scratch allocation | low | mitigate | Margin S bounded (max(64, ceil(font_px))) + single retry cap; font px already capped at 1024 upstream (quick-260825-wfy), bounding worst-case scratch size |
| T-Q09M-02 | Tampering | npm/pip installs | low | accept | No new dependencies — reuses numpy/PySide6 already vendored |
</threat_model>

<verification>
- `& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q` — full suite green
- The Mango regression test actually RUNS on this machine (not skipped): confirm via `-rs` output absence of skip for it
- Existing D-01 equivalence tests unchanged and passing
</verification>

<success_criteria>
- Canvas overlay pixmaps contain the full glyph paint for overshooting fonts (Mango 'M' at 100px shows >=1px transparent margin on all sides; pixmap taller than the analytic ink+pad estimate)
- Composite-at-position matches bake_typeset_page within the established AA envelopes at 0 and 40 degrees
- Rotation-0 legacy contract preserved (_render_offset None, _baked_rotation 0.0, refresh_position byte-identical formula)
- Empty-text cache-clear behavior intact
</success_criteria>

<output>
Create .planning/quick/260826-09m-fix-canvas-text-clipping-for-fonts-whose/260826-09m-SUMMARY.md when done
</output>
