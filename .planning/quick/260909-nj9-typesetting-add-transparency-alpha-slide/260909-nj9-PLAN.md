---
phase: quick-260909-nj9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/text_style.py
  - manga_ai_studio/gui/inspector_panel.py
  - tests/test_core/test_text_style.py
  - tests/test_core/test_text_renderer.py
  - tests/test_core/test_typeset_bake.py
  - tests/test_core/test_project_io.py
  - tests/test_gui_inspector_styling.py
autonomous: true
requirements: [quick-260909-nj9]

estimate:
  tokens: 35000
  raw_tokens: 25000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The Typesetting Inspector's Color picker offers a transparency (alpha) control: picking a color with reduced opacity stores the alpha-carrying #AARRGGBB spelling on the box's TextStyle.color and the 24x24 swatch visually shows the transparency (checkerboard under the alpha fill)."
    - "Canvas overlay text renders at the chosen opacity live — TypesetOverlayItem and the bake share text_renderer's one fill path (D-01), so no separate canvas wiring exists or is needed."
    - "WYSIWYG export parity: bake_typeset_page composites the same alpha (50% white over a black page reads ~50% grey ink), proven by a pixel-level bake test."
    - "Alpha round-trips .mas save/load verbatim; legacy projects (7-char #RRGGBB, no alpha) load opaque — QColor defaults alpha to 255 — with zero behavioral change."
    - "The WR-01 no-op commit guard is alpha-aware: re-picking the same color in opaque spelling never fires a commit or pushes an undo entry."
    - "Scope decision (documented): alpha applies to the glyph-fill Color row ONLY. The picker is text-specific (QColorDialog exists nowhere else in the app — mask tools paint binary masks with no color concept), so no other consumer needs alpha stripping. Effect colors (outline/glow/shadow) deliberately stay opaque: each effect dict already owns its own opacity field (text_style.py DEFAULT_GLOW/DEFAULT_SHADOW)."
  artifacts:
    - "manga_ai_studio/gui/inspector_panel.py — _pick_style_color gains ShowAlphaButton|DontUseNativeDialog options and emits QColor.name(HexArgb); _emit_style_color_if_changed compares alpha-normalized spellings; _ColorSwatchButton.paintEvent paints a checkerboard under sub-opaque fills; _pick_effect_color and effect swatches unchanged"
    - "manga_ai_studio/core/text_style.py — docstrings only (color field + module header document the #AARRGGBB spelling); zero behavior change (strings already pass through to_dict/from_dict verbatim)"
    - "tests: test_text_style.py (alpha string round-trip pins), test_text_renderer.py (alpha fill brush pin), test_typeset_bake.py (alpha pixel parity), test_project_io.py (ARGB .mas round-trip), test_gui_inspector_styling.py (picker seam + WR-01 + swatch paint)"
  key_links:
    - "_pick_style_color -> HexArgb emit -> MainWindow._on_inspector_style_color_committed (main_window.py:4976) -> _replace_style(color=...) -> TextStyle.color -> _valid_color at text_renderer.py:413 (horizontal) / :1013 (vertical) -> QTextCharFormat foreground QBrush -> paint()/bake_typeset_page (text_renderer.py:1354) — ONE path, canvas == export (D-01)"
    - "project_io.py:289 writes style.to_dict() verbatim into the .mas page JSON and :369 reads TextStyle.from_dict(d.get('style')) — the alpha hex needs no persistence-layer change, only proof"
    - "text_style.py:260 from_dict color coercion is _coerce_str (any string passes; invalid hex falls back at RENDER time via _valid_color) — a crafted color string can never crash a load"
---

<objective>
Add a transparency (alpha) slider to the typesetting color picker so text can be semi-transparent (e.g. 50%-opacity white over art), carried end-to-end: picker -> TextStyle.color -> canvas overlay render -> .mas save/load -> baked/exported pages.

Purpose: first step of the user's "more advanced typesetting system" — establish that color carries alpha before discussing pattern fills and gradients. Deliberately NO pattern/gradient infrastructure, no style-model restructuring.

Output: alpha-capable picker + honest swatch in the Inspector; alpha pinning tests at every layer (model, render, bake, persistence, GUI seams); full suite green.

Scope decision (per the user's "smallest change" constraint): alpha lives in the glyph-fill Color row only; effect rows stay opaque (they already have per-effect `opacity` fields). The picker is text-specific so no non-text consumer exists.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@manga_ai_studio/core/text_style.py
@manga_ai_studio/gui/inspector_panel.py
@manga_ai_studio/gui/text_renderer.py
@tests/test_gui_inspector_styling.py
@tests/test_core/test_typeset_bake.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Alpha flows model -> render -> bake — headless pins (the tracer)</name>
  <files>tests/test_core/test_text_style.py, tests/test_core/test_text_renderer.py, tests/test_core/test_typeset_bake.py, manga_ai_studio/core/text_style.py</files>
  <behavior>
    - TextStyle round-trip: TextStyle(color="#80ff0000").to_dict()["color"] == "#80ff0000" and TextStyle.from_dict({"color": "#80ff0000"}).color == "#80ff0000" — verbatim, no clamping or spelling normalization (text_style.py:199-215 / :254-276 pass strings through _coerce_str untouched).
    - Legacy default: TextStyle.from_dict({"color": "#ff0000"}).color == "#ff0000" (7-char hex preserved as-is; opacity comes from QColor's default alpha 255 at parse time, never from a model rewrite).
    - Default spelling unchanged: TextStyle().to_dict()["color"] == "#000000" (no churn for existing projects).
    - Render fill, horizontal: _build_document("X", style_with_color_#80ff0000, size_px, inner_w) foreground brush color .alpha() == 128 (text_renderer.py:413 `fill = _valid_color(style.color, "#000000")` -> fmt.setForeground(QBrush(fill)) at :421-424).
    - Render fill, vertical: _valid_color("#80ff0000", "#000000").alpha() == 128 — the exact input to the _paint_fill_pass vertical branch (text_renderer.py:1013).
    - Bake pixel parity (the export path — ocr_export.batch_export_typeset calls bake_typeset_page at ocr_export.py:461): bake a black page with a box whose style color is "#80ffffff" (50% white); the brightest composited ink pixel inside the box region reads ~128 grey (range-based assert, e.g. 105..150 — antialiasing/premultiplied rounding tolerance), and the identical style with "#ffffff" reads ~255. Mirror the ink-pixel location pattern already used in tests/test_core/test_typeset_bake.py.
    - Effects interaction (expected behavior, no code change): with glow/shadow enabled under an alpha fill, the silhouette alpha is premultiplied by the fill alpha so the halo dims proportionally — pin with one assertion only if cheap, otherwise note in the test docstring.
  </behavior>
  <action>Write the pins FIRST and run them against the current code. IMPORTANT: this tracer is expected to pass on first run — QColor natively parses #AARRGGBB and TextStyle passes the string through verbatim, so alpha already flows through the one shared render path. If any pin FAILS, the failure IS the defect: fix the underlying path (e.g. a stray `.name()` HexRgb normalization or an unwanted clamp in the color pipeline), never weaken the test. Then update text_style.py docstrings ONLY (zero behavior change): the module header (:1-28) and the `color` field doc (:155-156, :181) — color is "the glyph fill, #RRGGBB or #AARRGGBB (Qt HexArgb spelling; alpha ff = opaque, the legacy default), quick-260909-nj9". Do NOT add hex validation to from_dict — the existing contract (any string accepted, invalid falls back at render time via _valid_color) is deliberate V5 tolerance; a validation clamp here would be new behavior, not a fix.</action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_text_style.py tests/test_core/test_text_renderer.py tests/test_core/test_typeset_bake.py -q</automated>
  </verify>
  <done>All three files green (existing tests + new alpha pins); text_style.py docstrings document the alpha-capable spelling; no production code changed unless a pin exposed a real defect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inspector — alpha-capable picker, honest swatch, alpha-aware WR-01 guard</name>
  <files>manga_ai_studio/gui/inspector_panel.py, tests/test_gui_inspector_styling.py</files>
  <behavior>
    - Picker seam: _pick_style_color (inspector_panel.py:1569-1576) opens QColorDialog with options ShowAlphaButton | DontUseNativeDialog; on accept it emits the HexArgb spelling. Test by monkeypatching PySide6.QtWidgets.QColorDialog.getColor to capture args and return QColor("#80ff0000"), then invoke panel._pick_style_color(cb): assert both option flags present in the captured options, and cb received "#80ff0000".
    - Alpha-aware WR-01: with _loaded_style_color "#ff0000" (legacy spelling loaded from an old project), _commit_style_color("#ffff0000") fires NO callback (same color, opaque spelling — normalization makes the compare apples-to-apples); _commit_style_color("#80ff0000") fires cb with "#80ff0000"; re-committing "#80ff0000" after it is loaded is a no-op.
    - Cancel path unchanged: an invalid QColor from getColor still commits nothing (existing :1575 guard).
    - Swatch honesty: a _ColorSwatchButton with color "#80ff0000" rendered offscreen (QWidget.render into a 24x24 ARGB QImage) shows a blended center pixel — red channel strictly below the opaque value and above the bare checkerboard (range-based, NOT exact pixel math); an opaque "#ff0000" swatch renders red == 255 exactly as today (byte-identical legacy path); the None Mixed split swatch is unchanged.
  </behavior>
  <action>
    In inspector_panel.py:
    (1) _pick_style_color — call QColorDialog.getColor(QColor(seed), self, "Select Font Color", QColorDialog.ColorDialogOption.ShowAlphaButton | QColorDialog.ColorDialogOption.DontUseNativeDialog) and emit color.name(QColor.NameFormat.HexArgb). DontUseNativeDialog is REQUIRED, not cosmetic: the Windows native color dialog has no alpha control, so ShowAlphaButton alone would silently show nothing. The seed QColor already parses #AARRGGBB (line 1571-1574 pass the swatch string straight in), so the dialog's alpha slider opens at the stored alpha and updates the dialog preview live while dragging — that is the "live-updating color preview" the user asked for; the canvas then refreshes on the existing commit-on-pick cadence. Do NOT build a custom picker widget.
    (2) Add a small module-level helper (e.g. _norm_hex_a(color: str) -> str): parse with QColor; on a valid 7- or 9-char hex return color.name(QColor.NameFormat.HexArgb) (the canonical lowercase #aarrggbb spelling, matching today's color.name() casing); on invalid input return the string unchanged. Use it in _emit_style_color_if_changed (inspector_panel.py:1578-1582) to compare BOTH sides normalized and to emit the normalized form — the model converges on one spelling without a load-time rewrite (keeps _loaded_style_color == "#ff0000" expectations at :1013 intact and the existing test at tests/test_gui_inspector_styling.py:557 passing).
    (3) _ColorSwatchButton.paintEvent (inspector_panel.py:246-262): when self.color is a 9-char ARGB hex whose alpha byte < ff, first fill the rect with a 2-tone checkerboard (~4x4 px squares, "#cccccc"/"#8a8a8a" — neutral grey so the hue reads through), then the existing fillRect(QColor(self.color)) composites the transparency over it. Opaque/7-char/None paths keep today's paint exactly.
    (4) Deliberately UNCHANGED (scope decision, per the quick-task constraint): _pick_effect_color (inspector_panel.py:1631-1636) keeps its HexRgb dialog and emit — effect colors stay opaque because each effect dict already owns an opacity field (text_style.py DEFAULT_GLOW/DEFAULT_SHADOW); adding a second transparency control per row would be redundant, and the effect dicts' V5 coercion (:115-139) already clamps those opacities. Add a one-line comment at _pick_effect_color citing this decision so a future reader does not "fix" the asymmetry.
    (5) Update the _ColorSwatchButton docstring (color "hex string" -> "hex string (#RRGGBB or #AARRGGBB)") and the Color-row tooltip (:550-552) to mention transparency.
    Tests go in tests/test_gui_inspector_styling.py following its existing qtbot patterns (direct commit hooks, no modal dialogs).
  </action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_inspector_styling.py -q</automated>
  </verify>
  <done>Picker emits alpha-carrying HexArgb with ShowAlphaButton options; WR-01 guard is alpha-normalized (no spurious undo entries); swatch shows sub-opaque colors over a checkerboard; effect rows untouched; file green.</done>
</task>

<task type="auto">
  <name>Task 3: Persistence round-trip proof + full-suite closeout</name>
  <files>tests/test_core/test_project_io.py</files>
  <behavior>
    - ARGB survives .mas save/load byte-verbatim: mirror test_style_field_round_trip (tests/test_core/test_project_io.py:190) — save a project whose PageBox.style.color == "#80ff0000", reload, assert style.color == "#80ff0000" exactly (project_io.py:289 writes to_dict() verbatim; :369 reads from_dict — no persistence change needed, this test is the proof).
    - Legacy color unchanged: a project saved with style.color "#ff0000" reloads "#ff0000" (7-char spelling NOT rewritten to 9-char at the persistence layer).
  </behavior>
  <action>Add the two round-trip tests to tests/test_core/test_project_io.py mirroring the existing style round-trip test's fixture/Writer structure. No production change — if either fails, a persistence layer is normalizing color strings and that layer gets the fix. Then run the FULL suite: expected baseline 1432 passed (STATE.md, quick-260909-ke1 close) plus the new tests from Tasks 1-3; any failure gets triaged before declaring done (known environmental flakes are re-run in isolation per project precedent).</action>
  <verify>
    <automated>"C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_project_io.py -q && "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <done>ARGB color round-trips .mas verbatim; legacy spelling untouched; full suite green (1432 + new tests, 0 failed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mas file -> TextStyle.from_dict | A crafted project file supplies the color string consumed by QColor at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-nj9-01 | Tampering | TextStyle.from_dict color string from a crafted .mas | low | mitigate | Existing contract already handles it: any string is accepted (_coerce_str) but an invalid QColor falls back to the default at render time (_valid_color, text_renderer.py:357-360) — a malformed alpha hex can never crash a load or render. New tests pin the round-trip; no new validation added (would change V5 tolerance). |
| T-nj9-02 | DoS | Pathological color string length in style dict | low | accept | A multi-MB color string is already possible today (pre-change) and bounded by the .mas container entry caps; QColor parsing rejects it, render falls back. Not made worse by this task. |
| T-nj9-03 | Information (user-visible) | Alpha 00 text renders invisible | low | accept | A fully transparent fill is user intent, not a defect; the Inspector swatch + picker preview make the chosen alpha visible before commit. |
</threat_model>

<verification>
- All Task 1-3 automated verifies green under the pinned interpreter (AGENTS.md: C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe — never bare `python`/`pytest`).
- Grep gate: `grep -n "HexArgb" manga_ai_studio/gui/inspector_panel.py` shows both the emit site and the normalization helper; `grep -n "ShowAlphaButton" manga_ai_studio/gui/inspector_panel.py` shows the picker options.
- Post-execute manual spot-check (goes in SUMMARY + STATE "Needs Review" per project cadence): open a project, select a box, open the Color picker, drag alpha to ~50%, pick white — canvas text is translucent; save + reopen — still translucent; export Typeset Pages — sidecar PNG matches the canvas.
</verification>

<success_criteria>
- The Color picker in the Typesetting Inspector exposes a transparency slider and a picked semi-transparent color reaches: the box's TextStyle.color as #AARRGGBB, the on-canvas overlay render, the baked/exported page, and the .mas file — with legacy opaque projects loading and rendering byte-compatibly.
- Effect (outline/glow/shadow) colors deliberately remain opaque with the scope decision recorded in a code comment.
- Full suite green; all new pins range-based/platform-robust per project test lessons.
</success_criteria>

<output>
Create `.planning/quick/260909-nj9-typesetting-add-transparency-alpha-slide/260909-nj9-SUMMARY.md` when done
</output>
