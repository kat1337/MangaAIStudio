---
phase: quick-260826-1by
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/gui/main_window.py
  - tests/test_gui_project.py
autonomous: true
requirements: [QUICK-FIX-CROSS-SESSION-PLANE-BLEED]
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Replacing a session (Open Folder / Open Image / Open Project / .mas open) can never persist the outgoing canvas planes into the incoming session's ImageFile list — no wrong-dims blob can be created by a page switch after any session swap.
    - A corrupt or wrong-dims packed plane blob degrades to "no plane" (loguru warning, slot skipped) instead of raising inside on_page_selected / _display_page_state — text boxes still restore.
    - Full suite green under the pinned interpreter C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe.
  artifacts:
    - manga_ai_studio/gui/main_window.py with None-resetting session swaps + dims-guarded persistence + guarded restores
    - tests/test_gui_project.py with three new regression tests (bleed, dims guard, restore backstop)
  key_links:
    - _last_page_index reset ↔ on_page_selected Step 1 outgoing write gate
    - canvas plane dims ↔ outgoing ImageFile.current_image shape at persistence time
    - unpack_binary blob length ↔ declared page dims at restore time
---

<objective>
Fix cross-session plane bleed: on_page_selected persists outgoing canvas planes into the WRONG page's ImageFile after a project/folder re-open, creating wrong-dims packed blobs whose restore raises repeated ValueError at main_window.py:2057 and kills text-box restoration.

Code-verified root cause chain (planner-validated):
1. `_set_pages` (main_window.py:1877-1885) rebuilds `self.image_files` and calls `on_page_selected(first.path)` WITHOUT resetting `_last_page_index` — the stale index passes the `0 <= idx < len(new_list)` bounds check whenever it lands inside the new list, so Step 1 packs the OLD page's planes (old dims) into the NEW list. (`_load_project_session`:2974 and `_load_single_page_mas`:3010 DO reset to 0, but to a valid index while the canvas may briefly hold prior-session state on seam paths; `None` is the safe sentinel.)
2. Cascade amplifier: when Step 4's `unpack_binary` raises (lines ~2047/2052/2057), `on_page_selected` aborts BEFORE line 2139 flips `self._last_page_index = self._current_page_index()` — the frozen index makes EVERY subsequent switch repeat both the wrong-slot write and the crash, and boxes never restore ("text boxes dead").

Purpose: user-visible data corruption class (wrong-page mask blobs) plus an unusable GUI until restart.
Output: guarded main_window.py + three regression tests in tests/test_gui_project.py.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@manga_ai_studio/gui/main_window.py
@manga_ai_studio/gui/canvas.py
@manga_ai_studio/core/mask_planes.py
@tests/test_gui_project.py
@AGENTS.md

Key facts established by planner code-trace (do not re-derive):
- on_page_selected Step 1 persist block: lines ~1929-1959 (composite mask write at 1942, plane packs at 1951-1959); Step 4 restore block: lines ~2038-2069 (three unguarded unpack_binary at 2047/2052/2057).
- _display_page_state restore block: lines ~2822-2855 (three unguarded unpack_binary at 2833/2838/2843).
- ALL `_last_page_index` consumers are None-tolerant: line 1290 (gating_idx), 1933 (Step 1), 2304 (_snapshot_current_page), 6733/6804 (export/bake). Setting it to None anywhere is safe.
- `unpack_binary(packed, h, w)` (core/mask_planes.py:58) raises ValueError when len(packed) != ceil(h*w/8).
- `set_image_from_numpy` does NOT touch planes/composite; only explicit `set_planes(...)` wipes them.
- Test fixtures: tests/test_gui_project.py helpers `_make_pages`, `_make_window`, `_seed_mask_content`, `_isolate_settings`; run tests with the PINNED interpreter from AGENTS.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session-swap hygiene — reset _last_page_index on every image_files swap</name>
  <files>manga_ai_studio/gui/main_window.py</files>
  <action>
Three edits, all in main_window.py:

1. `_set_pages` (the genuinely unguarded swap): immediately AFTER the `self.image_files = [...]` rebuild (line 1877) and BEFORE `self.file_table.set_pages(...)`, insert `self._last_page_index = None`. This kills the contamination path where the explicitly-called `on_page_selected(first.path)` (line 1885) persists old-canvas planes into the fresh list at the stale index. Update the method docstring with a one-line note that the reset prevents cross-session outgoing-state bleed (D-11 seam rule).

2. `_load_project_session`: change `self._last_page_index = 0` (line 2974) to `self._last_page_index = None`.

3. `_load_single_page_mas`: change `self._last_page_index = 0` (line 3010) to `self._last_page_index = None`.

Rationale for None over 0 in the loaders: all consumers are None-guarded (verified at lines 1290/1933/2304/6733/6804), and None means the first post-load navigation cannot write anything — correct, because at that moment the canvas holds exactly what `_display_page_state` just restored FROM the same ImageFile, so there is nothing to persist. Add a brief comment at each reset citing the cross-session bleed defect.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py -x -q</automated>
  </verify>
  <done>Every site that assigns self.image_files is preceded-or-followed by _last_page_index = None within the same swap block; existing gui_project suite passes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Dimension guard on outgoing persistence + restore backstop on unpack</name>
  <files>manga_ai_studio/gui/main_window.py</files>
  <action>
Defense-in-depth, two parts in main_window.py:

Part A — dims guard in on_page_selected Step 1 (~1929-1959): before ANY write into `imf_out` (the composite mask at line 1942 AND the three plane packs at 1951-1959), derive the outgoing page dims from `imf_out.current_image.shape[:2]` when `current_image is not None`, and the canvas plane dims from `canvas._mask_manual.height()/width()` when non-null (skip the guard when either side is unknown — preserve current behavior for legacy no-image slots). On MISMATCH: log a warning via loguru (include outgoing index, page dims, canvas dims) and skip ALL of Step 1's writes for that navigation (mask + auto/manual/erase planes + the Step-1b boxes snapshot at lines ~1971-1976, which shares the same stale-index failure mode). Do NOT raise — this is contamination prevention, not validation of trusted input.

Apply the identical guard inside `_snapshot_current_page` (~2303-2325) and `_flush_current_canvas_mask_to_data_model` (~7141+), which duplicate the same pack pattern — extract one small private helper (e.g. `_plane_dims_match(imf, canvas)` returning bool) rather than triplicating logic.

Part B — restore backstop: wrap EACH of the six unpack_binary call sites in try/except ValueError: on_page_selected (~2047 manual, ~2052 erase, ~2057 auto) and _display_page_state (~2833, ~2838, ~2843). On ValueError log a loguru warning naming the slot and both lengths, treat the plane as None, and continue. In _display_page_state this means the subsequent set_planes receives whatever planes parsed successfully — boxes/text restoration below MUST still run (that is the point: a corrupt plane degrades to "no plane", never kills the rest of the display path).

Do NOT touch the other unpack sites (lines ~2348-2354, ~3350, ~3678) — they operate on batch-derived data validated elsewhere; scope discipline.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py tests/test_gui_batch.py tests/test_box_persistence.py -x -q</automated>
  </verify>
  <done>A wrong-dims packed blob in any plane slot can no longer raise out of on_page_selected or _display_page_state; persistence writes nothing when canvas dims disagree with the outgoing page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression tests — bleed, dims guard, restore backstop</name>
  <files>tests/test_gui_project.py</files>
  <behavior>
    - Test 1 (cross-session bleed regression): window opened on folder A (2 pages, size 60); navigate to page 2 (click via file_table.file_clicked.emit or select_path + on_page_selected), seed real mask content (_seed_mask_content pattern); then replace the session with folder B pages of DIFFERENT size (e.g. 90px) by calling _load_folder/_set_pages seam directly. Assert: window._last_page_index is None immediately after the swap, and for every ImageFile in the new list, any populated plane slot satisfies len(blob) == ceil(h*w/8) for THAT page's dims (i.e. no foreign-length blob landed anywhere).
    - Test 2 (dims guard unit): craft the bad state directly — window with a loaded page, manually set window._last_page_index to a stale index pointing at a DIFFERENT-sized ImageFile, put oversized mask content on canvas, trigger on_page_selected on some page. Assert the stale-target ImageFile's mask/plane slots are UNCHANGED (write skipped) and navigation otherwise completed (canvas shows the incoming image).
    - Test 3 (restore backstop): build/load a session whose current ImageFile has current_image of (h,w) but auto_mask = pack_binary of a DIFFERENT-dims array (e.g. np.zeros((40,40),np.uint8) packed, page 60x60) and one valid box in .boxes; call window._display_page_state(imf). Assert: no exception propagates, canvas has_boxes() reports the box, and the corrupt auto plane contributed nothing (loguru warning asserted via caplog or the runner's logging fixture if the suite has one — otherwise assert behavior only).
    All tests follow the existing qtbot/tmp_path/monkeypatch helper conventions in this file (reuse _make_window, _make_pages, _seed_mask_content; isolate QSettings where dialogs/settings are touched).
  </behavior>
  <action>
Write the failing versions of the three tests FIRST (they should fail against the pre-fix code: Test 1 fails on the stale-index write, Test 2 fails on slots mutated, Test 3 fails with ValueError), confirm RED, then re-run GREEN after Tasks 1-2. Name them descriptively, e.g. test_cross_session_swap_does_not_bleed_planes_into_new_list, test_outgoing_persistence_dims_guard_skips_mismatched_write, test_corrupt_plane_blob_restores_degraded_with_boxes_intact. Keep assertions behavioral (slot lengths, is None, has_boxes) — no private-string greps beyond the documented attributes.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_gui_project.py -q && & "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <done>Three new tests pass; full suite green (baseline 1159 passed) under the pinned interpreter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted .mas blobs → canvas restore | packed plane bytes read from disk/in-memory ImageFile are unpacked against page dims |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q1B-01 | Tampering | on_page_selected Step 1 outgoing persistence | high | mitigate | dims guard: never pack canvas planes into an ImageFile whose page dims differ |
| T-Q1B-02 | Tampering | unpack_binary restore sites (on_page_selected, _display_page_state) | high | mitigate | try/except ValueError per plane → degrade to None + loguru warning |
| T-Q1B-03 | Denial of Service | repeated Qt slot crash loop (frozen _last_page_index) | high | mitigate | None-reset on session swaps breaks the write-crash cycle; backstop removes the crash itself |
</threat_model>

<verification>
- Pinned-interpreter run: full suite green (baseline 1159 passed + 3 new).
- Grep check: every `self.image_files =` assignment site in main_window.py sits in a swap block that resets `_last_page_index = None`.
- No unpack_binary restore call in the two display paths remains unguarded.
</verification>

<success_criteria>
Reopening a project (or opening any folder/image/project over a live session) can no longer produce wrong-dims packed blobs, and a corrupt blob degrades to "no plane" with text boxes intact instead of crashing the page-switch slot repeatedly.
</success_criteria>

<output>
Create .planning/quick/260826-1by-fix-cross-session-plane-bleed-on-page-se/260826-1by-SUMMARY.md when done
</output>
