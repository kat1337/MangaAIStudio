---
phase: quick
plan: 260822-347
subsystem: text-rendering
tags: [text-style, typesetting, defaults, overlay]
requires: []
provides:
  - "Default TextStyle = black (#000000) fill, outline disabled (keys intact)"
affects:
  - "New boxes render black/no-outline on both the canvas overlay and the bake/export path"
tech-stack:
  added: []
  patterns:
    - "Rendering-behavior pixel probes pin former-default colors explicitly instead of inheriting app defaults"
key-files:
  created: []
  modified:
    - manga_ai_studio/core/text_style.py
    - manga_ai_studio/gui/text_renderer.py
    - tests/test_core/test_text_style.py
    - tests/test_gui_boxes.py
    - tests/test_core/test_typeset_effects.py
    - tests/test_core/test_typeset_bake.py
    - tests/test_gui_export.py
    - tests/test_gui_inspector_styling.py
decisions:
  - "DEFAULT_OUTLINE keeps color #0b0b0e / width_px 2.0 with only enabled flipped to False — re-enabling the outline in the Inspector restores the exact previous stroke look"
  - "Effect-probe test files (typeset_effects/typeset_bake/gui_export) PIN color=#e8e8ea / outline enabled explicitly rather than switching their pixel math to black — tests stay independent of future default changes"
  - "Inspector WR-01 effect-toggle test now checks the outline ON (real change under the disabled default) instead of OFF"
metrics:
  duration: ~18 min
  completed: 2026-08-22
status: complete
---

# Quick Task 260822-347: Change default text overlay style from white+outline to black+no-outline Summary

**One-liner:** Default TextStyle flipped to black (#000000) opaque fill with the outline disabled (stroke color/width retained as enable-time defaults), mirrored in the renderer's invalid-color fallbacks, across both the live overlay and typeset bake/export surfaces.

## What Was Built

1. **Task 1 — Defaults flip (`85bf63b`):** `text_style.py` DEFAULT_COLOR `#e8e8ea` → `#000000`; DEFAULT_OUTLINE `enabled` True → False (all dict keys intact); module docstring updated. `text_renderer.py` both `_valid_color(style.color, "#e8e8ea")` invalid-color fallbacks → `"#000000"` (overlay layout path + horizontal/vertical paint pass). Glow/shadow defaults and line-888 glow/shadow color path untouched.
2. **Task 2 — Tests updated (`57e40ce`):** default-equality assertions moved to the new defaults (test_text_style, test_gui_boxes); rendering-behavior pixel probes pin `color="#e8e8ea"` / explicit outline dicts so they no longer inherit changed defaults (test_typeset_effects, test_typeset_bake, test_gui_export).
3. **Task 3 — Full suite (`a39df29` fix):** one additional old-default encoding found in `tests/test_gui_inspector_styling.py::test_style_commit_signal_fires` — the WR-01 real-change toggle was uncheck-OFF, a no-op now that the default ships disabled; switched to check-ON with matching emission expectation. Full suite re-run green.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Inspector styling test encoded the old enabled-outline default**
- **Found during:** Task 3 full-suite run
- **Issue:** `test_style_commit_signal_fires` unchecked the outline checkbox expecting an emission; under the new disabled default the checkbox already starts unchecked, so the toggle became a no-op (WR-01 correctly emits nothing).
- **Fix:** Toggle ON instead (a real change) and assert the `{"enabled": True, ...}` emission; no-op repeat updated symmetrically. Test intent (one emission per real change, zero on no-op cycles) preserved.
- **Files modified:** tests/test_gui_inspector_styling.py
- **Commit:** a39df29

Note: `test_run_ocr_selected_real_model_end_to_end` failed once during an isolated test_gui_boxes run but passes standalone and in the final full suite — pre-existing flake (real OCR-model e2e), unrelated to this change; not touched.

## Verification

- Task 1 probe: `TextStyle()` → color `#000000`, outline `{enabled: False, color: #0b0b0e, width_px: 2.0}`, glow color `#e8e8ea` unchanged ✓
- Targeted files: 44 passed ✓
- Full suite: **1023 passed, 0 failed** (strict superset of the Phase 5 baseline 552) ✓

## Threat Flags

None — `from_dict` V5 boundary unchanged (T-Q347-01 accepted as planned).

## Known Stubs

None.

## Self-Check: PASSED

- Commits verified in git log: 85bf63b, 57e40ce, a39df29
- All modified files present on disk; full suite green post-change
