---
phase: quick
plan: 260822-347
type: execute
wave: 1
depends_on: []
files_modified:
  - manga_ai_studio/core/text_style.py
  - manga_ai_studio/gui/text_renderer.py
  - tests/test_core/test_text_style.py
  - tests/test_core/test_typeset_effects.py
  - tests/test_core/test_typeset_bake.py
  - tests/test_gui_boxes.py
  - tests/test_gui_export.py
autonomous: true
requirements: [QUICK-default-black-text]
must_haves:
  truths:
    - A freshly created box (no saved style) renders its overlay/typeset text in BLACK (#000000) with NO outline by default.
    - The outline API is intact — enabling outline in the Inspector still works exactly as before.
    - Glow/shadow defaults are untouched.
  artifacts:
    - manga_ai_studio/core/text_style.py (DEFAULT_COLOR = "#000000", DEFAULT_OUTLINE enabled = False)
    - Updated test assertions matching the new defaults
  key_links:
    - TextStyle() defaults -> text_renderer fill/outline application (both the QGraphicsTextItem overlay path and the bake/export path read style.color + style.outline)
---

<objective>
Change the default text overlay style from near-white text (#e8e8ea) with an enabled 2px dark outline to BLACK text (#000000) with the outline DISABLED — while keeping the outline effect API fully functional.

Purpose: User request — "currently the text is white with a black outline by default, i want black without an outline by default".

Output: New defaults in `text_style.py`, consistency fallback updated in `text_renderer.py`, and all tests that encode the OLD defaults updated so the suite stays green.

The single source of truth for defaults is `manga_ai_studio/core/text_style.py` (module constants DEFAULT_COLOR / DEFAULT_OUTLINE). Both render paths (the BoxItem QGraphicsTextItem overlay via QTextCharFormat.setTextOutline+setForeground, and the typeset bake/export) consume `TextStyle.color` + `TextStyle.outline["enabled"]`, so changing the constants changes both surfaces at once. No other production code needs to change except one mirrored fallback literal in `text_renderer.py`.
</objective>

<execution_context>
@C:/Users/Stella/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Stella/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@manga_ai_studio/core/text_style.py
@manga_ai_studio/gui/text_renderer.py

# Tests encoding the old defaults
@tests/test_core/test_text_style.py
@tests/test_core/test_typeset_effects.py
@tests/test_core/test_typeset_bake.py
@tests/test_gui_boxes.py
@tests/test_gui_export.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the default style constants to black text, outline off</name>
  <files>manga_ai_studio/core/text_style.py, manga_ai_studio/gui/text_renderer.py</files>
  <action>
In `manga_ai_studio/core/text_style.py`:

1. Change `DEFAULT_COLOR = "#e8e8ea"` to `DEFAULT_COLOR = "#000000"` (line ~33).
2. Change `DEFAULT_OUTLINE` (line ~34) to `{"enabled": False, "color": "#0b0b0e", "width_px": 2.0}` — flip ONLY `enabled` to False; keep the `color` and `width_px` values so re-enabling the outline in the Inspector restores the exact previous stroke look. Do NOT remove any dict key (the outline API must stay intact).
3. Leave DEFAULT_GLOW, DEFAULT_SHADOW, DEFAULT_FONT_FAMILY untouched.
4. Update the module docstring paragraph describing the defaults ("#e8e8ea opaque fill, 2px #0b0b0e outline") to describe the new default look: black opaque fill, outline disabled by default (stroke color/width retained as the enable-time defaults).

In `manga_ai_studio/gui/text_renderer.py`:

5. The two invalid-color fallbacks mirror the default fill: change `_valid_color(style.color, "#e8e8ea")` at line ~329 AND line ~740 to `_valid_color(style.color, "#000000")`. These are fallbacks for an unparseable style.color and should agree with the new default. Do NOT touch line ~888 (`effect.get("color", "#ffffff")`) — that is the glow/shadow color path, out of scope.

Do NOT touch `inspector_panel.py::_EFFECT_DEFAULT_COLORS` — those are per-effect stroke/glow/shadow color fallbacks, not the text fill.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -c "import sys; sys.path.insert(0, '.'); from manga_ai_studio.core.text_style import TextStyle; s = TextStyle(); assert s.color == '#000000' and s.outline['enabled'] is False and s.outline['width_px'] == 2.0 and s.glow['color'] == '#e8e8ea', (s.color, s.outline); print('defaults OK')"</automated>
  </verify>
  <done>TextStyle() constructs with color "#000000" and outline {"enabled": False, "color": "#0b0b0e", "width_px": 2.0}; glow/shadow defaults byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: Update every test that encodes the old defaults</name>
  <files>tests/test_core/test_text_style.py, tests/test_gui_boxes.py, tests/test_core/test_typeset_effects.py, tests/test_core/test_typeset_bake.py, tests/test_gui_export.py</files>
  <action>
Update assertions/styles that hard-code the OLD default look. Known sites (grep may reveal more — sweep each file for `#e8e8ea`, `(232, 232, 234)`, `outline["enabled"] is True`, and "default" comments about the fill):

1. `tests/test_core/test_text_style.py`:
   - The defaults test (~line 36): `s.color == "#e8e8ea"` -> `"#000000"`; the outline-dict assertion -> `{"enabled": False, "color": "#0b0b0e", "width_px": 2.0}`; docstring wording updated. The glow assertion at ~line 48 (`glow == {..., "color": "#e8e8ea", ...}`) STAYS — glow keeps its color.
   - Lines ~152 and ~221 assert an invalid/list color falls back to the default fill: update the expected value to `"#000000"` (keep the test intent: non-str falls back to default).

2. `tests/test_gui_boxes.py` ~lines 1143-1146 (`test_text_overlay_renders_through_shared_renderer`): `assert TextStyle().outline["enabled"] is True` -> `is False`; keep the width_px == 2.0 assertion (width default unchanged); refresh the docstring/comment that calls 2px the active default.

3. `tests/test_core/test_typeset_effects.py`: this file PROBES the old default look (module constant `_FILL = (232, 232, 234)` at line ~43, and `test_effects_outline_ring_horizontal` at ~line 123 renders with bare `TextStyle(font_size_px=48.0, auto_fit=False)` expecting the default outline ON). These tests verify EFFECT RENDERING, not app defaults — pin the old look explicitly rather than rewriting their pixel math: give the rendering styles an explicit `color="#e8e8ea"` and, for the ring test, an explicit `outline={"enabled": True, "color": "#0b0b0e", "width_px": 2.0}` (via the existing `replace(...)` pattern where convenient). Update the `_FILL`/`_OUTLINE` constant comments to say these are pinned probe colors, not app defaults. Other tests in the file that only count fill pixels can either pin color explicitly or switch `_FILL` to `(0, 0, 0)` — pick ONE approach per file and apply consistently; prefer explicit pinning so the tests remain independent of future default changes.

4. `tests/test_core/test_typeset_bake.py`: `_PIXEL_STYLE` (line ~38) does not set `color`, so it inherits the default — add `color="#e8e8ea"` explicitly OR change `_FILL_RGB` to `(0, 0, 0)` and the line-~98 comment accordingly. Prefer adding the explicit color so `_FILL_RGB = (232, 232, 234)` stays truthful; adjust the comment to note it is pinned, not the app default. Note the page fixture is solid (30,40,50) — black glyphs would still satisfy the "page changed inside box" checks, but keep exact-match assertions consistent with whichever choice you make.

5. `tests/test_gui_export.py` (`test_typeset_export_action`, style built ~line 295, fill asserted ~line 323 as `[232, 232, 234]` "the default opaque fill"): add `color="#e8e8ea"` to the explicitly-built style and reword the comment to "pinned fill (former default)" — do not leave it inheriting a changed default.

Run the five files after editing; if any OTHER test fails due to the new defaults (e.g., something asserting `TextStyle()` equality against a literal dict elsewhere), update that assertion to the NEW defaults — but never weaken a test that verifies rendering behavior; pin inputs instead.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest tests/test_core/test_text_style.py tests/test_core/test_typeset_effects.py tests/test_core/test_typeset_bake.py tests/test_gui_export.py -q</automated>
  </verify>
  <done>All four targeted test files pass; no assertion still encodes near-white as the DEFAULT fill or an enabled outline as the DEFAULT state.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression run</name>
  <files></files>
  <action>
Run the complete test suite to catch any indirect dependency on the old defaults (GUI boxes/inspector/batch tests construct boxes whose overlays render through the changed path; baseline at Phase 5 close was 552 passed). Investigate any failure: if it asserts the OLD default appearance, update the assertion per Task 2 discipline (pin inputs for rendering-behavior tests; update expected values for pure default-equality tests). Fix nothing unrelated.
  </action>
  <verify>
    <automated>& "C:\Users\Stella\.pyenv\pyenv-win\versions\3.14.2\python.exe" -m pytest -q</automated>
  </verify>
  <done>Full suite passes with 0 failures; suite remains a strict superset of the pre-change baseline (552 passed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project-file -> TextStyle.from_dict | Untrusted persisted style dicts cross into defaults coercion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q347-01 | Tampering | TextStyle.from_dict V5 boundary | low | accept | No new input surface — from_dict clamps/coerces unchanged; defaults swap does not alter the boundary |
</threat_model>

<verification>
- `TextStyle()` defaults: color "#000000", outline disabled (keys intact).
- Renderer invalid-color fallback agrees with the default (#000000) at both call sites.
- Targeted test files pass; full suite green.
- Outline re-enable path unaffected (Inspector styling tests still pass unchanged).
</verification>

<success_criteria>
Default text overlay renders black with no outline on BOTH surfaces (live canvas overlay + typeset bake/export); outline API intact; full test suite passes.
</success_criteria>

<output>
Create `.planning/quick/260822-347-change-default-text-overlay-style-from-w/260822-347-SUMMARY.md` when done
</output>
