# Placeholder package satisfying the ``import panelcleaner.data`` reference at
# panelcleaner/image_ops.py:15 (vendored near-verbatim from PanelCleaner). The
# upstream package bundles the LiberationSans-Regular.ttf font asset used by
# image_ops' visualize_* debug helpers (Phase 2 batch analytics). Phase 1
# interactive inpainting does not exercise those helpers, so an empty package is
# sufficient for the import to resolve; the font asset itself is deferred until
# a batch/analytics plan needs it.
