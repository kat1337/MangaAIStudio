[pytest]
qt_api = pyside6
testpaths = tests
addopts = -ra
markers =
    unit: lightweight unit test (no Qt, no model weights)
    gui: GUI test requiring PySide6 + a display (pytest-qt)
filterwarnings =
